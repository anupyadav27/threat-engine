version: '1.0'
provider: azure
service: servicebus
discovery:
- discovery_id: azure.servicebus.list
  calls:
  - action: list
    save_as: list_response
  emit:
    items_for: '{{ list_response.value }}'
    as: item
    item:
      id: '{{ item.id }}'
      name: '{{ item.name }}'
      type: '{{ item.type }}'
checks:
- rule_id: azure.servicebus.basic.compliance.check
  for_each: azure.servicebus.list
  conditions:
    var: item.id
    op: exists
