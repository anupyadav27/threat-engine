container_instances:
  version: '1.0'
  provider: oci
  service: container_instances
  scope: regional
  discovery: []
  checks:
  - check_id: oci.container_instances.container_instance.container_instance_env_no_plaintext_secrets
    title: 'OCI CONTAINER_INSTANCES Container_instance: Container Instance Env No Plaintext Secrets'
    severity: high
    for_each: list_container_instances
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.container_instances.container_instance.container_instance_no_privileged_containers
    title: 'OCI CONTAINER_INSTANCES Container_instance: Container Instance No Privileged Containers'
    severity: high
    for_each: list_container_instances
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.container_instances.container_instance.container_instance_read_only_root_filesystem
    title: 'OCI CONTAINER_INSTANCES Container_instance: Container Instance Read Only Root Filesystem'
    severity: critical
    for_each: list_container_instances
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.container_instances.container_instance.task_definitions_logging_enabled
    title: 'OCI CONTAINER_INSTANCES Container_instance: Task Definitions Logging Enabled'
    severity: medium
    for_each: list_container_instances
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.container_instances.container_instance.task_definitions_logging_block_mode
    title: 'OCI CONTAINER_INSTANCES Container_instance: Task Definitions Logging Block Mode'
    severity: medium
    for_each: list_container_instances
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
