version: '1.0'
provider: aws
service: cur
services:
  client: cur
  module: boto3.client
discovery:
- discovery_id: aws.cur.describe_report_definitions
  calls:
  - action: describe_report_definitions
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.ReportDefinitions }}'
- discovery_id: aws.cur.list_tags_for_resource
  for_each: aws.cur.describe_report_definitions
  calls:
  - action: list_tags_for_resource
    save_as: response
    on_error: continue
    params:
      reportDefinitionReportName: '{{ item.reportDefinitionReportName }}'
  emit:
    as: item
    items_for: '{{ response.Tags }}'
checks: []
