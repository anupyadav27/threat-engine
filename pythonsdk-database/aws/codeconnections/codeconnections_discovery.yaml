version: '1.0'
provider: aws
service: codeconnections
services:
  client: codeconnections
  module: boto3.client
discovery:
- discovery_id: aws.codeconnections.list_repository_links
  calls:
  - action: list_repository_links
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.RepositoryLinks }}'
- discovery_id: aws.codeconnections.list_hosts
  calls:
  - action: list_hosts
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.Hosts }}'
- discovery_id: aws.codeconnections.list_connections
  calls:
  - action: list_connections
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.Connections }}'
- discovery_id: aws.codeconnections.get_host
  for_each: aws.codeconnections.list_hosts
  calls:
  - action: get_host
    save_as: response
    on_error: continue
    params:
      connectionHostArn: '{{ item.connectionHostArn }}'
  emit:
    as: item
    items_for: '{{ response.VpcConfiguration }}'
- discovery_id: aws.codeconnections.list_tags_for_resource
  calls:
  - action: list_tags_for_resource
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.Tags }}'
- discovery_id: aws.codeconnections.get_connection
  for_each: aws.codeconnections.list_repository_links
  calls:
  - action: get_connection
    save_as: response
    on_error: continue
    params:
      connectionConnectionArn: '{{ item.connectionConnectionArn }}'
  emit:
    as: item
    items_for: '{{ response.Connection }}'
checks: []
