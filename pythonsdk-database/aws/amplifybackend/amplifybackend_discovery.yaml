version: '1.0'
provider: aws
service: amplifybackend
services:
  client: amplifybackend
  module: boto3.client
discovery:
- discovery_id: aws.amplifybackend.list_s3_buckets
  calls:
  - action: list_s3_buckets
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.Buckets }}'
checks: []
