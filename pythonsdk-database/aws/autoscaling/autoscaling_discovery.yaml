version: '1.0'
provider: aws
service: autoscaling
services:
  client: autoscaling
  module: boto3.client
discovery:
- discovery_id: aws.autoscaling.describe_auto_scaling_groups
  calls:
  - action: describe_auto_scaling_groups
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.AutoScalingGroups }}'
- discovery_id: aws.autoscaling.describe_launch_configurations
  calls:
  - action: describe_launch_configurations
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.LaunchConfigurations }}'
- discovery_id: aws.autoscaling.describe_policies
  calls:
  - action: describe_policies
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.ScalingPolicies }}'
- discovery_id: aws.autoscaling.describe_scaling_activities
  calls:
  - action: describe_scaling_activities
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.Activities }}'
- discovery_id: aws.autoscaling.describe_scheduled_actions
  calls:
  - action: describe_scheduled_actions
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.ScheduledUpdateGroupActions }}'
- discovery_id: aws.autoscaling.describe_auto_scaling_instances
  calls:
  - action: describe_auto_scaling_instances
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.AutoScalingInstances }}'
- discovery_id: aws.autoscaling.describe_tags
  calls:
  - action: describe_tags
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.Tags }}'
- discovery_id: aws.autoscaling.describe_notification_configurations
  calls:
  - action: describe_notification_configurations
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.NotificationConfigurations }}'
- discovery_id: aws.autoscaling.describe_metric_collection_types
  calls:
  - action: describe_metric_collection_types
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.Metrics }}'
- discovery_id: aws.autoscaling.describe_scaling_process_types
  calls:
  - action: describe_scaling_process_types
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.Processes }}'
- discovery_id: aws.autoscaling.describe_lifecycle_hook_types
  calls:
  - action: describe_lifecycle_hook_types
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.LifecycleHookTypes }}'
- discovery_id: aws.autoscaling.describe_auto_scaling_notification_types
  calls:
  - action: describe_auto_scaling_notification_types
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.AutoScalingNotificationTypes }}'
- discovery_id: aws.autoscaling.describe_account_limits
  calls:
  - action: describe_account_limits
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.MaxNumberOfAutoScalingGroups }}'
- discovery_id: aws.autoscaling.describe_termination_policy_types
  calls:
  - action: describe_termination_policy_types
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.TerminationPolicyTypes }}'
- discovery_id: aws.autoscaling.describe_instance_refreshes
  for_each: aws.autoscaling.describe_auto_scaling_groups
  calls:
  - action: describe_instance_refreshes
    save_as: response
    on_error: continue
    params:
      autoScalingGroupAutoScalingGroupName: '{{ item.autoScalingGroupAutoScalingGroupName
        }}'
  emit:
    as: item
    items_for: '{{ response.InstanceRefreshes }}'
- discovery_id: aws.autoscaling.describe_lifecycle_hooks
  for_each: aws.autoscaling.describe_auto_scaling_groups
  calls:
  - action: describe_lifecycle_hooks
    save_as: response
    on_error: continue
    params:
      autoScalingGroupAutoScalingGroupName: '{{ item.autoScalingGroupAutoScalingGroupName
        }}'
  emit:
    as: item
    items_for: '{{ response.LifecycleHooks }}'
- discovery_id: aws.autoscaling.describe_traffic_sources
  for_each: aws.autoscaling.describe_auto_scaling_groups
  calls:
  - action: describe_traffic_sources
    save_as: response
    on_error: continue
    params:
      autoScalingGroupAutoScalingGroupName: '{{ item.autoScalingGroupAutoScalingGroupName
        }}'
  emit:
    as: item
    items_for: '{{ response.TrafficSources }}'
- discovery_id: aws.autoscaling.get_predictive_scaling_forecast
  for_each: aws.autoscaling.describe_scaling_activities
  calls:
  - action: get_predictive_scaling_forecast
    save_as: response
    on_error: continue
    params:
      activityEndTime: '{{ item.activityEndTime }}'
      activityStartTime: '{{ item.activityStartTime }}'
      autoScalingGroupAutoScalingGroupName: '{{ item.autoScalingGroupAutoScalingGroupName
        }}'
      PolicyName: '{{ item.policyName }}'
  emit:
    as: item
    items_for: '{{ response.LoadForecast }}'
- discovery_id: aws.autoscaling.describe_load_balancer_target_groups
  for_each: aws.autoscaling.describe_auto_scaling_groups
  calls:
  - action: describe_load_balancer_target_groups
    save_as: response
    on_error: continue
    params:
      autoScalingGroupAutoScalingGroupName: '{{ item.autoScalingGroupAutoScalingGroupName
        }}'
  emit:
    as: item
    items_for: '{{ response.LoadBalancerTargetGroups }}'
- discovery_id: aws.autoscaling.describe_load_balancers
  for_each: aws.autoscaling.describe_auto_scaling_groups
  calls:
  - action: describe_load_balancers
    save_as: response
    on_error: continue
    params:
      autoScalingGroupAutoScalingGroupName: '{{ item.autoScalingGroupAutoScalingGroupName
        }}'
  emit:
    as: item
    items_for: '{{ response.LoadBalancers }}'
checks: []
