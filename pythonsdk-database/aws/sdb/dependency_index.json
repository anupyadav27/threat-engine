{
  "service": "sdb",
  "read_only": true,
  "roots": [
    {
      "op": "ListDomains",
      "produces": ["sdb.domain_names", "sdb.next_token"]
    }
  ],
  "entity_paths": {
    "sdb.domain_names": [
      {
        "operations": ["ListDomains"],
        "produces": {
          "ListDomains": ["sdb.domain_names"]
        },
        "consumes": {
          "ListDomains": []
        },
        "external_inputs": []
      }
    ],
    "sdb.next_token": [
      {
        "operations": ["ListDomains"],
        "produces": {
          "ListDomains": ["sdb.next_token"]
        },
        "consumes": {
          "ListDomains": []
        },
        "external_inputs": []
      }
    ]
  }
}