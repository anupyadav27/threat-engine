version: '1.0'
provider: azure
service: netappfiles
discovery:
- discovery_id: azure.netappfiles.list
  calls:
  - action: list
    save_as: list_response
  emit:
    items_for: '{{ list_response.value }}'
    as: item
    item:
      id: '{{ item.id }}'
      name: '{{ item.name }}'
      type: '{{ item.type }}'
      storage_account_resource_id: '{{ item.storage_account_resource_id }}'
      location: '{{ item.location }}'
      restore_reference: '{{ item.restore_reference }}'
      creation_time: '{{ item.creation_time }}'
      deletion_time: '{{ item.deletion_time }}'
checks:
- rule_id: azure.netappfiles.encryption.key.source.cmk.check
  for_each: azure.netappfiles.list
  conditions:
    var: item.id
    op: exists
