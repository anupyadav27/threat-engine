# EFS Service - Check Metadata
# This file contains titles, descriptions, and severity for all checks
# The main YAML file only contains the logic

version: '1.0'
service: efs
checks:
  aws.efs.not_publicly_accessible.not_publicly_accessible_configured:
    title: 'EFS not publicly accessible: Not Publicly Accessible Configured'
    severity: high
  aws.efs.user.identity_and_root_directory_enforced:
    title: 'EFS user: Identity And Root Directory Enforced'
    severity: medium
  aws.efs.filesystem.encryption_at_rest_enabled:
    title: 'EFS filesystem: Encryption At Rest Enabled'
    severity: high
  aws.efs.have.backup_enabled:
    title: 'EFS have: Backup Enabled'
    severity: medium
  aws.efs.filesystem.snapshots_enabled:
    title: 'EFS filesystem: Snapshots Enabled'
    severity: medium
  aws.efs.resource.backup_enabled:
    title: 'EFS resource: Backup Enabled'
    severity: medium
  aws.efs.resource.multi_az_enabled:
    title: 'EFS resource: Multi Az Enabled'
    severity: medium
  aws.efs.resource.encryption_at_rest_enabled:
    title: 'EFS resource: Encryption At Rest Enabled'
    severity: high
  aws.efs.encryption.at_rest_enabled:
    title: 'EFS encryption: At Rest Enabled'
    severity: medium
  aws.efs.filesystem.kms_key_policy_least_privilege:
    title: 'EFS filesystem: Kms Key Policy Least Privilege'
    severity: high
  aws.efs.filesystem.private_network_only_configured:
    title: 'EFS filesystem: Private Network Only Configured'
    severity: high
