# CLOUDFORMATION Service - Check Metadata
# This file contains titles, descriptions, and severity for all checks
# The main YAML file only contains the logic

version: '1.0'
service: cloudformation
checks:
  aws.cloudformation.stack.cloudformation_approvals_required_for_changes:
    title: 'CLOUDFORMATION stack: Cloudformation Approvals Required For Changes'
    severity: medium
  aws.cloudformation.stack.cloudformation_storage_encrypted_and_private:
    title: 'CLOUDFORMATION stack: Cloudformation Storage Encrypted And Private'
    severity: high
  aws.cloudformation.stack.change_audit_logging_enabled:
    title: RTO and RPO are defined and monitored
    severity: low
