version: '1.0'
provider: aws
service: eip
checks:
- rule_id: aws.eip.address.address_enabled
  for_each: aws.eip.describe_addresses
  conditions:
    var: item.AllocationId
    op: exists
    value: null
