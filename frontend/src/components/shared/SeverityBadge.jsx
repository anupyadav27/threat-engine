'use client';

/**
 * SeverityBadge — re-exports from SecurityBadges for backwards compatibility.
 * All new code should import directly from SecurityBadges.jsx.
 */
export { SeverityBadge as default } from './SecurityBadges';
