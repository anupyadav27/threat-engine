# ============================================================
# Discovery YAML — kinesis-video-media (AWS)
# Generated: 2026-02-21T04:56:58Z
# Actions use boto3 SDK snake_case method names
# ============================================================
version: '1.0'
provider: aws
service: kinesis-video-media
services:
  client: kinesis-video-media
  module: boto3.client
discovery:
  # ── Dependent read operations (require identifiers) ──
  # ── GetMedia ──
  - discovery_id: aws.kinesis-video-media.get_media
    calls:
      - action: get_media
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        ContentType: '{{ response.ContentType }}'
        Payload: '{{ response.Payload }}'
