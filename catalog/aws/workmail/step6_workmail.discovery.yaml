# ============================================================
# Discovery YAML — workmail (AWS)
# Generated: 2026-02-21T04:57:00Z
# Actions use boto3 SDK snake_case method names
# ============================================================
version: '1.0'
provider: aws
service: workmail
services:
  client: workmail
  module: boto3.client
discovery:
  # ── ListOrganizations ──
  - discovery_id: aws.workmail.list_organizations
    calls:
      - action: list_organizations
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.OrganizationSummaries }}'
      item:
        Name: '{{ item.Name }}'
        RoleArn: '{{ item.RoleArn }}'
        State: '{{ item.State }}'
        ARN: '{{ item.ARN }}'
        Action: '{{ item.Action }}'
        Actions: '{{ item.Actions }}'
        Alias: '{{ item.Alias }}'
        AuthenticationMode: '{{ item.AuthenticationMode }}'
        CompletedDate: '{{ item.CompletedDate }}'
        DateCreated: '{{ item.DateCreated }}'
        DateModified: '{{ item.DateModified }}'
        DefaultDomain: '{{ item.DefaultDomain }}'
        DefaultMailDomain: '{{ item.DefaultMailDomain }}'
        Description: '{{ item.Description }}'
        DeviceModels: '{{ item.DeviceModels }}'
        DeviceOperatingSystems: '{{ item.DeviceOperatingSystems }}'
        DeviceTypes: '{{ item.DeviceTypes }}'
        DeviceUserAgents: '{{ item.DeviceUserAgents }}'
        DirectoryId: '{{ item.DirectoryId }}'
        DirectoryType: '{{ item.DirectoryType }}'
        DkimVerificationStatus: '{{ item.DkimVerificationStatus }}'
        Effect: '{{ item.Effect }}'
        Email: '{{ item.Email }}'
        EndTime: '{{ item.EndTime }}'
        ErrorMessage: '{{ item.ErrorMessage }}'
        EstimatedProgress: '{{ item.EstimatedProgress }}'
        EwsProvider: '{{ item.EwsProvider }}'
        FolderConfigurations: '{{ item.FolderConfigurations }}'
        Hostname: '{{ item.Hostname }}'
        IdentityCenterConfiguration: '{{ item.IdentityCenterConfiguration }}'
        IdentityProviderIdentityStoreId: '{{ item.IdentityProviderIdentityStoreId }}'
        ImpersonationRoleIds: '{{ item.ImpersonationRoleIds }}'
        ImpersonationRuleId: '{{ item.ImpersonationRuleId }}'
        InteroperabilityEnabled: '{{ item.InteroperabilityEnabled }}'
        IpRanges: '{{ item.IpRanges }}'
        IsDefault: '{{ item.IsDefault }}'
        IsTestDomain: '{{ item.IsTestDomain }}'
        LambdaProvider: '{{ item.LambdaProvider }}'
        LogGroupArn: '{{ item.LogGroupArn }}'
        MatchedRules: '{{ item.MatchedRules }}'
        MigrationAdmin: '{{ item.MigrationAdmin }}'
        MobileDeviceAccessRuleId: '{{ item.MobileDeviceAccessRuleId }}'
        NotActions: '{{ item.NotActions }}'
        NotDeviceModels: '{{ item.NotDeviceModels }}'
        NotDeviceOperatingSystems: '{{ item.NotDeviceOperatingSystems }}'
        NotDeviceTypes: '{{ item.NotDeviceTypes }}'
        NotDeviceUserAgents: '{{ item.NotDeviceUserAgents }}'
        NotImpersonationRoleIds: '{{ item.NotImpersonationRoleIds }}'
        NotIpRanges: '{{ item.NotIpRanges }}'
        NotTargetUsers: '{{ item.NotTargetUsers }}'
        NotUserIds: '{{ item.NotUserIds }}'
        OrganizationId: '{{ item.OrganizationId }}'
        OwnershipVerificationStatus: '{{ item.OwnershipVerificationStatus }}'
        Period: '{{ item.Period }}'
        PersonalAccessTokenConfiguration: '{{ item.PersonalAccessTokenConfiguration }}'
        ProviderType: '{{ item.ProviderType }}'
        S3BucketName: '{{ item.S3BucketName }}'
        S3Path: '{{ item.S3Path }}'
        StartTime: '{{ item.StartTime }}'
        TargetUsers: '{{ item.TargetUsers }}'
        Type: '{{ item.Type }}'
        UserIds: '{{ item.UserIds }}'
        Value: '{{ item.Value }}'
  # ── Dependent read operations (require identifiers) ──
  # ── DescribeEmailMonitoringConfiguration ──
  - discovery_id: aws.workmail.describe_email_monitoring_configuration
    for_each: aws.workmail.list_organizations
    calls:
      - action: describe_email_monitoring_configuration
        params:
          OrganizationId: '{{ item.OrganizationId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.OrganizationSummaries }}'
      item:
        RoleArn: '{{ item.RoleArn }}'
        LogGroupArn: '{{ item.LogGroupArn }}'
  # ── DescribeEntity ──
  - discovery_id: aws.workmail.describe_entity
    for_each: aws.workmail.list_organizations
    calls:
      - action: describe_entity
        params:
          OrganizationId: '{{ item.OrganizationId }}'
          Email: '{{ item.Email }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        Name: '{{ response.Name }}'
        EntityId: '{{ response.EntityId }}'
        Type: '{{ response.Type }}'
  # ── DescribeGroup ──
  - discovery_id: aws.workmail.describe_group
    for_each: aws.workmail.list_organizations
    calls:
      - action: describe_group
        params:
          OrganizationId: '{{ item.OrganizationId }}'
          GroupId: '{{ item.GroupId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        Name: '{{ response.Name }}'
        State: '{{ response.State }}'
        DisabledDate: '{{ response.DisabledDate }}'
        Email: '{{ response.Email }}'
        EnabledDate: '{{ response.EnabledDate }}'
        GroupId: '{{ response.GroupId }}'
        HiddenFromGlobalAddressList: '{{ response.HiddenFromGlobalAddressList }}'
  # ── DescribeIdentityProviderConfiguration ──
  - discovery_id: aws.workmail.describe_identity_provider_configuration
    for_each: aws.workmail.list_organizations
    calls:
      - action: describe_identity_provider_configuration
        params:
          OrganizationId: '{{ item.OrganizationId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.IdentityCenterConfiguration }}'
      item:
        Status: '{{ item.Status }}'
        ApplicationArn: '{{ item.ApplicationArn }}'
        AuthenticationMode: '{{ item.AuthenticationMode }}'
        Enabled: '{{ item.Enabled }}'
        IdentityCenterConfiguration: '{{ item.IdentityCenterConfiguration }}'
        InstanceArn: '{{ item.InstanceArn }}'
        PersonalAccessTokenConfiguration: '{{ item.PersonalAccessTokenConfiguration }}'
  # ── DescribeInboundDmarcSettings ──
  - discovery_id: aws.workmail.describe_inbound_dmarc_settings
    for_each: aws.workmail.list_organizations
    calls:
      - action: describe_inbound_dmarc_settings
        params:
          OrganizationId: '{{ item.OrganizationId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Enforced }}'
      item:
        Enforced: '{{ item.Enforced }}'
  # ── DescribeMailboxExportJob ──
  - discovery_id: aws.workmail.describe_mailbox_export_job
    for_each: aws.workmail.list_organizations
    calls:
      - action: describe_mailbox_export_job
        params:
          JobId: '{{ item.JobId }}'
          OrganizationId: '{{ item.OrganizationId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        RoleArn: '{{ response.RoleArn }}'
        State: '{{ response.State }}'
        Description: '{{ response.Description }}'
        EndTime: '{{ response.EndTime }}'
        EntityId: '{{ response.EntityId }}'
        ErrorInfo: '{{ response.ErrorInfo }}'
        EstimatedProgress: '{{ response.EstimatedProgress }}'
        KmsKeyArn: '{{ response.KmsKeyArn }}'
        S3BucketName: '{{ response.S3BucketName }}'
        S3Path: '{{ response.S3Path }}'
        S3Prefix: '{{ response.S3Prefix }}'
        StartTime: '{{ response.StartTime }}'
  # ── DescribeOrganization ──
  - discovery_id: aws.workmail.describe_organization
    for_each: aws.workmail.list_organizations
    calls:
      - action: describe_organization
        params:
          OrganizationId: '{{ item.OrganizationId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.OrganizationSummaries }}'
      item:
        ARN: '{{ item.ARN }}'
        CompletedDate: '{{ item.CompletedDate }}'
        DirectoryId: '{{ item.DirectoryId }}'
        DirectoryType: '{{ item.DirectoryType }}'
        InteroperabilityEnabled: '{{ item.InteroperabilityEnabled }}'
        MigrationAdmin: '{{ item.MigrationAdmin }}'
  # ── DescribeResource ──
  - discovery_id: aws.workmail.describe_resource
    for_each: aws.workmail.list_organizations
    calls:
      - action: describe_resource
        params:
          OrganizationId: '{{ item.OrganizationId }}'
          ResourceId: '{{ item.ResourceId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.BookingOptions }}'
      item:
        AutoAcceptRequests: '{{ item.AutoAcceptRequests }}'
        AutoDeclineConflictingRequests: '{{ item.AutoDeclineConflictingRequests }}'
        AutoDeclineRecurringRequests: '{{ item.AutoDeclineRecurringRequests }}'
  # ── DescribeUser ──
  - discovery_id: aws.workmail.describe_user
    for_each: aws.workmail.list_organizations
    calls:
      - action: describe_user
        params:
          OrganizationId: '{{ item.OrganizationId }}'
          UserId: '{{ item.UserId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Users }}'
      item:
        City: '{{ item.City }}'
        Company: '{{ item.Company }}'
        Country: '{{ item.Country }}'
        Department: '{{ item.Department }}'
        Initials: '{{ item.Initials }}'
        Office: '{{ item.Office }}'
        Street: '{{ item.Street }}'
        Telephone: '{{ item.Telephone }}'
  # ── GetAccessControlEffect ──
  - discovery_id: aws.workmail.get_access_control_effect
    for_each: aws.workmail.list_organizations
    calls:
      - action: get_access_control_effect
        params:
          OrganizationId: '{{ item.OrganizationId }}'
          IpAddress: '{{ item.IpAddress }}'
          Action: '{{ item.Action }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.MatchedRules }}'
      item:
        MatchedRules: '{{ item.MatchedRules }}'
  # ── GetDefaultRetentionPolicy ──
  - discovery_id: aws.workmail.get_default_retention_policy
    for_each: aws.workmail.list_organizations
    calls:
      - action: get_default_retention_policy
        params:
          OrganizationId: '{{ item.OrganizationId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.FolderConfigurations }}'
      item:
        Name: '{{ item.Name }}'
        Action: '{{ item.Action }}'
        FolderConfigurations: '{{ item.FolderConfigurations }}'
        Period: '{{ item.Period }}'
  # ── GetImpersonationRole ──
  - discovery_id: aws.workmail.get_impersonation_role
    for_each: aws.workmail.list_organizations
    calls:
      - action: get_impersonation_role
        params:
          OrganizationId: '{{ item.OrganizationId }}'
          ImpersonationRoleId: '{{ item.ImpersonationRoleId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Rules }}'
      item:
        Name: '{{ item.Name }}'
        ImpersonationRuleId: '{{ item.ImpersonationRuleId }}'
        NotTargetUsers: '{{ item.NotTargetUsers }}'
        Rules: '{{ item.Rules }}'
        TargetUsers: '{{ item.TargetUsers }}'
  # ── GetImpersonationRoleEffect ──
  - discovery_id: aws.workmail.get_impersonation_role_effect
    for_each: aws.workmail.list_organizations
    calls:
      - action: get_impersonation_role_effect
        params:
          OrganizationId: '{{ item.OrganizationId }}'
          ImpersonationRoleId: '{{ item.ImpersonationRoleId }}'
          TargetUser: '{{ item.TargetUser }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.MatchedRules }}'
      item:
        Name: '{{ item.Name }}'
        ImpersonationRuleId: '{{ item.ImpersonationRuleId }}'
  # ── GetMailDomain ──
  - discovery_id: aws.workmail.get_mail_domain
    for_each: aws.workmail.list_organizations
    calls:
      - action: get_mail_domain
        params:
          OrganizationId: '{{ item.OrganizationId }}'
          DomainName: '{{ item.DomainName }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Records }}'
      item:
        DkimVerificationStatus: '{{ item.DkimVerificationStatus }}'
        Hostname: '{{ item.Hostname }}'
        IsDefault: '{{ item.IsDefault }}'
        IsTestDomain: '{{ item.IsTestDomain }}'
        OwnershipVerificationStatus: '{{ item.OwnershipVerificationStatus }}'
        Records: '{{ item.Records }}'
        Type: '{{ item.Type }}'
        Value: '{{ item.Value }}'
  # ── GetMailboxDetails ──
  - discovery_id: aws.workmail.get_mailbox_details
    calls:
      - action: get_mailbox_details
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        MailboxQuota: '{{ response.MailboxQuota }}'
        MailboxSize: '{{ response.MailboxSize }}'
  # ── GetMobileDeviceAccessEffect ──
  - discovery_id: aws.workmail.get_mobile_device_access_effect
    for_each: aws.workmail.list_organizations
    calls:
      - action: get_mobile_device_access_effect
        params:
          OrganizationId: '{{ item.OrganizationId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.MatchedRules }}'
      item:
        Name: '{{ item.Name }}'
        MatchedRules: '{{ item.MatchedRules }}'
        MobileDeviceAccessRuleId: '{{ item.MobileDeviceAccessRuleId }}'
  # ── GetMobileDeviceAccessOverride ──
  - discovery_id: aws.workmail.get_mobile_device_access_override
    for_each: aws.workmail.list_organizations
    calls:
      - action: get_mobile_device_access_override
        params:
          OrganizationId: '{{ item.OrganizationId }}'
          UserId: '{{ item.UserId }}'
          DeviceId: '{{ item.DeviceId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        DateCreated: '{{ response.DateCreated }}'
        DateModified: '{{ response.DateModified }}'
        Description: '{{ response.Description }}'
        DeviceId: '{{ response.DeviceId }}'
        Effect: '{{ response.Effect }}'
        UserId: '{{ response.UserId }}'
  # ── GetPersonalAccessTokenMetadata ──
  - discovery_id: aws.workmail.get_personal_access_token_metadata
    for_each: aws.workmail.list_organizations
    calls:
      - action: get_personal_access_token_metadata
        params:
          OrganizationId: '{{ item.OrganizationId }}'
          PersonalAccessTokenId: '{{ item.PersonalAccessTokenId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Scopes }}'
  # ── ListAccessControlRules ──
  - discovery_id: aws.workmail.list_access_control_rules
    for_each: aws.workmail.list_organizations
    calls:
      - action: list_access_control_rules
        params:
          OrganizationId: '{{ item.OrganizationId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Rules }}'
      item:
        Name: '{{ item.Name }}'
        Actions: '{{ item.Actions }}'
        DateCreated: '{{ item.DateCreated }}'
        DateModified: '{{ item.DateModified }}'
        Description: '{{ item.Description }}'
        Effect: '{{ item.Effect }}'
        ImpersonationRoleIds: '{{ item.ImpersonationRoleIds }}'
        IpRanges: '{{ item.IpRanges }}'
        NotActions: '{{ item.NotActions }}'
        NotImpersonationRoleIds: '{{ item.NotImpersonationRoleIds }}'
        NotIpRanges: '{{ item.NotIpRanges }}'
        NotUserIds: '{{ item.NotUserIds }}'
        UserIds: '{{ item.UserIds }}'
  # ── ListAliases ──
  - discovery_id: aws.workmail.list_aliases
    calls:
      - action: list_aliases
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Aliases }}'
  # ── ListAvailabilityConfigurations ──
  - discovery_id: aws.workmail.list_availability_configurations
    for_each: aws.workmail.list_organizations
    calls:
      - action: list_availability_configurations
        params:
          OrganizationId: '{{ item.OrganizationId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.AvailabilityConfigurations }}'
      item:
        DateCreated: '{{ item.DateCreated }}'
        DateModified: '{{ item.DateModified }}'
        DkimVerificationStatus: '{{ item.DkimVerificationStatus }}'
        DomainName: '{{ item.DomainName }}'
        EwsProvider: '{{ item.EwsProvider }}'
        Hostname: '{{ item.Hostname }}'
        IsDefault: '{{ item.IsDefault }}'
        IsTestDomain: '{{ item.IsTestDomain }}'
        LambdaProvider: '{{ item.LambdaProvider }}'
        OwnershipVerificationStatus: '{{ item.OwnershipVerificationStatus }}'
        ProviderType: '{{ item.ProviderType }}'
        Type: '{{ item.Type }}'
        Value: '{{ item.Value }}'
  # ── ListGroupMembers ──
  - discovery_id: aws.workmail.list_group_members
    for_each: aws.workmail.list_organizations
    calls:
      - action: list_group_members
        params:
          OrganizationId: '{{ item.OrganizationId }}'
          GroupId: '{{ item.GroupId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Members }}'
      item:
        Id: '{{ item.Id }}'
        Name: '{{ item.Name }}'
        State: '{{ item.State }}'
        DisabledDate: '{{ item.DisabledDate }}'
        EnabledDate: '{{ item.EnabledDate }}'
        Type: '{{ item.Type }}'
  # ── ListGroups ──
  - discovery_id: aws.workmail.list_groups
    for_each: aws.workmail.list_organizations
    calls:
      - action: list_groups
        params:
          OrganizationId: '{{ item.OrganizationId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Groups }}'
      item:
        Id: '{{ item.Id }}'
        Name: '{{ item.Name }}'
        State: '{{ item.State }}'
        DisabledDate: '{{ item.DisabledDate }}'
        Email: '{{ item.Email }}'
        EnabledDate: '{{ item.EnabledDate }}'
  # ── ListGroupsForEntity ──
  - discovery_id: aws.workmail.list_groups_for_entity
    for_each: aws.workmail.list_organizations
    calls:
      - action: list_groups_for_entity
        params:
          OrganizationId: '{{ item.OrganizationId }}'
          EntityId: '{{ item.EntityId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Groups }}'
      item:
        GroupId: '{{ item.GroupId }}'
        GroupName: '{{ item.GroupName }}'
  # ── ListImpersonationRoles ──
  - discovery_id: aws.workmail.list_impersonation_roles
    for_each: aws.workmail.list_organizations
    calls:
      - action: list_impersonation_roles
        params:
          OrganizationId: '{{ item.OrganizationId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Roles }}'
      item:
        Name: '{{ item.Name }}'
        DateCreated: '{{ item.DateCreated }}'
        DateModified: '{{ item.DateModified }}'
        ImpersonationRoleId: '{{ item.ImpersonationRoleId }}'
        Rules: '{{ item.Rules }}'
        Type: '{{ item.Type }}'
  # ── ListMailDomains ──
  - discovery_id: aws.workmail.list_mail_domains
    for_each: aws.workmail.list_organizations
    calls:
      - action: list_mail_domains
        params:
          OrganizationId: '{{ item.OrganizationId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.MailDomains }}'
      item:
        DefaultDomain: '{{ item.DefaultDomain }}'
        DkimVerificationStatus: '{{ item.DkimVerificationStatus }}'
        DomainName: '{{ item.DomainName }}'
        IsDefault: '{{ item.IsDefault }}'
        IsTestDomain: '{{ item.IsTestDomain }}'
        OwnershipVerificationStatus: '{{ item.OwnershipVerificationStatus }}'
        Records: '{{ item.Records }}'
  # ── ListMailboxExportJobs ──
  - discovery_id: aws.workmail.list_mailbox_export_jobs
    for_each: aws.workmail.list_organizations
    calls:
      - action: list_mailbox_export_jobs
        params:
          OrganizationId: '{{ item.OrganizationId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Jobs }}'
      item:
        State: '{{ item.State }}'
        Description: '{{ item.Description }}'
        EndTime: '{{ item.EndTime }}'
        EntityId: '{{ item.EntityId }}'
        EstimatedProgress: '{{ item.EstimatedProgress }}'
        JobId: '{{ item.JobId }}'
        S3BucketName: '{{ item.S3BucketName }}'
        S3Path: '{{ item.S3Path }}'
        StartTime: '{{ item.StartTime }}'
  # ── ListMailboxPermissions ──
  - discovery_id: aws.workmail.list_mailbox_permissions
    for_each: aws.workmail.list_organizations
    calls:
      - action: list_mailbox_permissions
        params:
          OrganizationId: '{{ item.OrganizationId }}'
          EntityId: '{{ item.EntityId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Permissions }}'
      item:
        GranteeId: '{{ item.GranteeId }}'
        GranteeType: '{{ item.GranteeType }}'
        PermissionValues: '{{ item.PermissionValues }}'
  # ── ListMobileDeviceAccessOverrides ──
  - discovery_id: aws.workmail.list_mobile_device_access_overrides
    for_each: aws.workmail.list_organizations
    calls:
      - action: list_mobile_device_access_overrides
        params:
          OrganizationId: '{{ item.OrganizationId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Overrides }}'
      item:
        DateCreated: '{{ item.DateCreated }}'
        DateModified: '{{ item.DateModified }}'
        Description: '{{ item.Description }}'
        DeviceId: '{{ item.DeviceId }}'
        Effect: '{{ item.Effect }}'
        UserId: '{{ item.UserId }}'
  # ── ListMobileDeviceAccessRules ──
  - discovery_id: aws.workmail.list_mobile_device_access_rules
    for_each: aws.workmail.list_organizations
    calls:
      - action: list_mobile_device_access_rules
        params:
          OrganizationId: '{{ item.OrganizationId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Rules }}'
      item:
        Name: '{{ item.Name }}'
        DateCreated: '{{ item.DateCreated }}'
        DateModified: '{{ item.DateModified }}'
        Description: '{{ item.Description }}'
        DeviceModels: '{{ item.DeviceModels }}'
        DeviceOperatingSystems: '{{ item.DeviceOperatingSystems }}'
        DeviceTypes: '{{ item.DeviceTypes }}'
        DeviceUserAgents: '{{ item.DeviceUserAgents }}'
        Effect: '{{ item.Effect }}'
        MobileDeviceAccessRuleId: '{{ item.MobileDeviceAccessRuleId }}'
        NotDeviceModels: '{{ item.NotDeviceModels }}'
        NotDeviceOperatingSystems: '{{ item.NotDeviceOperatingSystems }}'
        NotDeviceTypes: '{{ item.NotDeviceTypes }}'
        NotDeviceUserAgents: '{{ item.NotDeviceUserAgents }}'
  # ── ListPersonalAccessTokens ──
  - discovery_id: aws.workmail.list_personal_access_tokens
    for_each: aws.workmail.list_organizations
    calls:
      - action: list_personal_access_tokens
        params:
          OrganizationId: '{{ item.OrganizationId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.PersonalAccessTokenSummaries }}'
      item:
        Name: '{{ item.Name }}'
        DateCreated: '{{ item.DateCreated }}'
        DateLastUsed: '{{ item.DateLastUsed }}'
        ExpiresTime: '{{ item.ExpiresTime }}'
        PersonalAccessTokenId: '{{ item.PersonalAccessTokenId }}'
        Scopes: '{{ item.Scopes }}'
        UserId: '{{ item.UserId }}'
  # ── ListResourceDelegates ──
  - discovery_id: aws.workmail.list_resource_delegates
    for_each: aws.workmail.list_organizations
    calls:
      - action: list_resource_delegates
        params:
          OrganizationId: '{{ item.OrganizationId }}'
          ResourceId: '{{ item.ResourceId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Delegates }}'
      item:
        Id: '{{ item.Id }}'
        Type: '{{ item.Type }}'
  # ── ListResources ──
  - discovery_id: aws.workmail.list_resources
    for_each: aws.workmail.list_organizations
    calls:
      - action: list_resources
        params:
          OrganizationId: '{{ item.OrganizationId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Resources }}'
      item:
        Id: '{{ item.Id }}'
        Name: '{{ item.Name }}'
        State: '{{ item.State }}'
        Description: '{{ item.Description }}'
        DisabledDate: '{{ item.DisabledDate }}'
        Email: '{{ item.Email }}'
        EnabledDate: '{{ item.EnabledDate }}'
        Type: '{{ item.Type }}'
  # ── ListTagsForResource ──
  - discovery_id: aws.workmail.list_tags_for_resource
    calls:
      - action: list_tags_for_resource
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Tags }}'
      item:
        Key: '{{ item.Key }}'
        Value: '{{ item.Value }}'
  # ── ListUsers ──
  - discovery_id: aws.workmail.list_users
    for_each: aws.workmail.list_organizations
    calls:
      - action: list_users
        params:
          OrganizationId: '{{ item.OrganizationId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Users }}'
      item:
        Id: '{{ item.Id }}'
        Name: '{{ item.Name }}'
        State: '{{ item.State }}'
        City: '{{ item.City }}'
        Company: '{{ item.Company }}'
        Country: '{{ item.Country }}'
        Department: '{{ item.Department }}'
        DisabledDate: '{{ item.DisabledDate }}'
        DisplayName: '{{ item.DisplayName }}'
        Email: '{{ item.Email }}'
        EnabledDate: '{{ item.EnabledDate }}'
        IdentityProviderIdentityStoreId: '{{ item.IdentityProviderIdentityStoreId }}'
        IdentityProviderUserId: '{{ item.IdentityProviderUserId }}'
        Initials: '{{ item.Initials }}'
        Office: '{{ item.Office }}'
        Street: '{{ item.Street }}'
        Telephone: '{{ item.Telephone }}'
        UserRole: '{{ item.UserRole }}'
