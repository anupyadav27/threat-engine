# ============================================================
# Discovery YAML — kinesisanalyticsv2 (AWS)
# Generated: 2026-02-21T04:56:58Z
# Actions use boto3 SDK snake_case method names
# ============================================================
version: '1.0'
provider: aws
service: kinesisanalyticsv2
services:
  client: kinesisanalyticsv2
  module: boto3.client
discovery:
  # ── ListApplications ──
  - discovery_id: aws.kinesisanalyticsv2.list_applications
    calls:
      - action: list_applications
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.ApplicationSummaries }}'
      item:
        ApplicationARN: '{{ item.ApplicationARN }}'
        ApplicationDetail: '{{ item.ApplicationDetail }}'
        ApplicationMode: '{{ item.ApplicationMode }}'
        ApplicationName: '{{ item.ApplicationName }}'
        ApplicationOperationInfoDetails: '{{ item.ApplicationOperationInfoDetails }}'
        ApplicationStatus: '{{ item.ApplicationStatus }}'
        ApplicationVersionDetail: '{{ item.ApplicationVersionDetail }}'
        ApplicationVersionId: '{{ item.ApplicationVersionId }}'
        RuntimeEnvironment: '{{ item.RuntimeEnvironment }}'
        SnapshotDetails: '{{ item.SnapshotDetails }}'
  # ── Dependent read operations (require identifiers) ──
  # ── DescribeApplication ──
  - discovery_id: aws.kinesisanalyticsv2.describe_application
    for_each: aws.kinesisanalyticsv2.list_applications
    calls:
      - action: describe_application
        params:
          ApplicationName: '{{ item.ApplicationName }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.ApplicationSummaries }}'
      item:
        ApplicationARN: '{{ item.ApplicationARN }}'
        ApplicationConfigurationDescription: '{{ item.ApplicationConfigurationDescription }}'
        ApplicationDescription: '{{ item.ApplicationDescription }}'
        ApplicationDetail: '{{ item.ApplicationDetail }}'
        ApplicationMaintenanceConfigurationDescription: '{{ item.ApplicationMaintenanceConfigurationDescription }}'
        ApplicationMode: '{{ item.ApplicationMode }}'
        ApplicationName: '{{ item.ApplicationName }}'
        ApplicationStatus: '{{ item.ApplicationStatus }}'
        ApplicationVersionCreateTimestamp: '{{ item.ApplicationVersionCreateTimestamp }}'
        ApplicationVersionId: '{{ item.ApplicationVersionId }}'
        ApplicationVersionRolledBackFrom: '{{ item.ApplicationVersionRolledBackFrom }}'
        ApplicationVersionRolledBackTo: '{{ item.ApplicationVersionRolledBackTo }}'
        ApplicationVersionUpdatedFrom: '{{ item.ApplicationVersionUpdatedFrom }}'
        CloudWatchLoggingOptionDescriptions: '{{ item.CloudWatchLoggingOptionDescriptions }}'
        ConditionalToken: '{{ item.ConditionalToken }}'
        CreateTimestamp: '{{ item.CreateTimestamp }}'
        LastUpdateTimestamp: '{{ item.LastUpdateTimestamp }}'
        RuntimeEnvironment: '{{ item.RuntimeEnvironment }}'
        ServiceExecutionRole: '{{ item.ServiceExecutionRole }}'
  # ── DescribeApplicationOperation ──
  - discovery_id: aws.kinesisanalyticsv2.describe_application_operation
    for_each: aws.kinesisanalyticsv2.list_applications
    calls:
      - action: describe_application_operation
        params:
          ApplicationName: '{{ item.ApplicationName }}'
          OperationId: '{{ item.OperationId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.ApplicationSummaries }}'
      item:
        ApplicationOperationInfoDetails: '{{ item.ApplicationOperationInfoDetails }}'
        ApplicationVersionChangeDetails: '{{ item.ApplicationVersionChangeDetails }}'
        EndTime: '{{ item.EndTime }}'
        Operation: '{{ item.Operation }}'
        OperationFailureDetails: '{{ item.OperationFailureDetails }}'
        OperationStatus: '{{ item.OperationStatus }}'
        StartTime: '{{ item.StartTime }}'
  # ── DescribeApplicationSnapshot ──
  - discovery_id: aws.kinesisanalyticsv2.describe_application_snapshot
    for_each: aws.kinesisanalyticsv2.list_applications
    calls:
      - action: describe_application_snapshot
        params:
          ApplicationName: '{{ item.ApplicationName }}'
          SnapshotName: '{{ item.SnapshotName }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.SnapshotDetails }}'
      item:
        ApplicationEncryptionConfigurationDescription: '{{ item.ApplicationEncryptionConfigurationDescription }}'
        ApplicationVersionId: '{{ item.ApplicationVersionId }}'
        RuntimeEnvironment: '{{ item.RuntimeEnvironment }}'
        SnapshotCreationTimestamp: '{{ item.SnapshotCreationTimestamp }}'
        SnapshotDetails: '{{ item.SnapshotDetails }}'
        SnapshotName: '{{ item.SnapshotName }}'
        SnapshotStatus: '{{ item.SnapshotStatus }}'
  # ── DescribeApplicationVersion ──
  - discovery_id: aws.kinesisanalyticsv2.describe_application_version
    for_each: aws.kinesisanalyticsv2.list_applications
    calls:
      - action: describe_application_version
        params:
          ApplicationName: '{{ item.ApplicationName }}'
          ApplicationVersionId: '{{ item.ApplicationVersionId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.ApplicationSummaries }}'
      item:
        ApplicationARN: '{{ item.ApplicationARN }}'
        ApplicationConfigurationDescription: '{{ item.ApplicationConfigurationDescription }}'
        ApplicationDescription: '{{ item.ApplicationDescription }}'
        ApplicationMaintenanceConfigurationDescription: '{{ item.ApplicationMaintenanceConfigurationDescription }}'
        ApplicationMode: '{{ item.ApplicationMode }}'
        ApplicationName: '{{ item.ApplicationName }}'
        ApplicationStatus: '{{ item.ApplicationStatus }}'
        ApplicationVersionCreateTimestamp: '{{ item.ApplicationVersionCreateTimestamp }}'
        ApplicationVersionDetail: '{{ item.ApplicationVersionDetail }}'
        ApplicationVersionId: '{{ item.ApplicationVersionId }}'
        ApplicationVersionRolledBackFrom: '{{ item.ApplicationVersionRolledBackFrom }}'
        ApplicationVersionRolledBackTo: '{{ item.ApplicationVersionRolledBackTo }}'
        ApplicationVersionUpdatedFrom: '{{ item.ApplicationVersionUpdatedFrom }}'
        CloudWatchLoggingOptionDescriptions: '{{ item.CloudWatchLoggingOptionDescriptions }}'
        ConditionalToken: '{{ item.ConditionalToken }}'
        CreateTimestamp: '{{ item.CreateTimestamp }}'
        LastUpdateTimestamp: '{{ item.LastUpdateTimestamp }}'
        RuntimeEnvironment: '{{ item.RuntimeEnvironment }}'
        ServiceExecutionRole: '{{ item.ServiceExecutionRole }}'
  # ── ListApplicationOperations ──
  - discovery_id: aws.kinesisanalyticsv2.list_application_operations
    for_each: aws.kinesisanalyticsv2.list_applications
    calls:
      - action: list_application_operations
        params:
          ApplicationName: '{{ item.ApplicationName }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.ApplicationOperationInfoList }}'
      item:
        ApplicationOperationInfoDetails: '{{ item.ApplicationOperationInfoDetails }}'
        EndTime: '{{ item.EndTime }}'
        Operation: '{{ item.Operation }}'
        OperationId: '{{ item.OperationId }}'
        OperationStatus: '{{ item.OperationStatus }}'
        StartTime: '{{ item.StartTime }}'
  # ── ListApplicationSnapshots ──
  - discovery_id: aws.kinesisanalyticsv2.list_application_snapshots
    for_each: aws.kinesisanalyticsv2.list_applications
    calls:
      - action: list_application_snapshots
        params:
          ApplicationName: '{{ item.ApplicationName }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.SnapshotSummaries }}'
      item:
        ApplicationEncryptionConfigurationDescription: '{{ item.ApplicationEncryptionConfigurationDescription }}'
        ApplicationVersionId: '{{ item.ApplicationVersionId }}'
        RuntimeEnvironment: '{{ item.RuntimeEnvironment }}'
        SnapshotCreationTimestamp: '{{ item.SnapshotCreationTimestamp }}'
        SnapshotDetails: '{{ item.SnapshotDetails }}'
        SnapshotName: '{{ item.SnapshotName }}'
        SnapshotStatus: '{{ item.SnapshotStatus }}'
  # ── ListApplicationVersions ──
  - discovery_id: aws.kinesisanalyticsv2.list_application_versions
    for_each: aws.kinesisanalyticsv2.list_applications
    calls:
      - action: list_application_versions
        params:
          ApplicationName: '{{ item.ApplicationName }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.ApplicationVersionSummaries }}'
      item:
        ApplicationStatus: '{{ item.ApplicationStatus }}'
        ApplicationVersionDetail: '{{ item.ApplicationVersionDetail }}'
        ApplicationVersionId: '{{ item.ApplicationVersionId }}'
  # ── ListTagsForResource ──
  - discovery_id: aws.kinesisanalyticsv2.list_tags_for_resource
    calls:
      - action: list_tags_for_resource
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Tags }}'
      item:
        Key: '{{ item.Key }}'
        Value: '{{ item.Value }}'
