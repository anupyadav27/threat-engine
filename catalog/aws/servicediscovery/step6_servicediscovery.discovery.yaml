# ============================================================
# Discovery YAML — servicediscovery (AWS)
# Generated: 2026-02-21T04:56:59Z
# Actions use boto3 SDK snake_case method names
# ============================================================
version: '1.0'
provider: aws
service: servicediscovery
services:
  client: servicediscovery
  module: boto3.client
discovery:
  # ── ListNamespaces ──
  - discovery_id: aws.servicediscovery.list_namespaces
    calls:
      - action: list_namespaces
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Namespaces }}'
      item:
        Arn: '{{ item.Arn }}'
        Id: '{{ item.Id }}'
        Name: '{{ item.Name }}'
        CreateDate: '{{ item.CreateDate }}'
        Description: '{{ item.Description }}'
        Properties: '{{ item.Properties }}'
        ResourceOwner: '{{ item.ResourceOwner }}'
        ServiceCount: '{{ item.ServiceCount }}'
        Type: '{{ item.Type }}'
  # ── ListOperations ──
  - discovery_id: aws.servicediscovery.list_operations
    calls:
      - action: list_operations
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Operations }}'
      item:
        Id: '{{ item.Id }}'
        Status: '{{ item.Status }}'
        ErrorCode: '{{ item.ErrorCode }}'
        ErrorMessage: '{{ item.ErrorMessage }}'
        Operation: '{{ item.Operation }}'
        OwnerAccount: '{{ item.OwnerAccount }}'
        Targets: '{{ item.Targets }}'
        UpdateDate: '{{ item.UpdateDate }}'
  # ── ListServices ──
  - discovery_id: aws.servicediscovery.list_services
    calls:
      - action: list_services
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Services }}'
      item:
        Arn: '{{ item.Arn }}'
        Id: '{{ item.Id }}'
        Name: '{{ item.Name }}'
        CreateDate: '{{ item.CreateDate }}'
        CreatedByAccount: '{{ item.CreatedByAccount }}'
        Description: '{{ item.Description }}'
        DnsConfig: '{{ item.DnsConfig }}'
        HealthCheckConfig: '{{ item.HealthCheckConfig }}'
        HealthCheckCustomConfig: '{{ item.HealthCheckCustomConfig }}'
        InstanceCount: '{{ item.InstanceCount }}'
        ResourceOwner: '{{ item.ResourceOwner }}'
        Type: '{{ item.Type }}'
  # ── Dependent read operations (require identifiers) ──
  # ── GetInstance ──
  - discovery_id: aws.servicediscovery.get_instance
    calls:
      - action: get_instance
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Instance }}'
      item:
        Id: '{{ item.Id }}'
        Attributes: '{{ item.Attributes }}'
        CreatedByAccount: '{{ item.CreatedByAccount }}'
        CreatorRequestId: '{{ item.CreatorRequestId }}'
        Instance: '{{ item.Instance }}'
  # ── GetInstancesHealthStatus ──
  - discovery_id: aws.servicediscovery.get_instances_health_status
    calls:
      - action: get_instances_health_status
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        Status: '{{ response.Status }}'
  # ── GetNamespace ──
  - discovery_id: aws.servicediscovery.get_namespace
    for_each: aws.servicediscovery.list_namespaces
    calls:
      - action: get_namespace
        params:
          Id: '{{ item.Id }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Namespaces }}'
      item:
        Arn: '{{ item.Arn }}'
        Id: '{{ item.Id }}'
        Name: '{{ item.Name }}'
        CreateDate: '{{ item.CreateDate }}'
        CreatorRequestId: '{{ item.CreatorRequestId }}'
        Description: '{{ item.Description }}'
        Properties: '{{ item.Properties }}'
        ResourceOwner: '{{ item.ResourceOwner }}'
        ServiceCount: '{{ item.ServiceCount }}'
        Type: '{{ item.Type }}'
  # ── GetOperation ──
  - discovery_id: aws.servicediscovery.get_operation
    calls:
      - action: get_operation
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Namespaces }}'
      item:
        Id: '{{ item.Id }}'
        Status: '{{ item.Status }}'
        CreateDate: '{{ item.CreateDate }}'
        ErrorCode: '{{ item.ErrorCode }}'
        ErrorMessage: '{{ item.ErrorMessage }}'
        Operation: '{{ item.Operation }}'
        OwnerAccount: '{{ item.OwnerAccount }}'
        Targets: '{{ item.Targets }}'
        Type: '{{ item.Type }}'
        UpdateDate: '{{ item.UpdateDate }}'
  # ── GetService ──
  - discovery_id: aws.servicediscovery.get_service
    for_each: aws.servicediscovery.list_namespaces
    calls:
      - action: get_service
        params:
          Id: '{{ item.Id }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Namespaces }}'
      item:
        Arn: '{{ item.Arn }}'
        Id: '{{ item.Id }}'
        Name: '{{ item.Name }}'
        CreateDate: '{{ item.CreateDate }}'
        CreatedByAccount: '{{ item.CreatedByAccount }}'
        CreatorRequestId: '{{ item.CreatorRequestId }}'
        Description: '{{ item.Description }}'
        DnsConfig: '{{ item.DnsConfig }}'
        HealthCheckConfig: '{{ item.HealthCheckConfig }}'
        HealthCheckCustomConfig: '{{ item.HealthCheckCustomConfig }}'
        InstanceCount: '{{ item.InstanceCount }}'
        NamespaceId: '{{ item.NamespaceId }}'
        ResourceOwner: '{{ item.ResourceOwner }}'
        Type: '{{ item.Type }}'
  # ── GetServiceAttributes ──
  - discovery_id: aws.servicediscovery.get_service_attributes
    calls:
      - action: get_service_attributes
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Instance }}'
      item:
        Attributes: '{{ item.Attributes }}'
        ResourceOwner: '{{ item.ResourceOwner }}'
        ServiceArn: '{{ item.ServiceArn }}'
  # ── ListInstances ──
  - discovery_id: aws.servicediscovery.list_instances
    calls:
      - action: list_instances
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Instances }}'
      item:
        Id: '{{ item.Id }}'
        Attributes: '{{ item.Attributes }}'
        CreatedByAccount: '{{ item.CreatedByAccount }}'
        Instance: '{{ item.Instance }}'
  # ── ListTagsForResource ──
  - discovery_id: aws.servicediscovery.list_tags_for_resource
    calls:
      - action: list_tags_for_resource
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Tags }}'
      item:
        Key: '{{ item.Key }}'
        Value: '{{ item.Value }}'
