# ============================================================
# Discovery YAML — vpc-lattice (AWS)
# Generated: 2026-02-21T04:57:00Z
# Actions use boto3 SDK snake_case method names
# ============================================================
version: '1.0'
provider: aws
service: vpc-lattice
services:
  client: vpc-lattice
  module: boto3.client
discovery:
  # ── ListDomainVerifications ──
  - discovery_id: aws.vpc-lattice.list_domain_verifications
    calls:
      - action: list_domain_verifications
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        arn: '{{ item.arn }}'
        createdAt: '{{ item.createdAt }}'
        domainName: '{{ item.domainName }}'
        id: '{{ item.id }}'
        lastVerifiedTime: '{{ item.lastVerifiedTime }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
        txtMethodConfig: '{{ item.txtMethodConfig }}'
  # ── ListResourceConfigurations ──
  - discovery_id: aws.vpc-lattice.list_resource_configurations
    calls:
      - action: list_resource_configurations
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        Status: '{{ item.Status }}'
        Enabled: '{{ item.Enabled }}'
        amazonManaged: '{{ item.amazonManaged }}'
        arn: '{{ item.arn }}'
        createdAt: '{{ item.createdAt }}'
        customDomainName: '{{ item.customDomainName }}'
        domainVerificationId: '{{ item.domainVerificationId }}'
        groupDomain: '{{ item.groupDomain }}'
        id: '{{ item.id }}'
        lastUpdatedAt: '{{ item.lastUpdatedAt }}'
        name: '{{ item.name }}'
        resourceConfigurationGroupId: '{{ item.resourceConfigurationGroupId }}'
        resourceGatewayId: '{{ item.resourceGatewayId }}'
        status: '{{ item.status }}'
        type: '{{ item.type }}'
  # ── ListResourceGateways ──
  - discovery_id: aws.vpc-lattice.list_resource_gateways
    calls:
      - action: list_resource_gateways
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        arn: '{{ item.arn }}'
        createdAt: '{{ item.createdAt }}'
        id: '{{ item.id }}'
        ipAddressType: '{{ item.ipAddressType }}'
        ipv4AddressesPerEni: '{{ item.ipv4AddressesPerEni }}'
        lastUpdatedAt: '{{ item.lastUpdatedAt }}'
        name: '{{ item.name }}'
        securityGroupIds: '{{ item.securityGroupIds }}'
        status: '{{ item.status }}'
        subnetIds: '{{ item.subnetIds }}'
        vpcIdentifier: '{{ item.vpcIdentifier }}'
  # ── ListServiceNetworkResourceAssociations ──
  - discovery_id: aws.vpc-lattice.list_service_network_resource_associations
    calls:
      - action: list_service_network_resource_associations
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        arn: '{{ item.arn }}'
        createdAt: '{{ item.createdAt }}'
        createdBy: '{{ item.createdBy }}'
        dnsEntry: '{{ item.dnsEntry }}'
        failureCode: '{{ item.failureCode }}'
        id: '{{ item.id }}'
        isManagedAssociation: '{{ item.isManagedAssociation }}'
        privateDnsEnabled: '{{ item.privateDnsEnabled }}'
        privateDnsEntry: '{{ item.privateDnsEntry }}'
        resourceConfigurationArn: '{{ item.resourceConfigurationArn }}'
        resourceConfigurationId: '{{ item.resourceConfigurationId }}'
        resourceConfigurationName: '{{ item.resourceConfigurationName }}'
        serviceNetworkArn: '{{ item.serviceNetworkArn }}'
        serviceNetworkId: '{{ item.serviceNetworkId }}'
        serviceNetworkName: '{{ item.serviceNetworkName }}'
        status: '{{ item.status }}'
  # ── ListServiceNetworkServiceAssociations ──
  - discovery_id: aws.vpc-lattice.list_service_network_service_associations
    calls:
      - action: list_service_network_service_associations
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        arn: '{{ item.arn }}'
        createdAt: '{{ item.createdAt }}'
        createdBy: '{{ item.createdBy }}'
        customDomainName: '{{ item.customDomainName }}'
        dnsEntry: '{{ item.dnsEntry }}'
        id: '{{ item.id }}'
        serviceArn: '{{ item.serviceArn }}'
        serviceId: '{{ item.serviceId }}'
        serviceName: '{{ item.serviceName }}'
        serviceNetworkArn: '{{ item.serviceNetworkArn }}'
        serviceNetworkId: '{{ item.serviceNetworkId }}'
        serviceNetworkName: '{{ item.serviceNetworkName }}'
        status: '{{ item.status }}'
  # ── ListServiceNetworkVpcAssociations ──
  - discovery_id: aws.vpc-lattice.list_service_network_vpc_associations
    calls:
      - action: list_service_network_vpc_associations
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        arn: '{{ item.arn }}'
        createdAt: '{{ item.createdAt }}'
        createdBy: '{{ item.createdBy }}'
        dnsOptions: '{{ item.dnsOptions }}'
        id: '{{ item.id }}'
        lastUpdatedAt: '{{ item.lastUpdatedAt }}'
        privateDnsEnabled: '{{ item.privateDnsEnabled }}'
        serviceNetworkArn: '{{ item.serviceNetworkArn }}'
        serviceNetworkId: '{{ item.serviceNetworkId }}'
        serviceNetworkName: '{{ item.serviceNetworkName }}'
        status: '{{ item.status }}'
        vpcId: '{{ item.vpcId }}'
  # ── ListServiceNetworks ──
  - discovery_id: aws.vpc-lattice.list_service_networks
    calls:
      - action: list_service_networks
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        arn: '{{ item.arn }}'
        createdAt: '{{ item.createdAt }}'
        id: '{{ item.id }}'
        lastUpdatedAt: '{{ item.lastUpdatedAt }}'
        name: '{{ item.name }}'
        numberOfAssociatedResourceConfigurations: '{{ item.numberOfAssociatedResourceConfigurations }}'
        numberOfAssociatedServices: '{{ item.numberOfAssociatedServices }}'
        numberOfAssociatedVPCs: '{{ item.numberOfAssociatedVPCs }}'
  # ── ListServices ──
  - discovery_id: aws.vpc-lattice.list_services
    calls:
      - action: list_services
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        arn: '{{ item.arn }}'
        createdAt: '{{ item.createdAt }}'
        customDomainName: '{{ item.customDomainName }}'
        dnsEntry: '{{ item.dnsEntry }}'
        id: '{{ item.id }}'
        lastUpdatedAt: '{{ item.lastUpdatedAt }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
  # ── ListTargetGroups ──
  - discovery_id: aws.vpc-lattice.list_target_groups
    calls:
      - action: list_target_groups
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        arn: '{{ item.arn }}'
        createdAt: '{{ item.createdAt }}'
        id: '{{ item.id }}'
        ipAddressType: '{{ item.ipAddressType }}'
        lambdaEventStructureVersion: '{{ item.lambdaEventStructureVersion }}'
        lastUpdatedAt: '{{ item.lastUpdatedAt }}'
        name: '{{ item.name }}'
        port: '{{ item.port }}'
        protocol: '{{ item.protocol }}'
        serviceArns: '{{ item.serviceArns }}'
        status: '{{ item.status }}'
        type: '{{ item.type }}'
        vpcIdentifier: '{{ item.vpcIdentifier }}'
  # ── Dependent read operations (require identifiers) ──
  # ── GetAccessLogSubscription ──
  - discovery_id: aws.vpc-lattice.get_access_log_subscription
    calls:
      - action: get_access_log_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        arn: '{{ response.arn }}'
        createdAt: '{{ response.createdAt }}'
        destinationArn: '{{ response.destinationArn }}'
        id: '{{ response.id }}'
        lastUpdatedAt: '{{ response.lastUpdatedAt }}'
        resourceArn: '{{ response.resourceArn }}'
        resourceId: '{{ response.resourceId }}'
        serviceNetworkLogType: '{{ response.serviceNetworkLogType }}'
  # ── GetAuthPolicy ──
  - discovery_id: aws.vpc-lattice.get_auth_policy
    calls:
      - action: get_auth_policy
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.policy }}'
      item:
        Statement: '{{ item.Statement }}'
        Version: '{{ item.Version }}'
        policy: '{{ item.policy }}'
  # ── GetDomainVerification ──
  - discovery_id: aws.vpc-lattice.get_domain_verification
    calls:
      - action: get_domain_verification
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        name: '{{ item.name }}'
        value: '{{ item.value }}'
  # ── GetListener ──
  - discovery_id: aws.vpc-lattice.get_listener
    calls:
      - action: get_listener
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.defaultAction }}'
      item:
        fixedResponse: '{{ item.fixedResponse }}'
        forward: '{{ item.forward }}'
  # ── GetResourceConfiguration ──
  - discovery_id: aws.vpc-lattice.get_resource_configuration
    calls:
      - action: get_resource_configuration
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.portRanges }}'
  # ── GetResourceGateway ──
  - discovery_id: aws.vpc-lattice.get_resource_gateway
    calls:
      - action: get_resource_gateway
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.subnetIds }}'
  # ── GetResourcePolicy ──
  - discovery_id: aws.vpc-lattice.get_resource_policy
    calls:
      - action: get_resource_policy
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        policy: '{{ response.policy }}'
  # ── GetRule ──
  - discovery_id: aws.vpc-lattice.get_rule
    calls:
      - action: get_rule
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.match }}'
      item:
        httpMatch: '{{ item.httpMatch }}'
  # ── GetService ──
  - discovery_id: aws.vpc-lattice.get_service
    calls:
      - action: get_service
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        domainName: '{{ item.domainName }}'
        hostedZoneId: '{{ item.hostedZoneId }}'
  # ── GetServiceNetwork ──
  - discovery_id: aws.vpc-lattice.get_service_network
    calls:
      - action: get_service_network
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.sharingConfig }}'
      item:
        enabled: '{{ item.enabled }}'
  # ── GetServiceNetworkResourceAssociation ──
  - discovery_id: aws.vpc-lattice.get_service_network_resource_association
    calls:
      - action: get_service_network_resource_association
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        domainName: '{{ item.domainName }}'
        hostedZoneId: '{{ item.hostedZoneId }}'
  # ── GetServiceNetworkServiceAssociation ──
  - discovery_id: aws.vpc-lattice.get_service_network_service_association
    calls:
      - action: get_service_network_service_association
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        domainName: '{{ item.domainName }}'
        hostedZoneId: '{{ item.hostedZoneId }}'
  # ── GetServiceNetworkVpcAssociation ──
  - discovery_id: aws.vpc-lattice.get_service_network_vpc_association
    calls:
      - action: get_service_network_vpc_association
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.securityGroupIds }}'
  # ── GetTargetGroup ──
  - discovery_id: aws.vpc-lattice.get_target_group
    calls:
      - action: get_target_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.serviceArns }}'
  # ── ListAccessLogSubscriptions ──
  - discovery_id: aws.vpc-lattice.list_access_log_subscriptions
    calls:
      - action: list_access_log_subscriptions
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        arn: '{{ item.arn }}'
        createdAt: '{{ item.createdAt }}'
        destinationArn: '{{ item.destinationArn }}'
        id: '{{ item.id }}'
        lastUpdatedAt: '{{ item.lastUpdatedAt }}'
        resourceArn: '{{ item.resourceArn }}'
        resourceId: '{{ item.resourceId }}'
        serviceNetworkLogType: '{{ item.serviceNetworkLogType }}'
  # ── ListListeners ──
  - discovery_id: aws.vpc-lattice.list_listeners
    calls:
      - action: list_listeners
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        arn: '{{ item.arn }}'
        createdAt: '{{ item.createdAt }}'
        id: '{{ item.id }}'
        lastUpdatedAt: '{{ item.lastUpdatedAt }}'
        name: '{{ item.name }}'
        port: '{{ item.port }}'
        protocol: '{{ item.protocol }}'
  # ── ListResourceEndpointAssociations ──
  - discovery_id: aws.vpc-lattice.list_resource_endpoint_associations
    calls:
      - action: list_resource_endpoint_associations
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        arn: '{{ item.arn }}'
        createdAt: '{{ item.createdAt }}'
        createdBy: '{{ item.createdBy }}'
        id: '{{ item.id }}'
        resourceConfigurationArn: '{{ item.resourceConfigurationArn }}'
        resourceConfigurationId: '{{ item.resourceConfigurationId }}'
        resourceConfigurationName: '{{ item.resourceConfigurationName }}'
        vpcEndpointId: '{{ item.vpcEndpointId }}'
        vpcEndpointOwner: '{{ item.vpcEndpointOwner }}'
  # ── ListRules ──
  - discovery_id: aws.vpc-lattice.list_rules
    calls:
      - action: list_rules
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        arn: '{{ item.arn }}'
        createdAt: '{{ item.createdAt }}'
        id: '{{ item.id }}'
        isDefault: '{{ item.isDefault }}'
        lastUpdatedAt: '{{ item.lastUpdatedAt }}'
        name: '{{ item.name }}'
        priority: '{{ item.priority }}'
  # ── ListServiceNetworkVpcEndpointAssociations ──
  - discovery_id: aws.vpc-lattice.list_service_network_vpc_endpoint_associations
    calls:
      - action: list_service_network_vpc_endpoint_associations
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        createdAt: '{{ item.createdAt }}'
        id: '{{ item.id }}'
        serviceNetworkArn: '{{ item.serviceNetworkArn }}'
        state: '{{ item.state }}'
        vpcEndpointId: '{{ item.vpcEndpointId }}'
        vpcEndpointOwnerId: '{{ item.vpcEndpointOwnerId }}'
        vpcId: '{{ item.vpcId }}'
  # ── ListTagsForResource ──
  - discovery_id: aws.vpc-lattice.list_tags_for_resource
    calls:
      - action: list_tags_for_resource
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        tags: '{{ response.tags }}'
  # ── ListTargets ──
  - discovery_id: aws.vpc-lattice.list_targets
    calls:
      - action: list_targets
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        id: '{{ item.id }}'
        port: '{{ item.port }}'
        reasonCode: '{{ item.reasonCode }}'
        status: '{{ item.status }}'
