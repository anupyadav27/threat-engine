# ============================================================
# Discovery YAML — datazone (AWS)
# Generated: 2026-02-21T04:56:58Z
# Actions use boto3 SDK snake_case method names
# ============================================================
version: '1.0'
provider: aws
service: datazone
services:
  client: datazone
  module: boto3.client
discovery:
  # ── ListDomains ──
  - discovery_id: aws.datazone.list_domains
    calls:
      - action: list_domains
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        arn: '{{ item.arn }}'
        createdAt: '{{ item.createdAt }}'
        description: '{{ item.description }}'
        domainVersion: '{{ item.domainVersion }}'
        items: '{{ item.items }}'
        lastUpdatedAt: '{{ item.lastUpdatedAt }}'
        managedAccountId: '{{ item.managedAccountId }}'
        name: '{{ item.name }}'
        portalUrl: '{{ item.portalUrl }}'
  # ── Dependent read operations (require identifiers) ──
  # ── GetAccountPool ──
  - discovery_id: aws.datazone.get_account_pool
    calls:
      - action: get_account_pool
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.accountSource }}'
      item:
        accounts: '{{ item.accounts }}'
        customAccountPoolHandler: '{{ item.customAccountPoolHandler }}'
  # ── GetAsset ──
  - discovery_id: aws.datazone.get_asset
    calls:
      - action: get_asset
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.formsOutput }}'
      item:
        content: '{{ item.content }}'
        formName: '{{ item.formName }}'
        typeName: '{{ item.typeName }}'
        typeRevision: '{{ item.typeRevision }}'
  # ── GetAssetFilter ──
  - discovery_id: aws.datazone.get_asset_filter
    calls:
      - action: get_asset_filter
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.effectiveColumnNames }}'
  # ── GetAssetType ──
  - discovery_id: aws.datazone.get_asset_type
    calls:
      - action: get_asset_type
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createdAt: '{{ response.createdAt }}'
        createdBy: '{{ response.createdBy }}'
        description: '{{ response.description }}'
        domainId: '{{ response.domainId }}'
        formsOutput: '{{ response.formsOutput }}'
        name: '{{ response.name }}'
        originDomainId: '{{ response.originDomainId }}'
        originProjectId: '{{ response.originProjectId }}'
        owningProjectId: '{{ response.owningProjectId }}'
        revision: '{{ response.revision }}'
        updatedAt: '{{ response.updatedAt }}'
        updatedBy: '{{ response.updatedBy }}'
  # ── GetConnection ──
  - discovery_id: aws.datazone.get_connection
    calls:
      - action: get_connection
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.physicalEndpoints }}'
      item:
        awsLocation: '{{ item.awsLocation }}'
        enableTrustedIdentityPropagation: '{{ item.enableTrustedIdentityPropagation }}'
        glueConnection: '{{ item.glueConnection }}'
        glueConnectionName: '{{ item.glueConnectionName }}'
        host: '{{ item.host }}'
        port: '{{ item.port }}'
        protocol: '{{ item.protocol }}'
        stage: '{{ item.stage }}'
  # ── GetDataProduct ──
  - discovery_id: aws.datazone.get_data_product
    calls:
      - action: get_data_product
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.formsOutput }}'
      item:
        glossaryTerms: '{{ item.glossaryTerms }}'
        identifier: '{{ item.identifier }}'
        itemType: '{{ item.itemType }}'
        revision: '{{ item.revision }}'
  # ── GetDataSource ──
  - discovery_id: aws.datazone.get_data_source
    calls:
      - action: get_data_source
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.assetFormsOutput }}'
      item:
        content: '{{ item.content }}'
        formName: '{{ item.formName }}'
        typeName: '{{ item.typeName }}'
        typeRevision: '{{ item.typeRevision }}'
  # ── GetDataSourceRun ──
  - discovery_id: aws.datazone.get_data_source_run
    calls:
      - action: get_data_source_run
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.errorMessage }}'
      item:
        errorDetail: '{{ item.errorDetail }}'
        errorType: '{{ item.errorType }}'
  # ── GetDomain ──
  - discovery_id: aws.datazone.get_domain
    calls:
      - action: get_domain
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.singleSignOn }}'
      item:
        idcInstanceArn: '{{ item.idcInstanceArn }}'
        userAssignment: '{{ item.userAssignment }}'
  # ── GetDomainUnit ──
  - discovery_id: aws.datazone.get_domain_unit
    calls:
      - action: get_domain_unit
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.owners }}'
      item:
        group: '{{ item.group }}'
        user: '{{ item.user }}'
  # ── GetEnvironment ──
  - discovery_id: aws.datazone.get_environment
    calls:
      - action: get_environment
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.environmentActions }}'
      item:
        name: '{{ item.name }}'
        provider: '{{ item.provider }}'
        value: '{{ item.value }}'
  # ── GetEnvironmentAction ──
  - discovery_id: aws.datazone.get_environment_action
    calls:
      - action: get_environment_action
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.parameters }}'
      item:
        awsConsoleLink: '{{ item.awsConsoleLink }}'
  # ── GetEnvironmentBlueprint ──
  - discovery_id: aws.datazone.get_environment_blueprint
    calls:
      - action: get_environment_blueprint
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.glossaryTerms }}'
  # ── GetEnvironmentBlueprintConfiguration ──
  - discovery_id: aws.datazone.get_environment_blueprint_configuration
    calls:
      - action: get_environment_blueprint_configuration
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.enabledRegions }}'
  # ── GetEnvironmentCredentials ──
  - discovery_id: aws.datazone.get_environment_credentials
    calls:
      - action: get_environment_credentials
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.accessKeyId }}'
      item:
        expiration: '{{ item.expiration }}'
  # ── GetEnvironmentProfile ──
  - discovery_id: aws.datazone.get_environment_profile
    calls:
      - action: get_environment_profile
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.userParameters }}'
      item:
        defaultValue: '{{ item.defaultValue }}'
        description: '{{ item.description }}'
        fieldType: '{{ item.fieldType }}'
        isEditable: '{{ item.isEditable }}'
        isOptional: '{{ item.isOptional }}'
        isUpdateSupported: '{{ item.isUpdateSupported }}'
        keyName: '{{ item.keyName }}'
  # ── GetFormType ──
  - discovery_id: aws.datazone.get_form_type
    calls:
      - action: get_form_type
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.imports }}'
      item:
        imports: '{{ item.imports }}'
        name: '{{ item.name }}'
        revision: '{{ item.revision }}'
  # ── GetGlossary ──
  - discovery_id: aws.datazone.get_glossary
    calls:
      - action: get_glossary
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.usageRestrictions }}'
  # ── GetGlossaryTerm ──
  - discovery_id: aws.datazone.get_glossary_term
    calls:
      - action: get_glossary_term
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.usageRestrictions }}'
  # ── GetGroupProfile ──
  - discovery_id: aws.datazone.get_group_profile
    calls:
      - action: get_group_profile
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        domainId: '{{ response.domainId }}'
        groupName: '{{ response.groupName }}'
        id: '{{ response.id }}'
        status: '{{ response.status }}'
  # ── GetIamPortalLoginUrl ──
  - discovery_id: aws.datazone.get_iam_portal_login_url
    calls:
      - action: get_iam_portal_login_url
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        authCodeUrl: '{{ response.authCodeUrl }}'
        userProfileId: '{{ response.userProfileId }}'
  # ── GetJobRun ──
  - discovery_id: aws.datazone.get_job_run
    calls:
      - action: get_job_run
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.details }}'
      item:
        lineageRunDetails: '{{ item.lineageRunDetails }}'
  # ── GetLineageEvent ──
  - discovery_id: aws.datazone.get_lineage_event
    calls:
      - action: get_lineage_event
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createdAt: '{{ response.createdAt }}'
        createdBy: '{{ response.createdBy }}'
        domainId: '{{ response.domainId }}'
        event: '{{ response.event }}'
        eventTime: '{{ response.eventTime }}'
        id: '{{ response.id }}'
        processingStatus: '{{ response.processingStatus }}'
  # ── GetLineageNode ──
  - discovery_id: aws.datazone.get_lineage_node
    calls:
      - action: get_lineage_node
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.downstreamNodes }}'
      item:
        eventTimestamp: '{{ item.eventTimestamp }}'
  # ── GetListing ──
  - discovery_id: aws.datazone.get_listing
    calls:
      - action: get_listing
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createdAt: '{{ response.createdAt }}'
        createdBy: '{{ response.createdBy }}'
        description: '{{ response.description }}'
        domainId: '{{ response.domainId }}'
        id: '{{ response.id }}'
        item: '{{ response.item }}'
        listingRevision: '{{ response.listingRevision }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        updatedAt: '{{ response.updatedAt }}'
        updatedBy: '{{ response.updatedBy }}'
  # ── GetMetadataGenerationRun ──
  - discovery_id: aws.datazone.get_metadata_generation_run
    calls:
      - action: get_metadata_generation_run
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        identifier: '{{ item.identifier }}'
        revision: '{{ item.revision }}'
  # ── GetProject ──
  - discovery_id: aws.datazone.get_project
    calls:
      - action: get_project
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.failureReasons }}'
      item:
        code: '{{ item.code }}'
        message: '{{ item.message }}'
  # ── GetProjectProfile ──
  - discovery_id: aws.datazone.get_project_profile
    calls:
      - action: get_project_profile
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.environmentConfigurations }}'
      item:
        accountPools: '{{ item.accountPools }}'
        awsAccount: '{{ item.awsAccount }}'
        awsRegion: '{{ item.awsRegion }}'
        configurationParameters: '{{ item.configurationParameters }}'
        deploymentMode: '{{ item.deploymentMode }}'
        deploymentOrder: '{{ item.deploymentOrder }}'
        description: '{{ item.description }}'
        environmentBlueprintId: '{{ item.environmentBlueprintId }}'
        name: '{{ item.name }}'
  # ── GetRule ──
  - discovery_id: aws.datazone.get_rule
    calls:
      - action: get_rule
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.detail }}'
      item:
        glossaryTermEnforcementDetail: '{{ item.glossaryTermEnforcementDetail }}'
        metadataFormEnforcementDetail: '{{ item.metadataFormEnforcementDetail }}'
  # ── GetSubscription ──
  - discovery_id: aws.datazone.get_subscription
    calls:
      - action: get_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        description: '{{ item.description }}'
        item: '{{ item.item }}'
        name: '{{ item.name }}'
        ownerProjectId: '{{ item.ownerProjectId }}'
        ownerProjectName: '{{ item.ownerProjectName }}'
        revision: '{{ item.revision }}'
  # ── GetSubscriptionGrant ──
  - discovery_id: aws.datazone.get_subscription_grant
    calls:
      - action: get_subscription_grant
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.assets }}'
      item:
        assetId: '{{ item.assetId }}'
        assetRevision: '{{ item.assetRevision }}'
        assetScope: '{{ item.assetScope }}'
        failureCause: '{{ item.failureCause }}'
        failureTimestamp: '{{ item.failureTimestamp }}'
        grantedTimestamp: '{{ item.grantedTimestamp }}'
        permissions: '{{ item.permissions }}'
        targetName: '{{ item.targetName }}'
  # ── GetSubscriptionRequestDetails ──
  - discovery_id: aws.datazone.get_subscription_request_details
    calls:
      - action: get_subscription_request_details
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.metadataForms }}'
      item:
        description: '{{ item.description }}'
        item: '{{ item.item }}'
        name: '{{ item.name }}'
        ownerProjectId: '{{ item.ownerProjectId }}'
        ownerProjectName: '{{ item.ownerProjectName }}'
        revision: '{{ item.revision }}'
  # ── GetSubscriptionTarget ──
  - discovery_id: aws.datazone.get_subscription_target
    calls:
      - action: get_subscription_target
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.applicableAssetTypes }}'
  # ── GetTimeSeriesDataPoint ──
  - discovery_id: aws.datazone.get_time_series_data_point
    calls:
      - action: get_time_series_data_point
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.formsOutput }}'
      item:
        content: '{{ item.content }}'
        form: '{{ item.form }}'
        formName: '{{ item.formName }}'
        timestamp: '{{ item.timestamp }}'
        typeIdentifier: '{{ item.typeIdentifier }}'
        typeRevision: '{{ item.typeRevision }}'
  # ── GetUserProfile ──
  - discovery_id: aws.datazone.get_user_profile
    calls:
      - action: get_user_profile
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.details }}'
      item:
        iam: '{{ item.iam }}'
        sso: '{{ item.sso }}'
  # ── ListAccountPools ──
  - discovery_id: aws.datazone.list_account_pools
    calls:
      - action: list_account_pools
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        createdBy: '{{ item.createdBy }}'
        domainUnitId: '{{ item.domainUnitId }}'
        name: '{{ item.name }}'
        resolutionStrategy: '{{ item.resolutionStrategy }}'
        updatedBy: '{{ item.updatedBy }}'
  # ── ListAccountsInAccountPool ──
  - discovery_id: aws.datazone.list_accounts_in_account_pool
    calls:
      - action: list_accounts_in_account_pool
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        awsAccountId: '{{ item.awsAccountId }}'
        awsAccountName: '{{ item.awsAccountName }}'
        supportedRegions: '{{ item.supportedRegions }}'
  # ── ListAssetFilters ──
  - discovery_id: aws.datazone.list_asset_filters
    calls:
      - action: list_asset_filters
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        assetId: '{{ item.assetId }}'
        createdAt: '{{ item.createdAt }}'
        description: '{{ item.description }}'
        effectiveColumnNames: '{{ item.effectiveColumnNames }}'
        effectiveRowFilter: '{{ item.effectiveRowFilter }}'
        errorMessage: '{{ item.errorMessage }}'
        name: '{{ item.name }}'
  # ── ListAssetRevisions ──
  - discovery_id: aws.datazone.list_asset_revisions
    calls:
      - action: list_asset_revisions
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        createdAt: '{{ item.createdAt }}'
        createdBy: '{{ item.createdBy }}'
        revision: '{{ item.revision }}'
  # ── ListConnections ──
  - discovery_id: aws.datazone.list_connections
    calls:
      - action: list_connections
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        connectionId: '{{ item.connectionId }}'
        domainUnitId: '{{ item.domainUnitId }}'
        environmentId: '{{ item.environmentId }}'
        name: '{{ item.name }}'
        physicalEndpoints: '{{ item.physicalEndpoints }}'
        projectId: '{{ item.projectId }}'
        props: '{{ item.props }}'
        scope: '{{ item.scope }}'
  # ── ListDataProductRevisions ──
  - discovery_id: aws.datazone.list_data_product_revisions
    calls:
      - action: list_data_product_revisions
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        createdAt: '{{ item.createdAt }}'
        createdBy: '{{ item.createdBy }}'
        revision: '{{ item.revision }}'
  # ── ListDataSourceRunActivities ──
  - discovery_id: aws.datazone.list_data_source_run_activities
    calls:
      - action: list_data_source_run_activities
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        createdAt: '{{ item.createdAt }}'
        dataAssetId: '{{ item.dataAssetId }}'
        dataAssetStatus: '{{ item.dataAssetStatus }}'
        dataSourceRunId: '{{ item.dataSourceRunId }}'
        database: '{{ item.database }}'
        errorMessage: '{{ item.errorMessage }}'
        lineageSummary: '{{ item.lineageSummary }}'
        projectId: '{{ item.projectId }}'
        technicalDescription: '{{ item.technicalDescription }}'
        technicalName: '{{ item.technicalName }}'
        updatedAt: '{{ item.updatedAt }}'
  # ── ListDataSourceRuns ──
  - discovery_id: aws.datazone.list_data_source_runs
    calls:
      - action: list_data_source_runs
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        createdAt: '{{ item.createdAt }}'
        dataSourceId: '{{ item.dataSourceId }}'
        errorMessage: '{{ item.errorMessage }}'
        lineageSummary: '{{ item.lineageSummary }}'
        projectId: '{{ item.projectId }}'
        runStatisticsForAssets: '{{ item.runStatisticsForAssets }}'
        startedAt: '{{ item.startedAt }}'
        stoppedAt: '{{ item.stoppedAt }}'
        updatedAt: '{{ item.updatedAt }}'
  # ── ListDataSources ──
  - discovery_id: aws.datazone.list_data_sources
    calls:
      - action: list_data_sources
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        connectionId: '{{ item.connectionId }}'
        createdAt: '{{ item.createdAt }}'
        dataSourceId: '{{ item.dataSourceId }}'
        description: '{{ item.description }}'
        enableSetting: '{{ item.enableSetting }}'
        environmentId: '{{ item.environmentId }}'
        lastRunAssetCount: '{{ item.lastRunAssetCount }}'
        lastRunAt: '{{ item.lastRunAt }}'
        lastRunErrorMessage: '{{ item.lastRunErrorMessage }}'
        lastRunStatus: '{{ item.lastRunStatus }}'
        name: '{{ item.name }}'
        schedule: '{{ item.schedule }}'
        updatedAt: '{{ item.updatedAt }}'
  # ── ListDomainUnitsForParent ──
  - discovery_id: aws.datazone.list_domain_units_for_parent
    calls:
      - action: list_domain_units_for_parent
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        name: '{{ item.name }}'
  # ── ListEntityOwners ──
  - discovery_id: aws.datazone.list_entity_owners
    calls:
      - action: list_entity_owners
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.owners }}'
      item:
        group: '{{ item.group }}'
        user: '{{ item.user }}'
  # ── ListEnvironmentActions ──
  - discovery_id: aws.datazone.list_environment_actions
    calls:
      - action: list_environment_actions
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        description: '{{ item.description }}'
        environmentId: '{{ item.environmentId }}'
        name: '{{ item.name }}'
        parameters: '{{ item.parameters }}'
  # ── ListEnvironmentBlueprintConfigurations ──
  - discovery_id: aws.datazone.list_environment_blueprint_configurations
    calls:
      - action: list_environment_blueprint_configurations
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        createdAt: '{{ item.createdAt }}'
        enabledRegions: '{{ item.enabledRegions }}'
        environmentBlueprintId: '{{ item.environmentBlueprintId }}'
        environmentRolePermissionBoundary: '{{ item.environmentRolePermissionBoundary }}'
        manageAccessRoleArn: '{{ item.manageAccessRoleArn }}'
        provisioningConfigurations: '{{ item.provisioningConfigurations }}'
        provisioningRoleArn: '{{ item.provisioningRoleArn }}'
        regionalParameters: '{{ item.regionalParameters }}'
        updatedAt: '{{ item.updatedAt }}'
  # ── ListEnvironmentBlueprints ──
  - discovery_id: aws.datazone.list_environment_blueprints
    calls:
      - action: list_environment_blueprints
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        createdAt: '{{ item.createdAt }}'
        description: '{{ item.description }}'
        name: '{{ item.name }}'
        provider: '{{ item.provider }}'
        provisioningProperties: '{{ item.provisioningProperties }}'
        updatedAt: '{{ item.updatedAt }}'
  # ── ListEnvironmentProfiles ──
  - discovery_id: aws.datazone.list_environment_profiles
    calls:
      - action: list_environment_profiles
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        awsAccountId: '{{ item.awsAccountId }}'
        awsAccountRegion: '{{ item.awsAccountRegion }}'
        createdAt: '{{ item.createdAt }}'
        createdBy: '{{ item.createdBy }}'
        description: '{{ item.description }}'
        environmentBlueprintId: '{{ item.environmentBlueprintId }}'
        name: '{{ item.name }}'
        projectId: '{{ item.projectId }}'
        updatedAt: '{{ item.updatedAt }}'
  # ── ListEnvironments ──
  - discovery_id: aws.datazone.list_environments
    calls:
      - action: list_environments
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        awsAccountId: '{{ item.awsAccountId }}'
        awsAccountRegion: '{{ item.awsAccountRegion }}'
        createdAt: '{{ item.createdAt }}'
        createdBy: '{{ item.createdBy }}'
        description: '{{ item.description }}'
        environmentConfigurationId: '{{ item.environmentConfigurationId }}'
        environmentProfileId: '{{ item.environmentProfileId }}'
        name: '{{ item.name }}'
        projectId: '{{ item.projectId }}'
        provider: '{{ item.provider }}'
        updatedAt: '{{ item.updatedAt }}'
  # ── ListJobRuns ──
  - discovery_id: aws.datazone.list_job_runs
    calls:
      - action: list_job_runs
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        createdAt: '{{ item.createdAt }}'
        createdBy: '{{ item.createdBy }}'
        endTime: '{{ item.endTime }}'
        error: '{{ item.error }}'
        jobId: '{{ item.jobId }}'
        jobType: '{{ item.jobType }}'
        runId: '{{ item.runId }}'
        runMode: '{{ item.runMode }}'
        startTime: '{{ item.startTime }}'
  # ── ListLineageEvents ──
  - discovery_id: aws.datazone.list_lineage_events
    calls:
      - action: list_lineage_events
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        createdAt: '{{ item.createdAt }}'
        createdBy: '{{ item.createdBy }}'
        eventSummary: '{{ item.eventSummary }}'
        eventTime: '{{ item.eventTime }}'
        processingStatus: '{{ item.processingStatus }}'
  # ── ListLineageNodeHistory ──
  - discovery_id: aws.datazone.list_lineage_node_history
    calls:
      - action: list_lineage_node_history
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.nodes }}'
      item:
        createdAt: '{{ item.createdAt }}'
        createdBy: '{{ item.createdBy }}'
        description: '{{ item.description }}'
        eventTimestamp: '{{ item.eventTimestamp }}'
        name: '{{ item.name }}'
        sourceIdentifier: '{{ item.sourceIdentifier }}'
        typeName: '{{ item.typeName }}'
        typeRevision: '{{ item.typeRevision }}'
        updatedAt: '{{ item.updatedAt }}'
        updatedBy: '{{ item.updatedBy }}'
  # ── ListMetadataGenerationRuns ──
  - discovery_id: aws.datazone.list_metadata_generation_runs
    calls:
      - action: list_metadata_generation_runs
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        createdAt: '{{ item.createdAt }}'
        createdBy: '{{ item.createdBy }}'
        owningProjectId: '{{ item.owningProjectId }}'
        target: '{{ item.target }}'
  # ── ListNotifications ──
  - discovery_id: aws.datazone.list_notifications
    calls:
      - action: list_notifications
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.notifications }}'
      item:
        actionLink: '{{ item.actionLink }}'
        creationTimestamp: '{{ item.creationTimestamp }}'
        domainIdentifier: '{{ item.domainIdentifier }}'
        identifier: '{{ item.identifier }}'
        lastUpdatedTimestamp: '{{ item.lastUpdatedTimestamp }}'
        message: '{{ item.message }}'
        metadata: '{{ item.metadata }}'
        title: '{{ item.title }}'
        topic: '{{ item.topic }}'
  # ── ListPolicyGrants ──
  - discovery_id: aws.datazone.list_policy_grants
    calls:
      - action: list_policy_grants
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.grantList }}'
      item:
        createdAt: '{{ item.createdAt }}'
        createdBy: '{{ item.createdBy }}'
        detail: '{{ item.detail }}'
        grantId: '{{ item.grantId }}'
        principal: '{{ item.principal }}'
  # ── ListProjectMemberships ──
  - discovery_id: aws.datazone.list_project_memberships
    calls:
      - action: list_project_memberships
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.members }}'
      item:
        designation: '{{ item.designation }}'
        memberDetails: '{{ item.memberDetails }}'
  # ── ListProjectProfiles ──
  - discovery_id: aws.datazone.list_project_profiles
    calls:
      - action: list_project_profiles
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        createdAt: '{{ item.createdAt }}'
        createdBy: '{{ item.createdBy }}'
        description: '{{ item.description }}'
        domainUnitId: '{{ item.domainUnitId }}'
        lastUpdatedAt: '{{ item.lastUpdatedAt }}'
        name: '{{ item.name }}'
  # ── ListProjects ──
  - discovery_id: aws.datazone.list_projects
    calls:
      - action: list_projects
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        createdAt: '{{ item.createdAt }}'
        createdBy: '{{ item.createdBy }}'
        description: '{{ item.description }}'
        domainUnitId: '{{ item.domainUnitId }}'
        failureReasons: '{{ item.failureReasons }}'
        name: '{{ item.name }}'
        projectStatus: '{{ item.projectStatus }}'
        updatedAt: '{{ item.updatedAt }}'
  # ── ListRules ──
  - discovery_id: aws.datazone.list_rules
    calls:
      - action: list_rules
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        action: '{{ item.action }}'
        identifier: '{{ item.identifier }}'
        lastUpdatedBy: '{{ item.lastUpdatedBy }}'
        name: '{{ item.name }}'
        revision: '{{ item.revision }}'
        ruleType: '{{ item.ruleType }}'
        scope: '{{ item.scope }}'
        target: '{{ item.target }}'
        targetType: '{{ item.targetType }}'
        updatedAt: '{{ item.updatedAt }}'
  # ── ListSubscriptionGrants ──
  - discovery_id: aws.datazone.list_subscription_grants
    calls:
      - action: list_subscription_grants
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        assets: '{{ item.assets }}'
        createdAt: '{{ item.createdAt }}'
        createdBy: '{{ item.createdBy }}'
        environmentId: '{{ item.environmentId }}'
        grantedEntity: '{{ item.grantedEntity }}'
        subscriptionId: '{{ item.subscriptionId }}'
        subscriptionTargetId: '{{ item.subscriptionTargetId }}'
        updatedAt: '{{ item.updatedAt }}'
        updatedBy: '{{ item.updatedBy }}'
  # ── ListSubscriptionRequests ──
  - discovery_id: aws.datazone.list_subscription_requests
    calls:
      - action: list_subscription_requests
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        createdAt: '{{ item.createdAt }}'
        createdBy: '{{ item.createdBy }}'
        decisionComment: '{{ item.decisionComment }}'
        existingSubscriptionId: '{{ item.existingSubscriptionId }}'
        metadataFormsSummary: '{{ item.metadataFormsSummary }}'
        requestReason: '{{ item.requestReason }}'
        reviewerId: '{{ item.reviewerId }}'
        subscribedListings: '{{ item.subscribedListings }}'
        subscribedPrincipals: '{{ item.subscribedPrincipals }}'
        updatedAt: '{{ item.updatedAt }}'
        updatedBy: '{{ item.updatedBy }}'
  # ── ListSubscriptionTargets ──
  - discovery_id: aws.datazone.list_subscription_targets
    calls:
      - action: list_subscription_targets
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        applicableAssetTypes: '{{ item.applicableAssetTypes }}'
        authorizedPrincipals: '{{ item.authorizedPrincipals }}'
        createdAt: '{{ item.createdAt }}'
        createdBy: '{{ item.createdBy }}'
        environmentId: '{{ item.environmentId }}'
        manageAccessRole: '{{ item.manageAccessRole }}'
        name: '{{ item.name }}'
        projectId: '{{ item.projectId }}'
        provider: '{{ item.provider }}'
        subscriptionTargetConfig: '{{ item.subscriptionTargetConfig }}'
        updatedAt: '{{ item.updatedAt }}'
        updatedBy: '{{ item.updatedBy }}'
  # ── ListSubscriptions ──
  - discovery_id: aws.datazone.list_subscriptions
    calls:
      - action: list_subscriptions
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        createdAt: '{{ item.createdAt }}'
        createdBy: '{{ item.createdBy }}'
        retainPermissions: '{{ item.retainPermissions }}'
        subscribedListing: '{{ item.subscribedListing }}'
        subscribedPrincipal: '{{ item.subscribedPrincipal }}'
        subscriptionRequestId: '{{ item.subscriptionRequestId }}'
        updatedAt: '{{ item.updatedAt }}'
        updatedBy: '{{ item.updatedBy }}'
  # ── ListTagsForResource ──
  - discovery_id: aws.datazone.list_tags_for_resource
    calls:
      - action: list_tags_for_resource
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        tags: '{{ response.tags }}'
  # ── ListTimeSeriesDataPoints ──
  - discovery_id: aws.datazone.list_time_series_data_points
    calls:
      - action: list_time_series_data_points
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        contentSummary: '{{ item.contentSummary }}'
        form: '{{ item.form }}'
        formName: '{{ item.formName }}'
        timestamp: '{{ item.timestamp }}'
        typeIdentifier: '{{ item.typeIdentifier }}'
        typeRevision: '{{ item.typeRevision }}'
  # ── Search ──
  - discovery_id: aws.datazone.search
    calls:
      - action: search
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
  # ── SearchGroupProfiles ──
  - discovery_id: aws.datazone.search_group_profiles
    calls:
      - action: search_group_profiles
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
  # ── SearchListings ──
  - discovery_id: aws.datazone.search_listings
    calls:
      - action: search_listings
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.aggregates }}'
  # ── SearchTypes ──
  - discovery_id: aws.datazone.search_types
    calls:
      - action: search_types
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
  # ── SearchUserProfiles ──
  - discovery_id: aws.datazone.search_user_profiles
    calls:
      - action: search_user_profiles
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
