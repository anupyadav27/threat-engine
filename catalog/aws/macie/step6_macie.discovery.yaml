# ============================================================
# Discovery YAML — macie (AWS)
# Generated: 2026-02-21T04:56:59Z
# Actions use boto3 SDK snake_case method names
# ============================================================
version: '1.0'
provider: aws
service: macie
services:
  client: macie
  module: boto3.client
discovery:
  # ── DescribeBuckets ──
  - discovery_id: aws.macie.describe_buckets
    calls:
      - action: describe_buckets
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.buckets }}'
      item:
        accountId: '{{ item.accountId }}'
        allowsUnencryptedObjectUploads: '{{ item.allowsUnencryptedObjectUploads }}'
        automatedDiscoveryMonitoringStatus: '{{ item.automatedDiscoveryMonitoringStatus }}'
        bucketArn: '{{ item.bucketArn }}'
        bucketCreatedAt: '{{ item.bucketCreatedAt }}'
        bucketName: '{{ item.bucketName }}'
        buckets: '{{ item.buckets }}'
        classifiableObjectCount: '{{ item.classifiableObjectCount }}'
        classifiableSizeInBytes: '{{ item.classifiableSizeInBytes }}'
        errorCode: '{{ item.errorCode }}'
        errorMessage: '{{ item.errorMessage }}'
        jobDetails: '{{ item.jobDetails }}'
        lastAutomatedDiscoveryTime: '{{ item.lastAutomatedDiscoveryTime }}'
        lastUpdated: '{{ item.lastUpdated }}'
        objectCount: '{{ item.objectCount }}'
        objectCountByEncryptionType: '{{ item.objectCountByEncryptionType }}'
        publicAccess: '{{ item.publicAccess }}'
        region: '{{ item.region }}'
        replicationDetails: '{{ item.replicationDetails }}'
        sensitivityScore: '{{ item.sensitivityScore }}'
        serverSideEncryption: '{{ item.serverSideEncryption }}'
        sharedAccess: '{{ item.sharedAccess }}'
        sizeInBytes: '{{ item.sizeInBytes }}'
        sizeInBytesCompressed: '{{ item.sizeInBytesCompressed }}'
        tags: '{{ item.tags }}'
        unclassifiableObjectCount: '{{ item.unclassifiableObjectCount }}'
        unclassifiableObjectSizeInBytes: '{{ item.unclassifiableObjectSizeInBytes }}'
        versioning: '{{ item.versioning }}'
  # ── DescribeOrganizationConfiguration ──
  - discovery_id: aws.macie.describe_organization_configuration
    calls:
      - action: describe_organization_configuration
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.autoEnable }}'
      item:
        autoEnable: '{{ item.autoEnable }}'
        maxAccountLimitReached: '{{ item.maxAccountLimitReached }}'
  # ── GetAdministratorAccount ──
  - discovery_id: aws.macie.get_administrator_account
    calls:
      - action: get_administrator_account
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.buckets }}'
      item:
        accountId: '{{ item.accountId }}'
        administrator: '{{ item.administrator }}'
        invitationId: '{{ item.invitationId }}'
        invitedAt: '{{ item.invitedAt }}'
        relationshipStatus: '{{ item.relationshipStatus }}'
  # ── GetAutomatedDiscoveryConfiguration ──
  - discovery_id: aws.macie.get_automated_discovery_configuration
    calls:
      - action: get_automated_discovery_configuration
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.autoEnableOrganizationMembers }}'
      item:
        autoEnableOrganizationMembers: '{{ item.autoEnableOrganizationMembers }}'
        classificationScopeId: '{{ item.classificationScopeId }}'
        disabledAt: '{{ item.disabledAt }}'
        firstEnabledAt: '{{ item.firstEnabledAt }}'
        lastUpdatedAt: '{{ item.lastUpdatedAt }}'
        profileUpdatedAt: '{{ item.profileUpdatedAt }}'
        sensitivityInspectionTemplateId: '{{ item.sensitivityInspectionTemplateId }}'
  # ── GetBucketStatistics ──
  - discovery_id: aws.macie.get_bucket_statistics
    calls:
      - action: get_bucket_statistics
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.bucketCount }}'
      item:
        bucketCount: '{{ item.bucketCount }}'
        bucketCountByEffectivePermission: '{{ item.bucketCountByEffectivePermission }}'
        bucketCountByEncryptionType: '{{ item.bucketCountByEncryptionType }}'
        bucketCountByObjectEncryptionRequirement: '{{ item.bucketCountByObjectEncryptionRequirement }}'
        bucketCountBySharedAccessType: '{{ item.bucketCountBySharedAccessType }}'
        bucketStatisticsBySensitivity: '{{ item.bucketStatisticsBySensitivity }}'
        publiclyAccessible: '{{ item.publiclyAccessible }}'
        publiclyReadable: '{{ item.publiclyReadable }}'
        publiclyWritable: '{{ item.publiclyWritable }}'
        unknown: '{{ item.unknown }}'
  # ── GetClassificationExportConfiguration ──
  - discovery_id: aws.macie.get_classification_export_configuration
    calls:
      - action: get_classification_export_configuration
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.configuration }}'
      item:
        Status: '{{ item.Status }}'
        Enabled: '{{ item.Enabled }}'
        configuration: '{{ item.configuration }}'
        s3Destination: '{{ item.s3Destination }}'
  # ── GetFindingsPublicationConfiguration ──
  - discovery_id: aws.macie.get_findings_publication_configuration
    calls:
      - action: get_findings_publication_configuration
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.securityHubConfiguration }}'
      item:
        Statement: '{{ item.Statement }}'
        Version: '{{ item.Version }}'
        publishClassificationFindings: '{{ item.publishClassificationFindings }}'
        publishPolicyFindings: '{{ item.publishPolicyFindings }}'
        securityHubConfiguration: '{{ item.securityHubConfiguration }}'
  # ── GetInvitationsCount ──
  - discovery_id: aws.macie.get_invitations_count
    calls:
      - action: get_invitations_count
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.invitationsCount }}'
      item:
        invitationsCount: '{{ item.invitationsCount }}'
  # ── GetMacieSession ──
  - discovery_id: aws.macie.get_macie_session
    calls:
      - action: get_macie_session
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.createdAt }}'
      item:
        findingPublishingFrequency: '{{ item.findingPublishingFrequency }}'
        serviceRole: '{{ item.serviceRole }}'
  # ── GetMasterAccount ──
  - discovery_id: aws.macie.get_master_account
    calls:
      - action: get_master_account
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.buckets }}'
      item:
        accountId: '{{ item.accountId }}'
        invitationId: '{{ item.invitationId }}'
        invitedAt: '{{ item.invitedAt }}'
        master: '{{ item.master }}'
        relationshipStatus: '{{ item.relationshipStatus }}'
  # ── GetRevealConfiguration ──
  - discovery_id: aws.macie.get_reveal_configuration
    calls:
      - action: get_reveal_configuration
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.configuration }}'
      item:
        kmsKeyId: '{{ item.kmsKeyId }}'
        retrievalConfiguration: '{{ item.retrievalConfiguration }}'
        status: '{{ item.status }}'
  # ── GetUsageStatistics ──
  - discovery_id: aws.macie.get_usage_statistics
    calls:
      - action: get_usage_statistics
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.buckets }}'
      item:
        accountId: '{{ item.accountId }}'
        automatedDiscoveryFreeTrialStartDate: '{{ item.automatedDiscoveryFreeTrialStartDate }}'
        freeTrialStartDate: '{{ item.freeTrialStartDate }}'
        records: '{{ item.records }}'
        timeRange: '{{ item.timeRange }}'
        usage: '{{ item.usage }}'
  # ── GetUsageTotals ──
  - discovery_id: aws.macie.get_usage_totals
    calls:
      - action: get_usage_totals
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.usageTotals }}'
      item:
        currency: '{{ item.currency }}'
        estimatedCost: '{{ item.estimatedCost }}'
        type: '{{ item.type }}'
        usageTotals: '{{ item.usageTotals }}'
  # ── ListAllowLists ──
  - discovery_id: aws.macie.list_allow_lists
    calls:
      - action: list_allow_lists
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.allowLists }}'
      item:
        arn: '{{ item.arn }}'
        createdAt: '{{ item.createdAt }}'
        criteria: '{{ item.criteria }}'
        description: '{{ item.description }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        updatedAt: '{{ item.updatedAt }}'
  # ── ListAutomatedDiscoveryAccounts ──
  - discovery_id: aws.macie.list_automated_discovery_accounts
    calls:
      - action: list_automated_discovery_accounts
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.buckets }}'
      item:
        accountId: '{{ item.accountId }}'
        status: '{{ item.status }}'
  # ── ListClassificationJobs ──
  - discovery_id: aws.macie.list_classification_jobs
    calls:
      - action: list_classification_jobs
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        allowListIds: '{{ item.allowListIds }}'
        bucketCriteria: '{{ item.bucketCriteria }}'
        bucketDefinitions: '{{ item.bucketDefinitions }}'
        clientToken: '{{ item.clientToken }}'
        createdAt: '{{ item.createdAt }}'
        customDataIdentifierIds: '{{ item.customDataIdentifierIds }}'
        initialRun: '{{ item.initialRun }}'
        jobArn: '{{ item.jobArn }}'
        jobId: '{{ item.jobId }}'
        jobStatus: '{{ item.jobStatus }}'
        jobType: '{{ item.jobType }}'
        lastRunErrorStatus: '{{ item.lastRunErrorStatus }}'
        lastRunTime: '{{ item.lastRunTime }}'
        managedDataIdentifierSelector: '{{ item.managedDataIdentifierSelector }}'
        name: '{{ item.name }}'
        s3JobDefinition: '{{ item.s3JobDefinition }}'
        samplingPercentage: '{{ item.samplingPercentage }}'
        scheduleFrequency: '{{ item.scheduleFrequency }}'
        statistics: '{{ item.statistics }}'
        userPausedDetails: '{{ item.userPausedDetails }}'
  # ── ListClassificationScopes ──
  - discovery_id: aws.macie.list_classification_scopes
    calls:
      - action: list_classification_scopes
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.allowLists }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        s3: '{{ item.s3 }}'
  # ── ListCustomDataIdentifiers ──
  - discovery_id: aws.macie.list_custom_data_identifiers
    calls:
      - action: list_custom_data_identifiers
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.allowLists }}'
      item:
        arn: '{{ item.arn }}'
        createdAt: '{{ item.createdAt }}'
        description: '{{ item.description }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
  # ── ListFindings ──
  - discovery_id: aws.macie.list_findings
    calls:
      - action: list_findings
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.findings }}'
      item:
        archived: '{{ item.archived }}'
        classificationDetails: '{{ item.classificationDetails }}'
        findings: '{{ item.findings }}'
        partition: '{{ item.partition }}'
        policyDetails: '{{ item.policyDetails }}'
        resourcesAffected: '{{ item.resourcesAffected }}'
        sample: '{{ item.sample }}'
        schemaVersion: '{{ item.schemaVersion }}'
        severity: '{{ item.severity }}'
        title: '{{ item.title }}'
  # ── ListFindingsFilters ──
  - discovery_id: aws.macie.list_findings_filters
    calls:
      - action: list_findings_filters
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.findingsFilterListItems }}'
      item:
        action: '{{ item.action }}'
        arn: '{{ item.arn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        position: '{{ item.position }}'
        tags: '{{ item.tags }}'
  # ── ListInvitations ──
  - discovery_id: aws.macie.list_invitations
    calls:
      - action: list_invitations
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.buckets }}'
      item:
        accountId: '{{ item.accountId }}'
        invitationId: '{{ item.invitationId }}'
        invitedAt: '{{ item.invitedAt }}'
        relationshipStatus: '{{ item.relationshipStatus }}'
  # ── ListManagedDataIdentifiers ──
  - discovery_id: aws.macie.list_managed_data_identifiers
    calls:
      - action: list_managed_data_identifiers
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        category: '{{ item.category }}'
        id: '{{ item.id }}'
  # ── ListMembers ──
  - discovery_id: aws.macie.list_members
    calls:
      - action: list_members
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.buckets }}'
      item:
        accountId: '{{ item.accountId }}'
        administratorAccountId: '{{ item.administratorAccountId }}'
        arn: '{{ item.arn }}'
        email: '{{ item.email }}'
        invitedAt: '{{ item.invitedAt }}'
        masterAccountId: '{{ item.masterAccountId }}'
        relationshipStatus: '{{ item.relationshipStatus }}'
        tags: '{{ item.tags }}'
        updatedAt: '{{ item.updatedAt }}'
  # ── ListOrganizationAdminAccounts ──
  - discovery_id: aws.macie.list_organization_admin_accounts
    calls:
      - action: list_organization_admin_accounts
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.buckets }}'
      item:
        accountId: '{{ item.accountId }}'
        status: '{{ item.status }}'
  # ── ListSensitivityInspectionTemplates ──
  - discovery_id: aws.macie.list_sensitivity_inspection_templates
    calls:
      - action: list_sensitivity_inspection_templates
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.allowLists }}'
      item:
        id: '{{ item.id }}'
        includes: '{{ item.includes }}'
        name: '{{ item.name }}'
  # ── SearchResources ──
  - discovery_id: aws.macie.search_resources
    calls:
      - action: search_resources
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        matchingResources: '{{ response.matchingResources }}'
  # ── Dependent read operations (require identifiers) ──
  # ── DescribeClassificationJob ──
  - discovery_id: aws.macie.describe_classification_job
    for_each: aws.macie.list_classification_jobs
    calls:
      - action: describe_classification_job
        params:
          jobId: '{{ item.jobId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        allowListIds: '{{ item.allowListIds }}'
        clientToken: '{{ item.clientToken }}'
        customDataIdentifierIds: '{{ item.customDataIdentifierIds }}'
        initialRun: '{{ item.initialRun }}'
        jobArn: '{{ item.jobArn }}'
        lastRunTime: '{{ item.lastRunTime }}'
        managedDataIdentifierSelector: '{{ item.managedDataIdentifierSelector }}'
        s3JobDefinition: '{{ item.s3JobDefinition }}'
        samplingPercentage: '{{ item.samplingPercentage }}'
        scheduleFrequency: '{{ item.scheduleFrequency }}'
        statistics: '{{ item.statistics }}'
  # ── GetAllowList ──
  - discovery_id: aws.macie.get_allow_list
    calls:
      - action: get_allow_list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.allowLists }}'
      item:
        criteria: '{{ item.criteria }}'
        regex: '{{ item.regex }}'
        s3WordsList: '{{ item.s3WordsList }}'
  # ── GetClassificationScope ──
  - discovery_id: aws.macie.get_classification_scope
    calls:
      - action: get_classification_scope
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.s3 }}'
      item:
        excludes: '{{ item.excludes }}'
        s3: '{{ item.s3 }}'
  # ── GetCustomDataIdentifier ──
  - discovery_id: aws.macie.get_custom_data_identifier
    calls:
      - action: get_custom_data_identifier
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        arn: '{{ response.arn }}'
        createdAt: '{{ response.createdAt }}'
        deleted: '{{ response.deleted }}'
        description: '{{ response.description }}'
        id: '{{ response.id }}'
        ignoreWords: '{{ response.ignoreWords }}'
        keywords: '{{ response.keywords }}'
        maximumMatchDistance: '{{ response.maximumMatchDistance }}'
        name: '{{ response.name }}'
        regex: '{{ response.regex }}'
        severityLevels: '{{ response.severityLevels }}'
        tags: '{{ response.tags }}'
  # ── GetFindingStatistics ──
  - discovery_id: aws.macie.get_finding_statistics
    calls:
      - action: get_finding_statistics
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.countsByGroup }}'
      item:
        count: '{{ item.count }}'
        groupKey: '{{ item.groupKey }}'
  # ── GetFindings ──
  - discovery_id: aws.macie.get_findings
    for_each: aws.macie.list_findings
    calls:
      - action: get_findings
        params:
          findingIds: '{{ item.findingIds }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.buckets }}'
      item:
        accountId: '{{ item.accountId }}'
        archived: '{{ item.archived }}'
        category: '{{ item.category }}'
        classificationDetails: '{{ item.classificationDetails }}'
        count: '{{ item.count }}'
        createdAt: '{{ item.createdAt }}'
        description: '{{ item.description }}'
        findings: '{{ item.findings }}'
        id: '{{ item.id }}'
        partition: '{{ item.partition }}'
        policyDetails: '{{ item.policyDetails }}'
        region: '{{ item.region }}'
        resourcesAffected: '{{ item.resourcesAffected }}'
        sample: '{{ item.sample }}'
        schemaVersion: '{{ item.schemaVersion }}'
        severity: '{{ item.severity }}'
        title: '{{ item.title }}'
        type: '{{ item.type }}'
        updatedAt: '{{ item.updatedAt }}'
  # ── GetFindingsFilter ──
  - discovery_id: aws.macie.get_findings_filter
    calls:
      - action: get_findings_filter
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.findingCriteria }}'
      item:
        criterion: '{{ item.criterion }}'
        position: '{{ item.position }}'
  # ── GetMember ──
  - discovery_id: aws.macie.get_member
    calls:
      - action: get_member
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        accountId: '{{ response.accountId }}'
        administratorAccountId: '{{ response.administratorAccountId }}'
        arn: '{{ response.arn }}'
        email: '{{ response.email }}'
        invitedAt: '{{ response.invitedAt }}'
        masterAccountId: '{{ response.masterAccountId }}'
        relationshipStatus: '{{ response.relationshipStatus }}'
        tags: '{{ response.tags }}'
        updatedAt: '{{ response.updatedAt }}'
  # ── GetResourceProfile ──
  - discovery_id: aws.macie.get_resource_profile
    calls:
      - action: get_resource_profile
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.autoEnableOrganizationMembers }}'
      item:
        profileUpdatedAt: '{{ item.profileUpdatedAt }}'
        totalBytesClassified: '{{ item.totalBytesClassified }}'
        totalDetections: '{{ item.totalDetections }}'
        totalDetectionsSuppressed: '{{ item.totalDetectionsSuppressed }}'
        totalItemsClassified: '{{ item.totalItemsClassified }}'
        totalItemsSensitive: '{{ item.totalItemsSensitive }}'
        totalItemsSkipped: '{{ item.totalItemsSkipped }}'
        totalItemsSkippedInvalidEncryption: '{{ item.totalItemsSkippedInvalidEncryption }}'
        totalItemsSkippedInvalidKms: '{{ item.totalItemsSkippedInvalidKms }}'
        totalItemsSkippedPermissionDenied: '{{ item.totalItemsSkippedPermissionDenied }}'
  # ── GetSensitiveDataOccurrences ──
  - discovery_id: aws.macie.get_sensitive_data_occurrences
    calls:
      - action: get_sensitive_data_occurrences
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.error }}'
      item:
        error: '{{ item.error }}'
  # ── GetSensitiveDataOccurrencesAvailability ──
  - discovery_id: aws.macie.get_sensitive_data_occurrences_availability
    calls:
      - action: get_sensitive_data_occurrences_availability
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.code }}'
      item:
        code: '{{ item.code }}'
        reasons: '{{ item.reasons }}'
  # ── GetSensitivityInspectionTemplate ──
  - discovery_id: aws.macie.get_sensitivity_inspection_template
    calls:
      - action: get_sensitivity_inspection_template
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.nextToken }}'
      item:
        includes: '{{ item.includes }}'
        managedDataIdentifierIds: '{{ item.managedDataIdentifierIds }}'
  # ── ListResourceProfileArtifacts ──
  - discovery_id: aws.macie.list_resource_profile_artifacts
    calls:
      - action: list_resource_profile_artifacts
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.allowLists }}'
      item:
        arn: '{{ item.arn }}'
        classificationResultStatus: '{{ item.classificationResultStatus }}'
        profileUpdatedAt: '{{ item.profileUpdatedAt }}'
        sensitive: '{{ item.sensitive }}'
  # ── ListResourceProfileDetections ──
  - discovery_id: aws.macie.list_resource_profile_detections
    calls:
      - action: list_resource_profile_detections
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.allowLists }}'
      item:
        arn: '{{ item.arn }}'
        count: '{{ item.count }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        suppressed: '{{ item.suppressed }}'
        type: '{{ item.type }}'
  # ── ListTagsForResource ──
  - discovery_id: aws.macie.list_tags_for_resource
    calls:
      - action: list_tags_for_resource
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        tags: '{{ response.tags }}'
