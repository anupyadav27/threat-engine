# ============================================================
# Discovery YAML — emr (AWS)
# Generated: 2026-02-21T04:56:58Z
# Actions use boto3 SDK snake_case method names
# ============================================================
version: '1.0'
provider: aws
service: emr
services:
  client: emr
  module: boto3.client
discovery:
  # ── DescribeJobFlows ──
  - discovery_id: aws.emr.describe_job_flows
    calls:
      - action: describe_job_flows
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.JobFlows }}'
      item:
        Name: '{{ item.Name }}'
        ServiceRole: '{{ item.ServiceRole }}'
        AmiVersion: '{{ item.AmiVersion }}'
        AutoScalingRole: '{{ item.AutoScalingRole }}'
        BootstrapActions: '{{ item.BootstrapActions }}'
        ExecutionStatusDetail: '{{ item.ExecutionStatusDetail }}'
        Instances: '{{ item.Instances }}'
        JobFlowId: '{{ item.JobFlowId }}'
        JobFlowRole: '{{ item.JobFlowRole }}'
        JobFlows: '{{ item.JobFlows }}'
        LogEncryptionKmsKeyId: '{{ item.LogEncryptionKmsKeyId }}'
        LogUri: '{{ item.LogUri }}'
        ScaleDownBehavior: '{{ item.ScaleDownBehavior }}'
        Step: '{{ item.Step }}'
        Steps: '{{ item.Steps }}'
        SupportedProducts: '{{ item.SupportedProducts }}'
        VisibleToAllUsers: '{{ item.VisibleToAllUsers }}'
  # ── DescribeReleaseLabel ──
  - discovery_id: aws.emr.describe_release_label
    calls:
      - action: describe_release_label
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Applications }}'
      item:
        Name: '{{ item.Name }}'
        Architecture: '{{ item.Architecture }}'
        AvailableOSReleases: '{{ item.AvailableOSReleases }}'
        EbsOptimizedAvailable: '{{ item.EbsOptimizedAvailable }}'
        EbsOptimizedByDefault: '{{ item.EbsOptimizedByDefault }}'
        EbsStorageOnly: '{{ item.EbsStorageOnly }}'
        ExpiresAt: '{{ item.ExpiresAt }}'
        InstanceFamilyId: '{{ item.InstanceFamilyId }}'
        Is64BitsOnly: '{{ item.Is64BitsOnly }}'
        MemoryGB: '{{ item.MemoryGB }}'
        NumberOfDisks: '{{ item.NumberOfDisks }}'
        PersistentAppUI: '{{ item.PersistentAppUI }}'
        StorageGB: '{{ item.StorageGB }}'
        Type: '{{ item.Type }}'
        VCPU: '{{ item.VCPU }}'
        Version: '{{ item.Version }}'
  # ── GetBlockPublicAccessConfiguration ──
  - discovery_id: aws.emr.get_block_public_access_configuration
    calls:
      - action: get_block_public_access_configuration
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.BlockPublicAccessConfiguration }}'
      item:
        BlockPublicAccessConfiguration: '{{ item.BlockPublicAccessConfiguration }}'
        BlockPublicAccessConfigurationMetadata: '{{ item.BlockPublicAccessConfigurationMetadata }}'
        BlockPublicSecurityGroupRules: '{{ item.BlockPublicSecurityGroupRules }}'
        Enabled: '{{ item.Enabled }}'
        PermittedPublicSecurityGroupRuleRanges: '{{ item.PermittedPublicSecurityGroupRuleRanges }}'
        PresignedURL: '{{ item.PresignedURL }}'
        PresignedURLReady: '{{ item.PresignedURLReady }}'
  # ── ListClusters ──
  - discovery_id: aws.emr.list_clusters
    calls:
      - action: list_clusters
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Clusters }}'
      item:
        Id: '{{ item.Id }}'
        Name: '{{ item.Name }}'
        Status: '{{ item.Status }}'
        Cluster: '{{ item.Cluster }}'
        ClusterArn: '{{ item.ClusterArn }}'
        NormalizedInstanceHours: '{{ item.NormalizedInstanceHours }}'
        OutpostArn: '{{ item.OutpostArn }}'
  # ── ListNotebookExecutions ──
  - discovery_id: aws.emr.list_notebook_executions
    calls:
      - action: list_notebook_executions
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.NotebookExecutions }}'
      item:
        Status: '{{ item.Status }}'
        EditorId: '{{ item.EditorId }}'
        EndTime: '{{ item.EndTime }}'
        ExecutionEngineId: '{{ item.ExecutionEngineId }}'
        NotebookExecution: '{{ item.NotebookExecution }}'
        NotebookExecutionId: '{{ item.NotebookExecutionId }}'
        NotebookExecutionName: '{{ item.NotebookExecutionName }}'
        NotebookS3Location: '{{ item.NotebookS3Location }}'
        StartTime: '{{ item.StartTime }}'
  # ── ListReleaseLabels ──
  - discovery_id: aws.emr.list_release_labels
    calls:
      - action: list_release_labels
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.ReleaseLabels }}'
  # ── ListSecurityConfigurations ──
  - discovery_id: aws.emr.list_security_configurations
    calls:
      - action: list_security_configurations
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.SecurityConfigurations }}'
      item:
        Name: '{{ item.Name }}'
        CreationDateTime: '{{ item.CreationDateTime }}'
  # ── ListStudioSessionMappings ──
  - discovery_id: aws.emr.list_studio_session_mappings
    calls:
      - action: list_studio_session_mappings
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.SessionMappings }}'
      item:
        CreationTime: '{{ item.CreationTime }}'
        IdentityId: '{{ item.IdentityId }}'
        IdentityName: '{{ item.IdentityName }}'
        IdentityType: '{{ item.IdentityType }}'
        SessionMapping: '{{ item.SessionMapping }}'
        SessionPolicyArn: '{{ item.SessionPolicyArn }}'
        Statement: '{{ item.Statement }}'
        StudioId: '{{ item.StudioId }}'
  # ── ListStudios ──
  - discovery_id: aws.emr.list_studios
    calls:
      - action: list_studios
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Studios }}'
      item:
        CreationTime: '{{ item.CreationTime }}'
        Name: '{{ item.Name }}'
        VpcId: '{{ item.VpcId }}'
        AuthMode: '{{ item.AuthMode }}'
        Description: '{{ item.Description }}'
        Studio: '{{ item.Studio }}'
        StudioId: '{{ item.StudioId }}'
        Url: '{{ item.Url }}'
  # ── Dependent read operations (require identifiers) ──
  # ── DescribeCluster ──
  - discovery_id: aws.emr.describe_cluster
    for_each: aws.emr.list_clusters
    calls:
      - action: describe_cluster
        params:
          ClusterId: '{{ item.ClusterId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Cluster }}'
      item:
        Id: '{{ item.Id }}'
        Name: '{{ item.Name }}'
        ServiceRole: '{{ item.ServiceRole }}'
        Status: '{{ item.Status }}'
        Tags: '{{ item.Tags }}'
        Applications: '{{ item.Applications }}'
        AutoScalingRole: '{{ item.AutoScalingRole }}'
        AutoTerminate: '{{ item.AutoTerminate }}'
        ClusterArn: '{{ item.ClusterArn }}'
        Configurations: '{{ item.Configurations }}'
        CustomAmiId: '{{ item.CustomAmiId }}'
        EbsRootVolumeIops: '{{ item.EbsRootVolumeIops }}'
        EbsRootVolumeSize: '{{ item.EbsRootVolumeSize }}'
        EbsRootVolumeThroughput: '{{ item.EbsRootVolumeThroughput }}'
        Ec2InstanceAttributes: '{{ item.Ec2InstanceAttributes }}'
        ExtendedSupport: '{{ item.ExtendedSupport }}'
        InstanceCollectionType: '{{ item.InstanceCollectionType }}'
        KerberosAttributes: '{{ item.KerberosAttributes }}'
        LogEncryptionKmsKeyId: '{{ item.LogEncryptionKmsKeyId }}'
        LogUri: '{{ item.LogUri }}'
        MasterPublicDnsName: '{{ item.MasterPublicDnsName }}'
        MonitoringConfiguration: '{{ item.MonitoringConfiguration }}'
        NormalizedInstanceHours: '{{ item.NormalizedInstanceHours }}'
        OSReleaseLabel: '{{ item.OSReleaseLabel }}'
        OutpostArn: '{{ item.OutpostArn }}'
        PlacementGroups: '{{ item.PlacementGroups }}'
        ReleaseLabel: '{{ item.ReleaseLabel }}'
        RepoUpgradeOnBoot: '{{ item.RepoUpgradeOnBoot }}'
        RequestedAmiVersion: '{{ item.RequestedAmiVersion }}'
        RunningAmiVersion: '{{ item.RunningAmiVersion }}'
        ScaleDownBehavior: '{{ item.ScaleDownBehavior }}'
        SecurityConfiguration: '{{ item.SecurityConfiguration }}'
        StepConcurrencyLevel: '{{ item.StepConcurrencyLevel }}'
        TerminationProtected: '{{ item.TerminationProtected }}'
        UnhealthyNodeReplacement: '{{ item.UnhealthyNodeReplacement }}'
        VisibleToAllUsers: '{{ item.VisibleToAllUsers }}'
  # ── DescribeNotebookExecution ──
  - discovery_id: aws.emr.describe_notebook_execution
    for_each: aws.emr.list_notebook_executions
    calls:
      - action: describe_notebook_execution
        params:
          NotebookExecutionId: '{{ item.NotebookExecutionId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.NotebookExecution }}'
      item:
        Arn: '{{ item.Arn }}'
        Status: '{{ item.Status }}'
        Tags: '{{ item.Tags }}'
        EditorId: '{{ item.EditorId }}'
        EndTime: '{{ item.EndTime }}'
        EnvironmentVariables: '{{ item.EnvironmentVariables }}'
        ExecutionEngine: '{{ item.ExecutionEngine }}'
        LastStateChangeReason: '{{ item.LastStateChangeReason }}'
        NotebookExecutionId: '{{ item.NotebookExecutionId }}'
        NotebookExecutionName: '{{ item.NotebookExecutionName }}'
        NotebookInstanceSecurityGroupId: '{{ item.NotebookInstanceSecurityGroupId }}'
        NotebookParams: '{{ item.NotebookParams }}'
        NotebookS3Location: '{{ item.NotebookS3Location }}'
        OutputNotebookFormat: '{{ item.OutputNotebookFormat }}'
        OutputNotebookS3Location: '{{ item.OutputNotebookS3Location }}'
        OutputNotebookURI: '{{ item.OutputNotebookURI }}'
        StartTime: '{{ item.StartTime }}'
  # ── DescribePersistentAppUI ──
  - discovery_id: aws.emr.describe_persistent_app_ui
    calls:
      - action: describe_persistent_app_ui
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.PersistentAppUI }}'
      item:
        CreationTime: '{{ item.CreationTime }}'
        LastModifiedTime: '{{ item.LastModifiedTime }}'
        Tags: '{{ item.Tags }}'
        AuthorId: '{{ item.AuthorId }}'
        LastStateChangeReason: '{{ item.LastStateChangeReason }}'
        PersistentAppUIId: '{{ item.PersistentAppUIId }}'
        PersistentAppUIStatus: '{{ item.PersistentAppUIStatus }}'
        PersistentAppUITypeList: '{{ item.PersistentAppUITypeList }}'
  # ── DescribeSecurityConfiguration ──
  - discovery_id: aws.emr.describe_security_configuration
    for_each: aws.emr.describe_job_flows
    calls:
      - action: describe_security_configuration
        params:
          Name: '{{ item.Name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        Name: '{{ response.Name }}'
        CreationDateTime: '{{ response.CreationDateTime }}'
        SecurityConfiguration: '{{ response.SecurityConfiguration }}'
  # ── DescribeStep ──
  - discovery_id: aws.emr.describe_step
    for_each: aws.emr.list_clusters
    calls:
      - action: describe_step
        params:
          ClusterId: '{{ item.ClusterId }}'
          StepId: '{{ item.StepId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Step }}'
      item:
        ExecutionRoleArn: '{{ item.ExecutionRoleArn }}'
        Id: '{{ item.Id }}'
        Name: '{{ item.Name }}'
        Status: '{{ item.Status }}'
        ActionOnFailure: '{{ item.ActionOnFailure }}'
        Config: '{{ item.Config }}'
        EncryptionKeyArn: '{{ item.EncryptionKeyArn }}'
        LogUri: '{{ item.LogUri }}'
  # ── DescribeStudio ──
  - discovery_id: aws.emr.describe_studio
    for_each: aws.emr.list_studio_session_mappings
    calls:
      - action: describe_studio
        params:
          StudioId: '{{ item.StudioId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Studios }}'
      item:
        CreationTime: '{{ item.CreationTime }}'
        Name: '{{ item.Name }}'
        ServiceRole: '{{ item.ServiceRole }}'
        SubnetIds: '{{ item.SubnetIds }}'
        Tags: '{{ item.Tags }}'
        VpcId: '{{ item.VpcId }}'
        AuthMode: '{{ item.AuthMode }}'
        DefaultS3Location: '{{ item.DefaultS3Location }}'
        Description: '{{ item.Description }}'
        EncryptionKeyArn: '{{ item.EncryptionKeyArn }}'
        EngineSecurityGroupId: '{{ item.EngineSecurityGroupId }}'
        IdcInstanceArn: '{{ item.IdcInstanceArn }}'
        IdcUserAssignment: '{{ item.IdcUserAssignment }}'
        IdpAuthUrl: '{{ item.IdpAuthUrl }}'
        IdpRelayStateParameterName: '{{ item.IdpRelayStateParameterName }}'
        StudioArn: '{{ item.StudioArn }}'
        StudioId: '{{ item.StudioId }}'
        TrustedIdentityPropagationEnabled: '{{ item.TrustedIdentityPropagationEnabled }}'
        Url: '{{ item.Url }}'
        UserRole: '{{ item.UserRole }}'
        WorkspaceSecurityGroupId: '{{ item.WorkspaceSecurityGroupId }}'
  # ── GetAutoTerminationPolicy ──
  - discovery_id: aws.emr.get_auto_termination_policy
    for_each: aws.emr.list_clusters
    calls:
      - action: get_auto_termination_policy
        params:
          ClusterId: '{{ item.ClusterId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.AutoTerminationPolicy }}'
      item:
        IdleTimeout: '{{ item.IdleTimeout }}'
  # ── GetClusterSessionCredentials ──
  - discovery_id: aws.emr.get_cluster_session_credentials
    for_each: aws.emr.list_clusters
    calls:
      - action: get_cluster_session_credentials
        params:
          ClusterId: '{{ item.ClusterId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Credentials }}'
      item:
        Credentials: '{{ item.Credentials }}'
        UsernamePassword: '{{ item.UsernamePassword }}'
  # ── GetManagedScalingPolicy ──
  - discovery_id: aws.emr.get_managed_scaling_policy
    for_each: aws.emr.list_clusters
    calls:
      - action: get_managed_scaling_policy
        params:
          ClusterId: '{{ item.ClusterId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.ManagedScalingPolicy }}'
      item:
        ComputeLimits: '{{ item.ComputeLimits }}'
        ScalingStrategy: '{{ item.ScalingStrategy }}'
        UtilizationPerformanceIndex: '{{ item.UtilizationPerformanceIndex }}'
  # ── GetOnClusterAppUIPresignedURL ──
  - discovery_id: aws.emr.get_on_cluster_app_ui_presigned_url
    for_each: aws.emr.list_clusters
    calls:
      - action: get_on_cluster_app_ui_presigned_url
        params:
          ClusterId: '{{ item.ClusterId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        PresignedURL: '{{ response.PresignedURL }}'
        PresignedURLReady: '{{ response.PresignedURLReady }}'
  # ── GetPersistentAppUIPresignedURL ──
  - discovery_id: aws.emr.get_persistent_app_ui_presigned_url
    calls:
      - action: get_persistent_app_ui_presigned_url
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        PresignedURL: '{{ response.PresignedURL }}'
        PresignedURLReady: '{{ response.PresignedURLReady }}'
  # ── GetStudioSessionMapping ──
  - discovery_id: aws.emr.get_studio_session_mapping
    for_each: aws.emr.list_studio_session_mappings
    calls:
      - action: get_studio_session_mapping
        params:
          StudioId: '{{ item.StudioId }}'
          IdentityType: '{{ item.IdentityType }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.SessionMappings }}'
      item:
        CreationTime: '{{ item.CreationTime }}'
        LastModifiedTime: '{{ item.LastModifiedTime }}'
        IdentityId: '{{ item.IdentityId }}'
        IdentityName: '{{ item.IdentityName }}'
        IdentityType: '{{ item.IdentityType }}'
        SessionPolicyArn: '{{ item.SessionPolicyArn }}'
        StudioId: '{{ item.StudioId }}'
  # ── ListBootstrapActions ──
  - discovery_id: aws.emr.list_bootstrap_actions
    for_each: aws.emr.list_clusters
    calls:
      - action: list_bootstrap_actions
        params:
          ClusterId: '{{ item.ClusterId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.BootstrapActions }}'
      item:
        Name: '{{ item.Name }}'
        Args: '{{ item.Args }}'
        ScriptPath: '{{ item.ScriptPath }}'
  # ── ListInstanceFleets ──
  - discovery_id: aws.emr.list_instance_fleets
    for_each: aws.emr.list_clusters
    calls:
      - action: list_instance_fleets
        params:
          ClusterId: '{{ item.ClusterId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.InstanceFleets }}'
      item:
        Id: '{{ item.Id }}'
        Name: '{{ item.Name }}'
        Status: '{{ item.Status }}'
        Context: '{{ item.Context }}'
        InstanceFleetType: '{{ item.InstanceFleetType }}'
        InstanceTypeSpecifications: '{{ item.InstanceTypeSpecifications }}'
        LaunchSpecifications: '{{ item.LaunchSpecifications }}'
        ProvisionedOnDemandCapacity: '{{ item.ProvisionedOnDemandCapacity }}'
        ProvisionedSpotCapacity: '{{ item.ProvisionedSpotCapacity }}'
        ResizeSpecifications: '{{ item.ResizeSpecifications }}'
        TargetOnDemandCapacity: '{{ item.TargetOnDemandCapacity }}'
        TargetSpotCapacity: '{{ item.TargetSpotCapacity }}'
  # ── ListInstanceGroups ──
  - discovery_id: aws.emr.list_instance_groups
    for_each: aws.emr.list_clusters
    calls:
      - action: list_instance_groups
        params:
          ClusterId: '{{ item.ClusterId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.InstanceGroups }}'
      item:
        Id: '{{ item.Id }}'
        Name: '{{ item.Name }}'
        Status: '{{ item.Status }}'
        AutoScalingPolicy: '{{ item.AutoScalingPolicy }}'
        BidPrice: '{{ item.BidPrice }}'
        Configurations: '{{ item.Configurations }}'
        ConfigurationsVersion: '{{ item.ConfigurationsVersion }}'
        CustomAmiId: '{{ item.CustomAmiId }}'
        EbsBlockDevices: '{{ item.EbsBlockDevices }}'
        EbsOptimized: '{{ item.EbsOptimized }}'
        InstanceGroupType: '{{ item.InstanceGroupType }}'
        InstanceType: '{{ item.InstanceType }}'
        LastSuccessfullyAppliedConfigurations: '{{ item.LastSuccessfullyAppliedConfigurations }}'
        LastSuccessfullyAppliedConfigurationsVersion: '{{ item.LastSuccessfullyAppliedConfigurationsVersion }}'
        Market: '{{ item.Market }}'
        RequestedInstanceCount: '{{ item.RequestedInstanceCount }}'
        RunningInstanceCount: '{{ item.RunningInstanceCount }}'
        ShrinkPolicy: '{{ item.ShrinkPolicy }}'
  # ── ListInstances ──
  - discovery_id: aws.emr.list_instances
    for_each: aws.emr.list_clusters
    calls:
      - action: list_instances
        params:
          ClusterId: '{{ item.ClusterId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Instances }}'
      item:
        Id: '{{ item.Id }}'
        PublicIpAddress: '{{ item.PublicIpAddress }}'
        Status: '{{ item.Status }}'
        EbsVolumes: '{{ item.EbsVolumes }}'
        Ec2InstanceId: '{{ item.Ec2InstanceId }}'
        InstanceFleetId: '{{ item.InstanceFleetId }}'
        InstanceGroupId: '{{ item.InstanceGroupId }}'
        InstanceType: '{{ item.InstanceType }}'
        Market: '{{ item.Market }}'
        PrivateDnsName: '{{ item.PrivateDnsName }}'
        PrivateIpAddress: '{{ item.PrivateIpAddress }}'
        PublicDnsName: '{{ item.PublicDnsName }}'
  # ── ListSteps ──
  - discovery_id: aws.emr.list_steps
    for_each: aws.emr.list_clusters
    calls:
      - action: list_steps
        params:
          ClusterId: '{{ item.ClusterId }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Steps }}'
      item:
        Id: '{{ item.Id }}'
        Name: '{{ item.Name }}'
        Status: '{{ item.Status }}'
        ActionOnFailure: '{{ item.ActionOnFailure }}'
        Config: '{{ item.Config }}'
        EncryptionKeyArn: '{{ item.EncryptionKeyArn }}'
        LogUri: '{{ item.LogUri }}'
  # ── ListSupportedInstanceTypes ──
  - discovery_id: aws.emr.list_supported_instance_types
    for_each: aws.emr.describe_release_label
    calls:
      - action: list_supported_instance_types
        params:
          ReleaseLabel: '{{ item.ReleaseLabel }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.SupportedInstanceTypes }}'
      item:
        Architecture: '{{ item.Architecture }}'
        EbsOptimizedAvailable: '{{ item.EbsOptimizedAvailable }}'
        EbsOptimizedByDefault: '{{ item.EbsOptimizedByDefault }}'
        EbsStorageOnly: '{{ item.EbsStorageOnly }}'
        InstanceFamilyId: '{{ item.InstanceFamilyId }}'
        Is64BitsOnly: '{{ item.Is64BitsOnly }}'
        MemoryGB: '{{ item.MemoryGB }}'
        NumberOfDisks: '{{ item.NumberOfDisks }}'
        StorageGB: '{{ item.StorageGB }}'
        Type: '{{ item.Type }}'
        VCPU: '{{ item.VCPU }}'
