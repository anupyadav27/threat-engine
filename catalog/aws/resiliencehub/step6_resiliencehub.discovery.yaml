# ============================================================
# Discovery YAML — resiliencehub (AWS)
# Generated: 2026-02-21T04:56:59Z
# Actions use boto3 SDK snake_case method names
# ============================================================
version: '1.0'
provider: aws
service: resiliencehub
is_active: false
services:
  client: resiliencehub
  module: boto3.client
discovery:
  # ── ListAppAssessments ──
  - discovery_id: aws.resiliencehub.list_app_assessments
    calls:
      - action: list_app_assessments
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.assessmentSummaries }}'
      item:
        actualReferenceId: '{{ item.actualReferenceId }}'
        actualValue: '{{ item.actualValue }}'
        appArn: '{{ item.appArn }}'
        appComponentNames: '{{ item.appComponentNames }}'
        appId: '{{ item.appId }}'
        appVersion: '{{ item.appVersion }}'
        assessment: '{{ item.assessment }}'
        assessmentArn: '{{ item.assessmentArn }}'
        assessmentName: '{{ item.assessmentName }}'
        assessmentStatus: '{{ item.assessmentStatus }}'
        compliance: '{{ item.compliance }}'
        complianceStatus: '{{ item.complianceStatus }}'
        configRecommendations: '{{ item.configRecommendations }}'
        cost: '{{ item.cost }}'
        dependsOnAlarms: '{{ item.dependsOnAlarms }}'
        diffType: '{{ item.diffType }}'
        driftStatus: '{{ item.driftStatus }}'
        driftType: '{{ item.driftType }}'
        endTime: '{{ item.endTime }}'
        entityId: '{{ item.entityId }}'
        entityType: '{{ item.entityType }}'
        errorDetails: '{{ item.errorDetails }}'
        eventSubscriptions: '{{ item.eventSubscriptions }}'
        expectedReferenceId: '{{ item.expectedReferenceId }}'
        expectedValue: '{{ item.expectedValue }}'
        groupingId: '{{ item.groupingId }}'
        id: '{{ item.id }}'
        identifier: '{{ item.identifier }}'
        intent: '{{ item.intent }}'
        invoker: '{{ item.invoker }}'
        items: '{{ item.items }}'
        lastDriftEvaluationTime: '{{ item.lastDriftEvaluationTime }}'
        lastResiliencyScoreEvaluationTime: '{{ item.lastResiliencyScoreEvaluationTime }}'
        message: '{{ item.message }}'
        permissionModel: '{{ item.permissionModel }}'
        prerequisite: '{{ item.prerequisite }}'
        recommendationId: '{{ item.recommendationId }}'
        recommendationStatus: '{{ item.recommendationStatus }}'
        referenceId: '{{ item.referenceId }}'
        resiliencyScore: '{{ item.resiliencyScore }}'
        resourceErrorsDetails: '{{ item.resourceErrorsDetails }}'
        resourceIdentifier: '{{ item.resourceIdentifier }}'
        risk: '{{ item.risk }}'
        serviceType: '{{ item.serviceType }}'
        startTime: '{{ item.startTime }}'
        statusChangeTime: '{{ item.statusChangeTime }}'
        versionName: '{{ item.versionName }}'
  # ── ListApps ──
  - discovery_id: aws.resiliencehub.list_apps
    calls:
      - action: list_apps
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.appSummaries }}'
      item:
        appArn: '{{ item.appArn }}'
        assessmentSchedule: '{{ item.assessmentSchedule }}'
        awsApplicationArn: '{{ item.awsApplicationArn }}'
        complianceStatus: '{{ item.complianceStatus }}'
        creationTime: '{{ item.creationTime }}'
        description: '{{ item.description }}'
        driftStatus: '{{ item.driftStatus }}'
        lastAppComplianceEvaluationTime: '{{ item.lastAppComplianceEvaluationTime }}'
        name: '{{ item.name }}'
        resiliencyScore: '{{ item.resiliencyScore }}'
        rpoInSecs: '{{ item.rpoInSecs }}'
        rtoInSecs: '{{ item.rtoInSecs }}'
        status: '{{ item.status }}'
  # ── ListMetrics ──
  - discovery_id: aws.resiliencehub.list_metrics
    calls:
      - action: list_metrics
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.rows }}'
  # ── ListRecommendationTemplates ──
  - discovery_id: aws.resiliencehub.list_recommendation_templates
    calls:
      - action: list_recommendation_templates
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.recommendationTemplates }}'
      item:
        appArn: '{{ item.appArn }}'
        assessmentArn: '{{ item.assessmentArn }}'
        endTime: '{{ item.endTime }}'
        format: '{{ item.format }}'
        message: '{{ item.message }}'
        name: '{{ item.name }}'
        needsReplacements: '{{ item.needsReplacements }}'
        recommendationIds: '{{ item.recommendationIds }}'
        recommendationTemplateArn: '{{ item.recommendationTemplateArn }}'
        recommendationTypes: '{{ item.recommendationTypes }}'
        startTime: '{{ item.startTime }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
        templatesLocation: '{{ item.templatesLocation }}'
  # ── ListResiliencyPolicies ──
  - discovery_id: aws.resiliencehub.list_resiliency_policies
    calls:
      - action: list_resiliency_policies
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.resiliencyPolicies }}'
      item:
        Statement: '{{ item.Statement }}'
        Version: '{{ item.Version }}'
        creationTime: '{{ item.creationTime }}'
        dataLocationConstraint: '{{ item.dataLocationConstraint }}'
        estimatedCostTier: '{{ item.estimatedCostTier }}'
        policy: '{{ item.policy }}'
        policyArn: '{{ item.policyArn }}'
        policyDescription: '{{ item.policyDescription }}'
        policyName: '{{ item.policyName }}'
        tags: '{{ item.tags }}'
        tier: '{{ item.tier }}'
  # ── ListResourceGroupingRecommendations ──
  - discovery_id: aws.resiliencehub.list_resource_grouping_recommendations
    calls:
      - action: list_resource_grouping_recommendations
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.groupingRecommendations }}'
      item:
        appComponent: '{{ item.appComponent }}'
        confidenceLevel: '{{ item.confidenceLevel }}'
        creationTime: '{{ item.creationTime }}'
        groupingAppComponent: '{{ item.groupingAppComponent }}'
        groupingRecommendationId: '{{ item.groupingRecommendationId }}'
        recommendationReasons: '{{ item.recommendationReasons }}'
        rejectionReason: '{{ item.rejectionReason }}'
        resources: '{{ item.resources }}'
        score: '{{ item.score }}'
        status: '{{ item.status }}'
  # ── ListSuggestedResiliencyPolicies ──
  - discovery_id: aws.resiliencehub.list_suggested_resiliency_policies
    calls:
      - action: list_suggested_resiliency_policies
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.resiliencyPolicies }}'
      item:
        creationTime: '{{ item.creationTime }}'
        dataLocationConstraint: '{{ item.dataLocationConstraint }}'
        estimatedCostTier: '{{ item.estimatedCostTier }}'
        policy: '{{ item.policy }}'
        policyArn: '{{ item.policyArn }}'
        policyDescription: '{{ item.policyDescription }}'
        policyName: '{{ item.policyName }}'
        tags: '{{ item.tags }}'
        tier: '{{ item.tier }}'
  # ── Dependent read operations (require identifiers) ──
  # ── DescribeApp ──
  - discovery_id: aws.resiliencehub.describe_app
    for_each: aws.resiliencehub.list_app_assessments
    calls:
      - action: describe_app
        params:
          appArn: '{{ item.appArn }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.assessmentSummaries }}'
      item:
        appArn: '{{ item.appArn }}'
        assessmentSchedule: '{{ item.assessmentSchedule }}'
        awsApplicationArn: '{{ item.awsApplicationArn }}'
        complianceStatus: '{{ item.complianceStatus }}'
        creationTime: '{{ item.creationTime }}'
        description: '{{ item.description }}'
        driftStatus: '{{ item.driftStatus }}'
        eventSubscriptions: '{{ item.eventSubscriptions }}'
        lastAppComplianceEvaluationTime: '{{ item.lastAppComplianceEvaluationTime }}'
        lastDriftEvaluationTime: '{{ item.lastDriftEvaluationTime }}'
        lastResiliencyScoreEvaluationTime: '{{ item.lastResiliencyScoreEvaluationTime }}'
        name: '{{ item.name }}'
        permissionModel: '{{ item.permissionModel }}'
        policyArn: '{{ item.policyArn }}'
        resiliencyScore: '{{ item.resiliencyScore }}'
        rpoInSecs: '{{ item.rpoInSecs }}'
        rtoInSecs: '{{ item.rtoInSecs }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── DescribeAppAssessment ──
  - discovery_id: aws.resiliencehub.describe_app_assessment
    for_each: aws.resiliencehub.list_app_assessments
    calls:
      - action: describe_app_assessment
        params:
          assessmentArn: '{{ item.assessmentArn }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.assessmentSummaries }}'
      item:
        appArn: '{{ item.appArn }}'
        appVersion: '{{ item.appVersion }}'
        assessment: '{{ item.assessment }}'
        assessmentArn: '{{ item.assessmentArn }}'
        assessmentName: '{{ item.assessmentName }}'
        assessmentStatus: '{{ item.assessmentStatus }}'
        compliance: '{{ item.compliance }}'
        complianceStatus: '{{ item.complianceStatus }}'
        cost: '{{ item.cost }}'
        driftStatus: '{{ item.driftStatus }}'
        endTime: '{{ item.endTime }}'
        invoker: '{{ item.invoker }}'
        message: '{{ item.message }}'
        policy: '{{ item.policy }}'
        resiliencyScore: '{{ item.resiliencyScore }}'
        resourceErrorsDetails: '{{ item.resourceErrorsDetails }}'
        startTime: '{{ item.startTime }}'
        summary: '{{ item.summary }}'
        tags: '{{ item.tags }}'
        versionName: '{{ item.versionName }}'
  # ── DescribeAppVersion ──
  - discovery_id: aws.resiliencehub.describe_app_version
    for_each: aws.resiliencehub.list_app_assessments
    calls:
      - action: describe_app_version
        params:
          appArn: '{{ item.appArn }}'
          appVersion: '{{ item.appVersion }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        additionalInfo: '{{ response.additionalInfo }}'
        appArn: '{{ response.appArn }}'
        appVersion: '{{ response.appVersion }}'
  # ── DescribeAppVersionAppComponent ──
  - discovery_id: aws.resiliencehub.describe_app_version_app_component
    for_each: aws.resiliencehub.list_app_assessments
    calls:
      - action: describe_app_version_app_component
        params:
          appArn: '{{ item.appArn }}'
          appVersion: '{{ item.appVersion }}'
          id: '{{ item.id }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.appComponent }}'
      item:
        additionalInfo: '{{ item.additionalInfo }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
  # ── DescribeAppVersionResource ──
  - discovery_id: aws.resiliencehub.describe_app_version_resource
    for_each: aws.resiliencehub.list_app_assessments
    calls:
      - action: describe_app_version_resource
        params:
          appArn: '{{ item.appArn }}'
          appVersion: '{{ item.appVersion }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.appComponent }}'
      item:
        additionalInfo: '{{ item.additionalInfo }}'
        appComponents: '{{ item.appComponents }}'
        excluded: '{{ item.excluded }}'
        logicalResourceId: '{{ item.logicalResourceId }}'
        parentResourceName: '{{ item.parentResourceName }}'
        physicalResourceId: '{{ item.physicalResourceId }}'
        resourceName: '{{ item.resourceName }}'
        resourceType: '{{ item.resourceType }}'
        sourceType: '{{ item.sourceType }}'
  # ── DescribeAppVersionResourcesResolutionStatus ──
  - discovery_id: aws.resiliencehub.describe_app_version_resources_resolution_status
    for_each: aws.resiliencehub.list_app_assessments
    calls:
      - action: describe_app_version_resources_resolution_status
        params:
          appArn: '{{ item.appArn }}'
          appVersion: '{{ item.appVersion }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        appArn: '{{ response.appArn }}'
        appVersion: '{{ response.appVersion }}'
        errorMessage: '{{ response.errorMessage }}'
        resolutionId: '{{ response.resolutionId }}'
        status: '{{ response.status }}'
  # ── DescribeAppVersionTemplate ──
  - discovery_id: aws.resiliencehub.describe_app_version_template
    for_each: aws.resiliencehub.list_app_assessments
    calls:
      - action: describe_app_version_template
        params:
          appArn: '{{ item.appArn }}'
          appVersion: '{{ item.appVersion }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        appArn: '{{ response.appArn }}'
        appTemplateBody: '{{ response.appTemplateBody }}'
        appVersion: '{{ response.appVersion }}'
  # ── DescribeDraftAppVersionResourcesImportStatus ──
  - discovery_id: aws.resiliencehub.describe_draft_app_version_resources_import_status
    for_each: aws.resiliencehub.list_app_assessments
    calls:
      - action: describe_draft_app_version_resources_import_status
        params:
          appArn: '{{ item.appArn }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.errorDetails }}'
      item:
        errorDetails: '{{ item.errorDetails }}'
        errorMessage: '{{ item.errorMessage }}'
        statusChangeTime: '{{ item.statusChangeTime }}'
  # ── DescribeMetricsExport ──
  - discovery_id: aws.resiliencehub.describe_metrics_export
    calls:
      - action: describe_metrics_export
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.exportLocation }}'
      item:
        bucket: '{{ item.bucket }}'
        prefix: '{{ item.prefix }}'
  # ── DescribeResiliencyPolicy ──
  - discovery_id: aws.resiliencehub.describe_resiliency_policy
    for_each: aws.resiliencehub.list_resiliency_policies
    calls:
      - action: describe_resiliency_policy
        params:
          policyArn: '{{ item.policyArn }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.appSummaries }}'
      item:
        creationTime: '{{ item.creationTime }}'
        dataLocationConstraint: '{{ item.dataLocationConstraint }}'
        estimatedCostTier: '{{ item.estimatedCostTier }}'
        policy: '{{ item.policy }}'
        policyArn: '{{ item.policyArn }}'
        policyDescription: '{{ item.policyDescription }}'
        policyName: '{{ item.policyName }}'
        tags: '{{ item.tags }}'
        tier: '{{ item.tier }}'
  # ── DescribeResourceGroupingRecommendationTask ──
  - discovery_id: aws.resiliencehub.describe_resource_grouping_recommendation_task
    for_each: aws.resiliencehub.list_app_assessments
    calls:
      - action: describe_resource_grouping_recommendation_task
        params:
          appArn: '{{ item.appArn }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.assessmentSummaries }}'
      item:
        groupingId: '{{ item.groupingId }}'
  # ── ListAlarmRecommendations ──
  - discovery_id: aws.resiliencehub.list_alarm_recommendations
    for_each: aws.resiliencehub.list_app_assessments
    calls:
      - action: list_alarm_recommendations
        params:
          assessmentArn: '{{ item.assessmentArn }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.alarmRecommendations }}'
      item:
        appComponentName: '{{ item.appComponentName }}'
        appComponentNames: '{{ item.appComponentNames }}'
        description: '{{ item.description }}'
        items: '{{ item.items }}'
        name: '{{ item.name }}'
        prerequisite: '{{ item.prerequisite }}'
        recommendationId: '{{ item.recommendationId }}'
        recommendationStatus: '{{ item.recommendationStatus }}'
        referenceId: '{{ item.referenceId }}'
        type: '{{ item.type }}'
  # ── ListAppAssessmentComplianceDrifts ──
  - discovery_id: aws.resiliencehub.list_app_assessment_compliance_drifts
    for_each: aws.resiliencehub.list_app_assessments
    calls:
      - action: list_app_assessment_compliance_drifts
        params:
          assessmentArn: '{{ item.assessmentArn }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.complianceDrifts }}'
      item:
        actualReferenceId: '{{ item.actualReferenceId }}'
        actualValue: '{{ item.actualValue }}'
        appId: '{{ item.appId }}'
        appVersion: '{{ item.appVersion }}'
        diffType: '{{ item.diffType }}'
        driftType: '{{ item.driftType }}'
        entityId: '{{ item.entityId }}'
        entityType: '{{ item.entityType }}'
        expectedReferenceId: '{{ item.expectedReferenceId }}'
        expectedValue: '{{ item.expectedValue }}'
  # ── ListAppAssessmentResourceDrifts ──
  - discovery_id: aws.resiliencehub.list_app_assessment_resource_drifts
    for_each: aws.resiliencehub.list_app_assessments
    calls:
      - action: list_app_assessment_resource_drifts
        params:
          assessmentArn: '{{ item.assessmentArn }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.resourceDrifts }}'
      item:
        appArn: '{{ item.appArn }}'
        appVersion: '{{ item.appVersion }}'
        diffType: '{{ item.diffType }}'
        referenceId: '{{ item.referenceId }}'
        resourceIdentifier: '{{ item.resourceIdentifier }}'
  # ── ListAppComponentCompliances ──
  - discovery_id: aws.resiliencehub.list_app_component_compliances
    for_each: aws.resiliencehub.list_app_assessments
    calls:
      - action: list_app_component_compliances
        params:
          assessmentArn: '{{ item.assessmentArn }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.componentCompliances }}'
      item:
        appComponentName: '{{ item.appComponentName }}'
        compliance: '{{ item.compliance }}'
        cost: '{{ item.cost }}'
        message: '{{ item.message }}'
        resiliencyScore: '{{ item.resiliencyScore }}'
        status: '{{ item.status }}'
  # ── ListAppComponentRecommendations ──
  - discovery_id: aws.resiliencehub.list_app_component_recommendations
    for_each: aws.resiliencehub.list_app_assessments
    calls:
      - action: list_app_component_recommendations
        params:
          assessmentArn: '{{ item.assessmentArn }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.componentRecommendations }}'
      item:
        appComponentName: '{{ item.appComponentName }}'
        configRecommendations: '{{ item.configRecommendations }}'
        recommendationStatus: '{{ item.recommendationStatus }}'
  # ── ListAppInputSources ──
  - discovery_id: aws.resiliencehub.list_app_input_sources
    for_each: aws.resiliencehub.list_app_assessments
    calls:
      - action: list_app_input_sources
        params:
          appArn: '{{ item.appArn }}'
          appVersion: '{{ item.appVersion }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.appInputSources }}'
      item:
        eksSourceClusterNamespace: '{{ item.eksSourceClusterNamespace }}'
        importType: '{{ item.importType }}'
        resourceCount: '{{ item.resourceCount }}'
        sourceArn: '{{ item.sourceArn }}'
        sourceName: '{{ item.sourceName }}'
        terraformSource: '{{ item.terraformSource }}'
  # ── ListAppVersionAppComponents ──
  - discovery_id: aws.resiliencehub.list_app_version_app_components
    for_each: aws.resiliencehub.list_app_assessments
    calls:
      - action: list_app_version_app_components
        params:
          appArn: '{{ item.appArn }}'
          appVersion: '{{ item.appVersion }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.appComponents }}'
      item:
        additionalInfo: '{{ item.additionalInfo }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
  # ── ListAppVersionResourceMappings ──
  - discovery_id: aws.resiliencehub.list_app_version_resource_mappings
    for_each: aws.resiliencehub.list_app_assessments
    calls:
      - action: list_app_version_resource_mappings
        params:
          appArn: '{{ item.appArn }}'
          appVersion: '{{ item.appVersion }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.resourceMappings }}'
      item:
        appRegistryAppName: '{{ item.appRegistryAppName }}'
        eksSourceName: '{{ item.eksSourceName }}'
        logicalStackName: '{{ item.logicalStackName }}'
        mappingType: '{{ item.mappingType }}'
        physicalResourceId: '{{ item.physicalResourceId }}'
        resourceGroupName: '{{ item.resourceGroupName }}'
        resourceName: '{{ item.resourceName }}'
        terraformSourceName: '{{ item.terraformSourceName }}'
  # ── ListAppVersionResources ──
  - discovery_id: aws.resiliencehub.list_app_version_resources
    for_each: aws.resiliencehub.list_app_assessments
    calls:
      - action: list_app_version_resources
        params:
          appArn: '{{ item.appArn }}'
          appVersion: '{{ item.appVersion }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.physicalResources }}'
      item:
        additionalInfo: '{{ item.additionalInfo }}'
        appComponents: '{{ item.appComponents }}'
        excluded: '{{ item.excluded }}'
        logicalResourceId: '{{ item.logicalResourceId }}'
        parentResourceName: '{{ item.parentResourceName }}'
        physicalResourceId: '{{ item.physicalResourceId }}'
        resourceName: '{{ item.resourceName }}'
        resourceType: '{{ item.resourceType }}'
        sourceType: '{{ item.sourceType }}'
  # ── ListAppVersions ──
  - discovery_id: aws.resiliencehub.list_app_versions
    for_each: aws.resiliencehub.list_app_assessments
    calls:
      - action: list_app_versions
        params:
          appArn: '{{ item.appArn }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.appVersions }}'
      item:
        appVersion: '{{ item.appVersion }}'
        creationTime: '{{ item.creationTime }}'
        identifier: '{{ item.identifier }}'
        versionName: '{{ item.versionName }}'
  # ── ListSopRecommendations ──
  - discovery_id: aws.resiliencehub.list_sop_recommendations
    for_each: aws.resiliencehub.list_app_assessments
    calls:
      - action: list_sop_recommendations
        params:
          assessmentArn: '{{ item.assessmentArn }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.sopRecommendations }}'
      item:
        appComponentName: '{{ item.appComponentName }}'
        description: '{{ item.description }}'
        items: '{{ item.items }}'
        name: '{{ item.name }}'
        prerequisite: '{{ item.prerequisite }}'
        recommendationId: '{{ item.recommendationId }}'
        recommendationStatus: '{{ item.recommendationStatus }}'
        referenceId: '{{ item.referenceId }}'
        serviceType: '{{ item.serviceType }}'
  # ── ListTagsForResource ──
  - discovery_id: aws.resiliencehub.list_tags_for_resource
    calls:
      - action: list_tags_for_resource
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        tags: '{{ response.tags }}'
  # ── ListTestRecommendations ──
  - discovery_id: aws.resiliencehub.list_test_recommendations
    for_each: aws.resiliencehub.list_app_assessments
    calls:
      - action: list_test_recommendations
        params:
          assessmentArn: '{{ item.assessmentArn }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.testRecommendations }}'
      item:
        appComponentId: '{{ item.appComponentId }}'
        appComponentName: '{{ item.appComponentName }}'
        dependsOnAlarms: '{{ item.dependsOnAlarms }}'
        description: '{{ item.description }}'
        id: '{{ item.id }}'
        intent: '{{ item.intent }}'
        items: '{{ item.items }}'
        name: '{{ item.name }}'
        prerequisite: '{{ item.prerequisite }}'
        recommendationId: '{{ item.recommendationId }}'
        recommendationStatus: '{{ item.recommendationStatus }}'
        referenceId: '{{ item.referenceId }}'
        risk: '{{ item.risk }}'
        type: '{{ item.type }}'
  # ── ListUnsupportedAppVersionResources ──
  - discovery_id: aws.resiliencehub.list_unsupported_app_version_resources
    for_each: aws.resiliencehub.list_app_assessments
    calls:
      - action: list_unsupported_app_version_resources
        params:
          appArn: '{{ item.appArn }}'
          appVersion: '{{ item.appVersion }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.unsupportedResources }}'
      item:
        logicalResourceId: '{{ item.logicalResourceId }}'
        physicalResourceId: '{{ item.physicalResourceId }}'
        resourceType: '{{ item.resourceType }}'
        unsupportedResourceStatus: '{{ item.unsupportedResourceStatus }}'
