# Discovery YAML — enterprise_billing_units (IBM Cloud)
# Generated: 2026-02-21T04:45:38Z
version: '1.0'
provider: ibm
service: enterprise_billing_units
services:
  client: enterprise_billing_units
  module: ibm_platform_services.enterprise_billing_units
discovery:
  # ── list_billing_units ──
  - discovery_id: ibm.enterprise_billing_units.list-billing-units
    calls:
      - action: list-billing-units
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── Dependent ops ──
  # ── get_billing_unit [dependent] ──
  - discovery_id: ibm.enterprise_billing_units.get-billing-unit
    calls:
      - action: get-billing-unit
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_credit_pools [dependent] ──
  - discovery_id: ibm.enterprise_billing_units.get-credit-pools
    calls:
      - action: get-credit-pools
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── list_billing_options [dependent] ──
  - discovery_id: ibm.enterprise_billing_units.list-billing-options
    calls:
      - action: list-billing-options
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
