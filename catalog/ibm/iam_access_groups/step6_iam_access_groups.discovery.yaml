# Discovery YAML — iam_access_groups (IBM Cloud)
# Generated: 2026-02-21T04:45:39Z
version: '1.0'
provider: ibm
service: iam_access_groups
services:
  client: iam_access_groups
  module: ibm_platform_services.iam_access_groups
discovery:
  # ── Dependent ops ──
  # ── get_access_group [dependent] ──
  - discovery_id: ibm.iam_access_groups.get-access-group
    calls:
      - action: get-access-group
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_access_group_rule [dependent] ──
  - discovery_id: ibm.iam_access_groups.get-access-group-rule
    calls:
      - action: get-access-group-rule
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_account_settings [dependent] ──
  - discovery_id: ibm.iam_access_groups.get-account-settings
    calls:
      - action: get-account-settings
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_assignment [dependent] ──
  - discovery_id: ibm.iam_access_groups.get-assignment
    calls:
      - action: get-assignment
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_latest_template_version [dependent] ──
  - discovery_id: ibm.iam_access_groups.get-latest-template-version
    calls:
      - action: get-latest-template-version
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_template_version [dependent] ──
  - discovery_id: ibm.iam_access_groups.get-template-version
    calls:
      - action: get-template-version
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── list_access_group_members [dependent] ──
  - discovery_id: ibm.iam_access_groups.list-access-group-members
    calls:
      - action: list-access-group-members
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_access_group_rules [dependent] ──
  - discovery_id: ibm.iam_access_groups.list-access-group-rules
    calls:
      - action: list-access-group-rules
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_access_groups [dependent] ──
  - discovery_id: ibm.iam_access_groups.list-access-groups
    calls:
      - action: list-access-groups
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_assignments [dependent] ──
  - discovery_id: ibm.iam_access_groups.list-assignments
    calls:
      - action: list-assignments
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_template_versions [dependent] ──
  - discovery_id: ibm.iam_access_groups.list-template-versions
    calls:
      - action: list-template-versions
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_templates [dependent] ──
  - discovery_id: ibm.iam_access_groups.list-templates
    calls:
      - action: list-templates
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
