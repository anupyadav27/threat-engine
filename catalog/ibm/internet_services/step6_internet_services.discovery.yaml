# Discovery YAML — internet_services (IBM Cloud)
# Generated: 2026-02-21T04:45:39Z
version: '1.0'
provider: ibm
service: internet_services
services:
  client: internet_services
  module: ibm_platform_services.internet_services
discovery:
  # ── get_service ──
  - discovery_id: ibm.internet_services.get-service
    calls:
      - action: get-service
        save_as: response
        on_error: continue
    emit:
      as: item
  # ── list_services ──
  - discovery_id: ibm.internet_services.list-services
    calls:
      - action: list-services
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
