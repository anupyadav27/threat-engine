# Discovery YAML — partner_management (IBM Cloud)
# Generated: 2026-02-21T04:45:39Z
version: '1.0'
provider: ibm
service: partner_management
services:
  client: partner_management
  module: ibm_platform_services.partner_management
discovery:
  # ── Dependent ops ──
  # ── get_billing_options [dependent] ──
  - discovery_id: ibm.partner_management.get-billing-options
    calls:
      - action: get-billing-options
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_credit_pools_report [dependent] ──
  - discovery_id: ibm.partner_management.get-credit-pools-report
    calls:
      - action: get-credit-pools-report
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_resource_usage_report [dependent] ──
  - discovery_id: ibm.partner_management.get-resource-usage-report
    calls:
      - action: get-resource-usage-report
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
