version: '1.0'
provider: ibm
service: stub
services:
  client: stub
  module: boto3.client
discovery:
- discovery_id: ibm.stub.listStub
  calls:
  - action: listStub
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.stub.listStubResources
  calls:
  - action: listStubResources
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.stub.getStub
  calls:
  - action: getStub
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.stub.getStubDetails
  calls:
  - action: getStubDetails
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
checks: []
