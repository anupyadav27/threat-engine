# Discovery YAML — iam_policy_management (IBM Cloud)
# Generated: 2026-02-21T04:45:39Z
version: '1.0'
provider: ibm
service: iam_policy_management
services:
  client: iam_policy_management
  module: ibm_platform_services.iam_policy_management
discovery:
  # ── list_roles ──
  - discovery_id: ibm.iam_policy_management.list-roles
    calls:
      - action: list-roles
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── Dependent ops ──
  # ── get_action_control_assignment [dependent] ──
  - discovery_id: ibm.iam_policy_management.get-action-control-assignment
    calls:
      - action: get-action-control-assignment
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_action_control_template [dependent] ──
  - discovery_id: ibm.iam_policy_management.get-action-control-template
    calls:
      - action: get-action-control-template
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_action_control_template_version [dependent] ──
  - discovery_id: ibm.iam_policy_management.get-action-control-template-version
    calls:
      - action: get-action-control-template-version
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_policy [dependent] ──
  - discovery_id: ibm.iam_policy_management.get-policy
    calls:
      - action: get-policy
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_policy_assignment [dependent] ──
  - discovery_id: ibm.iam_policy_management.get-policy-assignment
    calls:
      - action: get-policy-assignment
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_policy_template [dependent] ──
  - discovery_id: ibm.iam_policy_management.get-policy-template
    calls:
      - action: get-policy-template
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_policy_template_version [dependent] ──
  - discovery_id: ibm.iam_policy_management.get-policy-template-version
    calls:
      - action: get-policy-template-version
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_role [dependent] ──
  - discovery_id: ibm.iam_policy_management.get-role
    calls:
      - action: get-role
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_role_assignment [dependent] ──
  - discovery_id: ibm.iam_policy_management.get-role-assignment
    calls:
      - action: get-role-assignment
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_role_template [dependent] ──
  - discovery_id: ibm.iam_policy_management.get-role-template
    calls:
      - action: get-role-template
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_role_template_version [dependent] ──
  - discovery_id: ibm.iam_policy_management.get-role-template-version
    calls:
      - action: get-role-template-version
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_settings [dependent] ──
  - discovery_id: ibm.iam_policy_management.get-settings
    calls:
      - action: get-settings
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_v2_policy [dependent] ──
  - discovery_id: ibm.iam_policy_management.get-v2-policy
    calls:
      - action: get-v2-policy
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── list_action_control_assignments [dependent] ──
  - discovery_id: ibm.iam_policy_management.list-action-control-assignments
    calls:
      - action: list-action-control-assignments
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_action_control_template_versions [dependent] ──
  - discovery_id: ibm.iam_policy_management.list-action-control-template-versions
    calls:
      - action: list-action-control-template-versions
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_action_control_templates [dependent] ──
  - discovery_id: ibm.iam_policy_management.list-action-control-templates
    calls:
      - action: list-action-control-templates
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_policies [dependent] ──
  - discovery_id: ibm.iam_policy_management.list-policies
    calls:
      - action: list-policies
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_policy_assignments [dependent] ──
  - discovery_id: ibm.iam_policy_management.list-policy-assignments
    calls:
      - action: list-policy-assignments
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_policy_template_versions [dependent] ──
  - discovery_id: ibm.iam_policy_management.list-policy-template-versions
    calls:
      - action: list-policy-template-versions
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_policy_templates [dependent] ──
  - discovery_id: ibm.iam_policy_management.list-policy-templates
    calls:
      - action: list-policy-templates
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_role_assignments [dependent] ──
  - discovery_id: ibm.iam_policy_management.list-role-assignments
    calls:
      - action: list-role-assignments
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_role_template_versions [dependent] ──
  - discovery_id: ibm.iam_policy_management.list-role-template-versions
    calls:
      - action: list-role-template-versions
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_role_templates [dependent] ──
  - discovery_id: ibm.iam_policy_management.list-role-templates
    calls:
      - action: list-role-templates
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_v2_policies [dependent] ──
  - discovery_id: ibm.iam_policy_management.list-v2-policies
    calls:
      - action: list-v2-policies
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
