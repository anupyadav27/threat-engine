# Discovery YAML — resource_controller (IBM Cloud)
# Generated: 2026-02-21T04:45:39Z
version: '1.0'
provider: ibm
service: resource_controller
services:
  client: resource_controller
  module: ibm_platform_services.resource_controller
discovery:
  # ── list_reclamations ──
  - discovery_id: ibm.resource_controller.list-reclamations
    calls:
      - action: list-reclamations
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_resource_aliases ──
  - discovery_id: ibm.resource_controller.list-resource-aliases
    calls:
      - action: list-resource-aliases
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_resource_bindings ──
  - discovery_id: ibm.resource_controller.list-resource-bindings
    calls:
      - action: list-resource-bindings
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_resource_instances ──
  - discovery_id: ibm.resource_controller.list-resource-instances
    calls:
      - action: list-resource-instances
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_resource_keys ──
  - discovery_id: ibm.resource_controller.list-resource-keys
    calls:
      - action: list-resource-keys
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── Dependent ops ──
  # ── get_resource_alias [dependent] ──
  - discovery_id: ibm.resource_controller.get-resource-alias
    calls:
      - action: get-resource-alias
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_resource_binding [dependent] ──
  - discovery_id: ibm.resource_controller.get-resource-binding
    calls:
      - action: get-resource-binding
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_resource_instance [dependent] ──
  - discovery_id: ibm.resource_controller.get-resource-instance
    calls:
      - action: get-resource-instance
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_resource_key [dependent] ──
  - discovery_id: ibm.resource_controller.get-resource-key
    calls:
      - action: get-resource-key
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── list_resource_aliases_for_instance [dependent] ──
  - discovery_id: ibm.resource_controller.list-resource-aliases-for-instance
    calls:
      - action: list-resource-aliases-for-instance
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_resource_bindings_for_alias [dependent] ──
  - discovery_id: ibm.resource_controller.list-resource-bindings-for-alias
    calls:
      - action: list-resource-bindings-for-alias
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_resource_keys_for_instance [dependent] ──
  - discovery_id: ibm.resource_controller.list-resource-keys-for-instance
    calls:
      - action: list-resource-keys-for-instance
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
