# Discovery YAML — global_search (IBM Cloud)
# Generated: 2026-02-21T04:45:39Z
version: '1.0'
provider: ibm
service: global_search
services:
  client: global_search
  module: ibm_platform_services.global_search
discovery:
  # ── search ──
  - discovery_id: ibm.global_search.search
    calls:
      - action: search
        save_as: response
        on_error: continue
    emit:
      as: item
