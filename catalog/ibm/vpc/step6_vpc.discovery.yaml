# Discovery YAML — vpc (IBM Cloud)
# Generated: 2026-02-21T04:45:39Z
version: '1.0'
provider: ibm
service: vpc
services:
  client: vpc
  module: ibm_platform_services.vpc
discovery:
  # ── list_backup_policies ──
  - discovery_id: ibm.vpc.list-backup-policies
    calls:
      - action: list-backup-policies
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_bare_metal_server_profiles ──
  - discovery_id: ibm.vpc.list-bare-metal-server-profiles
    calls:
      - action: list-bare-metal-server-profiles
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_bare_metal_servers ──
  - discovery_id: ibm.vpc.list-bare-metal-servers
    calls:
      - action: list-bare-metal-servers
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_cluster_network_profiles ──
  - discovery_id: ibm.vpc.list-cluster-network-profiles
    calls:
      - action: list-cluster-network-profiles
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_cluster_networks ──
  - discovery_id: ibm.vpc.list-cluster-networks
    calls:
      - action: list-cluster-networks
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_dedicated_host_groups ──
  - discovery_id: ibm.vpc.list-dedicated-host-groups
    calls:
      - action: list-dedicated-host-groups
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_dedicated_host_profiles ──
  - discovery_id: ibm.vpc.list-dedicated-host-profiles
    calls:
      - action: list-dedicated-host-profiles
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_dedicated_hosts ──
  - discovery_id: ibm.vpc.list-dedicated-hosts
    calls:
      - action: list-dedicated-hosts
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_endpoint_gateways ──
  - discovery_id: ibm.vpc.list-endpoint-gateways
    calls:
      - action: list-endpoint-gateways
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_floating_ips ──
  - discovery_id: ibm.vpc.list-floating-ips
    calls:
      - action: list-floating-ips
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_flow_log_collectors ──
  - discovery_id: ibm.vpc.list-flow-log-collectors
    calls:
      - action: list-flow-log-collectors
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_ike_policies ──
  - discovery_id: ibm.vpc.list-ike-policies
    calls:
      - action: list-ike-policies
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_images ──
  - discovery_id: ibm.vpc.list-images
    calls:
      - action: list-images
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_instance_groups ──
  - discovery_id: ibm.vpc.list-instance-groups
    calls:
      - action: list-instance-groups
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_instance_profiles ──
  - discovery_id: ibm.vpc.list-instance-profiles
    calls:
      - action: list-instance-profiles
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_instance_templates ──
  - discovery_id: ibm.vpc.list-instance-templates
    calls:
      - action: list-instance-templates
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_instances ──
  - discovery_id: ibm.vpc.list-instances
    calls:
      - action: list-instances
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_ipsec_policies ──
  - discovery_id: ibm.vpc.list-ipsec-policies
    calls:
      - action: list-ipsec-policies
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_keys ──
  - discovery_id: ibm.vpc.list-keys
    calls:
      - action: list-keys
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_load_balancer_profiles ──
  - discovery_id: ibm.vpc.list-load-balancer-profiles
    calls:
      - action: list-load-balancer-profiles
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_load_balancers ──
  - discovery_id: ibm.vpc.list-load-balancers
    calls:
      - action: list-load-balancers
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_network_acls ──
  - discovery_id: ibm.vpc.list-network-acls
    calls:
      - action: list-network-acls
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_operating_systems ──
  - discovery_id: ibm.vpc.list-operating-systems
    calls:
      - action: list-operating-systems
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_placement_groups ──
  - discovery_id: ibm.vpc.list-placement-groups
    calls:
      - action: list-placement-groups
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_private_path_service_gateways ──
  - discovery_id: ibm.vpc.list-private-path-service-gateways
    calls:
      - action: list-private-path-service-gateways
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_public_address_ranges ──
  - discovery_id: ibm.vpc.list-public-address-ranges
    calls:
      - action: list-public-address-ranges
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_public_gateways ──
  - discovery_id: ibm.vpc.list-public-gateways
    calls:
      - action: list-public-gateways
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_regions ──
  - discovery_id: ibm.vpc.list-regions
    calls:
      - action: list-regions
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_reservations ──
  - discovery_id: ibm.vpc.list-reservations
    calls:
      - action: list-reservations
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_security_groups ──
  - discovery_id: ibm.vpc.list-security-groups
    calls:
      - action: list-security-groups
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_share_profiles ──
  - discovery_id: ibm.vpc.list-share-profiles
    calls:
      - action: list-share-profiles
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_shares ──
  - discovery_id: ibm.vpc.list-shares
    calls:
      - action: list-shares
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_snapshot_consistency_groups ──
  - discovery_id: ibm.vpc.list-snapshot-consistency-groups
    calls:
      - action: list-snapshot-consistency-groups
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_snapshots ──
  - discovery_id: ibm.vpc.list-snapshots
    calls:
      - action: list-snapshots
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_subnets ──
  - discovery_id: ibm.vpc.list-subnets
    calls:
      - action: list-subnets
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_virtual_network_interfaces ──
  - discovery_id: ibm.vpc.list-virtual-network-interfaces
    calls:
      - action: list-virtual-network-interfaces
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_volume_profiles ──
  - discovery_id: ibm.vpc.list-volume-profiles
    calls:
      - action: list-volume-profiles
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_volumes ──
  - discovery_id: ibm.vpc.list-volumes
    calls:
      - action: list-volumes
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_vpcs ──
  - discovery_id: ibm.vpc.list-vpcs
    calls:
      - action: list-vpcs
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_vpn_gateways ──
  - discovery_id: ibm.vpc.list-vpn-gateways
    calls:
      - action: list-vpn-gateways
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_vpn_servers ──
  - discovery_id: ibm.vpc.list-vpn-servers
    calls:
      - action: list-vpn-servers
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── Dependent ops ──
  # ── get_backup_policy [dependent] ──
  - discovery_id: ibm.vpc.get-backup-policy
    calls:
      - action: get-backup-policy
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_backup_policy_job [dependent] ──
  - discovery_id: ibm.vpc.get-backup-policy-job
    calls:
      - action: get-backup-policy-job
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_backup_policy_plan [dependent] ──
  - discovery_id: ibm.vpc.get-backup-policy-plan
    calls:
      - action: get-backup-policy-plan
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_bare_metal_server [dependent] ──
  - discovery_id: ibm.vpc.get-bare-metal-server
    calls:
      - action: get-bare-metal-server
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_bare_metal_server_disk [dependent] ──
  - discovery_id: ibm.vpc.get-bare-metal-server-disk
    calls:
      - action: get-bare-metal-server-disk
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_bare_metal_server_initialization [dependent] ──
  - discovery_id: ibm.vpc.get-bare-metal-server-initialization
    calls:
      - action: get-bare-metal-server-initialization
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_bare_metal_server_network_attachment [dependent] ──
  - discovery_id: ibm.vpc.get-bare-metal-server-network-attachment
    calls:
      - action: get-bare-metal-server-network-attachment
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_bare_metal_server_network_interface [dependent] ──
  - discovery_id: ibm.vpc.get-bare-metal-server-network-interface
    calls:
      - action: get-bare-metal-server-network-interface
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_bare_metal_server_network_interface_floating_ip [dependent] ──
  - discovery_id: ibm.vpc.get-bare-metal-server-network-interface-floating-ip
    calls:
      - action: get-bare-metal-server-network-interface-floating-ip
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_bare_metal_server_network_interface_ip [dependent] ──
  - discovery_id: ibm.vpc.get-bare-metal-server-network-interface-ip
    calls:
      - action: get-bare-metal-server-network-interface-ip
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_bare_metal_server_profile [dependent] ──
  - discovery_id: ibm.vpc.get-bare-metal-server-profile
    calls:
      - action: get-bare-metal-server-profile
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_cluster_network [dependent] ──
  - discovery_id: ibm.vpc.get-cluster-network
    calls:
      - action: get-cluster-network
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_cluster_network_interface [dependent] ──
  - discovery_id: ibm.vpc.get-cluster-network-interface
    calls:
      - action: get-cluster-network-interface
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_cluster_network_profile [dependent] ──
  - discovery_id: ibm.vpc.get-cluster-network-profile
    calls:
      - action: get-cluster-network-profile
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_cluster_network_subnet [dependent] ──
  - discovery_id: ibm.vpc.get-cluster-network-subnet
    calls:
      - action: get-cluster-network-subnet
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_cluster_network_subnet_reserved_ip [dependent] ──
  - discovery_id: ibm.vpc.get-cluster-network-subnet-reserved-ip
    calls:
      - action: get-cluster-network-subnet-reserved-ip
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_dedicated_host [dependent] ──
  - discovery_id: ibm.vpc.get-dedicated-host
    calls:
      - action: get-dedicated-host
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_dedicated_host_disk [dependent] ──
  - discovery_id: ibm.vpc.get-dedicated-host-disk
    calls:
      - action: get-dedicated-host-disk
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_dedicated_host_group [dependent] ──
  - discovery_id: ibm.vpc.get-dedicated-host-group
    calls:
      - action: get-dedicated-host-group
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_dedicated_host_profile [dependent] ──
  - discovery_id: ibm.vpc.get-dedicated-host-profile
    calls:
      - action: get-dedicated-host-profile
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_endpoint_gateway [dependent] ──
  - discovery_id: ibm.vpc.get-endpoint-gateway
    calls:
      - action: get-endpoint-gateway
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_endpoint_gateway_ip [dependent] ──
  - discovery_id: ibm.vpc.get-endpoint-gateway-ip
    calls:
      - action: get-endpoint-gateway-ip
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_endpoint_gateway_resource_binding [dependent] ──
  - discovery_id: ibm.vpc.get-endpoint-gateway-resource-binding
    calls:
      - action: get-endpoint-gateway-resource-binding
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_floating_ip [dependent] ──
  - discovery_id: ibm.vpc.get-floating-ip
    calls:
      - action: get-floating-ip
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_flow_log_collector [dependent] ──
  - discovery_id: ibm.vpc.get-flow-log-collector
    calls:
      - action: get-flow-log-collector
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_ike_policy [dependent] ──
  - discovery_id: ibm.vpc.get-ike-policy
    calls:
      - action: get-ike-policy
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_image [dependent] ──
  - discovery_id: ibm.vpc.get-image
    calls:
      - action: get-image
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_image_export_job [dependent] ──
  - discovery_id: ibm.vpc.get-image-export-job
    calls:
      - action: get-image-export-job
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_instance [dependent] ──
  - discovery_id: ibm.vpc.get-instance
    calls:
      - action: get-instance
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_instance_cluster_network_attachment [dependent] ──
  - discovery_id: ibm.vpc.get-instance-cluster-network-attachment
    calls:
      - action: get-instance-cluster-network-attachment
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_instance_disk [dependent] ──
  - discovery_id: ibm.vpc.get-instance-disk
    calls:
      - action: get-instance-disk
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_instance_group [dependent] ──
  - discovery_id: ibm.vpc.get-instance-group
    calls:
      - action: get-instance-group
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_instance_group_manager [dependent] ──
  - discovery_id: ibm.vpc.get-instance-group-manager
    calls:
      - action: get-instance-group-manager
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_instance_group_manager_action [dependent] ──
  - discovery_id: ibm.vpc.get-instance-group-manager-action
    calls:
      - action: get-instance-group-manager-action
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_instance_group_manager_policy [dependent] ──
  - discovery_id: ibm.vpc.get-instance-group-manager-policy
    calls:
      - action: get-instance-group-manager-policy
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_instance_group_membership [dependent] ──
  - discovery_id: ibm.vpc.get-instance-group-membership
    calls:
      - action: get-instance-group-membership
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_instance_initialization [dependent] ──
  - discovery_id: ibm.vpc.get-instance-initialization
    calls:
      - action: get-instance-initialization
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_instance_network_attachment [dependent] ──
  - discovery_id: ibm.vpc.get-instance-network-attachment
    calls:
      - action: get-instance-network-attachment
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_instance_network_interface [dependent] ──
  - discovery_id: ibm.vpc.get-instance-network-interface
    calls:
      - action: get-instance-network-interface
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_instance_network_interface_floating_ip [dependent] ──
  - discovery_id: ibm.vpc.get-instance-network-interface-floating-ip
    calls:
      - action: get-instance-network-interface-floating-ip
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_instance_network_interface_ip [dependent] ──
  - discovery_id: ibm.vpc.get-instance-network-interface-ip
    calls:
      - action: get-instance-network-interface-ip
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_instance_profile [dependent] ──
  - discovery_id: ibm.vpc.get-instance-profile
    calls:
      - action: get-instance-profile
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_instance_template [dependent] ──
  - discovery_id: ibm.vpc.get-instance-template
    calls:
      - action: get-instance-template
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_instance_volume_attachment [dependent] ──
  - discovery_id: ibm.vpc.get-instance-volume-attachment
    calls:
      - action: get-instance-volume-attachment
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_ipsec_policy [dependent] ──
  - discovery_id: ibm.vpc.get-ipsec-policy
    calls:
      - action: get-ipsec-policy
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_key [dependent] ──
  - discovery_id: ibm.vpc.get-key
    calls:
      - action: get-key
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_load_balancer [dependent] ──
  - discovery_id: ibm.vpc.get-load-balancer
    calls:
      - action: get-load-balancer
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_load_balancer_listener [dependent] ──
  - discovery_id: ibm.vpc.get-load-balancer-listener
    calls:
      - action: get-load-balancer-listener
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_load_balancer_listener_policy [dependent] ──
  - discovery_id: ibm.vpc.get-load-balancer-listener-policy
    calls:
      - action: get-load-balancer-listener-policy
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_load_balancer_listener_policy_rule [dependent] ──
  - discovery_id: ibm.vpc.get-load-balancer-listener-policy-rule
    calls:
      - action: get-load-balancer-listener-policy-rule
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_load_balancer_pool [dependent] ──
  - discovery_id: ibm.vpc.get-load-balancer-pool
    calls:
      - action: get-load-balancer-pool
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_load_balancer_pool_member [dependent] ──
  - discovery_id: ibm.vpc.get-load-balancer-pool-member
    calls:
      - action: get-load-balancer-pool-member
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_load_balancer_profile [dependent] ──
  - discovery_id: ibm.vpc.get-load-balancer-profile
    calls:
      - action: get-load-balancer-profile
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_load_balancer_statistics [dependent] ──
  - discovery_id: ibm.vpc.get-load-balancer-statistics
    calls:
      - action: get-load-balancer-statistics
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_network_acl [dependent] ──
  - discovery_id: ibm.vpc.get-network-acl
    calls:
      - action: get-network-acl
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_network_acl_rule [dependent] ──
  - discovery_id: ibm.vpc.get-network-acl-rule
    calls:
      - action: get-network-acl-rule
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_network_interface_floating_ip [dependent] ──
  - discovery_id: ibm.vpc.get-network-interface-floating-ip
    calls:
      - action: get-network-interface-floating-ip
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_operating_system [dependent] ──
  - discovery_id: ibm.vpc.get-operating-system
    calls:
      - action: get-operating-system
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_placement_group [dependent] ──
  - discovery_id: ibm.vpc.get-placement-group
    calls:
      - action: get-placement-group
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_private_path_service_gateway [dependent] ──
  - discovery_id: ibm.vpc.get-private-path-service-gateway
    calls:
      - action: get-private-path-service-gateway
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_private_path_service_gateway_account_policy [dependent] ──
  - discovery_id: ibm.vpc.get-private-path-service-gateway-account-policy
    calls:
      - action: get-private-path-service-gateway-account-policy
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_private_path_service_gateway_endpoint_gateway_binding [dependent] ──
  - discovery_id: ibm.vpc.get-private-path-service-gateway-endpoint-gateway-binding
    calls:
      - action: get-private-path-service-gateway-endpoint-gateway-binding
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_public_address_range [dependent] ──
  - discovery_id: ibm.vpc.get-public-address-range
    calls:
      - action: get-public-address-range
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_public_gateway [dependent] ──
  - discovery_id: ibm.vpc.get-public-gateway
    calls:
      - action: get-public-gateway
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_region [dependent] ──
  - discovery_id: ibm.vpc.get-region
    calls:
      - action: get-region
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_region_zone [dependent] ──
  - discovery_id: ibm.vpc.get-region-zone
    calls:
      - action: get-region-zone
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_reservation [dependent] ──
  - discovery_id: ibm.vpc.get-reservation
    calls:
      - action: get-reservation
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_security_group [dependent] ──
  - discovery_id: ibm.vpc.get-security-group
    calls:
      - action: get-security-group
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_security_group_rule [dependent] ──
  - discovery_id: ibm.vpc.get-security-group-rule
    calls:
      - action: get-security-group-rule
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_security_group_target [dependent] ──
  - discovery_id: ibm.vpc.get-security-group-target
    calls:
      - action: get-security-group-target
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_share [dependent] ──
  - discovery_id: ibm.vpc.get-share
    calls:
      - action: get-share
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_share_accessor_binding [dependent] ──
  - discovery_id: ibm.vpc.get-share-accessor-binding
    calls:
      - action: get-share-accessor-binding
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_share_mount_target [dependent] ──
  - discovery_id: ibm.vpc.get-share-mount-target
    calls:
      - action: get-share-mount-target
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_share_profile [dependent] ──
  - discovery_id: ibm.vpc.get-share-profile
    calls:
      - action: get-share-profile
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_share_snapshot [dependent] ──
  - discovery_id: ibm.vpc.get-share-snapshot
    calls:
      - action: get-share-snapshot
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_share_source [dependent] ──
  - discovery_id: ibm.vpc.get-share-source
    calls:
      - action: get-share-source
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_snapshot [dependent] ──
  - discovery_id: ibm.vpc.get-snapshot
    calls:
      - action: get-snapshot
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_snapshot_clone [dependent] ──
  - discovery_id: ibm.vpc.get-snapshot-clone
    calls:
      - action: get-snapshot-clone
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_snapshot_consistency_group [dependent] ──
  - discovery_id: ibm.vpc.get-snapshot-consistency-group
    calls:
      - action: get-snapshot-consistency-group
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_subnet [dependent] ──
  - discovery_id: ibm.vpc.get-subnet
    calls:
      - action: get-subnet
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_subnet_network_acl [dependent] ──
  - discovery_id: ibm.vpc.get-subnet-network-acl
    calls:
      - action: get-subnet-network-acl
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_subnet_public_gateway [dependent] ──
  - discovery_id: ibm.vpc.get-subnet-public-gateway
    calls:
      - action: get-subnet-public-gateway
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_subnet_reserved_ip [dependent] ──
  - discovery_id: ibm.vpc.get-subnet-reserved-ip
    calls:
      - action: get-subnet-reserved-ip
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_subnet_routing_table [dependent] ──
  - discovery_id: ibm.vpc.get-subnet-routing-table
    calls:
      - action: get-subnet-routing-table
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_virtual_network_interface [dependent] ──
  - discovery_id: ibm.vpc.get-virtual-network-interface
    calls:
      - action: get-virtual-network-interface
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_virtual_network_interface_ip [dependent] ──
  - discovery_id: ibm.vpc.get-virtual-network-interface-ip
    calls:
      - action: get-virtual-network-interface-ip
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_volume [dependent] ──
  - discovery_id: ibm.vpc.get-volume
    calls:
      - action: get-volume
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_volume_profile [dependent] ──
  - discovery_id: ibm.vpc.get-volume-profile
    calls:
      - action: get-volume-profile
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_vpc [dependent] ──
  - discovery_id: ibm.vpc.get-vpc
    calls:
      - action: get-vpc
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_vpc_address_prefix [dependent] ──
  - discovery_id: ibm.vpc.get-vpc-address-prefix
    calls:
      - action: get-vpc-address-prefix
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_vpc_default_network_acl [dependent] ──
  - discovery_id: ibm.vpc.get-vpc-default-network-acl
    calls:
      - action: get-vpc-default-network-acl
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_vpc_default_routing_table [dependent] ──
  - discovery_id: ibm.vpc.get-vpc-default-routing-table
    calls:
      - action: get-vpc-default-routing-table
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_vpc_default_security_group [dependent] ──
  - discovery_id: ibm.vpc.get-vpc-default-security-group
    calls:
      - action: get-vpc-default-security-group
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_vpc_dns_resolution_binding [dependent] ──
  - discovery_id: ibm.vpc.get-vpc-dns-resolution-binding
    calls:
      - action: get-vpc-dns-resolution-binding
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_vpc_route [dependent] ──
  - discovery_id: ibm.vpc.get-vpc-route
    calls:
      - action: get-vpc-route
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_vpc_routing_table [dependent] ──
  - discovery_id: ibm.vpc.get-vpc-routing-table
    calls:
      - action: get-vpc-routing-table
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_vpc_routing_table_route [dependent] ──
  - discovery_id: ibm.vpc.get-vpc-routing-table-route
    calls:
      - action: get-vpc-routing-table-route
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_vpn_gateway [dependent] ──
  - discovery_id: ibm.vpc.get-vpn-gateway
    calls:
      - action: get-vpn-gateway
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_vpn_gateway_connection [dependent] ──
  - discovery_id: ibm.vpc.get-vpn-gateway-connection
    calls:
      - action: get-vpn-gateway-connection
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_vpn_gateway_service_connection [dependent] ──
  - discovery_id: ibm.vpc.get-vpn-gateway-service-connection
    calls:
      - action: get-vpn-gateway-service-connection
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_vpn_server [dependent] ──
  - discovery_id: ibm.vpc.get-vpn-server
    calls:
      - action: get-vpn-server
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_vpn_server_client [dependent] ──
  - discovery_id: ibm.vpc.get-vpn-server-client
    calls:
      - action: get-vpn-server-client
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_vpn_server_client_configuration [dependent] ──
  - discovery_id: ibm.vpc.get-vpn-server-client-configuration
    calls:
      - action: get-vpn-server-client-configuration
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_vpn_server_route [dependent] ──
  - discovery_id: ibm.vpc.get-vpn-server-route
    calls:
      - action: get-vpn-server-route
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── list_backup_policy_jobs [dependent] ──
  - discovery_id: ibm.vpc.list-backup-policy-jobs
    calls:
      - action: list-backup-policy-jobs
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_backup_policy_plans [dependent] ──
  - discovery_id: ibm.vpc.list-backup-policy-plans
    calls:
      - action: list-backup-policy-plans
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_bare_metal_server_disks [dependent] ──
  - discovery_id: ibm.vpc.list-bare-metal-server-disks
    calls:
      - action: list-bare-metal-server-disks
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_bare_metal_server_network_attachments [dependent] ──
  - discovery_id: ibm.vpc.list-bare-metal-server-network-attachments
    calls:
      - action: list-bare-metal-server-network-attachments
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_bare_metal_server_network_interface_floating_ips [dependent] ──
  - discovery_id: ibm.vpc.list-bare-metal-server-network-interface-floating-ips
    calls:
      - action: list-bare-metal-server-network-interface-floating-ips
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_bare_metal_server_network_interface_ips [dependent] ──
  - discovery_id: ibm.vpc.list-bare-metal-server-network-interface-ips
    calls:
      - action: list-bare-metal-server-network-interface-ips
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_bare_metal_server_network_interfaces [dependent] ──
  - discovery_id: ibm.vpc.list-bare-metal-server-network-interfaces
    calls:
      - action: list-bare-metal-server-network-interfaces
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_cluster_network_interfaces [dependent] ──
  - discovery_id: ibm.vpc.list-cluster-network-interfaces
    calls:
      - action: list-cluster-network-interfaces
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_cluster_network_subnet_reserved_ips [dependent] ──
  - discovery_id: ibm.vpc.list-cluster-network-subnet-reserved-ips
    calls:
      - action: list-cluster-network-subnet-reserved-ips
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_cluster_network_subnets [dependent] ──
  - discovery_id: ibm.vpc.list-cluster-network-subnets
    calls:
      - action: list-cluster-network-subnets
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_dedicated_host_disks [dependent] ──
  - discovery_id: ibm.vpc.list-dedicated-host-disks
    calls:
      - action: list-dedicated-host-disks
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_endpoint_gateway_ips [dependent] ──
  - discovery_id: ibm.vpc.list-endpoint-gateway-ips
    calls:
      - action: list-endpoint-gateway-ips
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_endpoint_gateway_resource_bindings [dependent] ──
  - discovery_id: ibm.vpc.list-endpoint-gateway-resource-bindings
    calls:
      - action: list-endpoint-gateway-resource-bindings
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_ike_policy_connections [dependent] ──
  - discovery_id: ibm.vpc.list-ike-policy-connections
    calls:
      - action: list-ike-policy-connections
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_image_bare_metal_server_profiles [dependent] ──
  - discovery_id: ibm.vpc.list-image-bare-metal-server-profiles
    calls:
      - action: list-image-bare-metal-server-profiles
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_image_export_jobs [dependent] ──
  - discovery_id: ibm.vpc.list-image-export-jobs
    calls:
      - action: list-image-export-jobs
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_image_instance_profiles [dependent] ──
  - discovery_id: ibm.vpc.list-image-instance-profiles
    calls:
      - action: list-image-instance-profiles
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_instance_cluster_network_attachments [dependent] ──
  - discovery_id: ibm.vpc.list-instance-cluster-network-attachments
    calls:
      - action: list-instance-cluster-network-attachments
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_instance_disks [dependent] ──
  - discovery_id: ibm.vpc.list-instance-disks
    calls:
      - action: list-instance-disks
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_instance_group_manager_actions [dependent] ──
  - discovery_id: ibm.vpc.list-instance-group-manager-actions
    calls:
      - action: list-instance-group-manager-actions
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_instance_group_manager_policies [dependent] ──
  - discovery_id: ibm.vpc.list-instance-group-manager-policies
    calls:
      - action: list-instance-group-manager-policies
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_instance_group_managers [dependent] ──
  - discovery_id: ibm.vpc.list-instance-group-managers
    calls:
      - action: list-instance-group-managers
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_instance_group_memberships [dependent] ──
  - discovery_id: ibm.vpc.list-instance-group-memberships
    calls:
      - action: list-instance-group-memberships
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_instance_network_attachments [dependent] ──
  - discovery_id: ibm.vpc.list-instance-network-attachments
    calls:
      - action: list-instance-network-attachments
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_instance_network_interface_floating_ips [dependent] ──
  - discovery_id: ibm.vpc.list-instance-network-interface-floating-ips
    calls:
      - action: list-instance-network-interface-floating-ips
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_instance_network_interface_ips [dependent] ──
  - discovery_id: ibm.vpc.list-instance-network-interface-ips
    calls:
      - action: list-instance-network-interface-ips
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_instance_network_interfaces [dependent] ──
  - discovery_id: ibm.vpc.list-instance-network-interfaces
    calls:
      - action: list-instance-network-interfaces
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_instance_volume_attachments [dependent] ──
  - discovery_id: ibm.vpc.list-instance-volume-attachments
    calls:
      - action: list-instance-volume-attachments
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_ipsec_policy_connections [dependent] ──
  - discovery_id: ibm.vpc.list-ipsec-policy-connections
    calls:
      - action: list-ipsec-policy-connections
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_load_balancer_listener_policies [dependent] ──
  - discovery_id: ibm.vpc.list-load-balancer-listener-policies
    calls:
      - action: list-load-balancer-listener-policies
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_load_balancer_listener_policy_rules [dependent] ──
  - discovery_id: ibm.vpc.list-load-balancer-listener-policy-rules
    calls:
      - action: list-load-balancer-listener-policy-rules
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_load_balancer_listeners [dependent] ──
  - discovery_id: ibm.vpc.list-load-balancer-listeners
    calls:
      - action: list-load-balancer-listeners
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_load_balancer_pool_members [dependent] ──
  - discovery_id: ibm.vpc.list-load-balancer-pool-members
    calls:
      - action: list-load-balancer-pool-members
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_load_balancer_pools [dependent] ──
  - discovery_id: ibm.vpc.list-load-balancer-pools
    calls:
      - action: list-load-balancer-pools
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_network_acl_rules [dependent] ──
  - discovery_id: ibm.vpc.list-network-acl-rules
    calls:
      - action: list-network-acl-rules
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_network_interface_floating_ips [dependent] ──
  - discovery_id: ibm.vpc.list-network-interface-floating-ips
    calls:
      - action: list-network-interface-floating-ips
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_private_path_service_gateway_account_policies [dependent] ──
  - discovery_id: ibm.vpc.list-private-path-service-gateway-account-policies
    calls:
      - action: list-private-path-service-gateway-account-policies
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_private_path_service_gateway_endpoint_gateway_bindings [dependent] ──
  - discovery_id: ibm.vpc.list-private-path-service-gateway-endpoint-gateway-bindings
    calls:
      - action: list-private-path-service-gateway-endpoint-gateway-bindings
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_region_zones [dependent] ──
  - discovery_id: ibm.vpc.list-region-zones
    calls:
      - action: list-region-zones
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_security_group_rules [dependent] ──
  - discovery_id: ibm.vpc.list-security-group-rules
    calls:
      - action: list-security-group-rules
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_security_group_targets [dependent] ──
  - discovery_id: ibm.vpc.list-security-group-targets
    calls:
      - action: list-security-group-targets
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_share_accessor_bindings [dependent] ──
  - discovery_id: ibm.vpc.list-share-accessor-bindings
    calls:
      - action: list-share-accessor-bindings
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_share_mount_targets [dependent] ──
  - discovery_id: ibm.vpc.list-share-mount-targets
    calls:
      - action: list-share-mount-targets
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_share_snapshots [dependent] ──
  - discovery_id: ibm.vpc.list-share-snapshots
    calls:
      - action: list-share-snapshots
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_snapshot_clones [dependent] ──
  - discovery_id: ibm.vpc.list-snapshot-clones
    calls:
      - action: list-snapshot-clones
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_snapshot_instance_profiles [dependent] ──
  - discovery_id: ibm.vpc.list-snapshot-instance-profiles
    calls:
      - action: list-snapshot-instance-profiles
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_subnet_reserved_ips [dependent] ──
  - discovery_id: ibm.vpc.list-subnet-reserved-ips
    calls:
      - action: list-subnet-reserved-ips
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_virtual_network_interface_ips [dependent] ──
  - discovery_id: ibm.vpc.list-virtual-network-interface-ips
    calls:
      - action: list-virtual-network-interface-ips
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_volume_instance_profiles [dependent] ──
  - discovery_id: ibm.vpc.list-volume-instance-profiles
    calls:
      - action: list-volume-instance-profiles
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_vpc_address_prefixes [dependent] ──
  - discovery_id: ibm.vpc.list-vpc-address-prefixes
    calls:
      - action: list-vpc-address-prefixes
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_vpc_dns_resolution_bindings [dependent] ──
  - discovery_id: ibm.vpc.list-vpc-dns-resolution-bindings
    calls:
      - action: list-vpc-dns-resolution-bindings
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_vpc_routes [dependent] ──
  - discovery_id: ibm.vpc.list-vpc-routes
    calls:
      - action: list-vpc-routes
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_vpc_routing_table_routes [dependent] ──
  - discovery_id: ibm.vpc.list-vpc-routing-table-routes
    calls:
      - action: list-vpc-routing-table-routes
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_vpc_routing_tables [dependent] ──
  - discovery_id: ibm.vpc.list-vpc-routing-tables
    calls:
      - action: list-vpc-routing-tables
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_vpn_gateway_advertised_cidrs [dependent] ──
  - discovery_id: ibm.vpc.list-vpn-gateway-advertised-cidrs
    calls:
      - action: list-vpn-gateway-advertised-cidrs
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_vpn_gateway_connections [dependent] ──
  - discovery_id: ibm.vpc.list-vpn-gateway-connections
    calls:
      - action: list-vpn-gateway-connections
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_vpn_gateway_connections_local_cidrs [dependent] ──
  - discovery_id: ibm.vpc.list-vpn-gateway-connections-local-cidrs
    calls:
      - action: list-vpn-gateway-connections-local-cidrs
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_vpn_gateway_connections_peer_cidrs [dependent] ──
  - discovery_id: ibm.vpc.list-vpn-gateway-connections-peer-cidrs
    calls:
      - action: list-vpn-gateway-connections-peer-cidrs
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_vpn_gateway_service_connections [dependent] ──
  - discovery_id: ibm.vpc.list-vpn-gateway-service-connections
    calls:
      - action: list-vpn-gateway-service-connections
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_vpn_server_clients [dependent] ──
  - discovery_id: ibm.vpc.list-vpn-server-clients
    calls:
      - action: list-vpn-server-clients
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_vpn_server_routes [dependent] ──
  - discovery_id: ibm.vpc.list-vpn-server-routes
    calls:
      - action: list-vpn-server-routes
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
