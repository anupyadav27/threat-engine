# Discovery YAML — event_notifications (IBM Cloud)
# Generated: 2026-02-21T04:45:39Z
version: '1.0'
provider: ibm
service: event_notifications
services:
  client: event_notifications
  module: ibm_platform_services.event_notifications
discovery:
  # ── get_topic ──
  - discovery_id: ibm.event_notifications.get-topic
    calls:
      - action: get-topic
        save_as: response
        on_error: continue
    emit:
      as: item
  # ── list_topics ──
  - discovery_id: ibm.event_notifications.list-topics
    calls:
      - action: list-topics
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
