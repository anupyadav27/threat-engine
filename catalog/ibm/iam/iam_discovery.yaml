version: '1.0'
provider: ibm
service: iam
services:
  client: iam
  module: ibm_cloud_sdk.iam
discovery:
- discovery_id: ibm.iam.list-api-keys
  calls:
  - action: list-api-keys
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.iam.list-claim-rules
  calls:
  - action: list-claim-rules
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.iam.list-versions-of-account-settings-template
  calls:
  - action: list-versions-of-account-settings-template
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.iam.list-service-id-group
  calls:
  - action: list-service-id-group
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.iam.list-links
  calls:
  - action: list-links
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.iam.list-profiles
  calls:
  - action: list-profiles
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.iam.list-account-settings-templates
  calls:
  - action: list-account-settings-templates
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.iam.list-trusted-profile-assignments
  calls:
  - action: list-trusted-profile-assignments
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.iam.list-account-settings-assignments
  calls:
  - action: list-account-settings-assignments
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.iam.list-versions-of-profile-template
  calls:
  - action: list-versions-of-profile-template
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.iam.list-service-ids
  calls:
  - action: list-service-ids
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.iam.list-profile-templates
  calls:
  - action: list-profile-templates
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.iam.get-api-keys-details
  calls:
  - action: get-api-keys-details
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.iam.get-authenticator
  calls:
  - action: get-authenticator
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.iam.get-account-settings-template-version
  calls:
  - action: get-account-settings-template-version
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.iam.get-http-client
  calls:
  - action: get-http-client
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.iam.get-mfa-status
  calls:
  - action: get-mfa-status
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.iam.get-account-settings
  calls:
  - action: get-account-settings
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.iam.get-mfa-report
  calls:
  - action: get-mfa-report
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.iam.get-preferences-on-scope-account
  calls:
  - action: get-preferences-on-scope-account
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.iam.get-enable-gzip-compression
  calls:
  - action: get-enable-gzip-compression
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.iam.get-claim-rule
  calls:
  - action: get-claim-rule
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.iam.get-all-preferences-on-scope-account
  calls:
  - action: get-all-preferences-on-scope-account
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.iam.get-profile-identities
  calls:
  - action: get-profile-identities
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.iam.get-latest-profile-template-version
  calls:
  - action: get-latest-profile-template-version
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.iam.get-report
  calls:
  - action: get-report
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.iam.get-latest-account-settings-template-version
  calls:
  - action: get-latest-account-settings-template-version
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.iam.get-effective-account-settings
  calls:
  - action: get-effective-account-settings
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: ibm.iam.get-profile-template-version
  calls:
  - action: get-profile-template-version
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
checks: []
