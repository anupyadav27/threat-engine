# Discovery YAML — usage_reports (IBM Cloud)
# Generated: 2026-02-21T04:45:39Z
version: '1.0'
provider: ibm
service: usage_reports
services:
  client: usage_reports
  module: ibm_platform_services.usage_reports
discovery:
  # ── Dependent ops ──
  # ── get_account_summary [dependent] ──
  - discovery_id: ibm.usage_reports.get-account-summary
    calls:
      - action: get-account-summary
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_account_usage [dependent] ──
  - discovery_id: ibm.usage_reports.get-account-usage
    calls:
      - action: get-account-usage
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_org_usage [dependent] ──
  - discovery_id: ibm.usage_reports.get-org-usage
    calls:
      - action: get-org-usage
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_reports_snapshot [dependent] ──
  - discovery_id: ibm.usage_reports.get-reports-snapshot
    calls:
      - action: get-reports-snapshot
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_reports_snapshot_config [dependent] ──
  - discovery_id: ibm.usage_reports.get-reports-snapshot-config
    calls:
      - action: get-reports-snapshot-config
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_resource_group_usage [dependent] ──
  - discovery_id: ibm.usage_reports.get-resource-group-usage
    calls:
      - action: get-resource-group-usage
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_resource_usage_account [dependent] ──
  - discovery_id: ibm.usage_reports.get-resource-usage-account
    calls:
      - action: get-resource-usage-account
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_resource_usage_org [dependent] ──
  - discovery_id: ibm.usage_reports.get-resource-usage-org
    calls:
      - action: get-resource-usage-org
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_resource_usage_resource_group [dependent] ──
  - discovery_id: ibm.usage_reports.get-resource-usage-resource-group
    calls:
      - action: get-resource-usage-resource-group
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
