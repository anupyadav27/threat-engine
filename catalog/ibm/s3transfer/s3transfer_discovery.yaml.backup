version: '1.0'
provider: ibm
service: s3transfer
services:
  client: s3transfer
  module: NullHandler
discovery:
- discovery_id: ibm.s3transfer.get-name
  calls:
  - action: get-name
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.item }}'
checks: []
