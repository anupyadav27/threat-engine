# ============================================================
# Discovery YAML — deploymentmanager (alpha)
# Generated: 2026-02-21T03:06:39Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 17 | independent: 5 | dependent: 12
# ops with fields: 17/17 | dep ops with chain: 11/12
# ============================================================
version: '1.0'
provider: gcp
service: deploymentmanager

services:
  client: deploymentmanager
  module: "googleapiclient.discovery.build('deploymentmanager', 'alpha')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists all composite types for Deployment Manager.
  # ── gcp.deploymentmanager.compositeTypes.list ──
  - discovery_id: gcp.deploymentmanager.compositeTypes.list
    # python: svc.compositeTypes().list(**params).execute()
    calls:
      - action: compositeTypes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.compositeTypes }}'
      item:
        description: '{{ item.description }}'
        id: '{{ item.id }}'
        insertTime: '{{ item.insertTime }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        operation: '{{ item.operation }}'
        status: '{{ item.status }}'
        templateContents: '{{ item.templateContents }}'
  # Lists all deployments for a given project.
  # ── gcp.deploymentmanager.deployments.list ──
  - discovery_id: gcp.deploymentmanager.deployments.list
    # python: svc.deployments().list(**params).execute()
    calls:
      - action: deployments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.deployments }}'
      item:
        credential: '{{ item.credential }}'
        description: '{{ item.description }}'
        fingerprint: '{{ item.fingerprint }}'
        id: '{{ item.id }}'
        insertTime: '{{ item.insertTime }}'
        labels: '{{ item.labels }}'
        manifest: '{{ item.manifest }}'
        name: '{{ item.name }}'
        operation: '{{ item.operation }}'
        outputs: '{{ item.outputs }}'
        target: '{{ item.target }}'
        update: '{{ item.update }}'
        updateTime: '{{ item.updateTime }}'
  # Lists all operations for a project.
  # ── gcp.deploymentmanager.operations.list ──
  - discovery_id: gcp.deploymentmanager.operations.list
    # python: svc.operations().list(**params).execute()
    calls:
      - action: operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        clientOperationId: '{{ item.clientOperationId }}'
        creationTimestamp: '{{ item.creationTimestamp }}'
        description: '{{ item.description }}'
        endTime: '{{ item.endTime }}'
        error: '{{ item.error }}'
        firewallPolicyRuleOperationMetadata: '{{ item.firewallPolicyRuleOperationMetadata }}'
        httpErrorMessage: '{{ item.httpErrorMessage }}'
        httpErrorStatusCode: '{{ item.httpErrorStatusCode }}'
        id: '{{ item.id }}'
        insertTime: '{{ item.insertTime }}'
        instancesBulkInsertOperationMetadata: '{{ item.instancesBulkInsertOperationMetadata }}'
        name: '{{ item.name }}'
        operationGroupId: '{{ item.operationGroupId }}'
        operationType: '{{ item.operationType }}'
        progress: '{{ item.progress }}'
        region: '{{ item.region }}'
        selfLinkWithId: '{{ item.selfLinkWithId }}'
        setAutoscalerLinkOperationMetadata: '{{ item.setAutoscalerLinkOperationMetadata }}'
        setCommonInstanceMetadataOperationMetadata: '{{ item.setCommonInstanceMetadataOperationMetadata }}'
        startTime: '{{ item.startTime }}'
        status: '{{ item.status }}'
        statusMessage: '{{ item.statusMessage }}'
        targetId: '{{ item.targetId }}'
        targetLink: '{{ item.targetLink }}'
        user: '{{ item.user }}'
        zone: '{{ item.zone }}'
  # Lists all resource type providers for Deployment Manager.
  # ── gcp.deploymentmanager.typeProviders.list ──
  - discovery_id: gcp.deploymentmanager.typeProviders.list
    # python: svc.typeProviders().list(**params).execute()
    calls:
      - action: typeProviders.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.typeProviders }}'
      item:
        collectionOverrides: '{{ item.collectionOverrides }}'
        credential: '{{ item.credential }}'
        customCertificateAuthorityRoots: '{{ item.customCertificateAuthorityRoots }}'
        description: '{{ item.description }}'
        descriptorUrl: '{{ item.descriptorUrl }}'
        id: '{{ item.id }}'
        insertTime: '{{ item.insertTime }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        operation: '{{ item.operation }}'
        options: '{{ item.options }}'
  # Lists all resource types for Deployment Manager.
  # ── gcp.deploymentmanager.types.list ──
  - discovery_id: gcp.deploymentmanager.types.list
    # python: svc.types().list(**params).execute()
    calls:
      - action: types.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.types }}'
      item:
        base: '{{ item.base }}'
        description: '{{ item.description }}'
        id: '{{ item.id }}'
        insertTime: '{{ item.insertTime }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        operation: '{{ item.operation }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets information about a specific composite type.
  # ── gcp.deploymentmanager.compositeTypes.get [dependent] ──
  - discovery_id: gcp.deploymentmanager.compositeTypes.get
    for_each: gcp.deploymentmanager.compositeTypes.list
    # python: svc.compositeTypes().get(**params).execute()
    calls:
      - action: compositeTypes.get
        params:
          compositeType: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        description: '{{ response.description }}'
        id: '{{ response.id }}'
        insertTime: '{{ response.insertTime }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        operation: '{{ response.operation }}'
        status: '{{ response.status }}'
        templateContents: '{{ response.templateContents }}'
    # required_params: ['project', 'compositeType']
  # Gets information about a specific deployment.
  # ── gcp.deploymentmanager.deployments.get [dependent] ──
  - discovery_id: gcp.deploymentmanager.deployments.get
    for_each: gcp.deploymentmanager.deployments.list
    # python: svc.deployments().get(**params).execute()
    calls:
      - action: deployments.get
        params:
          deployment: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        credential: '{{ response.credential }}'
        description: '{{ response.description }}'
        fingerprint: '{{ response.fingerprint }}'
        id: '{{ response.id }}'
        insertTime: '{{ response.insertTime }}'
        labels: '{{ response.labels }}'
        manifest: '{{ response.manifest }}'
        name: '{{ response.name }}'
        operation: '{{ response.operation }}'
        outputs: '{{ response.outputs }}'
        target: '{{ response.target }}'
        update: '{{ response.update }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['deployment', 'project']
  # Gets the access control policy for a resource. May be empty if no such policy or resource exists.
  # ── gcp.deploymentmanager.deployments.getIamPolicy [dependent] ──
  - discovery_id: gcp.deploymentmanager.deployments.getIamPolicy
    for_each: gcp.deploymentmanager.resources.list
    # python: svc.deployments().getIamPolicy(**params).execute()
    calls:
      - action: deployments.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['project', 'resource']
  # Gets information about a specific manifest.
  # ── gcp.deploymentmanager.manifests.get [dependent] ──
  - discovery_id: gcp.deploymentmanager.manifests.get
    for_each: gcp.deploymentmanager.manifests.list
    # python: svc.manifests().get(**params).execute()
    calls:
      - action: manifests.get
        params:
          deployment: '{{ item.name }}'
          manifest: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        config: '{{ response.config }}'
        expandedConfig: '{{ response.expandedConfig }}'
        id: '{{ response.id }}'
        imports: '{{ response.imports }}'
        insertTime: '{{ response.insertTime }}'
        layout: '{{ response.layout }}'
        manifestSizeBytes: '{{ response.manifestSizeBytes }}'
        manifestSizeLimitBytes: '{{ response.manifestSizeLimitBytes }}'
        name: '{{ response.name }}'
    # required_params: ['deployment', 'manifest', 'project']
  # Lists all manifests for a given deployment.
  # ── gcp.deploymentmanager.manifests.list [dependent] ──
  - discovery_id: gcp.deploymentmanager.manifests.list
    for_each: gcp.deploymentmanager.deployments.list
    # python: svc.manifests().list(**params).execute()
    calls:
      - action: manifests.list
        params:
          deployment: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.manifests }}'
      item:
        config: '{{ item.config }}'
        expandedConfig: '{{ item.expandedConfig }}'
        id: '{{ item.id }}'
        imports: '{{ item.imports }}'
        insertTime: '{{ item.insertTime }}'
        layout: '{{ item.layout }}'
        manifestSizeBytes: '{{ item.manifestSizeBytes }}'
        manifestSizeLimitBytes: '{{ item.manifestSizeLimitBytes }}'
        name: '{{ item.name }}'
    # required_params: ['deployment', 'project']
  # Gets information about a specific operation.
  # ── gcp.deploymentmanager.operations.get [dependent] ──
  - discovery_id: gcp.deploymentmanager.operations.get
    for_each: gcp.deploymentmanager.operations.list
    # python: svc.operations().get(**params).execute()
    calls:
      - action: operations.get
        params:
          operation: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        clientOperationId: '{{ response.clientOperationId }}'
        creationTimestamp: '{{ response.creationTimestamp }}'
        description: '{{ response.description }}'
        endTime: '{{ response.endTime }}'
        error: '{{ response.error }}'
        firewallPolicyRuleOperationMetadata: '{{ response.firewallPolicyRuleOperationMetadata }}'
        httpErrorMessage: '{{ response.httpErrorMessage }}'
        httpErrorStatusCode: '{{ response.httpErrorStatusCode }}'
        id: '{{ response.id }}'
        insertTime: '{{ response.insertTime }}'
        instancesBulkInsertOperationMetadata: '{{ response.instancesBulkInsertOperationMetadata }}'
        name: '{{ response.name }}'
        operationGroupId: '{{ response.operationGroupId }}'
        operationType: '{{ response.operationType }}'
        progress: '{{ response.progress }}'
        region: '{{ response.region }}'
        selfLinkWithId: '{{ response.selfLinkWithId }}'
        setAutoscalerLinkOperationMetadata: '{{ response.setAutoscalerLinkOperationMetadata }}'
        setCommonInstanceMetadataOperationMetadata: '{{ response.setCommonInstanceMetadataOperationMetadata }}'
        startTime: '{{ response.startTime }}'
        status: '{{ response.status }}'
        statusMessage: '{{ response.statusMessage }}'
        targetId: '{{ response.targetId }}'
        targetLink: '{{ response.targetLink }}'
        user: '{{ response.user }}'
        zone: '{{ response.zone }}'
    # required_params: ['operation', 'project']
  # Gets information about a single resource.
  # ── gcp.deploymentmanager.resources.get [dependent] ──
  - discovery_id: gcp.deploymentmanager.resources.get
    for_each: gcp.deploymentmanager.resources.list
    # python: svc.resources().get(**params).execute()
    calls:
      - action: resources.get
        params:
          deployment: '{{ item.name }}'
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        accessControl: '{{ response.accessControl }}'
        finalProperties: '{{ response.finalProperties }}'
        id: '{{ response.id }}'
        insertTime: '{{ response.insertTime }}'
        lastUsedCredential: '{{ response.lastUsedCredential }}'
        manifest: '{{ response.manifest }}'
        name: '{{ response.name }}'
        properties: '{{ response.properties }}'
        runtimePolicies: '{{ response.runtimePolicies }}'
        type: '{{ response.type }}'
        update: '{{ response.update }}'
        updateTime: '{{ response.updateTime }}'
        url: '{{ response.url }}'
    # required_params: ['deployment', 'project', 'resource']
  # Lists all resources in a given deployment.
  # ── gcp.deploymentmanager.resources.list [dependent] ──
  - discovery_id: gcp.deploymentmanager.resources.list
    for_each: gcp.deploymentmanager.deployments.list
    # python: svc.resources().list(**params).execute()
    calls:
      - action: resources.list
        params:
          deployment: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.resources }}'
      item:
        accessControl: '{{ item.accessControl }}'
        finalProperties: '{{ item.finalProperties }}'
        id: '{{ item.id }}'
        insertTime: '{{ item.insertTime }}'
        lastUsedCredential: '{{ item.lastUsedCredential }}'
        manifest: '{{ item.manifest }}'
        name: '{{ item.name }}'
        properties: '{{ item.properties }}'
        runtimePolicies: '{{ item.runtimePolicies }}'
        type: '{{ item.type }}'
        update: '{{ item.update }}'
        updateTime: '{{ item.updateTime }}'
        url: '{{ item.url }}'
    # required_params: ['deployment', 'project']
  # Gets information about a specific type provider.
  # ── gcp.deploymentmanager.typeProviders.get [dependent] ──
  - discovery_id: gcp.deploymentmanager.typeProviders.get
    for_each: gcp.deploymentmanager.typeProviders.listTypes
    # python: svc.typeProviders().get(**params).execute()
    calls:
      - action: typeProviders.get
        params:
          typeProvider: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        collectionOverrides: '{{ response.collectionOverrides }}'
        credential: '{{ response.credential }}'
        customCertificateAuthorityRoots: '{{ response.customCertificateAuthorityRoots }}'
        description: '{{ response.description }}'
        descriptorUrl: '{{ response.descriptorUrl }}'
        id: '{{ response.id }}'
        insertTime: '{{ response.insertTime }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        operation: '{{ response.operation }}'
        options: '{{ response.options }}'
    # required_params: ['project', 'typeProvider']
  # Gets a type info for a type provided by a TypeProvider.
  # ── gcp.deploymentmanager.typeProviders.getType [dependent] ──
  - discovery_id: gcp.deploymentmanager.typeProviders.getType
    for_each: gcp.deploymentmanager.typeProviders.listTypes
    # python: svc.typeProviders().getType(**params).execute()
    calls:
      - action: typeProviders.getType
        params:
          type: '{{ item.name }}'
          typeProvider: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        description: '{{ response.description }}'
        documentationLink: '{{ response.documentationLink }}'
        name: '{{ response.name }}'
        schema: '{{ response.schema }}'
        title: '{{ response.title }}'
    # required_params: ['project', 'type', 'typeProvider']
  # Lists all the type info for a TypeProvider.
  # ── gcp.deploymentmanager.typeProviders.listTypes [dependent] ──
  - discovery_id: gcp.deploymentmanager.typeProviders.listTypes
    # python: svc.typeProviders().listTypes(**params).execute()
    calls:
      - action: typeProviders.listTypes
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.typeProviders }}'
      item:
        description: '{{ item.description }}'
        documentationLink: '{{ item.documentationLink }}'
        name: '{{ item.name }}'
        schema: '{{ item.schema }}'
        title: '{{ item.title }}'
    # required_params: ['project', 'typeProvider']
  # Gets information about a specific type.
  # ── gcp.deploymentmanager.types.get [dependent] ──
  - discovery_id: gcp.deploymentmanager.types.get
    for_each: gcp.deploymentmanager.types.list
    # python: svc.types().get(**params).execute()
    calls:
      - action: types.get
        params:
          type: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        base: '{{ response.base }}'
        description: '{{ response.description }}'
        id: '{{ response.id }}'
        insertTime: '{{ response.insertTime }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        operation: '{{ response.operation }}'
    # required_params: ['project', 'type']
