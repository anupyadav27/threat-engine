# ============================================================
# Discovery YAML — migrationcenter (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 29 | independent: 16 | dependent: 13
# ops with fields: 29/29 | dep ops with chain: 13/13
# ============================================================
version: '1.0'
provider: gcp
service: migrationcenter

services:
  client: migrationcenter
  module: "googleapiclient.discovery.build('migrationcenter', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists all the assets in a given project and location.
  # ── gcp.migrationcenter.projects.locations.assets.list ──
  - discovery_id: gcp.migrationcenter.projects.locations.assets.list
    # python: svc.projects().locations().assets().list(**params).execute()
    calls:
      - action: projects.locations.assets.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.assets }}'
      item:
        assignedGroups: '{{ item.assignedGroups }}'
        attributes: '{{ item.attributes }}'
        createTime: '{{ item.createTime }}'
        databaseDeploymentDetails: '{{ item.databaseDeploymentDetails }}'
        databaseDetails: '{{ item.databaseDetails }}'
        hidden: '{{ item.hidden }}'
        hideReason: '{{ item.hideReason }}'
        hideTime: '{{ item.hideTime }}'
        insightList: '{{ item.insightList }}'
        labels: '{{ item.labels }}'
        machineDetails: '{{ item.machineDetails }}'
        name: '{{ item.name }}'
        performanceData: '{{ item.performanceData }}'
        sources: '{{ item.sources }}'
        title: '{{ item.title }}'
        updateTime: '{{ item.updateTime }}'
  # Lists all the assets export jobs in a given project and location.
  # ── gcp.migrationcenter.projects.locations.assetsExportJobs.list ──
  - discovery_id: gcp.migrationcenter.projects.locations.assetsExportJobs.list
    # python: svc.projects().locations().assetsExportJobs().list(**params).execute()
    calls:
      - action: projects.locations.assetsExportJobs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.assetsExportJobs }}'
      item:
        condition: '{{ item.condition }}'
        createTime: '{{ item.createTime }}'
        inventory: '{{ item.inventory }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        networkDependencies: '{{ item.networkDependencies }}'
        performanceData: '{{ item.performanceData }}'
        recentExecutions: '{{ item.recentExecutions }}'
        showHidden: '{{ item.showHidden }}'
        signedUriDestination: '{{ item.signedUriDestination }}'
        updateTime: '{{ item.updateTime }}'
  # Lists all the discovery clients in a given project and location.
  # ── gcp.migrationcenter.projects.locations.discoveryClients.list ──
  - discovery_id: gcp.migrationcenter.projects.locations.discoveryClients.list
    # python: svc.projects().locations().discoveryClients().list(**params).execute()
    calls:
      - action: projects.locations.discoveryClients.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.discoveryClients }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        errors: '{{ item.errors }}'
        expireTime: '{{ item.expireTime }}'
        heartbeatTime: '{{ item.heartbeatTime }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        serviceAccount: '{{ item.serviceAccount }}'
        signalsEndpoint: '{{ item.signalsEndpoint }}'
        source: '{{ item.source }}'
        state: '{{ item.state }}'
        ttl: '{{ item.ttl }}'
        updateTime: '{{ item.updateTime }}'
        version: '{{ item.version }}'
  # Gets information about a location.
  # ── gcp.migrationcenter.projects.locations.get ──
  - discovery_id: gcp.migrationcenter.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Gets the details of regional settings.
  # ── gcp.migrationcenter.projects.locations.getSettings ──
  - discovery_id: gcp.migrationcenter.projects.locations.getSettings
    # python: svc.projects().locations().getSettings(**params).execute()
    calls:
      - action: projects.locations.getSettings
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        disableCloudLogging: '{{ response.disableCloudLogging }}'
        name: '{{ response.name }}'
        preferenceSet: '{{ response.preferenceSet }}'
  # Lists all groups in a given project and location.
  # ── gcp.migrationcenter.projects.locations.groups.list ──
  - discovery_id: gcp.migrationcenter.projects.locations.groups.list
    # python: svc.projects().locations().groups().list(**params).execute()
    calls:
      - action: projects.locations.groups.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.groups }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        updateTime: '{{ item.updateTime }}'
  # List import data files.
  # ── gcp.migrationcenter.projects.locations.importJobs.importDataFiles.list ──
  - discovery_id: gcp.migrationcenter.projects.locations.importJobs.importDataFiles.list
    # python: svc.projects().locations().importJobs().importDataFiles().list(**params).execute()
    calls:
      - action: projects.locations.importJobs.importDataFiles.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.importDataFiles }}'
      item:
        createTime: '{{ item.createTime }}'
        displayName: '{{ item.displayName }}'
        format: '{{ item.format }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
        uploadFileInfo: '{{ item.uploadFileInfo }}'
  # Lists all import jobs.
  # ── gcp.migrationcenter.projects.locations.importJobs.list ──
  - discovery_id: gcp.migrationcenter.projects.locations.importJobs.list
    # python: svc.projects().locations().importJobs().list(**params).execute()
    calls:
      - action: projects.locations.importJobs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.importJobs }}'
      item:
        assetSource: '{{ item.assetSource }}'
        completeTime: '{{ item.completeTime }}'
        createTime: '{{ item.createTime }}'
        displayName: '{{ item.displayName }}'
        executionReport: '{{ item.executionReport }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
        updateTime: '{{ item.updateTime }}'
        validationReport: '{{ item.validationReport }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.migrationcenter.projects.locations.list ──
  - discovery_id: gcp.migrationcenter.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.migrationcenter.projects.locations.operations.list ──
  - discovery_id: gcp.migrationcenter.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists all the preference sets in a given project and location.
  # ── gcp.migrationcenter.projects.locations.preferenceSets.list ──
  - discovery_id: gcp.migrationcenter.projects.locations.preferenceSets.list
    # python: svc.projects().locations().preferenceSets().list(**params).execute()
    calls:
      - action: projects.locations.preferenceSets.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.preferenceSets }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        name: '{{ item.name }}'
        updateTime: '{{ item.updateTime }}'
        virtualMachinePreferences: '{{ item.virtualMachinePreferences }}'
  # Lists all the relations in a given project and location.
  # ── gcp.migrationcenter.projects.locations.relations.list ──
  - discovery_id: gcp.migrationcenter.projects.locations.relations.list
    # python: svc.projects().locations().relations().list(**params).execute()
    calls:
      - action: projects.locations.relations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.relations }}'
      item:
        createTime: '{{ item.createTime }}'
        dstAsset: '{{ item.dstAsset }}'
        name: '{{ item.name }}'
        srcAsset: '{{ item.srcAsset }}'
        type: '{{ item.type }}'
  # Lists ReportConfigs in a given project and location.
  # ── gcp.migrationcenter.projects.locations.reportConfigs.list ──
  - discovery_id: gcp.migrationcenter.projects.locations.reportConfigs.list
    # python: svc.projects().locations().reportConfigs().list(**params).execute()
    calls:
      - action: projects.locations.reportConfigs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.reportConfigs }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        groupPreferencesetAssignments: '{{ item.groupPreferencesetAssignments }}'
        name: '{{ item.name }}'
        updateTime: '{{ item.updateTime }}'
  # Lists Reports in a given ReportConfig.
  # ── gcp.migrationcenter.projects.locations.reportConfigs.reports.list ──
  - discovery_id: gcp.migrationcenter.projects.locations.reportConfigs.reports.list
    # python: svc.projects().locations().reportConfigs().reports().list(**params).execute()
    calls:
      - action: projects.locations.reportConfigs.reports.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.reports }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
        summary: '{{ item.summary }}'
        type: '{{ item.type }}'
        updateTime: '{{ item.updateTime }}'
  # Lists all error frames in a given source and location.
  # ── gcp.migrationcenter.projects.locations.sources.errorFrames.list ──
  - discovery_id: gcp.migrationcenter.projects.locations.sources.errorFrames.list
    # python: svc.projects().locations().sources().errorFrames().list(**params).execute()
    calls:
      - action: projects.locations.sources.errorFrames.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.errorFrames }}'
      item:
        ingestionTime: '{{ item.ingestionTime }}'
        name: '{{ item.name }}'
        originalFrame: '{{ item.originalFrame }}'
        violations: '{{ item.violations }}'
  # Lists all the sources in a given project and location.
  # ── gcp.migrationcenter.projects.locations.sources.list ──
  - discovery_id: gcp.migrationcenter.projects.locations.sources.list
    # python: svc.projects().locations().sources().list(**params).execute()
    calls:
      - action: projects.locations.sources.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.sources }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        errorFrameCount: '{{ item.errorFrameCount }}'
        managed: '{{ item.managed }}'
        name: '{{ item.name }}'
        pendingFrameCount: '{{ item.pendingFrameCount }}'
        priority: '{{ item.priority }}'
        state: '{{ item.state }}'
        type: '{{ item.type }}'
        updateTime: '{{ item.updateTime }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets the details of an asset.
  # ── gcp.migrationcenter.projects.locations.assets.get [dependent] ──
  - discovery_id: gcp.migrationcenter.projects.locations.assets.get
    for_each: gcp.migrationcenter.projects.locations.assets.list
    # python: svc.projects().locations().assets().get(**params).execute()
    calls:
      - action: projects.locations.assets.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        assignedGroups: '{{ response.assignedGroups }}'
        attributes: '{{ response.attributes }}'
        createTime: '{{ response.createTime }}'
        databaseDeploymentDetails: '{{ response.databaseDeploymentDetails }}'
        databaseDetails: '{{ response.databaseDetails }}'
        hidden: '{{ response.hidden }}'
        hideReason: '{{ response.hideReason }}'
        hideTime: '{{ response.hideTime }}'
        insightList: '{{ response.insightList }}'
        labels: '{{ response.labels }}'
        machineDetails: '{{ response.machineDetails }}'
        name: '{{ response.name }}'
        performanceData: '{{ response.performanceData }}'
        sources: '{{ response.sources }}'
        title: '{{ response.title }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the details of an assets export job.
  # ── gcp.migrationcenter.projects.locations.assetsExportJobs.get [dependent] ──
  - discovery_id: gcp.migrationcenter.projects.locations.assetsExportJobs.get
    for_each: gcp.migrationcenter.projects.locations.assetsExportJobs.list
    # python: svc.projects().locations().assetsExportJobs().get(**params).execute()
    calls:
      - action: projects.locations.assetsExportJobs.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        condition: '{{ response.condition }}'
        createTime: '{{ response.createTime }}'
        inventory: '{{ response.inventory }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        networkDependencies: '{{ response.networkDependencies }}'
        performanceData: '{{ response.performanceData }}'
        recentExecutions: '{{ response.recentExecutions }}'
        showHidden: '{{ response.showHidden }}'
        signedUriDestination: '{{ response.signedUriDestination }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the details of a discovery client.
  # ── gcp.migrationcenter.projects.locations.discoveryClients.get [dependent] ──
  - discovery_id: gcp.migrationcenter.projects.locations.discoveryClients.get
    for_each: gcp.migrationcenter.projects.locations.discoveryClients.list
    # python: svc.projects().locations().discoveryClients().get(**params).execute()
    calls:
      - action: projects.locations.discoveryClients.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        errors: '{{ response.errors }}'
        expireTime: '{{ response.expireTime }}'
        heartbeatTime: '{{ response.heartbeatTime }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        serviceAccount: '{{ response.serviceAccount }}'
        signalsEndpoint: '{{ response.signalsEndpoint }}'
        source: '{{ response.source }}'
        state: '{{ response.state }}'
        ttl: '{{ response.ttl }}'
        updateTime: '{{ response.updateTime }}'
        version: '{{ response.version }}'
    # required_params: ['name']
  # Gets the details of a group.
  # ── gcp.migrationcenter.projects.locations.groups.get [dependent] ──
  - discovery_id: gcp.migrationcenter.projects.locations.groups.get
    for_each: gcp.migrationcenter.projects.locations.groups.list
    # python: svc.projects().locations().groups().get(**params).execute()
    calls:
      - action: projects.locations.groups.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the details of an import job.
  # ── gcp.migrationcenter.projects.locations.importJobs.get [dependent] ──
  - discovery_id: gcp.migrationcenter.projects.locations.importJobs.get
    for_each: gcp.migrationcenter.projects.locations.importJobs.list
    # python: svc.projects().locations().importJobs().get(**params).execute()
    calls:
      - action: projects.locations.importJobs.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        assetSource: '{{ response.assetSource }}'
        completeTime: '{{ response.completeTime }}'
        createTime: '{{ response.createTime }}'
        displayName: '{{ response.displayName }}'
        executionReport: '{{ response.executionReport }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
        updateTime: '{{ response.updateTime }}'
        validationReport: '{{ response.validationReport }}'
    # required_params: ['name']
  # Gets an import data file.
  # ── gcp.migrationcenter.projects.locations.importJobs.importDataFiles.get [dependent] ──
  - discovery_id: gcp.migrationcenter.projects.locations.importJobs.importDataFiles.get
    for_each: gcp.migrationcenter.projects.locations.importJobs.importDataFiles.list
    # python: svc.projects().locations().importJobs().importDataFiles().get(**params).execute()
    calls:
      - action: projects.locations.importJobs.importDataFiles.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        displayName: '{{ response.displayName }}'
        format: '{{ response.format }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
        uploadFileInfo: '{{ response.uploadFileInfo }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.migrationcenter.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.migrationcenter.projects.locations.operations.get
    for_each: gcp.migrationcenter.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets the details of a preference set.
  # ── gcp.migrationcenter.projects.locations.preferenceSets.get [dependent] ──
  - discovery_id: gcp.migrationcenter.projects.locations.preferenceSets.get
    for_each: gcp.migrationcenter.projects.locations.preferenceSets.list
    # python: svc.projects().locations().preferenceSets().get(**params).execute()
    calls:
      - action: projects.locations.preferenceSets.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        name: '{{ response.name }}'
        updateTime: '{{ response.updateTime }}'
        virtualMachinePreferences: '{{ response.virtualMachinePreferences }}'
    # required_params: ['name']
  # Gets the details of an relation.
  # ── gcp.migrationcenter.projects.locations.relations.get [dependent] ──
  - discovery_id: gcp.migrationcenter.projects.locations.relations.get
    for_each: gcp.migrationcenter.projects.locations.relations.list
    # python: svc.projects().locations().relations().get(**params).execute()
    calls:
      - action: projects.locations.relations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        dstAsset: '{{ response.dstAsset }}'
        name: '{{ response.name }}'
        srcAsset: '{{ response.srcAsset }}'
        type: '{{ response.type }}'
    # required_params: ['name']
  # Gets details of a single ReportConfig.
  # ── gcp.migrationcenter.projects.locations.reportConfigs.get [dependent] ──
  - discovery_id: gcp.migrationcenter.projects.locations.reportConfigs.get
    for_each: gcp.migrationcenter.projects.locations.reportConfigs.list
    # python: svc.projects().locations().reportConfigs().get(**params).execute()
    calls:
      - action: projects.locations.reportConfigs.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        groupPreferencesetAssignments: '{{ response.groupPreferencesetAssignments }}'
        name: '{{ response.name }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets details of a single Report.
  # ── gcp.migrationcenter.projects.locations.reportConfigs.reports.get [dependent] ──
  - discovery_id: gcp.migrationcenter.projects.locations.reportConfigs.reports.get
    for_each: gcp.migrationcenter.projects.locations.reportConfigs.reports.list
    # python: svc.projects().locations().reportConfigs().reports().get(**params).execute()
    calls:
      - action: projects.locations.reportConfigs.reports.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
        summary: '{{ response.summary }}'
        type: '{{ response.type }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the details of an error frame.
  # ── gcp.migrationcenter.projects.locations.sources.errorFrames.get [dependent] ──
  - discovery_id: gcp.migrationcenter.projects.locations.sources.errorFrames.get
    for_each: gcp.migrationcenter.projects.locations.sources.errorFrames.list
    # python: svc.projects().locations().sources().errorFrames().get(**params).execute()
    calls:
      - action: projects.locations.sources.errorFrames.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        ingestionTime: '{{ response.ingestionTime }}'
        name: '{{ response.name }}'
        originalFrame: '{{ response.originalFrame }}'
        violations: '{{ response.violations }}'
    # required_params: ['name']
  # Gets the details of a source.
  # ── gcp.migrationcenter.projects.locations.sources.get [dependent] ──
  - discovery_id: gcp.migrationcenter.projects.locations.sources.get
    for_each: gcp.migrationcenter.projects.locations.sources.list
    # python: svc.projects().locations().sources().get(**params).execute()
    calls:
      - action: projects.locations.sources.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        errorFrameCount: '{{ response.errorFrameCount }}'
        managed: '{{ response.managed }}'
        name: '{{ response.name }}'
        pendingFrameCount: '{{ response.pendingFrameCount }}'
        priority: '{{ response.priority }}'
        state: '{{ response.state }}'
        type: '{{ response.type }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
