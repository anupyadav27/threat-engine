# ============================================================
# Discovery YAML — rapidmigrationassessment (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 7 | independent: 4 | dependent: 3
# ops with fields: 7/7 | dep ops with chain: 2/3
# ============================================================
version: '1.0'
provider: gcp
service: rapidmigrationassessment

services:
  client: rapidmigrationassessment
  module: "googleapiclient.discovery.build('rapidmigrationassessment', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists Collectors in a given project and location.
  # ── gcp.rapidmigrationassessment.projects.locations.collectors.list ──
  - discovery_id: gcp.rapidmigrationassessment.projects.locations.collectors.list
    # python: svc.projects().locations().collectors().list(**params).execute()
    calls:
      - action: projects.locations.collectors.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.collectors }}'
      item:
        bucket: '{{ item.bucket }}'
        clientVersion: '{{ item.clientVersion }}'
        collectionDays: '{{ item.collectionDays }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        eulaUri: '{{ item.eulaUri }}'
        expectedAssetCount: '{{ item.expectedAssetCount }}'
        guestOsScan: '{{ item.guestOsScan }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        serviceAccount: '{{ item.serviceAccount }}'
        state: '{{ item.state }}'
        updateTime: '{{ item.updateTime }}'
        vsphereScan: '{{ item.vsphereScan }}'
  # Gets information about a location.
  # ── gcp.rapidmigrationassessment.projects.locations.get ──
  - discovery_id: gcp.rapidmigrationassessment.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists information about the supported locations for this service.
  # ── gcp.rapidmigrationassessment.projects.locations.list ──
  - discovery_id: gcp.rapidmigrationassessment.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.rapidmigrationassessment.projects.locations.operations.list ──
  - discovery_id: gcp.rapidmigrationassessment.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets details of a single Annotation.
  # ── gcp.rapidmigrationassessment.projects.locations.annotations.get [dependent] ──
  - discovery_id: gcp.rapidmigrationassessment.projects.locations.annotations.get
    # python: svc.projects().locations().annotations().get(**params).execute()
    calls:
      - action: projects.locations.annotations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        type: '{{ response.type }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets details of a single Collector.
  # ── gcp.rapidmigrationassessment.projects.locations.collectors.get [dependent] ──
  - discovery_id: gcp.rapidmigrationassessment.projects.locations.collectors.get
    for_each: gcp.rapidmigrationassessment.projects.locations.collectors.list
    # python: svc.projects().locations().collectors().get(**params).execute()
    calls:
      - action: projects.locations.collectors.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        bucket: '{{ response.bucket }}'
        clientVersion: '{{ response.clientVersion }}'
        collectionDays: '{{ response.collectionDays }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        eulaUri: '{{ response.eulaUri }}'
        expectedAssetCount: '{{ response.expectedAssetCount }}'
        guestOsScan: '{{ response.guestOsScan }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        serviceAccount: '{{ response.serviceAccount }}'
        state: '{{ response.state }}'
        updateTime: '{{ response.updateTime }}'
        vsphereScan: '{{ response.vsphereScan }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.rapidmigrationassessment.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.rapidmigrationassessment.projects.locations.operations.get
    for_each: gcp.rapidmigrationassessment.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
