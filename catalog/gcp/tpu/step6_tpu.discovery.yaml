# ============================================================
# Discovery YAML — tpu (v1)
# Generated: 2026-02-21T03:06:41Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 10 | independent: 6 | dependent: 4
# ops with fields: 10/10 | dep ops with chain: 4/4
# ============================================================
version: '1.0'
provider: gcp
service: tpu

services:
  client: tpu
  module: "googleapiclient.discovery.build('tpu', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists accelerator types supported by this API.
  # ── gcp.tpu.projects.locations.acceleratorTypes.list ──
  - discovery_id: gcp.tpu.projects.locations.acceleratorTypes.list
    # python: svc.projects().locations().acceleratorTypes().list(**params).execute()
    calls:
      - action: projects.locations.acceleratorTypes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.acceleratorTypes }}'
      item:
        name: '{{ item.name }}'
        type: '{{ item.type }}'
  # Gets information about a location.
  # ── gcp.tpu.projects.locations.get ──
  - discovery_id: gcp.tpu.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.tpu.projects.locations.list ──
  - discovery_id: gcp.tpu.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists nodes.
  # ── gcp.tpu.projects.locations.nodes.list ──
  - discovery_id: gcp.tpu.projects.locations.nodes.list
    # python: svc.projects().locations().nodes().list(**params).execute()
    calls:
      - action: projects.locations.nodes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.nodes }}'
      item:
        acceleratorType: '{{ item.acceleratorType }}'
        apiVersion: '{{ item.apiVersion }}'
        cidrBlock: '{{ item.cidrBlock }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        health: '{{ item.health }}'
        healthDescription: '{{ item.healthDescription }}'
        ipAddress: '{{ item.ipAddress }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        network: '{{ item.network }}'
        networkEndpoints: '{{ item.networkEndpoints }}'
        port: '{{ item.port }}'
        schedulingConfig: '{{ item.schedulingConfig }}'
        serviceAccount: '{{ item.serviceAccount }}'
        state: '{{ item.state }}'
        symptoms: '{{ item.symptoms }}'
        tensorflowVersion: '{{ item.tensorflowVersion }}'
        useServiceNetworking: '{{ item.useServiceNetworking }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.tpu.projects.locations.operations.list ──
  - discovery_id: gcp.tpu.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # List TensorFlow versions supported by this API.
  # ── gcp.tpu.projects.locations.tensorflowVersions.list ──
  - discovery_id: gcp.tpu.projects.locations.tensorflowVersions.list
    # python: svc.projects().locations().tensorflowVersions().list(**params).execute()
    calls:
      - action: projects.locations.tensorflowVersions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.tensorflowVersions }}'
      item:
        name: '{{ item.name }}'
        version: '{{ item.version }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets AcceleratorType.
  # ── gcp.tpu.projects.locations.acceleratorTypes.get [dependent] ──
  - discovery_id: gcp.tpu.projects.locations.acceleratorTypes.get
    for_each: gcp.tpu.projects.locations.acceleratorTypes.list
    # python: svc.projects().locations().acceleratorTypes().get(**params).execute()
    calls:
      - action: projects.locations.acceleratorTypes.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        type: '{{ response.type }}'
    # required_params: ['name']
  # Gets the details of a node.
  # ── gcp.tpu.projects.locations.nodes.get [dependent] ──
  - discovery_id: gcp.tpu.projects.locations.nodes.get
    for_each: gcp.tpu.projects.locations.nodes.list
    # python: svc.projects().locations().nodes().get(**params).execute()
    calls:
      - action: projects.locations.nodes.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        acceleratorType: '{{ response.acceleratorType }}'
        apiVersion: '{{ response.apiVersion }}'
        cidrBlock: '{{ response.cidrBlock }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        health: '{{ response.health }}'
        healthDescription: '{{ response.healthDescription }}'
        ipAddress: '{{ response.ipAddress }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        network: '{{ response.network }}'
        networkEndpoints: '{{ response.networkEndpoints }}'
        port: '{{ response.port }}'
        schedulingConfig: '{{ response.schedulingConfig }}'
        serviceAccount: '{{ response.serviceAccount }}'
        state: '{{ response.state }}'
        symptoms: '{{ response.symptoms }}'
        tensorflowVersion: '{{ response.tensorflowVersion }}'
        useServiceNetworking: '{{ response.useServiceNetworking }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.tpu.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.tpu.projects.locations.operations.get
    for_each: gcp.tpu.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets TensorFlow Version.
  # ── gcp.tpu.projects.locations.tensorflowVersions.get [dependent] ──
  - discovery_id: gcp.tpu.projects.locations.tensorflowVersions.get
    for_each: gcp.tpu.projects.locations.tensorflowVersions.list
    # python: svc.projects().locations().tensorflowVersions().get(**params).execute()
    calls:
      - action: projects.locations.tensorflowVersions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        version: '{{ response.version }}'
    # required_params: ['name']
