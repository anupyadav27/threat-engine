# ============================================================
# Discovery YAML — dialogflow (v2beta1)
# Generated: 2026-02-21T03:06:39Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 93 | independent: 57 | dependent: 36
# ops with fields: 93/93 | dep ops with chain: 36/36
# ============================================================
version: '1.0'
provider: gcp
service: dialogflow

services:
  client: dialogflow
  module: "googleapiclient.discovery.build('dialogflow', 'v2beta1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # ── gcp.dialogflow.projects.agent.entityTypes.list ──
  - discovery_id: gcp.dialogflow.projects.agent.entityTypes.list
    # python: svc.projects().agent().entityTypes().list(**params).execute()
    calls:
      - action: projects.agent.entityTypes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.entityTypes }}'
      item:
        autoExpansionMode: '{{ item.autoExpansionMode }}'
        displayName: '{{ item.displayName }}'
        enableFuzzyExtraction: '{{ item.enableFuzzyExtraction }}'
        entities: '{{ item.entities }}'
        name: '{{ item.name }}'
  # ── gcp.dialogflow.projects.agent.environments.getHistory ──
  - discovery_id: gcp.dialogflow.projects.agent.environments.getHistory
    # python: svc.projects().agent().environments().getHistory(**params).execute()
    calls:
      - action: projects.agent.environments.getHistory
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        entries: '{{ response.entries }}'
        parent: '{{ response.parent }}'
  # ── gcp.dialogflow.projects.agent.environments.intents.list ──
  - discovery_id: gcp.dialogflow.projects.agent.environments.intents.list
    # python: svc.projects().agent().environments().intents().list(**params).execute()
    calls:
      - action: projects.agent.environments.intents.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.intents }}'
      item:
        action: '{{ item.action }}'
        defaultResponsePlatforms: '{{ item.defaultResponsePlatforms }}'
        displayName: '{{ item.displayName }}'
        endInteraction: '{{ item.endInteraction }}'
        events: '{{ item.events }}'
        followupIntentInfo: '{{ item.followupIntentInfo }}'
        inputContextNames: '{{ item.inputContextNames }}'
        isFallback: '{{ item.isFallback }}'
        liveAgentHandoff: '{{ item.liveAgentHandoff }}'
        messages: '{{ item.messages }}'
        mlDisabled: '{{ item.mlDisabled }}'
        mlEnabled: '{{ item.mlEnabled }}'
        name: '{{ item.name }}'
        outputContexts: '{{ item.outputContexts }}'
        parameters: '{{ item.parameters }}'
        parentFollowupIntentName: '{{ item.parentFollowupIntentName }}'
        priority: '{{ item.priority }}'
        resetContexts: '{{ item.resetContexts }}'
        rootFollowupIntentName: '{{ item.rootFollowupIntentName }}'
        trainingPhrases: '{{ item.trainingPhrases }}'
        webhookState: '{{ item.webhookState }}'
  # ── gcp.dialogflow.projects.agent.environments.list ──
  - discovery_id: gcp.dialogflow.projects.agent.environments.list
    # python: svc.projects().agent().environments().list(**params).execute()
    calls:
      - action: projects.agent.environments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.environments }}'
      item:
        agentVersion: '{{ item.agentVersion }}'
        description: '{{ item.description }}'
        fulfillment: '{{ item.fulfillment }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
        textToSpeechSettings: '{{ item.textToSpeechSettings }}'
        updateTime: '{{ item.updateTime }}'
  # ── gcp.dialogflow.projects.agent.environments.users.sessions.contexts.list ──
  - discovery_id: gcp.dialogflow.projects.agent.environments.users.sessions.contexts.list
    # python: svc.projects().agent().environments().users().sessions().contexts().list(**params).execute()
    calls:
      - action: projects.agent.environments.users.sessions.contexts.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.contexts }}'
      item:
        lifespanCount: '{{ item.lifespanCount }}'
        name: '{{ item.name }}'
        parameters: '{{ item.parameters }}'
  # ── gcp.dialogflow.projects.agent.environments.users.sessions.entityTypes.list ──
  - discovery_id: gcp.dialogflow.projects.agent.environments.users.sessions.entityTypes.list
    # python: svc.projects().agent().environments().users().sessions().entityTypes().list(**params).execute()
    calls:
      - action: projects.agent.environments.users.sessions.entityTypes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.entityTypes }}'
      item:
        entities: '{{ item.entities }}'
        entityOverrideMode: '{{ item.entityOverrideMode }}'
        name: '{{ item.name }}'
  # ── gcp.dialogflow.projects.agent.getFulfillment ──
  - discovery_id: gcp.dialogflow.projects.agent.getFulfillment
    # python: svc.projects().agent().getFulfillment(**params).execute()
    calls:
      - action: projects.agent.getFulfillment
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        enabled: '{{ response.enabled }}'
        features: '{{ response.features }}'
        genericWebService: '{{ response.genericWebService }}'
        name: '{{ response.name }}'
  # ── gcp.dialogflow.projects.agent.getValidationResult ──
  - discovery_id: gcp.dialogflow.projects.agent.getValidationResult
    # python: svc.projects().agent().getValidationResult(**params).execute()
    calls:
      - action: projects.agent.getValidationResult
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        validationErrors: '{{ response.validationErrors }}'
  # ── gcp.dialogflow.projects.agent.intents.list ──
  - discovery_id: gcp.dialogflow.projects.agent.intents.list
    # python: svc.projects().agent().intents().list(**params).execute()
    calls:
      - action: projects.agent.intents.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.intents }}'
      item:
        action: '{{ item.action }}'
        defaultResponsePlatforms: '{{ item.defaultResponsePlatforms }}'
        displayName: '{{ item.displayName }}'
        endInteraction: '{{ item.endInteraction }}'
        events: '{{ item.events }}'
        followupIntentInfo: '{{ item.followupIntentInfo }}'
        inputContextNames: '{{ item.inputContextNames }}'
        isFallback: '{{ item.isFallback }}'
        liveAgentHandoff: '{{ item.liveAgentHandoff }}'
        messages: '{{ item.messages }}'
        mlDisabled: '{{ item.mlDisabled }}'
        mlEnabled: '{{ item.mlEnabled }}'
        name: '{{ item.name }}'
        outputContexts: '{{ item.outputContexts }}'
        parameters: '{{ item.parameters }}'
        parentFollowupIntentName: '{{ item.parentFollowupIntentName }}'
        priority: '{{ item.priority }}'
        resetContexts: '{{ item.resetContexts }}'
        rootFollowupIntentName: '{{ item.rootFollowupIntentName }}'
        trainingPhrases: '{{ item.trainingPhrases }}'
        webhookState: '{{ item.webhookState }}'
  # ── gcp.dialogflow.projects.agent.knowledgeBases.documents.list ──
  - discovery_id: gcp.dialogflow.projects.agent.knowledgeBases.documents.list
    # python: svc.projects().agent().knowledgeBases().documents().list(**params).execute()
    calls:
      - action: projects.agent.knowledgeBases.documents.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.documents }}'
      item:
        content: '{{ item.content }}'
        contentUri: '{{ item.contentUri }}'
        displayName: '{{ item.displayName }}'
        enableAutoReload: '{{ item.enableAutoReload }}'
        knowledgeTypes: '{{ item.knowledgeTypes }}'
        latestReloadStatus: '{{ item.latestReloadStatus }}'
        metadata: '{{ item.metadata }}'
        mimeType: '{{ item.mimeType }}'
        name: '{{ item.name }}'
        rawContent: '{{ item.rawContent }}'
        state: '{{ item.state }}'
  # ── gcp.dialogflow.projects.agent.knowledgeBases.list ──
  - discovery_id: gcp.dialogflow.projects.agent.knowledgeBases.list
    # python: svc.projects().agent().knowledgeBases().list(**params).execute()
    calls:
      - action: projects.agent.knowledgeBases.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.knowledgeBases }}'
      item:
        displayName: '{{ item.displayName }}'
        languageCode: '{{ item.languageCode }}'
        name: '{{ item.name }}'
  # ── gcp.dialogflow.projects.agent.search ──
  - discovery_id: gcp.dialogflow.projects.agent.search
    # python: svc.projects().agent().search(**params).execute()
    calls:
      - action: projects.agent.search
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.agent }}'
      item:
        apiVersion: '{{ item.apiVersion }}'
        avatarUri: '{{ item.avatarUri }}'
        classificationThreshold: '{{ item.classificationThreshold }}'
        defaultLanguageCode: '{{ item.defaultLanguageCode }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        enableLogging: '{{ item.enableLogging }}'
        matchMode: '{{ item.matchMode }}'
        parent: '{{ item.parent }}'
        supportedLanguageCodes: '{{ item.supportedLanguageCodes }}'
        tier: '{{ item.tier }}'
        timeZone: '{{ item.timeZone }}'
  # ── gcp.dialogflow.projects.agent.sessions.contexts.list ──
  - discovery_id: gcp.dialogflow.projects.agent.sessions.contexts.list
    # python: svc.projects().agent().sessions().contexts().list(**params).execute()
    calls:
      - action: projects.agent.sessions.contexts.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.contexts }}'
      item:
        lifespanCount: '{{ item.lifespanCount }}'
        name: '{{ item.name }}'
        parameters: '{{ item.parameters }}'
  # ── gcp.dialogflow.projects.agent.sessions.entityTypes.list ──
  - discovery_id: gcp.dialogflow.projects.agent.sessions.entityTypes.list
    # python: svc.projects().agent().sessions().entityTypes().list(**params).execute()
    calls:
      - action: projects.agent.sessions.entityTypes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.entityTypes }}'
      item:
        entities: '{{ item.entities }}'
        entityOverrideMode: '{{ item.entityOverrideMode }}'
        name: '{{ item.name }}'
  # ── gcp.dialogflow.projects.agent.versions.list ──
  - discovery_id: gcp.dialogflow.projects.agent.versions.list
    # python: svc.projects().agent().versions().list(**params).execute()
    calls:
      - action: projects.agent.versions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.versions }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        versionNumber: '{{ item.versionNumber }}'
  # ── gcp.dialogflow.projects.answerRecords.list ──
  - discovery_id: gcp.dialogflow.projects.answerRecords.list
    # python: svc.projects().answerRecords().list(**params).execute()
    calls:
      - action: projects.answerRecords.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.answerRecords }}'
      item:
        agentAssistantRecord: '{{ item.agentAssistantRecord }}'
        answerFeedback: '{{ item.answerFeedback }}'
        name: '{{ item.name }}'
  # ── gcp.dialogflow.projects.conversationProfiles.list ──
  - discovery_id: gcp.dialogflow.projects.conversationProfiles.list
    # python: svc.projects().conversationProfiles().list(**params).execute()
    calls:
      - action: projects.conversationProfiles.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.conversationProfiles }}'
      item:
        automatedAgentConfig: '{{ item.automatedAgentConfig }}'
        createTime: '{{ item.createTime }}'
        displayName: '{{ item.displayName }}'
        humanAgentAssistantConfig: '{{ item.humanAgentAssistantConfig }}'
        humanAgentHandoffConfig: '{{ item.humanAgentHandoffConfig }}'
        languageCode: '{{ item.languageCode }}'
        loggingConfig: '{{ item.loggingConfig }}'
        name: '{{ item.name }}'
        newMessageEventNotificationConfig: '{{ item.newMessageEventNotificationConfig }}'
        newRecognitionResultNotificationConfig: '{{ item.newRecognitionResultNotificationConfig }}'
        notificationConfig: '{{ item.notificationConfig }}'
        securitySettings: '{{ item.securitySettings }}'
        sttConfig: '{{ item.sttConfig }}'
        timeZone: '{{ item.timeZone }}'
        ttsConfig: '{{ item.ttsConfig }}'
        updateTime: '{{ item.updateTime }}'
  # ── gcp.dialogflow.projects.conversations.list ──
  - discovery_id: gcp.dialogflow.projects.conversations.list
    # python: svc.projects().conversations().list(**params).execute()
    calls:
      - action: projects.conversations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.conversations }}'
      item:
        conversationProfile: '{{ item.conversationProfile }}'
        conversationStage: '{{ item.conversationStage }}'
        endTime: '{{ item.endTime }}'
        ingestedContextReferences: '{{ item.ingestedContextReferences }}'
        lifecycleState: '{{ item.lifecycleState }}'
        name: '{{ item.name }}'
        phoneNumber: '{{ item.phoneNumber }}'
        startTime: '{{ item.startTime }}'
        telephonyConnectionInfo: '{{ item.telephonyConnectionInfo }}'
  # ── gcp.dialogflow.projects.conversations.messages.list ──
  - discovery_id: gcp.dialogflow.projects.conversations.messages.list
    # python: svc.projects().conversations().messages().list(**params).execute()
    calls:
      - action: projects.conversations.messages.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.messages }}'
      item:
        content: '{{ item.content }}'
        createTime: '{{ item.createTime }}'
        languageCode: '{{ item.languageCode }}'
        messageAnnotation: '{{ item.messageAnnotation }}'
        name: '{{ item.name }}'
        participant: '{{ item.participant }}'
        participantRole: '{{ item.participantRole }}'
        responseMessages: '{{ item.responseMessages }}'
        sendTime: '{{ item.sendTime }}'
        sentimentAnalysis: '{{ item.sentimentAnalysis }}'
  # ── gcp.dialogflow.projects.conversations.participants.list ──
  - discovery_id: gcp.dialogflow.projects.conversations.participants.list
    # python: svc.projects().conversations().participants().list(**params).execute()
    calls:
      - action: projects.conversations.participants.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.participants }}'
      item:
        agentDesktopSource: '{{ item.agentDesktopSource }}'
        documentsMetadataFilters: '{{ item.documentsMetadataFilters }}'
        name: '{{ item.name }}'
        obfuscatedExternalUserId: '{{ item.obfuscatedExternalUserId }}'
        role: '{{ item.role }}'
  # ── gcp.dialogflow.projects.conversations.participants.suggestions.list ──
  - discovery_id: gcp.dialogflow.projects.conversations.participants.suggestions.list
    # python: svc.projects().conversations().participants().suggestions().list(**params).execute()
    calls:
      - action: projects.conversations.participants.suggestions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.suggestions }}'
      item:
        articles: '{{ item.articles }}'
        createTime: '{{ item.createTime }}'
        faqAnswers: '{{ item.faqAnswers }}'
        latestMessage: '{{ item.latestMessage }}'
        name: '{{ item.name }}'
  # ── gcp.dialogflow.projects.generators.list ──
  - discovery_id: gcp.dialogflow.projects.generators.list
    # python: svc.projects().generators().list(**params).execute()
    calls:
      - action: projects.generators.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.generators }}'
      item:
        agentCoachingContext: '{{ item.agentCoachingContext }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        freeFormContext: '{{ item.freeFormContext }}'
        inferenceParameter: '{{ item.inferenceParameter }}'
        name: '{{ item.name }}'
        publishedModel: '{{ item.publishedModel }}'
        suggestionDedupingConfig: '{{ item.suggestionDedupingConfig }}'
        summarizationContext: '{{ item.summarizationContext }}'
        tools: '{{ item.tools }}'
        triggerEvent: '{{ item.triggerEvent }}'
        updateTime: '{{ item.updateTime }}'
  # ── gcp.dialogflow.projects.getAgent ──
  - discovery_id: gcp.dialogflow.projects.getAgent
    # python: svc.projects().getAgent(**params).execute()
    calls:
      - action: projects.getAgent
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiVersion: '{{ response.apiVersion }}'
        avatarUri: '{{ response.avatarUri }}'
        classificationThreshold: '{{ response.classificationThreshold }}'
        defaultLanguageCode: '{{ response.defaultLanguageCode }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        enableLogging: '{{ response.enableLogging }}'
        matchMode: '{{ response.matchMode }}'
        parent: '{{ response.parent }}'
        supportedLanguageCodes: '{{ response.supportedLanguageCodes }}'
        tier: '{{ response.tier }}'
        timeZone: '{{ response.timeZone }}'
  # ── gcp.dialogflow.projects.knowledgeBases.documents.list ──
  - discovery_id: gcp.dialogflow.projects.knowledgeBases.documents.list
    # python: svc.projects().knowledgeBases().documents().list(**params).execute()
    calls:
      - action: projects.knowledgeBases.documents.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.documents }}'
      item:
        content: '{{ item.content }}'
        contentUri: '{{ item.contentUri }}'
        displayName: '{{ item.displayName }}'
        enableAutoReload: '{{ item.enableAutoReload }}'
        knowledgeTypes: '{{ item.knowledgeTypes }}'
        latestReloadStatus: '{{ item.latestReloadStatus }}'
        metadata: '{{ item.metadata }}'
        mimeType: '{{ item.mimeType }}'
        name: '{{ item.name }}'
        rawContent: '{{ item.rawContent }}'
        state: '{{ item.state }}'
  # ── gcp.dialogflow.projects.knowledgeBases.list ──
  - discovery_id: gcp.dialogflow.projects.knowledgeBases.list
    # python: svc.projects().knowledgeBases().list(**params).execute()
    calls:
      - action: projects.knowledgeBases.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.knowledgeBases }}'
      item:
        displayName: '{{ item.displayName }}'
        languageCode: '{{ item.languageCode }}'
        name: '{{ item.name }}'
  # ── gcp.dialogflow.projects.locations.agent.entityTypes.list ──
  - discovery_id: gcp.dialogflow.projects.locations.agent.entityTypes.list
    # python: svc.projects().locations().agent().entityTypes().list(**params).execute()
    calls:
      - action: projects.locations.agent.entityTypes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.entityTypes }}'
      item:
        autoExpansionMode: '{{ item.autoExpansionMode }}'
        displayName: '{{ item.displayName }}'
        enableFuzzyExtraction: '{{ item.enableFuzzyExtraction }}'
        entities: '{{ item.entities }}'
        name: '{{ item.name }}'
  # ── gcp.dialogflow.projects.locations.agent.environments.getHistory ──
  - discovery_id: gcp.dialogflow.projects.locations.agent.environments.getHistory
    # python: svc.projects().locations().agent().environments().getHistory(**params).execute()
    calls:
      - action: projects.locations.agent.environments.getHistory
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        entries: '{{ response.entries }}'
        parent: '{{ response.parent }}'
  # ── gcp.dialogflow.projects.locations.agent.environments.intents.list ──
  - discovery_id: gcp.dialogflow.projects.locations.agent.environments.intents.list
    # python: svc.projects().locations().agent().environments().intents().list(**params).execute()
    calls:
      - action: projects.locations.agent.environments.intents.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.intents }}'
      item:
        action: '{{ item.action }}'
        defaultResponsePlatforms: '{{ item.defaultResponsePlatforms }}'
        displayName: '{{ item.displayName }}'
        endInteraction: '{{ item.endInteraction }}'
        events: '{{ item.events }}'
        followupIntentInfo: '{{ item.followupIntentInfo }}'
        inputContextNames: '{{ item.inputContextNames }}'
        isFallback: '{{ item.isFallback }}'
        liveAgentHandoff: '{{ item.liveAgentHandoff }}'
        messages: '{{ item.messages }}'
        mlDisabled: '{{ item.mlDisabled }}'
        mlEnabled: '{{ item.mlEnabled }}'
        name: '{{ item.name }}'
        outputContexts: '{{ item.outputContexts }}'
        parameters: '{{ item.parameters }}'
        parentFollowupIntentName: '{{ item.parentFollowupIntentName }}'
        priority: '{{ item.priority }}'
        resetContexts: '{{ item.resetContexts }}'
        rootFollowupIntentName: '{{ item.rootFollowupIntentName }}'
        trainingPhrases: '{{ item.trainingPhrases }}'
        webhookState: '{{ item.webhookState }}'
  # ── gcp.dialogflow.projects.locations.agent.environments.list ──
  - discovery_id: gcp.dialogflow.projects.locations.agent.environments.list
    # python: svc.projects().locations().agent().environments().list(**params).execute()
    calls:
      - action: projects.locations.agent.environments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.environments }}'
      item:
        agentVersion: '{{ item.agentVersion }}'
        description: '{{ item.description }}'
        fulfillment: '{{ item.fulfillment }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
        textToSpeechSettings: '{{ item.textToSpeechSettings }}'
        updateTime: '{{ item.updateTime }}'
  # ── gcp.dialogflow.projects.locations.agent.environments.users.sessions.contexts.list ──
  - discovery_id: gcp.dialogflow.projects.locations.agent.environments.users.sessions.contexts.list
    # python: svc.projects().locations().agent().environments().users().sessions().contexts().list(**params).execute()
    calls:
      - action: projects.locations.agent.environments.users.sessions.contexts.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.contexts }}'
      item:
        lifespanCount: '{{ item.lifespanCount }}'
        name: '{{ item.name }}'
        parameters: '{{ item.parameters }}'
  # ── gcp.dialogflow.projects.locations.agent.environments.users.sessions.entityTypes.list ──
  - discovery_id: gcp.dialogflow.projects.locations.agent.environments.users.sessions.entityTypes.list
    # python: svc.projects().locations().agent().environments().users().sessions().entityTypes().list(**params).execute()
    calls:
      - action: projects.locations.agent.environments.users.sessions.entityTypes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.entityTypes }}'
      item:
        entities: '{{ item.entities }}'
        entityOverrideMode: '{{ item.entityOverrideMode }}'
        name: '{{ item.name }}'
  # ── gcp.dialogflow.projects.locations.agent.getFulfillment ──
  - discovery_id: gcp.dialogflow.projects.locations.agent.getFulfillment
    # python: svc.projects().locations().agent().getFulfillment(**params).execute()
    calls:
      - action: projects.locations.agent.getFulfillment
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        enabled: '{{ response.enabled }}'
        features: '{{ response.features }}'
        genericWebService: '{{ response.genericWebService }}'
        name: '{{ response.name }}'
  # ── gcp.dialogflow.projects.locations.agent.getValidationResult ──
  - discovery_id: gcp.dialogflow.projects.locations.agent.getValidationResult
    # python: svc.projects().locations().agent().getValidationResult(**params).execute()
    calls:
      - action: projects.locations.agent.getValidationResult
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        validationErrors: '{{ response.validationErrors }}'
  # ── gcp.dialogflow.projects.locations.agent.intents.list ──
  - discovery_id: gcp.dialogflow.projects.locations.agent.intents.list
    # python: svc.projects().locations().agent().intents().list(**params).execute()
    calls:
      - action: projects.locations.agent.intents.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.intents }}'
      item:
        action: '{{ item.action }}'
        defaultResponsePlatforms: '{{ item.defaultResponsePlatforms }}'
        displayName: '{{ item.displayName }}'
        endInteraction: '{{ item.endInteraction }}'
        events: '{{ item.events }}'
        followupIntentInfo: '{{ item.followupIntentInfo }}'
        inputContextNames: '{{ item.inputContextNames }}'
        isFallback: '{{ item.isFallback }}'
        liveAgentHandoff: '{{ item.liveAgentHandoff }}'
        messages: '{{ item.messages }}'
        mlDisabled: '{{ item.mlDisabled }}'
        mlEnabled: '{{ item.mlEnabled }}'
        name: '{{ item.name }}'
        outputContexts: '{{ item.outputContexts }}'
        parameters: '{{ item.parameters }}'
        parentFollowupIntentName: '{{ item.parentFollowupIntentName }}'
        priority: '{{ item.priority }}'
        resetContexts: '{{ item.resetContexts }}'
        rootFollowupIntentName: '{{ item.rootFollowupIntentName }}'
        trainingPhrases: '{{ item.trainingPhrases }}'
        webhookState: '{{ item.webhookState }}'
  # ── gcp.dialogflow.projects.locations.agent.search ──
  - discovery_id: gcp.dialogflow.projects.locations.agent.search
    # python: svc.projects().locations().agent().search(**params).execute()
    calls:
      - action: projects.locations.agent.search
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.agent }}'
      item:
        apiVersion: '{{ item.apiVersion }}'
        avatarUri: '{{ item.avatarUri }}'
        classificationThreshold: '{{ item.classificationThreshold }}'
        defaultLanguageCode: '{{ item.defaultLanguageCode }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        enableLogging: '{{ item.enableLogging }}'
        matchMode: '{{ item.matchMode }}'
        parent: '{{ item.parent }}'
        supportedLanguageCodes: '{{ item.supportedLanguageCodes }}'
        tier: '{{ item.tier }}'
        timeZone: '{{ item.timeZone }}'
  # ── gcp.dialogflow.projects.locations.agent.sessions.contexts.list ──
  - discovery_id: gcp.dialogflow.projects.locations.agent.sessions.contexts.list
    # python: svc.projects().locations().agent().sessions().contexts().list(**params).execute()
    calls:
      - action: projects.locations.agent.sessions.contexts.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.contexts }}'
      item:
        lifespanCount: '{{ item.lifespanCount }}'
        name: '{{ item.name }}'
        parameters: '{{ item.parameters }}'
  # ── gcp.dialogflow.projects.locations.agent.sessions.entityTypes.list ──
  - discovery_id: gcp.dialogflow.projects.locations.agent.sessions.entityTypes.list
    # python: svc.projects().locations().agent().sessions().entityTypes().list(**params).execute()
    calls:
      - action: projects.locations.agent.sessions.entityTypes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.entityTypes }}'
      item:
        entities: '{{ item.entities }}'
        entityOverrideMode: '{{ item.entityOverrideMode }}'
        name: '{{ item.name }}'
  # ── gcp.dialogflow.projects.locations.agent.versions.list ──
  - discovery_id: gcp.dialogflow.projects.locations.agent.versions.list
    # python: svc.projects().locations().agent().versions().list(**params).execute()
    calls:
      - action: projects.locations.agent.versions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.versions }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        versionNumber: '{{ item.versionNumber }}'
  # ── gcp.dialogflow.projects.locations.answerRecords.list ──
  - discovery_id: gcp.dialogflow.projects.locations.answerRecords.list
    # python: svc.projects().locations().answerRecords().list(**params).execute()
    calls:
      - action: projects.locations.answerRecords.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.answerRecords }}'
      item:
        agentAssistantRecord: '{{ item.agentAssistantRecord }}'
        answerFeedback: '{{ item.answerFeedback }}'
        name: '{{ item.name }}'
  # ── gcp.dialogflow.projects.locations.conversationProfiles.list ──
  - discovery_id: gcp.dialogflow.projects.locations.conversationProfiles.list
    # python: svc.projects().locations().conversationProfiles().list(**params).execute()
    calls:
      - action: projects.locations.conversationProfiles.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.conversationProfiles }}'
      item:
        automatedAgentConfig: '{{ item.automatedAgentConfig }}'
        createTime: '{{ item.createTime }}'
        displayName: '{{ item.displayName }}'
        humanAgentAssistantConfig: '{{ item.humanAgentAssistantConfig }}'
        humanAgentHandoffConfig: '{{ item.humanAgentHandoffConfig }}'
        languageCode: '{{ item.languageCode }}'
        loggingConfig: '{{ item.loggingConfig }}'
        name: '{{ item.name }}'
        newMessageEventNotificationConfig: '{{ item.newMessageEventNotificationConfig }}'
        newRecognitionResultNotificationConfig: '{{ item.newRecognitionResultNotificationConfig }}'
        notificationConfig: '{{ item.notificationConfig }}'
        securitySettings: '{{ item.securitySettings }}'
        sttConfig: '{{ item.sttConfig }}'
        timeZone: '{{ item.timeZone }}'
        ttsConfig: '{{ item.ttsConfig }}'
        updateTime: '{{ item.updateTime }}'
  # ── gcp.dialogflow.projects.locations.conversations.list ──
  - discovery_id: gcp.dialogflow.projects.locations.conversations.list
    # python: svc.projects().locations().conversations().list(**params).execute()
    calls:
      - action: projects.locations.conversations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.conversations }}'
      item:
        conversationProfile: '{{ item.conversationProfile }}'
        conversationStage: '{{ item.conversationStage }}'
        endTime: '{{ item.endTime }}'
        ingestedContextReferences: '{{ item.ingestedContextReferences }}'
        lifecycleState: '{{ item.lifecycleState }}'
        name: '{{ item.name }}'
        phoneNumber: '{{ item.phoneNumber }}'
        startTime: '{{ item.startTime }}'
        telephonyConnectionInfo: '{{ item.telephonyConnectionInfo }}'
  # ── gcp.dialogflow.projects.locations.conversations.messages.list ──
  - discovery_id: gcp.dialogflow.projects.locations.conversations.messages.list
    # python: svc.projects().locations().conversations().messages().list(**params).execute()
    calls:
      - action: projects.locations.conversations.messages.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.messages }}'
      item:
        content: '{{ item.content }}'
        createTime: '{{ item.createTime }}'
        languageCode: '{{ item.languageCode }}'
        messageAnnotation: '{{ item.messageAnnotation }}'
        name: '{{ item.name }}'
        participant: '{{ item.participant }}'
        participantRole: '{{ item.participantRole }}'
        responseMessages: '{{ item.responseMessages }}'
        sendTime: '{{ item.sendTime }}'
        sentimentAnalysis: '{{ item.sentimentAnalysis }}'
  # ── gcp.dialogflow.projects.locations.conversations.participants.list ──
  - discovery_id: gcp.dialogflow.projects.locations.conversations.participants.list
    # python: svc.projects().locations().conversations().participants().list(**params).execute()
    calls:
      - action: projects.locations.conversations.participants.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.participants }}'
      item:
        agentDesktopSource: '{{ item.agentDesktopSource }}'
        documentsMetadataFilters: '{{ item.documentsMetadataFilters }}'
        name: '{{ item.name }}'
        obfuscatedExternalUserId: '{{ item.obfuscatedExternalUserId }}'
        role: '{{ item.role }}'
  # ── gcp.dialogflow.projects.locations.generators.evaluations.list ──
  - discovery_id: gcp.dialogflow.projects.locations.generators.evaluations.list
    # python: svc.projects().locations().generators().evaluations().list(**params).execute()
    calls:
      - action: projects.locations.generators.evaluations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.evaluations }}'
      item:
        completeTime: '{{ item.completeTime }}'
        createTime: '{{ item.createTime }}'
        displayName: '{{ item.displayName }}'
        evaluationStatus: '{{ item.evaluationStatus }}'
        generatorEvaluationConfig: '{{ item.generatorEvaluationConfig }}'
        initialGenerator: '{{ item.initialGenerator }}'
        name: '{{ item.name }}'
        satisfiesPzi: '{{ item.satisfiesPzi }}'
        satisfiesPzs: '{{ item.satisfiesPzs }}'
        summarizationMetrics: '{{ item.summarizationMetrics }}'
  # ── gcp.dialogflow.projects.locations.generators.list ──
  - discovery_id: gcp.dialogflow.projects.locations.generators.list
    # python: svc.projects().locations().generators().list(**params).execute()
    calls:
      - action: projects.locations.generators.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.generators }}'
      item:
        agentCoachingContext: '{{ item.agentCoachingContext }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        freeFormContext: '{{ item.freeFormContext }}'
        inferenceParameter: '{{ item.inferenceParameter }}'
        name: '{{ item.name }}'
        publishedModel: '{{ item.publishedModel }}'
        suggestionDedupingConfig: '{{ item.suggestionDedupingConfig }}'
        summarizationContext: '{{ item.summarizationContext }}'
        tools: '{{ item.tools }}'
        triggerEvent: '{{ item.triggerEvent }}'
        updateTime: '{{ item.updateTime }}'
  # ── gcp.dialogflow.projects.locations.get ──
  - discovery_id: gcp.dialogflow.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # ── gcp.dialogflow.projects.locations.getAgent ──
  - discovery_id: gcp.dialogflow.projects.locations.getAgent
    # python: svc.projects().locations().getAgent(**params).execute()
    calls:
      - action: projects.locations.getAgent
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiVersion: '{{ response.apiVersion }}'
        avatarUri: '{{ response.avatarUri }}'
        classificationThreshold: '{{ response.classificationThreshold }}'
        defaultLanguageCode: '{{ response.defaultLanguageCode }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        enableLogging: '{{ response.enableLogging }}'
        matchMode: '{{ response.matchMode }}'
        parent: '{{ response.parent }}'
        supportedLanguageCodes: '{{ response.supportedLanguageCodes }}'
        tier: '{{ response.tier }}'
        timeZone: '{{ response.timeZone }}'
  # ── gcp.dialogflow.projects.locations.getEncryptionSpec ──
  - discovery_id: gcp.dialogflow.projects.locations.getEncryptionSpec
    # python: svc.projects().locations().getEncryptionSpec(**params).execute()
    calls:
      - action: projects.locations.getEncryptionSpec
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        kmsKey: '{{ response.kmsKey }}'
        name: '{{ response.name }}'
  # ── gcp.dialogflow.projects.locations.knowledgeBases.documents.list ──
  - discovery_id: gcp.dialogflow.projects.locations.knowledgeBases.documents.list
    # python: svc.projects().locations().knowledgeBases().documents().list(**params).execute()
    calls:
      - action: projects.locations.knowledgeBases.documents.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.documents }}'
      item:
        content: '{{ item.content }}'
        contentUri: '{{ item.contentUri }}'
        displayName: '{{ item.displayName }}'
        enableAutoReload: '{{ item.enableAutoReload }}'
        knowledgeTypes: '{{ item.knowledgeTypes }}'
        latestReloadStatus: '{{ item.latestReloadStatus }}'
        metadata: '{{ item.metadata }}'
        mimeType: '{{ item.mimeType }}'
        name: '{{ item.name }}'
        rawContent: '{{ item.rawContent }}'
        state: '{{ item.state }}'
  # ── gcp.dialogflow.projects.locations.knowledgeBases.list ──
  - discovery_id: gcp.dialogflow.projects.locations.knowledgeBases.list
    # python: svc.projects().locations().knowledgeBases().list(**params).execute()
    calls:
      - action: projects.locations.knowledgeBases.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.knowledgeBases }}'
      item:
        displayName: '{{ item.displayName }}'
        languageCode: '{{ item.languageCode }}'
        name: '{{ item.name }}'
  # ── gcp.dialogflow.projects.locations.list ──
  - discovery_id: gcp.dialogflow.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # ── gcp.dialogflow.projects.locations.operations.list ──
  - discovery_id: gcp.dialogflow.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # ── gcp.dialogflow.projects.locations.phoneNumbers.list ──
  - discovery_id: gcp.dialogflow.projects.locations.phoneNumbers.list
    # python: svc.projects().locations().phoneNumbers().list(**params).execute()
    calls:
      - action: projects.locations.phoneNumbers.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.phoneNumbers }}'
      item:
        conversationProfile: '{{ item.conversationProfile }}'
        lifecycleState: '{{ item.lifecycleState }}'
        name: '{{ item.name }}'
        phoneNumber: '{{ item.phoneNumber }}'
  # ── gcp.dialogflow.projects.locations.sipTrunks.list ──
  - discovery_id: gcp.dialogflow.projects.locations.sipTrunks.list
    # python: svc.projects().locations().sipTrunks().list(**params).execute()
    calls:
      - action: projects.locations.sipTrunks.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.sipTrunks }}'
      item:
        connections: '{{ item.connections }}'
        displayName: '{{ item.displayName }}'
        expectedHostname: '{{ item.expectedHostname }}'
        name: '{{ item.name }}'
  # ── gcp.dialogflow.projects.locations.tools.list ──
  - discovery_id: gcp.dialogflow.projects.locations.tools.list
    # python: svc.projects().locations().tools().list(**params).execute()
    calls:
      - action: projects.locations.tools.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.tools }}'
      item:
        actionConfirmationRequirement: '{{ item.actionConfirmationRequirement }}'
        connectorSpec: '{{ item.connectorSpec }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        extensionSpec: '{{ item.extensionSpec }}'
        functionSpec: '{{ item.functionSpec }}'
        name: '{{ item.name }}'
        openApiSpec: '{{ item.openApiSpec }}'
        satisfiesPzi: '{{ item.satisfiesPzi }}'
        satisfiesPzs: '{{ item.satisfiesPzs }}'
        toolKey: '{{ item.toolKey }}'
        updateTime: '{{ item.updateTime }}'
  # ── gcp.dialogflow.projects.operations.list ──
  - discovery_id: gcp.dialogflow.projects.operations.list
    # python: svc.projects().operations().list(**params).execute()
    calls:
      - action: projects.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # ── gcp.dialogflow.projects.phoneNumbers.list ──
  - discovery_id: gcp.dialogflow.projects.phoneNumbers.list
    # python: svc.projects().phoneNumbers().list(**params).execute()
    calls:
      - action: projects.phoneNumbers.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.phoneNumbers }}'
      item:
        conversationProfile: '{{ item.conversationProfile }}'
        lifecycleState: '{{ item.lifecycleState }}'
        name: '{{ item.name }}'
        phoneNumber: '{{ item.phoneNumber }}'

  # ════ DEPENDENT (enrich) operations ════
  # ── gcp.dialogflow.projects.agent.entityTypes.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.agent.entityTypes.get
    for_each: gcp.dialogflow.projects.agent.entityTypes.list
    # python: svc.projects().agent().entityTypes().get(**params).execute()
    calls:
      - action: projects.agent.entityTypes.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        autoExpansionMode: '{{ response.autoExpansionMode }}'
        displayName: '{{ response.displayName }}'
        enableFuzzyExtraction: '{{ response.enableFuzzyExtraction }}'
        entities: '{{ response.entities }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.agent.environments.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.agent.environments.get
    for_each: gcp.dialogflow.projects.agent.environments.list
    # python: svc.projects().agent().environments().get(**params).execute()
    calls:
      - action: projects.agent.environments.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        agentVersion: '{{ response.agentVersion }}'
        description: '{{ response.description }}'
        fulfillment: '{{ response.fulfillment }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
        textToSpeechSettings: '{{ response.textToSpeechSettings }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.agent.environments.users.sessions.contexts.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.agent.environments.users.sessions.contexts.get
    for_each: gcp.dialogflow.projects.agent.sessions.contexts.list
    # python: svc.projects().agent().environments().users().sessions().contexts().get(**params).execute()
    calls:
      - action: projects.agent.environments.users.sessions.contexts.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        lifespanCount: '{{ response.lifespanCount }}'
        name: '{{ response.name }}'
        parameters: '{{ response.parameters }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.agent.environments.users.sessions.entityTypes.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.agent.environments.users.sessions.entityTypes.get
    for_each: gcp.dialogflow.projects.agent.entityTypes.list
    # python: svc.projects().agent().environments().users().sessions().entityTypes().get(**params).execute()
    calls:
      - action: projects.agent.environments.users.sessions.entityTypes.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        entities: '{{ response.entities }}'
        entityOverrideMode: '{{ response.entityOverrideMode }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.agent.intents.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.agent.intents.get
    for_each: gcp.dialogflow.projects.agent.intents.list
    # python: svc.projects().agent().intents().get(**params).execute()
    calls:
      - action: projects.agent.intents.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        action: '{{ response.action }}'
        defaultResponsePlatforms: '{{ response.defaultResponsePlatforms }}'
        displayName: '{{ response.displayName }}'
        endInteraction: '{{ response.endInteraction }}'
        events: '{{ response.events }}'
        followupIntentInfo: '{{ response.followupIntentInfo }}'
        inputContextNames: '{{ response.inputContextNames }}'
        isFallback: '{{ response.isFallback }}'
        liveAgentHandoff: '{{ response.liveAgentHandoff }}'
        messages: '{{ response.messages }}'
        mlDisabled: '{{ response.mlDisabled }}'
        mlEnabled: '{{ response.mlEnabled }}'
        name: '{{ response.name }}'
        outputContexts: '{{ response.outputContexts }}'
        parameters: '{{ response.parameters }}'
        parentFollowupIntentName: '{{ response.parentFollowupIntentName }}'
        priority: '{{ response.priority }}'
        resetContexts: '{{ response.resetContexts }}'
        rootFollowupIntentName: '{{ response.rootFollowupIntentName }}'
        trainingPhrases: '{{ response.trainingPhrases }}'
        webhookState: '{{ response.webhookState }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.agent.knowledgeBases.documents.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.agent.knowledgeBases.documents.get
    for_each: gcp.dialogflow.projects.agent.knowledgeBases.documents.list
    # python: svc.projects().agent().knowledgeBases().documents().get(**params).execute()
    calls:
      - action: projects.agent.knowledgeBases.documents.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        content: '{{ response.content }}'
        contentUri: '{{ response.contentUri }}'
        displayName: '{{ response.displayName }}'
        enableAutoReload: '{{ response.enableAutoReload }}'
        knowledgeTypes: '{{ response.knowledgeTypes }}'
        latestReloadStatus: '{{ response.latestReloadStatus }}'
        metadata: '{{ response.metadata }}'
        mimeType: '{{ response.mimeType }}'
        name: '{{ response.name }}'
        rawContent: '{{ response.rawContent }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.agent.knowledgeBases.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.agent.knowledgeBases.get
    for_each: gcp.dialogflow.projects.agent.knowledgeBases.list
    # python: svc.projects().agent().knowledgeBases().get(**params).execute()
    calls:
      - action: projects.agent.knowledgeBases.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        languageCode: '{{ response.languageCode }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.agent.sessions.contexts.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.agent.sessions.contexts.get
    for_each: gcp.dialogflow.projects.agent.sessions.contexts.list
    # python: svc.projects().agent().sessions().contexts().get(**params).execute()
    calls:
      - action: projects.agent.sessions.contexts.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        lifespanCount: '{{ response.lifespanCount }}'
        name: '{{ response.name }}'
        parameters: '{{ response.parameters }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.agent.sessions.entityTypes.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.agent.sessions.entityTypes.get
    for_each: gcp.dialogflow.projects.agent.entityTypes.list
    # python: svc.projects().agent().sessions().entityTypes().get(**params).execute()
    calls:
      - action: projects.agent.sessions.entityTypes.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        entities: '{{ response.entities }}'
        entityOverrideMode: '{{ response.entityOverrideMode }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.agent.versions.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.agent.versions.get
    for_each: gcp.dialogflow.projects.agent.versions.list
    # python: svc.projects().agent().versions().get(**params).execute()
    calls:
      - action: projects.agent.versions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        versionNumber: '{{ response.versionNumber }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.answerRecords.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.answerRecords.get
    for_each: gcp.dialogflow.projects.answerRecords.list
    # python: svc.projects().answerRecords().get(**params).execute()
    calls:
      - action: projects.answerRecords.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        agentAssistantRecord: '{{ response.agentAssistantRecord }}'
        answerFeedback: '{{ response.answerFeedback }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.conversationProfiles.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.conversationProfiles.get
    for_each: gcp.dialogflow.projects.conversationProfiles.list
    # python: svc.projects().conversationProfiles().get(**params).execute()
    calls:
      - action: projects.conversationProfiles.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        automatedAgentConfig: '{{ response.automatedAgentConfig }}'
        createTime: '{{ response.createTime }}'
        displayName: '{{ response.displayName }}'
        humanAgentAssistantConfig: '{{ response.humanAgentAssistantConfig }}'
        humanAgentHandoffConfig: '{{ response.humanAgentHandoffConfig }}'
        languageCode: '{{ response.languageCode }}'
        loggingConfig: '{{ response.loggingConfig }}'
        name: '{{ response.name }}'
        newMessageEventNotificationConfig: '{{ response.newMessageEventNotificationConfig }}'
        newRecognitionResultNotificationConfig: '{{ response.newRecognitionResultNotificationConfig }}'
        notificationConfig: '{{ response.notificationConfig }}'
        securitySettings: '{{ response.securitySettings }}'
        sttConfig: '{{ response.sttConfig }}'
        timeZone: '{{ response.timeZone }}'
        ttsConfig: '{{ response.ttsConfig }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.conversations.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.conversations.get
    for_each: gcp.dialogflow.projects.locations.conversations.list
    # python: svc.projects().conversations().get(**params).execute()
    calls:
      - action: projects.conversations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        conversationProfile: '{{ response.conversationProfile }}'
        conversationStage: '{{ response.conversationStage }}'
        endTime: '{{ response.endTime }}'
        ingestedContextReferences: '{{ response.ingestedContextReferences }}'
        lifecycleState: '{{ response.lifecycleState }}'
        name: '{{ response.name }}'
        phoneNumber: '{{ response.phoneNumber }}'
        startTime: '{{ response.startTime }}'
        telephonyConnectionInfo: '{{ response.telephonyConnectionInfo }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.conversations.participants.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.conversations.participants.get
    for_each: gcp.dialogflow.projects.locations.conversations.participants.list
    # python: svc.projects().conversations().participants().get(**params).execute()
    calls:
      - action: projects.conversations.participants.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        agentDesktopSource: '{{ response.agentDesktopSource }}'
        documentsMetadataFilters: '{{ response.documentsMetadataFilters }}'
        name: '{{ response.name }}'
        obfuscatedExternalUserId: '{{ response.obfuscatedExternalUserId }}'
        role: '{{ response.role }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.knowledgeBases.documents.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.knowledgeBases.documents.get
    for_each: gcp.dialogflow.projects.agent.knowledgeBases.documents.list
    # python: svc.projects().knowledgeBases().documents().get(**params).execute()
    calls:
      - action: projects.knowledgeBases.documents.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        content: '{{ response.content }}'
        contentUri: '{{ response.contentUri }}'
        displayName: '{{ response.displayName }}'
        enableAutoReload: '{{ response.enableAutoReload }}'
        knowledgeTypes: '{{ response.knowledgeTypes }}'
        latestReloadStatus: '{{ response.latestReloadStatus }}'
        metadata: '{{ response.metadata }}'
        mimeType: '{{ response.mimeType }}'
        name: '{{ response.name }}'
        rawContent: '{{ response.rawContent }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.knowledgeBases.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.knowledgeBases.get
    for_each: gcp.dialogflow.projects.agent.knowledgeBases.list
    # python: svc.projects().knowledgeBases().get(**params).execute()
    calls:
      - action: projects.knowledgeBases.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        languageCode: '{{ response.languageCode }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.locations.agent.entityTypes.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.locations.agent.entityTypes.get
    for_each: gcp.dialogflow.projects.agent.entityTypes.list
    # python: svc.projects().locations().agent().entityTypes().get(**params).execute()
    calls:
      - action: projects.locations.agent.entityTypes.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        autoExpansionMode: '{{ response.autoExpansionMode }}'
        displayName: '{{ response.displayName }}'
        enableFuzzyExtraction: '{{ response.enableFuzzyExtraction }}'
        entities: '{{ response.entities }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.locations.agent.environments.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.locations.agent.environments.get
    for_each: gcp.dialogflow.projects.agent.environments.list
    # python: svc.projects().locations().agent().environments().get(**params).execute()
    calls:
      - action: projects.locations.agent.environments.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        agentVersion: '{{ response.agentVersion }}'
        description: '{{ response.description }}'
        fulfillment: '{{ response.fulfillment }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
        textToSpeechSettings: '{{ response.textToSpeechSettings }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.locations.agent.environments.users.sessions.contexts.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.locations.agent.environments.users.sessions.contexts.get
    for_each: gcp.dialogflow.projects.agent.sessions.contexts.list
    # python: svc.projects().locations().agent().environments().users().sessions().contexts().get(**params).execute()
    calls:
      - action: projects.locations.agent.environments.users.sessions.contexts.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        lifespanCount: '{{ response.lifespanCount }}'
        name: '{{ response.name }}'
        parameters: '{{ response.parameters }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.locations.agent.environments.users.sessions.entityTypes.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.locations.agent.environments.users.sessions.entityTypes.get
    for_each: gcp.dialogflow.projects.agent.entityTypes.list
    # python: svc.projects().locations().agent().environments().users().sessions().entityTypes().get(**params).execute()
    calls:
      - action: projects.locations.agent.environments.users.sessions.entityTypes.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        entities: '{{ response.entities }}'
        entityOverrideMode: '{{ response.entityOverrideMode }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.locations.agent.intents.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.locations.agent.intents.get
    for_each: gcp.dialogflow.projects.agent.intents.list
    # python: svc.projects().locations().agent().intents().get(**params).execute()
    calls:
      - action: projects.locations.agent.intents.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        action: '{{ response.action }}'
        defaultResponsePlatforms: '{{ response.defaultResponsePlatforms }}'
        displayName: '{{ response.displayName }}'
        endInteraction: '{{ response.endInteraction }}'
        events: '{{ response.events }}'
        followupIntentInfo: '{{ response.followupIntentInfo }}'
        inputContextNames: '{{ response.inputContextNames }}'
        isFallback: '{{ response.isFallback }}'
        liveAgentHandoff: '{{ response.liveAgentHandoff }}'
        messages: '{{ response.messages }}'
        mlDisabled: '{{ response.mlDisabled }}'
        mlEnabled: '{{ response.mlEnabled }}'
        name: '{{ response.name }}'
        outputContexts: '{{ response.outputContexts }}'
        parameters: '{{ response.parameters }}'
        parentFollowupIntentName: '{{ response.parentFollowupIntentName }}'
        priority: '{{ response.priority }}'
        resetContexts: '{{ response.resetContexts }}'
        rootFollowupIntentName: '{{ response.rootFollowupIntentName }}'
        trainingPhrases: '{{ response.trainingPhrases }}'
        webhookState: '{{ response.webhookState }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.locations.agent.sessions.contexts.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.locations.agent.sessions.contexts.get
    for_each: gcp.dialogflow.projects.agent.sessions.contexts.list
    # python: svc.projects().locations().agent().sessions().contexts().get(**params).execute()
    calls:
      - action: projects.locations.agent.sessions.contexts.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        lifespanCount: '{{ response.lifespanCount }}'
        name: '{{ response.name }}'
        parameters: '{{ response.parameters }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.locations.agent.sessions.entityTypes.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.locations.agent.sessions.entityTypes.get
    for_each: gcp.dialogflow.projects.agent.entityTypes.list
    # python: svc.projects().locations().agent().sessions().entityTypes().get(**params).execute()
    calls:
      - action: projects.locations.agent.sessions.entityTypes.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        entities: '{{ response.entities }}'
        entityOverrideMode: '{{ response.entityOverrideMode }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.locations.agent.versions.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.locations.agent.versions.get
    for_each: gcp.dialogflow.projects.agent.versions.list
    # python: svc.projects().locations().agent().versions().get(**params).execute()
    calls:
      - action: projects.locations.agent.versions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        versionNumber: '{{ response.versionNumber }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.locations.answerRecords.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.locations.answerRecords.get
    for_each: gcp.dialogflow.projects.answerRecords.list
    # python: svc.projects().locations().answerRecords().get(**params).execute()
    calls:
      - action: projects.locations.answerRecords.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        agentAssistantRecord: '{{ response.agentAssistantRecord }}'
        answerFeedback: '{{ response.answerFeedback }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.locations.conversationProfiles.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.locations.conversationProfiles.get
    for_each: gcp.dialogflow.projects.conversationProfiles.list
    # python: svc.projects().locations().conversationProfiles().get(**params).execute()
    calls:
      - action: projects.locations.conversationProfiles.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        automatedAgentConfig: '{{ response.automatedAgentConfig }}'
        createTime: '{{ response.createTime }}'
        displayName: '{{ response.displayName }}'
        humanAgentAssistantConfig: '{{ response.humanAgentAssistantConfig }}'
        humanAgentHandoffConfig: '{{ response.humanAgentHandoffConfig }}'
        languageCode: '{{ response.languageCode }}'
        loggingConfig: '{{ response.loggingConfig }}'
        name: '{{ response.name }}'
        newMessageEventNotificationConfig: '{{ response.newMessageEventNotificationConfig }}'
        newRecognitionResultNotificationConfig: '{{ response.newRecognitionResultNotificationConfig }}'
        notificationConfig: '{{ response.notificationConfig }}'
        securitySettings: '{{ response.securitySettings }}'
        sttConfig: '{{ response.sttConfig }}'
        timeZone: '{{ response.timeZone }}'
        ttsConfig: '{{ response.ttsConfig }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.locations.conversations.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.locations.conversations.get
    for_each: gcp.dialogflow.projects.locations.conversations.list
    # python: svc.projects().locations().conversations().get(**params).execute()
    calls:
      - action: projects.locations.conversations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        conversationProfile: '{{ response.conversationProfile }}'
        conversationStage: '{{ response.conversationStage }}'
        endTime: '{{ response.endTime }}'
        ingestedContextReferences: '{{ response.ingestedContextReferences }}'
        lifecycleState: '{{ response.lifecycleState }}'
        name: '{{ response.name }}'
        phoneNumber: '{{ response.phoneNumber }}'
        startTime: '{{ response.startTime }}'
        telephonyConnectionInfo: '{{ response.telephonyConnectionInfo }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.locations.conversations.participants.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.locations.conversations.participants.get
    for_each: gcp.dialogflow.projects.locations.conversations.participants.list
    # python: svc.projects().locations().conversations().participants().get(**params).execute()
    calls:
      - action: projects.locations.conversations.participants.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        agentDesktopSource: '{{ response.agentDesktopSource }}'
        documentsMetadataFilters: '{{ response.documentsMetadataFilters }}'
        name: '{{ response.name }}'
        obfuscatedExternalUserId: '{{ response.obfuscatedExternalUserId }}'
        role: '{{ response.role }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.locations.generators.evaluations.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.locations.generators.evaluations.get
    for_each: gcp.dialogflow.projects.locations.generators.evaluations.list
    # python: svc.projects().locations().generators().evaluations().get(**params).execute()
    calls:
      - action: projects.locations.generators.evaluations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        completeTime: '{{ response.completeTime }}'
        createTime: '{{ response.createTime }}'
        displayName: '{{ response.displayName }}'
        evaluationStatus: '{{ response.evaluationStatus }}'
        generatorEvaluationConfig: '{{ response.generatorEvaluationConfig }}'
        initialGenerator: '{{ response.initialGenerator }}'
        name: '{{ response.name }}'
        satisfiesPzi: '{{ response.satisfiesPzi }}'
        satisfiesPzs: '{{ response.satisfiesPzs }}'
        summarizationMetrics: '{{ response.summarizationMetrics }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.locations.generators.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.locations.generators.get
    for_each: gcp.dialogflow.projects.generators.list
    # python: svc.projects().locations().generators().get(**params).execute()
    calls:
      - action: projects.locations.generators.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        agentCoachingContext: '{{ response.agentCoachingContext }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        freeFormContext: '{{ response.freeFormContext }}'
        inferenceParameter: '{{ response.inferenceParameter }}'
        name: '{{ response.name }}'
        publishedModel: '{{ response.publishedModel }}'
        suggestionDedupingConfig: '{{ response.suggestionDedupingConfig }}'
        summarizationContext: '{{ response.summarizationContext }}'
        tools: '{{ response.tools }}'
        triggerEvent: '{{ response.triggerEvent }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.locations.knowledgeBases.documents.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.locations.knowledgeBases.documents.get
    for_each: gcp.dialogflow.projects.agent.knowledgeBases.documents.list
    # python: svc.projects().locations().knowledgeBases().documents().get(**params).execute()
    calls:
      - action: projects.locations.knowledgeBases.documents.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        content: '{{ response.content }}'
        contentUri: '{{ response.contentUri }}'
        displayName: '{{ response.displayName }}'
        enableAutoReload: '{{ response.enableAutoReload }}'
        knowledgeTypes: '{{ response.knowledgeTypes }}'
        latestReloadStatus: '{{ response.latestReloadStatus }}'
        metadata: '{{ response.metadata }}'
        mimeType: '{{ response.mimeType }}'
        name: '{{ response.name }}'
        rawContent: '{{ response.rawContent }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.locations.knowledgeBases.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.locations.knowledgeBases.get
    for_each: gcp.dialogflow.projects.agent.knowledgeBases.list
    # python: svc.projects().locations().knowledgeBases().get(**params).execute()
    calls:
      - action: projects.locations.knowledgeBases.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        languageCode: '{{ response.languageCode }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.locations.operations.get
    for_each: gcp.dialogflow.projects.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.locations.sipTrunks.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.locations.sipTrunks.get
    for_each: gcp.dialogflow.projects.locations.sipTrunks.list
    # python: svc.projects().locations().sipTrunks().get(**params).execute()
    calls:
      - action: projects.locations.sipTrunks.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        connections: '{{ response.connections }}'
        displayName: '{{ response.displayName }}'
        expectedHostname: '{{ response.expectedHostname }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.locations.tools.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.locations.tools.get
    for_each: gcp.dialogflow.projects.locations.tools.list
    # python: svc.projects().locations().tools().get(**params).execute()
    calls:
      - action: projects.locations.tools.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        actionConfirmationRequirement: '{{ response.actionConfirmationRequirement }}'
        connectorSpec: '{{ response.connectorSpec }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        extensionSpec: '{{ response.extensionSpec }}'
        functionSpec: '{{ response.functionSpec }}'
        name: '{{ response.name }}'
        openApiSpec: '{{ response.openApiSpec }}'
        satisfiesPzi: '{{ response.satisfiesPzi }}'
        satisfiesPzs: '{{ response.satisfiesPzs }}'
        toolKey: '{{ response.toolKey }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # ── gcp.dialogflow.projects.operations.get [dependent] ──
  - discovery_id: gcp.dialogflow.projects.operations.get
    for_each: gcp.dialogflow.projects.operations.list
    # python: svc.projects().operations().get(**params).execute()
    calls:
      - action: projects.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
