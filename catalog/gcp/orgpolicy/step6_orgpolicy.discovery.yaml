# ============================================================
# Discovery YAML — orgpolicy (v2)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 14 | independent: 7 | dependent: 7
# ops with fields: 14/14 | dep ops with chain: 7/7
# ============================================================
version: '1.0'
provider: gcp
service: orgpolicy

services:
  client: orgpolicy
  module: "googleapiclient.discovery.build('orgpolicy', 'v2')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists constraints that could be applied on the specified resource.
  # ── gcp.orgpolicy.folders.constraints.list ──
  - discovery_id: gcp.orgpolicy.folders.constraints.list
    # python: svc.folders().constraints().list(**params).execute()
    calls:
      - action: folders.constraints.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.constraints }}'
      item:
        booleanConstraint: '{{ item.booleanConstraint }}'
        constraintDefault: '{{ item.constraintDefault }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        equivalentConstraint: '{{ item.equivalentConstraint }}'
        listConstraint: '{{ item.listConstraint }}'
        name: '{{ item.name }}'
        supportsDryRun: '{{ item.supportsDryRun }}'
        supportsSimulation: '{{ item.supportsSimulation }}'
  # Retrieves all of the policies that exist on a particular resource.
  # ── gcp.orgpolicy.folders.policies.list ──
  - discovery_id: gcp.orgpolicy.folders.policies.list
    # python: svc.folders().policies().list(**params).execute()
    calls:
      - action: folders.policies.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.policies }}'
      item:
        alternate: '{{ item.alternate }}'
        dryRunSpec: '{{ item.dryRunSpec }}'
        name: '{{ item.name }}'
        spec: '{{ item.spec }}'
  # Lists constraints that could be applied on the specified resource.
  # ── gcp.orgpolicy.organizations.constraints.list ──
  - discovery_id: gcp.orgpolicy.organizations.constraints.list
    # python: svc.organizations().constraints().list(**params).execute()
    calls:
      - action: organizations.constraints.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.constraints }}'
      item:
        booleanConstraint: '{{ item.booleanConstraint }}'
        constraintDefault: '{{ item.constraintDefault }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        equivalentConstraint: '{{ item.equivalentConstraint }}'
        listConstraint: '{{ item.listConstraint }}'
        name: '{{ item.name }}'
        supportsDryRun: '{{ item.supportsDryRun }}'
        supportsSimulation: '{{ item.supportsSimulation }}'
  # Retrieves all of the custom constraints that exist on a particular organization resource.
  # ── gcp.orgpolicy.organizations.customConstraints.list ──
  - discovery_id: gcp.orgpolicy.organizations.customConstraints.list
    # python: svc.organizations().customConstraints().list(**params).execute()
    calls:
      - action: organizations.customConstraints.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.customConstraints }}'
      item:
        actionType: '{{ item.actionType }}'
        condition: '{{ item.condition }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        methodTypes: '{{ item.methodTypes }}'
        name: '{{ item.name }}'
        resourceTypes: '{{ item.resourceTypes }}'
        updateTime: '{{ item.updateTime }}'
  # Retrieves all of the policies that exist on a particular resource.
  # ── gcp.orgpolicy.organizations.policies.list ──
  - discovery_id: gcp.orgpolicy.organizations.policies.list
    # python: svc.organizations().policies().list(**params).execute()
    calls:
      - action: organizations.policies.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.policies }}'
      item:
        alternate: '{{ item.alternate }}'
        dryRunSpec: '{{ item.dryRunSpec }}'
        name: '{{ item.name }}'
        spec: '{{ item.spec }}'
  # Lists constraints that could be applied on the specified resource.
  # ── gcp.orgpolicy.projects.constraints.list ──
  - discovery_id: gcp.orgpolicy.projects.constraints.list
    # python: svc.projects().constraints().list(**params).execute()
    calls:
      - action: projects.constraints.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.constraints }}'
      item:
        booleanConstraint: '{{ item.booleanConstraint }}'
        constraintDefault: '{{ item.constraintDefault }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        equivalentConstraint: '{{ item.equivalentConstraint }}'
        listConstraint: '{{ item.listConstraint }}'
        name: '{{ item.name }}'
        supportsDryRun: '{{ item.supportsDryRun }}'
        supportsSimulation: '{{ item.supportsSimulation }}'
  # Retrieves all of the policies that exist on a particular resource.
  # ── gcp.orgpolicy.projects.policies.list ──
  - discovery_id: gcp.orgpolicy.projects.policies.list
    # python: svc.projects().policies().list(**params).execute()
    calls:
      - action: projects.policies.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.policies }}'
      item:
        alternate: '{{ item.alternate }}'
        dryRunSpec: '{{ item.dryRunSpec }}'
        name: '{{ item.name }}'
        spec: '{{ item.spec }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets a policy on a resource. If no policy is set on the resource, `NOT_FOUND` is returned. The `etag
  # ── gcp.orgpolicy.folders.policies.get [dependent] ──
  - discovery_id: gcp.orgpolicy.folders.policies.get
    for_each: gcp.orgpolicy.organizations.policies.list
    # python: svc.folders().policies().get(**params).execute()
    calls:
      - action: folders.policies.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        alternate: '{{ response.alternate }}'
        dryRunSpec: '{{ response.dryRunSpec }}'
        name: '{{ response.name }}'
        spec: '{{ response.spec }}'
    # required_params: ['name']
  # Gets the effective policy on a resource. This is the result of merging policies in the resource hier
  # ── gcp.orgpolicy.folders.policies.getEffectivePolicy [dependent] ──
  - discovery_id: gcp.orgpolicy.folders.policies.getEffectivePolicy
    for_each: gcp.orgpolicy.organizations.policies.list
    # python: svc.folders().policies().getEffectivePolicy(**params).execute()
    calls:
      - action: folders.policies.getEffectivePolicy
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        alternate: '{{ response.alternate }}'
        dryRunSpec: '{{ response.dryRunSpec }}'
        name: '{{ response.name }}'
        spec: '{{ response.spec }}'
    # required_params: ['name']
  # Gets a custom or managed constraint. Returns a `google.rpc.Status` with `google.rpc.Code.NOT_FOUND` 
  # ── gcp.orgpolicy.organizations.customConstraints.get [dependent] ──
  - discovery_id: gcp.orgpolicy.organizations.customConstraints.get
    for_each: gcp.orgpolicy.organizations.customConstraints.list
    # python: svc.organizations().customConstraints().get(**params).execute()
    calls:
      - action: organizations.customConstraints.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        actionType: '{{ response.actionType }}'
        condition: '{{ response.condition }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        methodTypes: '{{ response.methodTypes }}'
        name: '{{ response.name }}'
        resourceTypes: '{{ response.resourceTypes }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets a policy on a resource. If no policy is set on the resource, `NOT_FOUND` is returned. The `etag
  # ── gcp.orgpolicy.organizations.policies.get [dependent] ──
  - discovery_id: gcp.orgpolicy.organizations.policies.get
    for_each: gcp.orgpolicy.organizations.policies.list
    # python: svc.organizations().policies().get(**params).execute()
    calls:
      - action: organizations.policies.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        alternate: '{{ response.alternate }}'
        dryRunSpec: '{{ response.dryRunSpec }}'
        name: '{{ response.name }}'
        spec: '{{ response.spec }}'
    # required_params: ['name']
  # Gets the effective policy on a resource. This is the result of merging policies in the resource hier
  # ── gcp.orgpolicy.organizations.policies.getEffectivePolicy [dependent] ──
  - discovery_id: gcp.orgpolicy.organizations.policies.getEffectivePolicy
    for_each: gcp.orgpolicy.organizations.policies.list
    # python: svc.organizations().policies().getEffectivePolicy(**params).execute()
    calls:
      - action: organizations.policies.getEffectivePolicy
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        alternate: '{{ response.alternate }}'
        dryRunSpec: '{{ response.dryRunSpec }}'
        name: '{{ response.name }}'
        spec: '{{ response.spec }}'
    # required_params: ['name']
  # Gets a policy on a resource. If no policy is set on the resource, `NOT_FOUND` is returned. The `etag
  # ── gcp.orgpolicy.projects.policies.get [dependent] ──
  - discovery_id: gcp.orgpolicy.projects.policies.get
    for_each: gcp.orgpolicy.organizations.policies.list
    # python: svc.projects().policies().get(**params).execute()
    calls:
      - action: projects.policies.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        alternate: '{{ response.alternate }}'
        dryRunSpec: '{{ response.dryRunSpec }}'
        name: '{{ response.name }}'
        spec: '{{ response.spec }}'
    # required_params: ['name']
  # Gets the effective policy on a resource. This is the result of merging policies in the resource hier
  # ── gcp.orgpolicy.projects.policies.getEffectivePolicy [dependent] ──
  - discovery_id: gcp.orgpolicy.projects.policies.getEffectivePolicy
    for_each: gcp.orgpolicy.organizations.policies.list
    # python: svc.projects().policies().getEffectivePolicy(**params).execute()
    calls:
      - action: projects.policies.getEffectivePolicy
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        alternate: '{{ response.alternate }}'
        dryRunSpec: '{{ response.dryRunSpec }}'
        name: '{{ response.name }}'
        spec: '{{ response.spec }}'
    # required_params: ['name']
