# ============================================================
# Discovery YAML — cloudidentity (v1)
# Generated: 2026-02-21T03:06:39Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 33 | independent: 20 | dependent: 13
# ops with fields: 33/33 | dep ops with chain: 13/13
# ============================================================
version: '1.0'
provider: gcp
service: cloudidentity

services:
  client: cloudidentity
  module: "googleapiclient.discovery.build('cloudidentity', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Retrieves a list of UserInvitation resources. **Note:** New consumer accounts with the customer's ve
  # ── gcp.cloudidentity.customers.userinvitations.list ──
  - discovery_id: gcp.cloudidentity.customers.userinvitations.list
    # python: svc.customers().userinvitations().list(**params).execute()
    calls:
      - action: customers.userinvitations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.userinvitations }}'
      item:
        mailsSentCount: '{{ item.mailsSentCount }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
        updateTime: '{{ item.updateTime }}'
  # Lists the client states for the given search query.
  # ── gcp.cloudidentity.devices.deviceUsers.clientStates.list ──
  - discovery_id: gcp.cloudidentity.devices.deviceUsers.clientStates.list
    # python: svc.devices().deviceUsers().clientStates().list(**params).execute()
    calls:
      - action: devices.deviceUsers.clientStates.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.clientStates }}'
      item:
        assetTags: '{{ item.assetTags }}'
        complianceState: '{{ item.complianceState }}'
        createTime: '{{ item.createTime }}'
        customId: '{{ item.customId }}'
        healthScore: '{{ item.healthScore }}'
        keyValuePairs: '{{ item.keyValuePairs }}'
        lastUpdateTime: '{{ item.lastUpdateTime }}'
        managed: '{{ item.managed }}'
        name: '{{ item.name }}'
        ownerType: '{{ item.ownerType }}'
        scoreReason: '{{ item.scoreReason }}'
  # Lists/Searches DeviceUsers.
  # ── gcp.cloudidentity.devices.deviceUsers.list ──
  - discovery_id: gcp.cloudidentity.devices.deviceUsers.list
    # python: svc.devices().deviceUsers().list(**params).execute()
    calls:
      - action: devices.deviceUsers.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.deviceUsers }}'
      item:
        compromisedState: '{{ item.compromisedState }}'
        createTime: '{{ item.createTime }}'
        firstSyncTime: '{{ item.firstSyncTime }}'
        languageCode: '{{ item.languageCode }}'
        lastSyncTime: '{{ item.lastSyncTime }}'
        managementState: '{{ item.managementState }}'
        name: '{{ item.name }}'
        passwordState: '{{ item.passwordState }}'
        userAgent: '{{ item.userAgent }}'
        userEmail: '{{ item.userEmail }}'
  # Looks up resource names of the DeviceUsers associated with the caller's credentials, as well as the 
  # ── gcp.cloudidentity.devices.deviceUsers.lookup ──
  - discovery_id: gcp.cloudidentity.devices.deviceUsers.lookup
    # python: svc.devices().deviceUsers().lookup(**params).execute()
    calls:
      - action: devices.deviceUsers.lookup
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        customer: '{{ response.customer }}'
        names: '{{ response.names }}'
  # Lists/Searches devices.
  # ── gcp.cloudidentity.devices.list ──
  - discovery_id: gcp.cloudidentity.devices.list
    # python: svc.devices().list(**params).execute()
    calls:
      - action: devices.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.devices }}'
      item:
        androidSpecificAttributes: '{{ item.androidSpecificAttributes }}'
        assetTag: '{{ item.assetTag }}'
        basebandVersion: '{{ item.basebandVersion }}'
        bootloaderVersion: '{{ item.bootloaderVersion }}'
        brand: '{{ item.brand }}'
        buildNumber: '{{ item.buildNumber }}'
        compromisedState: '{{ item.compromisedState }}'
        createTime: '{{ item.createTime }}'
        deviceId: '{{ item.deviceId }}'
        deviceType: '{{ item.deviceType }}'
        enabledDeveloperOptions: '{{ item.enabledDeveloperOptions }}'
        enabledUsbDebugging: '{{ item.enabledUsbDebugging }}'
        encryptionState: '{{ item.encryptionState }}'
        endpointVerificationSpecificAttributes: '{{ item.endpointVerificationSpecificAttributes }}'
        hostname: '{{ item.hostname }}'
        imei: '{{ item.imei }}'
        kernelVersion: '{{ item.kernelVersion }}'
        lastSyncTime: '{{ item.lastSyncTime }}'
        managementState: '{{ item.managementState }}'
        manufacturer: '{{ item.manufacturer }}'
        meid: '{{ item.meid }}'
        model: '{{ item.model }}'
        name: '{{ item.name }}'
        networkOperator: '{{ item.networkOperator }}'
        osVersion: '{{ item.osVersion }}'
        otherAccounts: '{{ item.otherAccounts }}'
        ownerType: '{{ item.ownerType }}'
        releaseVersion: '{{ item.releaseVersion }}'
        securityPatchTime: '{{ item.securityPatchTime }}'
        serialNumber: '{{ item.serialNumber }}'
        unifiedDeviceId: '{{ item.unifiedDeviceId }}'
        wifiMacAddresses: '{{ item.wifiMacAddresses }}'
  # Lists the `Group` resources under a customer or namespace.
  # ── gcp.cloudidentity.groups.list ──
  - discovery_id: gcp.cloudidentity.groups.list
    # python: svc.groups().list(**params).execute()
    calls:
      - action: groups.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.groups }}'
      item:
        additionalGroupKeys: '{{ item.additionalGroupKeys }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        dynamicGroupMetadata: '{{ item.dynamicGroupMetadata }}'
        groupKey: '{{ item.groupKey }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        parent: '{{ item.parent }}'
        updateTime: '{{ item.updateTime }}'
  # Looks up the [resource name](https://cloud.google.com/apis/design/resource_names) of a `Group` by it
  # ── gcp.cloudidentity.groups.lookup ──
  - discovery_id: gcp.cloudidentity.groups.lookup
    # python: svc.groups().lookup(**params).execute()
    calls:
      - action: groups.lookup
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
  # Check a potential member for membership in a group. **Note:** This feature is only available to Goog
  # ── gcp.cloudidentity.groups.memberships.checkTransitiveMembership ──
  - discovery_id: gcp.cloudidentity.groups.memberships.checkTransitiveMembership
    # python: svc.groups().memberships().checkTransitiveMembership(**params).execute()
    calls:
      - action: groups.memberships.checkTransitiveMembership
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        hasMembership: '{{ response.hasMembership }}'
  # Get a membership graph of just a member or both a member and a group. **Note:** This feature is only
  # ── gcp.cloudidentity.groups.memberships.getMembershipGraph ──
  - discovery_id: gcp.cloudidentity.groups.memberships.getMembershipGraph
    # python: svc.groups().memberships().getMembershipGraph(**params).execute()
    calls:
      - action: groups.memberships.getMembershipGraph
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
  # Lists the `Membership`s within a `Group`.
  # ── gcp.cloudidentity.groups.memberships.list ──
  - discovery_id: gcp.cloudidentity.groups.memberships.list
    # python: svc.groups().memberships().list(**params).execute()
    calls:
      - action: groups.memberships.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.memberships }}'
      item:
        createTime: '{{ item.createTime }}'
        deliverySetting: '{{ item.deliverySetting }}'
        name: '{{ item.name }}'
        preferredMemberKey: '{{ item.preferredMemberKey }}'
        roles: '{{ item.roles }}'
        type: '{{ item.type }}'
        updateTime: '{{ item.updateTime }}'
  # Looks up the [resource name](https://cloud.google.com/apis/design/resource_names) of a `Membership` 
  # ── gcp.cloudidentity.groups.memberships.lookup ──
  - discovery_id: gcp.cloudidentity.groups.memberships.lookup
    # python: svc.groups().memberships().lookup(**params).execute()
    calls:
      - action: groups.memberships.lookup
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
  # Searches direct groups of a member.
  # ── gcp.cloudidentity.groups.memberships.searchDirectGroups ──
  - discovery_id: gcp.cloudidentity.groups.memberships.searchDirectGroups
    # python: svc.groups().memberships().searchDirectGroups(**params).execute()
    calls:
      - action: groups.memberships.searchDirectGroups
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        memberships: '{{ response.memberships }}'
  # Search transitive groups of a member. **Note:** This feature is only available to Google Workspace E
  # ── gcp.cloudidentity.groups.memberships.searchTransitiveGroups ──
  - discovery_id: gcp.cloudidentity.groups.memberships.searchTransitiveGroups
    # python: svc.groups().memberships().searchTransitiveGroups(**params).execute()
    calls:
      - action: groups.memberships.searchTransitiveGroups
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        memberships: '{{ response.memberships }}'
  # Search transitive memberships of a group. **Note:** This feature is only available to Google Workspa
  # ── gcp.cloudidentity.groups.memberships.searchTransitiveMemberships ──
  - discovery_id: gcp.cloudidentity.groups.memberships.searchTransitiveMemberships
    # python: svc.groups().memberships().searchTransitiveMemberships(**params).execute()
    calls:
      - action: groups.memberships.searchTransitiveMemberships
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        memberships: '{{ response.memberships }}'
  # Searches for `Group` resources matching a specified query.
  # ── gcp.cloudidentity.groups.search ──
  - discovery_id: gcp.cloudidentity.groups.search
    # python: svc.groups().search(**params).execute()
    calls:
      - action: groups.search
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.groups }}'
      item:
        additionalGroupKeys: '{{ item.additionalGroupKeys }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        dynamicGroupMetadata: '{{ item.dynamicGroupMetadata }}'
        groupKey: '{{ item.groupKey }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        parent: '{{ item.parent }}'
        updateTime: '{{ item.updateTime }}'
  # Lists InboundOidcSsoProfile objects for a Google enterprise customer.
  # ── gcp.cloudidentity.inboundOidcSsoProfiles.list ──
  - discovery_id: gcp.cloudidentity.inboundOidcSsoProfiles.list
    # python: svc.inboundOidcSsoProfiles().list(**params).execute()
    calls:
      - action: inboundOidcSsoProfiles.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.inboundOidcSsoProfiles }}'
      item:
        customer: '{{ item.customer }}'
        displayName: '{{ item.displayName }}'
        idpConfig: '{{ item.idpConfig }}'
        name: '{{ item.name }}'
        rpConfig: '{{ item.rpConfig }}'
  # Returns a list of IdpCredentials in an InboundSamlSsoProfile.
  # ── gcp.cloudidentity.inboundSamlSsoProfiles.idpCredentials.list ──
  - discovery_id: gcp.cloudidentity.inboundSamlSsoProfiles.idpCredentials.list
    # python: svc.inboundSamlSsoProfiles().idpCredentials().list(**params).execute()
    calls:
      - action: inboundSamlSsoProfiles.idpCredentials.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.idpCredentials }}'
      item:
        dsaKeyInfo: '{{ item.dsaKeyInfo }}'
        name: '{{ item.name }}'
        rsaKeyInfo: '{{ item.rsaKeyInfo }}'
        updateTime: '{{ item.updateTime }}'
  # Lists InboundSamlSsoProfiles for a customer.
  # ── gcp.cloudidentity.inboundSamlSsoProfiles.list ──
  - discovery_id: gcp.cloudidentity.inboundSamlSsoProfiles.list
    # python: svc.inboundSamlSsoProfiles().list(**params).execute()
    calls:
      - action: inboundSamlSsoProfiles.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.inboundSamlSsoProfiles }}'
      item:
        customer: '{{ item.customer }}'
        displayName: '{{ item.displayName }}'
        idpConfig: '{{ item.idpConfig }}'
        name: '{{ item.name }}'
        spConfig: '{{ item.spConfig }}'
  # Lists the InboundSsoAssignments for a `Customer`.
  # ── gcp.cloudidentity.inboundSsoAssignments.list ──
  - discovery_id: gcp.cloudidentity.inboundSsoAssignments.list
    # python: svc.inboundSsoAssignments().list(**params).execute()
    calls:
      - action: inboundSsoAssignments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.inboundSsoAssignments }}'
      item:
        customer: '{{ item.customer }}'
        name: '{{ item.name }}'
        oidcSsoInfo: '{{ item.oidcSsoInfo }}'
        rank: '{{ item.rank }}'
        samlSsoInfo: '{{ item.samlSsoInfo }}'
        signInBehavior: '{{ item.signInBehavior }}'
        ssoMode: '{{ item.ssoMode }}'
        targetGroup: '{{ item.targetGroup }}'
        targetOrgUnit: '{{ item.targetOrgUnit }}'
  # List policies.
  # ── gcp.cloudidentity.policies.list ──
  - discovery_id: gcp.cloudidentity.policies.list
    # python: svc.policies().list(**params).execute()
    calls:
      - action: policies.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.policies }}'
      item:
        customer: '{{ item.customer }}'
        name: '{{ item.name }}'
        policyQuery: '{{ item.policyQuery }}'
        setting: '{{ item.setting }}'
        type: '{{ item.type }}'

  # ════ DEPENDENT (enrich) operations ════
  # Retrieves a UserInvitation resource. **Note:** New consumer accounts with the customer's verified do
  # ── gcp.cloudidentity.customers.userinvitations.get [dependent] ──
  - discovery_id: gcp.cloudidentity.customers.userinvitations.get
    for_each: gcp.cloudidentity.customers.userinvitations.list
    # python: svc.customers().userinvitations().get(**params).execute()
    calls:
      - action: customers.userinvitations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        mailsSentCount: '{{ response.mailsSentCount }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Verifies whether a user account is eligible to receive a UserInvitation (is an unmanaged account). E
  # ── gcp.cloudidentity.customers.userinvitations.isInvitableUser [dependent] ──
  - discovery_id: gcp.cloudidentity.customers.userinvitations.isInvitableUser
    for_each: gcp.cloudidentity.customers.userinvitations.list
    # python: svc.customers().userinvitations().isInvitableUser(**params).execute()
    calls:
      - action: customers.userinvitations.isInvitableUser
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        isInvitableUser: '{{ response.isInvitableUser }}'
    # required_params: ['name']
  # Gets the client state for the device user
  # ── gcp.cloudidentity.devices.deviceUsers.clientStates.get [dependent] ──
  - discovery_id: gcp.cloudidentity.devices.deviceUsers.clientStates.get
    for_each: gcp.cloudidentity.devices.deviceUsers.clientStates.list
    # python: svc.devices().deviceUsers().clientStates().get(**params).execute()
    calls:
      - action: devices.deviceUsers.clientStates.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        assetTags: '{{ response.assetTags }}'
        complianceState: '{{ response.complianceState }}'
        createTime: '{{ response.createTime }}'
        customId: '{{ response.customId }}'
        healthScore: '{{ response.healthScore }}'
        keyValuePairs: '{{ response.keyValuePairs }}'
        lastUpdateTime: '{{ response.lastUpdateTime }}'
        managed: '{{ response.managed }}'
        name: '{{ response.name }}'
        ownerType: '{{ response.ownerType }}'
        scoreReason: '{{ response.scoreReason }}'
    # required_params: ['name']
  # Retrieves the specified DeviceUser
  # ── gcp.cloudidentity.devices.deviceUsers.get [dependent] ──
  - discovery_id: gcp.cloudidentity.devices.deviceUsers.get
    for_each: gcp.cloudidentity.devices.deviceUsers.list
    # python: svc.devices().deviceUsers().get(**params).execute()
    calls:
      - action: devices.deviceUsers.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        compromisedState: '{{ response.compromisedState }}'
        createTime: '{{ response.createTime }}'
        firstSyncTime: '{{ response.firstSyncTime }}'
        languageCode: '{{ response.languageCode }}'
        lastSyncTime: '{{ response.lastSyncTime }}'
        managementState: '{{ response.managementState }}'
        name: '{{ response.name }}'
        passwordState: '{{ response.passwordState }}'
        userAgent: '{{ response.userAgent }}'
        userEmail: '{{ response.userEmail }}'
    # required_params: ['name']
  # Retrieves the specified device.
  # ── gcp.cloudidentity.devices.get [dependent] ──
  - discovery_id: gcp.cloudidentity.devices.get
    for_each: gcp.cloudidentity.devices.list
    # python: svc.devices().get(**params).execute()
    calls:
      - action: devices.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        androidSpecificAttributes: '{{ response.androidSpecificAttributes }}'
        assetTag: '{{ response.assetTag }}'
        basebandVersion: '{{ response.basebandVersion }}'
        bootloaderVersion: '{{ response.bootloaderVersion }}'
        brand: '{{ response.brand }}'
        buildNumber: '{{ response.buildNumber }}'
        compromisedState: '{{ response.compromisedState }}'
        createTime: '{{ response.createTime }}'
        deviceId: '{{ response.deviceId }}'
        deviceType: '{{ response.deviceType }}'
        enabledDeveloperOptions: '{{ response.enabledDeveloperOptions }}'
        enabledUsbDebugging: '{{ response.enabledUsbDebugging }}'
        encryptionState: '{{ response.encryptionState }}'
        endpointVerificationSpecificAttributes: '{{ response.endpointVerificationSpecificAttributes }}'
        hostname: '{{ response.hostname }}'
        imei: '{{ response.imei }}'
        kernelVersion: '{{ response.kernelVersion }}'
        lastSyncTime: '{{ response.lastSyncTime }}'
        managementState: '{{ response.managementState }}'
        manufacturer: '{{ response.manufacturer }}'
        meid: '{{ response.meid }}'
        model: '{{ response.model }}'
        name: '{{ response.name }}'
        networkOperator: '{{ response.networkOperator }}'
        osVersion: '{{ response.osVersion }}'
        otherAccounts: '{{ response.otherAccounts }}'
        ownerType: '{{ response.ownerType }}'
        releaseVersion: '{{ response.releaseVersion }}'
        securityPatchTime: '{{ response.securityPatchTime }}'
        serialNumber: '{{ response.serialNumber }}'
        unifiedDeviceId: '{{ response.unifiedDeviceId }}'
        wifiMacAddresses: '{{ response.wifiMacAddresses }}'
    # required_params: ['name']
  # Retrieves a `Group`.
  # ── gcp.cloudidentity.groups.get [dependent] ──
  - discovery_id: gcp.cloudidentity.groups.get
    for_each: gcp.cloudidentity.groups.search
    # python: svc.groups().get(**params).execute()
    calls:
      - action: groups.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        additionalGroupKeys: '{{ response.additionalGroupKeys }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        dynamicGroupMetadata: '{{ response.dynamicGroupMetadata }}'
        groupKey: '{{ response.groupKey }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        parent: '{{ response.parent }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Get Security Settings
  # ── gcp.cloudidentity.groups.getSecuritySettings [dependent] ──
  - discovery_id: gcp.cloudidentity.groups.getSecuritySettings
    for_each: gcp.cloudidentity.groups.search
    # python: svc.groups().getSecuritySettings(**params).execute()
    calls:
      - action: groups.getSecuritySettings
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        memberRestriction: '{{ response.memberRestriction }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Retrieves a `Membership`.
  # ── gcp.cloudidentity.groups.memberships.get [dependent] ──
  - discovery_id: gcp.cloudidentity.groups.memberships.get
    for_each: gcp.cloudidentity.groups.memberships.list
    # python: svc.groups().memberships().get(**params).execute()
    calls:
      - action: groups.memberships.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        deliverySetting: '{{ response.deliverySetting }}'
        name: '{{ response.name }}'
        preferredMemberKey: '{{ response.preferredMemberKey }}'
        roles: '{{ response.roles }}'
        type: '{{ response.type }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets an InboundOidcSsoProfile.
  # ── gcp.cloudidentity.inboundOidcSsoProfiles.get [dependent] ──
  - discovery_id: gcp.cloudidentity.inboundOidcSsoProfiles.get
    for_each: gcp.cloudidentity.inboundOidcSsoProfiles.list
    # python: svc.inboundOidcSsoProfiles().get(**params).execute()
    calls:
      - action: inboundOidcSsoProfiles.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        customer: '{{ response.customer }}'
        displayName: '{{ response.displayName }}'
        idpConfig: '{{ response.idpConfig }}'
        name: '{{ response.name }}'
        rpConfig: '{{ response.rpConfig }}'
    # required_params: ['name']
  # Gets an InboundSamlSsoProfile.
  # ── gcp.cloudidentity.inboundSamlSsoProfiles.get [dependent] ──
  - discovery_id: gcp.cloudidentity.inboundSamlSsoProfiles.get
    for_each: gcp.cloudidentity.inboundSamlSsoProfiles.list
    # python: svc.inboundSamlSsoProfiles().get(**params).execute()
    calls:
      - action: inboundSamlSsoProfiles.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        customer: '{{ response.customer }}'
        displayName: '{{ response.displayName }}'
        idpConfig: '{{ response.idpConfig }}'
        name: '{{ response.name }}'
        spConfig: '{{ response.spConfig }}'
    # required_params: ['name']
  # Gets an IdpCredential.
  # ── gcp.cloudidentity.inboundSamlSsoProfiles.idpCredentials.get [dependent] ──
  - discovery_id: gcp.cloudidentity.inboundSamlSsoProfiles.idpCredentials.get
    for_each: gcp.cloudidentity.inboundSamlSsoProfiles.idpCredentials.list
    # python: svc.inboundSamlSsoProfiles().idpCredentials().get(**params).execute()
    calls:
      - action: inboundSamlSsoProfiles.idpCredentials.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        dsaKeyInfo: '{{ response.dsaKeyInfo }}'
        name: '{{ response.name }}'
        rsaKeyInfo: '{{ response.rsaKeyInfo }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets an InboundSsoAssignment.
  # ── gcp.cloudidentity.inboundSsoAssignments.get [dependent] ──
  - discovery_id: gcp.cloudidentity.inboundSsoAssignments.get
    for_each: gcp.cloudidentity.inboundSsoAssignments.list
    # python: svc.inboundSsoAssignments().get(**params).execute()
    calls:
      - action: inboundSsoAssignments.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        customer: '{{ response.customer }}'
        name: '{{ response.name }}'
        oidcSsoInfo: '{{ response.oidcSsoInfo }}'
        rank: '{{ response.rank }}'
        samlSsoInfo: '{{ response.samlSsoInfo }}'
        signInBehavior: '{{ response.signInBehavior }}'
        ssoMode: '{{ response.ssoMode }}'
        targetGroup: '{{ response.targetGroup }}'
        targetOrgUnit: '{{ response.targetOrgUnit }}'
    # required_params: ['name']
  # Get a policy.
  # ── gcp.cloudidentity.policies.get [dependent] ──
  - discovery_id: gcp.cloudidentity.policies.get
    for_each: gcp.cloudidentity.policies.list
    # python: svc.policies().get(**params).execute()
    calls:
      - action: policies.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        customer: '{{ response.customer }}'
        name: '{{ response.name }}'
        policyQuery: '{{ response.policyQuery }}'
        setting: '{{ response.setting }}'
        type: '{{ response.type }}'
    # required_params: ['name']
