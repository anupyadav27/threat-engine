# ============================================================
# Discovery YAML — firebasedynamiclinks (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 1 | independent: 0 | dependent: 1
# ops with fields: 1/1 | dep ops with chain: 0/1
# ============================================================
version: '1.0'
provider: gcp
service: firebasedynamiclinks

services:
  client: firebasedynamiclinks
  module: "googleapiclient.discovery.build('firebasedynamiclinks', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:


  # ════ DEPENDENT (enrich) operations ════
  # Fetches analytics stats of a short Dynamic Link for a given duration. Metrics include number of clic
  # ── gcp.firebasedynamiclinks.v1.getLinkStats [dependent] ──
  - discovery_id: gcp.firebasedynamiclinks.v1.getLinkStats
    # python: svc.v1().getLinkStats(**params).execute()
    calls:
      - action: v1.getLinkStats
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        linkEventStats: '{{ response.linkEventStats }}'
    # required_params: ['dynamicLink']
