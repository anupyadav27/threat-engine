# ============================================================
# Discovery YAML — healthcare (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 67 | independent: 13 | dependent: 54
# ops with fields: 66/67 | dep ops with chain: 38/54
# ============================================================
version: '1.0'
provider: gcp
service: healthcare

services:
  client: healthcare
  module: "googleapiclient.discovery.build('healthcare', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists the Attribute definitions in the specified consent store.
  # ── gcp.healthcare.projects.locations.datasets.consentStores.attributeDefinitions.list ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.consentStores.attributeDefinitions.list
    # python: svc.projects().locations().datasets().consentStores().attributeDefinitions().list(**params).execute()
    calls:
      - action: projects.locations.datasets.consentStores.attributeDefinitions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.attributeDefinitions }}'
      item:
        allowedValues: '{{ item.allowedValues }}'
        category: '{{ item.category }}'
        consentDefaultValues: '{{ item.consentDefaultValues }}'
        dataMappingDefaultValue: '{{ item.dataMappingDefaultValue }}'
        description: '{{ item.description }}'
        name: '{{ item.name }}'
  # Lists the Consent artifacts in the specified consent store.
  # ── gcp.healthcare.projects.locations.datasets.consentStores.consentArtifacts.list ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.consentStores.consentArtifacts.list
    # python: svc.projects().locations().datasets().consentStores().consentArtifacts().list(**params).execute()
    calls:
      - action: projects.locations.datasets.consentStores.consentArtifacts.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.consentArtifacts }}'
      item:
        consentContentScreenshots: '{{ item.consentContentScreenshots }}'
        consentContentVersion: '{{ item.consentContentVersion }}'
        guardianSignature: '{{ item.guardianSignature }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        userId: '{{ item.userId }}'
        userSignature: '{{ item.userSignature }}'
        witnessSignature: '{{ item.witnessSignature }}'
  # Lists the Consent in the given consent store, returning each Consent's latest revision.
  # ── gcp.healthcare.projects.locations.datasets.consentStores.consents.list ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.consentStores.consents.list
    # python: svc.projects().locations().datasets().consentStores().consents().list(**params).execute()
    calls:
      - action: projects.locations.datasets.consentStores.consents.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.consents }}'
      item:
        consentArtifact: '{{ item.consentArtifact }}'
        expireTime: '{{ item.expireTime }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        policies: '{{ item.policies }}'
        revisionCreateTime: '{{ item.revisionCreateTime }}'
        revisionId: '{{ item.revisionId }}'
        state: '{{ item.state }}'
        ttl: '{{ item.ttl }}'
        userId: '{{ item.userId }}'
  # Lists the consent stores in the specified dataset.
  # ── gcp.healthcare.projects.locations.datasets.consentStores.list ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.consentStores.list
    # python: svc.projects().locations().datasets().consentStores().list(**params).execute()
    calls:
      - action: projects.locations.datasets.consentStores.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.consentStores }}'
      item:
        defaultConsentTtl: '{{ item.defaultConsentTtl }}'
        enableConsentCreateOnUpdate: '{{ item.enableConsentCreateOnUpdate }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
  # Lists the User data mappings in the specified consent store.
  # ── gcp.healthcare.projects.locations.datasets.consentStores.userDataMappings.list ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.consentStores.userDataMappings.list
    # python: svc.projects().locations().datasets().consentStores().userDataMappings().list(**params).execute()
    calls:
      - action: projects.locations.datasets.consentStores.userDataMappings.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.userDataMappings }}'
      item:
        archiveTime: '{{ item.archiveTime }}'
        archived: '{{ item.archived }}'
        dataId: '{{ item.dataId }}'
        name: '{{ item.name }}'
        resourceAttributes: '{{ item.resourceAttributes }}'
        userId: '{{ item.userId }}'
  # Lists the DICOM stores in the given dataset.
  # ── gcp.healthcare.projects.locations.datasets.dicomStores.list ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.dicomStores.list
    # python: svc.projects().locations().datasets().dicomStores().list(**params).execute()
    calls:
      - action: projects.locations.datasets.dicomStores.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.dicomStores }}'
      item:
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        notificationConfig: '{{ item.notificationConfig }}'
        notificationConfigs: '{{ item.notificationConfigs }}'
        streamConfigs: '{{ item.streamConfigs }}'
  # Searches for resources in the given FHIR store according to criteria specified as query parameters. 
  # ── gcp.healthcare.projects.locations.datasets.fhirStores.fhir.search ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.fhirStores.fhir.search
    # python: svc.projects().locations().datasets().fhirStores().fhir().search(**params).execute()
    calls:
      - action: projects.locations.datasets.fhirStores.fhir.search
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.fhir }}'
  # Lists the FHIR stores in the given dataset.
  # ── gcp.healthcare.projects.locations.datasets.fhirStores.list ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.fhirStores.list
    # python: svc.projects().locations().datasets().fhirStores().list(**params).execute()
    calls:
      - action: projects.locations.datasets.fhirStores.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.fhirStores }}'
      item:
        bulkExportGcsDestination: '{{ item.bulkExportGcsDestination }}'
        complexDataTypeReferenceParsing: '{{ item.complexDataTypeReferenceParsing }}'
        consentConfig: '{{ item.consentConfig }}'
        defaultSearchHandlingStrict: '{{ item.defaultSearchHandlingStrict }}'
        disableReferentialIntegrity: '{{ item.disableReferentialIntegrity }}'
        disableResourceVersioning: '{{ item.disableResourceVersioning }}'
        enableUpdateCreate: '{{ item.enableUpdateCreate }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        notificationConfig: '{{ item.notificationConfig }}'
        notificationConfigs: '{{ item.notificationConfigs }}'
        streamConfigs: '{{ item.streamConfigs }}'
        validationConfig: '{{ item.validationConfig }}'
        version: '{{ item.version }}'
  # Lists the HL7v2 stores in the given dataset.
  # ── gcp.healthcare.projects.locations.datasets.hl7V2Stores.list ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.hl7V2Stores.list
    # python: svc.projects().locations().datasets().hl7V2Stores().list(**params).execute()
    calls:
      - action: projects.locations.datasets.hl7V2Stores.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.hl7V2Stores }}'
      item:
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        notificationConfigs: '{{ item.notificationConfigs }}'
        parserConfig: '{{ item.parserConfig }}'
        rejectDuplicateMessage: '{{ item.rejectDuplicateMessage }}'
  # Lists all the messages in the given HL7v2 store with support for filtering. Note: HL7v2 messages are
  # ── gcp.healthcare.projects.locations.datasets.hl7V2Stores.messages.list ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.hl7V2Stores.messages.list
    # python: svc.projects().locations().datasets().hl7V2Stores().messages().list(**params).execute()
    calls:
      - action: projects.locations.datasets.hl7V2Stores.messages.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.messages }}'
      item:
        createTime: '{{ item.createTime }}'
        data: '{{ item.data }}'
        labels: '{{ item.labels }}'
        messageType: '{{ item.messageType }}'
        name: '{{ item.name }}'
        parsedData: '{{ item.parsedData }}'
        patientIds: '{{ item.patientIds }}'
        schematizedData: '{{ item.schematizedData }}'
        sendFacility: '{{ item.sendFacility }}'
        sendTime: '{{ item.sendTime }}'
  # Lists the health datasets in the current project.
  # ── gcp.healthcare.projects.locations.datasets.list ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.list
    # python: svc.projects().locations().datasets().list(**params).execute()
    calls:
      - action: projects.locations.datasets.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.datasets }}'
      item:
        encryptionSpec: '{{ item.encryptionSpec }}'
        name: '{{ item.name }}'
        satisfiesPzi: '{{ item.satisfiesPzi }}'
        satisfiesPzs: '{{ item.satisfiesPzs }}'
        timeZone: '{{ item.timeZone }}'
  # Gets information about a location.
  # ── gcp.healthcare.projects.locations.get ──
  - discovery_id: gcp.healthcare.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists information about the supported locations for this service.
  # ── gcp.healthcare.projects.locations.list ──
  - discovery_id: gcp.healthcare.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets the specified Attribute definition.
  # ── gcp.healthcare.projects.locations.datasets.consentStores.attributeDefinitions.get [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.consentStores.attributeDefinitions.get
    for_each: gcp.healthcare.projects.locations.datasets.consentStores.attributeDefinitions.list
    # python: svc.projects().locations().datasets().consentStores().attributeDefinitions().get(**params).execute()
    calls:
      - action: projects.locations.datasets.consentStores.attributeDefinitions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        allowedValues: '{{ response.allowedValues }}'
        category: '{{ response.category }}'
        consentDefaultValues: '{{ response.consentDefaultValues }}'
        dataMappingDefaultValue: '{{ response.dataMappingDefaultValue }}'
        description: '{{ response.description }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Gets the specified Consent artifact.
  # ── gcp.healthcare.projects.locations.datasets.consentStores.consentArtifacts.get [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.consentStores.consentArtifacts.get
    for_each: gcp.healthcare.projects.locations.datasets.consentStores.consentArtifacts.list
    # python: svc.projects().locations().datasets().consentStores().consentArtifacts().get(**params).execute()
    calls:
      - action: projects.locations.datasets.consentStores.consentArtifacts.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        consentContentScreenshots: '{{ response.consentContentScreenshots }}'
        consentContentVersion: '{{ response.consentContentVersion }}'
        guardianSignature: '{{ response.guardianSignature }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        userId: '{{ response.userId }}'
        userSignature: '{{ response.userSignature }}'
        witnessSignature: '{{ response.witnessSignature }}'
    # required_params: ['name']
  # Gets the specified revision of a Consent, or the latest revision if `revision_id` is not specified i
  # ── gcp.healthcare.projects.locations.datasets.consentStores.consents.get [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.consentStores.consents.get
    for_each: gcp.healthcare.projects.locations.datasets.consentStores.consents.list
    # python: svc.projects().locations().datasets().consentStores().consents().get(**params).execute()
    calls:
      - action: projects.locations.datasets.consentStores.consents.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        consentArtifact: '{{ response.consentArtifact }}'
        expireTime: '{{ response.expireTime }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        policies: '{{ response.policies }}'
        revisionCreateTime: '{{ response.revisionCreateTime }}'
        revisionId: '{{ response.revisionId }}'
        state: '{{ response.state }}'
        ttl: '{{ response.ttl }}'
        userId: '{{ response.userId }}'
    # required_params: ['name']
  # Lists the revisions of the specified Consent in reverse chronological order.
  # ── gcp.healthcare.projects.locations.datasets.consentStores.consents.listRevisions [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.consentStores.consents.listRevisions
    for_each: gcp.healthcare.projects.locations.datasets.consentStores.consents.list
    # python: svc.projects().locations().datasets().consentStores().consents().listRevisions(**params).execute()
    calls:
      - action: projects.locations.datasets.consentStores.consents.listRevisions
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.consents }}'
      item:
        consentArtifact: '{{ item.consentArtifact }}'
        expireTime: '{{ item.expireTime }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        policies: '{{ item.policies }}'
        revisionCreateTime: '{{ item.revisionCreateTime }}'
        revisionId: '{{ item.revisionId }}'
        state: '{{ item.state }}'
        ttl: '{{ item.ttl }}'
        userId: '{{ item.userId }}'
    # required_params: ['name']
  # Gets the specified consent store.
  # ── gcp.healthcare.projects.locations.datasets.consentStores.get [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.consentStores.get
    for_each: gcp.healthcare.projects.locations.datasets.consentStores.list
    # python: svc.projects().locations().datasets().consentStores().get(**params).execute()
    calls:
      - action: projects.locations.datasets.consentStores.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        defaultConsentTtl: '{{ response.defaultConsentTtl }}'
        enableConsentCreateOnUpdate: '{{ response.enableConsentCreateOnUpdate }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.healthcare.projects.locations.datasets.consentStores.getIamPolicy [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.consentStores.getIamPolicy
    for_each: gcp.healthcare.projects.locations.datasets.consentStores.list
    # python: svc.projects().locations().datasets().consentStores().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.datasets.consentStores.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Gets the specified User data mapping.
  # ── gcp.healthcare.projects.locations.datasets.consentStores.userDataMappings.get [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.consentStores.userDataMappings.get
    for_each: gcp.healthcare.projects.locations.datasets.consentStores.userDataMappings.list
    # python: svc.projects().locations().datasets().consentStores().userDataMappings().get(**params).execute()
    calls:
      - action: projects.locations.datasets.consentStores.userDataMappings.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        archiveTime: '{{ response.archiveTime }}'
        archived: '{{ response.archived }}'
        dataId: '{{ response.dataId }}'
        name: '{{ response.name }}'
        resourceAttributes: '{{ response.resourceAttributes }}'
        userId: '{{ response.userId }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.healthcare.projects.locations.datasets.dataMapperWorkspaces.getIamPolicy [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.dataMapperWorkspaces.getIamPolicy
    for_each: gcp.healthcare.projects.locations.datasets.fhirStores.fhir.search
    # python: svc.projects().locations().datasets().dataMapperWorkspaces().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.datasets.dataMapperWorkspaces.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # GetStudyMetrics returns metrics for a study.
  # ── gcp.healthcare.projects.locations.datasets.dicomStores.dicomWeb.studies.getStudyMetrics [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.dicomStores.dicomWeb.studies.getStudyMetrics
    for_each: gcp.healthcare.projects.locations.datasets.fhirStores.fhir.search
    # python: svc.projects().locations().datasets().dicomStores().dicomWeb().studies().getStudyMetrics(**params).execute()
    calls:
      - action: projects.locations.datasets.dicomStores.dicomWeb.studies.getStudyMetrics
        params:
          study: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        blobStorageSizeBytes: '{{ response.blobStorageSizeBytes }}'
        instanceCount: '{{ response.instanceCount }}'
        seriesCount: '{{ response.seriesCount }}'
        structuredStorageSizeBytes: '{{ response.structuredStorageSizeBytes }}'
        study: '{{ response.study }}'
    # required_params: ['study']
  # GetSeriesMetrics returns metrics for a series.
  # ── gcp.healthcare.projects.locations.datasets.dicomStores.dicomWeb.studies.series.getSeriesMetrics [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.dicomStores.dicomWeb.studies.series.getSeriesMetrics
    for_each: gcp.healthcare.projects.locations.datasets.fhirStores.fhir.search
    # python: svc.projects().locations().datasets().dicomStores().dicomWeb().studies().series().getSeriesMetrics(**params).execute()
    calls:
      - action: projects.locations.datasets.dicomStores.dicomWeb.studies.series.getSeriesMetrics
        params:
          series: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        blobStorageSizeBytes: '{{ response.blobStorageSizeBytes }}'
        instanceCount: '{{ response.instanceCount }}'
        series: '{{ response.series }}'
        structuredStorageSizeBytes: '{{ response.structuredStorageSizeBytes }}'
    # required_params: ['series']
  # GetStorageInfo returns the storage info of the specified resource.
  # ── gcp.healthcare.projects.locations.datasets.dicomStores.dicomWeb.studies.series.instances.getStorageInfo [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.dicomStores.dicomWeb.studies.series.instances.getStorageInfo
    for_each: gcp.healthcare.projects.locations.datasets.fhirStores.fhir.search
    # python: svc.projects().locations().datasets().dicomStores().dicomWeb().studies().series().instances().getStorageInfo(**params).execute()
    calls:
      - action: projects.locations.datasets.dicomStores.dicomWeb.studies.series.instances.getStorageInfo
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        blobStorageInfo: '{{ response.blobStorageInfo }}'
        referencedResource: '{{ response.referencedResource }}'
        structuredStorageInfo: '{{ response.structuredStorageInfo }}'
    # required_params: ['resource']
  # Gets the specified DICOM store.
  # ── gcp.healthcare.projects.locations.datasets.dicomStores.get [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.dicomStores.get
    for_each: gcp.healthcare.projects.locations.datasets.dicomStores.list
    # python: svc.projects().locations().datasets().dicomStores().get(**params).execute()
    calls:
      - action: projects.locations.datasets.dicomStores.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        notificationConfig: '{{ response.notificationConfig }}'
        notificationConfigs: '{{ response.notificationConfigs }}'
        streamConfigs: '{{ response.streamConfigs }}'
    # required_params: ['name']
  # Gets metrics associated with the DICOM store.
  # ── gcp.healthcare.projects.locations.datasets.dicomStores.getDICOMStoreMetrics [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.dicomStores.getDICOMStoreMetrics
    for_each: gcp.healthcare.projects.locations.datasets.dicomStores.list
    # python: svc.projects().locations().datasets().dicomStores().getDICOMStoreMetrics(**params).execute()
    calls:
      - action: projects.locations.datasets.dicomStores.getDICOMStoreMetrics
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        blobStorageSizeBytes: '{{ response.blobStorageSizeBytes }}'
        instanceCount: '{{ response.instanceCount }}'
        name: '{{ response.name }}'
        seriesCount: '{{ response.seriesCount }}'
        structuredStorageSizeBytes: '{{ response.structuredStorageSizeBytes }}'
        studyCount: '{{ response.studyCount }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.healthcare.projects.locations.datasets.dicomStores.getIamPolicy [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.dicomStores.getIamPolicy
    for_each: gcp.healthcare.projects.locations.datasets.dicomStores.list
    # python: svc.projects().locations().datasets().dicomStores().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.datasets.dicomStores.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # SearchForInstances returns a list of matching instances. See [Search Transaction] (https://dicom.nem
  # ── gcp.healthcare.projects.locations.datasets.dicomStores.searchForInstances [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.dicomStores.searchForInstances
    # python: svc.projects().locations().datasets().dicomStores().searchForInstances(**params).execute()
    calls:
      - action: projects.locations.datasets.dicomStores.searchForInstances
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['parent', 'dicomWebPath']
  # SearchForSeries returns a list of matching series. See [Search Transaction] (https://dicom.nema.org/
  # ── gcp.healthcare.projects.locations.datasets.dicomStores.searchForSeries [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.dicomStores.searchForSeries
    # python: svc.projects().locations().datasets().dicomStores().searchForSeries(**params).execute()
    calls:
      - action: projects.locations.datasets.dicomStores.searchForSeries
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['parent', 'dicomWebPath']
  # SearchForStudies returns a list of matching studies. See [Search Transaction] (https://dicom.nema.or
  # ── gcp.healthcare.projects.locations.datasets.dicomStores.searchForStudies [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.dicomStores.searchForStudies
    # python: svc.projects().locations().datasets().dicomStores().searchForStudies(**params).execute()
    calls:
      - action: projects.locations.datasets.dicomStores.searchForStudies
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['parent', 'dicomWebPath']
  # RetrieveStudyMetadata returns instance associated with the given study presented as metadata. See [R
  # ── gcp.healthcare.projects.locations.datasets.dicomStores.studies.retrieveMetadata [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.dicomStores.studies.retrieveMetadata
    # python: svc.projects().locations().datasets().dicomStores().studies().retrieveMetadata(**params).execute()
    calls:
      - action: projects.locations.datasets.dicomStores.studies.retrieveMetadata
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['parent', 'dicomWebPath']
  # RetrieveStudy returns all instances within the given study. See [RetrieveTransaction] (https://dicom
  # ── gcp.healthcare.projects.locations.datasets.dicomStores.studies.retrieveStudy [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.dicomStores.studies.retrieveStudy
    # python: svc.projects().locations().datasets().dicomStores().studies().retrieveStudy(**params).execute()
    calls:
      - action: projects.locations.datasets.dicomStores.studies.retrieveStudy
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['parent', 'dicomWebPath']
  # SearchForInstances returns a list of matching instances. See [Search Transaction] (https://dicom.nem
  # ── gcp.healthcare.projects.locations.datasets.dicomStores.studies.searchForInstances [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.dicomStores.studies.searchForInstances
    # python: svc.projects().locations().datasets().dicomStores().studies().searchForInstances(**params).execute()
    calls:
      - action: projects.locations.datasets.dicomStores.studies.searchForInstances
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['parent', 'dicomWebPath']
  # SearchForSeries returns a list of matching series. See [Search Transaction] (https://dicom.nema.org/
  # ── gcp.healthcare.projects.locations.datasets.dicomStores.studies.searchForSeries [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.dicomStores.studies.searchForSeries
    # python: svc.projects().locations().datasets().dicomStores().studies().searchForSeries(**params).execute()
    calls:
      - action: projects.locations.datasets.dicomStores.studies.searchForSeries
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['parent', 'dicomWebPath']
  # Returns uncompressed, unencoded bytes representing the referenced bulkdata tag from an instance. See
  # ── gcp.healthcare.projects.locations.datasets.dicomStores.studies.series.instances.bulkdata.retrieveBulkdata [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.dicomStores.studies.series.instances.bulkdata.retrieveBulkdata
    # python: svc.projects().locations().datasets().dicomStores().studies().series().instances().bulkdata().retrieveBulkdata(**params).execute()
    calls:
      - action: projects.locations.datasets.dicomStores.studies.series.instances.bulkdata.retrieveBulkdata
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['parent', 'dicomWebPath']
  # RetrieveFrames returns instances associated with the given study, series, SOP Instance UID and frame
  # ── gcp.healthcare.projects.locations.datasets.dicomStores.studies.series.instances.frames.retrieveFrames [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.dicomStores.studies.series.instances.frames.retrieveFrames
    # python: svc.projects().locations().datasets().dicomStores().studies().series().instances().frames().retrieveFrames(**params).execute()
    calls:
      - action: projects.locations.datasets.dicomStores.studies.series.instances.frames.retrieveFrames
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['parent', 'dicomWebPath']
  # RetrieveRenderedFrames returns instances associated with the given study, series, SOP Instance UID a
  # ── gcp.healthcare.projects.locations.datasets.dicomStores.studies.series.instances.frames.retrieveRendered [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.dicomStores.studies.series.instances.frames.retrieveRendered
    # python: svc.projects().locations().datasets().dicomStores().studies().series().instances().frames().retrieveRendered(**params).execute()
    calls:
      - action: projects.locations.datasets.dicomStores.studies.series.instances.frames.retrieveRendered
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['parent', 'dicomWebPath']
  # RetrieveInstance returns instance associated with the given study, series, and SOP Instance UID. See
  # ── gcp.healthcare.projects.locations.datasets.dicomStores.studies.series.instances.retrieveInstance [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.dicomStores.studies.series.instances.retrieveInstance
    # python: svc.projects().locations().datasets().dicomStores().studies().series().instances().retrieveInstance(**params).execute()
    calls:
      - action: projects.locations.datasets.dicomStores.studies.series.instances.retrieveInstance
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['parent', 'dicomWebPath']
  # RetrieveInstanceMetadata returns instance associated with the given study, series, and SOP Instance 
  # ── gcp.healthcare.projects.locations.datasets.dicomStores.studies.series.instances.retrieveMetadata [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.dicomStores.studies.series.instances.retrieveMetadata
    # python: svc.projects().locations().datasets().dicomStores().studies().series().instances().retrieveMetadata(**params).execute()
    calls:
      - action: projects.locations.datasets.dicomStores.studies.series.instances.retrieveMetadata
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['parent', 'dicomWebPath']
  # RetrieveRenderedInstance returns instance associated with the given study, series, and SOP Instance 
  # ── gcp.healthcare.projects.locations.datasets.dicomStores.studies.series.instances.retrieveRendered [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.dicomStores.studies.series.instances.retrieveRendered
    # python: svc.projects().locations().datasets().dicomStores().studies().series().instances().retrieveRendered(**params).execute()
    calls:
      - action: projects.locations.datasets.dicomStores.studies.series.instances.retrieveRendered
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['parent', 'dicomWebPath']
  # RetrieveSeriesMetadata returns instance associated with the given study and series, presented as met
  # ── gcp.healthcare.projects.locations.datasets.dicomStores.studies.series.retrieveMetadata [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.dicomStores.studies.series.retrieveMetadata
    # python: svc.projects().locations().datasets().dicomStores().studies().series().retrieveMetadata(**params).execute()
    calls:
      - action: projects.locations.datasets.dicomStores.studies.series.retrieveMetadata
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['parent', 'dicomWebPath']
  # RetrieveSeries returns all instances within the given study and series. See [RetrieveTransaction] (h
  # ── gcp.healthcare.projects.locations.datasets.dicomStores.studies.series.retrieveSeries [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.dicomStores.studies.series.retrieveSeries
    # python: svc.projects().locations().datasets().dicomStores().studies().series().retrieveSeries(**params).execute()
    calls:
      - action: projects.locations.datasets.dicomStores.studies.series.retrieveSeries
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['parent', 'dicomWebPath']
  # SearchForInstances returns a list of matching instances. See [Search Transaction] (https://dicom.nem
  # ── gcp.healthcare.projects.locations.datasets.dicomStores.studies.series.searchForInstances [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.dicomStores.studies.series.searchForInstances
    # python: svc.projects().locations().datasets().dicomStores().studies().series().searchForInstances(**params).execute()
    calls:
      - action: projects.locations.datasets.dicomStores.studies.series.searchForInstances
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['parent', 'dicomWebPath']
  # Bulk exports a Group resource and resources in the member field, including related resources for eac
  # ── gcp.healthcare.projects.locations.datasets.fhirStores.bulk-export-group [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.fhirStores.bulk-export-group
    for_each: gcp.healthcare.projects.locations.datasets.fhirStores.fhir.search
    # python: svc.projects().locations().datasets().fhirStores().bulk-export-group(**params).execute()
    calls:
      - action: projects.locations.datasets.fhirStores.bulk-export-group
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['name']
  # Explains all the permitted/denied actor, purpose and environment for a given resource. FHIR Consent 
  # ── gcp.healthcare.projects.locations.datasets.fhirStores.explainDataAccess [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.fhirStores.explainDataAccess
    for_each: gcp.healthcare.projects.locations.datasets.fhirStores.list
    # python: svc.projects().locations().datasets().fhirStores().explainDataAccess(**params).execute()
    calls:
      - action: projects.locations.datasets.fhirStores.explainDataAccess
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        consentScopes: '{{ response.consentScopes }}'
    # required_params: ['name']
  # Gets the contents of a FHIR Binary resource. This method can be used to retrieve a Binary resource e
  # ── gcp.healthcare.projects.locations.datasets.fhirStores.fhir.Binary-read [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.fhirStores.fhir.Binary-read
    for_each: gcp.healthcare.projects.locations.datasets.fhirStores.fhir.search
    # python: svc.projects().locations().datasets().fhirStores().fhir().Binary-read(**params).execute()
    calls:
      - action: projects.locations.datasets.fhirStores.fhir.Binary-read
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['name']
  # Gets the contents of a version (current or historical) of a FHIR Binary resource by version ID. This
  # ── gcp.healthcare.projects.locations.datasets.fhirStores.fhir.Binary-vread [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.fhirStores.fhir.Binary-vread
    for_each: gcp.healthcare.projects.locations.datasets.fhirStores.fhir.search
    # python: svc.projects().locations().datasets().fhirStores().fhir().Binary-vread(**params).execute()
    calls:
      - action: projects.locations.datasets.fhirStores.fhir.Binary-vread
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['name']
  # Returns the consent enforcement status of a single consent resource. On success, the response body c
  # ── gcp.healthcare.projects.locations.datasets.fhirStores.fhir.Consent-enforcement-status [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.fhirStores.fhir.Consent-enforcement-status
    for_each: gcp.healthcare.projects.locations.datasets.consentStores.consents.list
    # python: svc.projects().locations().datasets().fhirStores().fhir().Consent-enforcement-status(**params).execute()
    calls:
      - action: projects.locations.datasets.fhirStores.fhir.Consent-enforcement-status
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['name']
  # Returns the consent enforcement status of all consent resources for a patient. On success, the respo
  # ── gcp.healthcare.projects.locations.datasets.fhirStores.fhir.Patient-consent-enforcement-status [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.fhirStores.fhir.Patient-consent-enforcement-status
    for_each: gcp.healthcare.projects.locations.datasets.hl7V2Stores.messages.list
    # python: svc.projects().locations().datasets().fhirStores().fhir().Patient-consent-enforcement-status(**params).execute()
    calls:
      - action: projects.locations.datasets.fhirStores.fhir.Patient-consent-enforcement-status
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['name']
  # Retrieves a Patient resource and resources related to that patient. Implements the FHIR extended ope
  # ── gcp.healthcare.projects.locations.datasets.fhirStores.fhir.Patient-everything [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.fhirStores.fhir.Patient-everything
    for_each: gcp.healthcare.projects.locations.datasets.hl7V2Stores.messages.list
    # python: svc.projects().locations().datasets().fhirStores().fhir().Patient-everything(**params).execute()
    calls:
      - action: projects.locations.datasets.fhirStores.fhir.Patient-everything
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['name']
  # Bulk exports all resources from the FHIR store to the specified destination. Implements the FHIR imp
  # ── gcp.healthcare.projects.locations.datasets.fhirStores.fhir.bulk-export [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.fhirStores.fhir.bulk-export
    for_each: gcp.healthcare.projects.locations.datasets.fhirStores.list
    # python: svc.projects().locations().datasets().fhirStores().fhir().bulk-export(**params).execute()
    calls:
      - action: projects.locations.datasets.fhirStores.fhir.bulk-export
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['name']
  # Gets the FHIR capability statement ([STU3](https://hl7.org/fhir/STU3/capabilitystatement.html), [R4]
  # ── gcp.healthcare.projects.locations.datasets.fhirStores.fhir.capabilities [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.fhirStores.fhir.capabilities
    for_each: gcp.healthcare.projects.locations.datasets.fhirStores.list
    # python: svc.projects().locations().datasets().fhirStores().fhir().capabilities(**params).execute()
    calls:
      - action: projects.locations.datasets.fhirStores.fhir.capabilities
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['name']
  # Lists all the versions of a resource (including the current version and deleted versions) from the F
  # ── gcp.healthcare.projects.locations.datasets.fhirStores.fhir.history [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.fhirStores.fhir.history
    for_each: gcp.healthcare.projects.locations.datasets.fhirStores.fhir.search
    # python: svc.projects().locations().datasets().fhirStores().fhir().history(**params).execute()
    calls:
      - action: projects.locations.datasets.fhirStores.fhir.history
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['name']
  # Gets the contents of a FHIR resource. Implements the FHIR standard read interaction ([DSTU2](https:/
  # ── gcp.healthcare.projects.locations.datasets.fhirStores.fhir.read [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.fhirStores.fhir.read
    for_each: gcp.healthcare.projects.locations.datasets.fhirStores.fhir.search
    # python: svc.projects().locations().datasets().fhirStores().fhir().read(**params).execute()
    calls:
      - action: projects.locations.datasets.fhirStores.fhir.read
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['name']
  # Gets the contents of a version (current or historical) of a FHIR resource by version ID. Implements 
  # ── gcp.healthcare.projects.locations.datasets.fhirStores.fhir.vread [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.fhirStores.fhir.vread
    for_each: gcp.healthcare.projects.locations.datasets.fhirStores.fhir.search
    # python: svc.projects().locations().datasets().fhirStores().fhir().vread(**params).execute()
    calls:
      - action: projects.locations.datasets.fhirStores.fhir.vread
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['name']
  # Gets the configuration of the specified FHIR store.
  # ── gcp.healthcare.projects.locations.datasets.fhirStores.get [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.fhirStores.get
    for_each: gcp.healthcare.projects.locations.datasets.fhirStores.list
    # python: svc.projects().locations().datasets().fhirStores().get(**params).execute()
    calls:
      - action: projects.locations.datasets.fhirStores.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        bulkExportGcsDestination: '{{ response.bulkExportGcsDestination }}'
        complexDataTypeReferenceParsing: '{{ response.complexDataTypeReferenceParsing }}'
        consentConfig: '{{ response.consentConfig }}'
        defaultSearchHandlingStrict: '{{ response.defaultSearchHandlingStrict }}'
        disableReferentialIntegrity: '{{ response.disableReferentialIntegrity }}'
        disableResourceVersioning: '{{ response.disableResourceVersioning }}'
        enableUpdateCreate: '{{ response.enableUpdateCreate }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        notificationConfig: '{{ response.notificationConfig }}'
        notificationConfigs: '{{ response.notificationConfigs }}'
        streamConfigs: '{{ response.streamConfigs }}'
        validationConfig: '{{ response.validationConfig }}'
        version: '{{ response.version }}'
    # required_params: ['name']
  # Gets metrics associated with the FHIR store.
  # ── gcp.healthcare.projects.locations.datasets.fhirStores.getFHIRStoreMetrics [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.fhirStores.getFHIRStoreMetrics
    for_each: gcp.healthcare.projects.locations.datasets.fhirStores.list
    # python: svc.projects().locations().datasets().fhirStores().getFHIRStoreMetrics(**params).execute()
    calls:
      - action: projects.locations.datasets.fhirStores.getFHIRStoreMetrics
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        metrics: '{{ response.metrics }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.healthcare.projects.locations.datasets.fhirStores.getIamPolicy [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.fhirStores.getIamPolicy
    for_each: gcp.healthcare.projects.locations.datasets.fhirStores.list
    # python: svc.projects().locations().datasets().fhirStores().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.datasets.fhirStores.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Gets the status of operations as defined in the FHIR specification. Implements the FHIR implementati
  # ── gcp.healthcare.projects.locations.datasets.fhirStores.operations.get-fhir-operation-status [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.fhirStores.operations.get-fhir-operation-status
    for_each: gcp.healthcare.projects.locations.datasets.operations.list
    # python: svc.projects().locations().datasets().fhirStores().operations().get-fhir-operation-status(**params).execute()
    calls:
      - action: projects.locations.datasets.fhirStores.operations.get-fhir-operation-status
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['name']
  # Gets any metadata associated with a dataset.
  # ── gcp.healthcare.projects.locations.datasets.get [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.get
    for_each: gcp.healthcare.projects.locations.datasets.list
    # python: svc.projects().locations().datasets().get(**params).execute()
    calls:
      - action: projects.locations.datasets.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        encryptionSpec: '{{ response.encryptionSpec }}'
        name: '{{ response.name }}'
        satisfiesPzi: '{{ response.satisfiesPzi }}'
        satisfiesPzs: '{{ response.satisfiesPzs }}'
        timeZone: '{{ response.timeZone }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.healthcare.projects.locations.datasets.getIamPolicy [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.getIamPolicy
    for_each: gcp.healthcare.projects.locations.datasets.list
    # python: svc.projects().locations().datasets().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.datasets.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Gets the specified HL7v2 store.
  # ── gcp.healthcare.projects.locations.datasets.hl7V2Stores.get [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.hl7V2Stores.get
    for_each: gcp.healthcare.projects.locations.datasets.hl7V2Stores.list
    # python: svc.projects().locations().datasets().hl7V2Stores().get(**params).execute()
    calls:
      - action: projects.locations.datasets.hl7V2Stores.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        notificationConfigs: '{{ response.notificationConfigs }}'
        parserConfig: '{{ response.parserConfig }}'
        rejectDuplicateMessage: '{{ response.rejectDuplicateMessage }}'
    # required_params: ['name']
  # Gets metrics associated with the HL7v2 store.
  # ── gcp.healthcare.projects.locations.datasets.hl7V2Stores.getHL7v2StoreMetrics [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.hl7V2Stores.getHL7v2StoreMetrics
    for_each: gcp.healthcare.projects.locations.datasets.hl7V2Stores.list
    # python: svc.projects().locations().datasets().hl7V2Stores().getHL7v2StoreMetrics(**params).execute()
    calls:
      - action: projects.locations.datasets.hl7V2Stores.getHL7v2StoreMetrics
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        metrics: '{{ response.metrics }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.healthcare.projects.locations.datasets.hl7V2Stores.getIamPolicy [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.hl7V2Stores.getIamPolicy
    for_each: gcp.healthcare.projects.locations.datasets.hl7V2Stores.list
    # python: svc.projects().locations().datasets().hl7V2Stores().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.datasets.hl7V2Stores.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Gets an HL7v2 message.
  # ── gcp.healthcare.projects.locations.datasets.hl7V2Stores.messages.get [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.hl7V2Stores.messages.get
    for_each: gcp.healthcare.projects.locations.datasets.hl7V2Stores.messages.list
    # python: svc.projects().locations().datasets().hl7V2Stores().messages().get(**params).execute()
    calls:
      - action: projects.locations.datasets.hl7V2Stores.messages.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        data: '{{ response.data }}'
        labels: '{{ response.labels }}'
        messageType: '{{ response.messageType }}'
        name: '{{ response.name }}'
        parsedData: '{{ response.parsedData }}'
        patientIds: '{{ response.patientIds }}'
        schematizedData: '{{ response.schematizedData }}'
        sendFacility: '{{ response.sendFacility }}'
        sendTime: '{{ response.sendTime }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.healthcare.projects.locations.datasets.operations.get [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.operations.get
    for_each: gcp.healthcare.projects.locations.datasets.operations.list
    # python: svc.projects().locations().datasets().operations().get(**params).execute()
    calls:
      - action: projects.locations.datasets.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.healthcare.projects.locations.datasets.operations.list [dependent] ──
  - discovery_id: gcp.healthcare.projects.locations.datasets.operations.list
    for_each: gcp.healthcare.projects.locations.datasets.list
    # python: svc.projects().locations().datasets().operations().list(**params).execute()
    calls:
      - action: projects.locations.datasets.operations.list
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
    # required_params: ['name']
