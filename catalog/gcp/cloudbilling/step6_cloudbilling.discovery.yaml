# ============================================================
# Discovery YAML — cloudbilling (v1)
# Generated: 2026-02-21T03:06:39Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 10 | independent: 10 | dependent: 0
# ops with fields: 10/10 | dep ops with chain: 0/0
# ============================================================
version: '1.0'
provider: gcp
service: cloudbilling

services:
  client: cloudbilling
  module: "googleapiclient.discovery.build('cloudbilling', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Gets information about a billing account. The current authenticated user must be a [viewer of the bi
  # ── gcp.cloudbilling.billingAccounts.get ──
  - discovery_id: gcp.cloudbilling.billingAccounts.get
    # python: svc.billingAccounts().get(**params).execute()
    calls:
      - action: billingAccounts.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        currencyCode: '{{ response.currencyCode }}'
        displayName: '{{ response.displayName }}'
        masterBillingAccount: '{{ response.masterBillingAccount }}'
        name: '{{ response.name }}'
        open: '{{ response.open }}'
        parent: '{{ response.parent }}'
  # Gets the access control policy for a billing account. The caller must have the `billing.accounts.get
  # ── gcp.cloudbilling.billingAccounts.getIamPolicy ──
  - discovery_id: gcp.cloudbilling.billingAccounts.getIamPolicy
    # python: svc.billingAccounts().getIamPolicy(**params).execute()
    calls:
      - action: billingAccounts.getIamPolicy
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
  # Lists the billing accounts that the current authenticated user has permission to [view](https://clou
  # ── gcp.cloudbilling.billingAccounts.list ──
  - discovery_id: gcp.cloudbilling.billingAccounts.list
    # python: svc.billingAccounts().list(**params).execute()
    calls:
      - action: billingAccounts.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.billingAccounts }}'
      item:
        currencyCode: '{{ item.currencyCode }}'
        displayName: '{{ item.displayName }}'
        masterBillingAccount: '{{ item.masterBillingAccount }}'
        name: '{{ item.name }}'
        open: '{{ item.open }}'
        parent: '{{ item.parent }}'
  # Lists the projects associated with a billing account. The current authenticated user must have the `
  # ── gcp.cloudbilling.billingAccounts.projects.list ──
  - discovery_id: gcp.cloudbilling.billingAccounts.projects.list
    # python: svc.billingAccounts().projects().list(**params).execute()
    calls:
      - action: billingAccounts.projects.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.projects }}'
      item:
        billingAccountName: '{{ item.billingAccountName }}'
        billingEnabled: '{{ item.billingEnabled }}'
        name: '{{ item.name }}'
        projectId: '{{ item.projectId }}'
  # Lists the billing accounts that the current authenticated user has permission to [view](https://clou
  # ── gcp.cloudbilling.billingAccounts.subAccounts.list ──
  - discovery_id: gcp.cloudbilling.billingAccounts.subAccounts.list
    # python: svc.billingAccounts().subAccounts().list(**params).execute()
    calls:
      - action: billingAccounts.subAccounts.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.subAccounts }}'
      item:
        currencyCode: '{{ item.currencyCode }}'
        displayName: '{{ item.displayName }}'
        masterBillingAccount: '{{ item.masterBillingAccount }}'
        name: '{{ item.name }}'
        open: '{{ item.open }}'
        parent: '{{ item.parent }}'
  # Lists the billing accounts that the current authenticated user has permission to [view](https://clou
  # ── gcp.cloudbilling.organizations.billingAccounts.list ──
  - discovery_id: gcp.cloudbilling.organizations.billingAccounts.list
    # python: svc.organizations().billingAccounts().list(**params).execute()
    calls:
      - action: organizations.billingAccounts.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.billingAccounts }}'
      item:
        currencyCode: '{{ item.currencyCode }}'
        displayName: '{{ item.displayName }}'
        masterBillingAccount: '{{ item.masterBillingAccount }}'
        name: '{{ item.name }}'
        open: '{{ item.open }}'
        parent: '{{ item.parent }}'
  # Changes which parent organization a billing account belongs to.
  # ── gcp.cloudbilling.organizations.billingAccounts.move ──
  - discovery_id: gcp.cloudbilling.organizations.billingAccounts.move
    # python: svc.organizations().billingAccounts().move(**params).execute()
    calls:
      - action: organizations.billingAccounts.move
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        currencyCode: '{{ response.currencyCode }}'
        displayName: '{{ response.displayName }}'
        masterBillingAccount: '{{ response.masterBillingAccount }}'
        name: '{{ response.name }}'
        open: '{{ response.open }}'
        parent: '{{ response.parent }}'
  # Gets the billing information for a project. The current authenticated user must have the `resourcema
  # ── gcp.cloudbilling.projects.getBillingInfo ──
  - discovery_id: gcp.cloudbilling.projects.getBillingInfo
    # python: svc.projects().getBillingInfo(**params).execute()
    calls:
      - action: projects.getBillingInfo
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        billingAccountName: '{{ response.billingAccountName }}'
        billingEnabled: '{{ response.billingEnabled }}'
        name: '{{ response.name }}'
        projectId: '{{ response.projectId }}'
  # Lists all public cloud services.
  # ── gcp.cloudbilling.services.list ──
  - discovery_id: gcp.cloudbilling.services.list
    # python: svc.services().list(**params).execute()
    calls:
      - action: services.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.services }}'
      item:
        businessEntityName: '{{ item.businessEntityName }}'
        displayName: '{{ item.displayName }}'
        name: '{{ item.name }}'
        serviceId: '{{ item.serviceId }}'
  # Lists all publicly available SKUs for a given cloud service.
  # ── gcp.cloudbilling.services.skus.list ──
  - discovery_id: gcp.cloudbilling.services.skus.list
    # python: svc.services().skus().list(**params).execute()
    calls:
      - action: services.skus.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.skus }}'
      item:
        category: '{{ item.category }}'
        description: '{{ item.description }}'
        geoTaxonomy: '{{ item.geoTaxonomy }}'
        name: '{{ item.name }}'
        pricingInfo: '{{ item.pricingInfo }}'
        serviceProviderName: '{{ item.serviceProviderName }}'
        serviceRegions: '{{ item.serviceRegions }}'
        skuId: '{{ item.skuId }}'
