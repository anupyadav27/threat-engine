# ============================================================
# Discovery YAML — androiddeviceprovisioning (v1)
# Generated: 2026-02-21T03:06:38Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 11 | independent: 6 | dependent: 5
# ops with fields: 11/11 | dep ops with chain: 3/5
# ============================================================
version: '1.0'
provider: gcp
service: androiddeviceprovisioning

services:
  client: androiddeviceprovisioning
  module: "googleapiclient.discovery.build('androiddeviceprovisioning', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists a customer's configurations.
  # ── gcp.androiddeviceprovisioning.customers.configurations.list ──
  - discovery_id: gcp.androiddeviceprovisioning.customers.configurations.list
    # python: svc.customers().configurations().list(**params).execute()
    calls:
      - action: customers.configurations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.configurations }}'
      item:
        companyName: '{{ item.companyName }}'
        configurationId: '{{ item.configurationId }}'
        configurationName: '{{ item.configurationName }}'
        contactEmail: '{{ item.contactEmail }}'
        contactPhone: '{{ item.contactPhone }}'
        customMessage: '{{ item.customMessage }}'
        dpcExtras: '{{ item.dpcExtras }}'
        dpcResourcePath: '{{ item.dpcResourcePath }}'
        forcedResetTime: '{{ item.forcedResetTime }}'
        isDefault: '{{ item.isDefault }}'
        name: '{{ item.name }}'
  # Lists a customer's devices.
  # ── gcp.androiddeviceprovisioning.customers.devices.list ──
  - discovery_id: gcp.androiddeviceprovisioning.customers.devices.list
    # python: svc.customers().devices().list(**params).execute()
    calls:
      - action: customers.devices.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.devices }}'
      item:
        claims: '{{ item.claims }}'
        configuration: '{{ item.configuration }}'
        deviceId: '{{ item.deviceId }}'
        deviceIdentifier: '{{ item.deviceIdentifier }}'
        deviceMetadata: '{{ item.deviceMetadata }}'
        name: '{{ item.name }}'
  # Lists the DPCs (device policy controllers) that support zero-touch enrollment.
  # ── gcp.androiddeviceprovisioning.customers.dpcs.list ──
  - discovery_id: gcp.androiddeviceprovisioning.customers.dpcs.list
    # python: svc.customers().dpcs().list(**params).execute()
    calls:
      - action: customers.dpcs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.dpcs }}'
      item:
        dpcName: '{{ item.dpcName }}'
        name: '{{ item.name }}'
        packageName: '{{ item.packageName }}'
  # Lists the user's customer accounts.
  # ── gcp.androiddeviceprovisioning.customers.list ──
  - discovery_id: gcp.androiddeviceprovisioning.customers.list
    # python: svc.customers().list(**params).execute()
    calls:
      - action: customers.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.customers }}'
      item:
        adminEmails: '{{ item.adminEmails }}'
        companyId: '{{ item.companyId }}'
        companyName: '{{ item.companyName }}'
        googleWorkspaceAccount: '{{ item.googleWorkspaceAccount }}'
        languageCode: '{{ item.languageCode }}'
        name: '{{ item.name }}'
        ownerEmails: '{{ item.ownerEmails }}'
        skipWelcomeEmail: '{{ item.skipWelcomeEmail }}'
        termsStatus: '{{ item.termsStatus }}'
  # Lists the customers of the vendor.
  # ── gcp.androiddeviceprovisioning.partners.vendors.customers.list ──
  - discovery_id: gcp.androiddeviceprovisioning.partners.vendors.customers.list
    # python: svc.partners().vendors().customers().list(**params).execute()
    calls:
      - action: partners.vendors.customers.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.customers }}'
      item:
        adminEmails: '{{ item.adminEmails }}'
        companyId: '{{ item.companyId }}'
        companyName: '{{ item.companyName }}'
        googleWorkspaceAccount: '{{ item.googleWorkspaceAccount }}'
        languageCode: '{{ item.languageCode }}'
        name: '{{ item.name }}'
        ownerEmails: '{{ item.ownerEmails }}'
        skipWelcomeEmail: '{{ item.skipWelcomeEmail }}'
        termsStatus: '{{ item.termsStatus }}'
  # Lists the vendors of the partner.
  # ── gcp.androiddeviceprovisioning.partners.vendors.list ──
  - discovery_id: gcp.androiddeviceprovisioning.partners.vendors.list
    # python: svc.partners().vendors().list(**params).execute()
    calls:
      - action: partners.vendors.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.vendors }}'
      item:
        adminEmails: '{{ item.adminEmails }}'
        companyId: '{{ item.companyId }}'
        companyName: '{{ item.companyName }}'
        googleWorkspaceAccount: '{{ item.googleWorkspaceAccount }}'
        languageCode: '{{ item.languageCode }}'
        name: '{{ item.name }}'
        ownerEmails: '{{ item.ownerEmails }}'
        skipWelcomeEmail: '{{ item.skipWelcomeEmail }}'
        termsStatus: '{{ item.termsStatus }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets the details of a configuration.
  # ── gcp.androiddeviceprovisioning.customers.configurations.get [dependent] ──
  - discovery_id: gcp.androiddeviceprovisioning.customers.configurations.get
    for_each: gcp.androiddeviceprovisioning.customers.configurations.list
    # python: svc.customers().configurations().get(**params).execute()
    calls:
      - action: customers.configurations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        companyName: '{{ response.companyName }}'
        configurationId: '{{ response.configurationId }}'
        configurationName: '{{ response.configurationName }}'
        contactEmail: '{{ response.contactEmail }}'
        contactPhone: '{{ response.contactPhone }}'
        customMessage: '{{ response.customMessage }}'
        dpcExtras: '{{ response.dpcExtras }}'
        dpcResourcePath: '{{ response.dpcResourcePath }}'
        forcedResetTime: '{{ response.forcedResetTime }}'
        isDefault: '{{ response.isDefault }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Gets the details of a device.
  # ── gcp.androiddeviceprovisioning.customers.devices.get [dependent] ──
  - discovery_id: gcp.androiddeviceprovisioning.customers.devices.get
    for_each: gcp.androiddeviceprovisioning.customers.devices.list
    # python: svc.customers().devices().get(**params).execute()
    calls:
      - action: customers.devices.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        claims: '{{ response.claims }}'
        configuration: '{{ response.configuration }}'
        deviceId: '{{ response.deviceId }}'
        deviceIdentifier: '{{ response.deviceIdentifier }}'
        deviceMetadata: '{{ response.deviceMetadata }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.androiddeviceprovisioning.operations.get [dependent] ──
  - discovery_id: gcp.androiddeviceprovisioning.operations.get
    # python: svc.operations().get(**params).execute()
    calls:
      - action: operations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Lists the customers that are enrolled to the reseller identified by the `partnerId` argument. This l
  # ── gcp.androiddeviceprovisioning.partners.customers.list [dependent] ──
  - discovery_id: gcp.androiddeviceprovisioning.partners.customers.list
    # python: svc.partners().customers().list(**params).execute()
    calls:
      - action: partners.customers.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.customers }}'
      item:
        adminEmails: '{{ item.adminEmails }}'
        companyId: '{{ item.companyId }}'
        companyName: '{{ item.companyName }}'
        googleWorkspaceAccount: '{{ item.googleWorkspaceAccount }}'
        languageCode: '{{ item.languageCode }}'
        name: '{{ item.name }}'
        ownerEmails: '{{ item.ownerEmails }}'
        skipWelcomeEmail: '{{ item.skipWelcomeEmail }}'
        termsStatus: '{{ item.termsStatus }}'
    # required_params: ['partnerId']
  # Gets a device.
  # ── gcp.androiddeviceprovisioning.partners.devices.get [dependent] ──
  - discovery_id: gcp.androiddeviceprovisioning.partners.devices.get
    for_each: gcp.androiddeviceprovisioning.customers.devices.list
    # python: svc.partners().devices().get(**params).execute()
    calls:
      - action: partners.devices.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        claims: '{{ response.claims }}'
        configuration: '{{ response.configuration }}'
        deviceId: '{{ response.deviceId }}'
        deviceIdentifier: '{{ response.deviceIdentifier }}'
        deviceMetadata: '{{ response.deviceMetadata }}'
        name: '{{ response.name }}'
    # required_params: ['name']
