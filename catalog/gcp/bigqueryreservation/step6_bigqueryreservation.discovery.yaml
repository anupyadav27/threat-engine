# ============================================================
# Discovery YAML — bigqueryreservation (v1)
# Generated: 2026-02-21T03:06:38Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 12 | independent: 7 | dependent: 5
# ops with fields: 12/12 | dep ops with chain: 5/5
# ============================================================
version: '1.0'
provider: gcp
service: bigqueryreservation

services:
  client: bigqueryreservation
  module: "googleapiclient.discovery.build('bigqueryreservation', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists all the capacity commitments for the admin project.
  # ── gcp.bigqueryreservation.projects.locations.capacityCommitments.list ──
  - discovery_id: gcp.bigqueryreservation.projects.locations.capacityCommitments.list
    # python: svc.projects().locations().capacityCommitments().list(**params).execute()
    calls:
      - action: projects.locations.capacityCommitments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.capacityCommitments }}'
      item:
        commitmentEndTime: '{{ item.commitmentEndTime }}'
        commitmentStartTime: '{{ item.commitmentStartTime }}'
        edition: '{{ item.edition }}'
        failureStatus: '{{ item.failureStatus }}'
        isFlatRate: '{{ item.isFlatRate }}'
        multiRegionAuxiliary: '{{ item.multiRegionAuxiliary }}'
        name: '{{ item.name }}'
        plan: '{{ item.plan }}'
        renewalPlan: '{{ item.renewalPlan }}'
        slotCount: '{{ item.slotCount }}'
        state: '{{ item.state }}'
  # Retrieves a BI reservation.
  # ── gcp.bigqueryreservation.projects.locations.getBiReservation ──
  - discovery_id: gcp.bigqueryreservation.projects.locations.getBiReservation
    # python: svc.projects().locations().getBiReservation(**params).execute()
    calls:
      - action: projects.locations.getBiReservation
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        preferredTables: '{{ response.preferredTables }}'
        size: '{{ response.size }}'
        updateTime: '{{ response.updateTime }}'
  # Lists all the reservation groups for the project in the specified location.
  # ── gcp.bigqueryreservation.projects.locations.reservationGroups.list ──
  - discovery_id: gcp.bigqueryreservation.projects.locations.reservationGroups.list
    # python: svc.projects().locations().reservationGroups().list(**params).execute()
    calls:
      - action: projects.locations.reservationGroups.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.reservationGroups }}'
      item:
        name: '{{ item.name }}'
  # Lists assignments. Only explicitly created assignments will be returned. Example: * Organization `or
  # ── gcp.bigqueryreservation.projects.locations.reservations.assignments.list ──
  - discovery_id: gcp.bigqueryreservation.projects.locations.reservations.assignments.list
    # python: svc.projects().locations().reservations().assignments().list(**params).execute()
    calls:
      - action: projects.locations.reservations.assignments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.assignments }}'
      item:
        assignee: '{{ item.assignee }}'
        enableGeminiInBigquery: '{{ item.enableGeminiInBigquery }}'
        jobType: '{{ item.jobType }}'
        name: '{{ item.name }}'
        schedulingPolicy: '{{ item.schedulingPolicy }}'
        state: '{{ item.state }}'
  # Lists all the reservations for the project in the specified location.
  # ── gcp.bigqueryreservation.projects.locations.reservations.list ──
  - discovery_id: gcp.bigqueryreservation.projects.locations.reservations.list
    # python: svc.projects().locations().reservations().list(**params).execute()
    calls:
      - action: projects.locations.reservations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.reservations }}'
      item:
        autoscale: '{{ item.autoscale }}'
        concurrency: '{{ item.concurrency }}'
        creationTime: '{{ item.creationTime }}'
        edition: '{{ item.edition }}'
        ignoreIdleSlots: '{{ item.ignoreIdleSlots }}'
        labels: '{{ item.labels }}'
        maxSlots: '{{ item.maxSlots }}'
        multiRegionAuxiliary: '{{ item.multiRegionAuxiliary }}'
        name: '{{ item.name }}'
        originalPrimaryLocation: '{{ item.originalPrimaryLocation }}'
        primaryLocation: '{{ item.primaryLocation }}'
        replicationStatus: '{{ item.replicationStatus }}'
        reservationGroup: '{{ item.reservationGroup }}'
        scalingMode: '{{ item.scalingMode }}'
        schedulingPolicy: '{{ item.schedulingPolicy }}'
        secondaryLocation: '{{ item.secondaryLocation }}'
        slotCapacity: '{{ item.slotCapacity }}'
        updateTime: '{{ item.updateTime }}'
  # Looks up assignments for a specified resource for a particular region. If the request is about a pro
  # ── gcp.bigqueryreservation.projects.locations.searchAllAssignments ──
  - discovery_id: gcp.bigqueryreservation.projects.locations.searchAllAssignments
    # python: svc.projects().locations().searchAllAssignments(**params).execute()
    calls:
      - action: projects.locations.searchAllAssignments
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        assignments: '{{ response.assignments }}'
  # Deprecated: Looks up assignments for a specified resource for a particular region. If the request is
  # ── gcp.bigqueryreservation.projects.locations.searchAssignments ──
  - discovery_id: gcp.bigqueryreservation.projects.locations.searchAssignments
    # python: svc.projects().locations().searchAssignments(**params).execute()
    calls:
      - action: projects.locations.searchAssignments
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        assignments: '{{ response.assignments }}'

  # ════ DEPENDENT (enrich) operations ════
  # Returns information about the capacity commitment.
  # ── gcp.bigqueryreservation.projects.locations.capacityCommitments.get [dependent] ──
  - discovery_id: gcp.bigqueryreservation.projects.locations.capacityCommitments.get
    for_each: gcp.bigqueryreservation.projects.locations.capacityCommitments.list
    # python: svc.projects().locations().capacityCommitments().get(**params).execute()
    calls:
      - action: projects.locations.capacityCommitments.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        commitmentEndTime: '{{ response.commitmentEndTime }}'
        commitmentStartTime: '{{ response.commitmentStartTime }}'
        edition: '{{ response.edition }}'
        failureStatus: '{{ response.failureStatus }}'
        isFlatRate: '{{ response.isFlatRate }}'
        multiRegionAuxiliary: '{{ response.multiRegionAuxiliary }}'
        name: '{{ response.name }}'
        plan: '{{ response.plan }}'
        renewalPlan: '{{ response.renewalPlan }}'
        slotCount: '{{ response.slotCount }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Returns information about the reservation group.
  # ── gcp.bigqueryreservation.projects.locations.reservationGroups.get [dependent] ──
  - discovery_id: gcp.bigqueryreservation.projects.locations.reservationGroups.get
    for_each: gcp.bigqueryreservation.projects.locations.reservationGroups.list
    # python: svc.projects().locations().reservationGroups().get(**params).execute()
    calls:
      - action: projects.locations.reservationGroups.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. May return: * A`NOT_FOUND` error if the resource does
  # ── gcp.bigqueryreservation.projects.locations.reservations.assignments.getIamPolicy [dependent] ──
  - discovery_id: gcp.bigqueryreservation.projects.locations.reservations.assignments.getIamPolicy
    for_each: gcp.bigqueryreservation.projects.locations.reservations.assignments.list
    # python: svc.projects().locations().reservations().assignments().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.reservations.assignments.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Returns information about the reservation.
  # ── gcp.bigqueryreservation.projects.locations.reservations.get [dependent] ──
  - discovery_id: gcp.bigqueryreservation.projects.locations.reservations.get
    for_each: gcp.bigqueryreservation.projects.locations.reservations.list
    # python: svc.projects().locations().reservations().get(**params).execute()
    calls:
      - action: projects.locations.reservations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        autoscale: '{{ response.autoscale }}'
        concurrency: '{{ response.concurrency }}'
        creationTime: '{{ response.creationTime }}'
        edition: '{{ response.edition }}'
        ignoreIdleSlots: '{{ response.ignoreIdleSlots }}'
        labels: '{{ response.labels }}'
        maxSlots: '{{ response.maxSlots }}'
        multiRegionAuxiliary: '{{ response.multiRegionAuxiliary }}'
        name: '{{ response.name }}'
        originalPrimaryLocation: '{{ response.originalPrimaryLocation }}'
        primaryLocation: '{{ response.primaryLocation }}'
        replicationStatus: '{{ response.replicationStatus }}'
        reservationGroup: '{{ response.reservationGroup }}'
        scalingMode: '{{ response.scalingMode }}'
        schedulingPolicy: '{{ response.schedulingPolicy }}'
        secondaryLocation: '{{ response.secondaryLocation }}'
        slotCapacity: '{{ response.slotCapacity }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. May return: * A`NOT_FOUND` error if the resource does
  # ── gcp.bigqueryreservation.projects.locations.reservations.getIamPolicy [dependent] ──
  - discovery_id: gcp.bigqueryreservation.projects.locations.reservations.getIamPolicy
    for_each: gcp.bigqueryreservation.projects.locations.reservations.list
    # python: svc.projects().locations().reservations().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.reservations.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
