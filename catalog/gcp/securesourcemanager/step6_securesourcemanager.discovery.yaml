# ============================================================
# Discovery YAML — securesourcemanager (v1)
# Generated: 2026-02-21T03:06:41Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 25 | independent: 11 | dependent: 14
# ops with fields: 25/25 | dep ops with chain: 14/14
# ============================================================
version: '1.0'
provider: gcp
service: securesourcemanager

services:
  client: securesourcemanager
  module: "googleapiclient.discovery.build('securesourcemanager', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Gets information about a location.
  # ── gcp.securesourcemanager.projects.locations.get ──
  - discovery_id: gcp.securesourcemanager.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists Instances in a given project and location.
  # ── gcp.securesourcemanager.projects.locations.instances.list ──
  - discovery_id: gcp.securesourcemanager.projects.locations.instances.list
    # python: svc.projects().locations().instances().list(**params).execute()
    calls:
      - action: projects.locations.instances.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.instances }}'
      item:
        createTime: '{{ item.createTime }}'
        hostConfig: '{{ item.hostConfig }}'
        kmsKey: '{{ item.kmsKey }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        privateConfig: '{{ item.privateConfig }}'
        state: '{{ item.state }}'
        stateNote: '{{ item.stateNote }}'
        updateTime: '{{ item.updateTime }}'
        workforceIdentityFederationConfig: '{{ item.workforceIdentityFederationConfig }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.securesourcemanager.projects.locations.list ──
  - discovery_id: gcp.securesourcemanager.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.securesourcemanager.projects.locations.operations.list ──
  - discovery_id: gcp.securesourcemanager.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # ListBranchRules lists branch rules in a given repository.
  # ── gcp.securesourcemanager.projects.locations.repositories.branchRules.list ──
  - discovery_id: gcp.securesourcemanager.projects.locations.repositories.branchRules.list
    # python: svc.projects().locations().repositories().branchRules().list(**params).execute()
    calls:
      - action: projects.locations.repositories.branchRules.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.branchRules }}'
      item:
        allowStaleReviews: '{{ item.allowStaleReviews }}'
        annotations: '{{ item.annotations }}'
        createTime: '{{ item.createTime }}'
        disabled: '{{ item.disabled }}'
        includePattern: '{{ item.includePattern }}'
        minimumApprovalsCount: '{{ item.minimumApprovalsCount }}'
        minimumReviewsCount: '{{ item.minimumReviewsCount }}'
        name: '{{ item.name }}'
        requireCommentsResolved: '{{ item.requireCommentsResolved }}'
        requireLinearHistory: '{{ item.requireLinearHistory }}'
        requirePullRequest: '{{ item.requirePullRequest }}'
        requiredStatusChecks: '{{ item.requiredStatusChecks }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # Lists hooks in a given repository.
  # ── gcp.securesourcemanager.projects.locations.repositories.hooks.list ──
  - discovery_id: gcp.securesourcemanager.projects.locations.repositories.hooks.list
    # python: svc.projects().locations().repositories().hooks().list(**params).execute()
    calls:
      - action: projects.locations.repositories.hooks.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.hooks }}'
      item:
        createTime: '{{ item.createTime }}'
        disabled: '{{ item.disabled }}'
        events: '{{ item.events }}'
        name: '{{ item.name }}'
        pushOption: '{{ item.pushOption }}'
        sensitiveQueryString: '{{ item.sensitiveQueryString }}'
        targetUri: '{{ item.targetUri }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # Lists comments in an issue.
  # ── gcp.securesourcemanager.projects.locations.repositories.issues.issueComments.list ──
  - discovery_id: gcp.securesourcemanager.projects.locations.repositories.issues.issueComments.list
    # python: svc.projects().locations().repositories().issues().issueComments().list(**params).execute()
    calls:
      - action: projects.locations.repositories.issues.issueComments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.issueComments }}'
      item:
        body: '{{ item.body }}'
        createTime: '{{ item.createTime }}'
        name: '{{ item.name }}'
        updateTime: '{{ item.updateTime }}'
  # Lists issues in a repository.
  # ── gcp.securesourcemanager.projects.locations.repositories.issues.list ──
  - discovery_id: gcp.securesourcemanager.projects.locations.repositories.issues.list
    # python: svc.projects().locations().repositories().issues().list(**params).execute()
    calls:
      - action: projects.locations.repositories.issues.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.issues }}'
      item:
        body: '{{ item.body }}'
        closeTime: '{{ item.closeTime }}'
        createTime: '{{ item.createTime }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
        title: '{{ item.title }}'
        updateTime: '{{ item.updateTime }}'
  # Lists Repositories in a given project and location. The instance field is required in the query para
  # ── gcp.securesourcemanager.projects.locations.repositories.list ──
  - discovery_id: gcp.securesourcemanager.projects.locations.repositories.list
    # python: svc.projects().locations().repositories().list(**params).execute()
    calls:
      - action: projects.locations.repositories.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.repositories }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        initialConfig: '{{ item.initialConfig }}'
        instance: '{{ item.instance }}'
        name: '{{ item.name }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
        uris: '{{ item.uris }}'
  # Lists pull requests in a repository.
  # ── gcp.securesourcemanager.projects.locations.repositories.pullRequests.list ──
  - discovery_id: gcp.securesourcemanager.projects.locations.repositories.pullRequests.list
    # python: svc.projects().locations().repositories().pullRequests().list(**params).execute()
    calls:
      - action: projects.locations.repositories.pullRequests.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.pullRequests }}'
      item:
        base: '{{ item.base }}'
        body: '{{ item.body }}'
        closeTime: '{{ item.closeTime }}'
        createTime: '{{ item.createTime }}'
        head: '{{ item.head }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
        title: '{{ item.title }}'
        updateTime: '{{ item.updateTime }}'
  # Lists pull request comments.
  # ── gcp.securesourcemanager.projects.locations.repositories.pullRequests.pullRequestComments.list ──
  - discovery_id: gcp.securesourcemanager.projects.locations.repositories.pullRequests.pullRequestComments.list
    # python: svc.projects().locations().repositories().pullRequests().pullRequestComments().list(**params).execute()
    calls:
      - action: projects.locations.repositories.pullRequests.pullRequestComments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.pullRequestComments }}'
      item:
        code: '{{ item.code }}'
        comment: '{{ item.comment }}'
        createTime: '{{ item.createTime }}'
        name: '{{ item.name }}'
        review: '{{ item.review }}'
        updateTime: '{{ item.updateTime }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets details of a single instance.
  # ── gcp.securesourcemanager.projects.locations.instances.get [dependent] ──
  - discovery_id: gcp.securesourcemanager.projects.locations.instances.get
    for_each: gcp.securesourcemanager.projects.locations.instances.list
    # python: svc.projects().locations().instances().get(**params).execute()
    calls:
      - action: projects.locations.instances.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        hostConfig: '{{ response.hostConfig }}'
        kmsKey: '{{ response.kmsKey }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        privateConfig: '{{ response.privateConfig }}'
        state: '{{ response.state }}'
        stateNote: '{{ response.stateNote }}'
        updateTime: '{{ response.updateTime }}'
        workforceIdentityFederationConfig: '{{ response.workforceIdentityFederationConfig }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.securesourcemanager.projects.locations.instances.getIamPolicy [dependent] ──
  - discovery_id: gcp.securesourcemanager.projects.locations.instances.getIamPolicy
    for_each: gcp.securesourcemanager.projects.locations.instances.list
    # python: svc.projects().locations().instances().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.instances.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.securesourcemanager.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.securesourcemanager.projects.locations.operations.get
    for_each: gcp.securesourcemanager.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # GetBranchRule gets a branch rule.
  # ── gcp.securesourcemanager.projects.locations.repositories.branchRules.get [dependent] ──
  - discovery_id: gcp.securesourcemanager.projects.locations.repositories.branchRules.get
    for_each: gcp.securesourcemanager.projects.locations.repositories.branchRules.list
    # python: svc.projects().locations().repositories().branchRules().get(**params).execute()
    calls:
      - action: projects.locations.repositories.branchRules.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        allowStaleReviews: '{{ response.allowStaleReviews }}'
        annotations: '{{ response.annotations }}'
        createTime: '{{ response.createTime }}'
        disabled: '{{ response.disabled }}'
        includePattern: '{{ response.includePattern }}'
        minimumApprovalsCount: '{{ response.minimumApprovalsCount }}'
        minimumReviewsCount: '{{ response.minimumReviewsCount }}'
        name: '{{ response.name }}'
        requireCommentsResolved: '{{ response.requireCommentsResolved }}'
        requireLinearHistory: '{{ response.requireLinearHistory }}'
        requirePullRequest: '{{ response.requirePullRequest }}'
        requiredStatusChecks: '{{ response.requiredStatusChecks }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Fetches a blob from a repository.
  # ── gcp.securesourcemanager.projects.locations.repositories.fetchBlob [dependent] ──
  - discovery_id: gcp.securesourcemanager.projects.locations.repositories.fetchBlob
    for_each: gcp.securesourcemanager.projects.locations.repositories.list
    # python: svc.projects().locations().repositories().fetchBlob(**params).execute()
    calls:
      - action: projects.locations.repositories.fetchBlob
        params:
          repository: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        content: '{{ response.content }}'
        sha: '{{ response.sha }}'
    # required_params: ['repository']
  # Fetches a tree from a repository.
  # ── gcp.securesourcemanager.projects.locations.repositories.fetchTree [dependent] ──
  - discovery_id: gcp.securesourcemanager.projects.locations.repositories.fetchTree
    for_each: gcp.securesourcemanager.projects.locations.repositories.list
    # python: svc.projects().locations().repositories().fetchTree(**params).execute()
    calls:
      - action: projects.locations.repositories.fetchTree
        params:
          repository: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        treeEntries: '{{ response.treeEntries }}'
    # required_params: ['repository']
  # Gets metadata of a repository.
  # ── gcp.securesourcemanager.projects.locations.repositories.get [dependent] ──
  - discovery_id: gcp.securesourcemanager.projects.locations.repositories.get
    for_each: gcp.securesourcemanager.projects.locations.repositories.list
    # python: svc.projects().locations().repositories().get(**params).execute()
    calls:
      - action: projects.locations.repositories.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        initialConfig: '{{ response.initialConfig }}'
        instance: '{{ response.instance }}'
        name: '{{ response.name }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
        uris: '{{ response.uris }}'
    # required_params: ['name']
  # Get IAM policy for a repository.
  # ── gcp.securesourcemanager.projects.locations.repositories.getIamPolicy [dependent] ──
  - discovery_id: gcp.securesourcemanager.projects.locations.repositories.getIamPolicy
    for_each: gcp.securesourcemanager.projects.locations.repositories.list
    # python: svc.projects().locations().repositories().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.repositories.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Gets metadata of a hook.
  # ── gcp.securesourcemanager.projects.locations.repositories.hooks.get [dependent] ──
  - discovery_id: gcp.securesourcemanager.projects.locations.repositories.hooks.get
    for_each: gcp.securesourcemanager.projects.locations.repositories.hooks.list
    # python: svc.projects().locations().repositories().hooks().get(**params).execute()
    calls:
      - action: projects.locations.repositories.hooks.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        disabled: '{{ response.disabled }}'
        events: '{{ response.events }}'
        name: '{{ response.name }}'
        pushOption: '{{ response.pushOption }}'
        sensitiveQueryString: '{{ response.sensitiveQueryString }}'
        targetUri: '{{ response.targetUri }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets an issue.
  # ── gcp.securesourcemanager.projects.locations.repositories.issues.get [dependent] ──
  - discovery_id: gcp.securesourcemanager.projects.locations.repositories.issues.get
    for_each: gcp.securesourcemanager.projects.locations.repositories.issues.list
    # python: svc.projects().locations().repositories().issues().get(**params).execute()
    calls:
      - action: projects.locations.repositories.issues.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        body: '{{ response.body }}'
        closeTime: '{{ response.closeTime }}'
        createTime: '{{ response.createTime }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
        title: '{{ response.title }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets an issue comment.
  # ── gcp.securesourcemanager.projects.locations.repositories.issues.issueComments.get [dependent] ──
  - discovery_id: gcp.securesourcemanager.projects.locations.repositories.issues.issueComments.get
    for_each: gcp.securesourcemanager.projects.locations.repositories.issues.issueComments.list
    # python: svc.projects().locations().repositories().issues().issueComments().get(**params).execute()
    calls:
      - action: projects.locations.repositories.issues.issueComments.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        body: '{{ response.body }}'
        createTime: '{{ response.createTime }}'
        name: '{{ response.name }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets a pull request.
  # ── gcp.securesourcemanager.projects.locations.repositories.pullRequests.get [dependent] ──
  - discovery_id: gcp.securesourcemanager.projects.locations.repositories.pullRequests.get
    for_each: gcp.securesourcemanager.projects.locations.repositories.pullRequests.listFileDiffs
    # python: svc.projects().locations().repositories().pullRequests().get(**params).execute()
    calls:
      - action: projects.locations.repositories.pullRequests.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        base: '{{ response.base }}'
        body: '{{ response.body }}'
        closeTime: '{{ response.closeTime }}'
        createTime: '{{ response.createTime }}'
        head: '{{ response.head }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
        title: '{{ response.title }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Lists a pull request's file diffs.
  # ── gcp.securesourcemanager.projects.locations.repositories.pullRequests.listFileDiffs [dependent] ──
  - discovery_id: gcp.securesourcemanager.projects.locations.repositories.pullRequests.listFileDiffs
    for_each: gcp.securesourcemanager.projects.locations.repositories.list
    # python: svc.projects().locations().repositories().pullRequests().listFileDiffs(**params).execute()
    calls:
      - action: projects.locations.repositories.pullRequests.listFileDiffs
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.pullRequests }}'
      item:
        action: '{{ item.action }}'
        name: '{{ item.name }}'
        patch: '{{ item.patch }}'
        sha: '{{ item.sha }}'
    # required_params: ['name']
  # Gets a pull request comment.
  # ── gcp.securesourcemanager.projects.locations.repositories.pullRequests.pullRequestComments.get [dependent] ──
  - discovery_id: gcp.securesourcemanager.projects.locations.repositories.pullRequests.pullRequestComments.get
    for_each: gcp.securesourcemanager.projects.locations.repositories.pullRequests.pullRequestComments.list
    # python: svc.projects().locations().repositories().pullRequests().pullRequestComments().get(**params).execute()
    calls:
      - action: projects.locations.repositories.pullRequests.pullRequestComments.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        code: '{{ response.code }}'
        comment: '{{ response.comment }}'
        createTime: '{{ response.createTime }}'
        name: '{{ response.name }}'
        review: '{{ response.review }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
