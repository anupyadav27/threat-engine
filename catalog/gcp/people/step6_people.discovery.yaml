# ============================================================
# Discovery YAML — people (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 11 | independent: 8 | dependent: 3
# ops with fields: 11/11 | dep ops with chain: 3/3
# ============================================================
version: '1.0'
provider: gcp
service: people

services:
  client: people
  module: "googleapiclient.discovery.build('people', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Get a list of contact groups owned by the authenticated user by specifying a list of contact group r
  # ── gcp.people.contactGroups.batchGet ──
  - discovery_id: gcp.people.contactGroups.batchGet
    # python: svc.contactGroups().batchGet(**params).execute()
    calls:
      - action: contactGroups.batchGet
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        responses: '{{ response.responses }}'
  # List all contact groups owned by the authenticated user. Members of the contact groups are not popul
  # ── gcp.people.contactGroups.list ──
  - discovery_id: gcp.people.contactGroups.list
    # python: svc.contactGroups().list(**params).execute()
    calls:
      - action: contactGroups.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.contactGroups }}'
      item:
        clientData: '{{ item.clientData }}'
        formattedName: '{{ item.formattedName }}'
        groupType: '{{ item.groupType }}'
        memberCount: '{{ item.memberCount }}'
        memberResourceNames: '{{ item.memberResourceNames }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        resourceName: '{{ item.resourceName }}'
  # List all "Other contacts", that is contacts that are not in a contact group. "Other contacts" are ty
  # ── gcp.people.otherContacts.list ──
  - discovery_id: gcp.people.otherContacts.list
    # python: svc.otherContacts().list(**params).execute()
    calls:
      - action: otherContacts.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.otherContacts }}'
      item:
        addresses: '{{ item.addresses }}'
        ageRange: '{{ item.ageRange }}'
        ageRanges: '{{ item.ageRanges }}'
        biographies: '{{ item.biographies }}'
        birthdays: '{{ item.birthdays }}'
        braggingRights: '{{ item.braggingRights }}'
        calendarUrls: '{{ item.calendarUrls }}'
        clientData: '{{ item.clientData }}'
        coverPhotos: '{{ item.coverPhotos }}'
        emailAddresses: '{{ item.emailAddresses }}'
        events: '{{ item.events }}'
        externalIds: '{{ item.externalIds }}'
        fileAses: '{{ item.fileAses }}'
        genders: '{{ item.genders }}'
        imClients: '{{ item.imClients }}'
        interests: '{{ item.interests }}'
        locales: '{{ item.locales }}'
        locations: '{{ item.locations }}'
        memberships: '{{ item.memberships }}'
        metadata: '{{ item.metadata }}'
        miscKeywords: '{{ item.miscKeywords }}'
        names: '{{ item.names }}'
        nicknames: '{{ item.nicknames }}'
        occupations: '{{ item.occupations }}'
        organizations: '{{ item.organizations }}'
        phoneNumbers: '{{ item.phoneNumbers }}'
        photos: '{{ item.photos }}'
        relations: '{{ item.relations }}'
        relationshipInterests: '{{ item.relationshipInterests }}'
        relationshipStatuses: '{{ item.relationshipStatuses }}'
        residences: '{{ item.residences }}'
        resourceName: '{{ item.resourceName }}'
        sipAddresses: '{{ item.sipAddresses }}'
        skills: '{{ item.skills }}'
        taglines: '{{ item.taglines }}'
        urls: '{{ item.urls }}'
        userDefined: '{{ item.userDefined }}'
  # Provides a list of contacts in the authenticated user's other contacts that matches the search query
  # ── gcp.people.otherContacts.search ──
  - discovery_id: gcp.people.otherContacts.search
    # python: svc.otherContacts().search(**params).execute()
    calls:
      - action: otherContacts.search
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.otherContacts }}'
      item:
        person: '{{ item.person }}'
  # Provides information about a list of specific people by specifying a list of requested resource name
  # ── gcp.people.people.getBatchGet ──
  - discovery_id: gcp.people.people.getBatchGet
    # python: svc.people().getBatchGet(**params).execute()
    calls:
      - action: people.getBatchGet
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        responses: '{{ response.responses }}'
  # Provides a list of domain profiles and domain contacts in the authenticated user's domain directory.
  # ── gcp.people.people.listDirectoryPeople ──
  - discovery_id: gcp.people.people.listDirectoryPeople
    # python: svc.people().listDirectoryPeople(**params).execute()
    calls:
      - action: people.listDirectoryPeople
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.people }}'
      item:
        addresses: '{{ item.addresses }}'
        ageRange: '{{ item.ageRange }}'
        ageRanges: '{{ item.ageRanges }}'
        biographies: '{{ item.biographies }}'
        birthdays: '{{ item.birthdays }}'
        braggingRights: '{{ item.braggingRights }}'
        calendarUrls: '{{ item.calendarUrls }}'
        clientData: '{{ item.clientData }}'
        coverPhotos: '{{ item.coverPhotos }}'
        emailAddresses: '{{ item.emailAddresses }}'
        events: '{{ item.events }}'
        externalIds: '{{ item.externalIds }}'
        fileAses: '{{ item.fileAses }}'
        genders: '{{ item.genders }}'
        imClients: '{{ item.imClients }}'
        interests: '{{ item.interests }}'
        locales: '{{ item.locales }}'
        locations: '{{ item.locations }}'
        memberships: '{{ item.memberships }}'
        metadata: '{{ item.metadata }}'
        miscKeywords: '{{ item.miscKeywords }}'
        names: '{{ item.names }}'
        nicknames: '{{ item.nicknames }}'
        occupations: '{{ item.occupations }}'
        organizations: '{{ item.organizations }}'
        phoneNumbers: '{{ item.phoneNumbers }}'
        photos: '{{ item.photos }}'
        relations: '{{ item.relations }}'
        relationshipInterests: '{{ item.relationshipInterests }}'
        relationshipStatuses: '{{ item.relationshipStatuses }}'
        residences: '{{ item.residences }}'
        resourceName: '{{ item.resourceName }}'
        sipAddresses: '{{ item.sipAddresses }}'
        skills: '{{ item.skills }}'
        taglines: '{{ item.taglines }}'
        urls: '{{ item.urls }}'
        userDefined: '{{ item.userDefined }}'
  # Provides a list of contacts in the authenticated user's grouped contacts that matches the search que
  # ── gcp.people.people.searchContacts ──
  - discovery_id: gcp.people.people.searchContacts
    # python: svc.people().searchContacts(**params).execute()
    calls:
      - action: people.searchContacts
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        results: '{{ response.results }}'
  # Provides a list of domain profiles and domain contacts in the authenticated user's domain directory 
  # ── gcp.people.people.searchDirectoryPeople ──
  - discovery_id: gcp.people.people.searchDirectoryPeople
    # python: svc.people().searchDirectoryPeople(**params).execute()
    calls:
      - action: people.searchDirectoryPeople
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        people: '{{ response.people }}'
        totalSize: '{{ response.totalSize }}'

  # ════ DEPENDENT (enrich) operations ════
  # Get a specific contact group owned by the authenticated user by specifying a contact group resource 
  # ── gcp.people.contactGroups.get [dependent] ──
  - discovery_id: gcp.people.contactGroups.get
    for_each: gcp.people.contactGroups.list
    # python: svc.contactGroups().get(**params).execute()
    calls:
      - action: contactGroups.get
        params:
          resourceName: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        clientData: '{{ response.clientData }}'
        formattedName: '{{ response.formattedName }}'
        groupType: '{{ response.groupType }}'
        memberCount: '{{ response.memberCount }}'
        memberResourceNames: '{{ response.memberResourceNames }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        resourceName: '{{ response.resourceName }}'
    # required_params: ['resourceName']
  # Provides a list of the authenticated user's contacts. Sync tokens expire 7 days after the full sync.
  # ── gcp.people.people.connections.list [dependent] ──
  - discovery_id: gcp.people.people.connections.list
    for_each: gcp.people.people.listDirectoryPeople
    # python: svc.people().connections().list(**params).execute()
    calls:
      - action: people.connections.list
        params:
          resourceName: '{{ item.ageRange }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.connections }}'
      item:
        addresses: '{{ item.addresses }}'
        ageRange: '{{ item.ageRange }}'
        ageRanges: '{{ item.ageRanges }}'
        biographies: '{{ item.biographies }}'
        birthdays: '{{ item.birthdays }}'
        braggingRights: '{{ item.braggingRights }}'
        calendarUrls: '{{ item.calendarUrls }}'
        clientData: '{{ item.clientData }}'
        coverPhotos: '{{ item.coverPhotos }}'
        emailAddresses: '{{ item.emailAddresses }}'
        events: '{{ item.events }}'
        externalIds: '{{ item.externalIds }}'
        fileAses: '{{ item.fileAses }}'
        genders: '{{ item.genders }}'
        imClients: '{{ item.imClients }}'
        interests: '{{ item.interests }}'
        locales: '{{ item.locales }}'
        locations: '{{ item.locations }}'
        memberships: '{{ item.memberships }}'
        metadata: '{{ item.metadata }}'
        miscKeywords: '{{ item.miscKeywords }}'
        names: '{{ item.names }}'
        nicknames: '{{ item.nicknames }}'
        occupations: '{{ item.occupations }}'
        organizations: '{{ item.organizations }}'
        phoneNumbers: '{{ item.phoneNumbers }}'
        photos: '{{ item.photos }}'
        relations: '{{ item.relations }}'
        relationshipInterests: '{{ item.relationshipInterests }}'
        relationshipStatuses: '{{ item.relationshipStatuses }}'
        residences: '{{ item.residences }}'
        resourceName: '{{ item.resourceName }}'
        sipAddresses: '{{ item.sipAddresses }}'
        skills: '{{ item.skills }}'
        taglines: '{{ item.taglines }}'
        urls: '{{ item.urls }}'
        userDefined: '{{ item.userDefined }}'
    # required_params: ['resourceName']
  # Provides information about a person by specifying a resource name. Use `people/me` to indicate the a
  # ── gcp.people.people.get [dependent] ──
  - discovery_id: gcp.people.people.get
    for_each: gcp.people.people.listDirectoryPeople
    # python: svc.people().get(**params).execute()
    calls:
      - action: people.get
        params:
          resourceName: '{{ item.ageRange }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        addresses: '{{ response.addresses }}'
        ageRange: '{{ response.ageRange }}'
        ageRanges: '{{ response.ageRanges }}'
        biographies: '{{ response.biographies }}'
        birthdays: '{{ response.birthdays }}'
        braggingRights: '{{ response.braggingRights }}'
        calendarUrls: '{{ response.calendarUrls }}'
        clientData: '{{ response.clientData }}'
        coverPhotos: '{{ response.coverPhotos }}'
        emailAddresses: '{{ response.emailAddresses }}'
        events: '{{ response.events }}'
        externalIds: '{{ response.externalIds }}'
        fileAses: '{{ response.fileAses }}'
        genders: '{{ response.genders }}'
        imClients: '{{ response.imClients }}'
        interests: '{{ response.interests }}'
        locales: '{{ response.locales }}'
        locations: '{{ response.locations }}'
        memberships: '{{ response.memberships }}'
        metadata: '{{ response.metadata }}'
        miscKeywords: '{{ response.miscKeywords }}'
        names: '{{ response.names }}'
        nicknames: '{{ response.nicknames }}'
        occupations: '{{ response.occupations }}'
        organizations: '{{ response.organizations }}'
        phoneNumbers: '{{ response.phoneNumbers }}'
        photos: '{{ response.photos }}'
        relations: '{{ response.relations }}'
        relationshipInterests: '{{ response.relationshipInterests }}'
        relationshipStatuses: '{{ response.relationshipStatuses }}'
        residences: '{{ response.residences }}'
        resourceName: '{{ response.resourceName }}'
        sipAddresses: '{{ response.sipAddresses }}'
        skills: '{{ response.skills }}'
        taglines: '{{ response.taglines }}'
        urls: '{{ response.urls }}'
        userDefined: '{{ response.userDefined }}'
    # required_params: ['resourceName']
