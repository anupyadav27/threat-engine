# ============================================================
# Discovery YAML — firebaseapphosting (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 13 | independent: 7 | dependent: 6
# ops with fields: 13/13 | dep ops with chain: 6/6
# ============================================================
version: '1.0'
provider: gcp
service: firebaseapphosting

services:
  client: firebaseapphosting
  module: "googleapiclient.discovery.build('firebaseapphosting', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists builds in a given project, location, and backend.
  # ── gcp.firebaseapphosting.projects.locations.backends.builds.list ──
  - discovery_id: gcp.firebaseapphosting.projects.locations.backends.builds.list
    # python: svc.projects().locations().backends().builds().list(**params).execute()
    calls:
      - action: projects.locations.backends.builds.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.builds }}'
      item:
        annotations: '{{ item.annotations }}'
        buildLogsUri: '{{ item.buildLogsUri }}'
        config: '{{ item.config }}'
        createTime: '{{ item.createTime }}'
        deleteTime: '{{ item.deleteTime }}'
        displayName: '{{ item.displayName }}'
        environment: '{{ item.environment }}'
        errors: '{{ item.errors }}'
        image: '{{ item.image }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        reconciling: '{{ item.reconciling }}'
        source: '{{ item.source }}'
        state: '{{ item.state }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # Lists domains of a backend.
  # ── gcp.firebaseapphosting.projects.locations.backends.domains.list ──
  - discovery_id: gcp.firebaseapphosting.projects.locations.backends.domains.list
    # python: svc.projects().locations().backends().domains().list(**params).execute()
    calls:
      - action: projects.locations.backends.domains.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.domains }}'
      item:
        annotations: '{{ item.annotations }}'
        createTime: '{{ item.createTime }}'
        customDomainStatus: '{{ item.customDomainStatus }}'
        deleteTime: '{{ item.deleteTime }}'
        disabled: '{{ item.disabled }}'
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        reconciling: '{{ item.reconciling }}'
        serve: '{{ item.serve }}'
        type: '{{ item.type }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # Lists backends in a given project and location.
  # ── gcp.firebaseapphosting.projects.locations.backends.list ──
  - discovery_id: gcp.firebaseapphosting.projects.locations.backends.list
    # python: svc.projects().locations().backends().list(**params).execute()
    calls:
      - action: projects.locations.backends.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.backends }}'
      item:
        annotations: '{{ item.annotations }}'
        appId: '{{ item.appId }}'
        codebase: '{{ item.codebase }}'
        createTime: '{{ item.createTime }}'
        deleteTime: '{{ item.deleteTime }}'
        displayName: '{{ item.displayName }}'
        environment: '{{ item.environment }}'
        labels: '{{ item.labels }}'
        managedResources: '{{ item.managedResources }}'
        mode: '{{ item.mode }}'
        name: '{{ item.name }}'
        reconciling: '{{ item.reconciling }}'
        requestLogsDisabled: '{{ item.requestLogsDisabled }}'
        serviceAccount: '{{ item.serviceAccount }}'
        servingLocality: '{{ item.servingLocality }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
        uri: '{{ item.uri }}'
  # Lists rollouts for a backend.
  # ── gcp.firebaseapphosting.projects.locations.backends.rollouts.list ──
  - discovery_id: gcp.firebaseapphosting.projects.locations.backends.rollouts.list
    # python: svc.projects().locations().backends().rollouts().list(**params).execute()
    calls:
      - action: projects.locations.backends.rollouts.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.rollouts }}'
      item:
        annotations: '{{ item.annotations }}'
        build: '{{ item.build }}'
        createTime: '{{ item.createTime }}'
        deleteTime: '{{ item.deleteTime }}'
        displayName: '{{ item.displayName }}'
        error: '{{ item.error }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        reconciling: '{{ item.reconciling }}'
        state: '{{ item.state }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # Gets information about a location.
  # ── gcp.firebaseapphosting.projects.locations.get ──
  - discovery_id: gcp.firebaseapphosting.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.firebaseapphosting.projects.locations.list ──
  - discovery_id: gcp.firebaseapphosting.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.firebaseapphosting.projects.locations.operations.list ──
  - discovery_id: gcp.firebaseapphosting.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets information about a build.
  # ── gcp.firebaseapphosting.projects.locations.backends.builds.get [dependent] ──
  - discovery_id: gcp.firebaseapphosting.projects.locations.backends.builds.get
    for_each: gcp.firebaseapphosting.projects.locations.backends.builds.list
    # python: svc.projects().locations().backends().builds().get(**params).execute()
    calls:
      - action: projects.locations.backends.builds.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        buildLogsUri: '{{ response.buildLogsUri }}'
        config: '{{ response.config }}'
        createTime: '{{ response.createTime }}'
        deleteTime: '{{ response.deleteTime }}'
        displayName: '{{ response.displayName }}'
        environment: '{{ response.environment }}'
        errors: '{{ response.errors }}'
        image: '{{ response.image }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        reconciling: '{{ response.reconciling }}'
        source: '{{ response.source }}'
        state: '{{ response.state }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets information about a domain.
  # ── gcp.firebaseapphosting.projects.locations.backends.domains.get [dependent] ──
  - discovery_id: gcp.firebaseapphosting.projects.locations.backends.domains.get
    for_each: gcp.firebaseapphosting.projects.locations.backends.domains.list
    # python: svc.projects().locations().backends().domains().get(**params).execute()
    calls:
      - action: projects.locations.backends.domains.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        createTime: '{{ response.createTime }}'
        customDomainStatus: '{{ response.customDomainStatus }}'
        deleteTime: '{{ response.deleteTime }}'
        disabled: '{{ response.disabled }}'
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        reconciling: '{{ response.reconciling }}'
        serve: '{{ response.serve }}'
        type: '{{ response.type }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets information about a backend.
  # ── gcp.firebaseapphosting.projects.locations.backends.get [dependent] ──
  - discovery_id: gcp.firebaseapphosting.projects.locations.backends.get
    for_each: gcp.firebaseapphosting.projects.locations.backends.list
    # python: svc.projects().locations().backends().get(**params).execute()
    calls:
      - action: projects.locations.backends.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        appId: '{{ response.appId }}'
        codebase: '{{ response.codebase }}'
        createTime: '{{ response.createTime }}'
        deleteTime: '{{ response.deleteTime }}'
        displayName: '{{ response.displayName }}'
        environment: '{{ response.environment }}'
        labels: '{{ response.labels }}'
        managedResources: '{{ response.managedResources }}'
        mode: '{{ response.mode }}'
        name: '{{ response.name }}'
        reconciling: '{{ response.reconciling }}'
        requestLogsDisabled: '{{ response.requestLogsDisabled }}'
        serviceAccount: '{{ response.serviceAccount }}'
        servingLocality: '{{ response.servingLocality }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
        uri: '{{ response.uri }}'
    # required_params: ['name']
  # Gets information about a rollout.
  # ── gcp.firebaseapphosting.projects.locations.backends.rollouts.get [dependent] ──
  - discovery_id: gcp.firebaseapphosting.projects.locations.backends.rollouts.get
    for_each: gcp.firebaseapphosting.projects.locations.backends.rollouts.list
    # python: svc.projects().locations().backends().rollouts().get(**params).execute()
    calls:
      - action: projects.locations.backends.rollouts.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        build: '{{ response.build }}'
        createTime: '{{ response.createTime }}'
        deleteTime: '{{ response.deleteTime }}'
        displayName: '{{ response.displayName }}'
        error: '{{ response.error }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        reconciling: '{{ response.reconciling }}'
        state: '{{ response.state }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets information about a backend's traffic.
  # ── gcp.firebaseapphosting.projects.locations.backends.traffic.get [dependent] ──
  - discovery_id: gcp.firebaseapphosting.projects.locations.backends.traffic.get
    for_each: gcp.firebaseapphosting.projects.locations.backends.list
    # python: svc.projects().locations().backends().traffic().get(**params).execute()
    calls:
      - action: projects.locations.backends.traffic.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        createTime: '{{ response.createTime }}'
        current: '{{ response.current }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        reconciling: '{{ response.reconciling }}'
        rolloutPolicy: '{{ response.rolloutPolicy }}'
        target: '{{ response.target }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.firebaseapphosting.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.firebaseapphosting.projects.locations.operations.get
    for_each: gcp.firebaseapphosting.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
