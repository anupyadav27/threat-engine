# ============================================================
# Discovery YAML — eventarc (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 29 | independent: 13 | dependent: 16
# ops with fields: 28/29 | dep ops with chain: 16/16
# ============================================================
version: '1.0'
provider: gcp
service: eventarc

services:
  client: eventarc
  module: "googleapiclient.discovery.build('eventarc', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # List channel connections.
  # ── gcp.eventarc.projects.locations.channelConnections.list ──
  - discovery_id: gcp.eventarc.projects.locations.channelConnections.list
    # python: svc.projects().locations().channelConnections().list(**params).execute()
    calls:
      - action: projects.locations.channelConnections.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.channelConnections }}'
      item:
        activationToken: '{{ item.activationToken }}'
        channel: '{{ item.channel }}'
        createTime: '{{ item.createTime }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # List channels.
  # ── gcp.eventarc.projects.locations.channels.list ──
  - discovery_id: gcp.eventarc.projects.locations.channels.list
    # python: svc.projects().locations().channels().list(**params).execute()
    calls:
      - action: projects.locations.channels.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.channels }}'
      item:
        activationToken: '{{ item.activationToken }}'
        createTime: '{{ item.createTime }}'
        cryptoKeyName: '{{ item.cryptoKeyName }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        provider: '{{ item.provider }}'
        pubsubTopic: '{{ item.pubsubTopic }}'
        satisfiesPzs: '{{ item.satisfiesPzs }}'
        state: '{{ item.state }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # List Enrollments.
  # ── gcp.eventarc.projects.locations.enrollments.list ──
  - discovery_id: gcp.eventarc.projects.locations.enrollments.list
    # python: svc.projects().locations().enrollments().list(**params).execute()
    calls:
      - action: projects.locations.enrollments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.enrollments }}'
      item:
        annotations: '{{ item.annotations }}'
        celMatch: '{{ item.celMatch }}'
        createTime: '{{ item.createTime }}'
        destination: '{{ item.destination }}'
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        messageBus: '{{ item.messageBus }}'
        name: '{{ item.name }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # Gets information about a location.
  # ── gcp.eventarc.projects.locations.get ──
  - discovery_id: gcp.eventarc.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Get a GoogleChannelConfig. The name of the GoogleChannelConfig in the response is ALWAYS coded with 
  # ── gcp.eventarc.projects.locations.getGoogleChannelConfig ──
  - discovery_id: gcp.eventarc.projects.locations.getGoogleChannelConfig
    # python: svc.projects().locations().getGoogleChannelConfig(**params).execute()
    calls:
      - action: projects.locations.getGoogleChannelConfig
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        cryptoKeyName: '{{ response.cryptoKeyName }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        updateTime: '{{ response.updateTime }}'
  # List GoogleApiSources.
  # ── gcp.eventarc.projects.locations.googleApiSources.list ──
  - discovery_id: gcp.eventarc.projects.locations.googleApiSources.list
    # python: svc.projects().locations().googleApiSources().list(**params).execute()
    calls:
      - action: projects.locations.googleApiSources.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.googleApiSources }}'
      item:
        annotations: '{{ item.annotations }}'
        createTime: '{{ item.createTime }}'
        cryptoKeyName: '{{ item.cryptoKeyName }}'
        destination: '{{ item.destination }}'
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        loggingConfig: '{{ item.loggingConfig }}'
        name: '{{ item.name }}'
        organizationSubscription: '{{ item.organizationSubscription }}'
        projectSubscriptions: '{{ item.projectSubscriptions }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.eventarc.projects.locations.list ──
  - discovery_id: gcp.eventarc.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # List message buses.
  # ── gcp.eventarc.projects.locations.messageBuses.list ──
  - discovery_id: gcp.eventarc.projects.locations.messageBuses.list
    # python: svc.projects().locations().messageBuses().list(**params).execute()
    calls:
      - action: projects.locations.messageBuses.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.messageBuses }}'
      item:
        annotations: '{{ item.annotations }}'
        createTime: '{{ item.createTime }}'
        cryptoKeyName: '{{ item.cryptoKeyName }}'
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        loggingConfig: '{{ item.loggingConfig }}'
        name: '{{ item.name }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # List message bus enrollments.
  # ── gcp.eventarc.projects.locations.messageBuses.listEnrollments ──
  - discovery_id: gcp.eventarc.projects.locations.messageBuses.listEnrollments
    # python: svc.projects().locations().messageBuses().listEnrollments(**params).execute()
    calls:
      - action: projects.locations.messageBuses.listEnrollments
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.messageBuses }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.eventarc.projects.locations.operations.list ──
  - discovery_id: gcp.eventarc.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # List pipelines.
  # ── gcp.eventarc.projects.locations.pipelines.list ──
  - discovery_id: gcp.eventarc.projects.locations.pipelines.list
    # python: svc.projects().locations().pipelines().list(**params).execute()
    calls:
      - action: projects.locations.pipelines.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.pipelines }}'
      item:
        annotations: '{{ item.annotations }}'
        createTime: '{{ item.createTime }}'
        cryptoKeyName: '{{ item.cryptoKeyName }}'
        destinations: '{{ item.destinations }}'
        displayName: '{{ item.displayName }}'
        inputPayloadFormat: '{{ item.inputPayloadFormat }}'
        labels: '{{ item.labels }}'
        loggingConfig: '{{ item.loggingConfig }}'
        mediations: '{{ item.mediations }}'
        name: '{{ item.name }}'
        retryPolicy: '{{ item.retryPolicy }}'
        satisfiesPzs: '{{ item.satisfiesPzs }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # List providers.
  # ── gcp.eventarc.projects.locations.providers.list ──
  - discovery_id: gcp.eventarc.projects.locations.providers.list
    # python: svc.projects().locations().providers().list(**params).execute()
    calls:
      - action: projects.locations.providers.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.providers }}'
      item:
        displayName: '{{ item.displayName }}'
        eventTypes: '{{ item.eventTypes }}'
        name: '{{ item.name }}'
  # List triggers.
  # ── gcp.eventarc.projects.locations.triggers.list ──
  - discovery_id: gcp.eventarc.projects.locations.triggers.list
    # python: svc.projects().locations().triggers().list(**params).execute()
    calls:
      - action: projects.locations.triggers.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.triggers }}'
      item:
        channel: '{{ item.channel }}'
        conditions: '{{ item.conditions }}'
        createTime: '{{ item.createTime }}'
        destination: '{{ item.destination }}'
        eventDataContentType: '{{ item.eventDataContentType }}'
        eventFilters: '{{ item.eventFilters }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        retryPolicy: '{{ item.retryPolicy }}'
        satisfiesPzs: '{{ item.satisfiesPzs }}'
        serviceAccount: '{{ item.serviceAccount }}'
        transport: '{{ item.transport }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'

  # ════ DEPENDENT (enrich) operations ════
  # Get a single ChannelConnection.
  # ── gcp.eventarc.projects.locations.channelConnections.get [dependent] ──
  - discovery_id: gcp.eventarc.projects.locations.channelConnections.get
    for_each: gcp.eventarc.projects.locations.channelConnections.list
    # python: svc.projects().locations().channelConnections().get(**params).execute()
    calls:
      - action: projects.locations.channelConnections.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        activationToken: '{{ response.activationToken }}'
        channel: '{{ response.channel }}'
        createTime: '{{ response.createTime }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.eventarc.projects.locations.channelConnections.getIamPolicy [dependent] ──
  - discovery_id: gcp.eventarc.projects.locations.channelConnections.getIamPolicy
    for_each: gcp.eventarc.projects.locations.channelConnections.list
    # python: svc.projects().locations().channelConnections().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.channelConnections.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Get a single Channel.
  # ── gcp.eventarc.projects.locations.channels.get [dependent] ──
  - discovery_id: gcp.eventarc.projects.locations.channels.get
    for_each: gcp.eventarc.projects.locations.channels.list
    # python: svc.projects().locations().channels().get(**params).execute()
    calls:
      - action: projects.locations.channels.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        activationToken: '{{ response.activationToken }}'
        createTime: '{{ response.createTime }}'
        cryptoKeyName: '{{ response.cryptoKeyName }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        provider: '{{ response.provider }}'
        pubsubTopic: '{{ response.pubsubTopic }}'
        satisfiesPzs: '{{ response.satisfiesPzs }}'
        state: '{{ response.state }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.eventarc.projects.locations.channels.getIamPolicy [dependent] ──
  - discovery_id: gcp.eventarc.projects.locations.channels.getIamPolicy
    for_each: gcp.eventarc.projects.locations.channels.list
    # python: svc.projects().locations().channels().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.channels.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Get a single Enrollment.
  # ── gcp.eventarc.projects.locations.enrollments.get [dependent] ──
  - discovery_id: gcp.eventarc.projects.locations.enrollments.get
    for_each: gcp.eventarc.projects.locations.enrollments.list
    # python: svc.projects().locations().enrollments().get(**params).execute()
    calls:
      - action: projects.locations.enrollments.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        celMatch: '{{ response.celMatch }}'
        createTime: '{{ response.createTime }}'
        destination: '{{ response.destination }}'
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        messageBus: '{{ response.messageBus }}'
        name: '{{ response.name }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.eventarc.projects.locations.enrollments.getIamPolicy [dependent] ──
  - discovery_id: gcp.eventarc.projects.locations.enrollments.getIamPolicy
    for_each: gcp.eventarc.projects.locations.enrollments.list
    # python: svc.projects().locations().enrollments().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.enrollments.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Get a single GoogleApiSource.
  # ── gcp.eventarc.projects.locations.googleApiSources.get [dependent] ──
  - discovery_id: gcp.eventarc.projects.locations.googleApiSources.get
    for_each: gcp.eventarc.projects.locations.googleApiSources.list
    # python: svc.projects().locations().googleApiSources().get(**params).execute()
    calls:
      - action: projects.locations.googleApiSources.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        createTime: '{{ response.createTime }}'
        cryptoKeyName: '{{ response.cryptoKeyName }}'
        destination: '{{ response.destination }}'
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        loggingConfig: '{{ response.loggingConfig }}'
        name: '{{ response.name }}'
        organizationSubscription: '{{ response.organizationSubscription }}'
        projectSubscriptions: '{{ response.projectSubscriptions }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.eventarc.projects.locations.googleApiSources.getIamPolicy [dependent] ──
  - discovery_id: gcp.eventarc.projects.locations.googleApiSources.getIamPolicy
    for_each: gcp.eventarc.projects.locations.googleApiSources.list
    # python: svc.projects().locations().googleApiSources().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.googleApiSources.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Get a single MessageBus.
  # ── gcp.eventarc.projects.locations.messageBuses.get [dependent] ──
  - discovery_id: gcp.eventarc.projects.locations.messageBuses.get
    for_each: gcp.eventarc.projects.locations.messageBuses.list
    # python: svc.projects().locations().messageBuses().get(**params).execute()
    calls:
      - action: projects.locations.messageBuses.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        createTime: '{{ response.createTime }}'
        cryptoKeyName: '{{ response.cryptoKeyName }}'
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        loggingConfig: '{{ response.loggingConfig }}'
        name: '{{ response.name }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.eventarc.projects.locations.messageBuses.getIamPolicy [dependent] ──
  - discovery_id: gcp.eventarc.projects.locations.messageBuses.getIamPolicy
    for_each: gcp.eventarc.projects.locations.messageBuses.list
    # python: svc.projects().locations().messageBuses().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.messageBuses.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.eventarc.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.eventarc.projects.locations.operations.get
    for_each: gcp.eventarc.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Get a single Pipeline.
  # ── gcp.eventarc.projects.locations.pipelines.get [dependent] ──
  - discovery_id: gcp.eventarc.projects.locations.pipelines.get
    for_each: gcp.eventarc.projects.locations.pipelines.list
    # python: svc.projects().locations().pipelines().get(**params).execute()
    calls:
      - action: projects.locations.pipelines.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        createTime: '{{ response.createTime }}'
        cryptoKeyName: '{{ response.cryptoKeyName }}'
        destinations: '{{ response.destinations }}'
        displayName: '{{ response.displayName }}'
        inputPayloadFormat: '{{ response.inputPayloadFormat }}'
        labels: '{{ response.labels }}'
        loggingConfig: '{{ response.loggingConfig }}'
        mediations: '{{ response.mediations }}'
        name: '{{ response.name }}'
        retryPolicy: '{{ response.retryPolicy }}'
        satisfiesPzs: '{{ response.satisfiesPzs }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.eventarc.projects.locations.pipelines.getIamPolicy [dependent] ──
  - discovery_id: gcp.eventarc.projects.locations.pipelines.getIamPolicy
    for_each: gcp.eventarc.projects.locations.pipelines.list
    # python: svc.projects().locations().pipelines().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.pipelines.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Get a single Provider.
  # ── gcp.eventarc.projects.locations.providers.get [dependent] ──
  - discovery_id: gcp.eventarc.projects.locations.providers.get
    for_each: gcp.eventarc.projects.locations.providers.list
    # python: svc.projects().locations().providers().get(**params).execute()
    calls:
      - action: projects.locations.providers.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        eventTypes: '{{ response.eventTypes }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Get a single trigger.
  # ── gcp.eventarc.projects.locations.triggers.get [dependent] ──
  - discovery_id: gcp.eventarc.projects.locations.triggers.get
    for_each: gcp.eventarc.projects.locations.triggers.list
    # python: svc.projects().locations().triggers().get(**params).execute()
    calls:
      - action: projects.locations.triggers.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        channel: '{{ response.channel }}'
        conditions: '{{ response.conditions }}'
        createTime: '{{ response.createTime }}'
        destination: '{{ response.destination }}'
        eventDataContentType: '{{ response.eventDataContentType }}'
        eventFilters: '{{ response.eventFilters }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        retryPolicy: '{{ response.retryPolicy }}'
        satisfiesPzs: '{{ response.satisfiesPzs }}'
        serviceAccount: '{{ response.serviceAccount }}'
        transport: '{{ response.transport }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.eventarc.projects.locations.triggers.getIamPolicy [dependent] ──
  - discovery_id: gcp.eventarc.projects.locations.triggers.getIamPolicy
    for_each: gcp.eventarc.projects.locations.triggers.list
    # python: svc.projects().locations().triggers().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.triggers.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
