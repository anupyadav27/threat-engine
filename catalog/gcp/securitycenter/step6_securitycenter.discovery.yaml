# ============================================================
# Discovery YAML — securitycenter (v1)
# Generated: 2026-02-21T03:06:41Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 75 | independent: 46 | dependent: 29
# ops with fields: 75/75 | dep ops with chain: 28/29
# ============================================================
version: '1.0'
provider: gcp
service: securitycenter

services:
  client: securitycenter
  module: "googleapiclient.discovery.build('securitycenter', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists an organization's assets.
  # ── gcp.securitycenter.folders.assets.list ──
  - discovery_id: gcp.securitycenter.folders.assets.list
    # python: svc.folders().assets().list(**params).execute()
    calls:
      - action: folders.assets.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.assets }}'
      item:
        asset: '{{ item.asset }}'
        stateChange: '{{ item.stateChange }}'
  # Lists BigQuery exports. Note that when requesting BigQuery exports at a given level all exports unde
  # ── gcp.securitycenter.folders.bigQueryExports.list ──
  - discovery_id: gcp.securitycenter.folders.bigQueryExports.list
    # python: svc.folders().bigQueryExports().list(**params).execute()
    calls:
      - action: folders.bigQueryExports.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.bigQueryExports }}'
      item:
        createTime: '{{ item.createTime }}'
        dataset: '{{ item.dataset }}'
        description: '{{ item.description }}'
        filter: '{{ item.filter }}'
        mostRecentEditor: '{{ item.mostRecentEditor }}'
        name: '{{ item.name }}'
        principal: '{{ item.principal }}'
        updateTime: '{{ item.updateTime }}'
  # Lists all Event Threat Detection custom modules for the given Resource Manager parent. This includes
  # ── gcp.securitycenter.folders.eventThreatDetectionSettings.customModules.list ──
  - discovery_id: gcp.securitycenter.folders.eventThreatDetectionSettings.customModules.list
    # python: svc.folders().eventThreatDetectionSettings().customModules().list(**params).execute()
    calls:
      - action: folders.eventThreatDetectionSettings.customModules.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.customModules }}'
      item:
        ancestorModule: '{{ item.ancestorModule }}'
        cloudProvider: '{{ item.cloudProvider }}'
        config: '{{ item.config }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        enablementState: '{{ item.enablementState }}'
        lastEditor: '{{ item.lastEditor }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
        updateTime: '{{ item.updateTime }}'
  # Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and
  # ── gcp.securitycenter.folders.eventThreatDetectionSettings.customModules.listDescendant ──
  - discovery_id: gcp.securitycenter.folders.eventThreatDetectionSettings.customModules.listDescendant
    # python: svc.folders().eventThreatDetectionSettings().customModules().listDescendant(**params).execute()
    calls:
      - action: folders.eventThreatDetectionSettings.customModules.listDescendant
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.customModules }}'
      item:
        ancestorModule: '{{ item.ancestorModule }}'
        cloudProvider: '{{ item.cloudProvider }}'
        config: '{{ item.config }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        enablementState: '{{ item.enablementState }}'
        lastEditor: '{{ item.lastEditor }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
        updateTime: '{{ item.updateTime }}'
  # Lists all effective Event Threat Detection custom modules for the given parent. This includes reside
  # ── gcp.securitycenter.folders.eventThreatDetectionSettings.effectiveCustomModules.list ──
  - discovery_id: gcp.securitycenter.folders.eventThreatDetectionSettings.effectiveCustomModules.list
    # python: svc.folders().eventThreatDetectionSettings().effectiveCustomModules().list(**params).execute()
    calls:
      - action: folders.eventThreatDetectionSettings.effectiveCustomModules.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.effectiveCustomModules }}'
      item:
        cloudProvider: '{{ item.cloudProvider }}'
        config: '{{ item.config }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        enablementState: '{{ item.enablementState }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
  # Lists mute configs.
  # ── gcp.securitycenter.folders.muteConfigs.list ──
  - discovery_id: gcp.securitycenter.folders.muteConfigs.list
    # python: svc.folders().muteConfigs().list(**params).execute()
    calls:
      - action: folders.muteConfigs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.muteConfigs }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        expiryTime: '{{ item.expiryTime }}'
        filter: '{{ item.filter }}'
        mostRecentEditor: '{{ item.mostRecentEditor }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
        updateTime: '{{ item.updateTime }}'
  # Lists notification configs.
  # ── gcp.securitycenter.folders.notificationConfigs.list ──
  - discovery_id: gcp.securitycenter.folders.notificationConfigs.list
    # python: svc.folders().notificationConfigs().list(**params).execute()
    calls:
      - action: folders.notificationConfigs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.notificationConfigs }}'
      item:
        description: '{{ item.description }}'
        name: '{{ item.name }}'
        pubsubTopic: '{{ item.pubsubTopic }}'
        serviceAccount: '{{ item.serviceAccount }}'
        streamingConfig: '{{ item.streamingConfig }}'
  # Returns a list of all SecurityHealthAnalyticsCustomModules for the given parent. This includes resid
  # ── gcp.securitycenter.folders.securityHealthAnalyticsSettings.customModules.list ──
  - discovery_id: gcp.securitycenter.folders.securityHealthAnalyticsSettings.customModules.list
    # python: svc.folders().securityHealthAnalyticsSettings().customModules().list(**params).execute()
    calls:
      - action: folders.securityHealthAnalyticsSettings.customModules.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.customModules }}'
      item:
        ancestorModule: '{{ item.ancestorModule }}'
        cloudProvider: '{{ item.cloudProvider }}'
        customConfig: '{{ item.customConfig }}'
        displayName: '{{ item.displayName }}'
        enablementState: '{{ item.enablementState }}'
        lastEditor: '{{ item.lastEditor }}'
        name: '{{ item.name }}'
        updateTime: '{{ item.updateTime }}'
  # Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and a
  # ── gcp.securitycenter.folders.securityHealthAnalyticsSettings.customModules.listDescendant ──
  - discovery_id: gcp.securitycenter.folders.securityHealthAnalyticsSettings.customModules.listDescendant
    # python: svc.folders().securityHealthAnalyticsSettings().customModules().listDescendant(**params).execute()
    calls:
      - action: folders.securityHealthAnalyticsSettings.customModules.listDescendant
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.customModules }}'
      item:
        ancestorModule: '{{ item.ancestorModule }}'
        cloudProvider: '{{ item.cloudProvider }}'
        customConfig: '{{ item.customConfig }}'
        displayName: '{{ item.displayName }}'
        enablementState: '{{ item.enablementState }}'
        lastEditor: '{{ item.lastEditor }}'
        name: '{{ item.name }}'
        updateTime: '{{ item.updateTime }}'
  # Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This inclu
  # ── gcp.securitycenter.folders.securityHealthAnalyticsSettings.effectiveCustomModules.list ──
  - discovery_id: gcp.securitycenter.folders.securityHealthAnalyticsSettings.effectiveCustomModules.list
    # python: svc.folders().securityHealthAnalyticsSettings().effectiveCustomModules().list(**params).execute()
    calls:
      - action: folders.securityHealthAnalyticsSettings.effectiveCustomModules.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.effectiveCustomModules }}'
      item:
        cloudProvider: '{{ item.cloudProvider }}'
        customConfig: '{{ item.customConfig }}'
        displayName: '{{ item.displayName }}'
        enablementState: '{{ item.enablementState }}'
        name: '{{ item.name }}'
  # Lists an organization or source's findings. To list across all sources provide a `-` as the source i
  # ── gcp.securitycenter.folders.sources.findings.list ──
  - discovery_id: gcp.securitycenter.folders.sources.findings.list
    # python: svc.folders().sources().findings().list(**params).execute()
    calls:
      - action: folders.sources.findings.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.findings }}'
      item:
        finding: '{{ item.finding }}'
        resource: '{{ item.resource }}'
        stateChange: '{{ item.stateChange }}'
  # Lists all sources belonging to an organization.
  # ── gcp.securitycenter.folders.sources.list ──
  - discovery_id: gcp.securitycenter.folders.sources.list
    # python: svc.folders().sources().list(**params).execute()
    calls:
      - action: folders.sources.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.sources }}'
      item:
        canonicalName: '{{ item.canonicalName }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        name: '{{ item.name }}'
  # Lists an organization's assets.
  # ── gcp.securitycenter.organizations.assets.list ──
  - discovery_id: gcp.securitycenter.organizations.assets.list
    # python: svc.organizations().assets().list(**params).execute()
    calls:
      - action: organizations.assets.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.assets }}'
      item:
        asset: '{{ item.asset }}'
        stateChange: '{{ item.stateChange }}'
  # Lists the attack paths for a set of simulation results or valued resources and filter.
  # ── gcp.securitycenter.organizations.attackPaths.list ──
  - discovery_id: gcp.securitycenter.organizations.attackPaths.list
    # python: svc.organizations().attackPaths().list(**params).execute()
    calls:
      - action: organizations.attackPaths.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.attackPaths }}'
      item:
        edges: '{{ item.edges }}'
        name: '{{ item.name }}'
        pathNodes: '{{ item.pathNodes }}'
  # Lists BigQuery exports. Note that when requesting BigQuery exports at a given level all exports unde
  # ── gcp.securitycenter.organizations.bigQueryExports.list ──
  - discovery_id: gcp.securitycenter.organizations.bigQueryExports.list
    # python: svc.organizations().bigQueryExports().list(**params).execute()
    calls:
      - action: organizations.bigQueryExports.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.bigQueryExports }}'
      item:
        createTime: '{{ item.createTime }}'
        dataset: '{{ item.dataset }}'
        description: '{{ item.description }}'
        filter: '{{ item.filter }}'
        mostRecentEditor: '{{ item.mostRecentEditor }}'
        name: '{{ item.name }}'
        principal: '{{ item.principal }}'
        updateTime: '{{ item.updateTime }}'
  # Lists all Event Threat Detection custom modules for the given Resource Manager parent. This includes
  # ── gcp.securitycenter.organizations.eventThreatDetectionSettings.customModules.list ──
  - discovery_id: gcp.securitycenter.organizations.eventThreatDetectionSettings.customModules.list
    # python: svc.organizations().eventThreatDetectionSettings().customModules().list(**params).execute()
    calls:
      - action: organizations.eventThreatDetectionSettings.customModules.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.customModules }}'
      item:
        ancestorModule: '{{ item.ancestorModule }}'
        cloudProvider: '{{ item.cloudProvider }}'
        config: '{{ item.config }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        enablementState: '{{ item.enablementState }}'
        lastEditor: '{{ item.lastEditor }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
        updateTime: '{{ item.updateTime }}'
  # Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and
  # ── gcp.securitycenter.organizations.eventThreatDetectionSettings.customModules.listDescendant ──
  - discovery_id: gcp.securitycenter.organizations.eventThreatDetectionSettings.customModules.listDescendant
    # python: svc.organizations().eventThreatDetectionSettings().customModules().listDescendant(**params).execute()
    calls:
      - action: organizations.eventThreatDetectionSettings.customModules.listDescendant
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.customModules }}'
      item:
        ancestorModule: '{{ item.ancestorModule }}'
        cloudProvider: '{{ item.cloudProvider }}'
        config: '{{ item.config }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        enablementState: '{{ item.enablementState }}'
        lastEditor: '{{ item.lastEditor }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
        updateTime: '{{ item.updateTime }}'
  # Lists all effective Event Threat Detection custom modules for the given parent. This includes reside
  # ── gcp.securitycenter.organizations.eventThreatDetectionSettings.effectiveCustomModules.list ──
  - discovery_id: gcp.securitycenter.organizations.eventThreatDetectionSettings.effectiveCustomModules.list
    # python: svc.organizations().eventThreatDetectionSettings().effectiveCustomModules().list(**params).execute()
    calls:
      - action: organizations.eventThreatDetectionSettings.effectiveCustomModules.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.effectiveCustomModules }}'
      item:
        cloudProvider: '{{ item.cloudProvider }}'
        config: '{{ item.config }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        enablementState: '{{ item.enablementState }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
  # Gets the settings for an organization.
  # ── gcp.securitycenter.organizations.getOrganizationSettings ──
  - discovery_id: gcp.securitycenter.organizations.getOrganizationSettings
    # python: svc.organizations().getOrganizationSettings(**params).execute()
    calls:
      - action: organizations.getOrganizationSettings
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        assetDiscoveryConfig: '{{ response.assetDiscoveryConfig }}'
        enableAssetDiscovery: '{{ response.enableAssetDiscovery }}'
        name: '{{ response.name }}'
  # Lists mute configs.
  # ── gcp.securitycenter.organizations.muteConfigs.list ──
  - discovery_id: gcp.securitycenter.organizations.muteConfigs.list
    # python: svc.organizations().muteConfigs().list(**params).execute()
    calls:
      - action: organizations.muteConfigs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.muteConfigs }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        expiryTime: '{{ item.expiryTime }}'
        filter: '{{ item.filter }}'
        mostRecentEditor: '{{ item.mostRecentEditor }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
        updateTime: '{{ item.updateTime }}'
  # Lists notification configs.
  # ── gcp.securitycenter.organizations.notificationConfigs.list ──
  - discovery_id: gcp.securitycenter.organizations.notificationConfigs.list
    # python: svc.organizations().notificationConfigs().list(**params).execute()
    calls:
      - action: organizations.notificationConfigs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.notificationConfigs }}'
      item:
        description: '{{ item.description }}'
        name: '{{ item.name }}'
        pubsubTopic: '{{ item.pubsubTopic }}'
        serviceAccount: '{{ item.serviceAccount }}'
        streamingConfig: '{{ item.streamingConfig }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.securitycenter.organizations.operations.list ──
  - discovery_id: gcp.securitycenter.organizations.operations.list
    # python: svc.organizations().operations().list(**params).execute()
    calls:
      - action: organizations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists all ResourceValueConfigs.
  # ── gcp.securitycenter.organizations.resourceValueConfigs.list ──
  - discovery_id: gcp.securitycenter.organizations.resourceValueConfigs.list
    # python: svc.organizations().resourceValueConfigs().list(**params).execute()
    calls:
      - action: organizations.resourceValueConfigs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.resourceValueConfigs }}'
      item:
        cloudProvider: '{{ item.cloudProvider }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        name: '{{ item.name }}'
        resourceLabelsSelector: '{{ item.resourceLabelsSelector }}'
        resourceType: '{{ item.resourceType }}'
        resourceValue: '{{ item.resourceValue }}'
        scope: '{{ item.scope }}'
        sensitiveDataProtectionMapping: '{{ item.sensitiveDataProtectionMapping }}'
        tagValues: '{{ item.tagValues }}'
        updateTime: '{{ item.updateTime }}'
  # Returns a list of all SecurityHealthAnalyticsCustomModules for the given parent. This includes resid
  # ── gcp.securitycenter.organizations.securityHealthAnalyticsSettings.customModules.list ──
  - discovery_id: gcp.securitycenter.organizations.securityHealthAnalyticsSettings.customModules.list
    # python: svc.organizations().securityHealthAnalyticsSettings().customModules().list(**params).execute()
    calls:
      - action: organizations.securityHealthAnalyticsSettings.customModules.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.customModules }}'
      item:
        ancestorModule: '{{ item.ancestorModule }}'
        cloudProvider: '{{ item.cloudProvider }}'
        customConfig: '{{ item.customConfig }}'
        displayName: '{{ item.displayName }}'
        enablementState: '{{ item.enablementState }}'
        lastEditor: '{{ item.lastEditor }}'
        name: '{{ item.name }}'
        updateTime: '{{ item.updateTime }}'
  # Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and a
  # ── gcp.securitycenter.organizations.securityHealthAnalyticsSettings.customModules.listDescendant ──
  - discovery_id: gcp.securitycenter.organizations.securityHealthAnalyticsSettings.customModules.listDescendant
    # python: svc.organizations().securityHealthAnalyticsSettings().customModules().listDescendant(**params).execute()
    calls:
      - action: organizations.securityHealthAnalyticsSettings.customModules.listDescendant
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.customModules }}'
      item:
        ancestorModule: '{{ item.ancestorModule }}'
        cloudProvider: '{{ item.cloudProvider }}'
        customConfig: '{{ item.customConfig }}'
        displayName: '{{ item.displayName }}'
        enablementState: '{{ item.enablementState }}'
        lastEditor: '{{ item.lastEditor }}'
        name: '{{ item.name }}'
        updateTime: '{{ item.updateTime }}'
  # Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This inclu
  # ── gcp.securitycenter.organizations.securityHealthAnalyticsSettings.effectiveCustomModules.list ──
  - discovery_id: gcp.securitycenter.organizations.securityHealthAnalyticsSettings.effectiveCustomModules.list
    # python: svc.organizations().securityHealthAnalyticsSettings().effectiveCustomModules().list(**params).execute()
    calls:
      - action: organizations.securityHealthAnalyticsSettings.effectiveCustomModules.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.effectiveCustomModules }}'
      item:
        cloudProvider: '{{ item.cloudProvider }}'
        customConfig: '{{ item.customConfig }}'
        displayName: '{{ item.displayName }}'
        enablementState: '{{ item.enablementState }}'
        name: '{{ item.name }}'
  # Lists the attack paths for a set of simulation results or valued resources and filter.
  # ── gcp.securitycenter.organizations.simulations.attackExposureResults.attackPaths.list ──
  - discovery_id: gcp.securitycenter.organizations.simulations.attackExposureResults.attackPaths.list
    # python: svc.organizations().simulations().attackExposureResults().attackPaths().list(**params).execute()
    calls:
      - action: organizations.simulations.attackExposureResults.attackPaths.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.attackPaths }}'
      item:
        edges: '{{ item.edges }}'
        name: '{{ item.name }}'
        pathNodes: '{{ item.pathNodes }}'
  # Lists the valued resources for a set of simulation results and filter.
  # ── gcp.securitycenter.organizations.simulations.attackExposureResults.valuedResources.list ──
  - discovery_id: gcp.securitycenter.organizations.simulations.attackExposureResults.valuedResources.list
    # python: svc.organizations().simulations().attackExposureResults().valuedResources().list(**params).execute()
    calls:
      - action: organizations.simulations.attackExposureResults.valuedResources.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.valuedResources }}'
      item:
        displayName: '{{ item.displayName }}'
        exposedScore: '{{ item.exposedScore }}'
        name: '{{ item.name }}'
        resource: '{{ item.resource }}'
        resourceType: '{{ item.resourceType }}'
        resourceValue: '{{ item.resourceValue }}'
        resourceValueConfigsUsed: '{{ item.resourceValueConfigsUsed }}'
  # Lists the attack paths for a set of simulation results or valued resources and filter.
  # ── gcp.securitycenter.organizations.simulations.attackPaths.list ──
  - discovery_id: gcp.securitycenter.organizations.simulations.attackPaths.list
    # python: svc.organizations().simulations().attackPaths().list(**params).execute()
    calls:
      - action: organizations.simulations.attackPaths.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.attackPaths }}'
      item:
        edges: '{{ item.edges }}'
        name: '{{ item.name }}'
        pathNodes: '{{ item.pathNodes }}'
  # Lists the attack paths for a set of simulation results or valued resources and filter.
  # ── gcp.securitycenter.organizations.simulations.valuedResources.attackPaths.list ──
  - discovery_id: gcp.securitycenter.organizations.simulations.valuedResources.attackPaths.list
    # python: svc.organizations().simulations().valuedResources().attackPaths().list(**params).execute()
    calls:
      - action: organizations.simulations.valuedResources.attackPaths.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.attackPaths }}'
      item:
        edges: '{{ item.edges }}'
        name: '{{ item.name }}'
        pathNodes: '{{ item.pathNodes }}'
  # Lists the valued resources for a set of simulation results and filter.
  # ── gcp.securitycenter.organizations.simulations.valuedResources.list ──
  - discovery_id: gcp.securitycenter.organizations.simulations.valuedResources.list
    # python: svc.organizations().simulations().valuedResources().list(**params).execute()
    calls:
      - action: organizations.simulations.valuedResources.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.valuedResources }}'
      item:
        displayName: '{{ item.displayName }}'
        exposedScore: '{{ item.exposedScore }}'
        name: '{{ item.name }}'
        resource: '{{ item.resource }}'
        resourceType: '{{ item.resourceType }}'
        resourceValue: '{{ item.resourceValue }}'
        resourceValueConfigsUsed: '{{ item.resourceValueConfigsUsed }}'
  # Lists an organization or source's findings. To list across all sources provide a `-` as the source i
  # ── gcp.securitycenter.organizations.sources.findings.list ──
  - discovery_id: gcp.securitycenter.organizations.sources.findings.list
    # python: svc.organizations().sources().findings().list(**params).execute()
    calls:
      - action: organizations.sources.findings.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.findings }}'
      item:
        finding: '{{ item.finding }}'
        resource: '{{ item.resource }}'
        stateChange: '{{ item.stateChange }}'
  # Lists all sources belonging to an organization.
  # ── gcp.securitycenter.organizations.sources.list ──
  - discovery_id: gcp.securitycenter.organizations.sources.list
    # python: svc.organizations().sources().list(**params).execute()
    calls:
      - action: organizations.sources.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.sources }}'
      item:
        canonicalName: '{{ item.canonicalName }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        name: '{{ item.name }}'
  # Lists the valued resources for a set of simulation results and filter.
  # ── gcp.securitycenter.organizations.valuedResources.list ──
  - discovery_id: gcp.securitycenter.organizations.valuedResources.list
    # python: svc.organizations().valuedResources().list(**params).execute()
    calls:
      - action: organizations.valuedResources.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.valuedResources }}'
      item:
        displayName: '{{ item.displayName }}'
        exposedScore: '{{ item.exposedScore }}'
        name: '{{ item.name }}'
        resource: '{{ item.resource }}'
        resourceType: '{{ item.resourceType }}'
        resourceValue: '{{ item.resourceValue }}'
        resourceValueConfigsUsed: '{{ item.resourceValueConfigsUsed }}'
  # Lists an organization's assets.
  # ── gcp.securitycenter.projects.assets.list ──
  - discovery_id: gcp.securitycenter.projects.assets.list
    # python: svc.projects().assets().list(**params).execute()
    calls:
      - action: projects.assets.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.assets }}'
      item:
        asset: '{{ item.asset }}'
        stateChange: '{{ item.stateChange }}'
  # Lists BigQuery exports. Note that when requesting BigQuery exports at a given level all exports unde
  # ── gcp.securitycenter.projects.bigQueryExports.list ──
  - discovery_id: gcp.securitycenter.projects.bigQueryExports.list
    # python: svc.projects().bigQueryExports().list(**params).execute()
    calls:
      - action: projects.bigQueryExports.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.bigQueryExports }}'
      item:
        createTime: '{{ item.createTime }}'
        dataset: '{{ item.dataset }}'
        description: '{{ item.description }}'
        filter: '{{ item.filter }}'
        mostRecentEditor: '{{ item.mostRecentEditor }}'
        name: '{{ item.name }}'
        principal: '{{ item.principal }}'
        updateTime: '{{ item.updateTime }}'
  # Lists all Event Threat Detection custom modules for the given Resource Manager parent. This includes
  # ── gcp.securitycenter.projects.eventThreatDetectionSettings.customModules.list ──
  - discovery_id: gcp.securitycenter.projects.eventThreatDetectionSettings.customModules.list
    # python: svc.projects().eventThreatDetectionSettings().customModules().list(**params).execute()
    calls:
      - action: projects.eventThreatDetectionSettings.customModules.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.customModules }}'
      item:
        ancestorModule: '{{ item.ancestorModule }}'
        cloudProvider: '{{ item.cloudProvider }}'
        config: '{{ item.config }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        enablementState: '{{ item.enablementState }}'
        lastEditor: '{{ item.lastEditor }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
        updateTime: '{{ item.updateTime }}'
  # Lists all resident Event Threat Detection custom modules under the given Resource Manager parent and
  # ── gcp.securitycenter.projects.eventThreatDetectionSettings.customModules.listDescendant ──
  - discovery_id: gcp.securitycenter.projects.eventThreatDetectionSettings.customModules.listDescendant
    # python: svc.projects().eventThreatDetectionSettings().customModules().listDescendant(**params).execute()
    calls:
      - action: projects.eventThreatDetectionSettings.customModules.listDescendant
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.customModules }}'
      item:
        ancestorModule: '{{ item.ancestorModule }}'
        cloudProvider: '{{ item.cloudProvider }}'
        config: '{{ item.config }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        enablementState: '{{ item.enablementState }}'
        lastEditor: '{{ item.lastEditor }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
        updateTime: '{{ item.updateTime }}'
  # Lists all effective Event Threat Detection custom modules for the given parent. This includes reside
  # ── gcp.securitycenter.projects.eventThreatDetectionSettings.effectiveCustomModules.list ──
  - discovery_id: gcp.securitycenter.projects.eventThreatDetectionSettings.effectiveCustomModules.list
    # python: svc.projects().eventThreatDetectionSettings().effectiveCustomModules().list(**params).execute()
    calls:
      - action: projects.eventThreatDetectionSettings.effectiveCustomModules.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.effectiveCustomModules }}'
      item:
        cloudProvider: '{{ item.cloudProvider }}'
        config: '{{ item.config }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        enablementState: '{{ item.enablementState }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
  # Lists mute configs.
  # ── gcp.securitycenter.projects.muteConfigs.list ──
  - discovery_id: gcp.securitycenter.projects.muteConfigs.list
    # python: svc.projects().muteConfigs().list(**params).execute()
    calls:
      - action: projects.muteConfigs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.muteConfigs }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        expiryTime: '{{ item.expiryTime }}'
        filter: '{{ item.filter }}'
        mostRecentEditor: '{{ item.mostRecentEditor }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
        updateTime: '{{ item.updateTime }}'
  # Lists notification configs.
  # ── gcp.securitycenter.projects.notificationConfigs.list ──
  - discovery_id: gcp.securitycenter.projects.notificationConfigs.list
    # python: svc.projects().notificationConfigs().list(**params).execute()
    calls:
      - action: projects.notificationConfigs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.notificationConfigs }}'
      item:
        description: '{{ item.description }}'
        name: '{{ item.name }}'
        pubsubTopic: '{{ item.pubsubTopic }}'
        serviceAccount: '{{ item.serviceAccount }}'
        streamingConfig: '{{ item.streamingConfig }}'
  # Returns a list of all SecurityHealthAnalyticsCustomModules for the given parent. This includes resid
  # ── gcp.securitycenter.projects.securityHealthAnalyticsSettings.customModules.list ──
  - discovery_id: gcp.securitycenter.projects.securityHealthAnalyticsSettings.customModules.list
    # python: svc.projects().securityHealthAnalyticsSettings().customModules().list(**params).execute()
    calls:
      - action: projects.securityHealthAnalyticsSettings.customModules.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.customModules }}'
      item:
        ancestorModule: '{{ item.ancestorModule }}'
        cloudProvider: '{{ item.cloudProvider }}'
        customConfig: '{{ item.customConfig }}'
        displayName: '{{ item.displayName }}'
        enablementState: '{{ item.enablementState }}'
        lastEditor: '{{ item.lastEditor }}'
        name: '{{ item.name }}'
        updateTime: '{{ item.updateTime }}'
  # Returns a list of all resident SecurityHealthAnalyticsCustomModules under the given CRM parent and a
  # ── gcp.securitycenter.projects.securityHealthAnalyticsSettings.customModules.listDescendant ──
  - discovery_id: gcp.securitycenter.projects.securityHealthAnalyticsSettings.customModules.listDescendant
    # python: svc.projects().securityHealthAnalyticsSettings().customModules().listDescendant(**params).execute()
    calls:
      - action: projects.securityHealthAnalyticsSettings.customModules.listDescendant
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.customModules }}'
      item:
        ancestorModule: '{{ item.ancestorModule }}'
        cloudProvider: '{{ item.cloudProvider }}'
        customConfig: '{{ item.customConfig }}'
        displayName: '{{ item.displayName }}'
        enablementState: '{{ item.enablementState }}'
        lastEditor: '{{ item.lastEditor }}'
        name: '{{ item.name }}'
        updateTime: '{{ item.updateTime }}'
  # Returns a list of all EffectiveSecurityHealthAnalyticsCustomModules for the given parent. This inclu
  # ── gcp.securitycenter.projects.securityHealthAnalyticsSettings.effectiveCustomModules.list ──
  - discovery_id: gcp.securitycenter.projects.securityHealthAnalyticsSettings.effectiveCustomModules.list
    # python: svc.projects().securityHealthAnalyticsSettings().effectiveCustomModules().list(**params).execute()
    calls:
      - action: projects.securityHealthAnalyticsSettings.effectiveCustomModules.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.effectiveCustomModules }}'
      item:
        cloudProvider: '{{ item.cloudProvider }}'
        customConfig: '{{ item.customConfig }}'
        displayName: '{{ item.displayName }}'
        enablementState: '{{ item.enablementState }}'
        name: '{{ item.name }}'
  # Lists an organization or source's findings. To list across all sources provide a `-` as the source i
  # ── gcp.securitycenter.projects.sources.findings.list ──
  - discovery_id: gcp.securitycenter.projects.sources.findings.list
    # python: svc.projects().sources().findings().list(**params).execute()
    calls:
      - action: projects.sources.findings.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.findings }}'
      item:
        finding: '{{ item.finding }}'
        resource: '{{ item.resource }}'
        stateChange: '{{ item.stateChange }}'
  # Lists all sources belonging to an organization.
  # ── gcp.securitycenter.projects.sources.list ──
  - discovery_id: gcp.securitycenter.projects.sources.list
    # python: svc.projects().sources().list(**params).execute()
    calls:
      - action: projects.sources.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.sources }}'
      item:
        canonicalName: '{{ item.canonicalName }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        name: '{{ item.name }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets a BigQuery export.
  # ── gcp.securitycenter.folders.bigQueryExports.get [dependent] ──
  - discovery_id: gcp.securitycenter.folders.bigQueryExports.get
    for_each: gcp.securitycenter.folders.bigQueryExports.list
    # python: svc.folders().bigQueryExports().get(**params).execute()
    calls:
      - action: folders.bigQueryExports.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        dataset: '{{ response.dataset }}'
        description: '{{ response.description }}'
        filter: '{{ response.filter }}'
        mostRecentEditor: '{{ response.mostRecentEditor }}'
        name: '{{ response.name }}'
        principal: '{{ response.principal }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets an Event Threat Detection custom module.
  # ── gcp.securitycenter.folders.eventThreatDetectionSettings.customModules.get [dependent] ──
  - discovery_id: gcp.securitycenter.folders.eventThreatDetectionSettings.customModules.get
    for_each: gcp.securitycenter.folders.securityHealthAnalyticsSettings.customModules.list
    # python: svc.folders().eventThreatDetectionSettings().customModules().get(**params).execute()
    calls:
      - action: folders.eventThreatDetectionSettings.customModules.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        ancestorModule: '{{ response.ancestorModule }}'
        cloudProvider: '{{ response.cloudProvider }}'
        config: '{{ response.config }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        enablementState: '{{ response.enablementState }}'
        lastEditor: '{{ response.lastEditor }}'
        name: '{{ response.name }}'
        type: '{{ response.type }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets an effective Event Threat Detection custom module at the given level.
  # ── gcp.securitycenter.folders.eventThreatDetectionSettings.effectiveCustomModules.get [dependent] ──
  - discovery_id: gcp.securitycenter.folders.eventThreatDetectionSettings.effectiveCustomModules.get
    for_each: gcp.securitycenter.folders.securityHealthAnalyticsSettings.effectiveCustomModules.list
    # python: svc.folders().eventThreatDetectionSettings().effectiveCustomModules().get(**params).execute()
    calls:
      - action: folders.eventThreatDetectionSettings.effectiveCustomModules.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        cloudProvider: '{{ response.cloudProvider }}'
        config: '{{ response.config }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        enablementState: '{{ response.enablementState }}'
        name: '{{ response.name }}'
        type: '{{ response.type }}'
    # required_params: ['name']
  # Gets a mute config.
  # ── gcp.securitycenter.folders.locations.muteConfigs.get [dependent] ──
  - discovery_id: gcp.securitycenter.folders.locations.muteConfigs.get
    for_each: gcp.securitycenter.folders.muteConfigs.list
    # python: svc.folders().locations().muteConfigs().get(**params).execute()
    calls:
      - action: folders.locations.muteConfigs.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        expiryTime: '{{ response.expiryTime }}'
        filter: '{{ response.filter }}'
        mostRecentEditor: '{{ response.mostRecentEditor }}'
        name: '{{ response.name }}'
        type: '{{ response.type }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets a mute config.
  # ── gcp.securitycenter.folders.muteConfigs.get [dependent] ──
  - discovery_id: gcp.securitycenter.folders.muteConfigs.get
    for_each: gcp.securitycenter.folders.muteConfigs.list
    # python: svc.folders().muteConfigs().get(**params).execute()
    calls:
      - action: folders.muteConfigs.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        expiryTime: '{{ response.expiryTime }}'
        filter: '{{ response.filter }}'
        mostRecentEditor: '{{ response.mostRecentEditor }}'
        name: '{{ response.name }}'
        type: '{{ response.type }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets a notification config.
  # ── gcp.securitycenter.folders.notificationConfigs.get [dependent] ──
  - discovery_id: gcp.securitycenter.folders.notificationConfigs.get
    for_each: gcp.securitycenter.folders.notificationConfigs.list
    # python: svc.folders().notificationConfigs().get(**params).execute()
    calls:
      - action: folders.notificationConfigs.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        description: '{{ response.description }}'
        name: '{{ response.name }}'
        pubsubTopic: '{{ response.pubsubTopic }}'
        serviceAccount: '{{ response.serviceAccount }}'
        streamingConfig: '{{ response.streamingConfig }}'
    # required_params: ['name']
  # Retrieves a SecurityHealthAnalyticsCustomModule.
  # ── gcp.securitycenter.folders.securityHealthAnalyticsSettings.customModules.get [dependent] ──
  - discovery_id: gcp.securitycenter.folders.securityHealthAnalyticsSettings.customModules.get
    for_each: gcp.securitycenter.folders.securityHealthAnalyticsSettings.customModules.list
    # python: svc.folders().securityHealthAnalyticsSettings().customModules().get(**params).execute()
    calls:
      - action: folders.securityHealthAnalyticsSettings.customModules.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        ancestorModule: '{{ response.ancestorModule }}'
        cloudProvider: '{{ response.cloudProvider }}'
        customConfig: '{{ response.customConfig }}'
        displayName: '{{ response.displayName }}'
        enablementState: '{{ response.enablementState }}'
        lastEditor: '{{ response.lastEditor }}'
        name: '{{ response.name }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Retrieves an EffectiveSecurityHealthAnalyticsCustomModule.
  # ── gcp.securitycenter.folders.securityHealthAnalyticsSettings.effectiveCustomModules.get [dependent] ──
  - discovery_id: gcp.securitycenter.folders.securityHealthAnalyticsSettings.effectiveCustomModules.get
    for_each: gcp.securitycenter.folders.securityHealthAnalyticsSettings.effectiveCustomModules.list
    # python: svc.folders().securityHealthAnalyticsSettings().effectiveCustomModules().get(**params).execute()
    calls:
      - action: folders.securityHealthAnalyticsSettings.effectiveCustomModules.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        cloudProvider: '{{ response.cloudProvider }}'
        customConfig: '{{ response.customConfig }}'
        displayName: '{{ response.displayName }}'
        enablementState: '{{ response.enablementState }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Gets a BigQuery export.
  # ── gcp.securitycenter.organizations.bigQueryExports.get [dependent] ──
  - discovery_id: gcp.securitycenter.organizations.bigQueryExports.get
    for_each: gcp.securitycenter.folders.bigQueryExports.list
    # python: svc.organizations().bigQueryExports().get(**params).execute()
    calls:
      - action: organizations.bigQueryExports.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        dataset: '{{ response.dataset }}'
        description: '{{ response.description }}'
        filter: '{{ response.filter }}'
        mostRecentEditor: '{{ response.mostRecentEditor }}'
        name: '{{ response.name }}'
        principal: '{{ response.principal }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets an Event Threat Detection custom module.
  # ── gcp.securitycenter.organizations.eventThreatDetectionSettings.customModules.get [dependent] ──
  - discovery_id: gcp.securitycenter.organizations.eventThreatDetectionSettings.customModules.get
    for_each: gcp.securitycenter.folders.securityHealthAnalyticsSettings.customModules.list
    # python: svc.organizations().eventThreatDetectionSettings().customModules().get(**params).execute()
    calls:
      - action: organizations.eventThreatDetectionSettings.customModules.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        ancestorModule: '{{ response.ancestorModule }}'
        cloudProvider: '{{ response.cloudProvider }}'
        config: '{{ response.config }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        enablementState: '{{ response.enablementState }}'
        lastEditor: '{{ response.lastEditor }}'
        name: '{{ response.name }}'
        type: '{{ response.type }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets an effective Event Threat Detection custom module at the given level.
  # ── gcp.securitycenter.organizations.eventThreatDetectionSettings.effectiveCustomModules.get [dependent] ──
  - discovery_id: gcp.securitycenter.organizations.eventThreatDetectionSettings.effectiveCustomModules.get
    for_each: gcp.securitycenter.folders.securityHealthAnalyticsSettings.effectiveCustomModules.list
    # python: svc.organizations().eventThreatDetectionSettings().effectiveCustomModules().get(**params).execute()
    calls:
      - action: organizations.eventThreatDetectionSettings.effectiveCustomModules.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        cloudProvider: '{{ response.cloudProvider }}'
        config: '{{ response.config }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        enablementState: '{{ response.enablementState }}'
        name: '{{ response.name }}'
        type: '{{ response.type }}'
    # required_params: ['name']
  # Gets a mute config.
  # ── gcp.securitycenter.organizations.locations.muteConfigs.get [dependent] ──
  - discovery_id: gcp.securitycenter.organizations.locations.muteConfigs.get
    for_each: gcp.securitycenter.folders.muteConfigs.list
    # python: svc.organizations().locations().muteConfigs().get(**params).execute()
    calls:
      - action: organizations.locations.muteConfigs.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        expiryTime: '{{ response.expiryTime }}'
        filter: '{{ response.filter }}'
        mostRecentEditor: '{{ response.mostRecentEditor }}'
        name: '{{ response.name }}'
        type: '{{ response.type }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets a mute config.
  # ── gcp.securitycenter.organizations.muteConfigs.get [dependent] ──
  - discovery_id: gcp.securitycenter.organizations.muteConfigs.get
    for_each: gcp.securitycenter.folders.muteConfigs.list
    # python: svc.organizations().muteConfigs().get(**params).execute()
    calls:
      - action: organizations.muteConfigs.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        expiryTime: '{{ response.expiryTime }}'
        filter: '{{ response.filter }}'
        mostRecentEditor: '{{ response.mostRecentEditor }}'
        name: '{{ response.name }}'
        type: '{{ response.type }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets a notification config.
  # ── gcp.securitycenter.organizations.notificationConfigs.get [dependent] ──
  - discovery_id: gcp.securitycenter.organizations.notificationConfigs.get
    for_each: gcp.securitycenter.folders.notificationConfigs.list
    # python: svc.organizations().notificationConfigs().get(**params).execute()
    calls:
      - action: organizations.notificationConfigs.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        description: '{{ response.description }}'
        name: '{{ response.name }}'
        pubsubTopic: '{{ response.pubsubTopic }}'
        serviceAccount: '{{ response.serviceAccount }}'
        streamingConfig: '{{ response.streamingConfig }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.securitycenter.organizations.operations.get [dependent] ──
  - discovery_id: gcp.securitycenter.organizations.operations.get
    for_each: gcp.securitycenter.organizations.operations.list
    # python: svc.organizations().operations().get(**params).execute()
    calls:
      - action: organizations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets a ResourceValueConfig.
  # ── gcp.securitycenter.organizations.resourceValueConfigs.get [dependent] ──
  - discovery_id: gcp.securitycenter.organizations.resourceValueConfigs.get
    for_each: gcp.securitycenter.organizations.resourceValueConfigs.list
    # python: svc.organizations().resourceValueConfigs().get(**params).execute()
    calls:
      - action: organizations.resourceValueConfigs.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        cloudProvider: '{{ response.cloudProvider }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        name: '{{ response.name }}'
        resourceLabelsSelector: '{{ response.resourceLabelsSelector }}'
        resourceType: '{{ response.resourceType }}'
        resourceValue: '{{ response.resourceValue }}'
        scope: '{{ response.scope }}'
        sensitiveDataProtectionMapping: '{{ response.sensitiveDataProtectionMapping }}'
        tagValues: '{{ response.tagValues }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Retrieves a SecurityHealthAnalyticsCustomModule.
  # ── gcp.securitycenter.organizations.securityHealthAnalyticsSettings.customModules.get [dependent] ──
  - discovery_id: gcp.securitycenter.organizations.securityHealthAnalyticsSettings.customModules.get
    for_each: gcp.securitycenter.folders.securityHealthAnalyticsSettings.customModules.list
    # python: svc.organizations().securityHealthAnalyticsSettings().customModules().get(**params).execute()
    calls:
      - action: organizations.securityHealthAnalyticsSettings.customModules.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        ancestorModule: '{{ response.ancestorModule }}'
        cloudProvider: '{{ response.cloudProvider }}'
        customConfig: '{{ response.customConfig }}'
        displayName: '{{ response.displayName }}'
        enablementState: '{{ response.enablementState }}'
        lastEditor: '{{ response.lastEditor }}'
        name: '{{ response.name }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Retrieves an EffectiveSecurityHealthAnalyticsCustomModule.
  # ── gcp.securitycenter.organizations.securityHealthAnalyticsSettings.effectiveCustomModules.get [dependent] ──
  - discovery_id: gcp.securitycenter.organizations.securityHealthAnalyticsSettings.effectiveCustomModules.get
    for_each: gcp.securitycenter.folders.securityHealthAnalyticsSettings.effectiveCustomModules.list
    # python: svc.organizations().securityHealthAnalyticsSettings().effectiveCustomModules().get(**params).execute()
    calls:
      - action: organizations.securityHealthAnalyticsSettings.effectiveCustomModules.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        cloudProvider: '{{ response.cloudProvider }}'
        customConfig: '{{ response.customConfig }}'
        displayName: '{{ response.displayName }}'
        enablementState: '{{ response.enablementState }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Get the simulation by name or the latest simulation for the given organization.
  # ── gcp.securitycenter.organizations.simulations.get [dependent] ──
  - discovery_id: gcp.securitycenter.organizations.simulations.get
    # python: svc.organizations().simulations().get(**params).execute()
    calls:
      - action: organizations.simulations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        cloudProvider: '{{ response.cloudProvider }}'
        createTime: '{{ response.createTime }}'
        name: '{{ response.name }}'
        resourceValueConfigsMetadata: '{{ response.resourceValueConfigsMetadata }}'
    # required_params: ['name']
  # Get the valued resource by name
  # ── gcp.securitycenter.organizations.simulations.valuedResources.get [dependent] ──
  - discovery_id: gcp.securitycenter.organizations.simulations.valuedResources.get
    for_each: gcp.securitycenter.organizations.simulations.valuedResources.list
    # python: svc.organizations().simulations().valuedResources().get(**params).execute()
    calls:
      - action: organizations.simulations.valuedResources.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        exposedScore: '{{ response.exposedScore }}'
        name: '{{ response.name }}'
        resource: '{{ response.resource }}'
        resourceType: '{{ response.resourceType }}'
        resourceValue: '{{ response.resourceValue }}'
        resourceValueConfigsUsed: '{{ response.resourceValueConfigsUsed }}'
    # required_params: ['name']
  # Gets a source.
  # ── gcp.securitycenter.organizations.sources.get [dependent] ──
  - discovery_id: gcp.securitycenter.organizations.sources.get
    for_each: gcp.securitycenter.folders.sources.list
    # python: svc.organizations().sources().get(**params).execute()
    calls:
      - action: organizations.sources.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        canonicalName: '{{ response.canonicalName }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Gets a BigQuery export.
  # ── gcp.securitycenter.projects.bigQueryExports.get [dependent] ──
  - discovery_id: gcp.securitycenter.projects.bigQueryExports.get
    for_each: gcp.securitycenter.folders.bigQueryExports.list
    # python: svc.projects().bigQueryExports().get(**params).execute()
    calls:
      - action: projects.bigQueryExports.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        dataset: '{{ response.dataset }}'
        description: '{{ response.description }}'
        filter: '{{ response.filter }}'
        mostRecentEditor: '{{ response.mostRecentEditor }}'
        name: '{{ response.name }}'
        principal: '{{ response.principal }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets an Event Threat Detection custom module.
  # ── gcp.securitycenter.projects.eventThreatDetectionSettings.customModules.get [dependent] ──
  - discovery_id: gcp.securitycenter.projects.eventThreatDetectionSettings.customModules.get
    for_each: gcp.securitycenter.folders.securityHealthAnalyticsSettings.customModules.list
    # python: svc.projects().eventThreatDetectionSettings().customModules().get(**params).execute()
    calls:
      - action: projects.eventThreatDetectionSettings.customModules.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        ancestorModule: '{{ response.ancestorModule }}'
        cloudProvider: '{{ response.cloudProvider }}'
        config: '{{ response.config }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        enablementState: '{{ response.enablementState }}'
        lastEditor: '{{ response.lastEditor }}'
        name: '{{ response.name }}'
        type: '{{ response.type }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets an effective Event Threat Detection custom module at the given level.
  # ── gcp.securitycenter.projects.eventThreatDetectionSettings.effectiveCustomModules.get [dependent] ──
  - discovery_id: gcp.securitycenter.projects.eventThreatDetectionSettings.effectiveCustomModules.get
    for_each: gcp.securitycenter.folders.securityHealthAnalyticsSettings.effectiveCustomModules.list
    # python: svc.projects().eventThreatDetectionSettings().effectiveCustomModules().get(**params).execute()
    calls:
      - action: projects.eventThreatDetectionSettings.effectiveCustomModules.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        cloudProvider: '{{ response.cloudProvider }}'
        config: '{{ response.config }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        enablementState: '{{ response.enablementState }}'
        name: '{{ response.name }}'
        type: '{{ response.type }}'
    # required_params: ['name']
  # Gets a mute config.
  # ── gcp.securitycenter.projects.locations.muteConfigs.get [dependent] ──
  - discovery_id: gcp.securitycenter.projects.locations.muteConfigs.get
    for_each: gcp.securitycenter.folders.muteConfigs.list
    # python: svc.projects().locations().muteConfigs().get(**params).execute()
    calls:
      - action: projects.locations.muteConfigs.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        expiryTime: '{{ response.expiryTime }}'
        filter: '{{ response.filter }}'
        mostRecentEditor: '{{ response.mostRecentEditor }}'
        name: '{{ response.name }}'
        type: '{{ response.type }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets a mute config.
  # ── gcp.securitycenter.projects.muteConfigs.get [dependent] ──
  - discovery_id: gcp.securitycenter.projects.muteConfigs.get
    for_each: gcp.securitycenter.folders.muteConfigs.list
    # python: svc.projects().muteConfigs().get(**params).execute()
    calls:
      - action: projects.muteConfigs.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        expiryTime: '{{ response.expiryTime }}'
        filter: '{{ response.filter }}'
        mostRecentEditor: '{{ response.mostRecentEditor }}'
        name: '{{ response.name }}'
        type: '{{ response.type }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets a notification config.
  # ── gcp.securitycenter.projects.notificationConfigs.get [dependent] ──
  - discovery_id: gcp.securitycenter.projects.notificationConfigs.get
    for_each: gcp.securitycenter.folders.notificationConfigs.list
    # python: svc.projects().notificationConfigs().get(**params).execute()
    calls:
      - action: projects.notificationConfigs.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        description: '{{ response.description }}'
        name: '{{ response.name }}'
        pubsubTopic: '{{ response.pubsubTopic }}'
        serviceAccount: '{{ response.serviceAccount }}'
        streamingConfig: '{{ response.streamingConfig }}'
    # required_params: ['name']
  # Retrieves a SecurityHealthAnalyticsCustomModule.
  # ── gcp.securitycenter.projects.securityHealthAnalyticsSettings.customModules.get [dependent] ──
  - discovery_id: gcp.securitycenter.projects.securityHealthAnalyticsSettings.customModules.get
    for_each: gcp.securitycenter.folders.securityHealthAnalyticsSettings.customModules.list
    # python: svc.projects().securityHealthAnalyticsSettings().customModules().get(**params).execute()
    calls:
      - action: projects.securityHealthAnalyticsSettings.customModules.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        ancestorModule: '{{ response.ancestorModule }}'
        cloudProvider: '{{ response.cloudProvider }}'
        customConfig: '{{ response.customConfig }}'
        displayName: '{{ response.displayName }}'
        enablementState: '{{ response.enablementState }}'
        lastEditor: '{{ response.lastEditor }}'
        name: '{{ response.name }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Retrieves an EffectiveSecurityHealthAnalyticsCustomModule.
  # ── gcp.securitycenter.projects.securityHealthAnalyticsSettings.effectiveCustomModules.get [dependent] ──
  - discovery_id: gcp.securitycenter.projects.securityHealthAnalyticsSettings.effectiveCustomModules.get
    for_each: gcp.securitycenter.folders.securityHealthAnalyticsSettings.effectiveCustomModules.list
    # python: svc.projects().securityHealthAnalyticsSettings().effectiveCustomModules().get(**params).execute()
    calls:
      - action: projects.securityHealthAnalyticsSettings.effectiveCustomModules.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        cloudProvider: '{{ response.cloudProvider }}'
        customConfig: '{{ response.customConfig }}'
        displayName: '{{ response.displayName }}'
        enablementState: '{{ response.enablementState }}'
        name: '{{ response.name }}'
    # required_params: ['name']
