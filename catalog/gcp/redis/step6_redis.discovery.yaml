# ============================================================
# Discovery YAML — redis (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 15 | independent: 8 | dependent: 7
# ops with fields: 15/15 | dep ops with chain: 7/7
# ============================================================
version: '1.0'
provider: gcp
service: redis

services:
  client: redis
  module: "googleapiclient.discovery.build('redis', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists all backups owned by a backup collection.
  # ── gcp.redis.projects.locations.backupCollections.backups.list ──
  - discovery_id: gcp.redis.projects.locations.backupCollections.backups.list
    # python: svc.projects().locations().backupCollections().backups().list(**params).execute()
    calls:
      - action: projects.locations.backupCollections.backups.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.backups }}'
      item:
        backupFiles: '{{ item.backupFiles }}'
        backupType: '{{ item.backupType }}'
        cluster: '{{ item.cluster }}'
        clusterUid: '{{ item.clusterUid }}'
        createTime: '{{ item.createTime }}'
        encryptionInfo: '{{ item.encryptionInfo }}'
        engineVersion: '{{ item.engineVersion }}'
        expireTime: '{{ item.expireTime }}'
        name: '{{ item.name }}'
        nodeType: '{{ item.nodeType }}'
        replicaCount: '{{ item.replicaCount }}'
        shardCount: '{{ item.shardCount }}'
        state: '{{ item.state }}'
        totalSizeBytes: '{{ item.totalSizeBytes }}'
        uid: '{{ item.uid }}'
  # Lists all backup collections owned by a consumer project in either the specified location (region) o
  # ── gcp.redis.projects.locations.backupCollections.list ──
  - discovery_id: gcp.redis.projects.locations.backupCollections.list
    # python: svc.projects().locations().backupCollections().list(**params).execute()
    calls:
      - action: projects.locations.backupCollections.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.backupCollections }}'
      item:
        cluster: '{{ item.cluster }}'
        clusterUid: '{{ item.clusterUid }}'
        createTime: '{{ item.createTime }}'
        kmsKey: '{{ item.kmsKey }}'
        lastBackupTime: '{{ item.lastBackupTime }}'
        name: '{{ item.name }}'
        totalBackupCount: '{{ item.totalBackupCount }}'
        totalBackupSizeBytes: '{{ item.totalBackupSizeBytes }}'
        uid: '{{ item.uid }}'
  # Lists all Redis clusters owned by a project in either the specified location (region) or all locatio
  # ── gcp.redis.projects.locations.clusters.list ──
  - discovery_id: gcp.redis.projects.locations.clusters.list
    # python: svc.projects().locations().clusters().list(**params).execute()
    calls:
      - action: projects.locations.clusters.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.clusters }}'
      item:
        allowFewerZonesDeployment: '{{ item.allowFewerZonesDeployment }}'
        asyncClusterEndpointsDeletionEnabled: '{{ item.asyncClusterEndpointsDeletionEnabled }}'
        authorizationMode: '{{ item.authorizationMode }}'
        automatedBackupConfig: '{{ item.automatedBackupConfig }}'
        availableMaintenanceVersions: '{{ item.availableMaintenanceVersions }}'
        backupCollection: '{{ item.backupCollection }}'
        clusterEndpoints: '{{ item.clusterEndpoints }}'
        createTime: '{{ item.createTime }}'
        crossClusterReplicationConfig: '{{ item.crossClusterReplicationConfig }}'
        deletionProtectionEnabled: '{{ item.deletionProtectionEnabled }}'
        discoveryEndpoints: '{{ item.discoveryEndpoints }}'
        effectiveMaintenanceVersion: '{{ item.effectiveMaintenanceVersion }}'
        encryptionInfo: '{{ item.encryptionInfo }}'
        gcsSource: '{{ item.gcsSource }}'
        kmsKey: '{{ item.kmsKey }}'
        labels: '{{ item.labels }}'
        maintenancePolicy: '{{ item.maintenancePolicy }}'
        maintenanceSchedule: '{{ item.maintenanceSchedule }}'
        maintenanceVersion: '{{ item.maintenanceVersion }}'
        managedBackupSource: '{{ item.managedBackupSource }}'
        name: '{{ item.name }}'
        nodeType: '{{ item.nodeType }}'
        ondemandMaintenance: '{{ item.ondemandMaintenance }}'
        persistenceConfig: '{{ item.persistenceConfig }}'
        preciseSizeGb: '{{ item.preciseSizeGb }}'
        pscConfigs: '{{ item.pscConfigs }}'
        pscConnections: '{{ item.pscConnections }}'
        pscServiceAttachments: '{{ item.pscServiceAttachments }}'
        redisConfigs: '{{ item.redisConfigs }}'
        replicaCount: '{{ item.replicaCount }}'
        rotateServerCertificate: '{{ item.rotateServerCertificate }}'
        satisfiesPzi: '{{ item.satisfiesPzi }}'
        satisfiesPzs: '{{ item.satisfiesPzs }}'
        serverCaMode: '{{ item.serverCaMode }}'
        serverCaPool: '{{ item.serverCaPool }}'
        shardCount: '{{ item.shardCount }}'
        simulateMaintenanceEvent: '{{ item.simulateMaintenanceEvent }}'
        sizeGb: '{{ item.sizeGb }}'
        state: '{{ item.state }}'
        stateInfo: '{{ item.stateInfo }}'
        transitEncryptionMode: '{{ item.transitEncryptionMode }}'
        uid: '{{ item.uid }}'
        zoneDistributionConfig: '{{ item.zoneDistributionConfig }}'
  # Gets information about a location.
  # ── gcp.redis.projects.locations.get ──
  - discovery_id: gcp.redis.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Gets the details of regional certificate authority information for Redis cluster.
  # ── gcp.redis.projects.locations.getSharedRegionalCertificateAuthority ──
  - discovery_id: gcp.redis.projects.locations.getSharedRegionalCertificateAuthority
    # python: svc.projects().locations().getSharedRegionalCertificateAuthority(**params).execute()
    calls:
      - action: projects.locations.getSharedRegionalCertificateAuthority
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        managedServerCa: '{{ response.managedServerCa }}'
        name: '{{ response.name }}'
  # Lists all Redis instances owned by a project in either the specified location (region) or all locati
  # ── gcp.redis.projects.locations.instances.list ──
  - discovery_id: gcp.redis.projects.locations.instances.list
    # python: svc.projects().locations().instances().list(**params).execute()
    calls:
      - action: projects.locations.instances.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.instances }}'
      item:
        alternativeLocationId: '{{ item.alternativeLocationId }}'
        authEnabled: '{{ item.authEnabled }}'
        authorizedNetwork: '{{ item.authorizedNetwork }}'
        availableMaintenanceVersions: '{{ item.availableMaintenanceVersions }}'
        connectMode: '{{ item.connectMode }}'
        createTime: '{{ item.createTime }}'
        currentLocationId: '{{ item.currentLocationId }}'
        customerManagedKey: '{{ item.customerManagedKey }}'
        displayName: '{{ item.displayName }}'
        host: '{{ item.host }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        maintenancePolicy: '{{ item.maintenancePolicy }}'
        maintenanceSchedule: '{{ item.maintenanceSchedule }}'
        maintenanceVersion: '{{ item.maintenanceVersion }}'
        memorySizeGb: '{{ item.memorySizeGb }}'
        name: '{{ item.name }}'
        nodes: '{{ item.nodes }}'
        persistenceConfig: '{{ item.persistenceConfig }}'
        persistenceIamIdentity: '{{ item.persistenceIamIdentity }}'
        port: '{{ item.port }}'
        readEndpoint: '{{ item.readEndpoint }}'
        readEndpointPort: '{{ item.readEndpointPort }}'
        readReplicasMode: '{{ item.readReplicasMode }}'
        redisConfigs: '{{ item.redisConfigs }}'
        redisVersion: '{{ item.redisVersion }}'
        replicaCount: '{{ item.replicaCount }}'
        reservedIpRange: '{{ item.reservedIpRange }}'
        satisfiesPzi: '{{ item.satisfiesPzi }}'
        satisfiesPzs: '{{ item.satisfiesPzs }}'
        secondaryIpRange: '{{ item.secondaryIpRange }}'
        serverCaCerts: '{{ item.serverCaCerts }}'
        state: '{{ item.state }}'
        statusMessage: '{{ item.statusMessage }}'
        suspensionReasons: '{{ item.suspensionReasons }}'
        tags: '{{ item.tags }}'
        tier: '{{ item.tier }}'
        transitEncryptionMode: '{{ item.transitEncryptionMode }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.redis.projects.locations.list ──
  - discovery_id: gcp.redis.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.redis.projects.locations.operations.list ──
  - discovery_id: gcp.redis.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets the details of a specific backup.
  # ── gcp.redis.projects.locations.backupCollections.backups.get [dependent] ──
  - discovery_id: gcp.redis.projects.locations.backupCollections.backups.get
    for_each: gcp.redis.projects.locations.backupCollections.backups.list
    # python: svc.projects().locations().backupCollections().backups().get(**params).execute()
    calls:
      - action: projects.locations.backupCollections.backups.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        backupFiles: '{{ response.backupFiles }}'
        backupType: '{{ response.backupType }}'
        cluster: '{{ response.cluster }}'
        clusterUid: '{{ response.clusterUid }}'
        createTime: '{{ response.createTime }}'
        encryptionInfo: '{{ response.encryptionInfo }}'
        engineVersion: '{{ response.engineVersion }}'
        expireTime: '{{ response.expireTime }}'
        name: '{{ response.name }}'
        nodeType: '{{ response.nodeType }}'
        replicaCount: '{{ response.replicaCount }}'
        shardCount: '{{ response.shardCount }}'
        state: '{{ response.state }}'
        totalSizeBytes: '{{ response.totalSizeBytes }}'
        uid: '{{ response.uid }}'
    # required_params: ['name']
  # Get a backup collection.
  # ── gcp.redis.projects.locations.backupCollections.get [dependent] ──
  - discovery_id: gcp.redis.projects.locations.backupCollections.get
    for_each: gcp.redis.projects.locations.backupCollections.list
    # python: svc.projects().locations().backupCollections().get(**params).execute()
    calls:
      - action: projects.locations.backupCollections.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        cluster: '{{ response.cluster }}'
        clusterUid: '{{ response.clusterUid }}'
        createTime: '{{ response.createTime }}'
        kmsKey: '{{ response.kmsKey }}'
        lastBackupTime: '{{ response.lastBackupTime }}'
        name: '{{ response.name }}'
        totalBackupCount: '{{ response.totalBackupCount }}'
        totalBackupSizeBytes: '{{ response.totalBackupSizeBytes }}'
        uid: '{{ response.uid }}'
    # required_params: ['name']
  # Gets the details of a specific Redis cluster.
  # ── gcp.redis.projects.locations.clusters.get [dependent] ──
  - discovery_id: gcp.redis.projects.locations.clusters.get
    for_each: gcp.redis.projects.locations.clusters.list
    # python: svc.projects().locations().clusters().get(**params).execute()
    calls:
      - action: projects.locations.clusters.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        allowFewerZonesDeployment: '{{ response.allowFewerZonesDeployment }}'
        asyncClusterEndpointsDeletionEnabled: '{{ response.asyncClusterEndpointsDeletionEnabled }}'
        authorizationMode: '{{ response.authorizationMode }}'
        automatedBackupConfig: '{{ response.automatedBackupConfig }}'
        availableMaintenanceVersions: '{{ response.availableMaintenanceVersions }}'
        backupCollection: '{{ response.backupCollection }}'
        clusterEndpoints: '{{ response.clusterEndpoints }}'
        createTime: '{{ response.createTime }}'
        crossClusterReplicationConfig: '{{ response.crossClusterReplicationConfig }}'
        deletionProtectionEnabled: '{{ response.deletionProtectionEnabled }}'
        discoveryEndpoints: '{{ response.discoveryEndpoints }}'
        effectiveMaintenanceVersion: '{{ response.effectiveMaintenanceVersion }}'
        encryptionInfo: '{{ response.encryptionInfo }}'
        gcsSource: '{{ response.gcsSource }}'
        kmsKey: '{{ response.kmsKey }}'
        labels: '{{ response.labels }}'
        maintenancePolicy: '{{ response.maintenancePolicy }}'
        maintenanceSchedule: '{{ response.maintenanceSchedule }}'
        maintenanceVersion: '{{ response.maintenanceVersion }}'
        managedBackupSource: '{{ response.managedBackupSource }}'
        name: '{{ response.name }}'
        nodeType: '{{ response.nodeType }}'
        ondemandMaintenance: '{{ response.ondemandMaintenance }}'
        persistenceConfig: '{{ response.persistenceConfig }}'
        preciseSizeGb: '{{ response.preciseSizeGb }}'
        pscConfigs: '{{ response.pscConfigs }}'
        pscConnections: '{{ response.pscConnections }}'
        pscServiceAttachments: '{{ response.pscServiceAttachments }}'
        redisConfigs: '{{ response.redisConfigs }}'
        replicaCount: '{{ response.replicaCount }}'
        rotateServerCertificate: '{{ response.rotateServerCertificate }}'
        satisfiesPzi: '{{ response.satisfiesPzi }}'
        satisfiesPzs: '{{ response.satisfiesPzs }}'
        serverCaMode: '{{ response.serverCaMode }}'
        serverCaPool: '{{ response.serverCaPool }}'
        shardCount: '{{ response.shardCount }}'
        simulateMaintenanceEvent: '{{ response.simulateMaintenanceEvent }}'
        sizeGb: '{{ response.sizeGb }}'
        state: '{{ response.state }}'
        stateInfo: '{{ response.stateInfo }}'
        transitEncryptionMode: '{{ response.transitEncryptionMode }}'
        uid: '{{ response.uid }}'
        zoneDistributionConfig: '{{ response.zoneDistributionConfig }}'
    # required_params: ['name']
  # Gets the details of certificate authority information for Redis cluster.
  # ── gcp.redis.projects.locations.clusters.getCertificateAuthority [dependent] ──
  - discovery_id: gcp.redis.projects.locations.clusters.getCertificateAuthority
    for_each: gcp.redis.projects.locations.clusters.list
    # python: svc.projects().locations().clusters().getCertificateAuthority(**params).execute()
    calls:
      - action: projects.locations.clusters.getCertificateAuthority
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        managedServerCa: '{{ response.managedServerCa }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Gets the details of a specific Redis instance.
  # ── gcp.redis.projects.locations.instances.get [dependent] ──
  - discovery_id: gcp.redis.projects.locations.instances.get
    for_each: gcp.redis.projects.locations.instances.list
    # python: svc.projects().locations().instances().get(**params).execute()
    calls:
      - action: projects.locations.instances.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        alternativeLocationId: '{{ response.alternativeLocationId }}'
        authEnabled: '{{ response.authEnabled }}'
        authorizedNetwork: '{{ response.authorizedNetwork }}'
        availableMaintenanceVersions: '{{ response.availableMaintenanceVersions }}'
        connectMode: '{{ response.connectMode }}'
        createTime: '{{ response.createTime }}'
        currentLocationId: '{{ response.currentLocationId }}'
        customerManagedKey: '{{ response.customerManagedKey }}'
        displayName: '{{ response.displayName }}'
        host: '{{ response.host }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        maintenancePolicy: '{{ response.maintenancePolicy }}'
        maintenanceSchedule: '{{ response.maintenanceSchedule }}'
        maintenanceVersion: '{{ response.maintenanceVersion }}'
        memorySizeGb: '{{ response.memorySizeGb }}'
        name: '{{ response.name }}'
        nodes: '{{ response.nodes }}'
        persistenceConfig: '{{ response.persistenceConfig }}'
        persistenceIamIdentity: '{{ response.persistenceIamIdentity }}'
        port: '{{ response.port }}'
        readEndpoint: '{{ response.readEndpoint }}'
        readEndpointPort: '{{ response.readEndpointPort }}'
        readReplicasMode: '{{ response.readReplicasMode }}'
        redisConfigs: '{{ response.redisConfigs }}'
        redisVersion: '{{ response.redisVersion }}'
        replicaCount: '{{ response.replicaCount }}'
        reservedIpRange: '{{ response.reservedIpRange }}'
        satisfiesPzi: '{{ response.satisfiesPzi }}'
        satisfiesPzs: '{{ response.satisfiesPzs }}'
        secondaryIpRange: '{{ response.secondaryIpRange }}'
        serverCaCerts: '{{ response.serverCaCerts }}'
        state: '{{ response.state }}'
        statusMessage: '{{ response.statusMessage }}'
        suspensionReasons: '{{ response.suspensionReasons }}'
        tags: '{{ response.tags }}'
        tier: '{{ response.tier }}'
        transitEncryptionMode: '{{ response.transitEncryptionMode }}'
    # required_params: ['name']
  # Gets the AUTH string for a Redis instance. If AUTH is not enabled for the instance the response will
  # ── gcp.redis.projects.locations.instances.getAuthString [dependent] ──
  - discovery_id: gcp.redis.projects.locations.instances.getAuthString
    for_each: gcp.redis.projects.locations.instances.list
    # python: svc.projects().locations().instances().getAuthString(**params).execute()
    calls:
      - action: projects.locations.instances.getAuthString
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        authString: '{{ response.authString }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.redis.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.redis.projects.locations.operations.get
    for_each: gcp.redis.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
