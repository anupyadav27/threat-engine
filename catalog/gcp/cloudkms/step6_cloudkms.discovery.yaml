# ============================================================
# Discovery YAML — cloudkms (v1)
# Generated: 2026-02-21T03:06:39Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 37 | independent: 21 | dependent: 16
# ops with fields: 36/37 | dep ops with chain: 15/16
# ============================================================
version: '1.0'
provider: gcp
service: cloudkms

services:
  client: cloudkms
  module: "googleapiclient.discovery.build('cloudkms', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Returns the AutokeyConfig for a folder or project.
  # ── gcp.cloudkms.folders.getAutokeyConfig ──
  - discovery_id: gcp.cloudkms.folders.getAutokeyConfig
    # python: svc.folders().getAutokeyConfig(**params).execute()
    calls:
      - action: folders.getAutokeyConfig
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        keyProject: '{{ response.keyProject }}'
        keyProjectResolutionMode: '{{ response.keyProjectResolutionMode }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
  # Gets the KeyAccessJustificationsPolicyConfig for a given organization, folder, or project.
  # ── gcp.cloudkms.folders.getKajPolicyConfig ──
  - discovery_id: gcp.cloudkms.folders.getKajPolicyConfig
    # python: svc.folders().getKajPolicyConfig(**params).execute()
    calls:
      - action: folders.getKajPolicyConfig
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        defaultKeyAccessJustificationPolicy: '{{ response.defaultKeyAccessJustificationPolicy }}'
        name: '{{ response.name }}'
  # Gets the KeyAccessJustificationsPolicyConfig for a given organization, folder, or project.
  # ── gcp.cloudkms.organizations.getKajPolicyConfig ──
  - discovery_id: gcp.cloudkms.organizations.getKajPolicyConfig
    # python: svc.organizations().getKajPolicyConfig(**params).execute()
    calls:
      - action: organizations.getKajPolicyConfig
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        defaultKeyAccessJustificationPolicy: '{{ response.defaultKeyAccessJustificationPolicy }}'
        name: '{{ response.name }}'
  # Returns the AutokeyConfig for a folder or project.
  # ── gcp.cloudkms.projects.getAutokeyConfig ──
  - discovery_id: gcp.cloudkms.projects.getAutokeyConfig
    # python: svc.projects().getAutokeyConfig(**params).execute()
    calls:
      - action: projects.getAutokeyConfig
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        keyProject: '{{ response.keyProject }}'
        keyProjectResolutionMode: '{{ response.keyProjectResolutionMode }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
  # Gets the KeyAccessJustificationsPolicyConfig for a given organization, folder, or project.
  # ── gcp.cloudkms.projects.getKajPolicyConfig ──
  - discovery_id: gcp.cloudkms.projects.getKajPolicyConfig
    # python: svc.projects().getKajPolicyConfig(**params).execute()
    calls:
      - action: projects.getKajPolicyConfig
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        defaultKeyAccessJustificationPolicy: '{{ response.defaultKeyAccessJustificationPolicy }}'
        name: '{{ response.name }}'
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.cloudkms.projects.locations.ekmConfig.getIamPolicy ──
  - discovery_id: gcp.cloudkms.projects.locations.ekmConfig.getIamPolicy
    # python: svc.projects().locations().ekmConfig().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.ekmConfig.getIamPolicy
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
  # Lists EkmConnections.
  # ── gcp.cloudkms.projects.locations.ekmConnections.list ──
  - discovery_id: gcp.cloudkms.projects.locations.ekmConnections.list
    # python: svc.projects().locations().ekmConnections().list(**params).execute()
    calls:
      - action: projects.locations.ekmConnections.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.ekmConnections }}'
      item:
        createTime: '{{ item.createTime }}'
        cryptoSpacePath: '{{ item.cryptoSpacePath }}'
        keyManagementMode: '{{ item.keyManagementMode }}'
        name: '{{ item.name }}'
        serviceResolvers: '{{ item.serviceResolvers }}'
  # Gets information about a location.
  # ── gcp.cloudkms.projects.locations.get ──
  - discovery_id: gcp.cloudkms.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Returns the EkmConfig singleton resource for a given project and location.
  # ── gcp.cloudkms.projects.locations.getEkmConfig ──
  - discovery_id: gcp.cloudkms.projects.locations.getEkmConfig
    # python: svc.projects().locations().getEkmConfig(**params).execute()
    calls:
      - action: projects.locations.getEkmConfig
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        defaultEkmConnection: '{{ response.defaultEkmConnection }}'
        name: '{{ response.name }}'
  # Lists KeyHandles.
  # ── gcp.cloudkms.projects.locations.keyHandles.list ──
  - discovery_id: gcp.cloudkms.projects.locations.keyHandles.list
    # python: svc.projects().locations().keyHandles().list(**params).execute()
    calls:
      - action: projects.locations.keyHandles.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.keyHandles }}'
      item:
        kmsKey: '{{ item.kmsKey }}'
        name: '{{ item.name }}'
        resourceTypeSelector: '{{ item.resourceTypeSelector }}'
  # Lists CryptoKeyVersions.
  # ── gcp.cloudkms.projects.locations.keyRings.cryptoKeys.cryptoKeyVersions.list ──
  - discovery_id: gcp.cloudkms.projects.locations.keyRings.cryptoKeys.cryptoKeyVersions.list
    # python: svc.projects().locations().keyRings().cryptoKeys().cryptoKeyVersions().list(**params).execute()
    calls:
      - action: projects.locations.keyRings.cryptoKeys.cryptoKeyVersions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.cryptoKeyVersions }}'
      item:
        algorithm: '{{ item.algorithm }}'
        attestation: '{{ item.attestation }}'
        createTime: '{{ item.createTime }}'
        destroyEventTime: '{{ item.destroyEventTime }}'
        destroyTime: '{{ item.destroyTime }}'
        externalDestructionFailureReason: '{{ item.externalDestructionFailureReason }}'
        externalProtectionLevelOptions: '{{ item.externalProtectionLevelOptions }}'
        generateTime: '{{ item.generateTime }}'
        generationFailureReason: '{{ item.generationFailureReason }}'
        importFailureReason: '{{ item.importFailureReason }}'
        importJob: '{{ item.importJob }}'
        importTime: '{{ item.importTime }}'
        name: '{{ item.name }}'
        protectionLevel: '{{ item.protectionLevel }}'
        reimportEligible: '{{ item.reimportEligible }}'
        state: '{{ item.state }}'
  # Lists CryptoKeys.
  # ── gcp.cloudkms.projects.locations.keyRings.cryptoKeys.list ──
  - discovery_id: gcp.cloudkms.projects.locations.keyRings.cryptoKeys.list
    # python: svc.projects().locations().keyRings().cryptoKeys().list(**params).execute()
    calls:
      - action: projects.locations.keyRings.cryptoKeys.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.cryptoKeys }}'
      item:
        createTime: '{{ item.createTime }}'
        cryptoKeyBackend: '{{ item.cryptoKeyBackend }}'
        destroyScheduledDuration: '{{ item.destroyScheduledDuration }}'
        importOnly: '{{ item.importOnly }}'
        keyAccessJustificationsPolicy: '{{ item.keyAccessJustificationsPolicy }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        nextRotationTime: '{{ item.nextRotationTime }}'
        primary: '{{ item.primary }}'
        purpose: '{{ item.purpose }}'
        rotationPeriod: '{{ item.rotationPeriod }}'
        versionTemplate: '{{ item.versionTemplate }}'
  # Lists ImportJobs.
  # ── gcp.cloudkms.projects.locations.keyRings.importJobs.list ──
  - discovery_id: gcp.cloudkms.projects.locations.keyRings.importJobs.list
    # python: svc.projects().locations().keyRings().importJobs().list(**params).execute()
    calls:
      - action: projects.locations.keyRings.importJobs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.importJobs }}'
      item:
        attestation: '{{ item.attestation }}'
        createTime: '{{ item.createTime }}'
        cryptoKeyBackend: '{{ item.cryptoKeyBackend }}'
        expireEventTime: '{{ item.expireEventTime }}'
        expireTime: '{{ item.expireTime }}'
        generateTime: '{{ item.generateTime }}'
        importMethod: '{{ item.importMethod }}'
        name: '{{ item.name }}'
        protectionLevel: '{{ item.protectionLevel }}'
        publicKey: '{{ item.publicKey }}'
        state: '{{ item.state }}'
  # Lists KeyRings.
  # ── gcp.cloudkms.projects.locations.keyRings.list ──
  - discovery_id: gcp.cloudkms.projects.locations.keyRings.list
    # python: svc.projects().locations().keyRings().list(**params).execute()
    calls:
      - action: projects.locations.keyRings.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.keyRings }}'
      item:
        createTime: '{{ item.createTime }}'
        name: '{{ item.name }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.cloudkms.projects.locations.list ──
  - discovery_id: gcp.cloudkms.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists the RetiredResources which are the records of deleted CryptoKeys. RetiredResources prevent the
  # ── gcp.cloudkms.projects.locations.retiredResources.list ──
  - discovery_id: gcp.cloudkms.projects.locations.retiredResources.list
    # python: svc.projects().locations().retiredResources().list(**params).execute()
    calls:
      - action: projects.locations.retiredResources.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.retiredResources }}'
      item:
        deleteTime: '{{ item.deleteTime }}'
        name: '{{ item.name }}'
        originalResource: '{{ item.originalResource }}'
        resourceType: '{{ item.resourceType }}'
  # Lists SingleTenantHsmInstances.
  # ── gcp.cloudkms.projects.locations.singleTenantHsmInstances.list ──
  - discovery_id: gcp.cloudkms.projects.locations.singleTenantHsmInstances.list
    # python: svc.projects().locations().singleTenantHsmInstances().list(**params).execute()
    calls:
      - action: projects.locations.singleTenantHsmInstances.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.singleTenantHsmInstances }}'
      item:
        createTime: '{{ item.createTime }}'
        deleteTime: '{{ item.deleteTime }}'
        disableTime: '{{ item.disableTime }}'
        name: '{{ item.name }}'
        quorumAuth: '{{ item.quorumAuth }}'
        state: '{{ item.state }}'
        unrefreshedDurationUntilDisable: '{{ item.unrefreshedDurationUntilDisable }}'
  # Lists SingleTenantHsmInstanceProposals.
  # ── gcp.cloudkms.projects.locations.singleTenantHsmInstances.proposals.list ──
  - discovery_id: gcp.cloudkms.projects.locations.singleTenantHsmInstances.proposals.list
    # python: svc.projects().locations().singleTenantHsmInstances().proposals().list(**params).execute()
    calls:
      - action: projects.locations.singleTenantHsmInstances.proposals.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.proposals }}'
      item:
        addQuorumMember: '{{ item.addQuorumMember }}'
        createTime: '{{ item.createTime }}'
        deleteSingleTenantHsmInstance: '{{ item.deleteSingleTenantHsmInstance }}'
        deleteTime: '{{ item.deleteTime }}'
        disableSingleTenantHsmInstance: '{{ item.disableSingleTenantHsmInstance }}'
        enableSingleTenantHsmInstance: '{{ item.enableSingleTenantHsmInstance }}'
        expireTime: '{{ item.expireTime }}'
        failureReason: '{{ item.failureReason }}'
        name: '{{ item.name }}'
        purgeTime: '{{ item.purgeTime }}'
        quorumParameters: '{{ item.quorumParameters }}'
        refreshSingleTenantHsmInstance: '{{ item.refreshSingleTenantHsmInstance }}'
        registerTwoFactorAuthKeys: '{{ item.registerTwoFactorAuthKeys }}'
        removeQuorumMember: '{{ item.removeQuorumMember }}'
        requiredActionQuorumParameters: '{{ item.requiredActionQuorumParameters }}'
        state: '{{ item.state }}'
        ttl: '{{ item.ttl }}'
  # Returns the effective Cloud KMS Autokey configuration for a given project.
  # ── gcp.cloudkms.projects.showEffectiveAutokeyConfig ──
  - discovery_id: gcp.cloudkms.projects.showEffectiveAutokeyConfig
    # python: svc.projects().showEffectiveAutokeyConfig(**params).execute()
    calls:
      - action: projects.showEffectiveAutokeyConfig
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        keyProject: '{{ response.keyProject }}'
  # Returns the KeyAccessJustificationsEnrollmentConfig of the resource closest to the given project in 
  # ── gcp.cloudkms.projects.showEffectiveKeyAccessJustificationsEnrollmentConfig ──
  - discovery_id: gcp.cloudkms.projects.showEffectiveKeyAccessJustificationsEnrollmentConfig
    # python: svc.projects().showEffectiveKeyAccessJustificationsEnrollmentConfig(**params).execute()
    calls:
      - action: projects.showEffectiveKeyAccessJustificationsEnrollmentConfig
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        externalConfig: '{{ response.externalConfig }}'
        hardwareConfig: '{{ response.hardwareConfig }}'
        softwareConfig: '{{ response.softwareConfig }}'
  # Returns the KeyAccessJustificationsPolicyConfig of the resource closest to the given project in hier
  # ── gcp.cloudkms.projects.showEffectiveKeyAccessJustificationsPolicyConfig ──
  - discovery_id: gcp.cloudkms.projects.showEffectiveKeyAccessJustificationsPolicyConfig
    # python: svc.projects().showEffectiveKeyAccessJustificationsPolicyConfig(**params).execute()
    calls:
      - action: projects.showEffectiveKeyAccessJustificationsPolicyConfig
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        effectiveKajPolicy: '{{ response.effectiveKajPolicy }}'

  # ════ DEPENDENT (enrich) operations ════
  # Returns metadata for a given EkmConnection.
  # ── gcp.cloudkms.projects.locations.ekmConnections.get [dependent] ──
  - discovery_id: gcp.cloudkms.projects.locations.ekmConnections.get
    for_each: gcp.cloudkms.projects.locations.ekmConnections.list
    # python: svc.projects().locations().ekmConnections().get(**params).execute()
    calls:
      - action: projects.locations.ekmConnections.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        cryptoSpacePath: '{{ response.cryptoSpacePath }}'
        keyManagementMode: '{{ response.keyManagementMode }}'
        name: '{{ response.name }}'
        serviceResolvers: '{{ response.serviceResolvers }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.cloudkms.projects.locations.ekmConnections.getIamPolicy [dependent] ──
  - discovery_id: gcp.cloudkms.projects.locations.ekmConnections.getIamPolicy
    for_each: gcp.cloudkms.projects.locations.ekmConnections.list
    # python: svc.projects().locations().ekmConnections().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.ekmConnections.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Verifies that Cloud KMS can successfully connect to the external key manager specified by an EkmConn
  # ── gcp.cloudkms.projects.locations.ekmConnections.verifyConnectivity [dependent] ──
  - discovery_id: gcp.cloudkms.projects.locations.ekmConnections.verifyConnectivity
    for_each: gcp.cloudkms.projects.locations.ekmConnections.list
    # python: svc.projects().locations().ekmConnections().verifyConnectivity(**params).execute()
    calls:
      - action: projects.locations.ekmConnections.verifyConnectivity
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
    # required_params: ['name']
  # Returns the KeyHandle.
  # ── gcp.cloudkms.projects.locations.keyHandles.get [dependent] ──
  - discovery_id: gcp.cloudkms.projects.locations.keyHandles.get
    for_each: gcp.cloudkms.projects.locations.keyHandles.list
    # python: svc.projects().locations().keyHandles().get(**params).execute()
    calls:
      - action: projects.locations.keyHandles.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        kmsKey: '{{ response.kmsKey }}'
        name: '{{ response.name }}'
        resourceTypeSelector: '{{ response.resourceTypeSelector }}'
    # required_params: ['name']
  # Returns metadata for a given CryptoKeyVersion.
  # ── gcp.cloudkms.projects.locations.keyRings.cryptoKeys.cryptoKeyVersions.get [dependent] ──
  - discovery_id: gcp.cloudkms.projects.locations.keyRings.cryptoKeys.cryptoKeyVersions.get
    for_each: gcp.cloudkms.projects.locations.keyRings.cryptoKeys.cryptoKeyVersions.list
    # python: svc.projects().locations().keyRings().cryptoKeys().cryptoKeyVersions().get(**params).execute()
    calls:
      - action: projects.locations.keyRings.cryptoKeys.cryptoKeyVersions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        algorithm: '{{ response.algorithm }}'
        attestation: '{{ response.attestation }}'
        createTime: '{{ response.createTime }}'
        destroyEventTime: '{{ response.destroyEventTime }}'
        destroyTime: '{{ response.destroyTime }}'
        externalDestructionFailureReason: '{{ response.externalDestructionFailureReason }}'
        externalProtectionLevelOptions: '{{ response.externalProtectionLevelOptions }}'
        generateTime: '{{ response.generateTime }}'
        generationFailureReason: '{{ response.generationFailureReason }}'
        importFailureReason: '{{ response.importFailureReason }}'
        importJob: '{{ response.importJob }}'
        importTime: '{{ response.importTime }}'
        name: '{{ response.name }}'
        protectionLevel: '{{ response.protectionLevel }}'
        reimportEligible: '{{ response.reimportEligible }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Returns the public key for the given CryptoKeyVersion. The CryptoKey.purpose must be ASYMMETRIC_SIGN
  # ── gcp.cloudkms.projects.locations.keyRings.cryptoKeys.cryptoKeyVersions.getPublicKey [dependent] ──
  - discovery_id: gcp.cloudkms.projects.locations.keyRings.cryptoKeys.cryptoKeyVersions.getPublicKey
    for_each: gcp.cloudkms.projects.locations.keyRings.cryptoKeys.cryptoKeyVersions.list
    # python: svc.projects().locations().keyRings().cryptoKeys().cryptoKeyVersions().getPublicKey(**params).execute()
    calls:
      - action: projects.locations.keyRings.cryptoKeys.cryptoKeyVersions.getPublicKey
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        algorithm: '{{ response.algorithm }}'
        name: '{{ response.name }}'
        pem: '{{ response.pem }}'
        pemCrc32c: '{{ response.pemCrc32c }}'
        protectionLevel: '{{ response.protectionLevel }}'
        publicKey: '{{ response.publicKey }}'
        publicKeyFormat: '{{ response.publicKeyFormat }}'
    # required_params: ['name']
  # Returns metadata for a given CryptoKey, as well as its primary CryptoKeyVersion.
  # ── gcp.cloudkms.projects.locations.keyRings.cryptoKeys.get [dependent] ──
  - discovery_id: gcp.cloudkms.projects.locations.keyRings.cryptoKeys.get
    for_each: gcp.cloudkms.projects.locations.keyRings.cryptoKeys.list
    # python: svc.projects().locations().keyRings().cryptoKeys().get(**params).execute()
    calls:
      - action: projects.locations.keyRings.cryptoKeys.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        cryptoKeyBackend: '{{ response.cryptoKeyBackend }}'
        destroyScheduledDuration: '{{ response.destroyScheduledDuration }}'
        importOnly: '{{ response.importOnly }}'
        keyAccessJustificationsPolicy: '{{ response.keyAccessJustificationsPolicy }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        nextRotationTime: '{{ response.nextRotationTime }}'
        primary: '{{ response.primary }}'
        purpose: '{{ response.purpose }}'
        rotationPeriod: '{{ response.rotationPeriod }}'
        versionTemplate: '{{ response.versionTemplate }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.cloudkms.projects.locations.keyRings.cryptoKeys.getIamPolicy [dependent] ──
  - discovery_id: gcp.cloudkms.projects.locations.keyRings.cryptoKeys.getIamPolicy
    for_each: gcp.cloudkms.projects.locations.keyRings.cryptoKeys.list
    # python: svc.projects().locations().keyRings().cryptoKeys().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.keyRings.cryptoKeys.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Returns metadata for a given KeyRing.
  # ── gcp.cloudkms.projects.locations.keyRings.get [dependent] ──
  - discovery_id: gcp.cloudkms.projects.locations.keyRings.get
    for_each: gcp.cloudkms.projects.locations.keyRings.list
    # python: svc.projects().locations().keyRings().get(**params).execute()
    calls:
      - action: projects.locations.keyRings.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.cloudkms.projects.locations.keyRings.getIamPolicy [dependent] ──
  - discovery_id: gcp.cloudkms.projects.locations.keyRings.getIamPolicy
    for_each: gcp.cloudkms.projects.locations.keyRings.list
    # python: svc.projects().locations().keyRings().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.keyRings.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Returns metadata for a given ImportJob.
  # ── gcp.cloudkms.projects.locations.keyRings.importJobs.get [dependent] ──
  - discovery_id: gcp.cloudkms.projects.locations.keyRings.importJobs.get
    for_each: gcp.cloudkms.projects.locations.keyRings.importJobs.list
    # python: svc.projects().locations().keyRings().importJobs().get(**params).execute()
    calls:
      - action: projects.locations.keyRings.importJobs.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        attestation: '{{ response.attestation }}'
        createTime: '{{ response.createTime }}'
        cryptoKeyBackend: '{{ response.cryptoKeyBackend }}'
        expireEventTime: '{{ response.expireEventTime }}'
        expireTime: '{{ response.expireTime }}'
        generateTime: '{{ response.generateTime }}'
        importMethod: '{{ response.importMethod }}'
        name: '{{ response.name }}'
        protectionLevel: '{{ response.protectionLevel }}'
        publicKey: '{{ response.publicKey }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.cloudkms.projects.locations.keyRings.importJobs.getIamPolicy [dependent] ──
  - discovery_id: gcp.cloudkms.projects.locations.keyRings.importJobs.getIamPolicy
    for_each: gcp.cloudkms.projects.locations.keyRings.importJobs.list
    # python: svc.projects().locations().keyRings().importJobs().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.keyRings.importJobs.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.cloudkms.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.cloudkms.projects.locations.operations.get
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Retrieves a specific RetiredResource resource, which represents the record of a deleted CryptoKey.
  # ── gcp.cloudkms.projects.locations.retiredResources.get [dependent] ──
  - discovery_id: gcp.cloudkms.projects.locations.retiredResources.get
    for_each: gcp.cloudkms.projects.locations.retiredResources.list
    # python: svc.projects().locations().retiredResources().get(**params).execute()
    calls:
      - action: projects.locations.retiredResources.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        deleteTime: '{{ response.deleteTime }}'
        name: '{{ response.name }}'
        originalResource: '{{ response.originalResource }}'
        resourceType: '{{ response.resourceType }}'
    # required_params: ['name']
  # Returns metadata for a given SingleTenantHsmInstance.
  # ── gcp.cloudkms.projects.locations.singleTenantHsmInstances.get [dependent] ──
  - discovery_id: gcp.cloudkms.projects.locations.singleTenantHsmInstances.get
    for_each: gcp.cloudkms.projects.locations.singleTenantHsmInstances.list
    # python: svc.projects().locations().singleTenantHsmInstances().get(**params).execute()
    calls:
      - action: projects.locations.singleTenantHsmInstances.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        deleteTime: '{{ response.deleteTime }}'
        disableTime: '{{ response.disableTime }}'
        name: '{{ response.name }}'
        quorumAuth: '{{ response.quorumAuth }}'
        state: '{{ response.state }}'
        unrefreshedDurationUntilDisable: '{{ response.unrefreshedDurationUntilDisable }}'
    # required_params: ['name']
  # Returns metadata for a given SingleTenantHsmInstanceProposal.
  # ── gcp.cloudkms.projects.locations.singleTenantHsmInstances.proposals.get [dependent] ──
  - discovery_id: gcp.cloudkms.projects.locations.singleTenantHsmInstances.proposals.get
    for_each: gcp.cloudkms.projects.locations.singleTenantHsmInstances.proposals.list
    # python: svc.projects().locations().singleTenantHsmInstances().proposals().get(**params).execute()
    calls:
      - action: projects.locations.singleTenantHsmInstances.proposals.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        addQuorumMember: '{{ response.addQuorumMember }}'
        createTime: '{{ response.createTime }}'
        deleteSingleTenantHsmInstance: '{{ response.deleteSingleTenantHsmInstance }}'
        deleteTime: '{{ response.deleteTime }}'
        disableSingleTenantHsmInstance: '{{ response.disableSingleTenantHsmInstance }}'
        enableSingleTenantHsmInstance: '{{ response.enableSingleTenantHsmInstance }}'
        expireTime: '{{ response.expireTime }}'
        failureReason: '{{ response.failureReason }}'
        name: '{{ response.name }}'
        purgeTime: '{{ response.purgeTime }}'
        quorumParameters: '{{ response.quorumParameters }}'
        refreshSingleTenantHsmInstance: '{{ response.refreshSingleTenantHsmInstance }}'
        registerTwoFactorAuthKeys: '{{ response.registerTwoFactorAuthKeys }}'
        removeQuorumMember: '{{ response.removeQuorumMember }}'
        requiredActionQuorumParameters: '{{ response.requiredActionQuorumParameters }}'
        state: '{{ response.state }}'
        ttl: '{{ response.ttl }}'
    # required_params: ['name']
