# ============================================================
# Discovery YAML — policysimulator (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 23 | independent: 5 | dependent: 18
# ops with fields: 23/23 | dep ops with chain: 11/18
# ============================================================
version: '1.0'
provider: gcp
service: policysimulator

services:
  client: policysimulator
  module: "googleapiclient.discovery.build('policysimulator', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists the results of running a Replay.
  # ── gcp.policysimulator.folders.locations.replays.results.list ──
  - discovery_id: gcp.policysimulator.folders.locations.replays.results.list
    # python: svc.folders().locations().replays().results().list(**params).execute()
    calls:
      - action: folders.locations.replays.results.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.results }}'
      item:
        accessTuple: '{{ item.accessTuple }}'
        diff: '{{ item.diff }}'
        error: '{{ item.error }}'
        lastSeenDate: '{{ item.lastSeenDate }}'
        name: '{{ item.name }}'
        parent: '{{ item.parent }}'
  # ListOrgPolicyViolationsPreviews lists each OrgPolicyViolationsPreview in an organization. Each OrgPo
  # ── gcp.policysimulator.organizations.locations.orgPolicyViolationsPreviews.list ──
  - discovery_id: gcp.policysimulator.organizations.locations.orgPolicyViolationsPreviews.list
    # python: svc.organizations().locations().orgPolicyViolationsPreviews().list(**params).execute()
    calls:
      - action: organizations.locations.orgPolicyViolationsPreviews.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.orgPolicyViolationsPreviews }}'
      item:
        createTime: '{{ item.createTime }}'
        customConstraints: '{{ item.customConstraints }}'
        name: '{{ item.name }}'
        overlay: '{{ item.overlay }}'
        resourceCounts: '{{ item.resourceCounts }}'
        state: '{{ item.state }}'
        violationsCount: '{{ item.violationsCount }}'
  # ListOrgPolicyViolations lists the OrgPolicyViolations that are present in an OrgPolicyViolationsPrev
  # ── gcp.policysimulator.organizations.locations.orgPolicyViolationsPreviews.orgPolicyViolations.list ──
  - discovery_id: gcp.policysimulator.organizations.locations.orgPolicyViolationsPreviews.orgPolicyViolations.list
    # python: svc.organizations().locations().orgPolicyViolationsPreviews().orgPolicyViolations().list(**params).execute()
    calls:
      - action: organizations.locations.orgPolicyViolationsPreviews.orgPolicyViolations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.orgPolicyViolations }}'
      item:
        customConstraint: '{{ item.customConstraint }}'
        error: '{{ item.error }}'
        name: '{{ item.name }}'
        resource: '{{ item.resource }}'
  # Lists the results of running a Replay.
  # ── gcp.policysimulator.organizations.locations.replays.results.list ──
  - discovery_id: gcp.policysimulator.organizations.locations.replays.results.list
    # python: svc.organizations().locations().replays().results().list(**params).execute()
    calls:
      - action: organizations.locations.replays.results.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.results }}'
      item:
        accessTuple: '{{ item.accessTuple }}'
        diff: '{{ item.diff }}'
        error: '{{ item.error }}'
        lastSeenDate: '{{ item.lastSeenDate }}'
        name: '{{ item.name }}'
        parent: '{{ item.parent }}'
  # Lists the results of running a Replay.
  # ── gcp.policysimulator.projects.locations.replays.results.list ──
  - discovery_id: gcp.policysimulator.projects.locations.replays.results.list
    # python: svc.projects().locations().replays().results().list(**params).execute()
    calls:
      - action: projects.locations.replays.results.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.results }}'
      item:
        accessTuple: '{{ item.accessTuple }}'
        diff: '{{ item.diff }}'
        error: '{{ item.error }}'
        lastSeenDate: '{{ item.lastSeenDate }}'
        name: '{{ item.name }}'
        parent: '{{ item.parent }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.policysimulator.folders.locations.accessPolicySimulations.operations.get [dependent] ──
  - discovery_id: gcp.policysimulator.folders.locations.accessPolicySimulations.operations.get
    for_each: gcp.policysimulator.folders.locations.replays.operations.list
    # python: svc.folders().locations().accessPolicySimulations().operations().get(**params).execute()
    calls:
      - action: folders.locations.accessPolicySimulations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.policysimulator.folders.locations.orgPolicyViolationsPreviews.operations.get [dependent] ──
  - discovery_id: gcp.policysimulator.folders.locations.orgPolicyViolationsPreviews.operations.get
    for_each: gcp.policysimulator.folders.locations.replays.operations.list
    # python: svc.folders().locations().orgPolicyViolationsPreviews().operations().get(**params).execute()
    calls:
      - action: folders.locations.orgPolicyViolationsPreviews.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets the specified Replay. Each `Replay` is available for at least 7 days.
  # ── gcp.policysimulator.folders.locations.replays.get [dependent] ──
  - discovery_id: gcp.policysimulator.folders.locations.replays.get
    # python: svc.folders().locations().replays().get(**params).execute()
    calls:
      - action: folders.locations.replays.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        config: '{{ response.config }}'
        name: '{{ response.name }}'
        resultsSummary: '{{ response.resultsSummary }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.policysimulator.folders.locations.replays.operations.get [dependent] ──
  - discovery_id: gcp.policysimulator.folders.locations.replays.operations.get
    for_each: gcp.policysimulator.folders.locations.replays.operations.list
    # python: svc.folders().locations().replays().operations().get(**params).execute()
    calls:
      - action: folders.locations.replays.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.policysimulator.folders.locations.replays.operations.list [dependent] ──
  - discovery_id: gcp.policysimulator.folders.locations.replays.operations.list
    # python: svc.folders().locations().replays().operations().list(**params).execute()
    calls:
      - action: folders.locations.replays.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.policysimulator.operations.get [dependent] ──
  - discovery_id: gcp.policysimulator.operations.get
    for_each: gcp.policysimulator.folders.locations.replays.operations.list
    # python: svc.operations().get(**params).execute()
    calls:
      - action: operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.policysimulator.operations.list [dependent] ──
  - discovery_id: gcp.policysimulator.operations.list
    # python: svc.operations().list(**params).execute()
    calls:
      - action: operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.policysimulator.organizations.locations.accessPolicySimulations.operations.get [dependent] ──
  - discovery_id: gcp.policysimulator.organizations.locations.accessPolicySimulations.operations.get
    for_each: gcp.policysimulator.folders.locations.replays.operations.list
    # python: svc.organizations().locations().accessPolicySimulations().operations().get(**params).execute()
    calls:
      - action: organizations.locations.accessPolicySimulations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # GetOrgPolicyViolationsPreview gets the specified OrgPolicyViolationsPreview. Each OrgPolicyViolation
  # ── gcp.policysimulator.organizations.locations.orgPolicyViolationsPreviews.get [dependent] ──
  - discovery_id: gcp.policysimulator.organizations.locations.orgPolicyViolationsPreviews.get
    for_each: gcp.policysimulator.organizations.locations.orgPolicyViolationsPreviews.list
    # python: svc.organizations().locations().orgPolicyViolationsPreviews().get(**params).execute()
    calls:
      - action: organizations.locations.orgPolicyViolationsPreviews.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        customConstraints: '{{ response.customConstraints }}'
        name: '{{ response.name }}'
        overlay: '{{ response.overlay }}'
        resourceCounts: '{{ response.resourceCounts }}'
        state: '{{ response.state }}'
        violationsCount: '{{ response.violationsCount }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.policysimulator.organizations.locations.orgPolicyViolationsPreviews.operations.get [dependent] ──
  - discovery_id: gcp.policysimulator.organizations.locations.orgPolicyViolationsPreviews.operations.get
    for_each: gcp.policysimulator.folders.locations.replays.operations.list
    # python: svc.organizations().locations().orgPolicyViolationsPreviews().operations().get(**params).execute()
    calls:
      - action: organizations.locations.orgPolicyViolationsPreviews.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets the specified Replay. Each `Replay` is available for at least 7 days.
  # ── gcp.policysimulator.organizations.locations.replays.get [dependent] ──
  - discovery_id: gcp.policysimulator.organizations.locations.replays.get
    # python: svc.organizations().locations().replays().get(**params).execute()
    calls:
      - action: organizations.locations.replays.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        config: '{{ response.config }}'
        name: '{{ response.name }}'
        resultsSummary: '{{ response.resultsSummary }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.policysimulator.organizations.locations.replays.operations.get [dependent] ──
  - discovery_id: gcp.policysimulator.organizations.locations.replays.operations.get
    for_each: gcp.policysimulator.folders.locations.replays.operations.list
    # python: svc.organizations().locations().replays().operations().get(**params).execute()
    calls:
      - action: organizations.locations.replays.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.policysimulator.organizations.locations.replays.operations.list [dependent] ──
  - discovery_id: gcp.policysimulator.organizations.locations.replays.operations.list
    # python: svc.organizations().locations().replays().operations().list(**params).execute()
    calls:
      - action: organizations.locations.replays.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.policysimulator.projects.locations.accessPolicySimulations.operations.get [dependent] ──
  - discovery_id: gcp.policysimulator.projects.locations.accessPolicySimulations.operations.get
    for_each: gcp.policysimulator.folders.locations.replays.operations.list
    # python: svc.projects().locations().accessPolicySimulations().operations().get(**params).execute()
    calls:
      - action: projects.locations.accessPolicySimulations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.policysimulator.projects.locations.orgPolicyViolationsPreviews.operations.get [dependent] ──
  - discovery_id: gcp.policysimulator.projects.locations.orgPolicyViolationsPreviews.operations.get
    for_each: gcp.policysimulator.folders.locations.replays.operations.list
    # python: svc.projects().locations().orgPolicyViolationsPreviews().operations().get(**params).execute()
    calls:
      - action: projects.locations.orgPolicyViolationsPreviews.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets the specified Replay. Each `Replay` is available for at least 7 days.
  # ── gcp.policysimulator.projects.locations.replays.get [dependent] ──
  - discovery_id: gcp.policysimulator.projects.locations.replays.get
    # python: svc.projects().locations().replays().get(**params).execute()
    calls:
      - action: projects.locations.replays.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        config: '{{ response.config }}'
        name: '{{ response.name }}'
        resultsSummary: '{{ response.resultsSummary }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.policysimulator.projects.locations.replays.operations.get [dependent] ──
  - discovery_id: gcp.policysimulator.projects.locations.replays.operations.get
    for_each: gcp.policysimulator.folders.locations.replays.operations.list
    # python: svc.projects().locations().replays().operations().get(**params).execute()
    calls:
      - action: projects.locations.replays.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.policysimulator.projects.locations.replays.operations.list [dependent] ──
  - discovery_id: gcp.policysimulator.projects.locations.replays.operations.list
    # python: svc.projects().locations().replays().operations().list(**params).execute()
    calls:
      - action: projects.locations.replays.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
    # required_params: ['name']
