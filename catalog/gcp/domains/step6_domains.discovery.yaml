# ============================================================
# Discovery YAML — domains (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 14 | independent: 8 | dependent: 6
# ops with fields: 14/14 | dep ops with chain: 6/6
# ============================================================
version: '1.0'
provider: gcp
service: domains

services:
  client: domains
  module: "googleapiclient.discovery.build('domains', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Gets information about a location.
  # ── gcp.domains.projects.locations.get ──
  - discovery_id: gcp.domains.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.domains.projects.locations.list ──
  - discovery_id: gcp.domains.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.domains.projects.locations.operations.list ──
  - discovery_id: gcp.domains.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists the `Registration` resources in a project.
  # ── gcp.domains.projects.locations.registrations.list ──
  - discovery_id: gcp.domains.projects.locations.registrations.list
    # python: svc.projects().locations().registrations().list(**params).execute()
    calls:
      - action: projects.locations.registrations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.registrations }}'
      item:
        contactSettings: '{{ item.contactSettings }}'
        createTime: '{{ item.createTime }}'
        dnsSettings: '{{ item.dnsSettings }}'
        domainName: '{{ item.domainName }}'
        domainProperties: '{{ item.domainProperties }}'
        expireTime: '{{ item.expireTime }}'
        issues: '{{ item.issues }}'
        labels: '{{ item.labels }}'
        managementSettings: '{{ item.managementSettings }}'
        name: '{{ item.name }}'
        pendingContactSettings: '{{ item.pendingContactSettings }}'
        registerFailureReason: '{{ item.registerFailureReason }}'
        state: '{{ item.state }}'
        supportedPrivacy: '{{ item.supportedPrivacy }}'
        transferFailureReason: '{{ item.transferFailureReason }}'
  # Deprecated: For more information, see [Cloud Domains feature deprecation](https://cloud.google.com/d
  # ── gcp.domains.projects.locations.registrations.retrieveImportableDomains ──
  - discovery_id: gcp.domains.projects.locations.registrations.retrieveImportableDomains
    # python: svc.projects().locations().registrations().retrieveImportableDomains(**params).execute()
    calls:
      - action: projects.locations.registrations.retrieveImportableDomains
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        domains: '{{ response.domains }}'
  # Gets parameters needed to register a new domain name, including price and up-to-date availability. U
  # ── gcp.domains.projects.locations.registrations.retrieveRegisterParameters ──
  - discovery_id: gcp.domains.projects.locations.registrations.retrieveRegisterParameters
    # python: svc.projects().locations().registrations().retrieveRegisterParameters(**params).execute()
    calls:
      - action: projects.locations.registrations.retrieveRegisterParameters
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        registerParameters: '{{ response.registerParameters }}'
  # Deprecated: For more information, see [Cloud Domains feature deprecation](https://cloud.google.com/d
  # ── gcp.domains.projects.locations.registrations.retrieveTransferParameters ──
  - discovery_id: gcp.domains.projects.locations.registrations.retrieveTransferParameters
    # python: svc.projects().locations().registrations().retrieveTransferParameters(**params).execute()
    calls:
      - action: projects.locations.registrations.retrieveTransferParameters
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        transferParameters: '{{ response.transferParameters }}'
  # Searches for available domain names similar to the provided query. Availability results from this me
  # ── gcp.domains.projects.locations.registrations.searchDomains ──
  - discovery_id: gcp.domains.projects.locations.registrations.searchDomains
    # python: svc.projects().locations().registrations().searchDomains(**params).execute()
    calls:
      - action: projects.locations.registrations.searchDomains
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        registerParameters: '{{ response.registerParameters }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.domains.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.domains.projects.locations.operations.get
    for_each: gcp.domains.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets the details of a `Registration` resource.
  # ── gcp.domains.projects.locations.registrations.get [dependent] ──
  - discovery_id: gcp.domains.projects.locations.registrations.get
    for_each: gcp.domains.projects.locations.registrations.list
    # python: svc.projects().locations().registrations().get(**params).execute()
    calls:
      - action: projects.locations.registrations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contactSettings: '{{ response.contactSettings }}'
        createTime: '{{ response.createTime }}'
        dnsSettings: '{{ response.dnsSettings }}'
        domainName: '{{ response.domainName }}'
        domainProperties: '{{ response.domainProperties }}'
        expireTime: '{{ response.expireTime }}'
        issues: '{{ response.issues }}'
        labels: '{{ response.labels }}'
        managementSettings: '{{ response.managementSettings }}'
        name: '{{ response.name }}'
        pendingContactSettings: '{{ response.pendingContactSettings }}'
        registerFailureReason: '{{ response.registerFailureReason }}'
        state: '{{ response.state }}'
        supportedPrivacy: '{{ response.supportedPrivacy }}'
        transferFailureReason: '{{ response.transferFailureReason }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.domains.projects.locations.registrations.getIamPolicy [dependent] ──
  - discovery_id: gcp.domains.projects.locations.registrations.getIamPolicy
    for_each: gcp.domains.projects.locations.registrations.list
    # python: svc.projects().locations().registrations().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.registrations.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Gets the authorization code of the `Registration` for the purpose of transferring the domain to anot
  # ── gcp.domains.projects.locations.registrations.retrieveAuthorizationCode [dependent] ──
  - discovery_id: gcp.domains.projects.locations.registrations.retrieveAuthorizationCode
    for_each: gcp.domains.projects.locations.registrations.list
    # python: svc.projects().locations().registrations().retrieveAuthorizationCode(**params).execute()
    calls:
      - action: projects.locations.registrations.retrieveAuthorizationCode
        params:
          registration: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        code: '{{ response.code }}'
    # required_params: ['registration']
  # Lists the DNS records from the Google Domains DNS zone for domains that use the deprecated `google_d
  # ── gcp.domains.projects.locations.registrations.retrieveGoogleDomainsDnsRecords [dependent] ──
  - discovery_id: gcp.domains.projects.locations.registrations.retrieveGoogleDomainsDnsRecords
    for_each: gcp.domains.projects.locations.registrations.list
    # python: svc.projects().locations().registrations().retrieveGoogleDomainsDnsRecords(**params).execute()
    calls:
      - action: projects.locations.registrations.retrieveGoogleDomainsDnsRecords
        params:
          registration: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        rrset: '{{ response.rrset }}'
    # required_params: ['registration']
  # Lists the deprecated domain and email forwarding configurations you set up in the deprecated Google 
  # ── gcp.domains.projects.locations.registrations.retrieveGoogleDomainsForwardingConfig [dependent] ──
  - discovery_id: gcp.domains.projects.locations.registrations.retrieveGoogleDomainsForwardingConfig
    for_each: gcp.domains.projects.locations.registrations.list
    # python: svc.projects().locations().registrations().retrieveGoogleDomainsForwardingConfig(**params).execute()
    calls:
      - action: projects.locations.registrations.retrieveGoogleDomainsForwardingConfig
        params:
          registration: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        domainForwardings: '{{ response.domainForwardings }}'
        emailForwardings: '{{ response.emailForwardings }}'
    # required_params: ['registration']
