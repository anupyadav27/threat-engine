# ============================================================
# Discovery YAML — firebasedataconnect (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 10 | independent: 6 | dependent: 4
# ops with fields: 10/10 | dep ops with chain: 4/4
# ============================================================
version: '1.0'
provider: gcp
service: firebasedataconnect

services:
  client: firebasedataconnect
  module: "googleapiclient.discovery.build('firebasedataconnect', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Gets information about a location.
  # ── gcp.firebasedataconnect.projects.locations.get ──
  - discovery_id: gcp.firebasedataconnect.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.firebasedataconnect.projects.locations.list ──
  - discovery_id: gcp.firebasedataconnect.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.firebasedataconnect.projects.locations.operations.list ──
  - discovery_id: gcp.firebasedataconnect.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists Connectors in a given project and location.
  # ── gcp.firebasedataconnect.projects.locations.services.connectors.list ──
  - discovery_id: gcp.firebasedataconnect.projects.locations.services.connectors.list
    # python: svc.projects().locations().services().connectors().list(**params).execute()
    calls:
      - action: projects.locations.services.connectors.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.connectors }}'
      item:
        annotations: '{{ item.annotations }}'
        clientCache: '{{ item.clientCache }}'
        createTime: '{{ item.createTime }}'
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        reconciling: '{{ item.reconciling }}'
        source: '{{ item.source }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # Lists Services in a given project and location.
  # ── gcp.firebasedataconnect.projects.locations.services.list ──
  - discovery_id: gcp.firebasedataconnect.projects.locations.services.list
    # python: svc.projects().locations().services().list(**params).execute()
    calls:
      - action: projects.locations.services.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.services }}'
      item:
        annotations: '{{ item.annotations }}'
        createTime: '{{ item.createTime }}'
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        reconciling: '{{ item.reconciling }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # Lists Schemas in a given project and location.
  # ── gcp.firebasedataconnect.projects.locations.services.schemas.list ──
  - discovery_id: gcp.firebasedataconnect.projects.locations.services.schemas.list
    # python: svc.projects().locations().services().schemas().list(**params).execute()
    calls:
      - action: projects.locations.services.schemas.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.schemas }}'
      item:
        annotations: '{{ item.annotations }}'
        createTime: '{{ item.createTime }}'
        datasources: '{{ item.datasources }}'
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        reconciling: '{{ item.reconciling }}'
        source: '{{ item.source }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.firebasedataconnect.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.firebasedataconnect.projects.locations.operations.get
    for_each: gcp.firebasedataconnect.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets details of a single Connector.
  # ── gcp.firebasedataconnect.projects.locations.services.connectors.get [dependent] ──
  - discovery_id: gcp.firebasedataconnect.projects.locations.services.connectors.get
    for_each: gcp.firebasedataconnect.projects.locations.services.connectors.list
    # python: svc.projects().locations().services().connectors().get(**params).execute()
    calls:
      - action: projects.locations.services.connectors.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        clientCache: '{{ response.clientCache }}'
        createTime: '{{ response.createTime }}'
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        reconciling: '{{ response.reconciling }}'
        source: '{{ response.source }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets details of a single Service.
  # ── gcp.firebasedataconnect.projects.locations.services.get [dependent] ──
  - discovery_id: gcp.firebasedataconnect.projects.locations.services.get
    for_each: gcp.firebasedataconnect.projects.locations.services.list
    # python: svc.projects().locations().services().get(**params).execute()
    calls:
      - action: projects.locations.services.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        createTime: '{{ response.createTime }}'
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        reconciling: '{{ response.reconciling }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets details of a single Schema.
  # ── gcp.firebasedataconnect.projects.locations.services.schemas.get [dependent] ──
  - discovery_id: gcp.firebasedataconnect.projects.locations.services.schemas.get
    for_each: gcp.firebasedataconnect.projects.locations.services.schemas.list
    # python: svc.projects().locations().services().schemas().get(**params).execute()
    calls:
      - action: projects.locations.services.schemas.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        createTime: '{{ response.createTime }}'
        datasources: '{{ response.datasources }}'
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        reconciling: '{{ response.reconciling }}'
        source: '{{ response.source }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
