# ============================================================
# Discovery YAML — config (v1)
# Generated: 2026-02-21T03:06:39Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 21 | independent: 11 | dependent: 10
# ops with fields: 21/21 | dep ops with chain: 10/10
# ============================================================
version: '1.0'
provider: gcp
service: config

services:
  client: config
  module: "googleapiclient.discovery.build('config', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists Deployments in a given project and location.
  # ── gcp.config.projects.locations.deployments.list ──
  - discovery_id: gcp.config.projects.locations.deployments.list
    # python: svc.projects().locations().deployments().list(**params).execute()
    calls:
      - action: projects.locations.deployments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.deployments }}'
      item:
        annotations: '{{ item.annotations }}'
        artifactsGcsBucket: '{{ item.artifactsGcsBucket }}'
        createTime: '{{ item.createTime }}'
        deleteBuild: '{{ item.deleteBuild }}'
        deleteLogs: '{{ item.deleteLogs }}'
        deleteResults: '{{ item.deleteResults }}'
        errorCode: '{{ item.errorCode }}'
        errorLogs: '{{ item.errorLogs }}'
        importExistingResources: '{{ item.importExistingResources }}'
        labels: '{{ item.labels }}'
        latestRevision: '{{ item.latestRevision }}'
        lockState: '{{ item.lockState }}'
        name: '{{ item.name }}'
        providerConfig: '{{ item.providerConfig }}'
        quotaValidation: '{{ item.quotaValidation }}'
        serviceAccount: '{{ item.serviceAccount }}'
        state: '{{ item.state }}'
        stateDetail: '{{ item.stateDetail }}'
        terraformBlueprint: '{{ item.terraformBlueprint }}'
        tfErrors: '{{ item.tfErrors }}'
        tfVersion: '{{ item.tfVersion }}'
        tfVersionConstraint: '{{ item.tfVersionConstraint }}'
        updateTime: '{{ item.updateTime }}'
        workerPool: '{{ item.workerPool }}'
  # Lists Revisions of a deployment.
  # ── gcp.config.projects.locations.deployments.revisions.list ──
  - discovery_id: gcp.config.projects.locations.deployments.revisions.list
    # python: svc.projects().locations().deployments().revisions().list(**params).execute()
    calls:
      - action: projects.locations.deployments.revisions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.revisions }}'
      item:
        action: '{{ item.action }}'
        applyResults: '{{ item.applyResults }}'
        build: '{{ item.build }}'
        createTime: '{{ item.createTime }}'
        errorCode: '{{ item.errorCode }}'
        errorLogs: '{{ item.errorLogs }}'
        importExistingResources: '{{ item.importExistingResources }}'
        logs: '{{ item.logs }}'
        name: '{{ item.name }}'
        providerConfig: '{{ item.providerConfig }}'
        quotaValidation: '{{ item.quotaValidation }}'
        quotaValidationResults: '{{ item.quotaValidationResults }}'
        serviceAccount: '{{ item.serviceAccount }}'
        state: '{{ item.state }}'
        stateDetail: '{{ item.stateDetail }}'
        terraformBlueprint: '{{ item.terraformBlueprint }}'
        tfErrors: '{{ item.tfErrors }}'
        tfVersion: '{{ item.tfVersion }}'
        tfVersionConstraint: '{{ item.tfVersionConstraint }}'
        updateTime: '{{ item.updateTime }}'
        workerPool: '{{ item.workerPool }}'
  # Lists Resources in a given revision.
  # ── gcp.config.projects.locations.deployments.revisions.resources.list ──
  - discovery_id: gcp.config.projects.locations.deployments.revisions.resources.list
    # python: svc.projects().locations().deployments().revisions().resources().list(**params).execute()
    calls:
      - action: projects.locations.deployments.revisions.resources.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.resources }}'
      item:
        caiAssets: '{{ item.caiAssets }}'
        intent: '{{ item.intent }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
        terraformInfo: '{{ item.terraformInfo }}'
  # Gets information about a location.
  # ── gcp.config.projects.locations.get ──
  - discovery_id: gcp.config.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Get the AutoMigrationConfig for a given project and location.
  # ── gcp.config.projects.locations.getAutoMigrationConfig ──
  - discovery_id: gcp.config.projects.locations.getAutoMigrationConfig
    # python: svc.projects().locations().getAutoMigrationConfig(**params).execute()
    calls:
      - action: projects.locations.getAutoMigrationConfig
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        autoMigrationEnabled: '{{ response.autoMigrationEnabled }}'
        name: '{{ response.name }}'
        updateTime: '{{ response.updateTime }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.config.projects.locations.list ──
  - discovery_id: gcp.config.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.config.projects.locations.operations.list ──
  - discovery_id: gcp.config.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists Previews in a given project and location.
  # ── gcp.config.projects.locations.previews.list ──
  - discovery_id: gcp.config.projects.locations.previews.list
    # python: svc.projects().locations().previews().list(**params).execute()
    calls:
      - action: projects.locations.previews.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.previews }}'
      item:
        annotations: '{{ item.annotations }}'
        artifactsGcsBucket: '{{ item.artifactsGcsBucket }}'
        build: '{{ item.build }}'
        createTime: '{{ item.createTime }}'
        deployment: '{{ item.deployment }}'
        errorCode: '{{ item.errorCode }}'
        errorLogs: '{{ item.errorLogs }}'
        errorStatus: '{{ item.errorStatus }}'
        labels: '{{ item.labels }}'
        logs: '{{ item.logs }}'
        name: '{{ item.name }}'
        previewArtifacts: '{{ item.previewArtifacts }}'
        previewMode: '{{ item.previewMode }}'
        providerConfig: '{{ item.providerConfig }}'
        serviceAccount: '{{ item.serviceAccount }}'
        state: '{{ item.state }}'
        terraformBlueprint: '{{ item.terraformBlueprint }}'
        tfErrors: '{{ item.tfErrors }}'
        tfVersion: '{{ item.tfVersion }}'
        tfVersionConstraint: '{{ item.tfVersionConstraint }}'
        workerPool: '{{ item.workerPool }}'
  # Lists ResourceChanges for a given preview.
  # ── gcp.config.projects.locations.previews.resourceChanges.list ──
  - discovery_id: gcp.config.projects.locations.previews.resourceChanges.list
    # python: svc.projects().locations().previews().resourceChanges().list(**params).execute()
    calls:
      - action: projects.locations.previews.resourceChanges.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.resourceChanges }}'
      item:
        intent: '{{ item.intent }}'
        name: '{{ item.name }}'
        propertyChanges: '{{ item.propertyChanges }}'
        terraformInfo: '{{ item.terraformInfo }}'
  # List ResourceDrifts for a given preview.
  # ── gcp.config.projects.locations.previews.resourceDrifts.list ──
  - discovery_id: gcp.config.projects.locations.previews.resourceDrifts.list
    # python: svc.projects().locations().previews().resourceDrifts().list(**params).execute()
    calls:
      - action: projects.locations.previews.resourceDrifts.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.resourceDrifts }}'
      item:
        name: '{{ item.name }}'
        propertyDrifts: '{{ item.propertyDrifts }}'
        terraformInfo: '{{ item.terraformInfo }}'
  # Lists TerraformVersions in a given project and location.
  # ── gcp.config.projects.locations.terraformVersions.list ──
  - discovery_id: gcp.config.projects.locations.terraformVersions.list
    # python: svc.projects().locations().terraformVersions().list(**params).execute()
    calls:
      - action: projects.locations.terraformVersions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.terraformVersions }}'
      item:
        deprecateTime: '{{ item.deprecateTime }}'
        name: '{{ item.name }}'
        obsoleteTime: '{{ item.obsoleteTime }}'
        state: '{{ item.state }}'
        supportTime: '{{ item.supportTime }}'

  # ════ DEPENDENT (enrich) operations ════
  # Exports the lock info on a locked deployment.
  # ── gcp.config.projects.locations.deployments.exportLock [dependent] ──
  - discovery_id: gcp.config.projects.locations.deployments.exportLock
    for_each: gcp.config.projects.locations.deployments.list
    # python: svc.projects().locations().deployments().exportLock(**params).execute()
    calls:
      - action: projects.locations.deployments.exportLock
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        info: '{{ response.info }}'
        lockId: '{{ response.lockId }}'
        operation: '{{ response.operation }}'
        version: '{{ response.version }}'
        who: '{{ response.who }}'
    # required_params: ['name']
  # Gets details about a Deployment.
  # ── gcp.config.projects.locations.deployments.get [dependent] ──
  - discovery_id: gcp.config.projects.locations.deployments.get
    for_each: gcp.config.projects.locations.deployments.list
    # python: svc.projects().locations().deployments().get(**params).execute()
    calls:
      - action: projects.locations.deployments.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        artifactsGcsBucket: '{{ response.artifactsGcsBucket }}'
        createTime: '{{ response.createTime }}'
        deleteBuild: '{{ response.deleteBuild }}'
        deleteLogs: '{{ response.deleteLogs }}'
        deleteResults: '{{ response.deleteResults }}'
        errorCode: '{{ response.errorCode }}'
        errorLogs: '{{ response.errorLogs }}'
        importExistingResources: '{{ response.importExistingResources }}'
        labels: '{{ response.labels }}'
        latestRevision: '{{ response.latestRevision }}'
        lockState: '{{ response.lockState }}'
        name: '{{ response.name }}'
        providerConfig: '{{ response.providerConfig }}'
        quotaValidation: '{{ response.quotaValidation }}'
        serviceAccount: '{{ response.serviceAccount }}'
        state: '{{ response.state }}'
        stateDetail: '{{ response.stateDetail }}'
        terraformBlueprint: '{{ response.terraformBlueprint }}'
        tfErrors: '{{ response.tfErrors }}'
        tfVersion: '{{ response.tfVersion }}'
        tfVersionConstraint: '{{ response.tfVersionConstraint }}'
        updateTime: '{{ response.updateTime }}'
        workerPool: '{{ response.workerPool }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.config.projects.locations.deployments.getIamPolicy [dependent] ──
  - discovery_id: gcp.config.projects.locations.deployments.getIamPolicy
    for_each: gcp.config.projects.locations.deployments.list
    # python: svc.projects().locations().deployments().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.deployments.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Gets details about a Revision.
  # ── gcp.config.projects.locations.deployments.revisions.get [dependent] ──
  - discovery_id: gcp.config.projects.locations.deployments.revisions.get
    for_each: gcp.config.projects.locations.deployments.revisions.list
    # python: svc.projects().locations().deployments().revisions().get(**params).execute()
    calls:
      - action: projects.locations.deployments.revisions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        action: '{{ response.action }}'
        applyResults: '{{ response.applyResults }}'
        build: '{{ response.build }}'
        createTime: '{{ response.createTime }}'
        errorCode: '{{ response.errorCode }}'
        errorLogs: '{{ response.errorLogs }}'
        importExistingResources: '{{ response.importExistingResources }}'
        logs: '{{ response.logs }}'
        name: '{{ response.name }}'
        providerConfig: '{{ response.providerConfig }}'
        quotaValidation: '{{ response.quotaValidation }}'
        quotaValidationResults: '{{ response.quotaValidationResults }}'
        serviceAccount: '{{ response.serviceAccount }}'
        state: '{{ response.state }}'
        stateDetail: '{{ response.stateDetail }}'
        terraformBlueprint: '{{ response.terraformBlueprint }}'
        tfErrors: '{{ response.tfErrors }}'
        tfVersion: '{{ response.tfVersion }}'
        tfVersionConstraint: '{{ response.tfVersionConstraint }}'
        updateTime: '{{ response.updateTime }}'
        workerPool: '{{ response.workerPool }}'
    # required_params: ['name']
  # Gets details about a Resource deployed by Infra Manager.
  # ── gcp.config.projects.locations.deployments.revisions.resources.get [dependent] ──
  - discovery_id: gcp.config.projects.locations.deployments.revisions.resources.get
    for_each: gcp.config.projects.locations.deployments.revisions.resources.list
    # python: svc.projects().locations().deployments().revisions().resources().get(**params).execute()
    calls:
      - action: projects.locations.deployments.revisions.resources.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        caiAssets: '{{ response.caiAssets }}'
        intent: '{{ response.intent }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
        terraformInfo: '{{ response.terraformInfo }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.config.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.config.projects.locations.operations.get
    for_each: gcp.config.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets details about a Preview.
  # ── gcp.config.projects.locations.previews.get [dependent] ──
  - discovery_id: gcp.config.projects.locations.previews.get
    for_each: gcp.config.projects.locations.previews.list
    # python: svc.projects().locations().previews().get(**params).execute()
    calls:
      - action: projects.locations.previews.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        artifactsGcsBucket: '{{ response.artifactsGcsBucket }}'
        build: '{{ response.build }}'
        createTime: '{{ response.createTime }}'
        deployment: '{{ response.deployment }}'
        errorCode: '{{ response.errorCode }}'
        errorLogs: '{{ response.errorLogs }}'
        errorStatus: '{{ response.errorStatus }}'
        labels: '{{ response.labels }}'
        logs: '{{ response.logs }}'
        name: '{{ response.name }}'
        previewArtifacts: '{{ response.previewArtifacts }}'
        previewMode: '{{ response.previewMode }}'
        providerConfig: '{{ response.providerConfig }}'
        serviceAccount: '{{ response.serviceAccount }}'
        state: '{{ response.state }}'
        terraformBlueprint: '{{ response.terraformBlueprint }}'
        tfErrors: '{{ response.tfErrors }}'
        tfVersion: '{{ response.tfVersion }}'
        tfVersionConstraint: '{{ response.tfVersionConstraint }}'
        workerPool: '{{ response.workerPool }}'
    # required_params: ['name']
  # Get a ResourceChange for a given preview.
  # ── gcp.config.projects.locations.previews.resourceChanges.get [dependent] ──
  - discovery_id: gcp.config.projects.locations.previews.resourceChanges.get
    for_each: gcp.config.projects.locations.previews.resourceChanges.list
    # python: svc.projects().locations().previews().resourceChanges().get(**params).execute()
    calls:
      - action: projects.locations.previews.resourceChanges.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        intent: '{{ response.intent }}'
        name: '{{ response.name }}'
        propertyChanges: '{{ response.propertyChanges }}'
        terraformInfo: '{{ response.terraformInfo }}'
    # required_params: ['name']
  # Get a ResourceDrift for a given preview.
  # ── gcp.config.projects.locations.previews.resourceDrifts.get [dependent] ──
  - discovery_id: gcp.config.projects.locations.previews.resourceDrifts.get
    for_each: gcp.config.projects.locations.previews.resourceDrifts.list
    # python: svc.projects().locations().previews().resourceDrifts().get(**params).execute()
    calls:
      - action: projects.locations.previews.resourceDrifts.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        propertyDrifts: '{{ response.propertyDrifts }}'
        terraformInfo: '{{ response.terraformInfo }}'
    # required_params: ['name']
  # Gets details about a TerraformVersion.
  # ── gcp.config.projects.locations.terraformVersions.get [dependent] ──
  - discovery_id: gcp.config.projects.locations.terraformVersions.get
    for_each: gcp.config.projects.locations.terraformVersions.list
    # python: svc.projects().locations().terraformVersions().get(**params).execute()
    calls:
      - action: projects.locations.terraformVersions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        deprecateTime: '{{ response.deprecateTime }}'
        name: '{{ response.name }}'
        obsoleteTime: '{{ response.obsoleteTime }}'
        state: '{{ response.state }}'
        supportTime: '{{ response.supportTime }}'
    # required_params: ['name']
