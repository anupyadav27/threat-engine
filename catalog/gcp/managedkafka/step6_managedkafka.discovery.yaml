# ============================================================
# Discovery YAML — managedkafka (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 44 | independent: 23 | dependent: 21
# ops with fields: 31/44 | dep ops with chain: 21/21
# ============================================================
version: '1.0'
provider: gcp
service: managedkafka

services:
  client: managedkafka
  module: "googleapiclient.discovery.build('managedkafka', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists the acls in a given cluster.
  # ── gcp.managedkafka.projects.locations.clusters.acls.list ──
  - discovery_id: gcp.managedkafka.projects.locations.clusters.acls.list
    # python: svc.projects().locations().clusters().acls().list(**params).execute()
    calls:
      - action: projects.locations.clusters.acls.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.acls }}'
      item:
        aclEntries: '{{ item.aclEntries }}'
        name: '{{ item.name }}'
        patternType: '{{ item.patternType }}'
        resourceName: '{{ item.resourceName }}'
        resourceType: '{{ item.resourceType }}'
  # Lists the consumer groups in a given cluster.
  # ── gcp.managedkafka.projects.locations.clusters.consumerGroups.list ──
  - discovery_id: gcp.managedkafka.projects.locations.clusters.consumerGroups.list
    # python: svc.projects().locations().clusters().consumerGroups().list(**params).execute()
    calls:
      - action: projects.locations.clusters.consumerGroups.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.consumerGroups }}'
      item:
        name: '{{ item.name }}'
        topics: '{{ item.topics }}'
  # Lists the clusters in a given project and location.
  # ── gcp.managedkafka.projects.locations.clusters.list ──
  - discovery_id: gcp.managedkafka.projects.locations.clusters.list
    # python: svc.projects().locations().clusters().list(**params).execute()
    calls:
      - action: projects.locations.clusters.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.clusters }}'
      item:
        brokerDetails: '{{ item.brokerDetails }}'
        capacityConfig: '{{ item.capacityConfig }}'
        createTime: '{{ item.createTime }}'
        gcpConfig: '{{ item.gcpConfig }}'
        kafkaVersion: '{{ item.kafkaVersion }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        rebalanceConfig: '{{ item.rebalanceConfig }}'
        satisfiesPzi: '{{ item.satisfiesPzi }}'
        satisfiesPzs: '{{ item.satisfiesPzs }}'
        state: '{{ item.state }}'
        tlsConfig: '{{ item.tlsConfig }}'
        updateOptions: '{{ item.updateOptions }}'
        updateTime: '{{ item.updateTime }}'
  # Lists the topics in a given cluster.
  # ── gcp.managedkafka.projects.locations.clusters.topics.list ──
  - discovery_id: gcp.managedkafka.projects.locations.clusters.topics.list
    # python: svc.projects().locations().clusters().topics().list(**params).execute()
    calls:
      - action: projects.locations.clusters.topics.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.topics }}'
      item:
        configs: '{{ item.configs }}'
        name: '{{ item.name }}'
        partitionCount: '{{ item.partitionCount }}'
        replicationFactor: '{{ item.replicationFactor }}'
  # Lists the connectors in a given Connect cluster.
  # ── gcp.managedkafka.projects.locations.connectClusters.connectors.list ──
  - discovery_id: gcp.managedkafka.projects.locations.connectClusters.connectors.list
    # python: svc.projects().locations().connectClusters().connectors().list(**params).execute()
    calls:
      - action: projects.locations.connectClusters.connectors.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.connectors }}'
      item:
        configs: '{{ item.configs }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
        taskRestartPolicy: '{{ item.taskRestartPolicy }}'
  # Lists the Kafka Connect clusters in a given project and location.
  # ── gcp.managedkafka.projects.locations.connectClusters.list ──
  - discovery_id: gcp.managedkafka.projects.locations.connectClusters.list
    # python: svc.projects().locations().connectClusters().list(**params).execute()
    calls:
      - action: projects.locations.connectClusters.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.connectClusters }}'
      item:
        capacityConfig: '{{ item.capacityConfig }}'
        config: '{{ item.config }}'
        createTime: '{{ item.createTime }}'
        gcpConfig: '{{ item.gcpConfig }}'
        kafkaCluster: '{{ item.kafkaCluster }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        satisfiesPzi: '{{ item.satisfiesPzi }}'
        satisfiesPzs: '{{ item.satisfiesPzs }}'
        state: '{{ item.state }}'
        updateTime: '{{ item.updateTime }}'
  # Gets information about a location.
  # ── gcp.managedkafka.projects.locations.get ──
  - discovery_id: gcp.managedkafka.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.managedkafka.projects.locations.list ──
  - discovery_id: gcp.managedkafka.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.managedkafka.projects.locations.operations.list ──
  - discovery_id: gcp.managedkafka.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # List contexts for a schema registry.
  # ── gcp.managedkafka.projects.locations.schemaRegistries.contexts.list ──
  - discovery_id: gcp.managedkafka.projects.locations.schemaRegistries.contexts.list
    # python: svc.projects().locations().schemaRegistries().contexts().list(**params).execute()
    calls:
      - action: projects.locations.schemaRegistries.contexts.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.contexts }}'
  # List subjects which reference a particular schema id. The response will be an array of subject names
  # ── gcp.managedkafka.projects.locations.schemaRegistries.contexts.schemas.subjects.list ──
  - discovery_id: gcp.managedkafka.projects.locations.schemaRegistries.contexts.schemas.subjects.list
    # python: svc.projects().locations().schemaRegistries().contexts().schemas().subjects().list(**params).execute()
    calls:
      - action: projects.locations.schemaRegistries.contexts.schemas.subjects.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.subjects }}'
  # List the supported schema types. The response will be an array of schema types.
  # ── gcp.managedkafka.projects.locations.schemaRegistries.contexts.schemas.types.list ──
  - discovery_id: gcp.managedkafka.projects.locations.schemaRegistries.contexts.schemas.types.list
    # python: svc.projects().locations().schemaRegistries().contexts().schemas().types().list(**params).execute()
    calls:
      - action: projects.locations.schemaRegistries.contexts.schemas.types.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.types }}'
  # List the schema versions for the given schema id. The response will be an array of subject-version p
  # ── gcp.managedkafka.projects.locations.schemaRegistries.contexts.schemas.versions.list ──
  - discovery_id: gcp.managedkafka.projects.locations.schemaRegistries.contexts.schemas.versions.list
    # python: svc.projects().locations().schemaRegistries().contexts().schemas().versions().list(**params).execute()
    calls:
      - action: projects.locations.schemaRegistries.contexts.schemas.versions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.versions }}'
  # List subjects in the schema registry. The response will be an array of subject names.
  # ── gcp.managedkafka.projects.locations.schemaRegistries.contexts.subjects.list ──
  - discovery_id: gcp.managedkafka.projects.locations.schemaRegistries.contexts.subjects.list
    # python: svc.projects().locations().schemaRegistries().contexts().subjects().list(**params).execute()
    calls:
      - action: projects.locations.schemaRegistries.contexts.subjects.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.subjects }}'
  # Get all versions of a subject. The response will be an array of versions of the subject.
  # ── gcp.managedkafka.projects.locations.schemaRegistries.contexts.subjects.versions.list ──
  - discovery_id: gcp.managedkafka.projects.locations.schemaRegistries.contexts.subjects.versions.list
    # python: svc.projects().locations().schemaRegistries().contexts().subjects().versions().list(**params).execute()
    calls:
      - action: projects.locations.schemaRegistries.contexts.subjects.versions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.versions }}'
  # Get a list of IDs of schemas that reference the schema with the given subject and version.
  # ── gcp.managedkafka.projects.locations.schemaRegistries.contexts.subjects.versions.referencedby.list ──
  - discovery_id: gcp.managedkafka.projects.locations.schemaRegistries.contexts.subjects.versions.referencedby.list
    # python: svc.projects().locations().schemaRegistries().contexts().subjects().versions().referencedby().list(**params).execute()
    calls:
      - action: projects.locations.schemaRegistries.contexts.subjects.versions.referencedby.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.referencedby }}'
  # List schema registries.
  # ── gcp.managedkafka.projects.locations.schemaRegistries.list ──
  - discovery_id: gcp.managedkafka.projects.locations.schemaRegistries.list
    # python: svc.projects().locations().schemaRegistries().list(**params).execute()
    calls:
      - action: projects.locations.schemaRegistries.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.schemaRegistries }}'
      item:
        contexts: '{{ item.contexts }}'
        name: '{{ item.name }}'
  # List subjects which reference a particular schema id. The response will be an array of subject names
  # ── gcp.managedkafka.projects.locations.schemaRegistries.schemas.subjects.list ──
  - discovery_id: gcp.managedkafka.projects.locations.schemaRegistries.schemas.subjects.list
    # python: svc.projects().locations().schemaRegistries().schemas().subjects().list(**params).execute()
    calls:
      - action: projects.locations.schemaRegistries.schemas.subjects.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.subjects }}'
  # List the supported schema types. The response will be an array of schema types.
  # ── gcp.managedkafka.projects.locations.schemaRegistries.schemas.types.list ──
  - discovery_id: gcp.managedkafka.projects.locations.schemaRegistries.schemas.types.list
    # python: svc.projects().locations().schemaRegistries().schemas().types().list(**params).execute()
    calls:
      - action: projects.locations.schemaRegistries.schemas.types.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.types }}'
  # List the schema versions for the given schema id. The response will be an array of subject-version p
  # ── gcp.managedkafka.projects.locations.schemaRegistries.schemas.versions.list ──
  - discovery_id: gcp.managedkafka.projects.locations.schemaRegistries.schemas.versions.list
    # python: svc.projects().locations().schemaRegistries().schemas().versions().list(**params).execute()
    calls:
      - action: projects.locations.schemaRegistries.schemas.versions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.versions }}'
  # List subjects in the schema registry. The response will be an array of subject names.
  # ── gcp.managedkafka.projects.locations.schemaRegistries.subjects.list ──
  - discovery_id: gcp.managedkafka.projects.locations.schemaRegistries.subjects.list
    # python: svc.projects().locations().schemaRegistries().subjects().list(**params).execute()
    calls:
      - action: projects.locations.schemaRegistries.subjects.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.subjects }}'
  # Get all versions of a subject. The response will be an array of versions of the subject.
  # ── gcp.managedkafka.projects.locations.schemaRegistries.subjects.versions.list ──
  - discovery_id: gcp.managedkafka.projects.locations.schemaRegistries.subjects.versions.list
    # python: svc.projects().locations().schemaRegistries().subjects().versions().list(**params).execute()
    calls:
      - action: projects.locations.schemaRegistries.subjects.versions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.versions }}'
  # Get a list of IDs of schemas that reference the schema with the given subject and version.
  # ── gcp.managedkafka.projects.locations.schemaRegistries.subjects.versions.referencedby.list ──
  - discovery_id: gcp.managedkafka.projects.locations.schemaRegistries.subjects.versions.referencedby.list
    # python: svc.projects().locations().schemaRegistries().subjects().versions().referencedby().list(**params).execute()
    calls:
      - action: projects.locations.schemaRegistries.subjects.versions.referencedby.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.referencedby }}'

  # ════ DEPENDENT (enrich) operations ════
  # Returns the properties of a single acl.
  # ── gcp.managedkafka.projects.locations.clusters.acls.get [dependent] ──
  - discovery_id: gcp.managedkafka.projects.locations.clusters.acls.get
    for_each: gcp.managedkafka.projects.locations.clusters.acls.list
    # python: svc.projects().locations().clusters().acls().get(**params).execute()
    calls:
      - action: projects.locations.clusters.acls.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        aclEntries: '{{ response.aclEntries }}'
        name: '{{ response.name }}'
        patternType: '{{ response.patternType }}'
        resourceName: '{{ response.resourceName }}'
        resourceType: '{{ response.resourceType }}'
    # required_params: ['name']
  # Returns the properties of a single consumer group.
  # ── gcp.managedkafka.projects.locations.clusters.consumerGroups.get [dependent] ──
  - discovery_id: gcp.managedkafka.projects.locations.clusters.consumerGroups.get
    for_each: gcp.managedkafka.projects.locations.clusters.consumerGroups.list
    # python: svc.projects().locations().clusters().consumerGroups().get(**params).execute()
    calls:
      - action: projects.locations.clusters.consumerGroups.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        topics: '{{ response.topics }}'
    # required_params: ['name']
  # Returns the properties of a single cluster.
  # ── gcp.managedkafka.projects.locations.clusters.get [dependent] ──
  - discovery_id: gcp.managedkafka.projects.locations.clusters.get
    for_each: gcp.managedkafka.projects.locations.clusters.list
    # python: svc.projects().locations().clusters().get(**params).execute()
    calls:
      - action: projects.locations.clusters.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        brokerDetails: '{{ response.brokerDetails }}'
        capacityConfig: '{{ response.capacityConfig }}'
        createTime: '{{ response.createTime }}'
        gcpConfig: '{{ response.gcpConfig }}'
        kafkaVersion: '{{ response.kafkaVersion }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        rebalanceConfig: '{{ response.rebalanceConfig }}'
        satisfiesPzi: '{{ response.satisfiesPzi }}'
        satisfiesPzs: '{{ response.satisfiesPzs }}'
        state: '{{ response.state }}'
        tlsConfig: '{{ response.tlsConfig }}'
        updateOptions: '{{ response.updateOptions }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Returns the properties of a single topic.
  # ── gcp.managedkafka.projects.locations.clusters.topics.get [dependent] ──
  - discovery_id: gcp.managedkafka.projects.locations.clusters.topics.get
    for_each: gcp.managedkafka.projects.locations.clusters.topics.list
    # python: svc.projects().locations().clusters().topics().get(**params).execute()
    calls:
      - action: projects.locations.clusters.topics.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        configs: '{{ response.configs }}'
        name: '{{ response.name }}'
        partitionCount: '{{ response.partitionCount }}'
        replicationFactor: '{{ response.replicationFactor }}'
    # required_params: ['name']
  # Returns the properties of a single connector.
  # ── gcp.managedkafka.projects.locations.connectClusters.connectors.get [dependent] ──
  - discovery_id: gcp.managedkafka.projects.locations.connectClusters.connectors.get
    for_each: gcp.managedkafka.projects.locations.connectClusters.connectors.list
    # python: svc.projects().locations().connectClusters().connectors().get(**params).execute()
    calls:
      - action: projects.locations.connectClusters.connectors.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        configs: '{{ response.configs }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
        taskRestartPolicy: '{{ response.taskRestartPolicy }}'
    # required_params: ['name']
  # Returns the properties of a single Kafka Connect cluster.
  # ── gcp.managedkafka.projects.locations.connectClusters.get [dependent] ──
  - discovery_id: gcp.managedkafka.projects.locations.connectClusters.get
    for_each: gcp.managedkafka.projects.locations.connectClusters.list
    # python: svc.projects().locations().connectClusters().get(**params).execute()
    calls:
      - action: projects.locations.connectClusters.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        capacityConfig: '{{ response.capacityConfig }}'
        config: '{{ response.config }}'
        createTime: '{{ response.createTime }}'
        gcpConfig: '{{ response.gcpConfig }}'
        kafkaCluster: '{{ response.kafkaCluster }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        satisfiesPzi: '{{ response.satisfiesPzi }}'
        satisfiesPzs: '{{ response.satisfiesPzs }}'
        state: '{{ response.state }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.managedkafka.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.managedkafka.projects.locations.operations.get
    for_each: gcp.managedkafka.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Get schema config at global level or for a subject.
  # ── gcp.managedkafka.projects.locations.schemaRegistries.config.get [dependent] ──
  - discovery_id: gcp.managedkafka.projects.locations.schemaRegistries.config.get
    for_each: gcp.managedkafka.projects.locations.clusters.list
    # python: svc.projects().locations().schemaRegistries().config().get(**params).execute()
    calls:
      - action: projects.locations.schemaRegistries.config.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        alias: '{{ response.alias }}'
        compatibility: '{{ response.compatibility }}'
        normalize: '{{ response.normalize }}'
    # required_params: ['name']
  # Get schema config at global level or for a subject.
  # ── gcp.managedkafka.projects.locations.schemaRegistries.contexts.config.get [dependent] ──
  - discovery_id: gcp.managedkafka.projects.locations.schemaRegistries.contexts.config.get
    for_each: gcp.managedkafka.projects.locations.clusters.list
    # python: svc.projects().locations().schemaRegistries().contexts().config().get(**params).execute()
    calls:
      - action: projects.locations.schemaRegistries.contexts.config.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        alias: '{{ response.alias }}'
        compatibility: '{{ response.compatibility }}'
        normalize: '{{ response.normalize }}'
    # required_params: ['name']
  # Get the context.
  # ── gcp.managedkafka.projects.locations.schemaRegistries.contexts.get [dependent] ──
  - discovery_id: gcp.managedkafka.projects.locations.schemaRegistries.contexts.get
    for_each: gcp.managedkafka.projects.locations.schemaRegistries.contexts.list
    # python: svc.projects().locations().schemaRegistries().contexts().get(**params).execute()
    calls:
      - action: projects.locations.schemaRegistries.contexts.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        subjects: '{{ response.subjects }}'
    # required_params: ['name']
  # Get mode at global level or for a subject.
  # ── gcp.managedkafka.projects.locations.schemaRegistries.contexts.mode.get [dependent] ──
  - discovery_id: gcp.managedkafka.projects.locations.schemaRegistries.contexts.mode.get
    for_each: gcp.managedkafka.projects.locations.schemaRegistries.subjects.list
    # python: svc.projects().locations().schemaRegistries().contexts().mode().get(**params).execute()
    calls:
      - action: projects.locations.schemaRegistries.contexts.mode.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        mode: '{{ response.mode }}'
    # required_params: ['name']
  # Get the schema for the given schema id.
  # ── gcp.managedkafka.projects.locations.schemaRegistries.contexts.schemas.get [dependent] ──
  - discovery_id: gcp.managedkafka.projects.locations.schemaRegistries.contexts.schemas.get
    for_each: gcp.managedkafka.projects.locations.schemaRegistries.subjects.list
    # python: svc.projects().locations().schemaRegistries().contexts().schemas().get(**params).execute()
    calls:
      - action: projects.locations.schemaRegistries.contexts.schemas.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        references: '{{ response.references }}'
        schema: '{{ response.schema }}'
        schemaType: '{{ response.schemaType }}'
    # required_params: ['name']
  # Get the schema string for the given schema id. The response will be the schema string.
  # ── gcp.managedkafka.projects.locations.schemaRegistries.contexts.schemas.getSchema [dependent] ──
  - discovery_id: gcp.managedkafka.projects.locations.schemaRegistries.contexts.schemas.getSchema
    for_each: gcp.managedkafka.projects.locations.schemaRegistries.subjects.list
    # python: svc.projects().locations().schemaRegistries().contexts().schemas().getSchema(**params).execute()
    calls:
      - action: projects.locations.schemaRegistries.contexts.schemas.getSchema
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['name']
  # Get a versioned schema (schema with subject/version) of a subject.
  # ── gcp.managedkafka.projects.locations.schemaRegistries.contexts.subjects.versions.get [dependent] ──
  - discovery_id: gcp.managedkafka.projects.locations.schemaRegistries.contexts.subjects.versions.get
    for_each: gcp.managedkafka.projects.locations.schemaRegistries.subjects.versions.list
    # python: svc.projects().locations().schemaRegistries().contexts().subjects().versions().get(**params).execute()
    calls:
      - action: projects.locations.schemaRegistries.contexts.subjects.versions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        id: '{{ response.id }}'
        references: '{{ response.references }}'
        schema: '{{ response.schema }}'
        schemaType: '{{ response.schemaType }}'
        subject: '{{ response.subject }}'
        version: '{{ response.version }}'
    # required_params: ['name']
  # Get the schema string only for a version of a subject. The response will be the schema string.
  # ── gcp.managedkafka.projects.locations.schemaRegistries.contexts.subjects.versions.getSchema [dependent] ──
  - discovery_id: gcp.managedkafka.projects.locations.schemaRegistries.contexts.subjects.versions.getSchema
    for_each: gcp.managedkafka.projects.locations.schemaRegistries.subjects.versions.list
    # python: svc.projects().locations().schemaRegistries().contexts().subjects().versions().getSchema(**params).execute()
    calls:
      - action: projects.locations.schemaRegistries.contexts.subjects.versions.getSchema
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['name']
  # Get the schema registry instance.
  # ── gcp.managedkafka.projects.locations.schemaRegistries.get [dependent] ──
  - discovery_id: gcp.managedkafka.projects.locations.schemaRegistries.get
    for_each: gcp.managedkafka.projects.locations.schemaRegistries.list
    # python: svc.projects().locations().schemaRegistries().get(**params).execute()
    calls:
      - action: projects.locations.schemaRegistries.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contexts: '{{ response.contexts }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Get mode at global level or for a subject.
  # ── gcp.managedkafka.projects.locations.schemaRegistries.mode.get [dependent] ──
  - discovery_id: gcp.managedkafka.projects.locations.schemaRegistries.mode.get
    for_each: gcp.managedkafka.projects.locations.schemaRegistries.subjects.list
    # python: svc.projects().locations().schemaRegistries().mode().get(**params).execute()
    calls:
      - action: projects.locations.schemaRegistries.mode.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        mode: '{{ response.mode }}'
    # required_params: ['name']
  # Get the schema for the given schema id.
  # ── gcp.managedkafka.projects.locations.schemaRegistries.schemas.get [dependent] ──
  - discovery_id: gcp.managedkafka.projects.locations.schemaRegistries.schemas.get
    for_each: gcp.managedkafka.projects.locations.schemaRegistries.subjects.list
    # python: svc.projects().locations().schemaRegistries().schemas().get(**params).execute()
    calls:
      - action: projects.locations.schemaRegistries.schemas.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        references: '{{ response.references }}'
        schema: '{{ response.schema }}'
        schemaType: '{{ response.schemaType }}'
    # required_params: ['name']
  # Get the schema string for the given schema id. The response will be the schema string.
  # ── gcp.managedkafka.projects.locations.schemaRegistries.schemas.getSchema [dependent] ──
  - discovery_id: gcp.managedkafka.projects.locations.schemaRegistries.schemas.getSchema
    for_each: gcp.managedkafka.projects.locations.schemaRegistries.subjects.list
    # python: svc.projects().locations().schemaRegistries().schemas().getSchema(**params).execute()
    calls:
      - action: projects.locations.schemaRegistries.schemas.getSchema
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['name']
  # Get a versioned schema (schema with subject/version) of a subject.
  # ── gcp.managedkafka.projects.locations.schemaRegistries.subjects.versions.get [dependent] ──
  - discovery_id: gcp.managedkafka.projects.locations.schemaRegistries.subjects.versions.get
    for_each: gcp.managedkafka.projects.locations.schemaRegistries.subjects.versions.list
    # python: svc.projects().locations().schemaRegistries().subjects().versions().get(**params).execute()
    calls:
      - action: projects.locations.schemaRegistries.subjects.versions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        id: '{{ response.id }}'
        references: '{{ response.references }}'
        schema: '{{ response.schema }}'
        schemaType: '{{ response.schemaType }}'
        subject: '{{ response.subject }}'
        version: '{{ response.version }}'
    # required_params: ['name']
  # Get the schema string only for a version of a subject. The response will be the schema string.
  # ── gcp.managedkafka.projects.locations.schemaRegistries.subjects.versions.getSchema [dependent] ──
  - discovery_id: gcp.managedkafka.projects.locations.schemaRegistries.subjects.versions.getSchema
    for_each: gcp.managedkafka.projects.locations.schemaRegistries.subjects.versions.list
    # python: svc.projects().locations().schemaRegistries().subjects().versions().getSchema(**params).execute()
    calls:
      - action: projects.locations.schemaRegistries.subjects.versions.getSchema
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['name']
