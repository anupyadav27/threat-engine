# ============================================================
# Discovery YAML — managedidentities (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 16 | independent: 7 | dependent: 9
# ops with fields: 16/16 | dep ops with chain: 9/9
# ============================================================
version: '1.0'
provider: gcp
service: managedidentities

services:
  client: managedidentities
  module: "googleapiclient.discovery.build('managedidentities', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Gets information about a location.
  # ── gcp.managedidentities.projects.locations.get ──
  - discovery_id: gcp.managedidentities.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists Backup in a given project.
  # ── gcp.managedidentities.projects.locations.global.domains.backups.list ──
  - discovery_id: gcp.managedidentities.projects.locations.global.domains.backups.list
    # python: svc.projects().locations().global().domains().backups().list(**params).execute()
    calls:
      - action: projects.locations.global.domains.backups.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.backups }}'
      item:
        createTime: '{{ item.createTime }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
        statusMessage: '{{ item.statusMessage }}'
        type: '{{ item.type }}'
        updateTime: '{{ item.updateTime }}'
  # Lists domains in a project.
  # ── gcp.managedidentities.projects.locations.global.domains.list ──
  - discovery_id: gcp.managedidentities.projects.locations.global.domains.list
    # python: svc.projects().locations().global().domains().list(**params).execute()
    calls:
      - action: projects.locations.global.domains.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.domains }}'
      item:
        admin: '{{ item.admin }}'
        auditLogsEnabled: '{{ item.auditLogsEnabled }}'
        authorizedNetworks: '{{ item.authorizedNetworks }}'
        createTime: '{{ item.createTime }}'
        fqdn: '{{ item.fqdn }}'
        labels: '{{ item.labels }}'
        locations: '{{ item.locations }}'
        name: '{{ item.name }}'
        reservedIpRange: '{{ item.reservedIpRange }}'
        state: '{{ item.state }}'
        statusMessage: '{{ item.statusMessage }}'
        trusts: '{{ item.trusts }}'
        updateTime: '{{ item.updateTime }}'
  # Lists SqlIntegrations in a given domain.
  # ── gcp.managedidentities.projects.locations.global.domains.sqlIntegrations.list ──
  - discovery_id: gcp.managedidentities.projects.locations.global.domains.sqlIntegrations.list
    # python: svc.projects().locations().global().domains().sqlIntegrations().list(**params).execute()
    calls:
      - action: projects.locations.global.domains.sqlIntegrations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.sqlIntegrations }}'
      item:
        createTime: '{{ item.createTime }}'
        name: '{{ item.name }}'
        sqlInstance: '{{ item.sqlInstance }}'
        state: '{{ item.state }}'
        updateTime: '{{ item.updateTime }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.managedidentities.projects.locations.global.operations.list ──
  - discovery_id: gcp.managedidentities.projects.locations.global.operations.list
    # python: svc.projects().locations().global().operations().list(**params).execute()
    calls:
      - action: projects.locations.global.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists Peerings in a given project.
  # ── gcp.managedidentities.projects.locations.global.peerings.list ──
  - discovery_id: gcp.managedidentities.projects.locations.global.peerings.list
    # python: svc.projects().locations().global().peerings().list(**params).execute()
    calls:
      - action: projects.locations.global.peerings.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.peerings }}'
      item:
        authorizedNetwork: '{{ item.authorizedNetwork }}'
        createTime: '{{ item.createTime }}'
        domainResource: '{{ item.domainResource }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
        statusMessage: '{{ item.statusMessage }}'
        updateTime: '{{ item.updateTime }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.managedidentities.projects.locations.list ──
  - discovery_id: gcp.managedidentities.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets details of a single Backup.
  # ── gcp.managedidentities.projects.locations.global.domains.backups.get [dependent] ──
  - discovery_id: gcp.managedidentities.projects.locations.global.domains.backups.get
    for_each: gcp.managedidentities.projects.locations.global.domains.backups.list
    # python: svc.projects().locations().global().domains().backups().get(**params).execute()
    calls:
      - action: projects.locations.global.domains.backups.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
        statusMessage: '{{ response.statusMessage }}'
        type: '{{ response.type }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.managedidentities.projects.locations.global.domains.backups.getIamPolicy [dependent] ──
  - discovery_id: gcp.managedidentities.projects.locations.global.domains.backups.getIamPolicy
    for_each: gcp.managedidentities.projects.locations.global.domains.backups.list
    # python: svc.projects().locations().global().domains().backups().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.global.domains.backups.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Gets information about a domain.
  # ── gcp.managedidentities.projects.locations.global.domains.get [dependent] ──
  - discovery_id: gcp.managedidentities.projects.locations.global.domains.get
    for_each: gcp.managedidentities.projects.locations.global.domains.list
    # python: svc.projects().locations().global().domains().get(**params).execute()
    calls:
      - action: projects.locations.global.domains.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        admin: '{{ response.admin }}'
        auditLogsEnabled: '{{ response.auditLogsEnabled }}'
        authorizedNetworks: '{{ response.authorizedNetworks }}'
        createTime: '{{ response.createTime }}'
        fqdn: '{{ response.fqdn }}'
        labels: '{{ response.labels }}'
        locations: '{{ response.locations }}'
        name: '{{ response.name }}'
        reservedIpRange: '{{ response.reservedIpRange }}'
        state: '{{ response.state }}'
        statusMessage: '{{ response.statusMessage }}'
        trusts: '{{ response.trusts }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.managedidentities.projects.locations.global.domains.getIamPolicy [dependent] ──
  - discovery_id: gcp.managedidentities.projects.locations.global.domains.getIamPolicy
    for_each: gcp.managedidentities.projects.locations.global.domains.list
    # python: svc.projects().locations().global().domains().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.global.domains.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Gets the domain ldaps settings.
  # ── gcp.managedidentities.projects.locations.global.domains.getLdapssettings [dependent] ──
  - discovery_id: gcp.managedidentities.projects.locations.global.domains.getLdapssettings
    for_each: gcp.managedidentities.projects.locations.global.domains.list
    # python: svc.projects().locations().global().domains().getLdapssettings(**params).execute()
    calls:
      - action: projects.locations.global.domains.getLdapssettings
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        certificate: '{{ response.certificate }}'
        certificatePassword: '{{ response.certificatePassword }}'
        certificatePfx: '{{ response.certificatePfx }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets details of a single sqlIntegration.
  # ── gcp.managedidentities.projects.locations.global.domains.sqlIntegrations.get [dependent] ──
  - discovery_id: gcp.managedidentities.projects.locations.global.domains.sqlIntegrations.get
    for_each: gcp.managedidentities.projects.locations.global.domains.sqlIntegrations.list
    # python: svc.projects().locations().global().domains().sqlIntegrations().get(**params).execute()
    calls:
      - action: projects.locations.global.domains.sqlIntegrations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        name: '{{ response.name }}'
        sqlInstance: '{{ response.sqlInstance }}'
        state: '{{ response.state }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.managedidentities.projects.locations.global.operations.get [dependent] ──
  - discovery_id: gcp.managedidentities.projects.locations.global.operations.get
    for_each: gcp.managedidentities.projects.locations.global.operations.list
    # python: svc.projects().locations().global().operations().get(**params).execute()
    calls:
      - action: projects.locations.global.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets details of a single Peering.
  # ── gcp.managedidentities.projects.locations.global.peerings.get [dependent] ──
  - discovery_id: gcp.managedidentities.projects.locations.global.peerings.get
    for_each: gcp.managedidentities.projects.locations.global.peerings.list
    # python: svc.projects().locations().global().peerings().get(**params).execute()
    calls:
      - action: projects.locations.global.peerings.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        authorizedNetwork: '{{ response.authorizedNetwork }}'
        createTime: '{{ response.createTime }}'
        domainResource: '{{ response.domainResource }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
        statusMessage: '{{ response.statusMessage }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.managedidentities.projects.locations.global.peerings.getIamPolicy [dependent] ──
  - discovery_id: gcp.managedidentities.projects.locations.global.peerings.getIamPolicy
    for_each: gcp.managedidentities.projects.locations.global.peerings.list
    # python: svc.projects().locations().global().peerings().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.global.peerings.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
