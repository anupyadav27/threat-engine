# ============================================================
# Discovery YAML — certificatemanager (v1)
# Generated: 2026-02-21T03:06:38Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 16 | independent: 9 | dependent: 7
# ops with fields: 16/16 | dep ops with chain: 7/7
# ============================================================
version: '1.0'
provider: gcp
service: certificatemanager

services:
  client: certificatemanager
  module: "googleapiclient.discovery.build('certificatemanager', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists CertificateIssuanceConfigs in a given project and location.
  # ── gcp.certificatemanager.projects.locations.certificateIssuanceConfigs.list ──
  - discovery_id: gcp.certificatemanager.projects.locations.certificateIssuanceConfigs.list
    # python: svc.projects().locations().certificateIssuanceConfigs().list(**params).execute()
    calls:
      - action: projects.locations.certificateIssuanceConfigs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.certificateIssuanceConfigs }}'
      item:
        certificateAuthorityConfig: '{{ item.certificateAuthorityConfig }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        keyAlgorithm: '{{ item.keyAlgorithm }}'
        labels: '{{ item.labels }}'
        lifetime: '{{ item.lifetime }}'
        name: '{{ item.name }}'
        rotationWindowPercentage: '{{ item.rotationWindowPercentage }}'
        updateTime: '{{ item.updateTime }}'
  # Lists CertificateMapEntries in a given project and location.
  # ── gcp.certificatemanager.projects.locations.certificateMaps.certificateMapEntries.list ──
  - discovery_id: gcp.certificatemanager.projects.locations.certificateMaps.certificateMapEntries.list
    # python: svc.projects().locations().certificateMaps().certificateMapEntries().list(**params).execute()
    calls:
      - action: projects.locations.certificateMaps.certificateMapEntries.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.certificateMapEntries }}'
      item:
        certificates: '{{ item.certificates }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        hostname: '{{ item.hostname }}'
        labels: '{{ item.labels }}'
        matcher: '{{ item.matcher }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
        updateTime: '{{ item.updateTime }}'
  # Lists CertificateMaps in a given project and location.
  # ── gcp.certificatemanager.projects.locations.certificateMaps.list ──
  - discovery_id: gcp.certificatemanager.projects.locations.certificateMaps.list
    # python: svc.projects().locations().certificateMaps().list(**params).execute()
    calls:
      - action: projects.locations.certificateMaps.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.certificateMaps }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        gclbTargets: '{{ item.gclbTargets }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        updateTime: '{{ item.updateTime }}'
  # Lists Certificates in a given project and location.
  # ── gcp.certificatemanager.projects.locations.certificates.list ──
  - discovery_id: gcp.certificatemanager.projects.locations.certificates.list
    # python: svc.projects().locations().certificates().list(**params).execute()
    calls:
      - action: projects.locations.certificates.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.certificates }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        expireTime: '{{ item.expireTime }}'
        labels: '{{ item.labels }}'
        managed: '{{ item.managed }}'
        managedIdentity: '{{ item.managedIdentity }}'
        name: '{{ item.name }}'
        pemCertificate: '{{ item.pemCertificate }}'
        sanDnsnames: '{{ item.sanDnsnames }}'
        scope: '{{ item.scope }}'
        selfManaged: '{{ item.selfManaged }}'
        updateTime: '{{ item.updateTime }}'
        usedBy: '{{ item.usedBy }}'
  # Lists DnsAuthorizations in a given project and location.
  # ── gcp.certificatemanager.projects.locations.dnsAuthorizations.list ──
  - discovery_id: gcp.certificatemanager.projects.locations.dnsAuthorizations.list
    # python: svc.projects().locations().dnsAuthorizations().list(**params).execute()
    calls:
      - action: projects.locations.dnsAuthorizations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.dnsAuthorizations }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        dnsResourceRecord: '{{ item.dnsResourceRecord }}'
        domain: '{{ item.domain }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
        updateTime: '{{ item.updateTime }}'
  # Gets information about a location.
  # ── gcp.certificatemanager.projects.locations.get ──
  - discovery_id: gcp.certificatemanager.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists information about the supported locations for this service.
  # ── gcp.certificatemanager.projects.locations.list ──
  - discovery_id: gcp.certificatemanager.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.certificatemanager.projects.locations.operations.list ──
  - discovery_id: gcp.certificatemanager.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists TrustConfigs in a given project and location.
  # ── gcp.certificatemanager.projects.locations.trustConfigs.list ──
  - discovery_id: gcp.certificatemanager.projects.locations.trustConfigs.list
    # python: svc.projects().locations().trustConfigs().list(**params).execute()
    calls:
      - action: projects.locations.trustConfigs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.trustConfigs }}'
      item:
        allowlistedCertificates: '{{ item.allowlistedCertificates }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        spiffeTrustStores: '{{ item.spiffeTrustStores }}'
        trustStores: '{{ item.trustStores }}'
        updateTime: '{{ item.updateTime }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets details of a single CertificateIssuanceConfig.
  # ── gcp.certificatemanager.projects.locations.certificateIssuanceConfigs.get [dependent] ──
  - discovery_id: gcp.certificatemanager.projects.locations.certificateIssuanceConfigs.get
    for_each: gcp.certificatemanager.projects.locations.certificateIssuanceConfigs.list
    # python: svc.projects().locations().certificateIssuanceConfigs().get(**params).execute()
    calls:
      - action: projects.locations.certificateIssuanceConfigs.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        certificateAuthorityConfig: '{{ response.certificateAuthorityConfig }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        keyAlgorithm: '{{ response.keyAlgorithm }}'
        labels: '{{ response.labels }}'
        lifetime: '{{ response.lifetime }}'
        name: '{{ response.name }}'
        rotationWindowPercentage: '{{ response.rotationWindowPercentage }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets details of a single CertificateMapEntry.
  # ── gcp.certificatemanager.projects.locations.certificateMaps.certificateMapEntries.get [dependent] ──
  - discovery_id: gcp.certificatemanager.projects.locations.certificateMaps.certificateMapEntries.get
    for_each: gcp.certificatemanager.projects.locations.certificateMaps.certificateMapEntries.list
    # python: svc.projects().locations().certificateMaps().certificateMapEntries().get(**params).execute()
    calls:
      - action: projects.locations.certificateMaps.certificateMapEntries.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        certificates: '{{ response.certificates }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        hostname: '{{ response.hostname }}'
        labels: '{{ response.labels }}'
        matcher: '{{ response.matcher }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets details of a single CertificateMap.
  # ── gcp.certificatemanager.projects.locations.certificateMaps.get [dependent] ──
  - discovery_id: gcp.certificatemanager.projects.locations.certificateMaps.get
    for_each: gcp.certificatemanager.projects.locations.certificateMaps.list
    # python: svc.projects().locations().certificateMaps().get(**params).execute()
    calls:
      - action: projects.locations.certificateMaps.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        gclbTargets: '{{ response.gclbTargets }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets details of a single Certificate.
  # ── gcp.certificatemanager.projects.locations.certificates.get [dependent] ──
  - discovery_id: gcp.certificatemanager.projects.locations.certificates.get
    for_each: gcp.certificatemanager.projects.locations.certificates.list
    # python: svc.projects().locations().certificates().get(**params).execute()
    calls:
      - action: projects.locations.certificates.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        expireTime: '{{ response.expireTime }}'
        labels: '{{ response.labels }}'
        managed: '{{ response.managed }}'
        managedIdentity: '{{ response.managedIdentity }}'
        name: '{{ response.name }}'
        pemCertificate: '{{ response.pemCertificate }}'
        sanDnsnames: '{{ response.sanDnsnames }}'
        scope: '{{ response.scope }}'
        selfManaged: '{{ response.selfManaged }}'
        updateTime: '{{ response.updateTime }}'
        usedBy: '{{ response.usedBy }}'
    # required_params: ['name']
  # Gets details of a single DnsAuthorization.
  # ── gcp.certificatemanager.projects.locations.dnsAuthorizations.get [dependent] ──
  - discovery_id: gcp.certificatemanager.projects.locations.dnsAuthorizations.get
    for_each: gcp.certificatemanager.projects.locations.dnsAuthorizations.list
    # python: svc.projects().locations().dnsAuthorizations().get(**params).execute()
    calls:
      - action: projects.locations.dnsAuthorizations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        dnsResourceRecord: '{{ response.dnsResourceRecord }}'
        domain: '{{ response.domain }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        type: '{{ response.type }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.certificatemanager.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.certificatemanager.projects.locations.operations.get
    for_each: gcp.certificatemanager.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets details of a single TrustConfig.
  # ── gcp.certificatemanager.projects.locations.trustConfigs.get [dependent] ──
  - discovery_id: gcp.certificatemanager.projects.locations.trustConfigs.get
    for_each: gcp.certificatemanager.projects.locations.trustConfigs.list
    # python: svc.projects().locations().trustConfigs().get(**params).execute()
    calls:
      - action: projects.locations.trustConfigs.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        allowlistedCertificates: '{{ response.allowlistedCertificates }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        spiffeTrustStores: '{{ response.spiffeTrustStores }}'
        trustStores: '{{ response.trustStores }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
