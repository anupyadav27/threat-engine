# ============================================================
# Discovery YAML — apphub (v1)
# Generated: 2026-02-21T03:06:38Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 23 | independent: 14 | dependent: 9
# ops with fields: 23/23 | dep ops with chain: 9/9
# ============================================================
version: '1.0'
provider: gcp
service: apphub

services:
  client: apphub
  module: "googleapiclient.discovery.build('apphub', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists Applications in a host project and location.
  # ── gcp.apphub.projects.locations.applications.list ──
  - discovery_id: gcp.apphub.projects.locations.applications.list
    # python: svc.projects().locations().applications().list(**params).execute()
    calls:
      - action: projects.locations.applications.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.applications }}'
      item:
        attributes: '{{ item.attributes }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        name: '{{ item.name }}'
        scope: '{{ item.scope }}'
        state: '{{ item.state }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # Lists Services in an Application.
  # ── gcp.apphub.projects.locations.applications.services.list ──
  - discovery_id: gcp.apphub.projects.locations.applications.services.list
    # python: svc.projects().locations().applications().services().list(**params).execute()
    calls:
      - action: projects.locations.applications.services.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.services }}'
      item:
        attributes: '{{ item.attributes }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        discoveredService: '{{ item.discoveredService }}'
        displayName: '{{ item.displayName }}'
        name: '{{ item.name }}'
        serviceProperties: '{{ item.serviceProperties }}'
        serviceReference: '{{ item.serviceReference }}'
        state: '{{ item.state }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # Lists Workloads in an Application.
  # ── gcp.apphub.projects.locations.applications.workloads.list ──
  - discovery_id: gcp.apphub.projects.locations.applications.workloads.list
    # python: svc.projects().locations().applications().workloads().list(**params).execute()
    calls:
      - action: projects.locations.applications.workloads.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.workloads }}'
      item:
        attributes: '{{ item.attributes }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        discoveredWorkload: '{{ item.discoveredWorkload }}'
        displayName: '{{ item.displayName }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
        workloadProperties: '{{ item.workloadProperties }}'
        workloadReference: '{{ item.workloadReference }}'
  # Lists Discovered Services that can be added to an Application in a host project and location.
  # ── gcp.apphub.projects.locations.discoveredServices.list ──
  - discovery_id: gcp.apphub.projects.locations.discoveredServices.list
    # python: svc.projects().locations().discoveredServices().list(**params).execute()
    calls:
      - action: projects.locations.discoveredServices.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.discoveredServices }}'
      item:
        name: '{{ item.name }}'
        serviceProperties: '{{ item.serviceProperties }}'
        serviceReference: '{{ item.serviceReference }}'
  # Lists a Discovered Service in a host project and location, with a given resource URI.
  # ── gcp.apphub.projects.locations.discoveredServices.lookup ──
  - discovery_id: gcp.apphub.projects.locations.discoveredServices.lookup
    # python: svc.projects().locations().discoveredServices().lookup(**params).execute()
    calls:
      - action: projects.locations.discoveredServices.lookup
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        discoveredService: '{{ response.discoveredService }}'
  # Lists Discovered Workloads that can be added to an Application in a host project and location.
  # ── gcp.apphub.projects.locations.discoveredWorkloads.list ──
  - discovery_id: gcp.apphub.projects.locations.discoveredWorkloads.list
    # python: svc.projects().locations().discoveredWorkloads().list(**params).execute()
    calls:
      - action: projects.locations.discoveredWorkloads.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.discoveredWorkloads }}'
      item:
        name: '{{ item.name }}'
        workloadProperties: '{{ item.workloadProperties }}'
        workloadReference: '{{ item.workloadReference }}'
  # Lists a Discovered Workload in a host project and location, with a given resource URI.
  # ── gcp.apphub.projects.locations.discoveredWorkloads.lookup ──
  - discovery_id: gcp.apphub.projects.locations.discoveredWorkloads.lookup
    # python: svc.projects().locations().discoveredWorkloads().lookup(**params).execute()
    calls:
      - action: projects.locations.discoveredWorkloads.lookup
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        discoveredWorkload: '{{ response.discoveredWorkload }}'
  # Lists Extended Metadata Schemas available in a host project and location.
  # ── gcp.apphub.projects.locations.extendedMetadataSchemas.list ──
  - discovery_id: gcp.apphub.projects.locations.extendedMetadataSchemas.list
    # python: svc.projects().locations().extendedMetadataSchemas().list(**params).execute()
    calls:
      - action: projects.locations.extendedMetadataSchemas.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.extendedMetadataSchemas }}'
      item:
        jsonSchema: '{{ item.jsonSchema }}'
        name: '{{ item.name }}'
        schemaVersion: '{{ item.schemaVersion }}'
  # Gets information about a location.
  # ── gcp.apphub.projects.locations.get ──
  - discovery_id: gcp.apphub.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Gets a Boundary.
  # ── gcp.apphub.projects.locations.getBoundary ──
  - discovery_id: gcp.apphub.projects.locations.getBoundary
    # python: svc.projects().locations().getBoundary(**params).execute()
    calls:
      - action: projects.locations.getBoundary
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        crmNode: '{{ response.crmNode }}'
        name: '{{ response.name }}'
        type: '{{ response.type }}'
        updateTime: '{{ response.updateTime }}'
  # Lists information about the supported locations for this service.
  # ── gcp.apphub.projects.locations.list ──
  - discovery_id: gcp.apphub.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists a service project attachment for a given service project. You can call this API from any proje
  # ── gcp.apphub.projects.locations.lookupServiceProjectAttachment ──
  - discovery_id: gcp.apphub.projects.locations.lookupServiceProjectAttachment
    # python: svc.projects().locations().lookupServiceProjectAttachment(**params).execute()
    calls:
      - action: projects.locations.lookupServiceProjectAttachment
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        serviceProjectAttachment: '{{ response.serviceProjectAttachment }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.apphub.projects.locations.operations.list ──
  - discovery_id: gcp.apphub.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists service projects attached to the host project.
  # ── gcp.apphub.projects.locations.serviceProjectAttachments.list ──
  - discovery_id: gcp.apphub.projects.locations.serviceProjectAttachments.list
    # python: svc.projects().locations().serviceProjectAttachments().list(**params).execute()
    calls:
      - action: projects.locations.serviceProjectAttachments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.serviceProjectAttachments }}'
      item:
        createTime: '{{ item.createTime }}'
        name: '{{ item.name }}'
        serviceProject: '{{ item.serviceProject }}'
        state: '{{ item.state }}'
        uid: '{{ item.uid }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets an Application in a host project and location.
  # ── gcp.apphub.projects.locations.applications.get [dependent] ──
  - discovery_id: gcp.apphub.projects.locations.applications.get
    for_each: gcp.apphub.projects.locations.applications.list
    # python: svc.projects().locations().applications().get(**params).execute()
    calls:
      - action: projects.locations.applications.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        attributes: '{{ response.attributes }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        name: '{{ response.name }}'
        scope: '{{ response.scope }}'
        state: '{{ response.state }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.apphub.projects.locations.applications.getIamPolicy [dependent] ──
  - discovery_id: gcp.apphub.projects.locations.applications.getIamPolicy
    for_each: gcp.apphub.projects.locations.applications.list
    # python: svc.projects().locations().applications().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.applications.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Gets a Service in an Application.
  # ── gcp.apphub.projects.locations.applications.services.get [dependent] ──
  - discovery_id: gcp.apphub.projects.locations.applications.services.get
    for_each: gcp.apphub.projects.locations.applications.services.list
    # python: svc.projects().locations().applications().services().get(**params).execute()
    calls:
      - action: projects.locations.applications.services.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        attributes: '{{ response.attributes }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        discoveredService: '{{ response.discoveredService }}'
        displayName: '{{ response.displayName }}'
        name: '{{ response.name }}'
        serviceProperties: '{{ response.serviceProperties }}'
        serviceReference: '{{ response.serviceReference }}'
        state: '{{ response.state }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets a Workload in an Application.
  # ── gcp.apphub.projects.locations.applications.workloads.get [dependent] ──
  - discovery_id: gcp.apphub.projects.locations.applications.workloads.get
    for_each: gcp.apphub.projects.locations.applications.workloads.list
    # python: svc.projects().locations().applications().workloads().get(**params).execute()
    calls:
      - action: projects.locations.applications.workloads.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        attributes: '{{ response.attributes }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        discoveredWorkload: '{{ response.discoveredWorkload }}'
        displayName: '{{ response.displayName }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
        workloadProperties: '{{ response.workloadProperties }}'
        workloadReference: '{{ response.workloadReference }}'
    # required_params: ['name']
  # Gets a Discovered Service in a host project and location.
  # ── gcp.apphub.projects.locations.discoveredServices.get [dependent] ──
  - discovery_id: gcp.apphub.projects.locations.discoveredServices.get
    for_each: gcp.apphub.projects.locations.discoveredServices.list
    # python: svc.projects().locations().discoveredServices().get(**params).execute()
    calls:
      - action: projects.locations.discoveredServices.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        serviceProperties: '{{ response.serviceProperties }}'
        serviceReference: '{{ response.serviceReference }}'
    # required_params: ['name']
  # Gets a Discovered Workload in a host project and location.
  # ── gcp.apphub.projects.locations.discoveredWorkloads.get [dependent] ──
  - discovery_id: gcp.apphub.projects.locations.discoveredWorkloads.get
    for_each: gcp.apphub.projects.locations.discoveredWorkloads.list
    # python: svc.projects().locations().discoveredWorkloads().get(**params).execute()
    calls:
      - action: projects.locations.discoveredWorkloads.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        workloadProperties: '{{ response.workloadProperties }}'
        workloadReference: '{{ response.workloadReference }}'
    # required_params: ['name']
  # Gets an Extended Metadata Schema.
  # ── gcp.apphub.projects.locations.extendedMetadataSchemas.get [dependent] ──
  - discovery_id: gcp.apphub.projects.locations.extendedMetadataSchemas.get
    for_each: gcp.apphub.projects.locations.extendedMetadataSchemas.list
    # python: svc.projects().locations().extendedMetadataSchemas().get(**params).execute()
    calls:
      - action: projects.locations.extendedMetadataSchemas.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        jsonSchema: '{{ response.jsonSchema }}'
        name: '{{ response.name }}'
        schemaVersion: '{{ response.schemaVersion }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.apphub.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.apphub.projects.locations.operations.get
    for_each: gcp.apphub.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets a service project attachment.
  # ── gcp.apphub.projects.locations.serviceProjectAttachments.get [dependent] ──
  - discovery_id: gcp.apphub.projects.locations.serviceProjectAttachments.get
    for_each: gcp.apphub.projects.locations.serviceProjectAttachments.list
    # python: svc.projects().locations().serviceProjectAttachments().get(**params).execute()
    calls:
      - action: projects.locations.serviceProjectAttachments.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        name: '{{ response.name }}'
        serviceProject: '{{ response.serviceProject }}'
        state: '{{ response.state }}'
        uid: '{{ response.uid }}'
    # required_params: ['name']
