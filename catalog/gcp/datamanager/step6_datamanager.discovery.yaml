# ============================================================
# Discovery YAML — datamanager (v1)
# Generated: 2026-02-21T03:06:39Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 9 | independent: 6 | dependent: 3
# ops with fields: 9/9 | dep ops with chain: 3/3
# ============================================================
version: '1.0'
provider: gcp
service: datamanager

services:
  client: datamanager
  module: "googleapiclient.discovery.build('datamanager', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Searches for all partner links to and from a given account. Authorization Headers: This method suppo
  # ── gcp.datamanager.accountTypes.accounts.partnerLinks.search ──
  - discovery_id: gcp.datamanager.accountTypes.accounts.partnerLinks.search
    # python: svc.accountTypes().accounts().partnerLinks().search(**params).execute()
    calls:
      - action: accountTypes.accounts.partnerLinks.search
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.partnerLinks }}'
      item:
        name: '{{ item.name }}'
        owningAccount: '{{ item.owningAccount }}'
        partnerAccount: '{{ item.partnerAccount }}'
        partnerLinkId: '{{ item.partnerLinkId }}'
  # Lists all user list direct licenses owned by the parent account. This feature is only available to d
  # ── gcp.datamanager.accountTypes.accounts.userListDirectLicenses.list ──
  - discovery_id: gcp.datamanager.accountTypes.accounts.userListDirectLicenses.list
    # python: svc.accountTypes().accounts().userListDirectLicenses().list(**params).execute()
    calls:
      - action: accountTypes.accounts.userListDirectLicenses.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.userListDirectLicenses }}'
      item:
        clientAccountDisplayName: '{{ item.clientAccountDisplayName }}'
        clientAccountId: '{{ item.clientAccountId }}'
        clientAccountType: '{{ item.clientAccountType }}'
        historicalPricings: '{{ item.historicalPricings }}'
        metrics: '{{ item.metrics }}'
        name: '{{ item.name }}'
        pricing: '{{ item.pricing }}'
        status: '{{ item.status }}'
        userListDisplayName: '{{ item.userListDisplayName }}'
        userListId: '{{ item.userListId }}'
  # Lists all user list global licenses owned by the parent account. This feature is only available to d
  # ── gcp.datamanager.accountTypes.accounts.userListGlobalLicenses.list ──
  - discovery_id: gcp.datamanager.accountTypes.accounts.userListGlobalLicenses.list
    # python: svc.accountTypes().accounts().userListGlobalLicenses().list(**params).execute()
    calls:
      - action: accountTypes.accounts.userListGlobalLicenses.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.userListGlobalLicenses }}'
      item:
        historicalPricings: '{{ item.historicalPricings }}'
        licenseType: '{{ item.licenseType }}'
        metrics: '{{ item.metrics }}'
        name: '{{ item.name }}'
        pricing: '{{ item.pricing }}'
        status: '{{ item.status }}'
        userListDisplayName: '{{ item.userListDisplayName }}'
        userListId: '{{ item.userListId }}'
  # Lists all customer info for a user list global license. This feature is only available to data partn
  # ── gcp.datamanager.accountTypes.accounts.userListGlobalLicenses.userListGlobalLicenseCustomerInfos.list ──
  - discovery_id: gcp.datamanager.accountTypes.accounts.userListGlobalLicenses.userListGlobalLicenseCustomerInfos.list
    # python: svc.accountTypes().accounts().userListGlobalLicenses().userListGlobalLicenseCustomerInfos().list(**params).execute()
    calls:
      - action: accountTypes.accounts.userListGlobalLicenses.userListGlobalLicenseCustomerInfos.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.userListGlobalLicenseCustomerInfos }}'
      item:
        clientAccountDisplayName: '{{ item.clientAccountDisplayName }}'
        clientAccountId: '{{ item.clientAccountId }}'
        clientAccountType: '{{ item.clientAccountType }}'
        historicalPricings: '{{ item.historicalPricings }}'
        licenseType: '{{ item.licenseType }}'
        metrics: '{{ item.metrics }}'
        name: '{{ item.name }}'
        pricing: '{{ item.pricing }}'
        status: '{{ item.status }}'
        userListDisplayName: '{{ item.userListDisplayName }}'
        userListId: '{{ item.userListId }}'
  # Lists UserLists. Authorization Headers: This method supports the following optional headers to defin
  # ── gcp.datamanager.accountTypes.accounts.userLists.list ──
  - discovery_id: gcp.datamanager.accountTypes.accounts.userLists.list
    # python: svc.accountTypes().accounts().userLists().list(**params).execute()
    calls:
      - action: accountTypes.accounts.userLists.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.userLists }}'
      item:
        accessReason: '{{ item.accessReason }}'
        accountAccessStatus: '{{ item.accountAccessStatus }}'
        closingReason: '{{ item.closingReason }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        id: '{{ item.id }}'
        ingestedUserListInfo: '{{ item.ingestedUserListInfo }}'
        integrationCode: '{{ item.integrationCode }}'
        membershipDuration: '{{ item.membershipDuration }}'
        membershipStatus: '{{ item.membershipStatus }}'
        name: '{{ item.name }}'
        readOnly: '{{ item.readOnly }}'
        sizeInfo: '{{ item.sizeInfo }}'
        targetNetworkInfo: '{{ item.targetNetworkInfo }}'
  # Gets the status of a request given request id.
  # ── gcp.datamanager.requestStatus.retrieve ──
  - discovery_id: gcp.datamanager.requestStatus.retrieve
    # python: svc.requestStatus().retrieve(**params).execute()
    calls:
      - action: requestStatus.retrieve
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        requestStatusPerDestination: '{{ response.requestStatusPerDestination }}'

  # ════ DEPENDENT (enrich) operations ════
  # Retrieves a user list direct license. This feature is only available to data partners.
  # ── gcp.datamanager.accountTypes.accounts.userListDirectLicenses.get [dependent] ──
  - discovery_id: gcp.datamanager.accountTypes.accounts.userListDirectLicenses.get
    for_each: gcp.datamanager.accountTypes.accounts.userListDirectLicenses.list
    # python: svc.accountTypes().accounts().userListDirectLicenses().get(**params).execute()
    calls:
      - action: accountTypes.accounts.userListDirectLicenses.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        clientAccountDisplayName: '{{ response.clientAccountDisplayName }}'
        clientAccountId: '{{ response.clientAccountId }}'
        clientAccountType: '{{ response.clientAccountType }}'
        historicalPricings: '{{ response.historicalPricings }}'
        metrics: '{{ response.metrics }}'
        name: '{{ response.name }}'
        pricing: '{{ response.pricing }}'
        status: '{{ response.status }}'
        userListDisplayName: '{{ response.userListDisplayName }}'
        userListId: '{{ response.userListId }}'
    # required_params: ['name']
  # Retrieves a user list global license. This feature is only available to data partners.
  # ── gcp.datamanager.accountTypes.accounts.userListGlobalLicenses.get [dependent] ──
  - discovery_id: gcp.datamanager.accountTypes.accounts.userListGlobalLicenses.get
    for_each: gcp.datamanager.accountTypes.accounts.userListGlobalLicenses.list
    # python: svc.accountTypes().accounts().userListGlobalLicenses().get(**params).execute()
    calls:
      - action: accountTypes.accounts.userListGlobalLicenses.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        historicalPricings: '{{ response.historicalPricings }}'
        licenseType: '{{ response.licenseType }}'
        metrics: '{{ response.metrics }}'
        name: '{{ response.name }}'
        pricing: '{{ response.pricing }}'
        status: '{{ response.status }}'
        userListDisplayName: '{{ response.userListDisplayName }}'
        userListId: '{{ response.userListId }}'
    # required_params: ['name']
  # Gets a UserList. Authorization Headers: This method supports the following optional headers to defin
  # ── gcp.datamanager.accountTypes.accounts.userLists.get [dependent] ──
  - discovery_id: gcp.datamanager.accountTypes.accounts.userLists.get
    for_each: gcp.datamanager.accountTypes.accounts.userLists.list
    # python: svc.accountTypes().accounts().userLists().get(**params).execute()
    calls:
      - action: accountTypes.accounts.userLists.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        accessReason: '{{ response.accessReason }}'
        accountAccessStatus: '{{ response.accountAccessStatus }}'
        closingReason: '{{ response.closingReason }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        id: '{{ response.id }}'
        ingestedUserListInfo: '{{ response.ingestedUserListInfo }}'
        integrationCode: '{{ response.integrationCode }}'
        membershipDuration: '{{ response.membershipDuration }}'
        membershipStatus: '{{ response.membershipStatus }}'
        name: '{{ response.name }}'
        readOnly: '{{ response.readOnly }}'
        sizeInfo: '{{ response.sizeInfo }}'
        targetNetworkInfo: '{{ response.targetNetworkInfo }}'
    # required_params: ['name']
