# ============================================================
# Discovery YAML — iamcredentials (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 3 | independent: 0 | dependent: 3
# ops with fields: 3/3 | dep ops with chain: 0/3
# ============================================================
version: '1.0'
provider: gcp
service: iamcredentials

services:
  client: iamcredentials
  module: "googleapiclient.discovery.build('iamcredentials', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:


  # ════ DEPENDENT (enrich) operations ════
  # Returns the trust boundary info for a given workforce pool.
  # ── gcp.iamcredentials.locations.workforcePools.getAllowedLocations [dependent] ──
  - discovery_id: gcp.iamcredentials.locations.workforcePools.getAllowedLocations
    # python: svc.locations().workforcePools().getAllowedLocations(**params).execute()
    calls:
      - action: locations.workforcePools.getAllowedLocations
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        encodedLocations: '{{ response.encodedLocations }}'
        locations: '{{ response.locations }}'
    # required_params: ['name']
  # Returns the trust boundary info for a given workload identity pool.
  # ── gcp.iamcredentials.projects.locations.workloadIdentityPools.getAllowedLocations [dependent] ──
  - discovery_id: gcp.iamcredentials.projects.locations.workloadIdentityPools.getAllowedLocations
    # python: svc.projects().locations().workloadIdentityPools().getAllowedLocations(**params).execute()
    calls:
      - action: projects.locations.workloadIdentityPools.getAllowedLocations
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        encodedLocations: '{{ response.encodedLocations }}'
        locations: '{{ response.locations }}'
    # required_params: ['name']
  # Returns the trust boundary info for a given service account.
  # ── gcp.iamcredentials.projects.serviceAccounts.getAllowedLocations [dependent] ──
  - discovery_id: gcp.iamcredentials.projects.serviceAccounts.getAllowedLocations
    # python: svc.projects().serviceAccounts().getAllowedLocations(**params).execute()
    calls:
      - action: projects.serviceAccounts.getAllowedLocations
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        encodedLocations: '{{ response.encodedLocations }}'
        locations: '{{ response.locations }}'
    # required_params: ['name']
