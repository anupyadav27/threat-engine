# ============================================================
# Discovery YAML — beyondcorp (v1)
# Generated: 2026-02-21T03:06:38Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 23 | independent: 10 | dependent: 13
# ops with fields: 23/23 | dep ops with chain: 13/13
# ============================================================
version: '1.0'
provider: gcp
service: beyondcorp

services:
  client: beyondcorp
  module: "googleapiclient.discovery.build('beyondcorp', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.beyondcorp.organizations.locations.operations.list ──
  - discovery_id: gcp.beyondcorp.organizations.locations.operations.list
    # python: svc.organizations().locations().operations().list(**params).execute()
    calls:
      - action: organizations.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists AppConnections in a given project and location.
  # ── gcp.beyondcorp.projects.locations.appConnections.list ──
  - discovery_id: gcp.beyondcorp.projects.locations.appConnections.list
    # python: svc.projects().locations().appConnections().list(**params).execute()
    calls:
      - action: projects.locations.appConnections.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.appConnections }}'
      item:
        applicationEndpoint: '{{ item.applicationEndpoint }}'
        connectors: '{{ item.connectors }}'
        createTime: '{{ item.createTime }}'
        displayName: '{{ item.displayName }}'
        gateway: '{{ item.gateway }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        satisfiesPzi: '{{ item.satisfiesPzi }}'
        satisfiesPzs: '{{ item.satisfiesPzs }}'
        state: '{{ item.state }}'
        type: '{{ item.type }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # Resolves AppConnections details for a given AppConnector. An internal method called by a connector t
  # ── gcp.beyondcorp.projects.locations.appConnections.resolve ──
  - discovery_id: gcp.beyondcorp.projects.locations.appConnections.resolve
    # python: svc.projects().locations().appConnections().resolve(**params).execute()
    calls:
      - action: projects.locations.appConnections.resolve
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        appConnectionDetails: '{{ response.appConnectionDetails }}'
  # Lists AppConnectors in a given project and location.
  # ── gcp.beyondcorp.projects.locations.appConnectors.list ──
  - discovery_id: gcp.beyondcorp.projects.locations.appConnectors.list
    # python: svc.projects().locations().appConnectors().list(**params).execute()
    calls:
      - action: projects.locations.appConnectors.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.appConnectors }}'
      item:
        createTime: '{{ item.createTime }}'
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        principalInfo: '{{ item.principalInfo }}'
        resourceInfo: '{{ item.resourceInfo }}'
        state: '{{ item.state }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # Lists AppGateways in a given project and location.
  # ── gcp.beyondcorp.projects.locations.appGateways.list ──
  - discovery_id: gcp.beyondcorp.projects.locations.appGateways.list
    # python: svc.projects().locations().appGateways().list(**params).execute()
    calls:
      - action: projects.locations.appGateways.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.appGateways }}'
      item:
        allocatedConnections: '{{ item.allocatedConnections }}'
        createTime: '{{ item.createTime }}'
        displayName: '{{ item.displayName }}'
        hostType: '{{ item.hostType }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        satisfiesPzi: '{{ item.satisfiesPzi }}'
        satisfiesPzs: '{{ item.satisfiesPzs }}'
        state: '{{ item.state }}'
        type: '{{ item.type }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
        uri: '{{ item.uri }}'
  # Gets information about a location.
  # ── gcp.beyondcorp.projects.locations.get ──
  - discovery_id: gcp.beyondcorp.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.beyondcorp.projects.locations.list ──
  - discovery_id: gcp.beyondcorp.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.beyondcorp.projects.locations.operations.list ──
  - discovery_id: gcp.beyondcorp.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists Applications in a given project and location.
  # ── gcp.beyondcorp.projects.locations.securityGateways.applications.list ──
  - discovery_id: gcp.beyondcorp.projects.locations.securityGateways.applications.list
    # python: svc.projects().locations().securityGateways().applications().list(**params).execute()
    calls:
      - action: projects.locations.securityGateways.applications.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.applications }}'
      item:
        createTime: '{{ item.createTime }}'
        displayName: '{{ item.displayName }}'
        endpointMatchers: '{{ item.endpointMatchers }}'
        name: '{{ item.name }}'
        schema: '{{ item.schema }}'
        updateTime: '{{ item.updateTime }}'
        upstreams: '{{ item.upstreams }}'
  # Lists SecurityGateways in a given project and location.
  # ── gcp.beyondcorp.projects.locations.securityGateways.list ──
  - discovery_id: gcp.beyondcorp.projects.locations.securityGateways.list
    # python: svc.projects().locations().securityGateways().list(**params).execute()
    calls:
      - action: projects.locations.securityGateways.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.securityGateways }}'
      item:
        createTime: '{{ item.createTime }}'
        delegatingServiceAccount: '{{ item.delegatingServiceAccount }}'
        displayName: '{{ item.displayName }}'
        externalIps: '{{ item.externalIps }}'
        hubs: '{{ item.hubs }}'
        name: '{{ item.name }}'
        proxyProtocolConfig: '{{ item.proxyProtocolConfig }}'
        serviceDiscovery: '{{ item.serviceDiscovery }}'
        state: '{{ item.state }}'
        updateTime: '{{ item.updateTime }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.beyondcorp.organizations.locations.operations.get [dependent] ──
  - discovery_id: gcp.beyondcorp.organizations.locations.operations.get
    for_each: gcp.beyondcorp.projects.locations.operations.list
    # python: svc.organizations().locations().operations().get(**params).execute()
    calls:
      - action: organizations.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets details of a single AppConnection.
  # ── gcp.beyondcorp.projects.locations.appConnections.get [dependent] ──
  - discovery_id: gcp.beyondcorp.projects.locations.appConnections.get
    for_each: gcp.beyondcorp.projects.locations.appConnections.list
    # python: svc.projects().locations().appConnections().get(**params).execute()
    calls:
      - action: projects.locations.appConnections.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        applicationEndpoint: '{{ response.applicationEndpoint }}'
        connectors: '{{ response.connectors }}'
        createTime: '{{ response.createTime }}'
        displayName: '{{ response.displayName }}'
        gateway: '{{ response.gateway }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        satisfiesPzi: '{{ response.satisfiesPzi }}'
        satisfiesPzs: '{{ response.satisfiesPzs }}'
        state: '{{ response.state }}'
        type: '{{ response.type }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.beyondcorp.projects.locations.appConnections.getIamPolicy [dependent] ──
  - discovery_id: gcp.beyondcorp.projects.locations.appConnections.getIamPolicy
    for_each: gcp.beyondcorp.projects.locations.appConnections.list
    # python: svc.projects().locations().appConnections().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.appConnections.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Gets details of a single AppConnector.
  # ── gcp.beyondcorp.projects.locations.appConnectors.get [dependent] ──
  - discovery_id: gcp.beyondcorp.projects.locations.appConnectors.get
    for_each: gcp.beyondcorp.projects.locations.appConnectors.list
    # python: svc.projects().locations().appConnectors().get(**params).execute()
    calls:
      - action: projects.locations.appConnectors.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        principalInfo: '{{ response.principalInfo }}'
        resourceInfo: '{{ response.resourceInfo }}'
        state: '{{ response.state }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.beyondcorp.projects.locations.appConnectors.getIamPolicy [dependent] ──
  - discovery_id: gcp.beyondcorp.projects.locations.appConnectors.getIamPolicy
    for_each: gcp.beyondcorp.projects.locations.appConnectors.list
    # python: svc.projects().locations().appConnectors().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.appConnectors.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Gets instance configuration for a given AppConnector. An internal method called by a AppConnector to
  # ── gcp.beyondcorp.projects.locations.appConnectors.resolveInstanceConfig [dependent] ──
  - discovery_id: gcp.beyondcorp.projects.locations.appConnectors.resolveInstanceConfig
    for_each: gcp.beyondcorp.projects.locations.appConnectors.list
    # python: svc.projects().locations().appConnectors().resolveInstanceConfig(**params).execute()
    calls:
      - action: projects.locations.appConnectors.resolveInstanceConfig
        params:
          appConnector: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        instanceConfig: '{{ response.instanceConfig }}'
    # required_params: ['appConnector']
  # Gets details of a single AppGateway.
  # ── gcp.beyondcorp.projects.locations.appGateways.get [dependent] ──
  - discovery_id: gcp.beyondcorp.projects.locations.appGateways.get
    for_each: gcp.beyondcorp.projects.locations.appGateways.list
    # python: svc.projects().locations().appGateways().get(**params).execute()
    calls:
      - action: projects.locations.appGateways.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        allocatedConnections: '{{ response.allocatedConnections }}'
        createTime: '{{ response.createTime }}'
        displayName: '{{ response.displayName }}'
        hostType: '{{ response.hostType }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        satisfiesPzi: '{{ response.satisfiesPzi }}'
        satisfiesPzs: '{{ response.satisfiesPzs }}'
        state: '{{ response.state }}'
        type: '{{ response.type }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
        uri: '{{ response.uri }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.beyondcorp.projects.locations.appGateways.getIamPolicy [dependent] ──
  - discovery_id: gcp.beyondcorp.projects.locations.appGateways.getIamPolicy
    for_each: gcp.beyondcorp.projects.locations.appGateways.list
    # python: svc.projects().locations().appGateways().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.appGateways.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.beyondcorp.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.beyondcorp.projects.locations.operations.get
    for_each: gcp.beyondcorp.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets details of a single Application.
  # ── gcp.beyondcorp.projects.locations.securityGateways.applications.get [dependent] ──
  - discovery_id: gcp.beyondcorp.projects.locations.securityGateways.applications.get
    for_each: gcp.beyondcorp.projects.locations.securityGateways.applications.list
    # python: svc.projects().locations().securityGateways().applications().get(**params).execute()
    calls:
      - action: projects.locations.securityGateways.applications.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        displayName: '{{ response.displayName }}'
        endpointMatchers: '{{ response.endpointMatchers }}'
        name: '{{ response.name }}'
        schema: '{{ response.schema }}'
        updateTime: '{{ response.updateTime }}'
        upstreams: '{{ response.upstreams }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.beyondcorp.projects.locations.securityGateways.applications.getIamPolicy [dependent] ──
  - discovery_id: gcp.beyondcorp.projects.locations.securityGateways.applications.getIamPolicy
    for_each: gcp.beyondcorp.projects.locations.securityGateways.applications.list
    # python: svc.projects().locations().securityGateways().applications().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.securityGateways.applications.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Gets details of a single SecurityGateway.
  # ── gcp.beyondcorp.projects.locations.securityGateways.get [dependent] ──
  - discovery_id: gcp.beyondcorp.projects.locations.securityGateways.get
    for_each: gcp.beyondcorp.projects.locations.securityGateways.list
    # python: svc.projects().locations().securityGateways().get(**params).execute()
    calls:
      - action: projects.locations.securityGateways.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        delegatingServiceAccount: '{{ response.delegatingServiceAccount }}'
        displayName: '{{ response.displayName }}'
        externalIps: '{{ response.externalIps }}'
        hubs: '{{ response.hubs }}'
        name: '{{ response.name }}'
        proxyProtocolConfig: '{{ response.proxyProtocolConfig }}'
        serviceDiscovery: '{{ response.serviceDiscovery }}'
        state: '{{ response.state }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.beyondcorp.projects.locations.securityGateways.getIamPolicy [dependent] ──
  - discovery_id: gcp.beyondcorp.projects.locations.securityGateways.getIamPolicy
    for_each: gcp.beyondcorp.projects.locations.securityGateways.list
    # python: svc.projects().locations().securityGateways().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.securityGateways.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
