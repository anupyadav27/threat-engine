# ============================================================
# Discovery YAML — prod_tt_sasportal (v1alpha1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 28 | independent: 19 | dependent: 9
# ops with fields: 28/28 | dep ops with chain: 9/9
# ============================================================
version: '1.0'
provider: gcp
service: prod_tt_sasportal

services:
  client: prod_tt_sasportal
  module: "googleapiclient.discovery.build('prod_tt_sasportal', 'v1alpha1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists devices under a node or customer.
  # ── gcp.prod_tt_sasportal.customers.deployments.devices.list ──
  - discovery_id: gcp.prod_tt_sasportal.customers.deployments.devices.list
    # python: svc.customers().deployments().devices().list(**params).execute()
    calls:
      - action: customers.deployments.devices.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.devices }}'
      item:
        activeConfig: '{{ item.activeConfig }}'
        currentChannels: '{{ item.currentChannels }}'
        deviceMetadata: '{{ item.deviceMetadata }}'
        displayName: '{{ item.displayName }}'
        fccId: '{{ item.fccId }}'
        grantRangeAllowlists: '{{ item.grantRangeAllowlists }}'
        grants: '{{ item.grants }}'
        name: '{{ item.name }}'
        preloadedConfig: '{{ item.preloadedConfig }}'
        serialNumber: '{{ item.serialNumber }}'
        state: '{{ item.state }}'
  # Lists deployments.
  # ── gcp.prod_tt_sasportal.customers.deployments.list ──
  - discovery_id: gcp.prod_tt_sasportal.customers.deployments.list
    # python: svc.customers().deployments().list(**params).execute()
    calls:
      - action: customers.deployments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.deployments }}'
      item:
        displayName: '{{ item.displayName }}'
        frns: '{{ item.frns }}'
        name: '{{ item.name }}'
        sasUserIds: '{{ item.sasUserIds }}'
  # Lists devices under a node or customer.
  # ── gcp.prod_tt_sasportal.customers.devices.list ──
  - discovery_id: gcp.prod_tt_sasportal.customers.devices.list
    # python: svc.customers().devices().list(**params).execute()
    calls:
      - action: customers.devices.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.devices }}'
      item:
        activeConfig: '{{ item.activeConfig }}'
        currentChannels: '{{ item.currentChannels }}'
        deviceMetadata: '{{ item.deviceMetadata }}'
        displayName: '{{ item.displayName }}'
        fccId: '{{ item.fccId }}'
        grantRangeAllowlists: '{{ item.grantRangeAllowlists }}'
        grants: '{{ item.grants }}'
        name: '{{ item.name }}'
        preloadedConfig: '{{ item.preloadedConfig }}'
        serialNumber: '{{ item.serialNumber }}'
        state: '{{ item.state }}'
  # Returns a requested customer.
  # ── gcp.prod_tt_sasportal.customers.get ──
  - discovery_id: gcp.prod_tt_sasportal.customers.get
    # python: svc.customers().get(**params).execute()
    calls:
      - action: customers.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        name: '{{ response.name }}'
        sasUserIds: '{{ response.sasUserIds }}'
  # Returns a list of requested customers.
  # ── gcp.prod_tt_sasportal.customers.list ──
  - discovery_id: gcp.prod_tt_sasportal.customers.list
    # python: svc.customers().list(**params).execute()
    calls:
      - action: customers.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.customers }}'
      item:
        displayName: '{{ item.displayName }}'
        name: '{{ item.name }}'
        sasUserIds: '{{ item.sasUserIds }}'
  # Returns a list of SAS deployments associated with current GCP project. Includes whether SAS analytic
  # ── gcp.prod_tt_sasportal.customers.listGcpProjectDeployments ──
  - discovery_id: gcp.prod_tt_sasportal.customers.listGcpProjectDeployments
    # python: svc.customers().listGcpProjectDeployments(**params).execute()
    calls:
      - action: customers.listGcpProjectDeployments
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.customers }}'
      item:
        deployment: '{{ item.deployment }}'
        hasEnabledAnalytics: '{{ item.hasEnabledAnalytics }}'
  # Returns a list of legacy organizations.
  # ── gcp.prod_tt_sasportal.customers.listLegacyOrganizations ──
  - discovery_id: gcp.prod_tt_sasportal.customers.listLegacyOrganizations
    # python: svc.customers().listLegacyOrganizations(**params).execute()
    calls:
      - action: customers.listLegacyOrganizations
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.customers }}'
      item:
        displayName: '{{ item.displayName }}'
        id: '{{ item.id }}'
  # Lists deployments.
  # ── gcp.prod_tt_sasportal.customers.nodes.deployments.list ──
  - discovery_id: gcp.prod_tt_sasportal.customers.nodes.deployments.list
    # python: svc.customers().nodes().deployments().list(**params).execute()
    calls:
      - action: customers.nodes.deployments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.deployments }}'
      item:
        displayName: '{{ item.displayName }}'
        frns: '{{ item.frns }}'
        name: '{{ item.name }}'
        sasUserIds: '{{ item.sasUserIds }}'
  # Lists devices under a node or customer.
  # ── gcp.prod_tt_sasportal.customers.nodes.devices.list ──
  - discovery_id: gcp.prod_tt_sasportal.customers.nodes.devices.list
    # python: svc.customers().nodes().devices().list(**params).execute()
    calls:
      - action: customers.nodes.devices.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.devices }}'
      item:
        activeConfig: '{{ item.activeConfig }}'
        currentChannels: '{{ item.currentChannels }}'
        deviceMetadata: '{{ item.deviceMetadata }}'
        displayName: '{{ item.displayName }}'
        fccId: '{{ item.fccId }}'
        grantRangeAllowlists: '{{ item.grantRangeAllowlists }}'
        grants: '{{ item.grants }}'
        name: '{{ item.name }}'
        preloadedConfig: '{{ item.preloadedConfig }}'
        serialNumber: '{{ item.serialNumber }}'
        state: '{{ item.state }}'
  # Lists nodes.
  # ── gcp.prod_tt_sasportal.customers.nodes.list ──
  - discovery_id: gcp.prod_tt_sasportal.customers.nodes.list
    # python: svc.customers().nodes().list(**params).execute()
    calls:
      - action: customers.nodes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.nodes }}'
      item:
        displayName: '{{ item.displayName }}'
        name: '{{ item.name }}'
        sasUserIds: '{{ item.sasUserIds }}'
  # Lists nodes.
  # ── gcp.prod_tt_sasportal.customers.nodes.nodes.list ──
  - discovery_id: gcp.prod_tt_sasportal.customers.nodes.nodes.list
    # python: svc.customers().nodes().nodes().list(**params).execute()
    calls:
      - action: customers.nodes.nodes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.nodes }}'
      item:
        displayName: '{{ item.displayName }}'
        name: '{{ item.name }}'
        sasUserIds: '{{ item.sasUserIds }}'
  # Lists devices under a node or customer.
  # ── gcp.prod_tt_sasportal.nodes.deployments.devices.list ──
  - discovery_id: gcp.prod_tt_sasportal.nodes.deployments.devices.list
    # python: svc.nodes().deployments().devices().list(**params).execute()
    calls:
      - action: nodes.deployments.devices.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.devices }}'
      item:
        activeConfig: '{{ item.activeConfig }}'
        currentChannels: '{{ item.currentChannels }}'
        deviceMetadata: '{{ item.deviceMetadata }}'
        displayName: '{{ item.displayName }}'
        fccId: '{{ item.fccId }}'
        grantRangeAllowlists: '{{ item.grantRangeAllowlists }}'
        grants: '{{ item.grants }}'
        name: '{{ item.name }}'
        preloadedConfig: '{{ item.preloadedConfig }}'
        serialNumber: '{{ item.serialNumber }}'
        state: '{{ item.state }}'
  # Lists deployments.
  # ── gcp.prod_tt_sasportal.nodes.deployments.list ──
  - discovery_id: gcp.prod_tt_sasportal.nodes.deployments.list
    # python: svc.nodes().deployments().list(**params).execute()
    calls:
      - action: nodes.deployments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.deployments }}'
      item:
        displayName: '{{ item.displayName }}'
        frns: '{{ item.frns }}'
        name: '{{ item.name }}'
        sasUserIds: '{{ item.sasUserIds }}'
  # Lists devices under a node or customer.
  # ── gcp.prod_tt_sasportal.nodes.devices.list ──
  - discovery_id: gcp.prod_tt_sasportal.nodes.devices.list
    # python: svc.nodes().devices().list(**params).execute()
    calls:
      - action: nodes.devices.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.devices }}'
      item:
        activeConfig: '{{ item.activeConfig }}'
        currentChannels: '{{ item.currentChannels }}'
        deviceMetadata: '{{ item.deviceMetadata }}'
        displayName: '{{ item.displayName }}'
        fccId: '{{ item.fccId }}'
        grantRangeAllowlists: '{{ item.grantRangeAllowlists }}'
        grants: '{{ item.grants }}'
        name: '{{ item.name }}'
        preloadedConfig: '{{ item.preloadedConfig }}'
        serialNumber: '{{ item.serialNumber }}'
        state: '{{ item.state }}'
  # Lists deployments.
  # ── gcp.prod_tt_sasportal.nodes.nodes.deployments.list ──
  - discovery_id: gcp.prod_tt_sasportal.nodes.nodes.deployments.list
    # python: svc.nodes().nodes().deployments().list(**params).execute()
    calls:
      - action: nodes.nodes.deployments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.deployments }}'
      item:
        displayName: '{{ item.displayName }}'
        frns: '{{ item.frns }}'
        name: '{{ item.name }}'
        sasUserIds: '{{ item.sasUserIds }}'
  # Lists devices under a node or customer.
  # ── gcp.prod_tt_sasportal.nodes.nodes.devices.list ──
  - discovery_id: gcp.prod_tt_sasportal.nodes.nodes.devices.list
    # python: svc.nodes().nodes().devices().list(**params).execute()
    calls:
      - action: nodes.nodes.devices.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.devices }}'
      item:
        activeConfig: '{{ item.activeConfig }}'
        currentChannels: '{{ item.currentChannels }}'
        deviceMetadata: '{{ item.deviceMetadata }}'
        displayName: '{{ item.displayName }}'
        fccId: '{{ item.fccId }}'
        grantRangeAllowlists: '{{ item.grantRangeAllowlists }}'
        grants: '{{ item.grants }}'
        name: '{{ item.name }}'
        preloadedConfig: '{{ item.preloadedConfig }}'
        serialNumber: '{{ item.serialNumber }}'
        state: '{{ item.state }}'
  # Lists nodes.
  # ── gcp.prod_tt_sasportal.nodes.nodes.list ──
  - discovery_id: gcp.prod_tt_sasportal.nodes.nodes.list
    # python: svc.nodes().nodes().list(**params).execute()
    calls:
      - action: nodes.nodes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.nodes }}'
      item:
        displayName: '{{ item.displayName }}'
        name: '{{ item.name }}'
        sasUserIds: '{{ item.sasUserIds }}'
  # Lists nodes.
  # ── gcp.prod_tt_sasportal.nodes.nodes.nodes.list ──
  - discovery_id: gcp.prod_tt_sasportal.nodes.nodes.nodes.list
    # python: svc.nodes().nodes().nodes().list(**params).execute()
    calls:
      - action: nodes.nodes.nodes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.nodes }}'
      item:
        displayName: '{{ item.displayName }}'
        name: '{{ item.name }}'
        sasUserIds: '{{ item.sasUserIds }}'
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.prod_tt_sasportal.policies.get ──
  - discovery_id: gcp.prod_tt_sasportal.policies.get
    # python: svc.policies().get(**params).execute()
    calls:
      - action: policies.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        assignments: '{{ response.assignments }}'

  # ════ DEPENDENT (enrich) operations ════
  # Returns a requested deployment.
  # ── gcp.prod_tt_sasportal.customers.deployments.get [dependent] ──
  - discovery_id: gcp.prod_tt_sasportal.customers.deployments.get
    for_each: gcp.prod_tt_sasportal.nodes.nodes.deployments.list
    # python: svc.customers().deployments().get(**params).execute()
    calls:
      - action: customers.deployments.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        frns: '{{ response.frns }}'
        name: '{{ response.name }}'
        sasUserIds: '{{ response.sasUserIds }}'
    # required_params: ['name']
  # Gets details about a device.
  # ── gcp.prod_tt_sasportal.customers.devices.get [dependent] ──
  - discovery_id: gcp.prod_tt_sasportal.customers.devices.get
    for_each: gcp.prod_tt_sasportal.nodes.nodes.devices.list
    # python: svc.customers().devices().get(**params).execute()
    calls:
      - action: customers.devices.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        activeConfig: '{{ response.activeConfig }}'
        currentChannels: '{{ response.currentChannels }}'
        deviceMetadata: '{{ response.deviceMetadata }}'
        displayName: '{{ response.displayName }}'
        fccId: '{{ response.fccId }}'
        grantRangeAllowlists: '{{ response.grantRangeAllowlists }}'
        grants: '{{ response.grants }}'
        name: '{{ response.name }}'
        preloadedConfig: '{{ response.preloadedConfig }}'
        serialNumber: '{{ response.serialNumber }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Returns a requested node.
  # ── gcp.prod_tt_sasportal.customers.nodes.get [dependent] ──
  - discovery_id: gcp.prod_tt_sasportal.customers.nodes.get
    for_each: gcp.prod_tt_sasportal.nodes.nodes.list
    # python: svc.customers().nodes().get(**params).execute()
    calls:
      - action: customers.nodes.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        name: '{{ response.name }}'
        sasUserIds: '{{ response.sasUserIds }}'
    # required_params: ['name']
  # Gets details about a device.
  # ── gcp.prod_tt_sasportal.deployments.devices.get [dependent] ──
  - discovery_id: gcp.prod_tt_sasportal.deployments.devices.get
    for_each: gcp.prod_tt_sasportal.nodes.nodes.devices.list
    # python: svc.deployments().devices().get(**params).execute()
    calls:
      - action: deployments.devices.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        activeConfig: '{{ response.activeConfig }}'
        currentChannels: '{{ response.currentChannels }}'
        deviceMetadata: '{{ response.deviceMetadata }}'
        displayName: '{{ response.displayName }}'
        fccId: '{{ response.fccId }}'
        grantRangeAllowlists: '{{ response.grantRangeAllowlists }}'
        grants: '{{ response.grants }}'
        name: '{{ response.name }}'
        preloadedConfig: '{{ response.preloadedConfig }}'
        serialNumber: '{{ response.serialNumber }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Returns a requested deployment.
  # ── gcp.prod_tt_sasportal.deployments.get [dependent] ──
  - discovery_id: gcp.prod_tt_sasportal.deployments.get
    for_each: gcp.prod_tt_sasportal.nodes.nodes.deployments.list
    # python: svc.deployments().get(**params).execute()
    calls:
      - action: deployments.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        frns: '{{ response.frns }}'
        name: '{{ response.name }}'
        sasUserIds: '{{ response.sasUserIds }}'
    # required_params: ['name']
  # Returns a requested deployment.
  # ── gcp.prod_tt_sasportal.nodes.deployments.get [dependent] ──
  - discovery_id: gcp.prod_tt_sasportal.nodes.deployments.get
    for_each: gcp.prod_tt_sasportal.nodes.nodes.deployments.list
    # python: svc.nodes().deployments().get(**params).execute()
    calls:
      - action: nodes.deployments.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        frns: '{{ response.frns }}'
        name: '{{ response.name }}'
        sasUserIds: '{{ response.sasUserIds }}'
    # required_params: ['name']
  # Gets details about a device.
  # ── gcp.prod_tt_sasportal.nodes.devices.get [dependent] ──
  - discovery_id: gcp.prod_tt_sasportal.nodes.devices.get
    for_each: gcp.prod_tt_sasportal.nodes.nodes.devices.list
    # python: svc.nodes().devices().get(**params).execute()
    calls:
      - action: nodes.devices.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        activeConfig: '{{ response.activeConfig }}'
        currentChannels: '{{ response.currentChannels }}'
        deviceMetadata: '{{ response.deviceMetadata }}'
        displayName: '{{ response.displayName }}'
        fccId: '{{ response.fccId }}'
        grantRangeAllowlists: '{{ response.grantRangeAllowlists }}'
        grants: '{{ response.grants }}'
        name: '{{ response.name }}'
        preloadedConfig: '{{ response.preloadedConfig }}'
        serialNumber: '{{ response.serialNumber }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Returns a requested node.
  # ── gcp.prod_tt_sasportal.nodes.get [dependent] ──
  - discovery_id: gcp.prod_tt_sasportal.nodes.get
    for_each: gcp.prod_tt_sasportal.nodes.nodes.list
    # python: svc.nodes().get(**params).execute()
    calls:
      - action: nodes.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        name: '{{ response.name }}'
        sasUserIds: '{{ response.sasUserIds }}'
    # required_params: ['name']
  # Returns a requested node.
  # ── gcp.prod_tt_sasportal.nodes.nodes.get [dependent] ──
  - discovery_id: gcp.prod_tt_sasportal.nodes.nodes.get
    for_each: gcp.prod_tt_sasportal.nodes.nodes.list
    # python: svc.nodes().nodes().get(**params).execute()
    calls:
      - action: nodes.nodes.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        name: '{{ response.name }}'
        sasUserIds: '{{ response.sasUserIds }}'
    # required_params: ['name']
