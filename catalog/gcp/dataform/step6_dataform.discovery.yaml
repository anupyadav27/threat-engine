# ============================================================
# Discovery YAML — dataform (v1)
# Generated: 2026-02-21T03:06:39Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 34 | independent: 11 | dependent: 23
# ops with fields: 34/34 | dep ops with chain: 22/23
# ============================================================
version: '1.0'
provider: gcp
service: dataform

services:
  client: dataform
  module: "googleapiclient.discovery.build('dataform', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.dataform.projects.locations.folders.getIamPolicy ──
  - discovery_id: gcp.dataform.projects.locations.folders.getIamPolicy
    # python: svc.projects().locations().folders().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.folders.getIamPolicy
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
  # Gets information about a location.
  # ── gcp.dataform.projects.locations.get ──
  - discovery_id: gcp.dataform.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Get default config for a given project and location.
  # ── gcp.dataform.projects.locations.getConfig ──
  - discovery_id: gcp.dataform.projects.locations.getConfig
    # python: svc.projects().locations().getConfig(**params).execute()
    calls:
      - action: projects.locations.getConfig
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        defaultKmsKeyName: '{{ response.defaultKmsKeyName }}'
        internalMetadata: '{{ response.internalMetadata }}'
        name: '{{ response.name }}'
  # Lists information about the supported locations for this service.
  # ── gcp.dataform.projects.locations.list ──
  - discovery_id: gcp.dataform.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.dataform.projects.locations.operations.list ──
  - discovery_id: gcp.dataform.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists CompilationResults in a given Repository.
  # ── gcp.dataform.projects.locations.repositories.compilationResults.list ──
  - discovery_id: gcp.dataform.projects.locations.repositories.compilationResults.list
    # python: svc.projects().locations().repositories().compilationResults().list(**params).execute()
    calls:
      - action: projects.locations.repositories.compilationResults.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.compilationResults }}'
      item:
        codeCompilationConfig: '{{ item.codeCompilationConfig }}'
        compilationErrors: '{{ item.compilationErrors }}'
        createTime: '{{ item.createTime }}'
        dataEncryptionState: '{{ item.dataEncryptionState }}'
        dataformCoreVersion: '{{ item.dataformCoreVersion }}'
        gitCommitish: '{{ item.gitCommitish }}'
        internalMetadata: '{{ item.internalMetadata }}'
        name: '{{ item.name }}'
        privateResourceMetadata: '{{ item.privateResourceMetadata }}'
        releaseConfig: '{{ item.releaseConfig }}'
        resolvedGitCommitSha: '{{ item.resolvedGitCommitSha }}'
        workspace: '{{ item.workspace }}'
  # Lists Repositories in a given project and location. **Note:** *This method can return repositories n
  # ── gcp.dataform.projects.locations.repositories.list ──
  - discovery_id: gcp.dataform.projects.locations.repositories.list
    # python: svc.projects().locations().repositories().list(**params).execute()
    calls:
      - action: projects.locations.repositories.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.repositories }}'
      item:
        createTime: '{{ item.createTime }}'
        dataEncryptionState: '{{ item.dataEncryptionState }}'
        displayName: '{{ item.displayName }}'
        gitRemoteSettings: '{{ item.gitRemoteSettings }}'
        internalMetadata: '{{ item.internalMetadata }}'
        kmsKeyName: '{{ item.kmsKeyName }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        npmrcEnvironmentVariablesSecretVersion: '{{ item.npmrcEnvironmentVariablesSecretVersion }}'
        serviceAccount: '{{ item.serviceAccount }}'
        setAuthenticatedUserAdmin: '{{ item.setAuthenticatedUserAdmin }}'
        workspaceCompilationOverrides: '{{ item.workspaceCompilationOverrides }}'
  # Lists ReleaseConfigs in a given Repository.
  # ── gcp.dataform.projects.locations.repositories.releaseConfigs.list ──
  - discovery_id: gcp.dataform.projects.locations.repositories.releaseConfigs.list
    # python: svc.projects().locations().repositories().releaseConfigs().list(**params).execute()
    calls:
      - action: projects.locations.repositories.releaseConfigs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.releaseConfigs }}'
      item:
        codeCompilationConfig: '{{ item.codeCompilationConfig }}'
        cronSchedule: '{{ item.cronSchedule }}'
        disabled: '{{ item.disabled }}'
        gitCommitish: '{{ item.gitCommitish }}'
        internalMetadata: '{{ item.internalMetadata }}'
        name: '{{ item.name }}'
        recentScheduledReleaseRecords: '{{ item.recentScheduledReleaseRecords }}'
        releaseCompilationResult: '{{ item.releaseCompilationResult }}'
        timeZone: '{{ item.timeZone }}'
  # Lists WorkflowConfigs in a given Repository.
  # ── gcp.dataform.projects.locations.repositories.workflowConfigs.list ──
  - discovery_id: gcp.dataform.projects.locations.repositories.workflowConfigs.list
    # python: svc.projects().locations().repositories().workflowConfigs().list(**params).execute()
    calls:
      - action: projects.locations.repositories.workflowConfigs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.workflowConfigs }}'
      item:
        createTime: '{{ item.createTime }}'
        cronSchedule: '{{ item.cronSchedule }}'
        disabled: '{{ item.disabled }}'
        internalMetadata: '{{ item.internalMetadata }}'
        invocationConfig: '{{ item.invocationConfig }}'
        name: '{{ item.name }}'
        recentScheduledExecutionRecords: '{{ item.recentScheduledExecutionRecords }}'
        releaseConfig: '{{ item.releaseConfig }}'
        timeZone: '{{ item.timeZone }}'
        updateTime: '{{ item.updateTime }}'
  # Lists WorkflowInvocations in a given Repository.
  # ── gcp.dataform.projects.locations.repositories.workflowInvocations.list ──
  - discovery_id: gcp.dataform.projects.locations.repositories.workflowInvocations.list
    # python: svc.projects().locations().repositories().workflowInvocations().list(**params).execute()
    calls:
      - action: projects.locations.repositories.workflowInvocations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.workflowInvocations }}'
      item:
        compilationResult: '{{ item.compilationResult }}'
        dataEncryptionState: '{{ item.dataEncryptionState }}'
        internalMetadata: '{{ item.internalMetadata }}'
        invocationConfig: '{{ item.invocationConfig }}'
        invocationTiming: '{{ item.invocationTiming }}'
        name: '{{ item.name }}'
        privateResourceMetadata: '{{ item.privateResourceMetadata }}'
        resolvedCompilationResult: '{{ item.resolvedCompilationResult }}'
        state: '{{ item.state }}'
        workflowConfig: '{{ item.workflowConfig }}'
  # Lists Workspaces in a given Repository.
  # ── gcp.dataform.projects.locations.repositories.workspaces.list ──
  - discovery_id: gcp.dataform.projects.locations.repositories.workspaces.list
    # python: svc.projects().locations().repositories().workspaces().list(**params).execute()
    calls:
      - action: projects.locations.repositories.workspaces.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.workspaces }}'
      item:
        createTime: '{{ item.createTime }}'
        dataEncryptionState: '{{ item.dataEncryptionState }}'
        internalMetadata: '{{ item.internalMetadata }}'
        name: '{{ item.name }}'
        privateResourceMetadata: '{{ item.privateResourceMetadata }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.dataform.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.dataform.projects.locations.operations.get
    for_each: gcp.dataform.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Fetches a single CompilationResult.
  # ── gcp.dataform.projects.locations.repositories.compilationResults.get [dependent] ──
  - discovery_id: gcp.dataform.projects.locations.repositories.compilationResults.get
    for_each: gcp.dataform.projects.locations.repositories.compilationResults.list
    # python: svc.projects().locations().repositories().compilationResults().get(**params).execute()
    calls:
      - action: projects.locations.repositories.compilationResults.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        codeCompilationConfig: '{{ response.codeCompilationConfig }}'
        compilationErrors: '{{ response.compilationErrors }}'
        createTime: '{{ response.createTime }}'
        dataEncryptionState: '{{ response.dataEncryptionState }}'
        dataformCoreVersion: '{{ response.dataformCoreVersion }}'
        gitCommitish: '{{ response.gitCommitish }}'
        internalMetadata: '{{ response.internalMetadata }}'
        name: '{{ response.name }}'
        privateResourceMetadata: '{{ response.privateResourceMetadata }}'
        releaseConfig: '{{ response.releaseConfig }}'
        resolvedGitCommitSha: '{{ response.resolvedGitCommitSha }}'
        workspace: '{{ response.workspace }}'
    # required_params: ['name']
  # Returns CompilationResultActions in a given CompilationResult.
  # ── gcp.dataform.projects.locations.repositories.compilationResults.query [dependent] ──
  - discovery_id: gcp.dataform.projects.locations.repositories.compilationResults.query
    for_each: gcp.dataform.projects.locations.repositories.compilationResults.list
    # python: svc.projects().locations().repositories().compilationResults().query(**params).execute()
    calls:
      - action: projects.locations.repositories.compilationResults.query
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.compilationResults }}'
      item:
        assertion: '{{ item.assertion }}'
        canonicalTarget: '{{ item.canonicalTarget }}'
        dataPreparation: '{{ item.dataPreparation }}'
        declaration: '{{ item.declaration }}'
        filePath: '{{ item.filePath }}'
        internalMetadata: '{{ item.internalMetadata }}'
        notebook: '{{ item.notebook }}'
        operations: '{{ item.operations }}'
        relation: '{{ item.relation }}'
        target: '{{ item.target }}'
    # required_params: ['name']
  # Computes a Repository's Git access token status.
  # ── gcp.dataform.projects.locations.repositories.computeAccessTokenStatus [dependent] ──
  - discovery_id: gcp.dataform.projects.locations.repositories.computeAccessTokenStatus
    for_each: gcp.dataform.projects.locations.repositories.list
    # python: svc.projects().locations().repositories().computeAccessTokenStatus(**params).execute()
    calls:
      - action: projects.locations.repositories.computeAccessTokenStatus
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        tokenStatus: '{{ response.tokenStatus }}'
    # required_params: ['name']
  # Fetches a Repository's history of commits. The Repository must not have a value for `git_remote_sett
  # ── gcp.dataform.projects.locations.repositories.fetchHistory [dependent] ──
  - discovery_id: gcp.dataform.projects.locations.repositories.fetchHistory
    for_each: gcp.dataform.projects.locations.repositories.list
    # python: svc.projects().locations().repositories().fetchHistory(**params).execute()
    calls:
      - action: projects.locations.repositories.fetchHistory
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        commits: '{{ response.commits }}'
    # required_params: ['name']
  # Fetches a Repository's remote branches.
  # ── gcp.dataform.projects.locations.repositories.fetchRemoteBranches [dependent] ──
  - discovery_id: gcp.dataform.projects.locations.repositories.fetchRemoteBranches
    for_each: gcp.dataform.projects.locations.repositories.list
    # python: svc.projects().locations().repositories().fetchRemoteBranches(**params).execute()
    calls:
      - action: projects.locations.repositories.fetchRemoteBranches
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        branches: '{{ response.branches }}'
    # required_params: ['name']
  # Fetches a single Repository.
  # ── gcp.dataform.projects.locations.repositories.get [dependent] ──
  - discovery_id: gcp.dataform.projects.locations.repositories.get
    for_each: gcp.dataform.projects.locations.repositories.list
    # python: svc.projects().locations().repositories().get(**params).execute()
    calls:
      - action: projects.locations.repositories.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        dataEncryptionState: '{{ response.dataEncryptionState }}'
        displayName: '{{ response.displayName }}'
        gitRemoteSettings: '{{ response.gitRemoteSettings }}'
        internalMetadata: '{{ response.internalMetadata }}'
        kmsKeyName: '{{ response.kmsKeyName }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        npmrcEnvironmentVariablesSecretVersion: '{{ response.npmrcEnvironmentVariablesSecretVersion }}'
        serviceAccount: '{{ response.serviceAccount }}'
        setAuthenticatedUserAdmin: '{{ response.setAuthenticatedUserAdmin }}'
        workspaceCompilationOverrides: '{{ response.workspaceCompilationOverrides }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.dataform.projects.locations.repositories.getIamPolicy [dependent] ──
  - discovery_id: gcp.dataform.projects.locations.repositories.getIamPolicy
    for_each: gcp.dataform.projects.locations.repositories.list
    # python: svc.projects().locations().repositories().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.repositories.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Returns the contents of a given Repository directory. The Repository must not have a value for `git_
  # ── gcp.dataform.projects.locations.repositories.queryDirectoryContents [dependent] ──
  - discovery_id: gcp.dataform.projects.locations.repositories.queryDirectoryContents
    for_each: gcp.dataform.projects.locations.repositories.list
    # python: svc.projects().locations().repositories().queryDirectoryContents(**params).execute()
    calls:
      - action: projects.locations.repositories.queryDirectoryContents
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        directoryEntries: '{{ response.directoryEntries }}'
    # required_params: ['name']
  # Returns the contents of a file (inside a Repository). The Repository must not have a value for `git_
  # ── gcp.dataform.projects.locations.repositories.readFile [dependent] ──
  - discovery_id: gcp.dataform.projects.locations.repositories.readFile
    for_each: gcp.dataform.projects.locations.repositories.list
    # python: svc.projects().locations().repositories().readFile(**params).execute()
    calls:
      - action: projects.locations.repositories.readFile
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contents: '{{ response.contents }}'
    # required_params: ['name']
  # Fetches a single ReleaseConfig.
  # ── gcp.dataform.projects.locations.repositories.releaseConfigs.get [dependent] ──
  - discovery_id: gcp.dataform.projects.locations.repositories.releaseConfigs.get
    for_each: gcp.dataform.projects.locations.repositories.releaseConfigs.list
    # python: svc.projects().locations().repositories().releaseConfigs().get(**params).execute()
    calls:
      - action: projects.locations.repositories.releaseConfigs.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        codeCompilationConfig: '{{ response.codeCompilationConfig }}'
        cronSchedule: '{{ response.cronSchedule }}'
        disabled: '{{ response.disabled }}'
        gitCommitish: '{{ response.gitCommitish }}'
        internalMetadata: '{{ response.internalMetadata }}'
        name: '{{ response.name }}'
        recentScheduledReleaseRecords: '{{ response.recentScheduledReleaseRecords }}'
        releaseCompilationResult: '{{ response.releaseCompilationResult }}'
        timeZone: '{{ response.timeZone }}'
    # required_params: ['name']
  # Fetches a single WorkflowConfig.
  # ── gcp.dataform.projects.locations.repositories.workflowConfigs.get [dependent] ──
  - discovery_id: gcp.dataform.projects.locations.repositories.workflowConfigs.get
    for_each: gcp.dataform.projects.locations.repositories.workflowConfigs.list
    # python: svc.projects().locations().repositories().workflowConfigs().get(**params).execute()
    calls:
      - action: projects.locations.repositories.workflowConfigs.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        cronSchedule: '{{ response.cronSchedule }}'
        disabled: '{{ response.disabled }}'
        internalMetadata: '{{ response.internalMetadata }}'
        invocationConfig: '{{ response.invocationConfig }}'
        name: '{{ response.name }}'
        recentScheduledExecutionRecords: '{{ response.recentScheduledExecutionRecords }}'
        releaseConfig: '{{ response.releaseConfig }}'
        timeZone: '{{ response.timeZone }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Fetches a single WorkflowInvocation.
  # ── gcp.dataform.projects.locations.repositories.workflowInvocations.get [dependent] ──
  - discovery_id: gcp.dataform.projects.locations.repositories.workflowInvocations.get
    for_each: gcp.dataform.projects.locations.repositories.workflowInvocations.query
    # python: svc.projects().locations().repositories().workflowInvocations().get(**params).execute()
    calls:
      - action: projects.locations.repositories.workflowInvocations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        compilationResult: '{{ response.compilationResult }}'
        dataEncryptionState: '{{ response.dataEncryptionState }}'
        internalMetadata: '{{ response.internalMetadata }}'
        invocationConfig: '{{ response.invocationConfig }}'
        invocationTiming: '{{ response.invocationTiming }}'
        name: '{{ response.name }}'
        privateResourceMetadata: '{{ response.privateResourceMetadata }}'
        resolvedCompilationResult: '{{ response.resolvedCompilationResult }}'
        state: '{{ response.state }}'
        workflowConfig: '{{ response.workflowConfig }}'
    # required_params: ['name']
  # Returns WorkflowInvocationActions in a given WorkflowInvocation.
  # ── gcp.dataform.projects.locations.repositories.workflowInvocations.query [dependent] ──
  - discovery_id: gcp.dataform.projects.locations.repositories.workflowInvocations.query
    for_each: gcp.dataform.projects.locations.repositories.list
    # python: svc.projects().locations().repositories().workflowInvocations().query(**params).execute()
    calls:
      - action: projects.locations.repositories.workflowInvocations.query
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.workflowInvocations }}'
      item:
        bigqueryAction: '{{ item.bigqueryAction }}'
        canonicalTarget: '{{ item.canonicalTarget }}'
        dataPreparationAction: '{{ item.dataPreparationAction }}'
        failureReason: '{{ item.failureReason }}'
        internalMetadata: '{{ item.internalMetadata }}'
        invocationTiming: '{{ item.invocationTiming }}'
        notebookAction: '{{ item.notebookAction }}'
        state: '{{ item.state }}'
        target: '{{ item.target }}'
    # required_params: ['name']
  # Fetches Git diff for an uncommitted file in a Workspace.
  # ── gcp.dataform.projects.locations.repositories.workspaces.fetchFileDiff [dependent] ──
  - discovery_id: gcp.dataform.projects.locations.repositories.workspaces.fetchFileDiff
    for_each: gcp.dataform.projects.locations.repositories.workspaces.list
    # python: svc.projects().locations().repositories().workspaces().fetchFileDiff(**params).execute()
    calls:
      - action: projects.locations.repositories.workspaces.fetchFileDiff
        params:
          workspace: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        formattedDiff: '{{ response.formattedDiff }}'
    # required_params: ['workspace']
  # Fetches Git statuses for the files in a Workspace.
  # ── gcp.dataform.projects.locations.repositories.workspaces.fetchFileGitStatuses [dependent] ──
  - discovery_id: gcp.dataform.projects.locations.repositories.workspaces.fetchFileGitStatuses
    for_each: gcp.dataform.projects.locations.repositories.workspaces.list
    # python: svc.projects().locations().repositories().workspaces().fetchFileGitStatuses(**params).execute()
    calls:
      - action: projects.locations.repositories.workspaces.fetchFileGitStatuses
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        uncommittedFileChanges: '{{ response.uncommittedFileChanges }}'
    # required_params: ['name']
  # Fetches Git ahead/behind against a remote branch.
  # ── gcp.dataform.projects.locations.repositories.workspaces.fetchGitAheadBehind [dependent] ──
  - discovery_id: gcp.dataform.projects.locations.repositories.workspaces.fetchGitAheadBehind
    for_each: gcp.dataform.projects.locations.repositories.workspaces.list
    # python: svc.projects().locations().repositories().workspaces().fetchGitAheadBehind(**params).execute()
    calls:
      - action: projects.locations.repositories.workspaces.fetchGitAheadBehind
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        commitsAhead: '{{ response.commitsAhead }}'
        commitsBehind: '{{ response.commitsBehind }}'
    # required_params: ['name']
  # Fetches a single Workspace.
  # ── gcp.dataform.projects.locations.repositories.workspaces.get [dependent] ──
  - discovery_id: gcp.dataform.projects.locations.repositories.workspaces.get
    for_each: gcp.dataform.projects.locations.repositories.workspaces.list
    # python: svc.projects().locations().repositories().workspaces().get(**params).execute()
    calls:
      - action: projects.locations.repositories.workspaces.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        dataEncryptionState: '{{ response.dataEncryptionState }}'
        internalMetadata: '{{ response.internalMetadata }}'
        name: '{{ response.name }}'
        privateResourceMetadata: '{{ response.privateResourceMetadata }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.dataform.projects.locations.repositories.workspaces.getIamPolicy [dependent] ──
  - discovery_id: gcp.dataform.projects.locations.repositories.workspaces.getIamPolicy
    for_each: gcp.dataform.projects.locations.repositories.workspaces.list
    # python: svc.projects().locations().repositories().workspaces().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.repositories.workspaces.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Returns the contents of a given Workspace directory.
  # ── gcp.dataform.projects.locations.repositories.workspaces.queryDirectoryContents [dependent] ──
  - discovery_id: gcp.dataform.projects.locations.repositories.workspaces.queryDirectoryContents
    for_each: gcp.dataform.projects.locations.repositories.workspaces.list
    # python: svc.projects().locations().repositories().workspaces().queryDirectoryContents(**params).execute()
    calls:
      - action: projects.locations.repositories.workspaces.queryDirectoryContents
        params:
          workspace: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        directoryEntries: '{{ response.directoryEntries }}'
    # required_params: ['workspace']
  # Returns the contents of a file (inside a Workspace).
  # ── gcp.dataform.projects.locations.repositories.workspaces.readFile [dependent] ──
  - discovery_id: gcp.dataform.projects.locations.repositories.workspaces.readFile
    for_each: gcp.dataform.projects.locations.repositories.workspaces.list
    # python: svc.projects().locations().repositories().workspaces().readFile(**params).execute()
    calls:
      - action: projects.locations.repositories.workspaces.readFile
        params:
          workspace: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        fileContents: '{{ response.fileContents }}'
    # required_params: ['workspace']
  # Finds the contents of a given Workspace directory by filter.
  # ── gcp.dataform.projects.locations.repositories.workspaces.searchFiles [dependent] ──
  - discovery_id: gcp.dataform.projects.locations.repositories.workspaces.searchFiles
    for_each: gcp.dataform.projects.locations.repositories.workspaces.list
    # python: svc.projects().locations().repositories().workspaces().searchFiles(**params).execute()
    calls:
      - action: projects.locations.repositories.workspaces.searchFiles
        params:
          workspace: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        searchResults: '{{ response.searchResults }}'
    # required_params: ['workspace']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.dataform.projects.locations.teamFolders.getIamPolicy [dependent] ──
  - discovery_id: gcp.dataform.projects.locations.teamFolders.getIamPolicy
    # python: svc.projects().locations().teamFolders().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.teamFolders.getIamPolicy
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
