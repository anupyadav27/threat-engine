# ============================================================
# Discovery YAML — apigee (v1)
# Generated: 2026-02-21T03:06:38Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 161 | independent: 64 | dependent: 97
# ops with fields: 161/161 | dep ops with chain: 94/97
# ============================================================
version: '1.0'
provider: gcp
service: apigee

services:
  client: apigee
  module: "googleapiclient.discovery.build('apigee', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists hybrid services and its trusted issuers service account ids. This api is authenticated and una
  # ── gcp.apigee.hybrid.issuers.list ──
  - discovery_id: gcp.apigee.hybrid.issuers.list
    # python: svc.hybrid().issuers().list(**params).execute()
    calls:
      - action: hybrid.issuers.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.issuers }}'
      item:
        emailIds: '{{ item.emailIds }}'
        service: '{{ item.service }}'
  # List Datastores
  # ── gcp.apigee.organizations.analytics.datastores.list ──
  - discovery_id: gcp.apigee.organizations.analytics.datastores.list
    # python: svc.organizations().analytics().datastores().list(**params).execute()
    calls:
      - action: organizations.analytics.datastores.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.datastores }}'
      item:
        createTime: '{{ item.createTime }}'
        datastoreConfig: '{{ item.datastoreConfig }}'
        displayName: '{{ item.displayName }}'
        lastUpdateTime: '{{ item.lastUpdateTime }}'
        org: '{{ item.org }}'
        self: '{{ item.self }}'
        targetType: '{{ item.targetType }}'
  # Lists all APIM service extensions in an organization.
  # ── gcp.apigee.organizations.apimServiceExtensions.list ──
  - discovery_id: gcp.apigee.organizations.apimServiceExtensions.list
    # python: svc.organizations().apimServiceExtensions().list(**params).execute()
    calls:
      - action: organizations.apimServiceExtensions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.apimServiceExtensions }}'
      item:
        createTime: '{{ item.createTime }}'
        extensionProcessor: '{{ item.extensionProcessor }}'
        extensions: '{{ item.extensions }}'
        lbForwardingRule: '{{ item.lbForwardingRule }}'
        name: '{{ item.name }}'
        network: '{{ item.network }}'
        networkConfigs: '{{ item.networkConfigs }}'
        state: '{{ item.state }}'
        updateTime: '{{ item.updateTime }}'
  # Lists all API product attributes.
  # ── gcp.apigee.organizations.apiproducts.attributes.list ──
  - discovery_id: gcp.apigee.organizations.apiproducts.attributes.list
    # python: svc.organizations().apiproducts().attributes().list(**params).execute()
    calls:
      - action: organizations.apiproducts.attributes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.attributes }}'
      item:
        name: '{{ item.name }}'
        value: '{{ item.value }}'
  # Lists all API product names for an organization. Filter the list by passing an `attributename` and `
  # ── gcp.apigee.organizations.apiproducts.list ──
  - discovery_id: gcp.apigee.organizations.apiproducts.list
    # python: svc.organizations().apiproducts().list(**params).execute()
    calls:
      - action: organizations.apiproducts.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.apiproducts }}'
      item:
        apiResources: '{{ item.apiResources }}'
        approvalType: '{{ item.approvalType }}'
        attributes: '{{ item.attributes }}'
        createdAt: '{{ item.createdAt }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        environments: '{{ item.environments }}'
        graphqlOperationGroup: '{{ item.graphqlOperationGroup }}'
        grpcOperationGroup: '{{ item.grpcOperationGroup }}'
        lastModifiedAt: '{{ item.lastModifiedAt }}'
        llmOperationGroup: '{{ item.llmOperationGroup }}'
        llmQuota: '{{ item.llmQuota }}'
        llmQuotaInterval: '{{ item.llmQuotaInterval }}'
        llmQuotaTimeUnit: '{{ item.llmQuotaTimeUnit }}'
        name: '{{ item.name }}'
        operationGroup: '{{ item.operationGroup }}'
        proxies: '{{ item.proxies }}'
        quota: '{{ item.quota }}'
        quotaCounterScope: '{{ item.quotaCounterScope }}'
        quotaInterval: '{{ item.quotaInterval }}'
        quotaTimeUnit: '{{ item.quotaTimeUnit }}'
        scopes: '{{ item.scopes }}'
        space: '{{ item.space }}'
  # Lists all the rate plans for an API product.
  # ── gcp.apigee.organizations.apiproducts.rateplans.list ──
  - discovery_id: gcp.apigee.organizations.apiproducts.rateplans.list
    # python: svc.organizations().apiproducts().rateplans().list(**params).execute()
    calls:
      - action: organizations.apiproducts.rateplans.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.rateplans }}'
      item:
        apiproduct: '{{ item.apiproduct }}'
        billingPeriod: '{{ item.billingPeriod }}'
        consumptionPricingRates: '{{ item.consumptionPricingRates }}'
        consumptionPricingType: '{{ item.consumptionPricingType }}'
        createdAt: '{{ item.createdAt }}'
        currencyCode: '{{ item.currencyCode }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        endTime: '{{ item.endTime }}'
        fixedFeeFrequency: '{{ item.fixedFeeFrequency }}'
        fixedRecurringFee: '{{ item.fixedRecurringFee }}'
        lastModifiedAt: '{{ item.lastModifiedAt }}'
        name: '{{ item.name }}'
        paymentFundingModel: '{{ item.paymentFundingModel }}'
        revenueShareRates: '{{ item.revenueShareRates }}'
        revenueShareType: '{{ item.revenueShareType }}'
        setupFee: '{{ item.setupFee }}'
        startTime: '{{ item.startTime }}'
        state: '{{ item.state }}'
  # Lists debug sessions that are currently active in the given API Proxy.
  # ── gcp.apigee.organizations.apis.debugsessions.list ──
  - discovery_id: gcp.apigee.organizations.apis.debugsessions.list
    # python: svc.organizations().apis().debugsessions().list(**params).execute()
    calls:
      - action: organizations.apis.debugsessions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.debugsessions }}'
      item:
        apiProxyRevisionId: '{{ item.apiProxyRevisionId }}'
        createTime: '{{ item.createTime }}'
        environmentId: '{{ item.environmentId }}'
        id: '{{ item.id }}'
  # Lists all deployments of an API proxy.
  # ── gcp.apigee.organizations.apis.deployments.list ──
  - discovery_id: gcp.apigee.organizations.apis.deployments.list
    # python: svc.organizations().apis().deployments().list(**params).execute()
    calls:
      - action: organizations.apis.deployments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.deployments }}'
      item:
        apiProxy: '{{ item.apiProxy }}'
        deployStartTime: '{{ item.deployStartTime }}'
        environment: '{{ item.environment }}'
        errors: '{{ item.errors }}'
        instances: '{{ item.instances }}'
        pods: '{{ item.pods }}'
        proxyDeploymentType: '{{ item.proxyDeploymentType }}'
        revision: '{{ item.revision }}'
        routeConflicts: '{{ item.routeConflicts }}'
        serviceAccount: '{{ item.serviceAccount }}'
        state: '{{ item.state }}'
  # Lists key value entries for key values maps scoped to an organization, environment, or API proxy. **
  # ── gcp.apigee.organizations.apis.keyvaluemaps.entries.list ──
  - discovery_id: gcp.apigee.organizations.apis.keyvaluemaps.entries.list
    # python: svc.organizations().apis().keyvaluemaps().entries().list(**params).execute()
    calls:
      - action: organizations.apis.keyvaluemaps.entries.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.entries }}'
      item:
        name: '{{ item.name }}'
        value: '{{ item.value }}'
  # Lists the names of all API proxies in an organization. The names returned correspond to the names de
  # ── gcp.apigee.organizations.apis.list ──
  - discovery_id: gcp.apigee.organizations.apis.list
    # python: svc.organizations().apis().list(**params).execute()
    calls:
      - action: organizations.apis.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.apis }}'
      item:
        apiProxyType: '{{ item.apiProxyType }}'
        labels: '{{ item.labels }}'
        latestRevisionId: '{{ item.latestRevisionId }}'
        metaData: '{{ item.metaData }}'
        name: '{{ item.name }}'
        readOnly: '{{ item.readOnly }}'
        revision: '{{ item.revision }}'
        space: '{{ item.space }}'
  # Lists all deployments of an API proxy revision.
  # ── gcp.apigee.organizations.apis.revisions.deployments.list ──
  - discovery_id: gcp.apigee.organizations.apis.revisions.deployments.list
    # python: svc.organizations().apis().revisions().deployments().list(**params).execute()
    calls:
      - action: organizations.apis.revisions.deployments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.deployments }}'
      item:
        apiProxy: '{{ item.apiProxy }}'
        deployStartTime: '{{ item.deployStartTime }}'
        environment: '{{ item.environment }}'
        errors: '{{ item.errors }}'
        instances: '{{ item.instances }}'
        pods: '{{ item.pods }}'
        proxyDeploymentType: '{{ item.proxyDeploymentType }}'
        revision: '{{ item.revision }}'
        routeConflicts: '{{ item.routeConflicts }}'
        serviceAccount: '{{ item.serviceAccount }}'
        state: '{{ item.state }}'
  # Lists all apps created by an AppGroup in an Apigee organization. Optionally, you can request an expa
  # ── gcp.apigee.organizations.appgroups.apps.list ──
  - discovery_id: gcp.apigee.organizations.appgroups.apps.list
    # python: svc.organizations().appgroups().apps().list(**params).execute()
    calls:
      - action: organizations.appgroups.apps.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.apps }}'
      item:
        apiProducts: '{{ item.apiProducts }}'
        appGroup: '{{ item.appGroup }}'
        appId: '{{ item.appId }}'
        attributes: '{{ item.attributes }}'
        callbackUrl: '{{ item.callbackUrl }}'
        createdAt: '{{ item.createdAt }}'
        credentials: '{{ item.credentials }}'
        keyExpiresIn: '{{ item.keyExpiresIn }}'
        lastModifiedAt: '{{ item.lastModifiedAt }}'
        name: '{{ item.name }}'
        scopes: '{{ item.scopes }}'
        status: '{{ item.status }}'
  # Lists all AppGroups in an organization. A maximum of 1000 AppGroups are returned in the response if 
  # ── gcp.apigee.organizations.appgroups.list ──
  - discovery_id: gcp.apigee.organizations.appgroups.list
    # python: svc.organizations().appgroups().list(**params).execute()
    calls:
      - action: organizations.appgroups.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.appgroups }}'
      item:
        appGroupId: '{{ item.appGroupId }}'
        attributes: '{{ item.attributes }}'
        channelId: '{{ item.channelId }}'
        channelUri: '{{ item.channelUri }}'
        createdAt: '{{ item.createdAt }}'
        displayName: '{{ item.displayName }}'
        email: '{{ item.email }}'
        lastModifiedAt: '{{ item.lastModifiedAt }}'
        name: '{{ item.name }}'
        organization: '{{ item.organization }}'
        status: '{{ item.status }}'
  # List all api product subscriptions for an appgroup.
  # ── gcp.apigee.organizations.appgroups.subscriptions.list ──
  - discovery_id: gcp.apigee.organizations.appgroups.subscriptions.list
    # python: svc.organizations().appgroups().subscriptions().list(**params).execute()
    calls:
      - action: organizations.appgroups.subscriptions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.subscriptions }}'
      item:
        apiproduct: '{{ item.apiproduct }}'
        createdAt: '{{ item.createdAt }}'
        endTime: '{{ item.endTime }}'
        lastModifiedAt: '{{ item.lastModifiedAt }}'
        name: '{{ item.name }}'
        startTime: '{{ item.startTime }}'
  # Lists IDs of apps within an organization that have the specified app status (approved or revoked) or
  # ── gcp.apigee.organizations.apps.list ──
  - discovery_id: gcp.apigee.organizations.apps.list
    # python: svc.organizations().apps().list(**params).execute()
    calls:
      - action: organizations.apps.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.apps }}'
      item:
        apiProducts: '{{ item.apiProducts }}'
        appGroup: '{{ item.appGroup }}'
        appId: '{{ item.appId }}'
        attributes: '{{ item.attributes }}'
        callbackUrl: '{{ item.callbackUrl }}'
        companyName: '{{ item.companyName }}'
        createdAt: '{{ item.createdAt }}'
        credentials: '{{ item.credentials }}'
        developerEmail: '{{ item.developerEmail }}'
        developerId: '{{ item.developerId }}'
        keyExpiresIn: '{{ item.keyExpiresIn }}'
        lastModifiedAt: '{{ item.lastModifiedAt }}'
        name: '{{ item.name }}'
        scopes: '{{ item.scopes }}'
        status: '{{ item.status }}'
  # Lists all data collectors.
  # ── gcp.apigee.organizations.datacollectors.list ──
  - discovery_id: gcp.apigee.organizations.datacollectors.list
    # python: svc.organizations().datacollectors().list(**params).execute()
    calls:
      - action: organizations.datacollectors.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.datacollectors }}'
      item:
        createdAt: '{{ item.createdAt }}'
        description: '{{ item.description }}'
        lastModifiedAt: '{{ item.lastModifiedAt }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
  # Lists all deployments of API proxies or shared flows.
  # ── gcp.apigee.organizations.deployments.list ──
  - discovery_id: gcp.apigee.organizations.deployments.list
    # python: svc.organizations().deployments().list(**params).execute()
    calls:
      - action: organizations.deployments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.deployments }}'
      item:
        apiProxy: '{{ item.apiProxy }}'
        deployStartTime: '{{ item.deployStartTime }}'
        environment: '{{ item.environment }}'
        errors: '{{ item.errors }}'
        instances: '{{ item.instances }}'
        pods: '{{ item.pods }}'
        proxyDeploymentType: '{{ item.proxyDeploymentType }}'
        revision: '{{ item.revision }}'
        routeConflicts: '{{ item.routeConflicts }}'
        serviceAccount: '{{ item.serviceAccount }}'
        state: '{{ item.state }}'
  # Returns a list of all developer app attributes.
  # ── gcp.apigee.organizations.developers.apps.attributes.list ──
  - discovery_id: gcp.apigee.organizations.developers.apps.attributes.list
    # python: svc.organizations().developers().apps().attributes().list(**params).execute()
    calls:
      - action: organizations.developers.apps.attributes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.attributes }}'
      item:
        name: '{{ item.name }}'
        value: '{{ item.value }}'
  # Lists all apps created by a developer in an Apigee organization. Optionally, you can request an expa
  # ── gcp.apigee.organizations.developers.apps.list ──
  - discovery_id: gcp.apigee.organizations.developers.apps.list
    # python: svc.organizations().developers().apps().list(**params).execute()
    calls:
      - action: organizations.developers.apps.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.apps }}'
      item:
        apiProducts: '{{ item.apiProducts }}'
        appFamily: '{{ item.appFamily }}'
        appId: '{{ item.appId }}'
        attributes: '{{ item.attributes }}'
        callbackUrl: '{{ item.callbackUrl }}'
        createdAt: '{{ item.createdAt }}'
        credentials: '{{ item.credentials }}'
        developerId: '{{ item.developerId }}'
        keyExpiresIn: '{{ item.keyExpiresIn }}'
        lastModifiedAt: '{{ item.lastModifiedAt }}'
        name: '{{ item.name }}'
        scopes: '{{ item.scopes }}'
        status: '{{ item.status }}'
  # Returns a list of all developer attributes.
  # ── gcp.apigee.organizations.developers.attributes.list ──
  - discovery_id: gcp.apigee.organizations.developers.attributes.list
    # python: svc.organizations().developers().attributes().list(**params).execute()
    calls:
      - action: organizations.developers.attributes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.attributes }}'
      item:
        name: '{{ item.name }}'
        value: '{{ item.value }}'
  # Lists all developers in an organization by email address. By default, the response does not include 
  # ── gcp.apigee.organizations.developers.list ──
  - discovery_id: gcp.apigee.organizations.developers.list
    # python: svc.organizations().developers().list(**params).execute()
    calls:
      - action: organizations.developers.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.developers }}'
      item:
        accessType: '{{ item.accessType }}'
        appFamily: '{{ item.appFamily }}'
        apps: '{{ item.apps }}'
        attributes: '{{ item.attributes }}'
        companies: '{{ item.companies }}'
        createdAt: '{{ item.createdAt }}'
        developerId: '{{ item.developerId }}'
        email: '{{ item.email }}'
        firstName: '{{ item.firstName }}'
        lastModifiedAt: '{{ item.lastModifiedAt }}'
        lastName: '{{ item.lastName }}'
        organizationName: '{{ item.organizationName }}'
        status: '{{ item.status }}'
        userName: '{{ item.userName }}'
  # Lists all API product subscriptions for a developer.
  # ── gcp.apigee.organizations.developers.subscriptions.list ──
  - discovery_id: gcp.apigee.organizations.developers.subscriptions.list
    # python: svc.organizations().developers().subscriptions().list(**params).execute()
    calls:
      - action: organizations.developers.subscriptions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.subscriptions }}'
      item:
        apiproduct: '{{ item.apiproduct }}'
        createdAt: '{{ item.createdAt }}'
        endTime: '{{ item.endTime }}'
        lastModifiedAt: '{{ item.lastModifiedAt }}'
        name: '{{ item.name }}'
        startTime: '{{ item.startTime }}'
  # Enumerates DNS zones that have been created but not yet deleted.
  # ── gcp.apigee.organizations.dnsZones.list ──
  - discovery_id: gcp.apigee.organizations.dnsZones.list
    # python: svc.organizations().dnsZones().list(**params).execute()
    calls:
      - action: organizations.dnsZones.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.dnsZones }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        domain: '{{ item.domain }}'
        name: '{{ item.name }}'
        peeringConfig: '{{ item.peeringConfig }}'
        state: '{{ item.state }}'
        updateTime: '{{ item.updateTime }}'
  # Lists the endpoint attachments in an organization.
  # ── gcp.apigee.organizations.endpointAttachments.list ──
  - discovery_id: gcp.apigee.organizations.endpointAttachments.list
    # python: svc.organizations().endpointAttachments().list(**params).execute()
    calls:
      - action: organizations.endpointAttachments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.endpointAttachments }}'
      item:
        connectionState: '{{ item.connectionState }}'
        host: '{{ item.host }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        serviceAttachment: '{{ item.serviceAttachment }}'
        state: '{{ item.state }}'
  # Lists all attachments of an environment group.
  # ── gcp.apigee.organizations.envgroups.attachments.list ──
  - discovery_id: gcp.apigee.organizations.envgroups.attachments.list
    # python: svc.organizations().envgroups().attachments().list(**params).execute()
    calls:
      - action: organizations.envgroups.attachments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.attachments }}'
      item:
        createdAt: '{{ item.createdAt }}'
        environment: '{{ item.environment }}'
        environmentGroupId: '{{ item.environmentGroupId }}'
        name: '{{ item.name }}'
  # Lists all environment groups.
  # ── gcp.apigee.organizations.envgroups.list ──
  - discovery_id: gcp.apigee.organizations.envgroups.list
    # python: svc.organizations().envgroups().list(**params).execute()
    calls:
      - action: organizations.envgroups.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.envgroups }}'
      item:
        createdAt: '{{ item.createdAt }}'
        hostnames: '{{ item.hostnames }}'
        lastModifiedAt: '{{ item.lastModifiedAt }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
  # Lists the details and status of all analytics export jobs belonging to the parent organization and e
  # ── gcp.apigee.organizations.environments.analytics.exports.list ──
  - discovery_id: gcp.apigee.organizations.environments.analytics.exports.list
    # python: svc.organizations().environments().analytics().exports().list(**params).execute()
    calls:
      - action: organizations.environments.analytics.exports.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.exports }}'
      item:
        created: '{{ item.created }}'
        datastoreName: '{{ item.datastoreName }}'
        description: '{{ item.description }}'
        error: '{{ item.error }}'
        executionTime: '{{ item.executionTime }}'
        name: '{{ item.name }}'
        self: '{{ item.self }}'
        state: '{{ item.state }}'
        updated: '{{ item.updated }}'
  # Lists all deployments of an API proxy in an environment.
  # ── gcp.apigee.organizations.environments.apis.deployments.list ──
  - discovery_id: gcp.apigee.organizations.environments.apis.deployments.list
    # python: svc.organizations().environments().apis().deployments().list(**params).execute()
    calls:
      - action: organizations.environments.apis.deployments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.deployments }}'
      item:
        apiProxy: '{{ item.apiProxy }}'
        deployStartTime: '{{ item.deployStartTime }}'
        environment: '{{ item.environment }}'
        errors: '{{ item.errors }}'
        instances: '{{ item.instances }}'
        pods: '{{ item.pods }}'
        proxyDeploymentType: '{{ item.proxyDeploymentType }}'
        revision: '{{ item.revision }}'
        routeConflicts: '{{ item.routeConflicts }}'
        serviceAccount: '{{ item.serviceAccount }}'
        state: '{{ item.state }}'
  # Lists debug sessions that are currently active in the given API Proxy revision.
  # ── gcp.apigee.organizations.environments.apis.revisions.debugsessions.list ──
  - discovery_id: gcp.apigee.organizations.environments.apis.revisions.debugsessions.list
    # python: svc.organizations().environments().apis().revisions().debugsessions().list(**params).execute()
    calls:
      - action: organizations.environments.apis.revisions.debugsessions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.debugsessions }}'
      item:
        id: '{{ item.id }}'
        timestampMs: '{{ item.timestampMs }}'
  # Lists the ArchiveDeployments in the specified Environment.
  # ── gcp.apigee.organizations.environments.archiveDeployments.list ──
  - discovery_id: gcp.apigee.organizations.environments.archiveDeployments.list
    # python: svc.organizations().environments().archiveDeployments().list(**params).execute()
    calls:
      - action: organizations.environments.archiveDeployments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.archiveDeployments }}'
      item:
        createdAt: '{{ item.createdAt }}'
        gcsUri: '{{ item.gcsUri }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        operation: '{{ item.operation }}'
        updatedAt: '{{ item.updatedAt }}'
  # Lists all deployments of API proxies or shared flows in an environment.
  # ── gcp.apigee.organizations.environments.deployments.list ──
  - discovery_id: gcp.apigee.organizations.environments.deployments.list
    # python: svc.organizations().environments().deployments().list(**params).execute()
    calls:
      - action: organizations.environments.deployments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.deployments }}'
      item:
        apiProxy: '{{ item.apiProxy }}'
        deployStartTime: '{{ item.deployStartTime }}'
        environment: '{{ item.environment }}'
        errors: '{{ item.errors }}'
        instances: '{{ item.instances }}'
        pods: '{{ item.pods }}'
        proxyDeploymentType: '{{ item.proxyDeploymentType }}'
        revision: '{{ item.revision }}'
        routeConflicts: '{{ item.routeConflicts }}'
        serviceAccount: '{{ item.serviceAccount }}'
        state: '{{ item.state }}'
  # Lists key value entries for key values maps scoped to an organization, environment, or API proxy. **
  # ── gcp.apigee.organizations.environments.keyvaluemaps.entries.list ──
  - discovery_id: gcp.apigee.organizations.environments.keyvaluemaps.entries.list
    # python: svc.organizations().environments().keyvaluemaps().entries().list(**params).execute()
    calls:
      - action: organizations.environments.keyvaluemaps.entries.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.entries }}'
      item:
        name: '{{ item.name }}'
        value: '{{ item.value }}'
  # Return a list of Asynchronous Queries
  # ── gcp.apigee.organizations.environments.queries.list ──
  - discovery_id: gcp.apigee.organizations.environments.queries.list
    # python: svc.organizations().environments().queries().list(**params).execute()
    calls:
      - action: organizations.environments.queries.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.queries }}'
      item:
        created: '{{ item.created }}'
        envgroupHostname: '{{ item.envgroupHostname }}'
        error: '{{ item.error }}'
        executionTime: '{{ item.executionTime }}'
        name: '{{ item.name }}'
        queryParams: '{{ item.queryParams }}'
        reportDefinitionId: '{{ item.reportDefinitionId }}'
        result: '{{ item.result }}'
        resultFileSize: '{{ item.resultFileSize }}'
        resultRows: '{{ item.resultRows }}'
        self: '{{ item.self }}'
        state: '{{ item.state }}'
        updated: '{{ item.updated }}'
  # Lists all resource files, optionally filtering by type. For more information about resource files, s
  # ── gcp.apigee.organizations.environments.resourcefiles.list ──
  - discovery_id: gcp.apigee.organizations.environments.resourcefiles.list
    # python: svc.organizations().environments().resourcefiles().list(**params).execute()
    calls:
      - action: organizations.environments.resourcefiles.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.resourcefiles }}'
      item:
        name: '{{ item.name }}'
        type: '{{ item.type }}'
  # Returns a list of SecurityActions. This returns both enabled and disabled actions.
  # ── gcp.apigee.organizations.environments.securityActions.list ──
  - discovery_id: gcp.apigee.organizations.environments.securityActions.list
    # python: svc.organizations().environments().securityActions().list(**params).execute()
    calls:
      - action: organizations.environments.securityActions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.securityActions }}'
      item:
        allow: '{{ item.allow }}'
        apiProxies: '{{ item.apiProxies }}'
        conditionConfig: '{{ item.conditionConfig }}'
        createTime: '{{ item.createTime }}'
        deny: '{{ item.deny }}'
        description: '{{ item.description }}'
        expireTime: '{{ item.expireTime }}'
        flag: '{{ item.flag }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
        ttl: '{{ item.ttl }}'
        updateTime: '{{ item.updateTime }}'
  # ListSecurityIncidents lists all the security incident associated with the environment.
  # ── gcp.apigee.organizations.environments.securityIncidents.list ──
  - discovery_id: gcp.apigee.organizations.environments.securityIncidents.list
    # python: svc.organizations().environments().securityIncidents().list(**params).execute()
    calls:
      - action: organizations.environments.securityIncidents.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.securityIncidents }}'
      item:
        detectionTypes: '{{ item.detectionTypes }}'
        displayName: '{{ item.displayName }}'
        firstDetectedTime: '{{ item.firstDetectedTime }}'
        lastDetectedTime: '{{ item.lastDetectedTime }}'
        lastObservabilityChangeTime: '{{ item.lastObservabilityChangeTime }}'
        name: '{{ item.name }}'
        observability: '{{ item.observability }}'
        riskLevel: '{{ item.riskLevel }}'
        trafficCount: '{{ item.trafficCount }}'
  # Return a list of Security Reports
  # ── gcp.apigee.organizations.environments.securityReports.list ──
  - discovery_id: gcp.apigee.organizations.environments.securityReports.list
    # python: svc.organizations().environments().securityReports().list(**params).execute()
    calls:
      - action: organizations.environments.securityReports.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.securityReports }}'
      item:
        created: '{{ item.created }}'
        displayName: '{{ item.displayName }}'
        envgroupHostname: '{{ item.envgroupHostname }}'
        error: '{{ item.error }}'
        executionTime: '{{ item.executionTime }}'
        queryParams: '{{ item.queryParams }}'
        reportDefinitionId: '{{ item.reportDefinitionId }}'
        result: '{{ item.result }}'
        resultFileSize: '{{ item.resultFileSize }}'
        resultRows: '{{ item.resultRows }}'
        self: '{{ item.self }}'
        state: '{{ item.state }}'
        updated: '{{ item.updated }}'
  # Lists all deployments of a shared flow in an environment.
  # ── gcp.apigee.organizations.environments.sharedflows.deployments.list ──
  - discovery_id: gcp.apigee.organizations.environments.sharedflows.deployments.list
    # python: svc.organizations().environments().sharedflows().deployments().list(**params).execute()
    calls:
      - action: organizations.environments.sharedflows.deployments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.deployments }}'
      item:
        apiProxy: '{{ item.apiProxy }}'
        deployStartTime: '{{ item.deployStartTime }}'
        environment: '{{ item.environment }}'
        errors: '{{ item.errors }}'
        instances: '{{ item.instances }}'
        pods: '{{ item.pods }}'
        proxyDeploymentType: '{{ item.proxyDeploymentType }}'
        revision: '{{ item.revision }}'
        routeConflicts: '{{ item.routeConflicts }}'
        serviceAccount: '{{ item.serviceAccount }}'
        state: '{{ item.state }}'
  # Lists all of the distributed trace configuration overrides in an environment.
  # ── gcp.apigee.organizations.environments.traceConfig.overrides.list ──
  - discovery_id: gcp.apigee.organizations.environments.traceConfig.overrides.list
    # python: svc.organizations().environments().traceConfig().overrides().list(**params).execute()
    calls:
      - action: organizations.environments.traceConfig.overrides.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.overrides }}'
      item:
        apiProxy: '{{ item.apiProxy }}'
        name: '{{ item.name }}'
        samplingConfig: '{{ item.samplingConfig }}'
  # Gets the profile for an Apigee organization. See [Understanding organizations](https://cloud.google.
  # ── gcp.apigee.organizations.get ──
  - discovery_id: gcp.apigee.organizations.get
    # python: svc.organizations().get(**params).execute()
    calls:
      - action: organizations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        addonsConfig: '{{ response.addonsConfig }}'
        analyticsRegion: '{{ response.analyticsRegion }}'
        apiConsumerDataEncryptionKeyName: '{{ response.apiConsumerDataEncryptionKeyName }}'
        apiConsumerDataLocation: '{{ response.apiConsumerDataLocation }}'
        apigeeProjectId: '{{ response.apigeeProjectId }}'
        attributes: '{{ response.attributes }}'
        authorizedNetwork: '{{ response.authorizedNetwork }}'
        billingType: '{{ response.billingType }}'
        caCertificate: '{{ response.caCertificate }}'
        caCertificates: '{{ response.caCertificates }}'
        controlPlaneEncryptionKeyName: '{{ response.controlPlaneEncryptionKeyName }}'
        createdAt: '{{ response.createdAt }}'
        customerName: '{{ response.customerName }}'
        description: '{{ response.description }}'
        disableVpcPeering: '{{ response.disableVpcPeering }}'
        displayName: '{{ response.displayName }}'
        environments: '{{ response.environments }}'
        expiresAt: '{{ response.expiresAt }}'
        lastModifiedAt: '{{ response.lastModifiedAt }}'
        name: '{{ response.name }}'
        networkEgressRestricted: '{{ response.networkEgressRestricted }}'
        portalDisabled: '{{ response.portalDisabled }}'
        projectId: '{{ response.projectId }}'
        properties: '{{ response.properties }}'
        runtimeDatabaseEncryptionKeyName: '{{ response.runtimeDatabaseEncryptionKeyName }}'
        runtimeType: '{{ response.runtimeType }}'
        state: '{{ response.state }}'
        subscriptionPlan: '{{ response.subscriptionPlan }}'
        subscriptionType: '{{ response.subscriptionType }}'
        type: '{{ response.type }}'
  # Lists the service accounts allowed to access Apigee control plane directly for limited functionality
  # ── gcp.apigee.organizations.getControlPlaneAccess ──
  - discovery_id: gcp.apigee.organizations.getControlPlaneAccess
    # python: svc.organizations().getControlPlaneAccess(**params).execute()
    calls:
      - action: organizations.getControlPlaneAccess
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        analyticsPublisherIdentities: '{{ response.analyticsPublisherIdentities }}'
        name: '{{ response.name }}'
        synchronizerIdentities: '{{ response.synchronizerIdentities }}'
  # Gets the deployed ingress configuration for an organization.
  # ── gcp.apigee.organizations.getDeployedIngressConfig ──
  - discovery_id: gcp.apigee.organizations.getDeployedIngressConfig
    # python: svc.organizations().getDeployedIngressConfig(**params).execute()
    calls:
      - action: organizations.getDeployedIngressConfig
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        environmentGroups: '{{ response.environmentGroups }}'
        name: '{{ response.name }}'
        revisionCreateTime: '{{ response.revisionCreateTime }}'
        revisionId: '{{ response.revisionId }}'
        uid: '{{ response.uid }}'
  # Gets the project ID and region for an Apigee organization.
  # ── gcp.apigee.organizations.getProjectMapping ──
  - discovery_id: gcp.apigee.organizations.getProjectMapping
    # python: svc.organizations().getProjectMapping(**params).execute()
    calls:
      - action: organizations.getProjectMapping
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        location: '{{ response.location }}'
        organization: '{{ response.organization }}'
        projectId: '{{ response.projectId }}'
        projectIds: '{{ response.projectIds }}'
  # Get runtime config for an organization.
  # ── gcp.apigee.organizations.getRuntimeConfig ──
  - discovery_id: gcp.apigee.organizations.getRuntimeConfig
    # python: svc.organizations().getRuntimeConfig(**params).execute()
    calls:
      - action: organizations.getRuntimeConfig
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        analyticsBucket: '{{ response.analyticsBucket }}'
        name: '{{ response.name }}'
        tenantProjectId: '{{ response.tenantProjectId }}'
        traceBucket: '{{ response.traceBucket }}'
  # GetSecuritySettings gets the security settings for API Security.
  # ── gcp.apigee.organizations.getSecuritySettings ──
  - discovery_id: gcp.apigee.organizations.getSecuritySettings
    # python: svc.organizations().getSecuritySettings(**params).execute()
    calls:
      - action: organizations.getSecuritySettings
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        mlRetrainingFeedbackEnabled: '{{ response.mlRetrainingFeedbackEnabled }}'
        name: '{{ response.name }}'
  # Return a list of Asynchronous Queries at host level.
  # ── gcp.apigee.organizations.hostQueries.list ──
  - discovery_id: gcp.apigee.organizations.hostQueries.list
    # python: svc.organizations().hostQueries().list(**params).execute()
    calls:
      - action: organizations.hostQueries.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.hostQueries }}'
      item:
        created: '{{ item.created }}'
        envgroupHostname: '{{ item.envgroupHostname }}'
        error: '{{ item.error }}'
        executionTime: '{{ item.executionTime }}'
        name: '{{ item.name }}'
        queryParams: '{{ item.queryParams }}'
        reportDefinitionId: '{{ item.reportDefinitionId }}'
        result: '{{ item.result }}'
        resultFileSize: '{{ item.resultFileSize }}'
        resultRows: '{{ item.resultRows }}'
        self: '{{ item.self }}'
        state: '{{ item.state }}'
        updated: '{{ item.updated }}'
  # Return a list of Security Reports at host level.
  # ── gcp.apigee.organizations.hostSecurityReports.list ──
  - discovery_id: gcp.apigee.organizations.hostSecurityReports.list
    # python: svc.organizations().hostSecurityReports().list(**params).execute()
    calls:
      - action: organizations.hostSecurityReports.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.hostSecurityReports }}'
      item:
        created: '{{ item.created }}'
        displayName: '{{ item.displayName }}'
        envgroupHostname: '{{ item.envgroupHostname }}'
        error: '{{ item.error }}'
        executionTime: '{{ item.executionTime }}'
        queryParams: '{{ item.queryParams }}'
        reportDefinitionId: '{{ item.reportDefinitionId }}'
        result: '{{ item.result }}'
        resultFileSize: '{{ item.resultFileSize }}'
        resultRows: '{{ item.resultRows }}'
        self: '{{ item.self }}'
        state: '{{ item.state }}'
        updated: '{{ item.updated }}'
  # Lists all attachments to an instance. **Note:** Not supported for Apigee hybrid.
  # ── gcp.apigee.organizations.instances.attachments.list ──
  - discovery_id: gcp.apigee.organizations.instances.attachments.list
    # python: svc.organizations().instances().attachments().list(**params).execute()
    calls:
      - action: organizations.instances.attachments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.attachments }}'
      item:
        createdAt: '{{ item.createdAt }}'
        environment: '{{ item.environment }}'
        name: '{{ item.name }}'
  # Lists all Apigee runtime instances for the organization. **Note:** Not supported for Apigee hybrid.
  # ── gcp.apigee.organizations.instances.list ──
  - discovery_id: gcp.apigee.organizations.instances.list
    # python: svc.organizations().instances().list(**params).execute()
    calls:
      - action: organizations.instances.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.instances }}'
      item:
        accessLoggingConfig: '{{ item.accessLoggingConfig }}'
        consumerAcceptList: '{{ item.consumerAcceptList }}'
        createdAt: '{{ item.createdAt }}'
        description: '{{ item.description }}'
        diskEncryptionKeyName: '{{ item.diskEncryptionKeyName }}'
        displayName: '{{ item.displayName }}'
        host: '{{ item.host }}'
        ipRange: '{{ item.ipRange }}'
        isVersionLocked: '{{ item.isVersionLocked }}'
        lastModifiedAt: '{{ item.lastModifiedAt }}'
        location: '{{ item.location }}'
        maintenanceUpdatePolicy: '{{ item.maintenanceUpdatePolicy }}'
        name: '{{ item.name }}'
        peeringCidrRange: '{{ item.peeringCidrRange }}'
        port: '{{ item.port }}'
        runtimeVersion: '{{ item.runtimeVersion }}'
        scheduledMaintenance: '{{ item.scheduledMaintenance }}'
        serviceAttachment: '{{ item.serviceAttachment }}'
        state: '{{ item.state }}'
  # Lists the NAT addresses for an Apigee instance. **Note:** Not supported for Apigee hybrid.
  # ── gcp.apigee.organizations.instances.natAddresses.list ──
  - discovery_id: gcp.apigee.organizations.instances.natAddresses.list
    # python: svc.organizations().instances().natAddresses().list(**params).execute()
    calls:
      - action: organizations.instances.natAddresses.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.natAddresses }}'
      item:
        ipAddress: '{{ item.ipAddress }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
  # Lists key value entries for key values maps scoped to an organization, environment, or API proxy. **
  # ── gcp.apigee.organizations.keyvaluemaps.entries.list ──
  - discovery_id: gcp.apigee.organizations.keyvaluemaps.entries.list
    # python: svc.organizations().keyvaluemaps().entries().list(**params).execute()
    calls:
      - action: organizations.keyvaluemaps.entries.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.entries }}'
      item:
        name: '{{ item.name }}'
        value: '{{ item.value }}'
  # Lists the Apigee organizations and associated Google Cloud projects that you have permission to acce
  # ── gcp.apigee.organizations.list ──
  - discovery_id: gcp.apigee.organizations.list
    # python: svc.organizations().list(**params).execute()
    calls:
      - action: organizations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.organizations }}'
      item:
        location: '{{ item.location }}'
        organization: '{{ item.organization }}'
        projectId: '{{ item.projectId }}'
        projectIds: '{{ item.projectIds }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.apigee.organizations.operations.list ──
  - discovery_id: gcp.apigee.organizations.operations.list
    # python: svc.organizations().operations().list(**params).execute()
    calls:
      - action: organizations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Return a list of Custom Reports
  # ── gcp.apigee.organizations.reports.list ──
  - discovery_id: gcp.apigee.organizations.reports.list
    # python: svc.organizations().reports().list(**params).execute()
    calls:
      - action: organizations.reports.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.reports }}'
      item:
        chartType: '{{ item.chartType }}'
        comments: '{{ item.comments }}'
        createdAt: '{{ item.createdAt }}'
        dimensions: '{{ item.dimensions }}'
        displayName: '{{ item.displayName }}'
        environment: '{{ item.environment }}'
        filter: '{{ item.filter }}'
        fromTime: '{{ item.fromTime }}'
        lastModifiedAt: '{{ item.lastModifiedAt }}'
        lastViewedAt: '{{ item.lastViewedAt }}'
        limit: '{{ item.limit }}'
        metrics: '{{ item.metrics }}'
        name: '{{ item.name }}'
        offset: '{{ item.offset }}'
        organization: '{{ item.organization }}'
        properties: '{{ item.properties }}'
        sortByCols: '{{ item.sortByCols }}'
        sortOrder: '{{ item.sortOrder }}'
        tags: '{{ item.tags }}'
        timeUnit: '{{ item.timeUnit }}'
        toTime: '{{ item.toTime }}'
        topk: '{{ item.topk }}'
  # Lists all feedback reports which have already been submitted.
  # ── gcp.apigee.organizations.securityFeedback.list ──
  - discovery_id: gcp.apigee.organizations.securityFeedback.list
    # python: svc.organizations().securityFeedback().list(**params).execute()
    calls:
      - action: organizations.securityFeedback.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.securityFeedback }}'
      item:
        comment: '{{ item.comment }}'
        createTime: '{{ item.createTime }}'
        displayName: '{{ item.displayName }}'
        feedbackContexts: '{{ item.feedbackContexts }}'
        feedbackType: '{{ item.feedbackType }}'
        name: '{{ item.name }}'
        reason: '{{ item.reason }}'
        updateTime: '{{ item.updateTime }}'
  # List security monitoring conditions.
  # ── gcp.apigee.organizations.securityMonitoringConditions.list ──
  - discovery_id: gcp.apigee.organizations.securityMonitoringConditions.list
    # python: svc.organizations().securityMonitoringConditions().list(**params).execute()
    calls:
      - action: organizations.securityMonitoringConditions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.securityMonitoringConditions }}'
      item:
        createTime: '{{ item.createTime }}'
        include: '{{ item.include }}'
        includeAllResources: '{{ item.includeAllResources }}'
        name: '{{ item.name }}'
        profile: '{{ item.profile }}'
        scope: '{{ item.scope }}'
        totalDeployedResources: '{{ item.totalDeployedResources }}'
        totalMonitoredResources: '{{ item.totalMonitoredResources }}'
        updateTime: '{{ item.updateTime }}'
  # ListSecurityProfiles lists all the security profiles associated with the org including attached and 
  # ── gcp.apigee.organizations.securityProfiles.list ──
  - discovery_id: gcp.apigee.organizations.securityProfiles.list
    # python: svc.organizations().securityProfiles().list(**params).execute()
    calls:
      - action: organizations.securityProfiles.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.securityProfiles }}'
      item:
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        environments: '{{ item.environments }}'
        maxScore: '{{ item.maxScore }}'
        minScore: '{{ item.minScore }}'
        name: '{{ item.name }}'
        profileConfig: '{{ item.profileConfig }}'
        revisionCreateTime: '{{ item.revisionCreateTime }}'
        revisionId: '{{ item.revisionId }}'
        revisionPublishTime: '{{ item.revisionPublishTime }}'
        revisionUpdateTime: '{{ item.revisionUpdateTime }}'
        scoringConfigs: '{{ item.scoringConfigs }}'
  # List security profiles v2.
  # ── gcp.apigee.organizations.securityProfilesV2.list ──
  - discovery_id: gcp.apigee.organizations.securityProfilesV2.list
    # python: svc.organizations().securityProfilesV2().list(**params).execute()
    calls:
      - action: organizations.securityProfilesV2.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.securityProfilesV2 }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        googleDefined: '{{ item.googleDefined }}'
        name: '{{ item.name }}'
        profileAssessmentConfigs: '{{ item.profileAssessmentConfigs }}'
        riskAssessmentType: '{{ item.riskAssessmentType }}'
        updateTime: '{{ item.updateTime }}'
  # Lists all deployments of a shared flow.
  # ── gcp.apigee.organizations.sharedflows.deployments.list ──
  - discovery_id: gcp.apigee.organizations.sharedflows.deployments.list
    # python: svc.organizations().sharedflows().deployments().list(**params).execute()
    calls:
      - action: organizations.sharedflows.deployments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.deployments }}'
      item:
        apiProxy: '{{ item.apiProxy }}'
        deployStartTime: '{{ item.deployStartTime }}'
        environment: '{{ item.environment }}'
        errors: '{{ item.errors }}'
        instances: '{{ item.instances }}'
        pods: '{{ item.pods }}'
        proxyDeploymentType: '{{ item.proxyDeploymentType }}'
        revision: '{{ item.revision }}'
        routeConflicts: '{{ item.routeConflicts }}'
        serviceAccount: '{{ item.serviceAccount }}'
        state: '{{ item.state }}'
  # Lists all shared flows in the organization. If the resource has the `space` attribute set, the respo
  # ── gcp.apigee.organizations.sharedflows.list ──
  - discovery_id: gcp.apigee.organizations.sharedflows.list
    # python: svc.organizations().sharedflows().list(**params).execute()
    calls:
      - action: organizations.sharedflows.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.sharedflows }}'
      item:
        latestRevisionId: '{{ item.latestRevisionId }}'
        metaData: '{{ item.metaData }}'
        name: '{{ item.name }}'
        revision: '{{ item.revision }}'
        space: '{{ item.space }}'
  # Lists all deployments of a shared flow revision.
  # ── gcp.apigee.organizations.sharedflows.revisions.deployments.list ──
  - discovery_id: gcp.apigee.organizations.sharedflows.revisions.deployments.list
    # python: svc.organizations().sharedflows().revisions().deployments().list(**params).execute()
    calls:
      - action: organizations.sharedflows.revisions.deployments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.deployments }}'
      item:
        apiProxy: '{{ item.apiProxy }}'
        deployStartTime: '{{ item.deployStartTime }}'
        environment: '{{ item.environment }}'
        errors: '{{ item.errors }}'
        instances: '{{ item.instances }}'
        pods: '{{ item.pods }}'
        proxyDeploymentType: '{{ item.proxyDeploymentType }}'
        revision: '{{ item.revision }}'
        routeConflicts: '{{ item.routeConflicts }}'
        serviceAccount: '{{ item.serviceAccount }}'
        state: '{{ item.state }}'
  # Returns the API categories associated with a portal.
  # ── gcp.apigee.organizations.sites.apicategories.list ──
  - discovery_id: gcp.apigee.organizations.sites.apicategories.list
    # python: svc.organizations().sites().apicategories().list(**params).execute()
    calls:
      - action: organizations.sites.apicategories.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.apicategories }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        siteId: '{{ item.siteId }}'
        updateTime: '{{ item.updateTime }}'
  # Returns the catalog items associated with a portal.
  # ── gcp.apigee.organizations.sites.apidocs.list ──
  - discovery_id: gcp.apigee.organizations.sites.apidocs.list
    # python: svc.organizations().sites().apidocs().list(**params).execute()
    calls:
      - action: organizations.sites.apidocs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.apidocs }}'
      item:
        anonAllowed: '{{ item.anonAllowed }}'
        apiProductName: '{{ item.apiProductName }}'
        categoryIds: '{{ item.categoryIds }}'
        description: '{{ item.description }}'
        edgeAPIProductName: '{{ item.edgeAPIProductName }}'
        graphqlEndpointUrl: '{{ item.graphqlEndpointUrl }}'
        graphqlSchema: '{{ item.graphqlSchema }}'
        graphqlSchemaDisplayName: '{{ item.graphqlSchemaDisplayName }}'
        id: '{{ item.id }}'
        imageUrl: '{{ item.imageUrl }}'
        modified: '{{ item.modified }}'
        published: '{{ item.published }}'
        requireCallbackUrl: '{{ item.requireCallbackUrl }}'
        siteId: '{{ item.siteId }}'
        specId: '{{ item.specId }}'
        title: '{{ item.title }}'
        visibility: '{{ item.visibility }}'
  # Lists spaces under an organization.
  # ── gcp.apigee.organizations.spaces.list ──
  - discovery_id: gcp.apigee.organizations.spaces.list
    # python: svc.organizations().spaces().list(**params).execute()
    calls:
      - action: organizations.spaces.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.spaces }}'
      item:
        createTime: '{{ item.createTime }}'
        displayName: '{{ item.displayName }}'
        name: '{{ item.name }}'
        updateTime: '{{ item.updateTime }}'

  # ════ DEPENDENT (enrich) operations ════
  # Get a Datastore
  # ── gcp.apigee.organizations.analytics.datastores.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.analytics.datastores.get
    for_each: gcp.apigee.organizations.analytics.datastores.list
    # python: svc.organizations().analytics().datastores().get(**params).execute()
    calls:
      - action: organizations.analytics.datastores.get
        params:
          name: '{{ item.datastoreConfig }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        datastoreConfig: '{{ response.datastoreConfig }}'
        displayName: '{{ response.displayName }}'
        lastUpdateTime: '{{ response.lastUpdateTime }}'
        org: '{{ response.org }}'
        self: '{{ response.self }}'
        targetType: '{{ response.targetType }}'
    # required_params: ['name']
  # Gets APIM service extension details.
  # ── gcp.apigee.organizations.apimServiceExtensions.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.apimServiceExtensions.get
    for_each: gcp.apigee.organizations.apimServiceExtensions.list
    # python: svc.organizations().apimServiceExtensions().get(**params).execute()
    calls:
      - action: organizations.apimServiceExtensions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        extensionProcessor: '{{ response.extensionProcessor }}'
        extensions: '{{ response.extensions }}'
        lbForwardingRule: '{{ response.lbForwardingRule }}'
        name: '{{ response.name }}'
        network: '{{ response.network }}'
        networkConfigs: '{{ response.networkConfigs }}'
        state: '{{ response.state }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the value of an API product attribute.
  # ── gcp.apigee.organizations.apiproducts.attributes.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.apiproducts.attributes.get
    for_each: gcp.apigee.organizations.developers.attributes.list
    # python: svc.organizations().apiproducts().attributes().get(**params).execute()
    calls:
      - action: organizations.apiproducts.attributes.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        value: '{{ response.value }}'
    # required_params: ['name']
  # Gets configuration details for an API product. The API product name required in the request URL is t
  # ── gcp.apigee.organizations.apiproducts.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.apiproducts.get
    for_each: gcp.apigee.organizations.apiproducts.list
    # python: svc.organizations().apiproducts().get(**params).execute()
    calls:
      - action: organizations.apiproducts.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiResources: '{{ response.apiResources }}'
        approvalType: '{{ response.approvalType }}'
        attributes: '{{ response.attributes }}'
        createdAt: '{{ response.createdAt }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        environments: '{{ response.environments }}'
        graphqlOperationGroup: '{{ response.graphqlOperationGroup }}'
        grpcOperationGroup: '{{ response.grpcOperationGroup }}'
        lastModifiedAt: '{{ response.lastModifiedAt }}'
        llmOperationGroup: '{{ response.llmOperationGroup }}'
        llmQuota: '{{ response.llmQuota }}'
        llmQuotaInterval: '{{ response.llmQuotaInterval }}'
        llmQuotaTimeUnit: '{{ response.llmQuotaTimeUnit }}'
        name: '{{ response.name }}'
        operationGroup: '{{ response.operationGroup }}'
        proxies: '{{ response.proxies }}'
        quota: '{{ response.quota }}'
        quotaCounterScope: '{{ response.quotaCounterScope }}'
        quotaInterval: '{{ response.quotaInterval }}'
        quotaTimeUnit: '{{ response.quotaTimeUnit }}'
        scopes: '{{ response.scopes }}'
        space: '{{ response.space }}'
    # required_params: ['name']
  # Gets the details of a rate plan.
  # ── gcp.apigee.organizations.apiproducts.rateplans.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.apiproducts.rateplans.get
    for_each: gcp.apigee.organizations.apiproducts.rateplans.list
    # python: svc.organizations().apiproducts().rateplans().get(**params).execute()
    calls:
      - action: organizations.apiproducts.rateplans.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiproduct: '{{ response.apiproduct }}'
        billingPeriod: '{{ response.billingPeriod }}'
        consumptionPricingRates: '{{ response.consumptionPricingRates }}'
        consumptionPricingType: '{{ response.consumptionPricingType }}'
        createdAt: '{{ response.createdAt }}'
        currencyCode: '{{ response.currencyCode }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        endTime: '{{ response.endTime }}'
        fixedFeeFrequency: '{{ response.fixedFeeFrequency }}'
        fixedRecurringFee: '{{ response.fixedRecurringFee }}'
        lastModifiedAt: '{{ response.lastModifiedAt }}'
        name: '{{ response.name }}'
        paymentFundingModel: '{{ response.paymentFundingModel }}'
        revenueShareRates: '{{ response.revenueShareRates }}'
        revenueShareType: '{{ response.revenueShareType }}'
        setupFee: '{{ response.setupFee }}'
        startTime: '{{ response.startTime }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Gets an API proxy including a list of existing revisions.
  # ── gcp.apigee.organizations.apis.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.apis.get
    for_each: gcp.apigee.organizations.apis.list
    # python: svc.organizations().apis().get(**params).execute()
    calls:
      - action: organizations.apis.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiProxyType: '{{ response.apiProxyType }}'
        labels: '{{ response.labels }}'
        latestRevisionId: '{{ response.latestRevisionId }}'
        metaData: '{{ response.metaData }}'
        name: '{{ response.name }}'
        readOnly: '{{ response.readOnly }}'
        revision: '{{ response.revision }}'
        space: '{{ response.space }}'
    # required_params: ['name']
  # Get the key value entry value for a key value map scoped to an organization, environment, or API pro
  # ── gcp.apigee.organizations.apis.keyvaluemaps.entries.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.apis.keyvaluemaps.entries.get
    for_each: gcp.apigee.organizations.apis.keyvaluemaps.entries.list
    # python: svc.organizations().apis().keyvaluemaps().entries().get(**params).execute()
    calls:
      - action: organizations.apis.keyvaluemaps.entries.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        value: '{{ response.value }}'
    # required_params: ['name']
  # Get the key value map scoped to an organization, environment, or API proxy.
  # ── gcp.apigee.organizations.apis.keyvaluemaps.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.apis.keyvaluemaps.get
    for_each: gcp.apigee.organizations.apis.list
    # python: svc.organizations().apis().keyvaluemaps().get(**params).execute()
    calls:
      - action: organizations.apis.keyvaluemaps.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        encrypted: '{{ response.encrypted }}'
        maskedValues: '{{ response.maskedValues }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Gets an API proxy revision. To download the API proxy configuration bundle for the specified revisio
  # ── gcp.apigee.organizations.apis.revisions.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.apis.revisions.get
    for_each: gcp.apigee.organizations.apis.list
    # python: svc.organizations().apis().revisions().get(**params).execute()
    calls:
      - action: organizations.apis.revisions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['name']
  # Returns the details for an AppGroup app.
  # ── gcp.apigee.organizations.appgroups.apps.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.appgroups.apps.get
    for_each: gcp.apigee.organizations.developers.apps.list
    # python: svc.organizations().appgroups().apps().get(**params).execute()
    calls:
      - action: organizations.appgroups.apps.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiProducts: '{{ response.apiProducts }}'
        appGroup: '{{ response.appGroup }}'
        appId: '{{ response.appId }}'
        attributes: '{{ response.attributes }}'
        callbackUrl: '{{ response.callbackUrl }}'
        createdAt: '{{ response.createdAt }}'
        credentials: '{{ response.credentials }}'
        keyExpiresIn: '{{ response.keyExpiresIn }}'
        lastModifiedAt: '{{ response.lastModifiedAt }}'
        name: '{{ response.name }}'
        scopes: '{{ response.scopes }}'
        status: '{{ response.status }}'
    # required_params: ['name']
  # Gets details for a consumer key for a AppGroup app, including the key and secret value, associated A
  # ── gcp.apigee.organizations.appgroups.apps.keys.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.appgroups.apps.keys.get
    for_each: gcp.apigee.organizations.developers.apps.list
    # python: svc.organizations().appgroups().apps().keys().get(**params).execute()
    calls:
      - action: organizations.appgroups.apps.keys.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiProducts: '{{ response.apiProducts }}'
        attributes: '{{ response.attributes }}'
        consumerKey: '{{ response.consumerKey }}'
        consumerSecret: '{{ response.consumerSecret }}'
        expiresAt: '{{ response.expiresAt }}'
        expiresInSeconds: '{{ response.expiresInSeconds }}'
        issuedAt: '{{ response.issuedAt }}'
        scopes: '{{ response.scopes }}'
        status: '{{ response.status }}'
    # required_params: ['name']
  # Returns the AppGroup details for the provided AppGroup name in the request URI.
  # ── gcp.apigee.organizations.appgroups.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.appgroups.get
    for_each: gcp.apigee.organizations.appgroups.list
    # python: svc.organizations().appgroups().get(**params).execute()
    calls:
      - action: organizations.appgroups.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        appGroupId: '{{ response.appGroupId }}'
        attributes: '{{ response.attributes }}'
        channelId: '{{ response.channelId }}'
        channelUri: '{{ response.channelUri }}'
        createdAt: '{{ response.createdAt }}'
        displayName: '{{ response.displayName }}'
        email: '{{ response.email }}'
        lastModifiedAt: '{{ response.lastModifiedAt }}'
        name: '{{ response.name }}'
        organization: '{{ response.organization }}'
        status: '{{ response.status }}'
    # required_params: ['name']
  # Gets the account balance for the AppGroup.
  # ── gcp.apigee.organizations.appgroups.getBalance [dependent] ──
  - discovery_id: gcp.apigee.organizations.appgroups.getBalance
    for_each: gcp.apigee.organizations.appgroups.list
    # python: svc.organizations().appgroups().getBalance(**params).execute()
    calls:
      - action: organizations.appgroups.getBalance
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        wallets: '{{ response.wallets }}'
    # required_params: ['name']
  # Gets the monetization configuration for the AppGroup.
  # ── gcp.apigee.organizations.appgroups.getMonetizationConfig [dependent] ──
  - discovery_id: gcp.apigee.organizations.appgroups.getMonetizationConfig
    for_each: gcp.apigee.organizations.appgroups.list
    # python: svc.organizations().appgroups().getMonetizationConfig(**params).execute()
    calls:
      - action: organizations.appgroups.getMonetizationConfig
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        billingType: '{{ response.billingType }}'
    # required_params: ['name']
  # Get an api product subscription for an appgroup.
  # ── gcp.apigee.organizations.appgroups.subscriptions.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.appgroups.subscriptions.get
    for_each: gcp.apigee.organizations.developers.subscriptions.list
    # python: svc.organizations().appgroups().subscriptions().get(**params).execute()
    calls:
      - action: organizations.appgroups.subscriptions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiproduct: '{{ response.apiproduct }}'
        createdAt: '{{ response.createdAt }}'
        endTime: '{{ response.endTime }}'
        lastModifiedAt: '{{ response.lastModifiedAt }}'
        name: '{{ response.name }}'
        startTime: '{{ response.startTime }}'
    # required_params: ['name']
  # Gets the app profile for the specified app ID.
  # ── gcp.apigee.organizations.apps.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.apps.get
    for_each: gcp.apigee.organizations.developers.apps.list
    # python: svc.organizations().apps().get(**params).execute()
    calls:
      - action: organizations.apps.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiProducts: '{{ response.apiProducts }}'
        appGroup: '{{ response.appGroup }}'
        appId: '{{ response.appId }}'
        attributes: '{{ response.attributes }}'
        callbackUrl: '{{ response.callbackUrl }}'
        companyName: '{{ response.companyName }}'
        createdAt: '{{ response.createdAt }}'
        credentials: '{{ response.credentials }}'
        developerEmail: '{{ response.developerEmail }}'
        developerId: '{{ response.developerId }}'
        keyExpiresIn: '{{ response.keyExpiresIn }}'
        lastModifiedAt: '{{ response.lastModifiedAt }}'
        name: '{{ response.name }}'
        scopes: '{{ response.scopes }}'
        status: '{{ response.status }}'
    # required_params: ['name']
  # Gets a data collector.
  # ── gcp.apigee.organizations.datacollectors.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.datacollectors.get
    for_each: gcp.apigee.organizations.datacollectors.list
    # python: svc.organizations().datacollectors().get(**params).execute()
    calls:
      - action: organizations.datacollectors.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createdAt: '{{ response.createdAt }}'
        description: '{{ response.description }}'
        lastModifiedAt: '{{ response.lastModifiedAt }}'
        name: '{{ response.name }}'
        type: '{{ response.type }}'
    # required_params: ['name']
  # Returns a developer app attribute.
  # ── gcp.apigee.organizations.developers.apps.attributes.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.developers.apps.attributes.get
    for_each: gcp.apigee.organizations.developers.attributes.list
    # python: svc.organizations().developers().apps().attributes().get(**params).execute()
    calls:
      - action: organizations.developers.apps.attributes.get
        params:
          name: '{{ item.email }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        value: '{{ response.value }}'
    # required_params: ['name']
  # Returns the details for a developer app.
  # ── gcp.apigee.organizations.developers.apps.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.developers.apps.get
    for_each: gcp.apigee.organizations.developers.apps.list
    # python: svc.organizations().developers().apps().get(**params).execute()
    calls:
      - action: organizations.developers.apps.get
        params:
          name: '{{ item.email }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiProducts: '{{ response.apiProducts }}'
        appFamily: '{{ response.appFamily }}'
        appId: '{{ response.appId }}'
        attributes: '{{ response.attributes }}'
        callbackUrl: '{{ response.callbackUrl }}'
        createdAt: '{{ response.createdAt }}'
        credentials: '{{ response.credentials }}'
        developerId: '{{ response.developerId }}'
        keyExpiresIn: '{{ response.keyExpiresIn }}'
        lastModifiedAt: '{{ response.lastModifiedAt }}'
        name: '{{ response.name }}'
        scopes: '{{ response.scopes }}'
        status: '{{ response.status }}'
    # required_params: ['name']
  # Gets details for a consumer key for a developer app, including the key and secret value, associated 
  # ── gcp.apigee.organizations.developers.apps.keys.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.developers.apps.keys.get
    for_each: gcp.apigee.organizations.developers.apps.list
    # python: svc.organizations().developers().apps().keys().get(**params).execute()
    calls:
      - action: organizations.developers.apps.keys.get
        params:
          name: '{{ item.email }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiProducts: '{{ response.apiProducts }}'
        attributes: '{{ response.attributes }}'
        consumerKey: '{{ response.consumerKey }}'
        consumerSecret: '{{ response.consumerSecret }}'
        expiresAt: '{{ response.expiresAt }}'
        expiresInSeconds: '{{ response.expiresInSeconds }}'
        issuedAt: '{{ response.issuedAt }}'
        scopes: '{{ response.scopes }}'
        status: '{{ response.status }}'
    # required_params: ['name']
  # Returns the value of the specified developer attribute.
  # ── gcp.apigee.organizations.developers.attributes.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.developers.attributes.get
    for_each: gcp.apigee.organizations.developers.attributes.list
    # python: svc.organizations().developers().attributes().get(**params).execute()
    calls:
      - action: organizations.developers.attributes.get
        params:
          name: '{{ item.email }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        value: '{{ response.value }}'
    # required_params: ['name']
  # Returns the developer details, including the developer's name, email address, apps, and other inform
  # ── gcp.apigee.organizations.developers.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.developers.get
    for_each: gcp.apigee.organizations.developers.list
    # python: svc.organizations().developers().get(**params).execute()
    calls:
      - action: organizations.developers.get
        params:
          name: '{{ item.email }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        accessType: '{{ response.accessType }}'
        appFamily: '{{ response.appFamily }}'
        apps: '{{ response.apps }}'
        attributes: '{{ response.attributes }}'
        companies: '{{ response.companies }}'
        createdAt: '{{ response.createdAt }}'
        developerId: '{{ response.developerId }}'
        email: '{{ response.email }}'
        firstName: '{{ response.firstName }}'
        lastModifiedAt: '{{ response.lastModifiedAt }}'
        lastName: '{{ response.lastName }}'
        organizationName: '{{ response.organizationName }}'
        status: '{{ response.status }}'
        userName: '{{ response.userName }}'
    # required_params: ['name']
  # Gets the account balance for the developer.
  # ── gcp.apigee.organizations.developers.getBalance [dependent] ──
  - discovery_id: gcp.apigee.organizations.developers.getBalance
    for_each: gcp.apigee.organizations.developers.list
    # python: svc.organizations().developers().getBalance(**params).execute()
    calls:
      - action: organizations.developers.getBalance
        params:
          name: '{{ item.email }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        wallets: '{{ response.wallets }}'
    # required_params: ['name']
  # Gets the monetization configuration for the developer.
  # ── gcp.apigee.organizations.developers.getMonetizationConfig [dependent] ──
  - discovery_id: gcp.apigee.organizations.developers.getMonetizationConfig
    for_each: gcp.apigee.organizations.developers.list
    # python: svc.organizations().developers().getMonetizationConfig(**params).execute()
    calls:
      - action: organizations.developers.getMonetizationConfig
        params:
          name: '{{ item.email }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        billingType: '{{ response.billingType }}'
    # required_params: ['name']
  # Gets details for an API product subscription.
  # ── gcp.apigee.organizations.developers.subscriptions.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.developers.subscriptions.get
    for_each: gcp.apigee.organizations.developers.subscriptions.list
    # python: svc.organizations().developers().subscriptions().get(**params).execute()
    calls:
      - action: organizations.developers.subscriptions.get
        params:
          name: '{{ item.email }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiproduct: '{{ response.apiproduct }}'
        createdAt: '{{ response.createdAt }}'
        endTime: '{{ response.endTime }}'
        lastModifiedAt: '{{ response.lastModifiedAt }}'
        name: '{{ response.name }}'
        startTime: '{{ response.startTime }}'
    # required_params: ['name']
  # Fetches the representation of an existing DNS zone.
  # ── gcp.apigee.organizations.dnsZones.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.dnsZones.get
    for_each: gcp.apigee.organizations.dnsZones.list
    # python: svc.organizations().dnsZones().get(**params).execute()
    calls:
      - action: organizations.dnsZones.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        domain: '{{ response.domain }}'
        name: '{{ response.name }}'
        peeringConfig: '{{ response.peeringConfig }}'
        state: '{{ response.state }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the endpoint attachment.
  # ── gcp.apigee.organizations.endpointAttachments.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.endpointAttachments.get
    for_each: gcp.apigee.organizations.endpointAttachments.list
    # python: svc.organizations().endpointAttachments().get(**params).execute()
    calls:
      - action: organizations.endpointAttachments.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        connectionState: '{{ response.connectionState }}'
        host: '{{ response.host }}'
        location: '{{ response.location }}'
        name: '{{ response.name }}'
        serviceAttachment: '{{ response.serviceAttachment }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Gets an environment group attachment.
  # ── gcp.apigee.organizations.envgroups.attachments.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.envgroups.attachments.get
    for_each: gcp.apigee.organizations.envgroups.attachments.list
    # python: svc.organizations().envgroups().attachments().get(**params).execute()
    calls:
      - action: organizations.envgroups.attachments.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createdAt: '{{ response.createdAt }}'
        environment: '{{ response.environment }}'
        environmentGroupId: '{{ response.environmentGroupId }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Gets an environment group.
  # ── gcp.apigee.organizations.envgroups.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.envgroups.get
    for_each: gcp.apigee.organizations.envgroups.list
    # python: svc.organizations().envgroups().get(**params).execute()
    calls:
      - action: organizations.envgroups.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createdAt: '{{ response.createdAt }}'
        hostnames: '{{ response.hostnames }}'
        lastModifiedAt: '{{ response.lastModifiedAt }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Gets the deployed ingress configuration for an environment group.
  # ── gcp.apigee.organizations.envgroups.getDeployedIngressConfig [dependent] ──
  - discovery_id: gcp.apigee.organizations.envgroups.getDeployedIngressConfig
    for_each: gcp.apigee.organizations.envgroups.list
    # python: svc.organizations().envgroups().getDeployedIngressConfig(**params).execute()
    calls:
      - action: organizations.envgroups.getDeployedIngressConfig
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        endpointChainingRules: '{{ response.endpointChainingRules }}'
        hostnames: '{{ response.hostnames }}'
        location: '{{ response.location }}'
        name: '{{ response.name }}'
        revisionId: '{{ response.revisionId }}'
        routingRules: '{{ response.routingRules }}'
        uid: '{{ response.uid }}'
    # required_params: ['name']
  # Gets a list of metrics and dimensions that can be used to create analytics queries and reports. Each
  # ── gcp.apigee.organizations.environments.analytics.admin.getSchemav2 [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.analytics.admin.getSchemav2
    for_each: gcp.apigee.organizations.envgroups.attachments.list
    # python: svc.organizations().environments().analytics().admin().getSchemav2(**params).execute()
    calls:
      - action: organizations.environments.analytics.admin.getSchemav2
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        dimensions: '{{ response.dimensions }}'
        meta: '{{ response.meta }}'
        metrics: '{{ response.metrics }}'
    # required_params: ['name']
  # Gets the details and status of an analytics export job. If the export job is still in progress, its 
  # ── gcp.apigee.organizations.environments.analytics.exports.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.analytics.exports.get
    for_each: gcp.apigee.organizations.environments.analytics.exports.list
    # python: svc.organizations().environments().analytics().exports().get(**params).execute()
    calls:
      - action: organizations.environments.analytics.exports.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created: '{{ response.created }}'
        datastoreName: '{{ response.datastoreName }}'
        description: '{{ response.description }}'
        error: '{{ response.error }}'
        executionTime: '{{ response.executionTime }}'
        name: '{{ response.name }}'
        self: '{{ response.self }}'
        state: '{{ response.state }}'
        updated: '{{ response.updated }}'
    # required_params: ['name']
  # Gets the debug data from a transaction.
  # ── gcp.apigee.organizations.environments.apis.revisions.debugsessions.data.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.apis.revisions.debugsessions.data.get
    for_each: gcp.apigee.organizations.apis.debugsessions.list
    # python: svc.organizations().environments().apis().revisions().debugsessions().data().get(**params).execute()
    calls:
      - action: organizations.environments.apis.revisions.debugsessions.data.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        completed: '{{ response.completed }}'
        point: '{{ response.point }}'
    # required_params: ['name']
  # Retrieves a debug session.
  # ── gcp.apigee.organizations.environments.apis.revisions.debugsessions.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.apis.revisions.debugsessions.get
    for_each: gcp.apigee.organizations.apis.debugsessions.list
    # python: svc.organizations().environments().apis().revisions().debugsessions().get(**params).execute()
    calls:
      - action: organizations.environments.apis.revisions.debugsessions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        count: '{{ response.count }}'
        createTime: '{{ response.createTime }}'
        filter: '{{ response.filter }}'
        name: '{{ response.name }}'
        timeout: '{{ response.timeout }}'
        tracesize: '{{ response.tracesize }}'
        validity: '{{ response.validity }}'
    # required_params: ['name']
  # Gets the deployment of an API proxy revision and actual state reported by runtime pods.
  # ── gcp.apigee.organizations.environments.apis.revisions.getDeployments [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.apis.revisions.getDeployments
    for_each: gcp.apigee.organizations.apis.list
    # python: svc.organizations().environments().apis().revisions().getDeployments(**params).execute()
    calls:
      - action: organizations.environments.apis.revisions.getDeployments
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiProxy: '{{ response.apiProxy }}'
        deployStartTime: '{{ response.deployStartTime }}'
        environment: '{{ response.environment }}'
        errors: '{{ response.errors }}'
        instances: '{{ response.instances }}'
        pods: '{{ response.pods }}'
        proxyDeploymentType: '{{ response.proxyDeploymentType }}'
        revision: '{{ response.revision }}'
        routeConflicts: '{{ response.routeConflicts }}'
        serviceAccount: '{{ response.serviceAccount }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Gets the specified ArchiveDeployment.
  # ── gcp.apigee.organizations.environments.archiveDeployments.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.archiveDeployments.get
    for_each: gcp.apigee.organizations.environments.archiveDeployments.list
    # python: svc.organizations().environments().archiveDeployments().get(**params).execute()
    calls:
      - action: organizations.environments.archiveDeployments.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createdAt: '{{ response.createdAt }}'
        gcsUri: '{{ response.gcsUri }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        operation: '{{ response.operation }}'
        updatedAt: '{{ response.updatedAt }}'
    # required_params: ['name']
  # Gets a particular deployment of Api proxy or a shared flow in an environment
  # ── gcp.apigee.organizations.environments.deployments.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.deployments.get
    for_each: gcp.apigee.organizations.apis.revisions.deployments.list
    # python: svc.organizations().environments().deployments().get(**params).execute()
    calls:
      - action: organizations.environments.deployments.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiProxy: '{{ response.apiProxy }}'
        deployStartTime: '{{ response.deployStartTime }}'
        environment: '{{ response.environment }}'
        errors: '{{ response.errors }}'
        instances: '{{ response.instances }}'
        pods: '{{ response.pods }}'
        proxyDeploymentType: '{{ response.proxyDeploymentType }}'
        revision: '{{ response.revision }}'
        routeConflicts: '{{ response.routeConflicts }}'
        serviceAccount: '{{ response.serviceAccount }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Gets the IAM policy on a deployment. For more information, see [Manage users, roles, and permissions
  # ── gcp.apigee.organizations.environments.deployments.getIamPolicy [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.deployments.getIamPolicy
    for_each: gcp.apigee.organizations.apis.revisions.deployments.list
    # python: svc.organizations().environments().deployments().getIamPolicy(**params).execute()
    calls:
      - action: organizations.environments.deployments.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Returns the name of the shared flow attached to the specified flow hook. If there's no shared flow a
  # ── gcp.apigee.organizations.environments.flowhooks.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.flowhooks.get
    for_each: gcp.apigee.organizations.envgroups.attachments.list
    # python: svc.organizations().environments().flowhooks().get(**params).execute()
    calls:
      - action: organizations.environments.flowhooks.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        continueOnError: '{{ response.continueOnError }}'
        description: '{{ response.description }}'
        flowHookPoint: '{{ response.flowHookPoint }}'
        sharedFlow: '{{ response.sharedFlow }}'
    # required_params: ['name']
  # Gets environment details.
  # ── gcp.apigee.organizations.environments.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.get
    for_each: gcp.apigee.organizations.envgroups.attachments.list
    # python: svc.organizations().environments().get(**params).execute()
    calls:
      - action: organizations.environments.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiProxyType: '{{ response.apiProxyType }}'
        clientIpResolutionConfig: '{{ response.clientIpResolutionConfig }}'
        createdAt: '{{ response.createdAt }}'
        deploymentType: '{{ response.deploymentType }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        forwardProxyUri: '{{ response.forwardProxyUri }}'
        hasAttachedFlowHooks: '{{ response.hasAttachedFlowHooks }}'
        lastModifiedAt: '{{ response.lastModifiedAt }}'
        name: '{{ response.name }}'
        nodeConfig: '{{ response.nodeConfig }}'
        properties: '{{ response.properties }}'
        state: '{{ response.state }}'
        type: '{{ response.type }}'
    # required_params: ['name']
  # Gets the add-ons config of an environment.
  # ── gcp.apigee.organizations.environments.getAddonsConfig [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.getAddonsConfig
    for_each: gcp.apigee.organizations.envgroups.attachments.list
    # python: svc.organizations().environments().getAddonsConfig(**params).execute()
    calls:
      - action: organizations.environments.getAddonsConfig
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        advancedApiOpsConfig: '{{ response.advancedApiOpsConfig }}'
        analyticsConfig: '{{ response.analyticsConfig }}'
        apiSecurityConfig: '{{ response.apiSecurityConfig }}'
        connectorsPlatformConfig: '{{ response.connectorsPlatformConfig }}'
        integrationConfig: '{{ response.integrationConfig }}'
        monetizationConfig: '{{ response.monetizationConfig }}'
    # required_params: ['name']
  # Gets the API Security runtime configuration for an environment. This named ApiSecurityRuntimeConfig 
  # ── gcp.apigee.organizations.environments.getApiSecurityRuntimeConfig [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.getApiSecurityRuntimeConfig
    for_each: gcp.apigee.organizations.envgroups.attachments.list
    # python: svc.organizations().environments().getApiSecurityRuntimeConfig(**params).execute()
    calls:
      - action: organizations.environments.getApiSecurityRuntimeConfig
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        location: '{{ response.location }}'
        name: '{{ response.name }}'
        revisionId: '{{ response.revisionId }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the debug mask singleton resource for an environment.
  # ── gcp.apigee.organizations.environments.getDebugmask [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.getDebugmask
    for_each: gcp.apigee.organizations.envgroups.attachments.list
    # python: svc.organizations().environments().getDebugmask(**params).execute()
    calls:
      - action: organizations.environments.getDebugmask
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        faultJSONPaths: '{{ response.faultJSONPaths }}'
        faultXPaths: '{{ response.faultXPaths }}'
        name: '{{ response.name }}'
        namespaces: '{{ response.namespaces }}'
        requestJSONPaths: '{{ response.requestJSONPaths }}'
        requestXPaths: '{{ response.requestXPaths }}'
        responseJSONPaths: '{{ response.responseJSONPaths }}'
        responseXPaths: '{{ response.responseXPaths }}'
        variables: '{{ response.variables }}'
    # required_params: ['name']
  # Gets the deployed configuration for an environment.
  # ── gcp.apigee.organizations.environments.getDeployedConfig [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.getDeployedConfig
    for_each: gcp.apigee.organizations.envgroups.attachments.list
    # python: svc.organizations().environments().getDeployedConfig(**params).execute()
    calls:
      - action: organizations.environments.getDeployedConfig
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        addonsConfig: '{{ response.addonsConfig }}'
        arcConfigLocation: '{{ response.arcConfigLocation }}'
        clientIpResolutionConfig: '{{ response.clientIpResolutionConfig }}'
        createTime: '{{ response.createTime }}'
        dataCollectors: '{{ response.dataCollectors }}'
        debugMask: '{{ response.debugMask }}'
        deploymentGroups: '{{ response.deploymentGroups }}'
        deployments: '{{ response.deployments }}'
        envScopedRevisionId: '{{ response.envScopedRevisionId }}'
        featureFlags: '{{ response.featureFlags }}'
        flowhooks: '{{ response.flowhooks }}'
        forwardProxyUri: '{{ response.forwardProxyUri }}'
        gatewayConfigLocation: '{{ response.gatewayConfigLocation }}'
        keystores: '{{ response.keystores }}'
        name: '{{ response.name }}'
        provider: '{{ response.provider }}'
        pubsubTopic: '{{ response.pubsubTopic }}'
        resourceReferences: '{{ response.resourceReferences }}'
        resources: '{{ response.resources }}'
        revisionId: '{{ response.revisionId }}'
        sequenceNumber: '{{ response.sequenceNumber }}'
        targets: '{{ response.targets }}'
        traceConfig: '{{ response.traceConfig }}'
        uid: '{{ response.uid }}'
    # required_params: ['name']
  # Gets the IAM policy on an environment. For more information, see [Manage users, roles, and permissio
  # ── gcp.apigee.organizations.environments.getIamPolicy [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.getIamPolicy
    for_each: gcp.apigee.organizations.envgroups.attachments.list
    # python: svc.organizations().environments().getIamPolicy(**params).execute()
    calls:
      - action: organizations.environments.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # GetSecurityActionConfig returns the current SecurityActions configuration.
  # ── gcp.apigee.organizations.environments.getSecurityActionsConfig [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.getSecurityActionsConfig
    for_each: gcp.apigee.organizations.envgroups.attachments.list
    # python: svc.organizations().environments().getSecurityActionsConfig(**params).execute()
    calls:
      - action: organizations.environments.getSecurityActionsConfig
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        enabled: '{{ response.enabled }}'
        name: '{{ response.name }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Get distributed trace configuration in an environment.
  # ── gcp.apigee.organizations.environments.getTraceConfig [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.getTraceConfig
    for_each: gcp.apigee.organizations.envgroups.attachments.list
    # python: svc.organizations().environments().getTraceConfig(**params).execute()
    calls:
      - action: organizations.environments.getTraceConfig
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        endpoint: '{{ response.endpoint }}'
        exporter: '{{ response.exporter }}'
        samplingConfig: '{{ response.samplingConfig }}'
    # required_params: ['name']
  # Generates a PKCS #10 Certificate Signing Request for the private key in an alias.
  # ── gcp.apigee.organizations.environments.keystores.aliases.csr [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.keystores.aliases.csr
    for_each: gcp.apigee.organizations.envgroups.attachments.list
    # python: svc.organizations().environments().keystores().aliases().csr(**params).execute()
    calls:
      - action: organizations.environments.keystores.aliases.csr
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['name']
  # Gets an alias.
  # ── gcp.apigee.organizations.environments.keystores.aliases.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.keystores.aliases.get
    for_each: gcp.apigee.organizations.envgroups.attachments.list
    # python: svc.organizations().environments().keystores().aliases().get(**params).execute()
    calls:
      - action: organizations.environments.keystores.aliases.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        alias: '{{ response.alias }}'
        certsInfo: '{{ response.certsInfo }}'
        type: '{{ response.type }}'
    # required_params: ['name']
  # Gets the certificate from an alias in PEM-encoded form.
  # ── gcp.apigee.organizations.environments.keystores.aliases.getCertificate [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.keystores.aliases.getCertificate
    for_each: gcp.apigee.organizations.envgroups.attachments.list
    # python: svc.organizations().environments().keystores().aliases().getCertificate(**params).execute()
    calls:
      - action: organizations.environments.keystores.aliases.getCertificate
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['name']
  # Gets a keystore or truststore.
  # ── gcp.apigee.organizations.environments.keystores.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.keystores.get
    for_each: gcp.apigee.organizations.envgroups.attachments.list
    # python: svc.organizations().environments().keystores().get(**params).execute()
    calls:
      - action: organizations.environments.keystores.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        aliases: '{{ response.aliases }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Get the key value entry value for a key value map scoped to an organization, environment, or API pro
  # ── gcp.apigee.organizations.environments.keyvaluemaps.entries.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.keyvaluemaps.entries.get
    for_each: gcp.apigee.organizations.apis.keyvaluemaps.entries.list
    # python: svc.organizations().environments().keyvaluemaps().entries().get(**params).execute()
    calls:
      - action: organizations.environments.keyvaluemaps.entries.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        value: '{{ response.value }}'
    # required_params: ['name']
  # Get the key value map scoped to an organization, environment, or API proxy.
  # ── gcp.apigee.organizations.environments.keyvaluemaps.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.keyvaluemaps.get
    for_each: gcp.apigee.organizations.envgroups.attachments.list
    # python: svc.organizations().environments().keyvaluemaps().get(**params).execute()
    calls:
      - action: organizations.environments.keyvaluemaps.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        encrypted: '{{ response.encrypted }}'
        maskedValues: '{{ response.maskedValues }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Similar to GetStats except that the response is less verbose.
  # ── gcp.apigee.organizations.environments.optimizedStats.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.optimizedStats.get
    for_each: gcp.apigee.organizations.envgroups.attachments.list
    # python: svc.organizations().environments().optimizedStats().get(**params).execute()
    calls:
      - action: organizations.environments.optimizedStats.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        Response: '{{ response.Response }}'
    # required_params: ['name']
  # Get query status If the query is still in progress, the `state` is set to "running" After the query 
  # ── gcp.apigee.organizations.environments.queries.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.queries.get
    for_each: gcp.apigee.organizations.environments.queries.list
    # python: svc.organizations().environments().queries().get(**params).execute()
    calls:
      - action: organizations.environments.queries.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created: '{{ response.created }}'
        envgroupHostname: '{{ response.envgroupHostname }}'
        error: '{{ response.error }}'
        executionTime: '{{ response.executionTime }}'
        name: '{{ response.name }}'
        queryParams: '{{ response.queryParams }}'
        reportDefinitionId: '{{ response.reportDefinitionId }}'
        result: '{{ response.result }}'
        resultFileSize: '{{ response.resultFileSize }}'
        resultRows: '{{ response.resultRows }}'
        self: '{{ response.self }}'
        state: '{{ response.state }}'
        updated: '{{ response.updated }}'
    # required_params: ['name']
  # After the query is completed, use this API to retrieve the results. If the request succeeds, and the
  # ── gcp.apigee.organizations.environments.queries.getResult [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.queries.getResult
    for_each: gcp.apigee.organizations.environments.queries.list
    # python: svc.organizations().environments().queries().getResult(**params).execute()
    calls:
      - action: organizations.environments.queries.getResult
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['name']
  # After the query is completed, use this API to retrieve the results. If the request succeeds, and the
  # ── gcp.apigee.organizations.environments.queries.getResulturl [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.queries.getResulturl
    for_each: gcp.apigee.organizations.environments.queries.list
    # python: svc.organizations().environments().queries().getResulturl(**params).execute()
    calls:
      - action: organizations.environments.queries.getResulturl
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        urls: '{{ response.urls }}'
    # required_params: ['name']
  # Gets a Reference resource.
  # ── gcp.apigee.organizations.environments.references.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.references.get
    for_each: gcp.apigee.organizations.envgroups.attachments.list
    # python: svc.organizations().environments().references().get(**params).execute()
    calls:
      - action: organizations.environments.references.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        description: '{{ response.description }}'
        name: '{{ response.name }}'
        refers: '{{ response.refers }}'
        resourceType: '{{ response.resourceType }}'
    # required_params: ['name']
  # Gets the contents of a resource file. For more information about resource files, see [Resource files
  # ── gcp.apigee.organizations.environments.resourcefiles.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.resourcefiles.get
    for_each: gcp.apigee.organizations.datacollectors.list
    # python: svc.organizations().environments().resourcefiles().get(**params).execute()
    calls:
      - action: organizations.environments.resourcefiles.get
        params:
          name: '{{ item.name }}'
          type: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['name', 'type', 'parent']
  # Lists all resource files, optionally filtering by type. For more information about resource files, s
  # ── gcp.apigee.organizations.environments.resourcefiles.listEnvironmentResources [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.resourcefiles.listEnvironmentResources
    for_each: gcp.apigee.organizations.datacollectors.list
    # python: svc.organizations().environments().resourcefiles().listEnvironmentResources(**params).execute()
    calls:
      - action: organizations.environments.resourcefiles.listEnvironmentResources
        params:
          type: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.resourcefiles }}'
      item:
        name: '{{ item.name }}'
        type: '{{ item.type }}'
    # required_params: ['parent', 'type']
  # Get a SecurityAction by name.
  # ── gcp.apigee.organizations.environments.securityActions.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.securityActions.get
    for_each: gcp.apigee.organizations.environments.securityActions.list
    # python: svc.organizations().environments().securityActions().get(**params).execute()
    calls:
      - action: organizations.environments.securityActions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        allow: '{{ response.allow }}'
        apiProxies: '{{ response.apiProxies }}'
        conditionConfig: '{{ response.conditionConfig }}'
        createTime: '{{ response.createTime }}'
        deny: '{{ response.deny }}'
        description: '{{ response.description }}'
        expireTime: '{{ response.expireTime }}'
        flag: '{{ response.flag }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
        ttl: '{{ response.ttl }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # GetSecurityIncident gets the specified security incident. Returns NOT_FOUND if security incident is 
  # ── gcp.apigee.organizations.environments.securityIncidents.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.securityIncidents.get
    for_each: gcp.apigee.organizations.environments.securityIncidents.list
    # python: svc.organizations().environments().securityIncidents().get(**params).execute()
    calls:
      - action: organizations.environments.securityIncidents.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        detectionTypes: '{{ response.detectionTypes }}'
        displayName: '{{ response.displayName }}'
        firstDetectedTime: '{{ response.firstDetectedTime }}'
        lastDetectedTime: '{{ response.lastDetectedTime }}'
        lastObservabilityChangeTime: '{{ response.lastObservabilityChangeTime }}'
        name: '{{ response.name }}'
        observability: '{{ response.observability }}'
        riskLevel: '{{ response.riskLevel }}'
        trafficCount: '{{ response.trafficCount }}'
    # required_params: ['name']
  # Get security report status If the query is still in progress, the `state` is set to "running" After 
  # ── gcp.apigee.organizations.environments.securityReports.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.securityReports.get
    for_each: gcp.apigee.organizations.environments.securityReports.list
    # python: svc.organizations().environments().securityReports().get(**params).execute()
    calls:
      - action: organizations.environments.securityReports.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created: '{{ response.created }}'
        displayName: '{{ response.displayName }}'
        envgroupHostname: '{{ response.envgroupHostname }}'
        error: '{{ response.error }}'
        executionTime: '{{ response.executionTime }}'
        queryParams: '{{ response.queryParams }}'
        reportDefinitionId: '{{ response.reportDefinitionId }}'
        result: '{{ response.result }}'
        resultFileSize: '{{ response.resultFileSize }}'
        resultRows: '{{ response.resultRows }}'
        self: '{{ response.self }}'
        state: '{{ response.state }}'
        updated: '{{ response.updated }}'
    # required_params: ['name']
  # After the query is completed, use this API to retrieve the results as file. If the request succeeds,
  # ── gcp.apigee.organizations.environments.securityReports.getResult [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.securityReports.getResult
    for_each: gcp.apigee.organizations.environments.securityReports.list
    # python: svc.organizations().environments().securityReports().getResult(**params).execute()
    calls:
      - action: organizations.environments.securityReports.getResult
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['name']
  # After the query is completed, use this API to view the query result when result size is small.
  # ── gcp.apigee.organizations.environments.securityReports.getResultView [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.securityReports.getResultView
    for_each: gcp.apigee.organizations.environments.securityReports.list
    # python: svc.organizations().environments().securityReports().getResultView(**params).execute()
    calls:
      - action: organizations.environments.securityReports.getResultView
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        code: '{{ response.code }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        rows: '{{ response.rows }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Gets the deployment of a shared flow revision and actual state reported by runtime pods.
  # ── gcp.apigee.organizations.environments.sharedflows.revisions.getDeployments [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.sharedflows.revisions.getDeployments
    for_each: gcp.apigee.organizations.apis.list
    # python: svc.organizations().environments().sharedflows().revisions().getDeployments(**params).execute()
    calls:
      - action: organizations.environments.sharedflows.revisions.getDeployments
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiProxy: '{{ response.apiProxy }}'
        deployStartTime: '{{ response.deployStartTime }}'
        environment: '{{ response.environment }}'
        errors: '{{ response.errors }}'
        instances: '{{ response.instances }}'
        pods: '{{ response.pods }}'
        proxyDeploymentType: '{{ response.proxyDeploymentType }}'
        revision: '{{ response.revision }}'
        routeConflicts: '{{ response.routeConflicts }}'
        serviceAccount: '{{ response.serviceAccount }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Retrieve metrics grouped by dimensions. The types of metrics you can retrieve include traffic, messa
  # ── gcp.apigee.organizations.environments.stats.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.stats.get
    for_each: gcp.apigee.organizations.envgroups.list
    # python: svc.organizations().environments().stats().get(**params).execute()
    calls:
      - action: organizations.environments.stats.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        environments: '{{ response.environments }}'
        hosts: '{{ response.hosts }}'
        metaData: '{{ response.metaData }}'
    # required_params: ['name']
  # Gets a TargetServer resource.
  # ── gcp.apigee.organizations.environments.targetservers.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.targetservers.get
    for_each: gcp.apigee.organizations.envgroups.attachments.list
    # python: svc.organizations().environments().targetservers().get(**params).execute()
    calls:
      - action: organizations.environments.targetservers.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        description: '{{ response.description }}'
        host: '{{ response.host }}'
        isEnabled: '{{ response.isEnabled }}'
        name: '{{ response.name }}'
        port: '{{ response.port }}'
        protocol: '{{ response.protocol }}'
        sSLInfo: '{{ response.sSLInfo }}'
    # required_params: ['name']
  # Gets a trace configuration override.
  # ── gcp.apigee.organizations.environments.traceConfig.overrides.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.environments.traceConfig.overrides.get
    for_each: gcp.apigee.organizations.environments.traceConfig.overrides.list
    # python: svc.organizations().environments().traceConfig().overrides().get(**params).execute()
    calls:
      - action: organizations.environments.traceConfig.overrides.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiProxy: '{{ response.apiProxy }}'
        name: '{{ response.name }}'
        samplingConfig: '{{ response.samplingConfig }}'
    # required_params: ['name']
  # Get status of a query submitted at host level. If the query is still in progress, the `state` is set
  # ── gcp.apigee.organizations.hostQueries.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.hostQueries.get
    for_each: gcp.apigee.organizations.hostQueries.list
    # python: svc.organizations().hostQueries().get(**params).execute()
    calls:
      - action: organizations.hostQueries.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created: '{{ response.created }}'
        envgroupHostname: '{{ response.envgroupHostname }}'
        error: '{{ response.error }}'
        executionTime: '{{ response.executionTime }}'
        name: '{{ response.name }}'
        queryParams: '{{ response.queryParams }}'
        reportDefinitionId: '{{ response.reportDefinitionId }}'
        result: '{{ response.result }}'
        resultFileSize: '{{ response.resultFileSize }}'
        resultRows: '{{ response.resultRows }}'
        self: '{{ response.self }}'
        state: '{{ response.state }}'
        updated: '{{ response.updated }}'
    # required_params: ['name']
  # After the query is completed, use this API to retrieve the results. If the request succeeds, and the
  # ── gcp.apigee.organizations.hostQueries.getResult [dependent] ──
  - discovery_id: gcp.apigee.organizations.hostQueries.getResult
    for_each: gcp.apigee.organizations.hostQueries.list
    # python: svc.organizations().hostQueries().getResult(**params).execute()
    calls:
      - action: organizations.hostQueries.getResult
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['name']
  # ── gcp.apigee.organizations.hostQueries.getResultView [dependent] ──
  - discovery_id: gcp.apigee.organizations.hostQueries.getResultView
    for_each: gcp.apigee.organizations.hostQueries.list
    # python: svc.organizations().hostQueries().getResultView(**params).execute()
    calls:
      - action: organizations.hostQueries.getResultView
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        code: '{{ response.code }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        rows: '{{ response.rows }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Get status of a query submitted at host level. If the query is still in progress, the `state` is set
  # ── gcp.apigee.organizations.hostSecurityReports.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.hostSecurityReports.get
    for_each: gcp.apigee.organizations.hostSecurityReports.list
    # python: svc.organizations().hostSecurityReports().get(**params).execute()
    calls:
      - action: organizations.hostSecurityReports.get
        params:
          name: '{{ item.executionTime }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created: '{{ response.created }}'
        displayName: '{{ response.displayName }}'
        envgroupHostname: '{{ response.envgroupHostname }}'
        error: '{{ response.error }}'
        executionTime: '{{ response.executionTime }}'
        queryParams: '{{ response.queryParams }}'
        reportDefinitionId: '{{ response.reportDefinitionId }}'
        result: '{{ response.result }}'
        resultFileSize: '{{ response.resultFileSize }}'
        resultRows: '{{ response.resultRows }}'
        self: '{{ response.self }}'
        state: '{{ response.state }}'
        updated: '{{ response.updated }}'
    # required_params: ['name']
  # After the query is completed, use this API to retrieve the results. If the request succeeds, and the
  # ── gcp.apigee.organizations.hostSecurityReports.getResult [dependent] ──
  - discovery_id: gcp.apigee.organizations.hostSecurityReports.getResult
    for_each: gcp.apigee.organizations.hostSecurityReports.list
    # python: svc.organizations().hostSecurityReports().getResult(**params).execute()
    calls:
      - action: organizations.hostSecurityReports.getResult
        params:
          name: '{{ item.executionTime }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['name']
  # After the query is completed, use this API to view the query result when result size is small.
  # ── gcp.apigee.organizations.hostSecurityReports.getResultView [dependent] ──
  - discovery_id: gcp.apigee.organizations.hostSecurityReports.getResultView
    for_each: gcp.apigee.organizations.hostSecurityReports.list
    # python: svc.organizations().hostSecurityReports().getResultView(**params).execute()
    calls:
      - action: organizations.hostSecurityReports.getResultView
        params:
          name: '{{ item.executionTime }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        code: '{{ response.code }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        rows: '{{ response.rows }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Retrieve metrics grouped by dimensions in host level. The types of metrics you can retrieve include 
  # ── gcp.apigee.organizations.hostStats.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.hostStats.get
    # python: svc.organizations().hostStats().get(**params).execute()
    calls:
      - action: organizations.hostStats.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        environments: '{{ response.environments }}'
        hosts: '{{ response.hosts }}'
        metaData: '{{ response.metaData }}'
    # required_params: ['name']
  # Gets an attachment. **Note:** Not supported for Apigee hybrid.
  # ── gcp.apigee.organizations.instances.attachments.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.instances.attachments.get
    for_each: gcp.apigee.organizations.envgroups.attachments.list
    # python: svc.organizations().instances().attachments().get(**params).execute()
    calls:
      - action: organizations.instances.attachments.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createdAt: '{{ response.createdAt }}'
        environment: '{{ response.environment }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Gets a CanaryEvaluation for an organization.
  # ── gcp.apigee.organizations.instances.canaryevaluations.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.instances.canaryevaluations.get
    for_each: gcp.apigee.organizations.instances.list
    # python: svc.organizations().instances().canaryevaluations().get(**params).execute()
    calls:
      - action: organizations.instances.canaryevaluations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        control: '{{ response.control }}'
        createTime: '{{ response.createTime }}'
        endTime: '{{ response.endTime }}'
        metricLabels: '{{ response.metricLabels }}'
        name: '{{ response.name }}'
        startTime: '{{ response.startTime }}'
        state: '{{ response.state }}'
        treatment: '{{ response.treatment }}'
        verdict: '{{ response.verdict }}'
    # required_params: ['name']
  # Gets the details for an Apigee runtime instance. **Note:** Not supported for Apigee hybrid.
  # ── gcp.apigee.organizations.instances.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.instances.get
    for_each: gcp.apigee.organizations.instances.list
    # python: svc.organizations().instances().get(**params).execute()
    calls:
      - action: organizations.instances.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        accessLoggingConfig: '{{ response.accessLoggingConfig }}'
        consumerAcceptList: '{{ response.consumerAcceptList }}'
        createdAt: '{{ response.createdAt }}'
        description: '{{ response.description }}'
        diskEncryptionKeyName: '{{ response.diskEncryptionKeyName }}'
        displayName: '{{ response.displayName }}'
        host: '{{ response.host }}'
        ipRange: '{{ response.ipRange }}'
        isVersionLocked: '{{ response.isVersionLocked }}'
        lastModifiedAt: '{{ response.lastModifiedAt }}'
        location: '{{ response.location }}'
        maintenanceUpdatePolicy: '{{ response.maintenanceUpdatePolicy }}'
        name: '{{ response.name }}'
        peeringCidrRange: '{{ response.peeringCidrRange }}'
        port: '{{ response.port }}'
        runtimeVersion: '{{ response.runtimeVersion }}'
        scheduledMaintenance: '{{ response.scheduledMaintenance }}'
        serviceAttachment: '{{ response.serviceAttachment }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Gets the details of a NAT address. **Note:** Not supported for Apigee hybrid.
  # ── gcp.apigee.organizations.instances.natAddresses.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.instances.natAddresses.get
    for_each: gcp.apigee.organizations.instances.natAddresses.list
    # python: svc.organizations().instances().natAddresses().get(**params).execute()
    calls:
      - action: organizations.instances.natAddresses.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        ipAddress: '{{ response.ipAddress }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Get the key value entry value for a key value map scoped to an organization, environment, or API pro
  # ── gcp.apigee.organizations.keyvaluemaps.entries.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.keyvaluemaps.entries.get
    for_each: gcp.apigee.organizations.apis.keyvaluemaps.entries.list
    # python: svc.organizations().keyvaluemaps().entries().get(**params).execute()
    calls:
      - action: organizations.keyvaluemaps.entries.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        value: '{{ response.value }}'
    # required_params: ['name']
  # Get the key value map scoped to an organization, environment, or API proxy.
  # ── gcp.apigee.organizations.keyvaluemaps.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.keyvaluemaps.get
    # python: svc.organizations().keyvaluemaps().get(**params).execute()
    calls:
      - action: organizations.keyvaluemaps.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        encrypted: '{{ response.encrypted }}'
        maskedValues: '{{ response.maskedValues }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.apigee.organizations.operations.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.operations.get
    for_each: gcp.apigee.organizations.operations.list
    # python: svc.organizations().operations().get(**params).execute()
    calls:
      - action: organizations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Similar to GetHostStats except that the response is less verbose.
  # ── gcp.apigee.organizations.optimizedHostStats.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.optimizedHostStats.get
    # python: svc.organizations().optimizedHostStats().get(**params).execute()
    calls:
      - action: organizations.optimizedHostStats.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        Response: '{{ response.Response }}'
    # required_params: ['name']
  # Retrieve a custom report definition.
  # ── gcp.apigee.organizations.reports.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.reports.get
    for_each: gcp.apigee.organizations.reports.list
    # python: svc.organizations().reports().get(**params).execute()
    calls:
      - action: organizations.reports.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        chartType: '{{ response.chartType }}'
        comments: '{{ response.comments }}'
        createdAt: '{{ response.createdAt }}'
        dimensions: '{{ response.dimensions }}'
        displayName: '{{ response.displayName }}'
        environment: '{{ response.environment }}'
        filter: '{{ response.filter }}'
        fromTime: '{{ response.fromTime }}'
        lastModifiedAt: '{{ response.lastModifiedAt }}'
        lastViewedAt: '{{ response.lastViewedAt }}'
        limit: '{{ response.limit }}'
        metrics: '{{ response.metrics }}'
        name: '{{ response.name }}'
        offset: '{{ response.offset }}'
        organization: '{{ response.organization }}'
        properties: '{{ response.properties }}'
        sortByCols: '{{ response.sortByCols }}'
        sortOrder: '{{ response.sortOrder }}'
        tags: '{{ response.tags }}'
        timeUnit: '{{ response.timeUnit }}'
        toTime: '{{ response.toTime }}'
        topk: '{{ response.topk }}'
    # required_params: ['name']
  # Gets a specific customer feedback report.
  # ── gcp.apigee.organizations.securityFeedback.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.securityFeedback.get
    for_each: gcp.apigee.organizations.securityFeedback.list
    # python: svc.organizations().securityFeedback().get(**params).execute()
    calls:
      - action: organizations.securityFeedback.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        comment: '{{ response.comment }}'
        createTime: '{{ response.createTime }}'
        displayName: '{{ response.displayName }}'
        feedbackContexts: '{{ response.feedbackContexts }}'
        feedbackType: '{{ response.feedbackType }}'
        name: '{{ response.name }}'
        reason: '{{ response.reason }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Get a security monitoring condition.
  # ── gcp.apigee.organizations.securityMonitoringConditions.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.securityMonitoringConditions.get
    for_each: gcp.apigee.organizations.securityMonitoringConditions.list
    # python: svc.organizations().securityMonitoringConditions().get(**params).execute()
    calls:
      - action: organizations.securityMonitoringConditions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        include: '{{ response.include }}'
        includeAllResources: '{{ response.includeAllResources }}'
        name: '{{ response.name }}'
        profile: '{{ response.profile }}'
        scope: '{{ response.scope }}'
        totalDeployedResources: '{{ response.totalDeployedResources }}'
        totalMonitoredResources: '{{ response.totalMonitoredResources }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # GetSecurityProfile gets the specified security profile. Returns NOT_FOUND if security profile is not
  # ── gcp.apigee.organizations.securityProfiles.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.securityProfiles.get
    for_each: gcp.apigee.organizations.securityProfiles.list
    # python: svc.organizations().securityProfiles().get(**params).execute()
    calls:
      - action: organizations.securityProfiles.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        environments: '{{ response.environments }}'
        maxScore: '{{ response.maxScore }}'
        minScore: '{{ response.minScore }}'
        name: '{{ response.name }}'
        profileConfig: '{{ response.profileConfig }}'
        revisionCreateTime: '{{ response.revisionCreateTime }}'
        revisionId: '{{ response.revisionId }}'
        revisionPublishTime: '{{ response.revisionPublishTime }}'
        revisionUpdateTime: '{{ response.revisionUpdateTime }}'
        scoringConfigs: '{{ response.scoringConfigs }}'
    # required_params: ['name']
  # ListSecurityProfileRevisions lists all the revisions of the security profile.
  # ── gcp.apigee.organizations.securityProfiles.listRevisions [dependent] ──
  - discovery_id: gcp.apigee.organizations.securityProfiles.listRevisions
    for_each: gcp.apigee.organizations.securityProfiles.list
    # python: svc.organizations().securityProfiles().listRevisions(**params).execute()
    calls:
      - action: organizations.securityProfiles.listRevisions
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.securityProfiles }}'
      item:
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        environments: '{{ item.environments }}'
        maxScore: '{{ item.maxScore }}'
        minScore: '{{ item.minScore }}'
        name: '{{ item.name }}'
        profileConfig: '{{ item.profileConfig }}'
        revisionCreateTime: '{{ item.revisionCreateTime }}'
        revisionId: '{{ item.revisionId }}'
        revisionPublishTime: '{{ item.revisionPublishTime }}'
        revisionUpdateTime: '{{ item.revisionUpdateTime }}'
        scoringConfigs: '{{ item.scoringConfigs }}'
    # required_params: ['name']
  # Get a security profile v2.
  # ── gcp.apigee.organizations.securityProfilesV2.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.securityProfilesV2.get
    for_each: gcp.apigee.organizations.securityProfilesV2.list
    # python: svc.organizations().securityProfilesV2().get(**params).execute()
    calls:
      - action: organizations.securityProfilesV2.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        googleDefined: '{{ response.googleDefined }}'
        name: '{{ response.name }}'
        profileAssessmentConfigs: '{{ response.profileAssessmentConfigs }}'
        riskAssessmentType: '{{ response.riskAssessmentType }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets a shared flow by name, including a list of its revisions.
  # ── gcp.apigee.organizations.sharedflows.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.sharedflows.get
    for_each: gcp.apigee.organizations.sharedflows.list
    # python: svc.organizations().sharedflows().get(**params).execute()
    calls:
      - action: organizations.sharedflows.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        latestRevisionId: '{{ response.latestRevisionId }}'
        metaData: '{{ response.metaData }}'
        name: '{{ response.name }}'
        revision: '{{ response.revision }}'
        space: '{{ response.space }}'
    # required_params: ['name']
  # Gets a revision of a shared flow. To download the shared flow configuration bundle for the specified
  # ── gcp.apigee.organizations.sharedflows.revisions.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.sharedflows.revisions.get
    for_each: gcp.apigee.organizations.apis.list
    # python: svc.organizations().sharedflows().revisions().get(**params).execute()
    calls:
      - action: organizations.sharedflows.revisions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        contentType: '{{ response.contentType }}'
        data: '{{ response.data }}'
        extensions: '{{ response.extensions }}'
    # required_params: ['name']
  # Gets an API category.
  # ── gcp.apigee.organizations.sites.apicategories.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.sites.apicategories.get
    for_each: gcp.apigee.organizations.sites.apicategories.list
    # python: svc.organizations().sites().apicategories().get(**params).execute()
    calls:
      - action: organizations.sites.apicategories.get
        params:
          name: '{{ item.id }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        data: '{{ response.data }}'
        errorCode: '{{ response.errorCode }}'
        message: '{{ response.message }}'
        requestId: '{{ response.requestId }}'
        status: '{{ response.status }}'
    # required_params: ['name']
  # Gets a catalog item.
  # ── gcp.apigee.organizations.sites.apidocs.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.sites.apidocs.get
    for_each: gcp.apigee.organizations.sites.apidocs.list
    # python: svc.organizations().sites().apidocs().get(**params).execute()
    calls:
      - action: organizations.sites.apidocs.get
        params:
          name: '{{ item.id }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        data: '{{ response.data }}'
        errorCode: '{{ response.errorCode }}'
        message: '{{ response.message }}'
        requestId: '{{ response.requestId }}'
        status: '{{ response.status }}'
    # required_params: ['name']
  # Gets the documentation for the specified catalog item.
  # ── gcp.apigee.organizations.sites.apidocs.getDocumentation [dependent] ──
  - discovery_id: gcp.apigee.organizations.sites.apidocs.getDocumentation
    for_each: gcp.apigee.organizations.sites.apidocs.list
    # python: svc.organizations().sites().apidocs().getDocumentation(**params).execute()
    calls:
      - action: organizations.sites.apidocs.getDocumentation
        params:
          name: '{{ item.id }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        data: '{{ response.data }}'
        errorCode: '{{ response.errorCode }}'
        message: '{{ response.message }}'
        requestId: '{{ response.requestId }}'
        status: '{{ response.status }}'
    # required_params: ['name']
  # Get a space under an Organization.
  # ── gcp.apigee.organizations.spaces.get [dependent] ──
  - discovery_id: gcp.apigee.organizations.spaces.get
    for_each: gcp.apigee.organizations.spaces.list
    # python: svc.organizations().spaces().get(**params).execute()
    calls:
      - action: organizations.spaces.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        displayName: '{{ response.displayName }}'
        name: '{{ response.name }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Callers must have apigee.spaces.getIamPolicy.
  # ── gcp.apigee.organizations.spaces.getIamPolicy [dependent] ──
  - discovery_id: gcp.apigee.organizations.spaces.getIamPolicy
    for_each: gcp.apigee.organizations.spaces.list
    # python: svc.organizations().spaces().getIamPolicy(**params).execute()
    calls:
      - action: organizations.spaces.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
