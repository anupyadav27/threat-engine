# ============================================================
# Discovery YAML — runtimeconfig (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 1 | independent: 0 | dependent: 1
# ops with fields: 1/1 | dep ops with chain: 0/1
# ============================================================
version: '1.0'
provider: gcp
service: runtimeconfig

services:
  client: runtimeconfig
  module: "googleapiclient.discovery.build('runtimeconfig', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:


  # ════ DEPENDENT (enrich) operations ════
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.runtimeconfig.operations.list [dependent] ──
  - discovery_id: gcp.runtimeconfig.operations.list
    # python: svc.operations().list(**params).execute()
    calls:
      - action: operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
    # required_params: ['name']
