# ============================================================
# Discovery YAML — workloadmanager (v1)
# Generated: 2026-02-21T03:06:41Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 18 | independent: 11 | dependent: 7
# ops with fields: 18/18 | dep ops with chain: 7/7
# ============================================================
version: '1.0'
provider: gcp
service: workloadmanager

services:
  client: workloadmanager
  module: "googleapiclient.discovery.build('workloadmanager', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists Actuations in a given project, location and deployment.
  # ── gcp.workloadmanager.projects.locations.deployments.actuations.list ──
  - discovery_id: gcp.workloadmanager.projects.locations.deployments.actuations.list
    # python: svc.projects().locations().deployments().actuations().list(**params).execute()
    calls:
      - action: projects.locations.deployments.actuations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.actuations }}'
      item:
        actuationOutput: '{{ item.actuationOutput }}'
        deploymentOutput: '{{ item.deploymentOutput }}'
        endTime: '{{ item.endTime }}'
        name: '{{ item.name }}'
        startTime: '{{ item.startTime }}'
        state: '{{ item.state }}'
  # Lists Deployments in a given project and location.
  # ── gcp.workloadmanager.projects.locations.deployments.list ──
  - discovery_id: gcp.workloadmanager.projects.locations.deployments.list
    # python: svc.projects().locations().deployments().list(**params).execute()
    calls:
      - action: projects.locations.deployments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.deployments }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        name: '{{ item.name }}'
        sapSystemS4Config: '{{ item.sapSystemS4Config }}'
        serviceAccount: '{{ item.serviceAccount }}'
        sqlServerWorkload: '{{ item.sqlServerWorkload }}'
        state: '{{ item.state }}'
        terraformVariables: '{{ item.terraformVariables }}'
        updateTime: '{{ item.updateTime }}'
        workerPool: '{{ item.workerPool }}'
        workloadType: '{{ item.workloadType }}'
  # List discovered workload profiles
  # ── gcp.workloadmanager.projects.locations.discoveredprofiles.list ──
  - discovery_id: gcp.workloadmanager.projects.locations.discoveredprofiles.list
    # python: svc.projects().locations().discoveredprofiles().list(**params).execute()
    calls:
      - action: projects.locations.discoveredprofiles.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.discoveredprofiles }}'
      item:
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        refreshedTime: '{{ item.refreshedTime }}'
        sapWorkload: '{{ item.sapWorkload }}'
        workloadType: '{{ item.workloadType }}'
  # Lists Executions in a given project and location.
  # ── gcp.workloadmanager.projects.locations.evaluations.executions.list ──
  - discovery_id: gcp.workloadmanager.projects.locations.evaluations.executions.list
    # python: svc.projects().locations().evaluations().executions().list(**params).execute()
    calls:
      - action: projects.locations.evaluations.executions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.executions }}'
      item:
        endTime: '{{ item.endTime }}'
        engine: '{{ item.engine }}'
        evaluationId: '{{ item.evaluationId }}'
        externalDataSources: '{{ item.externalDataSources }}'
        inventoryTime: '{{ item.inventoryTime }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        notices: '{{ item.notices }}'
        resultSummary: '{{ item.resultSummary }}'
        ruleResults: '{{ item.ruleResults }}'
        runType: '{{ item.runType }}'
        startTime: '{{ item.startTime }}'
        state: '{{ item.state }}'
  # Lists the result of a single evaluation.
  # ── gcp.workloadmanager.projects.locations.evaluations.executions.results.list ──
  - discovery_id: gcp.workloadmanager.projects.locations.evaluations.executions.results.list
    # python: svc.projects().locations().evaluations().executions().results().list(**params).execute()
    calls:
      - action: projects.locations.evaluations.executions.results.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.results }}'
      item:
        commands: '{{ item.commands }}'
        documentationUrl: '{{ item.documentationUrl }}'
        resource: '{{ item.resource }}'
        rule: '{{ item.rule }}'
        severity: '{{ item.severity }}'
        type: '{{ item.type }}'
        violationDetails: '{{ item.violationDetails }}'
        violationMessage: '{{ item.violationMessage }}'
  # List all scanned resources for a single Execution.
  # ── gcp.workloadmanager.projects.locations.evaluations.executions.scannedResources.list ──
  - discovery_id: gcp.workloadmanager.projects.locations.evaluations.executions.scannedResources.list
    # python: svc.projects().locations().evaluations().executions().scannedResources().list(**params).execute()
    calls:
      - action: projects.locations.evaluations.executions.scannedResources.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.scannedResources }}'
      item:
        resource: '{{ item.resource }}'
        type: '{{ item.type }}'
  # Lists Evaluations in a given project and location.
  # ── gcp.workloadmanager.projects.locations.evaluations.list ──
  - discovery_id: gcp.workloadmanager.projects.locations.evaluations.list
    # python: svc.projects().locations().evaluations().list(**params).execute()
    calls:
      - action: projects.locations.evaluations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.evaluations }}'
      item:
        bigQueryDestination: '{{ item.bigQueryDestination }}'
        createTime: '{{ item.createTime }}'
        customRulesBucket: '{{ item.customRulesBucket }}'
        description: '{{ item.description }}'
        evaluationType: '{{ item.evaluationType }}'
        kmsKey: '{{ item.kmsKey }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        resourceFilter: '{{ item.resourceFilter }}'
        resourceStatus: '{{ item.resourceStatus }}'
        ruleNames: '{{ item.ruleNames }}'
        ruleVersions: '{{ item.ruleVersions }}'
        schedule: '{{ item.schedule }}'
        updateTime: '{{ item.updateTime }}'
  # Gets information about a location.
  # ── gcp.workloadmanager.projects.locations.get ──
  - discovery_id: gcp.workloadmanager.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.workloadmanager.projects.locations.list ──
  - discovery_id: gcp.workloadmanager.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.workloadmanager.projects.locations.operations.list ──
  - discovery_id: gcp.workloadmanager.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists rules in a given project.
  # ── gcp.workloadmanager.projects.locations.rules.list ──
  - discovery_id: gcp.workloadmanager.projects.locations.rules.list
    # python: svc.projects().locations().rules().list(**params).execute()
    calls:
      - action: projects.locations.rules.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.rules }}'
      item:
        assetType: '{{ item.assetType }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        errorMessage: '{{ item.errorMessage }}'
        name: '{{ item.name }}'
        primaryCategory: '{{ item.primaryCategory }}'
        remediation: '{{ item.remediation }}'
        revisionId: '{{ item.revisionId }}'
        ruleType: '{{ item.ruleType }}'
        secondaryCategory: '{{ item.secondaryCategory }}'
        severity: '{{ item.severity }}'
        tags: '{{ item.tags }}'
        uri: '{{ item.uri }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets details of a single Actuation.
  # ── gcp.workloadmanager.projects.locations.deployments.actuations.get [dependent] ──
  - discovery_id: gcp.workloadmanager.projects.locations.deployments.actuations.get
    for_each: gcp.workloadmanager.projects.locations.deployments.actuations.list
    # python: svc.projects().locations().deployments().actuations().get(**params).execute()
    calls:
      - action: projects.locations.deployments.actuations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        actuationOutput: '{{ response.actuationOutput }}'
        deploymentOutput: '{{ response.deploymentOutput }}'
        endTime: '{{ response.endTime }}'
        name: '{{ response.name }}'
        startTime: '{{ response.startTime }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Gets details of a single Deployment.
  # ── gcp.workloadmanager.projects.locations.deployments.get [dependent] ──
  - discovery_id: gcp.workloadmanager.projects.locations.deployments.get
    for_each: gcp.workloadmanager.projects.locations.deployments.list
    # python: svc.projects().locations().deployments().get(**params).execute()
    calls:
      - action: projects.locations.deployments.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        name: '{{ response.name }}'
        sapSystemS4Config: '{{ response.sapSystemS4Config }}'
        serviceAccount: '{{ response.serviceAccount }}'
        sqlServerWorkload: '{{ response.sqlServerWorkload }}'
        state: '{{ response.state }}'
        terraformVariables: '{{ response.terraformVariables }}'
        updateTime: '{{ response.updateTime }}'
        workerPool: '{{ response.workerPool }}'
        workloadType: '{{ response.workloadType }}'
    # required_params: ['name']
  # Gets details of a discovered workload profile.
  # ── gcp.workloadmanager.projects.locations.discoveredprofiles.get [dependent] ──
  - discovery_id: gcp.workloadmanager.projects.locations.discoveredprofiles.get
    for_each: gcp.workloadmanager.projects.locations.discoveredprofiles.list
    # python: svc.projects().locations().discoveredprofiles().get(**params).execute()
    calls:
      - action: projects.locations.discoveredprofiles.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        refreshedTime: '{{ response.refreshedTime }}'
        sapWorkload: '{{ response.sapWorkload }}'
        workloadType: '{{ response.workloadType }}'
    # required_params: ['name']
  # Get the health of a discovered workload profile.
  # ── gcp.workloadmanager.projects.locations.discoveredprofiles.health.get [dependent] ──
  - discovery_id: gcp.workloadmanager.projects.locations.discoveredprofiles.health.get
    for_each: gcp.workloadmanager.projects.locations.discoveredprofiles.list
    # python: svc.projects().locations().discoveredprofiles().health().get(**params).execute()
    calls:
      - action: projects.locations.discoveredprofiles.health.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        checkTime: '{{ response.checkTime }}'
        componentsHealth: '{{ response.componentsHealth }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Gets details of a single Execution.
  # ── gcp.workloadmanager.projects.locations.evaluations.executions.get [dependent] ──
  - discovery_id: gcp.workloadmanager.projects.locations.evaluations.executions.get
    for_each: gcp.workloadmanager.projects.locations.evaluations.executions.list
    # python: svc.projects().locations().evaluations().executions().get(**params).execute()
    calls:
      - action: projects.locations.evaluations.executions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        endTime: '{{ response.endTime }}'
        engine: '{{ response.engine }}'
        evaluationId: '{{ response.evaluationId }}'
        externalDataSources: '{{ response.externalDataSources }}'
        inventoryTime: '{{ response.inventoryTime }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        notices: '{{ response.notices }}'
        resultSummary: '{{ response.resultSummary }}'
        ruleResults: '{{ response.ruleResults }}'
        runType: '{{ response.runType }}'
        startTime: '{{ response.startTime }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Gets details of a single Evaluation.
  # ── gcp.workloadmanager.projects.locations.evaluations.get [dependent] ──
  - discovery_id: gcp.workloadmanager.projects.locations.evaluations.get
    for_each: gcp.workloadmanager.projects.locations.evaluations.list
    # python: svc.projects().locations().evaluations().get(**params).execute()
    calls:
      - action: projects.locations.evaluations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        bigQueryDestination: '{{ response.bigQueryDestination }}'
        createTime: '{{ response.createTime }}'
        customRulesBucket: '{{ response.customRulesBucket }}'
        description: '{{ response.description }}'
        evaluationType: '{{ response.evaluationType }}'
        kmsKey: '{{ response.kmsKey }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        resourceFilter: '{{ response.resourceFilter }}'
        resourceStatus: '{{ response.resourceStatus }}'
        ruleNames: '{{ response.ruleNames }}'
        ruleVersions: '{{ response.ruleVersions }}'
        schedule: '{{ response.schedule }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.workloadmanager.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.workloadmanager.projects.locations.operations.get
    for_each: gcp.workloadmanager.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
