# ============================================================
# Discovery YAML — firebaseappcheck (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 19 | independent: 9 | dependent: 10
# ops with fields: 19/19 | dep ops with chain: 4/10
# ============================================================
version: '1.0'
provider: gcp
service: firebaseappcheck

services:
  client: firebaseappcheck
  module: "googleapiclient.discovery.build('firebaseappcheck', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Atomically gets the AppAttestConfigs for the specified list of apps.
  # ── gcp.firebaseappcheck.projects.apps.appAttestConfig.batchGet ──
  - discovery_id: gcp.firebaseappcheck.projects.apps.appAttestConfig.batchGet
    # python: svc.projects().apps().appAttestConfig().batchGet(**params).execute()
    calls:
      - action: projects.apps.appAttestConfig.batchGet
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        configs: '{{ response.configs }}'
  # Lists all DebugTokens for the specified app. For security reasons, the `token` field is never popula
  # ── gcp.firebaseappcheck.projects.apps.debugTokens.list ──
  - discovery_id: gcp.firebaseappcheck.projects.apps.debugTokens.list
    # python: svc.projects().apps().debugTokens().list(**params).execute()
    calls:
      - action: projects.apps.debugTokens.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.debugTokens }}'
      item:
        displayName: '{{ item.displayName }}'
        name: '{{ item.name }}'
        token: '{{ item.token }}'
        updateTime: '{{ item.updateTime }}'
  # Atomically gets the DeviceCheckConfigs for the specified list of apps. For security reasons, the `pr
  # ── gcp.firebaseappcheck.projects.apps.deviceCheckConfig.batchGet ──
  - discovery_id: gcp.firebaseappcheck.projects.apps.deviceCheckConfig.batchGet
    # python: svc.projects().apps().deviceCheckConfig().batchGet(**params).execute()
    calls:
      - action: projects.apps.deviceCheckConfig.batchGet
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        configs: '{{ response.configs }}'
  # Atomically gets the PlayIntegrityConfigs for the specified list of apps.
  # ── gcp.firebaseappcheck.projects.apps.playIntegrityConfig.batchGet ──
  - discovery_id: gcp.firebaseappcheck.projects.apps.playIntegrityConfig.batchGet
    # python: svc.projects().apps().playIntegrityConfig().batchGet(**params).execute()
    calls:
      - action: projects.apps.playIntegrityConfig.batchGet
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        configs: '{{ response.configs }}'
  # Atomically gets the RecaptchaEnterpriseConfigs for the specified list of apps.
  # ── gcp.firebaseappcheck.projects.apps.recaptchaEnterpriseConfig.batchGet ──
  - discovery_id: gcp.firebaseappcheck.projects.apps.recaptchaEnterpriseConfig.batchGet
    # python: svc.projects().apps().recaptchaEnterpriseConfig().batchGet(**params).execute()
    calls:
      - action: projects.apps.recaptchaEnterpriseConfig.batchGet
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        configs: '{{ response.configs }}'
  # Atomically gets the RecaptchaV3Configs for the specified list of apps. For security reasons, the `si
  # ── gcp.firebaseappcheck.projects.apps.recaptchaV3Config.batchGet ──
  - discovery_id: gcp.firebaseappcheck.projects.apps.recaptchaV3Config.batchGet
    # python: svc.projects().apps().recaptchaV3Config().batchGet(**params).execute()
    calls:
      - action: projects.apps.recaptchaV3Config.batchGet
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        configs: '{{ response.configs }}'
  # Atomically gets the SafetyNetConfigs for the specified list of apps.
  # ── gcp.firebaseappcheck.projects.apps.safetyNetConfig.batchGet ──
  - discovery_id: gcp.firebaseappcheck.projects.apps.safetyNetConfig.batchGet
    # python: svc.projects().apps().safetyNetConfig().batchGet(**params).execute()
    calls:
      - action: projects.apps.safetyNetConfig.batchGet
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        configs: '{{ response.configs }}'
  # Lists all Service configurations for the specified project. Only Services which were explicitly conf
  # ── gcp.firebaseappcheck.projects.services.list ──
  - discovery_id: gcp.firebaseappcheck.projects.services.list
    # python: svc.projects().services().list(**params).execute()
    calls:
      - action: projects.services.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.services }}'
      item:
        enforcementMode: '{{ item.enforcementMode }}'
        name: '{{ item.name }}'
  # Lists all ResourcePolicy configurations for the specified project and service.
  # ── gcp.firebaseappcheck.projects.services.resourcePolicies.list ──
  - discovery_id: gcp.firebaseappcheck.projects.services.resourcePolicies.list
    # python: svc.projects().services().resourcePolicies().list(**params).execute()
    calls:
      - action: projects.services.resourcePolicies.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.resourcePolicies }}'
      item:
        enforcementMode: '{{ item.enforcementMode }}'
        name: '{{ item.name }}'
        targetResource: '{{ item.targetResource }}'
        updateTime: '{{ item.updateTime }}'

  # ════ DEPENDENT (enrich) operations ════
  # Returns a public JWK set as specified by [RFC 7517](https://tools.ietf.org/html/rfc7517) that can be
  # ── gcp.firebaseappcheck.jwks.get [dependent] ──
  - discovery_id: gcp.firebaseappcheck.jwks.get
    for_each: gcp.firebaseappcheck.projects.apps.debugTokens.list
    # python: svc.jwks().get(**params).execute()
    calls:
      - action: jwks.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        keys: '{{ response.keys }}'
    # required_params: ['name']
  # Gets the AppAttestConfig for the specified app.
  # ── gcp.firebaseappcheck.projects.apps.appAttestConfig.get [dependent] ──
  - discovery_id: gcp.firebaseappcheck.projects.apps.appAttestConfig.get
    # python: svc.projects().apps().appAttestConfig().get(**params).execute()
    calls:
      - action: projects.apps.appAttestConfig.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        tokenTtl: '{{ response.tokenTtl }}'
    # required_params: ['name']
  # Gets the specified DebugToken. For security reasons, the `token` field is never populated in the res
  # ── gcp.firebaseappcheck.projects.apps.debugTokens.get [dependent] ──
  - discovery_id: gcp.firebaseappcheck.projects.apps.debugTokens.get
    for_each: gcp.firebaseappcheck.projects.apps.debugTokens.list
    # python: svc.projects().apps().debugTokens().get(**params).execute()
    calls:
      - action: projects.apps.debugTokens.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        name: '{{ response.name }}'
        token: '{{ response.token }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the DeviceCheckConfig for the specified app. For security reasons, the `private_key` field is n
  # ── gcp.firebaseappcheck.projects.apps.deviceCheckConfig.get [dependent] ──
  - discovery_id: gcp.firebaseappcheck.projects.apps.deviceCheckConfig.get
    # python: svc.projects().apps().deviceCheckConfig().get(**params).execute()
    calls:
      - action: projects.apps.deviceCheckConfig.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        keyId: '{{ response.keyId }}'
        name: '{{ response.name }}'
        privateKey: '{{ response.privateKey }}'
        privateKeySet: '{{ response.privateKeySet }}'
        tokenTtl: '{{ response.tokenTtl }}'
    # required_params: ['name']
  # Gets the PlayIntegrityConfig for the specified app.
  # ── gcp.firebaseappcheck.projects.apps.playIntegrityConfig.get [dependent] ──
  - discovery_id: gcp.firebaseappcheck.projects.apps.playIntegrityConfig.get
    # python: svc.projects().apps().playIntegrityConfig().get(**params).execute()
    calls:
      - action: projects.apps.playIntegrityConfig.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        accountDetails: '{{ response.accountDetails }}'
        appIntegrity: '{{ response.appIntegrity }}'
        deviceIntegrity: '{{ response.deviceIntegrity }}'
        name: '{{ response.name }}'
        tokenTtl: '{{ response.tokenTtl }}'
    # required_params: ['name']
  # Gets the RecaptchaEnterpriseConfig for the specified app.
  # ── gcp.firebaseappcheck.projects.apps.recaptchaEnterpriseConfig.get [dependent] ──
  - discovery_id: gcp.firebaseappcheck.projects.apps.recaptchaEnterpriseConfig.get
    # python: svc.projects().apps().recaptchaEnterpriseConfig().get(**params).execute()
    calls:
      - action: projects.apps.recaptchaEnterpriseConfig.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        riskAnalysis: '{{ response.riskAnalysis }}'
        siteKey: '{{ response.siteKey }}'
        tokenTtl: '{{ response.tokenTtl }}'
    # required_params: ['name']
  # Gets the RecaptchaV3Config for the specified app. For security reasons, the `site_secret` field is n
  # ── gcp.firebaseappcheck.projects.apps.recaptchaV3Config.get [dependent] ──
  - discovery_id: gcp.firebaseappcheck.projects.apps.recaptchaV3Config.get
    # python: svc.projects().apps().recaptchaV3Config().get(**params).execute()
    calls:
      - action: projects.apps.recaptchaV3Config.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        minValidScore: '{{ response.minValidScore }}'
        name: '{{ response.name }}'
        siteSecret: '{{ response.siteSecret }}'
        siteSecretSet: '{{ response.siteSecretSet }}'
        tokenTtl: '{{ response.tokenTtl }}'
    # required_params: ['name']
  # Gets the SafetyNetConfig for the specified app.
  # ── gcp.firebaseappcheck.projects.apps.safetyNetConfig.get [dependent] ──
  - discovery_id: gcp.firebaseappcheck.projects.apps.safetyNetConfig.get
    # python: svc.projects().apps().safetyNetConfig().get(**params).execute()
    calls:
      - action: projects.apps.safetyNetConfig.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        tokenTtl: '{{ response.tokenTtl }}'
    # required_params: ['name']
  # Gets the Service configuration for the specified service name.
  # ── gcp.firebaseappcheck.projects.services.get [dependent] ──
  - discovery_id: gcp.firebaseappcheck.projects.services.get
    for_each: gcp.firebaseappcheck.projects.services.list
    # python: svc.projects().services().get(**params).execute()
    calls:
      - action: projects.services.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        enforcementMode: '{{ response.enforcementMode }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Gets the requested ResourcePolicy configuration.
  # ── gcp.firebaseappcheck.projects.services.resourcePolicies.get [dependent] ──
  - discovery_id: gcp.firebaseappcheck.projects.services.resourcePolicies.get
    for_each: gcp.firebaseappcheck.projects.services.resourcePolicies.list
    # python: svc.projects().services().resourcePolicies().get(**params).execute()
    calls:
      - action: projects.services.resourcePolicies.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        enforcementMode: '{{ response.enforcementMode }}'
        name: '{{ response.name }}'
        targetResource: '{{ response.targetResource }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
