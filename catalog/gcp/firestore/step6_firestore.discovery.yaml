# ============================================================
# Discovery YAML — firestore (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 19 | independent: 8 | dependent: 11
# ops with fields: 19/19 | dep ops with chain: 9/11
# ============================================================
version: '1.0'
provider: gcp
service: firestore

services:
  client: firestore
  module: "googleapiclient.discovery.build('firestore', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # List backup schedules.
  # ── gcp.firestore.projects.databases.backupSchedules.list ──
  - discovery_id: gcp.firestore.projects.databases.backupSchedules.list
    # python: svc.projects().databases().backupSchedules().list(**params).execute()
    calls:
      - action: projects.databases.backupSchedules.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.backupSchedules }}'
      item:
        createTime: '{{ item.createTime }}'
        dailyRecurrence: '{{ item.dailyRecurrence }}'
        name: '{{ item.name }}'
        retention: '{{ item.retention }}'
        updateTime: '{{ item.updateTime }}'
        weeklyRecurrence: '{{ item.weeklyRecurrence }}'
  # Lists the field configuration and metadata for this database. Currently, FirestoreAdmin.ListFields o
  # ── gcp.firestore.projects.databases.collectionGroups.fields.list ──
  - discovery_id: gcp.firestore.projects.databases.collectionGroups.fields.list
    # python: svc.projects().databases().collectionGroups().fields().list(**params).execute()
    calls:
      - action: projects.databases.collectionGroups.fields.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.fields }}'
      item:
        indexConfig: '{{ item.indexConfig }}'
        name: '{{ item.name }}'
        ttlConfig: '{{ item.ttlConfig }}'
  # Lists composite indexes.
  # ── gcp.firestore.projects.databases.collectionGroups.indexes.list ──
  - discovery_id: gcp.firestore.projects.databases.collectionGroups.indexes.list
    # python: svc.projects().databases().collectionGroups().indexes().list(**params).execute()
    calls:
      - action: projects.databases.collectionGroups.indexes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.indexes }}'
      item:
        apiScope: '{{ item.apiScope }}'
        density: '{{ item.density }}'
        fields: '{{ item.fields }}'
        multikey: '{{ item.multikey }}'
        name: '{{ item.name }}'
        queryScope: '{{ item.queryScope }}'
        shardCount: '{{ item.shardCount }}'
        state: '{{ item.state }}'
        unique: '{{ item.unique }}'
  # List all the databases in the project.
  # ── gcp.firestore.projects.databases.list ──
  - discovery_id: gcp.firestore.projects.databases.list
    # python: svc.projects().databases().list(**params).execute()
    calls:
      - action: projects.databases.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.databases }}'
      item:
        appEngineIntegrationMode: '{{ item.appEngineIntegrationMode }}'
        cmekConfig: '{{ item.cmekConfig }}'
        concurrencyMode: '{{ item.concurrencyMode }}'
        createTime: '{{ item.createTime }}'
        databaseEdition: '{{ item.databaseEdition }}'
        deleteProtectionState: '{{ item.deleteProtectionState }}'
        deleteTime: '{{ item.deleteTime }}'
        earliestVersionTime: '{{ item.earliestVersionTime }}'
        firestoreDataAccessMode: '{{ item.firestoreDataAccessMode }}'
        freeTier: '{{ item.freeTier }}'
        keyPrefix: '{{ item.keyPrefix }}'
        locationId: '{{ item.locationId }}'
        mongodbCompatibleDataAccessMode: '{{ item.mongodbCompatibleDataAccessMode }}'
        name: '{{ item.name }}'
        pointInTimeRecoveryEnablement: '{{ item.pointInTimeRecoveryEnablement }}'
        previousId: '{{ item.previousId }}'
        realtimeUpdatesMode: '{{ item.realtimeUpdatesMode }}'
        sourceInfo: '{{ item.sourceInfo }}'
        tags: '{{ item.tags }}'
        type: '{{ item.type }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
        versionRetentionPeriod: '{{ item.versionRetentionPeriod }}'
  # List all user creds in the database. Note that the returned resource does not contain the secret val
  # ── gcp.firestore.projects.databases.userCreds.list ──
  - discovery_id: gcp.firestore.projects.databases.userCreds.list
    # python: svc.projects().databases().userCreds().list(**params).execute()
    calls:
      - action: projects.databases.userCreds.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.userCreds }}'
      item:
        createTime: '{{ item.createTime }}'
        name: '{{ item.name }}'
        resourceIdentity: '{{ item.resourceIdentity }}'
        securePassword: '{{ item.securePassword }}'
        state: '{{ item.state }}'
        updateTime: '{{ item.updateTime }}'
  # Lists all the backups.
  # ── gcp.firestore.projects.locations.backups.list ──
  - discovery_id: gcp.firestore.projects.locations.backups.list
    # python: svc.projects().locations().backups().list(**params).execute()
    calls:
      - action: projects.locations.backups.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.backups }}'
      item:
        database: '{{ item.database }}'
        databaseUid: '{{ item.databaseUid }}'
        expireTime: '{{ item.expireTime }}'
        name: '{{ item.name }}'
        snapshotTime: '{{ item.snapshotTime }}'
        state: '{{ item.state }}'
        stats: '{{ item.stats }}'
  # Gets information about a location.
  # ── gcp.firestore.projects.locations.get ──
  - discovery_id: gcp.firestore.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.firestore.projects.locations.list ──
  - discovery_id: gcp.firestore.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets information about a backup schedule.
  # ── gcp.firestore.projects.databases.backupSchedules.get [dependent] ──
  - discovery_id: gcp.firestore.projects.databases.backupSchedules.get
    for_each: gcp.firestore.projects.databases.backupSchedules.list
    # python: svc.projects().databases().backupSchedules().get(**params).execute()
    calls:
      - action: projects.databases.backupSchedules.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        dailyRecurrence: '{{ response.dailyRecurrence }}'
        name: '{{ response.name }}'
        retention: '{{ response.retention }}'
        updateTime: '{{ response.updateTime }}'
        weeklyRecurrence: '{{ response.weeklyRecurrence }}'
    # required_params: ['name']
  # Gets the metadata and configuration for a Field.
  # ── gcp.firestore.projects.databases.collectionGroups.fields.get [dependent] ──
  - discovery_id: gcp.firestore.projects.databases.collectionGroups.fields.get
    for_each: gcp.firestore.projects.databases.collectionGroups.fields.list
    # python: svc.projects().databases().collectionGroups().fields().get(**params).execute()
    calls:
      - action: projects.databases.collectionGroups.fields.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        indexConfig: '{{ response.indexConfig }}'
        name: '{{ response.name }}'
        ttlConfig: '{{ response.ttlConfig }}'
    # required_params: ['name']
  # Gets a composite index.
  # ── gcp.firestore.projects.databases.collectionGroups.indexes.get [dependent] ──
  - discovery_id: gcp.firestore.projects.databases.collectionGroups.indexes.get
    for_each: gcp.firestore.projects.databases.collectionGroups.indexes.list
    # python: svc.projects().databases().collectionGroups().indexes().get(**params).execute()
    calls:
      - action: projects.databases.collectionGroups.indexes.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiScope: '{{ response.apiScope }}'
        density: '{{ response.density }}'
        fields: '{{ response.fields }}'
        multikey: '{{ response.multikey }}'
        name: '{{ response.name }}'
        queryScope: '{{ response.queryScope }}'
        shardCount: '{{ response.shardCount }}'
        state: '{{ response.state }}'
        unique: '{{ response.unique }}'
    # required_params: ['name']
  # Gets a single document.
  # ── gcp.firestore.projects.databases.documents.get [dependent] ──
  - discovery_id: gcp.firestore.projects.databases.documents.get
    for_each: gcp.firestore.projects.databases.documents.listDocuments
    # python: svc.projects().databases().documents().get(**params).execute()
    calls:
      - action: projects.databases.documents.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        fields: '{{ response.fields }}'
        name: '{{ response.name }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Lists documents.
  # ── gcp.firestore.projects.databases.documents.list [dependent] ──
  - discovery_id: gcp.firestore.projects.databases.documents.list
    # python: svc.projects().databases().documents().list(**params).execute()
    calls:
      - action: projects.databases.documents.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.documents }}'
      item:
        createTime: '{{ item.createTime }}'
        fields: '{{ item.fields }}'
        name: '{{ item.name }}'
        updateTime: '{{ item.updateTime }}'
    # required_params: ['collectionId', 'parent']
  # Lists documents.
  # ── gcp.firestore.projects.databases.documents.listDocuments [dependent] ──
  - discovery_id: gcp.firestore.projects.databases.documents.listDocuments
    # python: svc.projects().databases().documents().listDocuments(**params).execute()
    calls:
      - action: projects.databases.documents.listDocuments
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.documents }}'
      item:
        createTime: '{{ item.createTime }}'
        fields: '{{ item.fields }}'
        name: '{{ item.name }}'
        updateTime: '{{ item.updateTime }}'
    # required_params: ['collectionId', 'parent']
  # Gets information about a database.
  # ── gcp.firestore.projects.databases.get [dependent] ──
  - discovery_id: gcp.firestore.projects.databases.get
    for_each: gcp.firestore.projects.databases.list
    # python: svc.projects().databases().get(**params).execute()
    calls:
      - action: projects.databases.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        appEngineIntegrationMode: '{{ response.appEngineIntegrationMode }}'
        cmekConfig: '{{ response.cmekConfig }}'
        concurrencyMode: '{{ response.concurrencyMode }}'
        createTime: '{{ response.createTime }}'
        databaseEdition: '{{ response.databaseEdition }}'
        deleteProtectionState: '{{ response.deleteProtectionState }}'
        deleteTime: '{{ response.deleteTime }}'
        earliestVersionTime: '{{ response.earliestVersionTime }}'
        firestoreDataAccessMode: '{{ response.firestoreDataAccessMode }}'
        freeTier: '{{ response.freeTier }}'
        keyPrefix: '{{ response.keyPrefix }}'
        locationId: '{{ response.locationId }}'
        mongodbCompatibleDataAccessMode: '{{ response.mongodbCompatibleDataAccessMode }}'
        name: '{{ response.name }}'
        pointInTimeRecoveryEnablement: '{{ response.pointInTimeRecoveryEnablement }}'
        previousId: '{{ response.previousId }}'
        realtimeUpdatesMode: '{{ response.realtimeUpdatesMode }}'
        sourceInfo: '{{ response.sourceInfo }}'
        tags: '{{ response.tags }}'
        type: '{{ response.type }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
        versionRetentionPeriod: '{{ response.versionRetentionPeriod }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.firestore.projects.databases.operations.get [dependent] ──
  - discovery_id: gcp.firestore.projects.databases.operations.get
    for_each: gcp.firestore.projects.databases.operations.list
    # python: svc.projects().databases().operations().get(**params).execute()
    calls:
      - action: projects.databases.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.firestore.projects.databases.operations.list [dependent] ──
  - discovery_id: gcp.firestore.projects.databases.operations.list
    for_each: gcp.firestore.projects.databases.list
    # python: svc.projects().databases().operations().list(**params).execute()
    calls:
      - action: projects.databases.operations.list
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
    # required_params: ['name']
  # Gets a user creds resource. Note that the returned resource does not contain the secret value itself
  # ── gcp.firestore.projects.databases.userCreds.get [dependent] ──
  - discovery_id: gcp.firestore.projects.databases.userCreds.get
    for_each: gcp.firestore.projects.databases.userCreds.list
    # python: svc.projects().databases().userCreds().get(**params).execute()
    calls:
      - action: projects.databases.userCreds.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        name: '{{ response.name }}'
        resourceIdentity: '{{ response.resourceIdentity }}'
        securePassword: '{{ response.securePassword }}'
        state: '{{ response.state }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets information about a backup.
  # ── gcp.firestore.projects.locations.backups.get [dependent] ──
  - discovery_id: gcp.firestore.projects.locations.backups.get
    for_each: gcp.firestore.projects.locations.backups.list
    # python: svc.projects().locations().backups().get(**params).execute()
    calls:
      - action: projects.locations.backups.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        database: '{{ response.database }}'
        databaseUid: '{{ response.databaseUid }}'
        expireTime: '{{ response.expireTime }}'
        name: '{{ response.name }}'
        snapshotTime: '{{ response.snapshotTime }}'
        state: '{{ response.state }}'
        stats: '{{ response.stats }}'
    # required_params: ['name']
