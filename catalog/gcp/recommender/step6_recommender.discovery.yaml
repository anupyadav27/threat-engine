# ============================================================
# Discovery YAML — recommender (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 22 | independent: 8 | dependent: 14
# ops with fields: 22/22 | dep ops with chain: 11/14
# ============================================================
version: '1.0'
provider: gcp
service: recommender

services:
  client: recommender
  module: "googleapiclient.discovery.build('recommender', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists insights for the specified Cloud Resource. Requires the recommender.*.list IAM permission for 
  # ── gcp.recommender.billingAccounts.locations.insightTypes.insights.list ──
  - discovery_id: gcp.recommender.billingAccounts.locations.insightTypes.insights.list
    # python: svc.billingAccounts().locations().insightTypes().insights().list(**params).execute()
    calls:
      - action: billingAccounts.locations.insightTypes.insights.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.insights }}'
      item:
        associatedRecommendations: '{{ item.associatedRecommendations }}'
        category: '{{ item.category }}'
        content: '{{ item.content }}'
        description: '{{ item.description }}'
        insightSubtype: '{{ item.insightSubtype }}'
        lastRefreshTime: '{{ item.lastRefreshTime }}'
        name: '{{ item.name }}'
        observationPeriod: '{{ item.observationPeriod }}'
        severity: '{{ item.severity }}'
        stateInfo: '{{ item.stateInfo }}'
        targetResources: '{{ item.targetResources }}'
  # Lists recommendations for the specified Cloud Resource. Requires the recommender.*.list IAM permissi
  # ── gcp.recommender.billingAccounts.locations.recommenders.recommendations.list ──
  - discovery_id: gcp.recommender.billingAccounts.locations.recommenders.recommendations.list
    # python: svc.billingAccounts().locations().recommenders().recommendations().list(**params).execute()
    calls:
      - action: billingAccounts.locations.recommenders.recommendations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.recommendations }}'
      item:
        additionalImpact: '{{ item.additionalImpact }}'
        associatedInsights: '{{ item.associatedInsights }}'
        content: '{{ item.content }}'
        description: '{{ item.description }}'
        lastRefreshTime: '{{ item.lastRefreshTime }}'
        name: '{{ item.name }}'
        primaryImpact: '{{ item.primaryImpact }}'
        priority: '{{ item.priority }}'
        recommenderSubtype: '{{ item.recommenderSubtype }}'
        stateInfo: '{{ item.stateInfo }}'
        targetResources: '{{ item.targetResources }}'
        xorGroupId: '{{ item.xorGroupId }}'
  # Lists insights for the specified Cloud Resource. Requires the recommender.*.list IAM permission for 
  # ── gcp.recommender.folders.locations.insightTypes.insights.list ──
  - discovery_id: gcp.recommender.folders.locations.insightTypes.insights.list
    # python: svc.folders().locations().insightTypes().insights().list(**params).execute()
    calls:
      - action: folders.locations.insightTypes.insights.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.insights }}'
      item:
        associatedRecommendations: '{{ item.associatedRecommendations }}'
        category: '{{ item.category }}'
        content: '{{ item.content }}'
        description: '{{ item.description }}'
        insightSubtype: '{{ item.insightSubtype }}'
        lastRefreshTime: '{{ item.lastRefreshTime }}'
        name: '{{ item.name }}'
        observationPeriod: '{{ item.observationPeriod }}'
        severity: '{{ item.severity }}'
        stateInfo: '{{ item.stateInfo }}'
        targetResources: '{{ item.targetResources }}'
  # Lists recommendations for the specified Cloud Resource. Requires the recommender.*.list IAM permissi
  # ── gcp.recommender.folders.locations.recommenders.recommendations.list ──
  - discovery_id: gcp.recommender.folders.locations.recommenders.recommendations.list
    # python: svc.folders().locations().recommenders().recommendations().list(**params).execute()
    calls:
      - action: folders.locations.recommenders.recommendations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.recommendations }}'
      item:
        additionalImpact: '{{ item.additionalImpact }}'
        associatedInsights: '{{ item.associatedInsights }}'
        content: '{{ item.content }}'
        description: '{{ item.description }}'
        lastRefreshTime: '{{ item.lastRefreshTime }}'
        name: '{{ item.name }}'
        primaryImpact: '{{ item.primaryImpact }}'
        priority: '{{ item.priority }}'
        recommenderSubtype: '{{ item.recommenderSubtype }}'
        stateInfo: '{{ item.stateInfo }}'
        targetResources: '{{ item.targetResources }}'
        xorGroupId: '{{ item.xorGroupId }}'
  # Lists insights for the specified Cloud Resource. Requires the recommender.*.list IAM permission for 
  # ── gcp.recommender.organizations.locations.insightTypes.insights.list ──
  - discovery_id: gcp.recommender.organizations.locations.insightTypes.insights.list
    # python: svc.organizations().locations().insightTypes().insights().list(**params).execute()
    calls:
      - action: organizations.locations.insightTypes.insights.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.insights }}'
      item:
        associatedRecommendations: '{{ item.associatedRecommendations }}'
        category: '{{ item.category }}'
        content: '{{ item.content }}'
        description: '{{ item.description }}'
        insightSubtype: '{{ item.insightSubtype }}'
        lastRefreshTime: '{{ item.lastRefreshTime }}'
        name: '{{ item.name }}'
        observationPeriod: '{{ item.observationPeriod }}'
        severity: '{{ item.severity }}'
        stateInfo: '{{ item.stateInfo }}'
        targetResources: '{{ item.targetResources }}'
  # Lists recommendations for the specified Cloud Resource. Requires the recommender.*.list IAM permissi
  # ── gcp.recommender.organizations.locations.recommenders.recommendations.list ──
  - discovery_id: gcp.recommender.organizations.locations.recommenders.recommendations.list
    # python: svc.organizations().locations().recommenders().recommendations().list(**params).execute()
    calls:
      - action: organizations.locations.recommenders.recommendations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.recommendations }}'
      item:
        additionalImpact: '{{ item.additionalImpact }}'
        associatedInsights: '{{ item.associatedInsights }}'
        content: '{{ item.content }}'
        description: '{{ item.description }}'
        lastRefreshTime: '{{ item.lastRefreshTime }}'
        name: '{{ item.name }}'
        primaryImpact: '{{ item.primaryImpact }}'
        priority: '{{ item.priority }}'
        recommenderSubtype: '{{ item.recommenderSubtype }}'
        stateInfo: '{{ item.stateInfo }}'
        targetResources: '{{ item.targetResources }}'
        xorGroupId: '{{ item.xorGroupId }}'
  # Lists insights for the specified Cloud Resource. Requires the recommender.*.list IAM permission for 
  # ── gcp.recommender.projects.locations.insightTypes.insights.list ──
  - discovery_id: gcp.recommender.projects.locations.insightTypes.insights.list
    # python: svc.projects().locations().insightTypes().insights().list(**params).execute()
    calls:
      - action: projects.locations.insightTypes.insights.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.insights }}'
      item:
        associatedRecommendations: '{{ item.associatedRecommendations }}'
        category: '{{ item.category }}'
        content: '{{ item.content }}'
        description: '{{ item.description }}'
        insightSubtype: '{{ item.insightSubtype }}'
        lastRefreshTime: '{{ item.lastRefreshTime }}'
        name: '{{ item.name }}'
        observationPeriod: '{{ item.observationPeriod }}'
        severity: '{{ item.severity }}'
        stateInfo: '{{ item.stateInfo }}'
        targetResources: '{{ item.targetResources }}'
  # Lists recommendations for the specified Cloud Resource. Requires the recommender.*.list IAM permissi
  # ── gcp.recommender.projects.locations.recommenders.recommendations.list ──
  - discovery_id: gcp.recommender.projects.locations.recommenders.recommendations.list
    # python: svc.projects().locations().recommenders().recommendations().list(**params).execute()
    calls:
      - action: projects.locations.recommenders.recommendations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.recommendations }}'
      item:
        additionalImpact: '{{ item.additionalImpact }}'
        associatedInsights: '{{ item.associatedInsights }}'
        content: '{{ item.content }}'
        description: '{{ item.description }}'
        lastRefreshTime: '{{ item.lastRefreshTime }}'
        name: '{{ item.name }}'
        primaryImpact: '{{ item.primaryImpact }}'
        priority: '{{ item.priority }}'
        recommenderSubtype: '{{ item.recommenderSubtype }}'
        stateInfo: '{{ item.stateInfo }}'
        targetResources: '{{ item.targetResources }}'
        xorGroupId: '{{ item.xorGroupId }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets the requested InsightTypeConfig. There is only one instance of the config for each InsightType.
  # ── gcp.recommender.billingAccounts.locations.insightTypes.getConfig [dependent] ──
  - discovery_id: gcp.recommender.billingAccounts.locations.insightTypes.getConfig
    # python: svc.billingAccounts().locations().insightTypes().getConfig(**params).execute()
    calls:
      - action: billingAccounts.locations.insightTypes.getConfig
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        displayName: '{{ response.displayName }}'
        insightTypeGenerationConfig: '{{ response.insightTypeGenerationConfig }}'
        name: '{{ response.name }}'
        revisionId: '{{ response.revisionId }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the requested insight. Requires the recommender.*.get IAM permission for the specified insight 
  # ── gcp.recommender.billingAccounts.locations.insightTypes.insights.get [dependent] ──
  - discovery_id: gcp.recommender.billingAccounts.locations.insightTypes.insights.get
    for_each: gcp.recommender.billingAccounts.locations.insightTypes.insights.list
    # python: svc.billingAccounts().locations().insightTypes().insights().get(**params).execute()
    calls:
      - action: billingAccounts.locations.insightTypes.insights.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        associatedRecommendations: '{{ response.associatedRecommendations }}'
        category: '{{ response.category }}'
        content: '{{ response.content }}'
        description: '{{ response.description }}'
        insightSubtype: '{{ response.insightSubtype }}'
        lastRefreshTime: '{{ response.lastRefreshTime }}'
        name: '{{ response.name }}'
        observationPeriod: '{{ response.observationPeriod }}'
        severity: '{{ response.severity }}'
        stateInfo: '{{ response.stateInfo }}'
        targetResources: '{{ response.targetResources }}'
    # required_params: ['name']
  # Gets the requested Recommender Config. There is only one instance of the config for each Recommender
  # ── gcp.recommender.billingAccounts.locations.recommenders.getConfig [dependent] ──
  - discovery_id: gcp.recommender.billingAccounts.locations.recommenders.getConfig
    for_each: gcp.recommender.billingAccounts.locations.recommenders.recommendations.list
    # python: svc.billingAccounts().locations().recommenders().getConfig(**params).execute()
    calls:
      - action: billingAccounts.locations.recommenders.getConfig
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        displayName: '{{ response.displayName }}'
        name: '{{ response.name }}'
        recommenderGenerationConfig: '{{ response.recommenderGenerationConfig }}'
        revisionId: '{{ response.revisionId }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the requested recommendation. Requires the recommender.*.get IAM permission for the specified r
  # ── gcp.recommender.billingAccounts.locations.recommenders.recommendations.get [dependent] ──
  - discovery_id: gcp.recommender.billingAccounts.locations.recommenders.recommendations.get
    for_each: gcp.recommender.billingAccounts.locations.recommenders.recommendations.list
    # python: svc.billingAccounts().locations().recommenders().recommendations().get(**params).execute()
    calls:
      - action: billingAccounts.locations.recommenders.recommendations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        additionalImpact: '{{ response.additionalImpact }}'
        associatedInsights: '{{ response.associatedInsights }}'
        content: '{{ response.content }}'
        description: '{{ response.description }}'
        lastRefreshTime: '{{ response.lastRefreshTime }}'
        name: '{{ response.name }}'
        primaryImpact: '{{ response.primaryImpact }}'
        priority: '{{ response.priority }}'
        recommenderSubtype: '{{ response.recommenderSubtype }}'
        stateInfo: '{{ response.stateInfo }}'
        targetResources: '{{ response.targetResources }}'
        xorGroupId: '{{ response.xorGroupId }}'
    # required_params: ['name']
  # Gets the requested insight. Requires the recommender.*.get IAM permission for the specified insight 
  # ── gcp.recommender.folders.locations.insightTypes.insights.get [dependent] ──
  - discovery_id: gcp.recommender.folders.locations.insightTypes.insights.get
    for_each: gcp.recommender.billingAccounts.locations.insightTypes.insights.list
    # python: svc.folders().locations().insightTypes().insights().get(**params).execute()
    calls:
      - action: folders.locations.insightTypes.insights.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        associatedRecommendations: '{{ response.associatedRecommendations }}'
        category: '{{ response.category }}'
        content: '{{ response.content }}'
        description: '{{ response.description }}'
        insightSubtype: '{{ response.insightSubtype }}'
        lastRefreshTime: '{{ response.lastRefreshTime }}'
        name: '{{ response.name }}'
        observationPeriod: '{{ response.observationPeriod }}'
        severity: '{{ response.severity }}'
        stateInfo: '{{ response.stateInfo }}'
        targetResources: '{{ response.targetResources }}'
    # required_params: ['name']
  # Gets the requested recommendation. Requires the recommender.*.get IAM permission for the specified r
  # ── gcp.recommender.folders.locations.recommenders.recommendations.get [dependent] ──
  - discovery_id: gcp.recommender.folders.locations.recommenders.recommendations.get
    for_each: gcp.recommender.billingAccounts.locations.recommenders.recommendations.list
    # python: svc.folders().locations().recommenders().recommendations().get(**params).execute()
    calls:
      - action: folders.locations.recommenders.recommendations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        additionalImpact: '{{ response.additionalImpact }}'
        associatedInsights: '{{ response.associatedInsights }}'
        content: '{{ response.content }}'
        description: '{{ response.description }}'
        lastRefreshTime: '{{ response.lastRefreshTime }}'
        name: '{{ response.name }}'
        primaryImpact: '{{ response.primaryImpact }}'
        priority: '{{ response.priority }}'
        recommenderSubtype: '{{ response.recommenderSubtype }}'
        stateInfo: '{{ response.stateInfo }}'
        targetResources: '{{ response.targetResources }}'
        xorGroupId: '{{ response.xorGroupId }}'
    # required_params: ['name']
  # Gets the requested InsightTypeConfig. There is only one instance of the config for each InsightType.
  # ── gcp.recommender.organizations.locations.insightTypes.getConfig [dependent] ──
  - discovery_id: gcp.recommender.organizations.locations.insightTypes.getConfig
    # python: svc.organizations().locations().insightTypes().getConfig(**params).execute()
    calls:
      - action: organizations.locations.insightTypes.getConfig
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        displayName: '{{ response.displayName }}'
        insightTypeGenerationConfig: '{{ response.insightTypeGenerationConfig }}'
        name: '{{ response.name }}'
        revisionId: '{{ response.revisionId }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the requested insight. Requires the recommender.*.get IAM permission for the specified insight 
  # ── gcp.recommender.organizations.locations.insightTypes.insights.get [dependent] ──
  - discovery_id: gcp.recommender.organizations.locations.insightTypes.insights.get
    for_each: gcp.recommender.billingAccounts.locations.insightTypes.insights.list
    # python: svc.organizations().locations().insightTypes().insights().get(**params).execute()
    calls:
      - action: organizations.locations.insightTypes.insights.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        associatedRecommendations: '{{ response.associatedRecommendations }}'
        category: '{{ response.category }}'
        content: '{{ response.content }}'
        description: '{{ response.description }}'
        insightSubtype: '{{ response.insightSubtype }}'
        lastRefreshTime: '{{ response.lastRefreshTime }}'
        name: '{{ response.name }}'
        observationPeriod: '{{ response.observationPeriod }}'
        severity: '{{ response.severity }}'
        stateInfo: '{{ response.stateInfo }}'
        targetResources: '{{ response.targetResources }}'
    # required_params: ['name']
  # Gets the requested Recommender Config. There is only one instance of the config for each Recommender
  # ── gcp.recommender.organizations.locations.recommenders.getConfig [dependent] ──
  - discovery_id: gcp.recommender.organizations.locations.recommenders.getConfig
    for_each: gcp.recommender.billingAccounts.locations.recommenders.recommendations.list
    # python: svc.organizations().locations().recommenders().getConfig(**params).execute()
    calls:
      - action: organizations.locations.recommenders.getConfig
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        displayName: '{{ response.displayName }}'
        name: '{{ response.name }}'
        recommenderGenerationConfig: '{{ response.recommenderGenerationConfig }}'
        revisionId: '{{ response.revisionId }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the requested recommendation. Requires the recommender.*.get IAM permission for the specified r
  # ── gcp.recommender.organizations.locations.recommenders.recommendations.get [dependent] ──
  - discovery_id: gcp.recommender.organizations.locations.recommenders.recommendations.get
    for_each: gcp.recommender.billingAccounts.locations.recommenders.recommendations.list
    # python: svc.organizations().locations().recommenders().recommendations().get(**params).execute()
    calls:
      - action: organizations.locations.recommenders.recommendations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        additionalImpact: '{{ response.additionalImpact }}'
        associatedInsights: '{{ response.associatedInsights }}'
        content: '{{ response.content }}'
        description: '{{ response.description }}'
        lastRefreshTime: '{{ response.lastRefreshTime }}'
        name: '{{ response.name }}'
        primaryImpact: '{{ response.primaryImpact }}'
        priority: '{{ response.priority }}'
        recommenderSubtype: '{{ response.recommenderSubtype }}'
        stateInfo: '{{ response.stateInfo }}'
        targetResources: '{{ response.targetResources }}'
        xorGroupId: '{{ response.xorGroupId }}'
    # required_params: ['name']
  # Gets the requested InsightTypeConfig. There is only one instance of the config for each InsightType.
  # ── gcp.recommender.projects.locations.insightTypes.getConfig [dependent] ──
  - discovery_id: gcp.recommender.projects.locations.insightTypes.getConfig
    # python: svc.projects().locations().insightTypes().getConfig(**params).execute()
    calls:
      - action: projects.locations.insightTypes.getConfig
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        displayName: '{{ response.displayName }}'
        insightTypeGenerationConfig: '{{ response.insightTypeGenerationConfig }}'
        name: '{{ response.name }}'
        revisionId: '{{ response.revisionId }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the requested insight. Requires the recommender.*.get IAM permission for the specified insight 
  # ── gcp.recommender.projects.locations.insightTypes.insights.get [dependent] ──
  - discovery_id: gcp.recommender.projects.locations.insightTypes.insights.get
    for_each: gcp.recommender.billingAccounts.locations.insightTypes.insights.list
    # python: svc.projects().locations().insightTypes().insights().get(**params).execute()
    calls:
      - action: projects.locations.insightTypes.insights.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        associatedRecommendations: '{{ response.associatedRecommendations }}'
        category: '{{ response.category }}'
        content: '{{ response.content }}'
        description: '{{ response.description }}'
        insightSubtype: '{{ response.insightSubtype }}'
        lastRefreshTime: '{{ response.lastRefreshTime }}'
        name: '{{ response.name }}'
        observationPeriod: '{{ response.observationPeriod }}'
        severity: '{{ response.severity }}'
        stateInfo: '{{ response.stateInfo }}'
        targetResources: '{{ response.targetResources }}'
    # required_params: ['name']
  # Gets the requested Recommender Config. There is only one instance of the config for each Recommender
  # ── gcp.recommender.projects.locations.recommenders.getConfig [dependent] ──
  - discovery_id: gcp.recommender.projects.locations.recommenders.getConfig
    for_each: gcp.recommender.billingAccounts.locations.recommenders.recommendations.list
    # python: svc.projects().locations().recommenders().getConfig(**params).execute()
    calls:
      - action: projects.locations.recommenders.getConfig
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        displayName: '{{ response.displayName }}'
        name: '{{ response.name }}'
        recommenderGenerationConfig: '{{ response.recommenderGenerationConfig }}'
        revisionId: '{{ response.revisionId }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the requested recommendation. Requires the recommender.*.get IAM permission for the specified r
  # ── gcp.recommender.projects.locations.recommenders.recommendations.get [dependent] ──
  - discovery_id: gcp.recommender.projects.locations.recommenders.recommendations.get
    for_each: gcp.recommender.billingAccounts.locations.recommenders.recommendations.list
    # python: svc.projects().locations().recommenders().recommendations().get(**params).execute()
    calls:
      - action: projects.locations.recommenders.recommendations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        additionalImpact: '{{ response.additionalImpact }}'
        associatedInsights: '{{ response.associatedInsights }}'
        content: '{{ response.content }}'
        description: '{{ response.description }}'
        lastRefreshTime: '{{ response.lastRefreshTime }}'
        name: '{{ response.name }}'
        primaryImpact: '{{ response.primaryImpact }}'
        priority: '{{ response.priority }}'
        recommenderSubtype: '{{ response.recommenderSubtype }}'
        stateInfo: '{{ response.stateInfo }}'
        targetResources: '{{ response.targetResources }}'
        xorGroupId: '{{ response.xorGroupId }}'
    # required_params: ['name']
