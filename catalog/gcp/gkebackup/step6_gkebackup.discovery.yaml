# ============================================================
# Discovery YAML — gkebackup (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 33 | independent: 13 | dependent: 20
# ops with fields: 33/33 | dep ops with chain: 20/20
# ============================================================
version: '1.0'
provider: gcp
service: gkebackup

services:
  client: gkebackup
  module: "googleapiclient.discovery.build('gkebackup', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists BackupPlanBindings in a given location.
  # ── gcp.gkebackup.projects.locations.backupChannels.backupPlanBindings.list ──
  - discovery_id: gcp.gkebackup.projects.locations.backupChannels.backupPlanBindings.list
    # python: svc.projects().locations().backupChannels().backupPlanBindings().list(**params).execute()
    calls:
      - action: projects.locations.backupChannels.backupPlanBindings.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.backupPlanBindings }}'
      item:
        backupPlan: '{{ item.backupPlan }}'
        backupPlanDetails: '{{ item.backupPlanDetails }}'
        cluster: '{{ item.cluster }}'
        createTime: '{{ item.createTime }}'
        name: '{{ item.name }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # Lists BackupChannels in a given location.
  # ── gcp.gkebackup.projects.locations.backupChannels.list ──
  - discovery_id: gcp.gkebackup.projects.locations.backupChannels.list
    # python: svc.projects().locations().backupChannels().list(**params).execute()
    calls:
      - action: projects.locations.backupChannels.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.backupChannels }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        destinationProject: '{{ item.destinationProject }}'
        destinationProjectId: '{{ item.destinationProjectId }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # Lists the Backups for a given BackupPlan.
  # ── gcp.gkebackup.projects.locations.backupPlans.backups.list ──
  - discovery_id: gcp.gkebackup.projects.locations.backupPlans.backups.list
    # python: svc.projects().locations().backupPlans().backups().list(**params).execute()
    calls:
      - action: projects.locations.backupPlans.backups.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.backups }}'
      item:
        allNamespaces: '{{ item.allNamespaces }}'
        clusterMetadata: '{{ item.clusterMetadata }}'
        completeTime: '{{ item.completeTime }}'
        configBackupSizeBytes: '{{ item.configBackupSizeBytes }}'
        containsSecrets: '{{ item.containsSecrets }}'
        containsVolumeData: '{{ item.containsVolumeData }}'
        createTime: '{{ item.createTime }}'
        deleteLockDays: '{{ item.deleteLockDays }}'
        deleteLockExpireTime: '{{ item.deleteLockExpireTime }}'
        description: '{{ item.description }}'
        encryptionKey: '{{ item.encryptionKey }}'
        labels: '{{ item.labels }}'
        manual: '{{ item.manual }}'
        name: '{{ item.name }}'
        permissiveMode: '{{ item.permissiveMode }}'
        podCount: '{{ item.podCount }}'
        resourceCount: '{{ item.resourceCount }}'
        retainDays: '{{ item.retainDays }}'
        retainExpireTime: '{{ item.retainExpireTime }}'
        satisfiesPzi: '{{ item.satisfiesPzi }}'
        satisfiesPzs: '{{ item.satisfiesPzs }}'
        selectedApplications: '{{ item.selectedApplications }}'
        selectedNamespaceLabels: '{{ item.selectedNamespaceLabels }}'
        selectedNamespaces: '{{ item.selectedNamespaces }}'
        sizeBytes: '{{ item.sizeBytes }}'
        state: '{{ item.state }}'
        stateReason: '{{ item.stateReason }}'
        troubleshootingInfo: '{{ item.troubleshootingInfo }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
        volumeCount: '{{ item.volumeCount }}'
  # Lists the VolumeBackups for a given Backup.
  # ── gcp.gkebackup.projects.locations.backupPlans.backups.volumeBackups.list ──
  - discovery_id: gcp.gkebackup.projects.locations.backupPlans.backups.volumeBackups.list
    # python: svc.projects().locations().backupPlans().backups().volumeBackups().list(**params).execute()
    calls:
      - action: projects.locations.backupPlans.backups.volumeBackups.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.volumeBackups }}'
      item:
        completeTime: '{{ item.completeTime }}'
        createTime: '{{ item.createTime }}'
        diskSizeBytes: '{{ item.diskSizeBytes }}'
        format: '{{ item.format }}'
        name: '{{ item.name }}'
        satisfiesPzi: '{{ item.satisfiesPzi }}'
        satisfiesPzs: '{{ item.satisfiesPzs }}'
        sourcePvc: '{{ item.sourcePvc }}'
        state: '{{ item.state }}'
        stateMessage: '{{ item.stateMessage }}'
        storageBytes: '{{ item.storageBytes }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
        volumeBackupHandle: '{{ item.volumeBackupHandle }}'
  # Lists BackupPlans in a given location.
  # ── gcp.gkebackup.projects.locations.backupPlans.list ──
  - discovery_id: gcp.gkebackup.projects.locations.backupPlans.list
    # python: svc.projects().locations().backupPlans().list(**params).execute()
    calls:
      - action: projects.locations.backupPlans.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.backupPlans }}'
      item:
        backupChannel: '{{ item.backupChannel }}'
        backupConfig: '{{ item.backupConfig }}'
        backupSchedule: '{{ item.backupSchedule }}'
        cluster: '{{ item.cluster }}'
        createTime: '{{ item.createTime }}'
        deactivated: '{{ item.deactivated }}'
        description: '{{ item.description }}'
        labels: '{{ item.labels }}'
        lastSuccessfulBackupTime: '{{ item.lastSuccessfulBackupTime }}'
        name: '{{ item.name }}'
        protectedPodCount: '{{ item.protectedPodCount }}'
        retentionPolicy: '{{ item.retentionPolicy }}'
        rpoRiskLevel: '{{ item.rpoRiskLevel }}'
        rpoRiskReason: '{{ item.rpoRiskReason }}'
        state: '{{ item.state }}'
        stateReason: '{{ item.stateReason }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # Gets information about a location.
  # ── gcp.gkebackup.projects.locations.get ──
  - discovery_id: gcp.gkebackup.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.gkebackup.projects.locations.list ──
  - discovery_id: gcp.gkebackup.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.gkebackup.projects.locations.operations.list ──
  - discovery_id: gcp.gkebackup.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists RestoreChannels in a given location.
  # ── gcp.gkebackup.projects.locations.restoreChannels.list ──
  - discovery_id: gcp.gkebackup.projects.locations.restoreChannels.list
    # python: svc.projects().locations().restoreChannels().list(**params).execute()
    calls:
      - action: projects.locations.restoreChannels.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.restoreChannels }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        destinationProject: '{{ item.destinationProject }}'
        destinationProjectId: '{{ item.destinationProjectId }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # Lists RestorePlanBindings in a given location.
  # ── gcp.gkebackup.projects.locations.restoreChannels.restorePlanBindings.list ──
  - discovery_id: gcp.gkebackup.projects.locations.restoreChannels.restorePlanBindings.list
    # python: svc.projects().locations().restoreChannels().restorePlanBindings().list(**params).execute()
    calls:
      - action: projects.locations.restoreChannels.restorePlanBindings.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.restorePlanBindings }}'
      item:
        backupPlan: '{{ item.backupPlan }}'
        createTime: '{{ item.createTime }}'
        name: '{{ item.name }}'
        restorePlan: '{{ item.restorePlan }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # Lists RestorePlans in a given location.
  # ── gcp.gkebackup.projects.locations.restorePlans.list ──
  - discovery_id: gcp.gkebackup.projects.locations.restorePlans.list
    # python: svc.projects().locations().restorePlans().list(**params).execute()
    calls:
      - action: projects.locations.restorePlans.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.restorePlans }}'
      item:
        backupPlan: '{{ item.backupPlan }}'
        cluster: '{{ item.cluster }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        restoreChannel: '{{ item.restoreChannel }}'
        restoreConfig: '{{ item.restoreConfig }}'
        state: '{{ item.state }}'
        stateReason: '{{ item.stateReason }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # Lists the Restores for a given RestorePlan.
  # ── gcp.gkebackup.projects.locations.restorePlans.restores.list ──
  - discovery_id: gcp.gkebackup.projects.locations.restorePlans.restores.list
    # python: svc.projects().locations().restorePlans().restores().list(**params).execute()
    calls:
      - action: projects.locations.restorePlans.restores.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.restores }}'
      item:
        backup: '{{ item.backup }}'
        cluster: '{{ item.cluster }}'
        completeTime: '{{ item.completeTime }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        filter: '{{ item.filter }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        resourcesExcludedCount: '{{ item.resourcesExcludedCount }}'
        resourcesFailedCount: '{{ item.resourcesFailedCount }}'
        resourcesRestoredCount: '{{ item.resourcesRestoredCount }}'
        restoreConfig: '{{ item.restoreConfig }}'
        state: '{{ item.state }}'
        stateReason: '{{ item.stateReason }}'
        troubleshootingInfo: '{{ item.troubleshootingInfo }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
        volumeDataRestorePolicyOverrides: '{{ item.volumeDataRestorePolicyOverrides }}'
        volumesRestoredCount: '{{ item.volumesRestoredCount }}'
  # Lists the VolumeRestores for a given Restore.
  # ── gcp.gkebackup.projects.locations.restorePlans.restores.volumeRestores.list ──
  - discovery_id: gcp.gkebackup.projects.locations.restorePlans.restores.volumeRestores.list
    # python: svc.projects().locations().restorePlans().restores().volumeRestores().list(**params).execute()
    calls:
      - action: projects.locations.restorePlans.restores.volumeRestores.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.volumeRestores }}'
      item:
        completeTime: '{{ item.completeTime }}'
        createTime: '{{ item.createTime }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
        stateMessage: '{{ item.stateMessage }}'
        targetPvc: '{{ item.targetPvc }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
        volumeBackup: '{{ item.volumeBackup }}'
        volumeHandle: '{{ item.volumeHandle }}'
        volumeType: '{{ item.volumeType }}'

  # ════ DEPENDENT (enrich) operations ════
  # Retrieve the details of a single BackupPlanBinding.
  # ── gcp.gkebackup.projects.locations.backupChannels.backupPlanBindings.get [dependent] ──
  - discovery_id: gcp.gkebackup.projects.locations.backupChannels.backupPlanBindings.get
    for_each: gcp.gkebackup.projects.locations.backupChannels.backupPlanBindings.list
    # python: svc.projects().locations().backupChannels().backupPlanBindings().get(**params).execute()
    calls:
      - action: projects.locations.backupChannels.backupPlanBindings.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        backupPlan: '{{ response.backupPlan }}'
        backupPlanDetails: '{{ response.backupPlanDetails }}'
        cluster: '{{ response.cluster }}'
        createTime: '{{ response.createTime }}'
        name: '{{ response.name }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Retrieve the details of a single BackupChannel.
  # ── gcp.gkebackup.projects.locations.backupChannels.get [dependent] ──
  - discovery_id: gcp.gkebackup.projects.locations.backupChannels.get
    for_each: gcp.gkebackup.projects.locations.backupChannels.list
    # python: svc.projects().locations().backupChannels().get(**params).execute()
    calls:
      - action: projects.locations.backupChannels.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        destinationProject: '{{ response.destinationProject }}'
        destinationProjectId: '{{ response.destinationProjectId }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Retrieve the details of a single Backup.
  # ── gcp.gkebackup.projects.locations.backupPlans.backups.get [dependent] ──
  - discovery_id: gcp.gkebackup.projects.locations.backupPlans.backups.get
    for_each: gcp.gkebackup.projects.locations.backupPlans.backups.list
    # python: svc.projects().locations().backupPlans().backups().get(**params).execute()
    calls:
      - action: projects.locations.backupPlans.backups.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        allNamespaces: '{{ response.allNamespaces }}'
        clusterMetadata: '{{ response.clusterMetadata }}'
        completeTime: '{{ response.completeTime }}'
        configBackupSizeBytes: '{{ response.configBackupSizeBytes }}'
        containsSecrets: '{{ response.containsSecrets }}'
        containsVolumeData: '{{ response.containsVolumeData }}'
        createTime: '{{ response.createTime }}'
        deleteLockDays: '{{ response.deleteLockDays }}'
        deleteLockExpireTime: '{{ response.deleteLockExpireTime }}'
        description: '{{ response.description }}'
        encryptionKey: '{{ response.encryptionKey }}'
        labels: '{{ response.labels }}'
        manual: '{{ response.manual }}'
        name: '{{ response.name }}'
        permissiveMode: '{{ response.permissiveMode }}'
        podCount: '{{ response.podCount }}'
        resourceCount: '{{ response.resourceCount }}'
        retainDays: '{{ response.retainDays }}'
        retainExpireTime: '{{ response.retainExpireTime }}'
        satisfiesPzi: '{{ response.satisfiesPzi }}'
        satisfiesPzs: '{{ response.satisfiesPzs }}'
        selectedApplications: '{{ response.selectedApplications }}'
        selectedNamespaceLabels: '{{ response.selectedNamespaceLabels }}'
        selectedNamespaces: '{{ response.selectedNamespaces }}'
        sizeBytes: '{{ response.sizeBytes }}'
        state: '{{ response.state }}'
        stateReason: '{{ response.stateReason }}'
        troubleshootingInfo: '{{ response.troubleshootingInfo }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
        volumeCount: '{{ response.volumeCount }}'
    # required_params: ['name']
  # Retrieve the link to the backupIndex.
  # ── gcp.gkebackup.projects.locations.backupPlans.backups.getBackupIndexDownloadUrl [dependent] ──
  - discovery_id: gcp.gkebackup.projects.locations.backupPlans.backups.getBackupIndexDownloadUrl
    for_each: gcp.gkebackup.projects.locations.backupPlans.backups.list
    # python: svc.projects().locations().backupPlans().backups().getBackupIndexDownloadUrl(**params).execute()
    calls:
      - action: projects.locations.backupPlans.backups.getBackupIndexDownloadUrl
        params:
          backup: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        signedUrl: '{{ response.signedUrl }}'
    # required_params: ['backup']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.gkebackup.projects.locations.backupPlans.backups.getIamPolicy [dependent] ──
  - discovery_id: gcp.gkebackup.projects.locations.backupPlans.backups.getIamPolicy
    for_each: gcp.gkebackup.projects.locations.backupPlans.backups.list
    # python: svc.projects().locations().backupPlans().backups().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.backupPlans.backups.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Retrieve the details of a single VolumeBackup.
  # ── gcp.gkebackup.projects.locations.backupPlans.backups.volumeBackups.get [dependent] ──
  - discovery_id: gcp.gkebackup.projects.locations.backupPlans.backups.volumeBackups.get
    for_each: gcp.gkebackup.projects.locations.backupPlans.backups.volumeBackups.list
    # python: svc.projects().locations().backupPlans().backups().volumeBackups().get(**params).execute()
    calls:
      - action: projects.locations.backupPlans.backups.volumeBackups.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        completeTime: '{{ response.completeTime }}'
        createTime: '{{ response.createTime }}'
        diskSizeBytes: '{{ response.diskSizeBytes }}'
        format: '{{ response.format }}'
        name: '{{ response.name }}'
        satisfiesPzi: '{{ response.satisfiesPzi }}'
        satisfiesPzs: '{{ response.satisfiesPzs }}'
        sourcePvc: '{{ response.sourcePvc }}'
        state: '{{ response.state }}'
        stateMessage: '{{ response.stateMessage }}'
        storageBytes: '{{ response.storageBytes }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
        volumeBackupHandle: '{{ response.volumeBackupHandle }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.gkebackup.projects.locations.backupPlans.backups.volumeBackups.getIamPolicy [dependent] ──
  - discovery_id: gcp.gkebackup.projects.locations.backupPlans.backups.volumeBackups.getIamPolicy
    for_each: gcp.gkebackup.projects.locations.backupPlans.backups.volumeBackups.list
    # python: svc.projects().locations().backupPlans().backups().volumeBackups().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.backupPlans.backups.volumeBackups.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Retrieve the details of a single BackupPlan.
  # ── gcp.gkebackup.projects.locations.backupPlans.get [dependent] ──
  - discovery_id: gcp.gkebackup.projects.locations.backupPlans.get
    for_each: gcp.gkebackup.projects.locations.backupPlans.list
    # python: svc.projects().locations().backupPlans().get(**params).execute()
    calls:
      - action: projects.locations.backupPlans.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        backupChannel: '{{ response.backupChannel }}'
        backupConfig: '{{ response.backupConfig }}'
        backupSchedule: '{{ response.backupSchedule }}'
        cluster: '{{ response.cluster }}'
        createTime: '{{ response.createTime }}'
        deactivated: '{{ response.deactivated }}'
        description: '{{ response.description }}'
        labels: '{{ response.labels }}'
        lastSuccessfulBackupTime: '{{ response.lastSuccessfulBackupTime }}'
        name: '{{ response.name }}'
        protectedPodCount: '{{ response.protectedPodCount }}'
        retentionPolicy: '{{ response.retentionPolicy }}'
        rpoRiskLevel: '{{ response.rpoRiskLevel }}'
        rpoRiskReason: '{{ response.rpoRiskReason }}'
        state: '{{ response.state }}'
        stateReason: '{{ response.stateReason }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.gkebackup.projects.locations.backupPlans.getIamPolicy [dependent] ──
  - discovery_id: gcp.gkebackup.projects.locations.backupPlans.getIamPolicy
    for_each: gcp.gkebackup.projects.locations.backupPlans.list
    # python: svc.projects().locations().backupPlans().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.backupPlans.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Returns tags directly bound to a GCP resource.
  # ── gcp.gkebackup.projects.locations.backupPlans.getTags [dependent] ──
  - discovery_id: gcp.gkebackup.projects.locations.backupPlans.getTags
    for_each: gcp.gkebackup.projects.locations.backupPlans.list
    # python: svc.projects().locations().backupPlans().getTags(**params).execute()
    calls:
      - action: projects.locations.backupPlans.getTags
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        tags: '{{ response.tags }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.gkebackup.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.gkebackup.projects.locations.operations.get
    for_each: gcp.gkebackup.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Retrieve the details of a single RestoreChannel.
  # ── gcp.gkebackup.projects.locations.restoreChannels.get [dependent] ──
  - discovery_id: gcp.gkebackup.projects.locations.restoreChannels.get
    for_each: gcp.gkebackup.projects.locations.restoreChannels.list
    # python: svc.projects().locations().restoreChannels().get(**params).execute()
    calls:
      - action: projects.locations.restoreChannels.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        destinationProject: '{{ response.destinationProject }}'
        destinationProjectId: '{{ response.destinationProjectId }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Retrieve the details of a single RestorePlanBinding.
  # ── gcp.gkebackup.projects.locations.restoreChannels.restorePlanBindings.get [dependent] ──
  - discovery_id: gcp.gkebackup.projects.locations.restoreChannels.restorePlanBindings.get
    for_each: gcp.gkebackup.projects.locations.restoreChannels.restorePlanBindings.list
    # python: svc.projects().locations().restoreChannels().restorePlanBindings().get(**params).execute()
    calls:
      - action: projects.locations.restoreChannels.restorePlanBindings.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        backupPlan: '{{ response.backupPlan }}'
        createTime: '{{ response.createTime }}'
        name: '{{ response.name }}'
        restorePlan: '{{ response.restorePlan }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Retrieve the details of a single RestorePlan.
  # ── gcp.gkebackup.projects.locations.restorePlans.get [dependent] ──
  - discovery_id: gcp.gkebackup.projects.locations.restorePlans.get
    for_each: gcp.gkebackup.projects.locations.restorePlans.list
    # python: svc.projects().locations().restorePlans().get(**params).execute()
    calls:
      - action: projects.locations.restorePlans.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        backupPlan: '{{ response.backupPlan }}'
        cluster: '{{ response.cluster }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        restoreChannel: '{{ response.restoreChannel }}'
        restoreConfig: '{{ response.restoreConfig }}'
        state: '{{ response.state }}'
        stateReason: '{{ response.stateReason }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.gkebackup.projects.locations.restorePlans.getIamPolicy [dependent] ──
  - discovery_id: gcp.gkebackup.projects.locations.restorePlans.getIamPolicy
    for_each: gcp.gkebackup.projects.locations.restorePlans.list
    # python: svc.projects().locations().restorePlans().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.restorePlans.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Returns tags directly bound to a GCP resource.
  # ── gcp.gkebackup.projects.locations.restorePlans.getTags [dependent] ──
  - discovery_id: gcp.gkebackup.projects.locations.restorePlans.getTags
    for_each: gcp.gkebackup.projects.locations.restorePlans.list
    # python: svc.projects().locations().restorePlans().getTags(**params).execute()
    calls:
      - action: projects.locations.restorePlans.getTags
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        tags: '{{ response.tags }}'
    # required_params: ['name']
  # Retrieves the details of a single Restore.
  # ── gcp.gkebackup.projects.locations.restorePlans.restores.get [dependent] ──
  - discovery_id: gcp.gkebackup.projects.locations.restorePlans.restores.get
    for_each: gcp.gkebackup.projects.locations.restorePlans.restores.list
    # python: svc.projects().locations().restorePlans().restores().get(**params).execute()
    calls:
      - action: projects.locations.restorePlans.restores.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        backup: '{{ response.backup }}'
        cluster: '{{ response.cluster }}'
        completeTime: '{{ response.completeTime }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        filter: '{{ response.filter }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        resourcesExcludedCount: '{{ response.resourcesExcludedCount }}'
        resourcesFailedCount: '{{ response.resourcesFailedCount }}'
        resourcesRestoredCount: '{{ response.resourcesRestoredCount }}'
        restoreConfig: '{{ response.restoreConfig }}'
        state: '{{ response.state }}'
        stateReason: '{{ response.stateReason }}'
        troubleshootingInfo: '{{ response.troubleshootingInfo }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
        volumeDataRestorePolicyOverrides: '{{ response.volumeDataRestorePolicyOverrides }}'
        volumesRestoredCount: '{{ response.volumesRestoredCount }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.gkebackup.projects.locations.restorePlans.restores.getIamPolicy [dependent] ──
  - discovery_id: gcp.gkebackup.projects.locations.restorePlans.restores.getIamPolicy
    for_each: gcp.gkebackup.projects.locations.restorePlans.restores.list
    # python: svc.projects().locations().restorePlans().restores().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.restorePlans.restores.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Retrieve the details of a single VolumeRestore.
  # ── gcp.gkebackup.projects.locations.restorePlans.restores.volumeRestores.get [dependent] ──
  - discovery_id: gcp.gkebackup.projects.locations.restorePlans.restores.volumeRestores.get
    for_each: gcp.gkebackup.projects.locations.restorePlans.restores.volumeRestores.list
    # python: svc.projects().locations().restorePlans().restores().volumeRestores().get(**params).execute()
    calls:
      - action: projects.locations.restorePlans.restores.volumeRestores.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        completeTime: '{{ response.completeTime }}'
        createTime: '{{ response.createTime }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
        stateMessage: '{{ response.stateMessage }}'
        targetPvc: '{{ response.targetPvc }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
        volumeBackup: '{{ response.volumeBackup }}'
        volumeHandle: '{{ response.volumeHandle }}'
        volumeType: '{{ response.volumeType }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.gkebackup.projects.locations.restorePlans.restores.volumeRestores.getIamPolicy [dependent] ──
  - discovery_id: gcp.gkebackup.projects.locations.restorePlans.restores.volumeRestores.getIamPolicy
    for_each: gcp.gkebackup.projects.locations.restorePlans.restores.volumeRestores.list
    # python: svc.projects().locations().restorePlans().restores().volumeRestores().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.restorePlans.restores.volumeRestores.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
