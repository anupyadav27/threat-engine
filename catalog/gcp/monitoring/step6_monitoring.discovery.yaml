# ============================================================
# Discovery YAML — monitoring (v3)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 28 | independent: 16 | dependent: 12
# ops with fields: 28/28 | dep ops with chain: 12/12
# ============================================================
version: '1.0'
provider: gcp
service: monitoring

services:
  client: monitoring
  module: "googleapiclient.discovery.build('monitoring', 'v3')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists time series that match a filter.
  # ── gcp.monitoring.folders.timeSeries.list ──
  - discovery_id: gcp.monitoring.folders.timeSeries.list
    # python: svc.folders().timeSeries().list(**params).execute()
    calls:
      - action: folders.timeSeries.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.timeSeries }}'
      item:
        description: '{{ item.description }}'
        metadata: '{{ item.metadata }}'
        metric: '{{ item.metric }}'
        metricKind: '{{ item.metricKind }}'
        points: '{{ item.points }}'
        resource: '{{ item.resource }}'
        valueType: '{{ item.valueType }}'
  # Lists time series that match a filter.
  # ── gcp.monitoring.organizations.timeSeries.list ──
  - discovery_id: gcp.monitoring.organizations.timeSeries.list
    # python: svc.organizations().timeSeries().list(**params).execute()
    calls:
      - action: organizations.timeSeries.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.timeSeries }}'
      item:
        description: '{{ item.description }}'
        metadata: '{{ item.metadata }}'
        metric: '{{ item.metric }}'
        metricKind: '{{ item.metricKind }}'
        points: '{{ item.points }}'
        resource: '{{ item.resource }}'
        valueType: '{{ item.valueType }}'
  # Lists the existing alerting policies for the workspace.
  # ── gcp.monitoring.projects.alertPolicies.list ──
  - discovery_id: gcp.monitoring.projects.alertPolicies.list
    # python: svc.projects().alertPolicies().list(**params).execute()
    calls:
      - action: projects.alertPolicies.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.alertPolicies }}'
      item:
        alertStrategy: '{{ item.alertStrategy }}'
        combiner: '{{ item.combiner }}'
        conditions: '{{ item.conditions }}'
        creationRecord: '{{ item.creationRecord }}'
        displayName: '{{ item.displayName }}'
        documentation: '{{ item.documentation }}'
        enabled: '{{ item.enabled }}'
        mutationRecord: '{{ item.mutationRecord }}'
        name: '{{ item.name }}'
        notificationChannels: '{{ item.notificationChannels }}'
        severity: '{{ item.severity }}'
        userLabels: '{{ item.userLabels }}'
        validity: '{{ item.validity }}'
  # Lists the existing alerts for the metrics scope of the project.
  # ── gcp.monitoring.projects.alerts.list ──
  - discovery_id: gcp.monitoring.projects.alerts.list
    # python: svc.projects().alerts().list(**params).execute()
    calls:
      - action: projects.alerts.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.alerts }}'
      item:
        closeTime: '{{ item.closeTime }}'
        log: '{{ item.log }}'
        metadata: '{{ item.metadata }}'
        metric: '{{ item.metric }}'
        name: '{{ item.name }}'
        openTime: '{{ item.openTime }}'
        policy: '{{ item.policy }}'
        resource: '{{ item.resource }}'
        state: '{{ item.state }}'
  # Lists the existing groups.
  # ── gcp.monitoring.projects.groups.list ──
  - discovery_id: gcp.monitoring.projects.groups.list
    # python: svc.projects().groups().list(**params).execute()
    calls:
      - action: projects.groups.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.groups }}'
      item:
        displayName: '{{ item.displayName }}'
        filter: '{{ item.filter }}'
        isCluster: '{{ item.isCluster }}'
        name: '{{ item.name }}'
        parentName: '{{ item.parentName }}'
  # Lists metric descriptors that match a filter.
  # ── gcp.monitoring.projects.metricDescriptors.list ──
  - discovery_id: gcp.monitoring.projects.metricDescriptors.list
    # python: svc.projects().metricDescriptors().list(**params).execute()
    calls:
      - action: projects.metricDescriptors.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.metricDescriptors }}'
      item:
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        launchStage: '{{ item.launchStage }}'
        metadata: '{{ item.metadata }}'
        metricKind: '{{ item.metricKind }}'
        monitoredResourceTypes: '{{ item.monitoredResourceTypes }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
        unit: '{{ item.unit }}'
        valueType: '{{ item.valueType }}'
  # Lists monitored resource descriptors that match a filter.
  # ── gcp.monitoring.projects.monitoredResourceDescriptors.list ──
  - discovery_id: gcp.monitoring.projects.monitoredResourceDescriptors.list
    # python: svc.projects().monitoredResourceDescriptors().list(**params).execute()
    calls:
      - action: projects.monitoredResourceDescriptors.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.monitoredResourceDescriptors }}'
      item:
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        launchStage: '{{ item.launchStage }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
  # Lists the descriptors for supported channel types. The use of descriptors makes it possible for new 
  # ── gcp.monitoring.projects.notificationChannelDescriptors.list ──
  - discovery_id: gcp.monitoring.projects.notificationChannelDescriptors.list
    # python: svc.projects().notificationChannelDescriptors().list(**params).execute()
    calls:
      - action: projects.notificationChannelDescriptors.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.notificationChannelDescriptors }}'
      item:
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        launchStage: '{{ item.launchStage }}'
        name: '{{ item.name }}'
        supportedTiers: '{{ item.supportedTiers }}'
        type: '{{ item.type }}'
  # Lists the notification channels that have been created for the project. To list the types of notific
  # ── gcp.monitoring.projects.notificationChannels.list ──
  - discovery_id: gcp.monitoring.projects.notificationChannels.list
    # python: svc.projects().notificationChannels().list(**params).execute()
    calls:
      - action: projects.notificationChannels.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.notificationChannels }}'
      item:
        creationRecord: '{{ item.creationRecord }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        enabled: '{{ item.enabled }}'
        labels: '{{ item.labels }}'
        mutationRecords: '{{ item.mutationRecords }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
        userLabels: '{{ item.userLabels }}'
        verificationStatus: '{{ item.verificationStatus }}'
  # Lists the Snoozes associated with a project. Can optionally pass in filter, which specifies predicat
  # ── gcp.monitoring.projects.snoozes.list ──
  - discovery_id: gcp.monitoring.projects.snoozes.list
    # python: svc.projects().snoozes().list(**params).execute()
    calls:
      - action: projects.snoozes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.snoozes }}'
      item:
        criteria: '{{ item.criteria }}'
        displayName: '{{ item.displayName }}'
        interval: '{{ item.interval }}'
        name: '{{ item.name }}'
  # Lists time series that match a filter.
  # ── gcp.monitoring.projects.timeSeries.list ──
  - discovery_id: gcp.monitoring.projects.timeSeries.list
    # python: svc.projects().timeSeries().list(**params).execute()
    calls:
      - action: projects.timeSeries.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.timeSeries }}'
      item:
        description: '{{ item.description }}'
        metadata: '{{ item.metadata }}'
        metric: '{{ item.metric }}'
        metricKind: '{{ item.metricKind }}'
        points: '{{ item.points }}'
        resource: '{{ item.resource }}'
        valueType: '{{ item.valueType }}'
  # Queries time series by using Monitoring Query Language (MQL). We recommend using PromQL instead of M
  # ── gcp.monitoring.projects.timeSeries.query ──
  - discovery_id: gcp.monitoring.projects.timeSeries.query
    # python: svc.projects().timeSeries().query(**params).execute()
    calls:
      - action: projects.timeSeries.query
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.timeSeries }}'
      item:
        labelValues: '{{ item.labelValues }}'
        pointData: '{{ item.pointData }}'
  # Lists the existing valid Uptime check configurations for the project (leaving out any invalid config
  # ── gcp.monitoring.projects.uptimeCheckConfigs.list ──
  - discovery_id: gcp.monitoring.projects.uptimeCheckConfigs.list
    # python: svc.projects().uptimeCheckConfigs().list(**params).execute()
    calls:
      - action: projects.uptimeCheckConfigs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.uptimeCheckConfigs }}'
      item:
        checkerType: '{{ item.checkerType }}'
        contentMatchers: '{{ item.contentMatchers }}'
        disabled: '{{ item.disabled }}'
        displayName: '{{ item.displayName }}'
        httpCheck: '{{ item.httpCheck }}'
        internalCheckers: '{{ item.internalCheckers }}'
        isInternal: '{{ item.isInternal }}'
        logCheckFailures: '{{ item.logCheckFailures }}'
        monitoredResource: '{{ item.monitoredResource }}'
        name: '{{ item.name }}'
        period: '{{ item.period }}'
        resourceGroup: '{{ item.resourceGroup }}'
        selectedRegions: '{{ item.selectedRegions }}'
        syntheticMonitor: '{{ item.syntheticMonitor }}'
        tcpCheck: '{{ item.tcpCheck }}'
        timeout: '{{ item.timeout }}'
        userLabels: '{{ item.userLabels }}'
  # List Services for this Metrics Scope.
  # ── gcp.monitoring.services.list ──
  - discovery_id: gcp.monitoring.services.list
    # python: svc.services().list(**params).execute()
    calls:
      - action: services.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.services }}'
      item:
        appEngine: '{{ item.appEngine }}'
        basicService: '{{ item.basicService }}'
        cloudEndpoints: '{{ item.cloudEndpoints }}'
        cloudRun: '{{ item.cloudRun }}'
        clusterIstio: '{{ item.clusterIstio }}'
        custom: '{{ item.custom }}'
        displayName: '{{ item.displayName }}'
        gkeNamespace: '{{ item.gkeNamespace }}'
        gkeService: '{{ item.gkeService }}'
        gkeWorkload: '{{ item.gkeWorkload }}'
        istioCanonicalService: '{{ item.istioCanonicalService }}'
        meshIstio: '{{ item.meshIstio }}'
        name: '{{ item.name }}'
        telemetry: '{{ item.telemetry }}'
        userLabels: '{{ item.userLabels }}'
  # List the ServiceLevelObjectives for the given Service.
  # ── gcp.monitoring.services.serviceLevelObjectives.list ──
  - discovery_id: gcp.monitoring.services.serviceLevelObjectives.list
    # python: svc.services().serviceLevelObjectives().list(**params).execute()
    calls:
      - action: services.serviceLevelObjectives.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.serviceLevelObjectives }}'
      item:
        calendarPeriod: '{{ item.calendarPeriod }}'
        displayName: '{{ item.displayName }}'
        goal: '{{ item.goal }}'
        name: '{{ item.name }}'
        rollingPeriod: '{{ item.rollingPeriod }}'
        serviceLevelIndicator: '{{ item.serviceLevelIndicator }}'
        userLabels: '{{ item.userLabels }}'
  # Returns the list of IP addresses that checkers run from.
  # ── gcp.monitoring.uptimeCheckIps.list ──
  - discovery_id: gcp.monitoring.uptimeCheckIps.list
    # python: svc.uptimeCheckIps().list(**params).execute()
    calls:
      - action: uptimeCheckIps.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.uptimeCheckIps }}'
      item:
        ipAddress: '{{ item.ipAddress }}'
        location: '{{ item.location }}'
        region: '{{ item.region }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets a single alerting policy.
  # ── gcp.monitoring.projects.alertPolicies.get [dependent] ──
  - discovery_id: gcp.monitoring.projects.alertPolicies.get
    for_each: gcp.monitoring.projects.alertPolicies.list
    # python: svc.projects().alertPolicies().get(**params).execute()
    calls:
      - action: projects.alertPolicies.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        alertStrategy: '{{ response.alertStrategy }}'
        combiner: '{{ response.combiner }}'
        conditions: '{{ response.conditions }}'
        creationRecord: '{{ response.creationRecord }}'
        displayName: '{{ response.displayName }}'
        documentation: '{{ response.documentation }}'
        enabled: '{{ response.enabled }}'
        mutationRecord: '{{ response.mutationRecord }}'
        name: '{{ response.name }}'
        notificationChannels: '{{ response.notificationChannels }}'
        severity: '{{ response.severity }}'
        userLabels: '{{ response.userLabels }}'
        validity: '{{ response.validity }}'
    # required_params: ['name']
  # Gets a single alert.
  # ── gcp.monitoring.projects.alerts.get [dependent] ──
  - discovery_id: gcp.monitoring.projects.alerts.get
    for_each: gcp.monitoring.projects.alerts.list
    # python: svc.projects().alerts().get(**params).execute()
    calls:
      - action: projects.alerts.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        closeTime: '{{ response.closeTime }}'
        log: '{{ response.log }}'
        metadata: '{{ response.metadata }}'
        metric: '{{ response.metric }}'
        name: '{{ response.name }}'
        openTime: '{{ response.openTime }}'
        policy: '{{ response.policy }}'
        resource: '{{ response.resource }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Gets a single group.
  # ── gcp.monitoring.projects.groups.get [dependent] ──
  - discovery_id: gcp.monitoring.projects.groups.get
    for_each: gcp.monitoring.projects.groups.list
    # python: svc.projects().groups().get(**params).execute()
    calls:
      - action: projects.groups.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        filter: '{{ response.filter }}'
        isCluster: '{{ response.isCluster }}'
        name: '{{ response.name }}'
        parentName: '{{ response.parentName }}'
    # required_params: ['name']
  # Lists the monitored resources that are members of a group.
  # ── gcp.monitoring.projects.groups.members.list [dependent] ──
  - discovery_id: gcp.monitoring.projects.groups.members.list
    for_each: gcp.monitoring.projects.groups.list
    # python: svc.projects().groups().members().list(**params).execute()
    calls:
      - action: projects.groups.members.list
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.members }}'
      item:
        labels: '{{ item.labels }}'
        type: '{{ item.type }}'
    # required_params: ['name']
  # Gets a single metric descriptor.
  # ── gcp.monitoring.projects.metricDescriptors.get [dependent] ──
  - discovery_id: gcp.monitoring.projects.metricDescriptors.get
    for_each: gcp.monitoring.projects.metricDescriptors.list
    # python: svc.projects().metricDescriptors().get(**params).execute()
    calls:
      - action: projects.metricDescriptors.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        launchStage: '{{ response.launchStage }}'
        metadata: '{{ response.metadata }}'
        metricKind: '{{ response.metricKind }}'
        monitoredResourceTypes: '{{ response.monitoredResourceTypes }}'
        name: '{{ response.name }}'
        type: '{{ response.type }}'
        unit: '{{ response.unit }}'
        valueType: '{{ response.valueType }}'
    # required_params: ['name']
  # Gets a single monitored resource descriptor.
  # ── gcp.monitoring.projects.monitoredResourceDescriptors.get [dependent] ──
  - discovery_id: gcp.monitoring.projects.monitoredResourceDescriptors.get
    for_each: gcp.monitoring.projects.monitoredResourceDescriptors.list
    # python: svc.projects().monitoredResourceDescriptors().get(**params).execute()
    calls:
      - action: projects.monitoredResourceDescriptors.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        launchStage: '{{ response.launchStage }}'
        name: '{{ response.name }}'
        type: '{{ response.type }}'
    # required_params: ['name']
  # Gets a single channel descriptor. The descriptor indicates which fields are expected / permitted for
  # ── gcp.monitoring.projects.notificationChannelDescriptors.get [dependent] ──
  - discovery_id: gcp.monitoring.projects.notificationChannelDescriptors.get
    for_each: gcp.monitoring.projects.notificationChannelDescriptors.list
    # python: svc.projects().notificationChannelDescriptors().get(**params).execute()
    calls:
      - action: projects.notificationChannelDescriptors.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        launchStage: '{{ response.launchStage }}'
        name: '{{ response.name }}'
        supportedTiers: '{{ response.supportedTiers }}'
        type: '{{ response.type }}'
    # required_params: ['name']
  # Gets a single notification channel. The channel includes the relevant configuration details with whi
  # ── gcp.monitoring.projects.notificationChannels.get [dependent] ──
  - discovery_id: gcp.monitoring.projects.notificationChannels.get
    for_each: gcp.monitoring.projects.notificationChannels.list
    # python: svc.projects().notificationChannels().get(**params).execute()
    calls:
      - action: projects.notificationChannels.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        creationRecord: '{{ response.creationRecord }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        enabled: '{{ response.enabled }}'
        labels: '{{ response.labels }}'
        mutationRecords: '{{ response.mutationRecords }}'
        name: '{{ response.name }}'
        type: '{{ response.type }}'
        userLabels: '{{ response.userLabels }}'
        verificationStatus: '{{ response.verificationStatus }}'
    # required_params: ['name']
  # Retrieves a Snooze by name.
  # ── gcp.monitoring.projects.snoozes.get [dependent] ──
  - discovery_id: gcp.monitoring.projects.snoozes.get
    for_each: gcp.monitoring.projects.snoozes.list
    # python: svc.projects().snoozes().get(**params).execute()
    calls:
      - action: projects.snoozes.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        criteria: '{{ response.criteria }}'
        displayName: '{{ response.displayName }}'
        interval: '{{ response.interval }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Gets a single Uptime check configuration.
  # ── gcp.monitoring.projects.uptimeCheckConfigs.get [dependent] ──
  - discovery_id: gcp.monitoring.projects.uptimeCheckConfigs.get
    for_each: gcp.monitoring.projects.uptimeCheckConfigs.list
    # python: svc.projects().uptimeCheckConfigs().get(**params).execute()
    calls:
      - action: projects.uptimeCheckConfigs.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        checkerType: '{{ response.checkerType }}'
        contentMatchers: '{{ response.contentMatchers }}'
        disabled: '{{ response.disabled }}'
        displayName: '{{ response.displayName }}'
        httpCheck: '{{ response.httpCheck }}'
        internalCheckers: '{{ response.internalCheckers }}'
        isInternal: '{{ response.isInternal }}'
        logCheckFailures: '{{ response.logCheckFailures }}'
        monitoredResource: '{{ response.monitoredResource }}'
        name: '{{ response.name }}'
        period: '{{ response.period }}'
        resourceGroup: '{{ response.resourceGroup }}'
        selectedRegions: '{{ response.selectedRegions }}'
        syntheticMonitor: '{{ response.syntheticMonitor }}'
        tcpCheck: '{{ response.tcpCheck }}'
        timeout: '{{ response.timeout }}'
        userLabels: '{{ response.userLabels }}'
    # required_params: ['name']
  # Get the named Service.
  # ── gcp.monitoring.services.get [dependent] ──
  - discovery_id: gcp.monitoring.services.get
    for_each: gcp.monitoring.services.list
    # python: svc.services().get(**params).execute()
    calls:
      - action: services.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        appEngine: '{{ response.appEngine }}'
        basicService: '{{ response.basicService }}'
        cloudEndpoints: '{{ response.cloudEndpoints }}'
        cloudRun: '{{ response.cloudRun }}'
        clusterIstio: '{{ response.clusterIstio }}'
        custom: '{{ response.custom }}'
        displayName: '{{ response.displayName }}'
        gkeNamespace: '{{ response.gkeNamespace }}'
        gkeService: '{{ response.gkeService }}'
        gkeWorkload: '{{ response.gkeWorkload }}'
        istioCanonicalService: '{{ response.istioCanonicalService }}'
        meshIstio: '{{ response.meshIstio }}'
        name: '{{ response.name }}'
        telemetry: '{{ response.telemetry }}'
        userLabels: '{{ response.userLabels }}'
    # required_params: ['name']
  # Get a ServiceLevelObjective by name.
  # ── gcp.monitoring.services.serviceLevelObjectives.get [dependent] ──
  - discovery_id: gcp.monitoring.services.serviceLevelObjectives.get
    for_each: gcp.monitoring.services.serviceLevelObjectives.list
    # python: svc.services().serviceLevelObjectives().get(**params).execute()
    calls:
      - action: services.serviceLevelObjectives.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        calendarPeriod: '{{ response.calendarPeriod }}'
        displayName: '{{ response.displayName }}'
        goal: '{{ response.goal }}'
        name: '{{ response.name }}'
        rollingPeriod: '{{ response.rollingPeriod }}'
        serviceLevelIndicator: '{{ response.serviceLevelIndicator }}'
        userLabels: '{{ response.userLabels }}'
    # required_params: ['name']
