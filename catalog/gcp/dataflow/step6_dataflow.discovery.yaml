# ============================================================
# Discovery YAML — dataflow (v1b3)
# Generated: 2026-02-21T03:06:39Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 20 | independent: 7 | dependent: 13
# ops with fields: 20/20 | dep ops with chain: 13/13
# ============================================================
version: '1.0'
provider: gcp
service: dataflow

services:
  client: dataflow
  module: "googleapiclient.discovery.build('dataflow', 'v1b3')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # List the jobs of a project across all regions. **Note:** This method doesn't support filtering the l
  # ── gcp.dataflow.projects.jobs.aggregated ──
  - discovery_id: gcp.dataflow.projects.jobs.aggregated
    # python: svc.projects().jobs().aggregated(**params).execute()
    calls:
      - action: projects.jobs.aggregated
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        failedLocation: '{{ response.failedLocation }}'
        jobs: '{{ response.jobs }}'
  # List the jobs of a project. To list the jobs of a project in a region, we recommend using `projects.
  # ── gcp.dataflow.projects.jobs.list ──
  - discovery_id: gcp.dataflow.projects.jobs.list
    # python: svc.projects().jobs().list(**params).execute()
    calls:
      - action: projects.jobs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.jobs }}'
      item:
        clientRequestId: '{{ item.clientRequestId }}'
        createTime: '{{ item.createTime }}'
        createdFromSnapshotId: '{{ item.createdFromSnapshotId }}'
        currentState: '{{ item.currentState }}'
        currentStateTime: '{{ item.currentStateTime }}'
        environment: '{{ item.environment }}'
        executionInfo: '{{ item.executionInfo }}'
        id: '{{ item.id }}'
        jobMetadata: '{{ item.jobMetadata }}'
        labels: '{{ item.labels }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        pausable: '{{ item.pausable }}'
        pipelineDescription: '{{ item.pipelineDescription }}'
        projectId: '{{ item.projectId }}'
        replaceJobId: '{{ item.replaceJobId }}'
        replacedByJobId: '{{ item.replacedByJobId }}'
        requestedState: '{{ item.requestedState }}'
        runtimeUpdatableParams: '{{ item.runtimeUpdatableParams }}'
        satisfiesPzi: '{{ item.satisfiesPzi }}'
        satisfiesPzs: '{{ item.satisfiesPzs }}'
        serviceResources: '{{ item.serviceResources }}'
        stageStates: '{{ item.stageStates }}'
        startTime: '{{ item.startTime }}'
        steps: '{{ item.steps }}'
        stepsLocation: '{{ item.stepsLocation }}'
        tempFiles: '{{ item.tempFiles }}'
        transformNameMapping: '{{ item.transformNameMapping }}'
        type: '{{ item.type }}'
  # List the jobs of a project. To list the jobs of a project in a region, we recommend using `projects.
  # ── gcp.dataflow.projects.locations.jobs.list ──
  - discovery_id: gcp.dataflow.projects.locations.jobs.list
    # python: svc.projects().locations().jobs().list(**params).execute()
    calls:
      - action: projects.locations.jobs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.jobs }}'
      item:
        clientRequestId: '{{ item.clientRequestId }}'
        createTime: '{{ item.createTime }}'
        createdFromSnapshotId: '{{ item.createdFromSnapshotId }}'
        currentState: '{{ item.currentState }}'
        currentStateTime: '{{ item.currentStateTime }}'
        environment: '{{ item.environment }}'
        executionInfo: '{{ item.executionInfo }}'
        id: '{{ item.id }}'
        jobMetadata: '{{ item.jobMetadata }}'
        labels: '{{ item.labels }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        pausable: '{{ item.pausable }}'
        pipelineDescription: '{{ item.pipelineDescription }}'
        projectId: '{{ item.projectId }}'
        replaceJobId: '{{ item.replaceJobId }}'
        replacedByJobId: '{{ item.replacedByJobId }}'
        requestedState: '{{ item.requestedState }}'
        runtimeUpdatableParams: '{{ item.runtimeUpdatableParams }}'
        satisfiesPzi: '{{ item.satisfiesPzi }}'
        satisfiesPzs: '{{ item.satisfiesPzs }}'
        serviceResources: '{{ item.serviceResources }}'
        stageStates: '{{ item.stageStates }}'
        startTime: '{{ item.startTime }}'
        steps: '{{ item.steps }}'
        stepsLocation: '{{ item.stepsLocation }}'
        tempFiles: '{{ item.tempFiles }}'
        transformNameMapping: '{{ item.transformNameMapping }}'
        type: '{{ item.type }}'
  # Lists snapshots.
  # ── gcp.dataflow.projects.locations.snapshots.list ──
  - discovery_id: gcp.dataflow.projects.locations.snapshots.list
    # python: svc.projects().locations().snapshots().list(**params).execute()
    calls:
      - action: projects.locations.snapshots.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.snapshots }}'
      item:
        creationTime: '{{ item.creationTime }}'
        description: '{{ item.description }}'
        diskSizeBytes: '{{ item.diskSizeBytes }}'
        id: '{{ item.id }}'
        projectId: '{{ item.projectId }}'
        pubsubMetadata: '{{ item.pubsubMetadata }}'
        region: '{{ item.region }}'
        sourceJobId: '{{ item.sourceJobId }}'
        state: '{{ item.state }}'
        ttl: '{{ item.ttl }}'
  # Get the template associated with a template. To get the template, we recommend using `projects.locat
  # ── gcp.dataflow.projects.locations.templates.get ──
  - discovery_id: gcp.dataflow.projects.locations.templates.get
    # python: svc.projects().locations().templates().get(**params).execute()
    calls:
      - action: projects.locations.templates.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        metadata: '{{ response.metadata }}'
        runtimeMetadata: '{{ response.runtimeMetadata }}'
        status: '{{ response.status }}'
        templateType: '{{ response.templateType }}'
  # Lists snapshots.
  # ── gcp.dataflow.projects.snapshots.list ──
  - discovery_id: gcp.dataflow.projects.snapshots.list
    # python: svc.projects().snapshots().list(**params).execute()
    calls:
      - action: projects.snapshots.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.snapshots }}'
      item:
        creationTime: '{{ item.creationTime }}'
        description: '{{ item.description }}'
        diskSizeBytes: '{{ item.diskSizeBytes }}'
        id: '{{ item.id }}'
        projectId: '{{ item.projectId }}'
        pubsubMetadata: '{{ item.pubsubMetadata }}'
        region: '{{ item.region }}'
        sourceJobId: '{{ item.sourceJobId }}'
        state: '{{ item.state }}'
        ttl: '{{ item.ttl }}'
  # Get the template associated with a template. To get the template, we recommend using `projects.locat
  # ── gcp.dataflow.projects.templates.get ──
  - discovery_id: gcp.dataflow.projects.templates.get
    # python: svc.projects().templates().get(**params).execute()
    calls:
      - action: projects.templates.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        metadata: '{{ response.metadata }}'
        runtimeMetadata: '{{ response.runtimeMetadata }}'
        status: '{{ response.status }}'
        templateType: '{{ response.templateType }}'

  # ════ DEPENDENT (enrich) operations ════
  # Get encoded debug configuration for component. Not cacheable.
  # ── gcp.dataflow.projects.jobs.debug.getConfig [dependent] ──
  - discovery_id: gcp.dataflow.projects.jobs.debug.getConfig
    for_each: gcp.dataflow.projects.locations.jobs.list
    # python: svc.projects().jobs().debug().getConfig(**params).execute()
    calls:
      - action: projects.jobs.debug.getConfig
        params:
          jobId: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        config: '{{ response.config }}'
    # required_params: ['projectId', 'jobId']
  # Gets the state of the specified Cloud Dataflow job. To get the state of a job, we recommend using `p
  # ── gcp.dataflow.projects.jobs.get [dependent] ──
  - discovery_id: gcp.dataflow.projects.jobs.get
    for_each: gcp.dataflow.projects.locations.jobs.list
    # python: svc.projects().jobs().get(**params).execute()
    calls:
      - action: projects.jobs.get
        params:
          jobId: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        clientRequestId: '{{ response.clientRequestId }}'
        createTime: '{{ response.createTime }}'
        createdFromSnapshotId: '{{ response.createdFromSnapshotId }}'
        currentState: '{{ response.currentState }}'
        currentStateTime: '{{ response.currentStateTime }}'
        environment: '{{ response.environment }}'
        executionInfo: '{{ response.executionInfo }}'
        id: '{{ response.id }}'
        jobMetadata: '{{ response.jobMetadata }}'
        labels: '{{ response.labels }}'
        location: '{{ response.location }}'
        name: '{{ response.name }}'
        pausable: '{{ response.pausable }}'
        pipelineDescription: '{{ response.pipelineDescription }}'
        projectId: '{{ response.projectId }}'
        replaceJobId: '{{ response.replaceJobId }}'
        replacedByJobId: '{{ response.replacedByJobId }}'
        requestedState: '{{ response.requestedState }}'
        runtimeUpdatableParams: '{{ response.runtimeUpdatableParams }}'
        satisfiesPzi: '{{ response.satisfiesPzi }}'
        satisfiesPzs: '{{ response.satisfiesPzs }}'
        serviceResources: '{{ response.serviceResources }}'
        stageStates: '{{ response.stageStates }}'
        startTime: '{{ response.startTime }}'
        steps: '{{ response.steps }}'
        stepsLocation: '{{ response.stepsLocation }}'
        tempFiles: '{{ response.tempFiles }}'
        transformNameMapping: '{{ response.transformNameMapping }}'
        type: '{{ response.type }}'
    # required_params: ['jobId', 'projectId']
  # Request the job status. To request the status of a job, we recommend using `projects.locations.jobs.
  # ── gcp.dataflow.projects.jobs.getMetrics [dependent] ──
  - discovery_id: gcp.dataflow.projects.jobs.getMetrics
    for_each: gcp.dataflow.projects.locations.jobs.list
    # python: svc.projects().jobs().getMetrics(**params).execute()
    calls:
      - action: projects.jobs.getMetrics
        params:
          jobId: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        metricTime: '{{ response.metricTime }}'
        metrics: '{{ response.metrics }}'
    # required_params: ['jobId', 'projectId']
  # Request the job status. To request the status of a job, we recommend using `projects.locations.jobs.
  # ── gcp.dataflow.projects.jobs.messages.list [dependent] ──
  - discovery_id: gcp.dataflow.projects.jobs.messages.list
    for_each: gcp.dataflow.projects.locations.jobs.list
    # python: svc.projects().jobs().messages().list(**params).execute()
    calls:
      - action: projects.jobs.messages.list
        params:
          jobId: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.messages }}'
      item:
        currentNumWorkers: '{{ item.currentNumWorkers }}'
        description: '{{ item.description }}'
        eventType: '{{ item.eventType }}'
        targetNumWorkers: '{{ item.targetNumWorkers }}'
        time: '{{ item.time }}'
        workerPool: '{{ item.workerPool }}'
    # required_params: ['jobId', 'projectId']
  # Get encoded debug configuration for component. Not cacheable.
  # ── gcp.dataflow.projects.locations.jobs.debug.getConfig [dependent] ──
  - discovery_id: gcp.dataflow.projects.locations.jobs.debug.getConfig
    for_each: gcp.dataflow.projects.locations.jobs.list
    # python: svc.projects().locations().jobs().debug().getConfig(**params).execute()
    calls:
      - action: projects.locations.jobs.debug.getConfig
        params:
          jobId: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        config: '{{ response.config }}'
    # required_params: ['projectId', 'location', 'jobId']
  # Gets the state of the specified Cloud Dataflow job. To get the state of a job, we recommend using `p
  # ── gcp.dataflow.projects.locations.jobs.get [dependent] ──
  - discovery_id: gcp.dataflow.projects.locations.jobs.get
    for_each: gcp.dataflow.projects.locations.jobs.list
    # python: svc.projects().locations().jobs().get(**params).execute()
    calls:
      - action: projects.locations.jobs.get
        params:
          jobId: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        clientRequestId: '{{ response.clientRequestId }}'
        createTime: '{{ response.createTime }}'
        createdFromSnapshotId: '{{ response.createdFromSnapshotId }}'
        currentState: '{{ response.currentState }}'
        currentStateTime: '{{ response.currentStateTime }}'
        environment: '{{ response.environment }}'
        executionInfo: '{{ response.executionInfo }}'
        id: '{{ response.id }}'
        jobMetadata: '{{ response.jobMetadata }}'
        labels: '{{ response.labels }}'
        location: '{{ response.location }}'
        name: '{{ response.name }}'
        pausable: '{{ response.pausable }}'
        pipelineDescription: '{{ response.pipelineDescription }}'
        projectId: '{{ response.projectId }}'
        replaceJobId: '{{ response.replaceJobId }}'
        replacedByJobId: '{{ response.replacedByJobId }}'
        requestedState: '{{ response.requestedState }}'
        runtimeUpdatableParams: '{{ response.runtimeUpdatableParams }}'
        satisfiesPzi: '{{ response.satisfiesPzi }}'
        satisfiesPzs: '{{ response.satisfiesPzs }}'
        serviceResources: '{{ response.serviceResources }}'
        stageStates: '{{ response.stageStates }}'
        startTime: '{{ response.startTime }}'
        steps: '{{ response.steps }}'
        stepsLocation: '{{ response.stepsLocation }}'
        tempFiles: '{{ response.tempFiles }}'
        transformNameMapping: '{{ response.transformNameMapping }}'
        type: '{{ response.type }}'
    # required_params: ['projectId', 'jobId', 'location']
  # Request detailed information about the execution status of the job. EXPERIMENTAL. This API is subjec
  # ── gcp.dataflow.projects.locations.jobs.getExecutionDetails [dependent] ──
  - discovery_id: gcp.dataflow.projects.locations.jobs.getExecutionDetails
    for_each: gcp.dataflow.projects.locations.jobs.list
    # python: svc.projects().locations().jobs().getExecutionDetails(**params).execute()
    calls:
      - action: projects.locations.jobs.getExecutionDetails
        params:
          jobId: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        stages: '{{ response.stages }}'
    # required_params: ['location', 'projectId', 'jobId']
  # Request the job status. To request the status of a job, we recommend using `projects.locations.jobs.
  # ── gcp.dataflow.projects.locations.jobs.getMetrics [dependent] ──
  - discovery_id: gcp.dataflow.projects.locations.jobs.getMetrics
    for_each: gcp.dataflow.projects.locations.jobs.list
    # python: svc.projects().locations().jobs().getMetrics(**params).execute()
    calls:
      - action: projects.locations.jobs.getMetrics
        params:
          jobId: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        metricTime: '{{ response.metricTime }}'
        metrics: '{{ response.metrics }}'
    # required_params: ['jobId', 'projectId', 'location']
  # Request the job status. To request the status of a job, we recommend using `projects.locations.jobs.
  # ── gcp.dataflow.projects.locations.jobs.messages.list [dependent] ──
  - discovery_id: gcp.dataflow.projects.locations.jobs.messages.list
    for_each: gcp.dataflow.projects.locations.jobs.list
    # python: svc.projects().locations().jobs().messages().list(**params).execute()
    calls:
      - action: projects.locations.jobs.messages.list
        params:
          jobId: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.messages }}'
      item:
        currentNumWorkers: '{{ item.currentNumWorkers }}'
        description: '{{ item.description }}'
        eventType: '{{ item.eventType }}'
        targetNumWorkers: '{{ item.targetNumWorkers }}'
        time: '{{ item.time }}'
        workerPool: '{{ item.workerPool }}'
    # required_params: ['location', 'projectId', 'jobId']
  # Lists snapshots.
  # ── gcp.dataflow.projects.locations.jobs.snapshots.list [dependent] ──
  - discovery_id: gcp.dataflow.projects.locations.jobs.snapshots.list
    for_each: gcp.dataflow.projects.locations.jobs.list
    # python: svc.projects().locations().jobs().snapshots().list(**params).execute()
    calls:
      - action: projects.locations.jobs.snapshots.list
        params:
          jobId: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.snapshots }}'
      item:
        creationTime: '{{ item.creationTime }}'
        description: '{{ item.description }}'
        diskSizeBytes: '{{ item.diskSizeBytes }}'
        id: '{{ item.id }}'
        projectId: '{{ item.projectId }}'
        pubsubMetadata: '{{ item.pubsubMetadata }}'
        region: '{{ item.region }}'
        sourceJobId: '{{ item.sourceJobId }}'
        state: '{{ item.state }}'
        ttl: '{{ item.ttl }}'
    # required_params: ['jobId', 'location', 'projectId']
  # Request detailed information about the execution status of a stage of the job. EXPERIMENTAL. This AP
  # ── gcp.dataflow.projects.locations.jobs.stages.getExecutionDetails [dependent] ──
  - discovery_id: gcp.dataflow.projects.locations.jobs.stages.getExecutionDetails
    for_each: gcp.dataflow.projects.locations.jobs.list
    # python: svc.projects().locations().jobs().stages().getExecutionDetails(**params).execute()
    calls:
      - action: projects.locations.jobs.stages.getExecutionDetails
        params:
          jobId: '{{ item.name }}'
          stageId: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        workers: '{{ response.workers }}'
    # required_params: ['jobId', 'stageId', 'projectId', 'location']
  # Gets information about a snapshot.
  # ── gcp.dataflow.projects.locations.snapshots.get [dependent] ──
  - discovery_id: gcp.dataflow.projects.locations.snapshots.get
    for_each: gcp.dataflow.projects.locations.jobs.snapshots.list
    # python: svc.projects().locations().snapshots().get(**params).execute()
    calls:
      - action: projects.locations.snapshots.get
        params:
          snapshotId: '{{ item.id }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        creationTime: '{{ response.creationTime }}'
        description: '{{ response.description }}'
        diskSizeBytes: '{{ response.diskSizeBytes }}'
        id: '{{ response.id }}'
        projectId: '{{ response.projectId }}'
        pubsubMetadata: '{{ response.pubsubMetadata }}'
        region: '{{ response.region }}'
        sourceJobId: '{{ response.sourceJobId }}'
        state: '{{ response.state }}'
        ttl: '{{ response.ttl }}'
    # required_params: ['location', 'snapshotId', 'projectId']
  # Gets information about a snapshot.
  # ── gcp.dataflow.projects.snapshots.get [dependent] ──
  - discovery_id: gcp.dataflow.projects.snapshots.get
    for_each: gcp.dataflow.projects.locations.jobs.snapshots.list
    # python: svc.projects().snapshots().get(**params).execute()
    calls:
      - action: projects.snapshots.get
        params:
          snapshotId: '{{ item.id }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        creationTime: '{{ response.creationTime }}'
        description: '{{ response.description }}'
        diskSizeBytes: '{{ response.diskSizeBytes }}'
        id: '{{ response.id }}'
        projectId: '{{ response.projectId }}'
        pubsubMetadata: '{{ response.pubsubMetadata }}'
        region: '{{ response.region }}'
        sourceJobId: '{{ response.sourceJobId }}'
        state: '{{ response.state }}'
        ttl: '{{ response.ttl }}'
    # required_params: ['snapshotId', 'projectId']
