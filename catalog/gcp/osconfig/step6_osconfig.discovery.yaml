# ============================================================
# Discovery YAML — osconfig (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 18 | independent: 9 | dependent: 9
# ops with fields: 18/18 | dep ops with chain: 9/9
# ============================================================
version: '1.0'
provider: gcp
service: osconfig

services:
  client: osconfig
  module: "googleapiclient.discovery.build('osconfig', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # GetProjectFeatureSettings returns the VM Manager feature settings for a project.
  # ── gcp.osconfig.projects.locations.global.getProjectFeatureSettings ──
  - discovery_id: gcp.osconfig.projects.locations.global.getProjectFeatureSettings
    # python: svc.projects().locations().global().getProjectFeatureSettings(**params).execute()
    calls:
      - action: projects.locations.global.getProjectFeatureSettings
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        patchAndConfigFeatureSet: '{{ response.patchAndConfigFeatureSet }}'
  # List inventory data for all VM instances in the specified zone.
  # ── gcp.osconfig.projects.locations.instances.inventories.list ──
  - discovery_id: gcp.osconfig.projects.locations.instances.inventories.list
    # python: svc.projects().locations().instances().inventories().list(**params).execute()
    calls:
      - action: projects.locations.instances.inventories.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.inventories }}'
      item:
        items: '{{ item.items }}'
        name: '{{ item.name }}'
        osInfo: '{{ item.osInfo }}'
        updateTime: '{{ item.updateTime }}'
  # List OS policy assignment reports for all Compute Engine VM instances in the specified zone.
  # ── gcp.osconfig.projects.locations.instances.osPolicyAssignments.reports.list ──
  - discovery_id: gcp.osconfig.projects.locations.instances.osPolicyAssignments.reports.list
    # python: svc.projects().locations().instances().osPolicyAssignments().reports().list(**params).execute()
    calls:
      - action: projects.locations.instances.osPolicyAssignments.reports.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.reports }}'
      item:
        instance: '{{ item.instance }}'
        lastRunId: '{{ item.lastRunId }}'
        name: '{{ item.name }}'
        osPolicyAssignment: '{{ item.osPolicyAssignment }}'
        osPolicyCompliances: '{{ item.osPolicyCompliances }}'
        updateTime: '{{ item.updateTime }}'
  # List vulnerability reports for all VM instances in the specified zone.
  # ── gcp.osconfig.projects.locations.instances.vulnerabilityReports.list ──
  - discovery_id: gcp.osconfig.projects.locations.instances.vulnerabilityReports.list
    # python: svc.projects().locations().instances().vulnerabilityReports().list(**params).execute()
    calls:
      - action: projects.locations.instances.vulnerabilityReports.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.vulnerabilityReports }}'
      item:
        highestUpgradableCveSeverity: '{{ item.highestUpgradableCveSeverity }}'
        name: '{{ item.name }}'
        updateTime: '{{ item.updateTime }}'
        vulnerabilities: '{{ item.vulnerabilities }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.osconfig.projects.locations.operations.list ──
  - discovery_id: gcp.osconfig.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # List the OS policy assignments under the parent resource. For each OS policy assignment, the latest 
  # ── gcp.osconfig.projects.locations.osPolicyAssignments.list ──
  - discovery_id: gcp.osconfig.projects.locations.osPolicyAssignments.list
    # python: svc.projects().locations().osPolicyAssignments().list(**params).execute()
    calls:
      - action: projects.locations.osPolicyAssignments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.osPolicyAssignments }}'
      item:
        baseline: '{{ item.baseline }}'
        deleted: '{{ item.deleted }}'
        description: '{{ item.description }}'
        instanceFilter: '{{ item.instanceFilter }}'
        name: '{{ item.name }}'
        osPolicies: '{{ item.osPolicies }}'
        reconciling: '{{ item.reconciling }}'
        revisionCreateTime: '{{ item.revisionCreateTime }}'
        revisionId: '{{ item.revisionId }}'
        rollout: '{{ item.rollout }}'
        rolloutState: '{{ item.rolloutState }}'
        uid: '{{ item.uid }}'
  # Get a page of OS Config patch deployments.
  # ── gcp.osconfig.projects.patchDeployments.list ──
  - discovery_id: gcp.osconfig.projects.patchDeployments.list
    # python: svc.projects().patchDeployments().list(**params).execute()
    calls:
      - action: projects.patchDeployments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.patchDeployments }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        duration: '{{ item.duration }}'
        instanceFilter: '{{ item.instanceFilter }}'
        lastExecuteTime: '{{ item.lastExecuteTime }}'
        name: '{{ item.name }}'
        oneTimeSchedule: '{{ item.oneTimeSchedule }}'
        patchConfig: '{{ item.patchConfig }}'
        recurringSchedule: '{{ item.recurringSchedule }}'
        rollout: '{{ item.rollout }}'
        state: '{{ item.state }}'
        updateTime: '{{ item.updateTime }}'
  # Get a list of instance details for a given patch job.
  # ── gcp.osconfig.projects.patchJobs.instanceDetails.list ──
  - discovery_id: gcp.osconfig.projects.patchJobs.instanceDetails.list
    # python: svc.projects().patchJobs().instanceDetails().list(**params).execute()
    calls:
      - action: projects.patchJobs.instanceDetails.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.instanceDetails }}'
      item:
        attemptCount: '{{ item.attemptCount }}'
        failureReason: '{{ item.failureReason }}'
        instanceSystemId: '{{ item.instanceSystemId }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
  # Get a list of patch jobs.
  # ── gcp.osconfig.projects.patchJobs.list ──
  - discovery_id: gcp.osconfig.projects.patchJobs.list
    # python: svc.projects().patchJobs().list(**params).execute()
    calls:
      - action: projects.patchJobs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.patchJobs }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        dryRun: '{{ item.dryRun }}'
        duration: '{{ item.duration }}'
        errorMessage: '{{ item.errorMessage }}'
        instanceDetailsSummary: '{{ item.instanceDetailsSummary }}'
        instanceFilter: '{{ item.instanceFilter }}'
        name: '{{ item.name }}'
        patchConfig: '{{ item.patchConfig }}'
        patchDeployment: '{{ item.patchDeployment }}'
        percentComplete: '{{ item.percentComplete }}'
        rollout: '{{ item.rollout }}'
        state: '{{ item.state }}'
        updateTime: '{{ item.updateTime }}'

  # ════ DEPENDENT (enrich) operations ════
  # Get inventory data for the specified VM instance. If the VM has no associated inventory, the message
  # ── gcp.osconfig.projects.locations.instances.inventories.get [dependent] ──
  - discovery_id: gcp.osconfig.projects.locations.instances.inventories.get
    for_each: gcp.osconfig.projects.patchJobs.list
    # python: svc.projects().locations().instances().inventories().get(**params).execute()
    calls:
      - action: projects.locations.instances.inventories.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        items: '{{ response.items }}'
        name: '{{ response.name }}'
        osInfo: '{{ response.osInfo }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Get the OS policy assignment report for the specified Compute Engine VM instance.
  # ── gcp.osconfig.projects.locations.instances.osPolicyAssignments.reports.get [dependent] ──
  - discovery_id: gcp.osconfig.projects.locations.instances.osPolicyAssignments.reports.get
    for_each: gcp.osconfig.projects.locations.osPolicyAssignments.list
    # python: svc.projects().locations().instances().osPolicyAssignments().reports().get(**params).execute()
    calls:
      - action: projects.locations.instances.osPolicyAssignments.reports.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        instance: '{{ response.instance }}'
        lastRunId: '{{ response.lastRunId }}'
        name: '{{ response.name }}'
        osPolicyAssignment: '{{ response.osPolicyAssignment }}'
        osPolicyCompliances: '{{ response.osPolicyCompliances }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the vulnerability report for the specified VM instance. Only VMs with inventory data have vulne
  # ── gcp.osconfig.projects.locations.instances.vulnerabilityReports.get [dependent] ──
  - discovery_id: gcp.osconfig.projects.locations.instances.vulnerabilityReports.get
    for_each: gcp.osconfig.projects.patchJobs.list
    # python: svc.projects().locations().instances().vulnerabilityReports().get(**params).execute()
    calls:
      - action: projects.locations.instances.vulnerabilityReports.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        highestUpgradableCveSeverity: '{{ response.highestUpgradableCveSeverity }}'
        name: '{{ response.name }}'
        updateTime: '{{ response.updateTime }}'
        vulnerabilities: '{{ response.vulnerabilities }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.osconfig.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.osconfig.projects.locations.operations.get
    for_each: gcp.osconfig.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Retrieve an existing OS policy assignment. This method always returns the latest revision. In order 
  # ── gcp.osconfig.projects.locations.osPolicyAssignments.get [dependent] ──
  - discovery_id: gcp.osconfig.projects.locations.osPolicyAssignments.get
    for_each: gcp.osconfig.projects.locations.osPolicyAssignments.list
    # python: svc.projects().locations().osPolicyAssignments().get(**params).execute()
    calls:
      - action: projects.locations.osPolicyAssignments.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        baseline: '{{ response.baseline }}'
        deleted: '{{ response.deleted }}'
        description: '{{ response.description }}'
        instanceFilter: '{{ response.instanceFilter }}'
        name: '{{ response.name }}'
        osPolicies: '{{ response.osPolicies }}'
        reconciling: '{{ response.reconciling }}'
        revisionCreateTime: '{{ response.revisionCreateTime }}'
        revisionId: '{{ response.revisionId }}'
        rollout: '{{ response.rollout }}'
        rolloutState: '{{ response.rolloutState }}'
        uid: '{{ response.uid }}'
    # required_params: ['name']
  # List the OS policy assignment revisions for a given OS policy assignment.
  # ── gcp.osconfig.projects.locations.osPolicyAssignments.listRevisions [dependent] ──
  - discovery_id: gcp.osconfig.projects.locations.osPolicyAssignments.listRevisions
    for_each: gcp.osconfig.projects.locations.osPolicyAssignments.list
    # python: svc.projects().locations().osPolicyAssignments().listRevisions(**params).execute()
    calls:
      - action: projects.locations.osPolicyAssignments.listRevisions
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.osPolicyAssignments }}'
      item:
        baseline: '{{ item.baseline }}'
        deleted: '{{ item.deleted }}'
        description: '{{ item.description }}'
        instanceFilter: '{{ item.instanceFilter }}'
        name: '{{ item.name }}'
        osPolicies: '{{ item.osPolicies }}'
        reconciling: '{{ item.reconciling }}'
        revisionCreateTime: '{{ item.revisionCreateTime }}'
        revisionId: '{{ item.revisionId }}'
        rollout: '{{ item.rollout }}'
        rolloutState: '{{ item.rolloutState }}'
        uid: '{{ item.uid }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.osconfig.projects.locations.osPolicyAssignments.operations.get [dependent] ──
  - discovery_id: gcp.osconfig.projects.locations.osPolicyAssignments.operations.get
    for_each: gcp.osconfig.projects.locations.operations.list
    # python: svc.projects().locations().osPolicyAssignments().operations().get(**params).execute()
    calls:
      - action: projects.locations.osPolicyAssignments.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Get an OS Config patch deployment.
  # ── gcp.osconfig.projects.patchDeployments.get [dependent] ──
  - discovery_id: gcp.osconfig.projects.patchDeployments.get
    for_each: gcp.osconfig.projects.patchDeployments.list
    # python: svc.projects().patchDeployments().get(**params).execute()
    calls:
      - action: projects.patchDeployments.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        duration: '{{ response.duration }}'
        instanceFilter: '{{ response.instanceFilter }}'
        lastExecuteTime: '{{ response.lastExecuteTime }}'
        name: '{{ response.name }}'
        oneTimeSchedule: '{{ response.oneTimeSchedule }}'
        patchConfig: '{{ response.patchConfig }}'
        recurringSchedule: '{{ response.recurringSchedule }}'
        rollout: '{{ response.rollout }}'
        state: '{{ response.state }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Get the patch job. This can be used to track the progress of an ongoing patch job or review the deta
  # ── gcp.osconfig.projects.patchJobs.get [dependent] ──
  - discovery_id: gcp.osconfig.projects.patchJobs.get
    for_each: gcp.osconfig.projects.patchJobs.list
    # python: svc.projects().patchJobs().get(**params).execute()
    calls:
      - action: projects.patchJobs.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        dryRun: '{{ response.dryRun }}'
        duration: '{{ response.duration }}'
        errorMessage: '{{ response.errorMessage }}'
        instanceDetailsSummary: '{{ response.instanceDetailsSummary }}'
        instanceFilter: '{{ response.instanceFilter }}'
        name: '{{ response.name }}'
        patchConfig: '{{ response.patchConfig }}'
        patchDeployment: '{{ response.patchDeployment }}'
        percentComplete: '{{ response.percentComplete }}'
        rollout: '{{ response.rollout }}'
        state: '{{ response.state }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
