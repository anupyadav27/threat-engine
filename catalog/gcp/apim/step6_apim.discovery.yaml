# ============================================================
# Discovery YAML — apim (v1alpha)
# Generated: 2026-02-21T03:06:38Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 14 | independent: 9 | dependent: 5
# ops with fields: 13/14 | dep ops with chain: 5/5
# ============================================================
version: '1.0'
provider: gcp
service: apim

services:
  client: apim
  module: "googleapiclient.discovery.build('apim', 'v1alpha')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Gets information about a location.
  # ── gcp.apim.projects.locations.get ──
  - discovery_id: gcp.apim.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # GetEntitlement returns the entitlement for the provided project.
  # ── gcp.apim.projects.locations.getEntitlement ──
  - discovery_id: gcp.apim.projects.locations.getEntitlement
    # python: svc.projects().locations().getEntitlement(**params).execute()
    calls:
      - action: projects.locations.getEntitlement
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiObservationEntitled: '{{ response.apiObservationEntitled }}'
        billingProjectNumber: '{{ response.billingProjectNumber }}'
        createTime: '{{ response.createTime }}'
        name: '{{ response.name }}'
        updateTime: '{{ response.updateTime }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.apim.projects.locations.list ──
  - discovery_id: gcp.apim.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # ListApiObservationTags lists all extant tags on any observation in the given project.
  # ── gcp.apim.projects.locations.listApiObservationTags ──
  - discovery_id: gcp.apim.projects.locations.listApiObservationTags
    # python: svc.projects().locations().listApiObservationTags(**params).execute()
    calls:
      - action: projects.locations.listApiObservationTags
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
  # ListApiOperations gets all ApiOperations for a given project and location and ObservationJob and Api
  # ── gcp.apim.projects.locations.observationJobs.apiObservations.apiOperations.list ──
  - discovery_id: gcp.apim.projects.locations.observationJobs.apiObservations.apiOperations.list
    # python: svc.projects().locations().observationJobs().apiObservations().apiOperations().list(**params).execute()
    calls:
      - action: projects.locations.observationJobs.apiObservations.apiOperations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.apiOperations }}'
      item:
        count: '{{ item.count }}'
        firstSeenTime: '{{ item.firstSeenTime }}'
        httpOperation: '{{ item.httpOperation }}'
        lastSeenTime: '{{ item.lastSeenTime }}'
        name: '{{ item.name }}'
  # ListApiObservations gets all ApiObservations for a given project and location and ObservationJob.
  # ── gcp.apim.projects.locations.observationJobs.apiObservations.list ──
  - discovery_id: gcp.apim.projects.locations.observationJobs.apiObservations.list
    # python: svc.projects().locations().observationJobs().apiObservations().list(**params).execute()
    calls:
      - action: projects.locations.observationJobs.apiObservations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.apiObservations }}'
      item:
        apiOperationCount: '{{ item.apiOperationCount }}'
        createTime: '{{ item.createTime }}'
        hostname: '{{ item.hostname }}'
        lastEventDetectedTime: '{{ item.lastEventDetectedTime }}'
        name: '{{ item.name }}'
        serverIps: '{{ item.serverIps }}'
        sourceLocations: '{{ item.sourceLocations }}'
        style: '{{ item.style }}'
        tags: '{{ item.tags }}'
        updateTime: '{{ item.updateTime }}'
  # ListObservationJobs gets all ObservationJobs for a given project and location.
  # ── gcp.apim.projects.locations.observationJobs.list ──
  - discovery_id: gcp.apim.projects.locations.observationJobs.list
    # python: svc.projects().locations().observationJobs().list(**params).execute()
    calls:
      - action: projects.locations.observationJobs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.observationJobs }}'
      item:
        createTime: '{{ item.createTime }}'
        name: '{{ item.name }}'
        sources: '{{ item.sources }}'
        state: '{{ item.state }}'
        updateTime: '{{ item.updateTime }}'
  # ListObservationSources gets all ObservationSources for a given project and location.
  # ── gcp.apim.projects.locations.observationSources.list ──
  - discovery_id: gcp.apim.projects.locations.observationSources.list
    # python: svc.projects().locations().observationSources().list(**params).execute()
    calls:
      - action: projects.locations.observationSources.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.observationSources }}'
      item:
        createTime: '{{ item.createTime }}'
        gclbObservationSource: '{{ item.gclbObservationSource }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
        updateTime: '{{ item.updateTime }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.apim.projects.locations.operations.list ──
  - discovery_id: gcp.apim.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'

  # ════ DEPENDENT (enrich) operations ════
  # GetApiOperation retrieves a single ApiOperation by name.
  # ── gcp.apim.projects.locations.observationJobs.apiObservations.apiOperations.get [dependent] ──
  - discovery_id: gcp.apim.projects.locations.observationJobs.apiObservations.apiOperations.get
    for_each: gcp.apim.projects.locations.observationJobs.apiObservations.apiOperations.list
    # python: svc.projects().locations().observationJobs().apiObservations().apiOperations().get(**params).execute()
    calls:
      - action: projects.locations.observationJobs.apiObservations.apiOperations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        count: '{{ response.count }}'
        firstSeenTime: '{{ response.firstSeenTime }}'
        httpOperation: '{{ response.httpOperation }}'
        lastSeenTime: '{{ response.lastSeenTime }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # GetApiObservation retrieves a single ApiObservation by name.
  # ── gcp.apim.projects.locations.observationJobs.apiObservations.get [dependent] ──
  - discovery_id: gcp.apim.projects.locations.observationJobs.apiObservations.get
    for_each: gcp.apim.projects.locations.observationJobs.apiObservations.list
    # python: svc.projects().locations().observationJobs().apiObservations().get(**params).execute()
    calls:
      - action: projects.locations.observationJobs.apiObservations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiOperationCount: '{{ response.apiOperationCount }}'
        createTime: '{{ response.createTime }}'
        hostname: '{{ response.hostname }}'
        lastEventDetectedTime: '{{ response.lastEventDetectedTime }}'
        name: '{{ response.name }}'
        serverIps: '{{ response.serverIps }}'
        sourceLocations: '{{ response.sourceLocations }}'
        style: '{{ response.style }}'
        tags: '{{ response.tags }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # GetObservationJob retrieves a single ObservationJob by name.
  # ── gcp.apim.projects.locations.observationJobs.get [dependent] ──
  - discovery_id: gcp.apim.projects.locations.observationJobs.get
    for_each: gcp.apim.projects.locations.observationJobs.list
    # python: svc.projects().locations().observationJobs().get(**params).execute()
    calls:
      - action: projects.locations.observationJobs.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        name: '{{ response.name }}'
        sources: '{{ response.sources }}'
        state: '{{ response.state }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # GetObservationSource retrieves a single ObservationSource by name.
  # ── gcp.apim.projects.locations.observationSources.get [dependent] ──
  - discovery_id: gcp.apim.projects.locations.observationSources.get
    for_each: gcp.apim.projects.locations.observationSources.list
    # python: svc.projects().locations().observationSources().get(**params).execute()
    calls:
      - action: projects.locations.observationSources.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        gclbObservationSource: '{{ response.gclbObservationSource }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.apim.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.apim.projects.locations.operations.get
    for_each: gcp.apim.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
