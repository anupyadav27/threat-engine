# ============================================================
# Discovery YAML — artifactregistry (v1)
# Generated: 2026-02-21T03:06:38Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 29 | independent: 15 | dependent: 14
# ops with fields: 28/29 | dep ops with chain: 14/14
# ============================================================
version: '1.0'
provider: gcp
service: artifactregistry

services:
  client: artifactregistry
  module: "googleapiclient.discovery.build('artifactregistry', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Retrieves the Settings for the Project.
  # ── gcp.artifactregistry.projects.getProjectSettings ──
  - discovery_id: gcp.artifactregistry.projects.getProjectSettings
    # python: svc.projects().getProjectSettings(**params).execute()
    calls:
      - action: projects.getProjectSettings
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        legacyRedirectionState: '{{ response.legacyRedirectionState }}'
        name: '{{ response.name }}'
        pullPercent: '{{ response.pullPercent }}'
  # Gets information about a location.
  # ── gcp.artifactregistry.projects.locations.get ──
  - discovery_id: gcp.artifactregistry.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Retrieves the VPCSC Config for the Project.
  # ── gcp.artifactregistry.projects.locations.getVpcscConfig ──
  - discovery_id: gcp.artifactregistry.projects.locations.getVpcscConfig
    # python: svc.projects().locations().getVpcscConfig(**params).execute()
    calls:
      - action: projects.locations.getVpcscConfig
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        vpcscPolicy: '{{ response.vpcscPolicy }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.artifactregistry.projects.locations.list ──
  - discovery_id: gcp.artifactregistry.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists attachments.
  # ── gcp.artifactregistry.projects.locations.repositories.attachments.list ──
  - discovery_id: gcp.artifactregistry.projects.locations.repositories.attachments.list
    # python: svc.projects().locations().repositories().attachments().list(**params).execute()
    calls:
      - action: projects.locations.repositories.attachments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.attachments }}'
      item:
        annotations: '{{ item.annotations }}'
        attachmentNamespace: '{{ item.attachmentNamespace }}'
        createTime: '{{ item.createTime }}'
        files: '{{ item.files }}'
        name: '{{ item.name }}'
        ociVersionName: '{{ item.ociVersionName }}'
        target: '{{ item.target }}'
        type: '{{ item.type }}'
        updateTime: '{{ item.updateTime }}'
  # Lists docker images.
  # ── gcp.artifactregistry.projects.locations.repositories.dockerImages.list ──
  - discovery_id: gcp.artifactregistry.projects.locations.repositories.dockerImages.list
    # python: svc.projects().locations().repositories().dockerImages().list(**params).execute()
    calls:
      - action: projects.locations.repositories.dockerImages.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.dockerImages }}'
      item:
        artifactType: '{{ item.artifactType }}'
        buildTime: '{{ item.buildTime }}'
        imageManifests: '{{ item.imageManifests }}'
        imageSizeBytes: '{{ item.imageSizeBytes }}'
        mediaType: '{{ item.mediaType }}'
        name: '{{ item.name }}'
        tags: '{{ item.tags }}'
        updateTime: '{{ item.updateTime }}'
        uploadTime: '{{ item.uploadTime }}'
        uri: '{{ item.uri }}'
  # Lists files.
  # ── gcp.artifactregistry.projects.locations.repositories.files.list ──
  - discovery_id: gcp.artifactregistry.projects.locations.repositories.files.list
    # python: svc.projects().locations().repositories().files().list(**params).execute()
    calls:
      - action: projects.locations.repositories.files.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.files }}'
      item:
        annotations: '{{ item.annotations }}'
        createTime: '{{ item.createTime }}'
        fetchTime: '{{ item.fetchTime }}'
        hashes: '{{ item.hashes }}'
        name: '{{ item.name }}'
        owner: '{{ item.owner }}'
        sizeBytes: '{{ item.sizeBytes }}'
        updateTime: '{{ item.updateTime }}'
  # Lists repositories.
  # ── gcp.artifactregistry.projects.locations.repositories.list ──
  - discovery_id: gcp.artifactregistry.projects.locations.repositories.list
    # python: svc.projects().locations().repositories().list(**params).execute()
    calls:
      - action: projects.locations.repositories.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.repositories }}'
      item:
        cleanupPolicies: '{{ item.cleanupPolicies }}'
        cleanupPolicyDryRun: '{{ item.cleanupPolicyDryRun }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        disallowUnspecifiedMode: '{{ item.disallowUnspecifiedMode }}'
        dockerConfig: '{{ item.dockerConfig }}'
        format: '{{ item.format }}'
        kmsKeyName: '{{ item.kmsKeyName }}'
        labels: '{{ item.labels }}'
        mavenConfig: '{{ item.mavenConfig }}'
        mode: '{{ item.mode }}'
        name: '{{ item.name }}'
        registryUri: '{{ item.registryUri }}'
        remoteRepositoryConfig: '{{ item.remoteRepositoryConfig }}'
        satisfiesPzi: '{{ item.satisfiesPzi }}'
        satisfiesPzs: '{{ item.satisfiesPzs }}'
        sizeBytes: '{{ item.sizeBytes }}'
        updateTime: '{{ item.updateTime }}'
        virtualRepositoryConfig: '{{ item.virtualRepositoryConfig }}'
        vulnerabilityScanningConfig: '{{ item.vulnerabilityScanningConfig }}'
  # Lists maven artifacts.
  # ── gcp.artifactregistry.projects.locations.repositories.mavenArtifacts.list ──
  - discovery_id: gcp.artifactregistry.projects.locations.repositories.mavenArtifacts.list
    # python: svc.projects().locations().repositories().mavenArtifacts().list(**params).execute()
    calls:
      - action: projects.locations.repositories.mavenArtifacts.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.mavenArtifacts }}'
      item:
        artifactId: '{{ item.artifactId }}'
        createTime: '{{ item.createTime }}'
        groupId: '{{ item.groupId }}'
        name: '{{ item.name }}'
        pomUri: '{{ item.pomUri }}'
        updateTime: '{{ item.updateTime }}'
        version: '{{ item.version }}'
  # Lists npm packages.
  # ── gcp.artifactregistry.projects.locations.repositories.npmPackages.list ──
  - discovery_id: gcp.artifactregistry.projects.locations.repositories.npmPackages.list
    # python: svc.projects().locations().repositories().npmPackages().list(**params).execute()
    calls:
      - action: projects.locations.repositories.npmPackages.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.npmPackages }}'
      item:
        createTime: '{{ item.createTime }}'
        name: '{{ item.name }}'
        packageName: '{{ item.packageName }}'
        tags: '{{ item.tags }}'
        updateTime: '{{ item.updateTime }}'
        version: '{{ item.version }}'
  # Lists packages.
  # ── gcp.artifactregistry.projects.locations.repositories.packages.list ──
  - discovery_id: gcp.artifactregistry.projects.locations.repositories.packages.list
    # python: svc.projects().locations().repositories().packages().list(**params).execute()
    calls:
      - action: projects.locations.repositories.packages.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.packages }}'
      item:
        annotations: '{{ item.annotations }}'
        createTime: '{{ item.createTime }}'
        displayName: '{{ item.displayName }}'
        name: '{{ item.name }}'
        updateTime: '{{ item.updateTime }}'
  # Lists tags.
  # ── gcp.artifactregistry.projects.locations.repositories.packages.tags.list ──
  - discovery_id: gcp.artifactregistry.projects.locations.repositories.packages.tags.list
    # python: svc.projects().locations().repositories().packages().tags().list(**params).execute()
    calls:
      - action: projects.locations.repositories.packages.tags.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.tags }}'
      item:
        name: '{{ item.name }}'
        version: '{{ item.version }}'
  # Lists versions.
  # ── gcp.artifactregistry.projects.locations.repositories.packages.versions.list ──
  - discovery_id: gcp.artifactregistry.projects.locations.repositories.packages.versions.list
    # python: svc.projects().locations().repositories().packages().versions().list(**params).execute()
    calls:
      - action: projects.locations.repositories.packages.versions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.versions }}'
      item:
        annotations: '{{ item.annotations }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        fingerprints: '{{ item.fingerprints }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        relatedTags: '{{ item.relatedTags }}'
        updateTime: '{{ item.updateTime }}'
  # Lists python packages.
  # ── gcp.artifactregistry.projects.locations.repositories.pythonPackages.list ──
  - discovery_id: gcp.artifactregistry.projects.locations.repositories.pythonPackages.list
    # python: svc.projects().locations().repositories().pythonPackages().list(**params).execute()
    calls:
      - action: projects.locations.repositories.pythonPackages.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.pythonPackages }}'
      item:
        createTime: '{{ item.createTime }}'
        name: '{{ item.name }}'
        packageName: '{{ item.packageName }}'
        updateTime: '{{ item.updateTime }}'
        uri: '{{ item.uri }}'
        version: '{{ item.version }}'
  # Lists rules.
  # ── gcp.artifactregistry.projects.locations.repositories.rules.list ──
  - discovery_id: gcp.artifactregistry.projects.locations.repositories.rules.list
    # python: svc.projects().locations().repositories().rules().list(**params).execute()
    calls:
      - action: projects.locations.repositories.rules.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.rules }}'
      item:
        action: '{{ item.action }}'
        condition: '{{ item.condition }}'
        name: '{{ item.name }}'
        operation: '{{ item.operation }}'
        packageId: '{{ item.packageId }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.artifactregistry.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.artifactregistry.projects.locations.operations.get
    for_each: gcp.artifactregistry.projects.locations.repositories.rules.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets an attachment.
  # ── gcp.artifactregistry.projects.locations.repositories.attachments.get [dependent] ──
  - discovery_id: gcp.artifactregistry.projects.locations.repositories.attachments.get
    for_each: gcp.artifactregistry.projects.locations.repositories.attachments.list
    # python: svc.projects().locations().repositories().attachments().get(**params).execute()
    calls:
      - action: projects.locations.repositories.attachments.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        attachmentNamespace: '{{ response.attachmentNamespace }}'
        createTime: '{{ response.createTime }}'
        files: '{{ response.files }}'
        name: '{{ response.name }}'
        ociVersionName: '{{ response.ociVersionName }}'
        target: '{{ response.target }}'
        type: '{{ response.type }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets a docker image.
  # ── gcp.artifactregistry.projects.locations.repositories.dockerImages.get [dependent] ──
  - discovery_id: gcp.artifactregistry.projects.locations.repositories.dockerImages.get
    for_each: gcp.artifactregistry.projects.locations.repositories.dockerImages.list
    # python: svc.projects().locations().repositories().dockerImages().get(**params).execute()
    calls:
      - action: projects.locations.repositories.dockerImages.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        artifactType: '{{ response.artifactType }}'
        buildTime: '{{ response.buildTime }}'
        imageManifests: '{{ response.imageManifests }}'
        imageSizeBytes: '{{ response.imageSizeBytes }}'
        mediaType: '{{ response.mediaType }}'
        name: '{{ response.name }}'
        tags: '{{ response.tags }}'
        updateTime: '{{ response.updateTime }}'
        uploadTime: '{{ response.uploadTime }}'
        uri: '{{ response.uri }}'
    # required_params: ['name']
  # Download a file.
  # ── gcp.artifactregistry.projects.locations.repositories.files.download [dependent] ──
  - discovery_id: gcp.artifactregistry.projects.locations.repositories.files.download
    for_each: gcp.artifactregistry.projects.locations.repositories.files.list
    # python: svc.projects().locations().repositories().files().download(**params).execute()
    calls:
      - action: projects.locations.repositories.files.download
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
    # required_params: ['name']
  # Gets a file.
  # ── gcp.artifactregistry.projects.locations.repositories.files.get [dependent] ──
  - discovery_id: gcp.artifactregistry.projects.locations.repositories.files.get
    for_each: gcp.artifactregistry.projects.locations.repositories.files.list
    # python: svc.projects().locations().repositories().files().get(**params).execute()
    calls:
      - action: projects.locations.repositories.files.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        createTime: '{{ response.createTime }}'
        fetchTime: '{{ response.fetchTime }}'
        hashes: '{{ response.hashes }}'
        name: '{{ response.name }}'
        owner: '{{ response.owner }}'
        sizeBytes: '{{ response.sizeBytes }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets a repository.
  # ── gcp.artifactregistry.projects.locations.repositories.get [dependent] ──
  - discovery_id: gcp.artifactregistry.projects.locations.repositories.get
    for_each: gcp.artifactregistry.projects.locations.repositories.list
    # python: svc.projects().locations().repositories().get(**params).execute()
    calls:
      - action: projects.locations.repositories.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        cleanupPolicies: '{{ response.cleanupPolicies }}'
        cleanupPolicyDryRun: '{{ response.cleanupPolicyDryRun }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        disallowUnspecifiedMode: '{{ response.disallowUnspecifiedMode }}'
        dockerConfig: '{{ response.dockerConfig }}'
        format: '{{ response.format }}'
        kmsKeyName: '{{ response.kmsKeyName }}'
        labels: '{{ response.labels }}'
        mavenConfig: '{{ response.mavenConfig }}'
        mode: '{{ response.mode }}'
        name: '{{ response.name }}'
        registryUri: '{{ response.registryUri }}'
        remoteRepositoryConfig: '{{ response.remoteRepositoryConfig }}'
        satisfiesPzi: '{{ response.satisfiesPzi }}'
        satisfiesPzs: '{{ response.satisfiesPzs }}'
        sizeBytes: '{{ response.sizeBytes }}'
        updateTime: '{{ response.updateTime }}'
        virtualRepositoryConfig: '{{ response.virtualRepositoryConfig }}'
        vulnerabilityScanningConfig: '{{ response.vulnerabilityScanningConfig }}'
    # required_params: ['name']
  # Gets the IAM policy for a given resource.
  # ── gcp.artifactregistry.projects.locations.repositories.getIamPolicy [dependent] ──
  - discovery_id: gcp.artifactregistry.projects.locations.repositories.getIamPolicy
    for_each: gcp.artifactregistry.projects.locations.repositories.list
    # python: svc.projects().locations().repositories().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.repositories.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Gets a maven artifact.
  # ── gcp.artifactregistry.projects.locations.repositories.mavenArtifacts.get [dependent] ──
  - discovery_id: gcp.artifactregistry.projects.locations.repositories.mavenArtifacts.get
    for_each: gcp.artifactregistry.projects.locations.repositories.mavenArtifacts.list
    # python: svc.projects().locations().repositories().mavenArtifacts().get(**params).execute()
    calls:
      - action: projects.locations.repositories.mavenArtifacts.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        artifactId: '{{ response.artifactId }}'
        createTime: '{{ response.createTime }}'
        groupId: '{{ response.groupId }}'
        name: '{{ response.name }}'
        pomUri: '{{ response.pomUri }}'
        updateTime: '{{ response.updateTime }}'
        version: '{{ response.version }}'
    # required_params: ['name']
  # Gets a npm package.
  # ── gcp.artifactregistry.projects.locations.repositories.npmPackages.get [dependent] ──
  - discovery_id: gcp.artifactregistry.projects.locations.repositories.npmPackages.get
    for_each: gcp.artifactregistry.projects.locations.repositories.npmPackages.list
    # python: svc.projects().locations().repositories().npmPackages().get(**params).execute()
    calls:
      - action: projects.locations.repositories.npmPackages.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        name: '{{ response.name }}'
        packageName: '{{ response.packageName }}'
        tags: '{{ response.tags }}'
        updateTime: '{{ response.updateTime }}'
        version: '{{ response.version }}'
    # required_params: ['name']
  # Gets a package.
  # ── gcp.artifactregistry.projects.locations.repositories.packages.get [dependent] ──
  - discovery_id: gcp.artifactregistry.projects.locations.repositories.packages.get
    for_each: gcp.artifactregistry.projects.locations.repositories.packages.list
    # python: svc.projects().locations().repositories().packages().get(**params).execute()
    calls:
      - action: projects.locations.repositories.packages.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        createTime: '{{ response.createTime }}'
        displayName: '{{ response.displayName }}'
        name: '{{ response.name }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets a tag.
  # ── gcp.artifactregistry.projects.locations.repositories.packages.tags.get [dependent] ──
  - discovery_id: gcp.artifactregistry.projects.locations.repositories.packages.tags.get
    for_each: gcp.artifactregistry.projects.locations.repositories.packages.tags.list
    # python: svc.projects().locations().repositories().packages().tags().get(**params).execute()
    calls:
      - action: projects.locations.repositories.packages.tags.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        version: '{{ response.version }}'
    # required_params: ['name']
  # Gets a version
  # ── gcp.artifactregistry.projects.locations.repositories.packages.versions.get [dependent] ──
  - discovery_id: gcp.artifactregistry.projects.locations.repositories.packages.versions.get
    for_each: gcp.artifactregistry.projects.locations.repositories.packages.versions.list
    # python: svc.projects().locations().repositories().packages().versions().get(**params).execute()
    calls:
      - action: projects.locations.repositories.packages.versions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        fingerprints: '{{ response.fingerprints }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        relatedTags: '{{ response.relatedTags }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets a python package.
  # ── gcp.artifactregistry.projects.locations.repositories.pythonPackages.get [dependent] ──
  - discovery_id: gcp.artifactregistry.projects.locations.repositories.pythonPackages.get
    for_each: gcp.artifactregistry.projects.locations.repositories.pythonPackages.list
    # python: svc.projects().locations().repositories().pythonPackages().get(**params).execute()
    calls:
      - action: projects.locations.repositories.pythonPackages.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        name: '{{ response.name }}'
        packageName: '{{ response.packageName }}'
        updateTime: '{{ response.updateTime }}'
        uri: '{{ response.uri }}'
        version: '{{ response.version }}'
    # required_params: ['name']
  # Gets a rule.
  # ── gcp.artifactregistry.projects.locations.repositories.rules.get [dependent] ──
  - discovery_id: gcp.artifactregistry.projects.locations.repositories.rules.get
    for_each: gcp.artifactregistry.projects.locations.repositories.rules.list
    # python: svc.projects().locations().repositories().rules().get(**params).execute()
    calls:
      - action: projects.locations.repositories.rules.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        action: '{{ response.action }}'
        condition: '{{ response.condition }}'
        name: '{{ response.name }}'
        operation: '{{ response.operation }}'
        packageId: '{{ response.packageId }}'
    # required_params: ['name']
