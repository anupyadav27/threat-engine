# ============================================================
# Discovery YAML — developerconnect (v1)
# Generated: 2026-02-21T03:06:39Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 21 | independent: 9 | dependent: 12
# ops with fields: 21/21 | dep ops with chain: 12/12
# ============================================================
version: '1.0'
provider: gcp
service: developerconnect

services:
  client: developerconnect
  module: "googleapiclient.discovery.build('developerconnect', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists AccountConnectors in a given project and location.
  # ── gcp.developerconnect.projects.locations.accountConnectors.list ──
  - discovery_id: gcp.developerconnect.projects.locations.accountConnectors.list
    # python: svc.projects().locations().accountConnectors().list(**params).execute()
    calls:
      - action: projects.locations.accountConnectors.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.accountConnectors }}'
      item:
        annotations: '{{ item.annotations }}'
        createTime: '{{ item.createTime }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        oauthStartUri: '{{ item.oauthStartUri }}'
        providerOauthConfig: '{{ item.providerOauthConfig }}'
        updateTime: '{{ item.updateTime }}'
  # Lists Users in a given project, location, and account_connector.
  # ── gcp.developerconnect.projects.locations.accountConnectors.users.list ──
  - discovery_id: gcp.developerconnect.projects.locations.accountConnectors.users.list
    # python: svc.projects().locations().accountConnectors().users().list(**params).execute()
    calls:
      - action: projects.locations.accountConnectors.users.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.users }}'
      item:
        createTime: '{{ item.createTime }}'
        displayName: '{{ item.displayName }}'
        lastTokenRequestTime: '{{ item.lastTokenRequestTime }}'
        name: '{{ item.name }}'
  # Lists GitRepositoryLinks in a given project, location, and connection.
  # ── gcp.developerconnect.projects.locations.connections.gitRepositoryLinks.list ──
  - discovery_id: gcp.developerconnect.projects.locations.connections.gitRepositoryLinks.list
    # python: svc.projects().locations().connections().gitRepositoryLinks().list(**params).execute()
    calls:
      - action: projects.locations.connections.gitRepositoryLinks.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.gitRepositoryLinks }}'
      item:
        annotations: '{{ item.annotations }}'
        cloneUri: '{{ item.cloneUri }}'
        createTime: '{{ item.createTime }}'
        deleteTime: '{{ item.deleteTime }}'
        gitProxyUri: '{{ item.gitProxyUri }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        reconciling: '{{ item.reconciling }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
        webhookId: '{{ item.webhookId }}'
  # Lists Connections in a given project and location.
  # ── gcp.developerconnect.projects.locations.connections.list ──
  - discovery_id: gcp.developerconnect.projects.locations.connections.list
    # python: svc.projects().locations().connections().list(**params).execute()
    calls:
      - action: projects.locations.connections.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.connections }}'
      item:
        annotations: '{{ item.annotations }}'
        bitbucketCloudConfig: '{{ item.bitbucketCloudConfig }}'
        bitbucketDataCenterConfig: '{{ item.bitbucketDataCenterConfig }}'
        createTime: '{{ item.createTime }}'
        cryptoKeyConfig: '{{ item.cryptoKeyConfig }}'
        deleteTime: '{{ item.deleteTime }}'
        disabled: '{{ item.disabled }}'
        gitProxyConfig: '{{ item.gitProxyConfig }}'
        githubConfig: '{{ item.githubConfig }}'
        githubEnterpriseConfig: '{{ item.githubEnterpriseConfig }}'
        gitlabConfig: '{{ item.gitlabConfig }}'
        gitlabEnterpriseConfig: '{{ item.gitlabEnterpriseConfig }}'
        httpConfig: '{{ item.httpConfig }}'
        installationState: '{{ item.installationState }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        reconciling: '{{ item.reconciling }}'
        secureSourceManagerInstanceConfig: '{{ item.secureSourceManagerInstanceConfig }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # Gets information about a location.
  # ── gcp.developerconnect.projects.locations.get ──
  - discovery_id: gcp.developerconnect.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists Deployment Events in a given insights config.
  # ── gcp.developerconnect.projects.locations.insightsConfigs.deploymentEvents.list ──
  - discovery_id: gcp.developerconnect.projects.locations.insightsConfigs.deploymentEvents.list
    # python: svc.projects().locations().insightsConfigs().deploymentEvents().list(**params).execute()
    calls:
      - action: projects.locations.insightsConfigs.deploymentEvents.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.deploymentEvents }}'
      item:
        artifactDeployments: '{{ item.artifactDeployments }}'
        createTime: '{{ item.createTime }}'
        deployTime: '{{ item.deployTime }}'
        name: '{{ item.name }}'
        runtimeConfig: '{{ item.runtimeConfig }}'
        runtimeDeploymentUri: '{{ item.runtimeDeploymentUri }}'
        state: '{{ item.state }}'
        undeployTime: '{{ item.undeployTime }}'
        updateTime: '{{ item.updateTime }}'
  # Lists InsightsConfigs in a given project and location.
  # ── gcp.developerconnect.projects.locations.insightsConfigs.list ──
  - discovery_id: gcp.developerconnect.projects.locations.insightsConfigs.list
    # python: svc.projects().locations().insightsConfigs().list(**params).execute()
    calls:
      - action: projects.locations.insightsConfigs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.insightsConfigs }}'
      item:
        annotations: '{{ item.annotations }}'
        appHubApplication: '{{ item.appHubApplication }}'
        artifactConfigs: '{{ item.artifactConfigs }}'
        createTime: '{{ item.createTime }}'
        errors: '{{ item.errors }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        projects: '{{ item.projects }}'
        reconciling: '{{ item.reconciling }}'
        runtimeConfigs: '{{ item.runtimeConfigs }}'
        state: '{{ item.state }}'
        updateTime: '{{ item.updateTime }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.developerconnect.projects.locations.list ──
  - discovery_id: gcp.developerconnect.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.developerconnect.projects.locations.operations.list ──
  - discovery_id: gcp.developerconnect.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets details of a single AccountConnector.
  # ── gcp.developerconnect.projects.locations.accountConnectors.get [dependent] ──
  - discovery_id: gcp.developerconnect.projects.locations.accountConnectors.get
    for_each: gcp.developerconnect.projects.locations.accountConnectors.list
    # python: svc.projects().locations().accountConnectors().get(**params).execute()
    calls:
      - action: projects.locations.accountConnectors.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        createTime: '{{ response.createTime }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        oauthStartUri: '{{ response.oauthStartUri }}'
        providerOauthConfig: '{{ response.providerOauthConfig }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Fetch the User based on the user credentials.
  # ── gcp.developerconnect.projects.locations.accountConnectors.users.fetchSelf [dependent] ──
  - discovery_id: gcp.developerconnect.projects.locations.accountConnectors.users.fetchSelf
    for_each: gcp.developerconnect.projects.locations.accountConnectors.list
    # python: svc.projects().locations().accountConnectors().users().fetchSelf(**params).execute()
    calls:
      - action: projects.locations.accountConnectors.users.fetchSelf
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        displayName: '{{ response.displayName }}'
        lastTokenRequestTime: '{{ response.lastTokenRequestTime }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Finishes OAuth flow for an account connector.
  # ── gcp.developerconnect.projects.locations.accountConnectors.users.finishOAuthFlow [dependent] ──
  - discovery_id: gcp.developerconnect.projects.locations.accountConnectors.users.finishOAuthFlow
    for_each: gcp.developerconnect.projects.locations.accountConnectors.list
    # python: svc.projects().locations().accountConnectors().users().finishOAuthFlow(**params).execute()
    calls:
      - action: projects.locations.accountConnectors.users.finishOAuthFlow
        params:
          accountConnector: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        exchangeError: '{{ response.exchangeError }}'
    # required_params: ['accountConnector']
  # Starts OAuth flow for an account connector.
  # ── gcp.developerconnect.projects.locations.accountConnectors.users.startOAuthFlow [dependent] ──
  - discovery_id: gcp.developerconnect.projects.locations.accountConnectors.users.startOAuthFlow
    for_each: gcp.developerconnect.projects.locations.accountConnectors.list
    # python: svc.projects().locations().accountConnectors().users().startOAuthFlow(**params).execute()
    calls:
      - action: projects.locations.accountConnectors.users.startOAuthFlow
        params:
          accountConnector: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        authUri: '{{ response.authUri }}'
        clientId: '{{ response.clientId }}'
        codeChallenge: '{{ response.codeChallenge }}'
        codeChallengeMethod: '{{ response.codeChallengeMethod }}'
        scopes: '{{ response.scopes }}'
        systemProviderId: '{{ response.systemProviderId }}'
        ticket: '{{ response.ticket }}'
    # required_params: ['accountConnector']
  # FetchGitHubInstallations returns the list of GitHub Installations that are available to be added to 
  # ── gcp.developerconnect.projects.locations.connections.fetchGitHubInstallations [dependent] ──
  - discovery_id: gcp.developerconnect.projects.locations.connections.fetchGitHubInstallations
    for_each: gcp.developerconnect.projects.locations.connections.list
    # python: svc.projects().locations().connections().fetchGitHubInstallations(**params).execute()
    calls:
      - action: projects.locations.connections.fetchGitHubInstallations
        params:
          connection: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        installations: '{{ response.installations }}'
    # required_params: ['connection']
  # FetchLinkableGitRepositories returns a list of git repositories from an SCM that are available to be
  # ── gcp.developerconnect.projects.locations.connections.fetchLinkableGitRepositories [dependent] ──
  - discovery_id: gcp.developerconnect.projects.locations.connections.fetchLinkableGitRepositories
    for_each: gcp.developerconnect.projects.locations.connections.list
    # python: svc.projects().locations().connections().fetchLinkableGitRepositories(**params).execute()
    calls:
      - action: projects.locations.connections.fetchLinkableGitRepositories
        params:
          connection: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        linkableGitRepositories: '{{ response.linkableGitRepositories }}'
    # required_params: ['connection']
  # Gets details of a single Connection.
  # ── gcp.developerconnect.projects.locations.connections.get [dependent] ──
  - discovery_id: gcp.developerconnect.projects.locations.connections.get
    for_each: gcp.developerconnect.projects.locations.connections.list
    # python: svc.projects().locations().connections().get(**params).execute()
    calls:
      - action: projects.locations.connections.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        bitbucketCloudConfig: '{{ response.bitbucketCloudConfig }}'
        bitbucketDataCenterConfig: '{{ response.bitbucketDataCenterConfig }}'
        createTime: '{{ response.createTime }}'
        cryptoKeyConfig: '{{ response.cryptoKeyConfig }}'
        deleteTime: '{{ response.deleteTime }}'
        disabled: '{{ response.disabled }}'
        gitProxyConfig: '{{ response.gitProxyConfig }}'
        githubConfig: '{{ response.githubConfig }}'
        githubEnterpriseConfig: '{{ response.githubEnterpriseConfig }}'
        gitlabConfig: '{{ response.gitlabConfig }}'
        gitlabEnterpriseConfig: '{{ response.gitlabEnterpriseConfig }}'
        httpConfig: '{{ response.httpConfig }}'
        installationState: '{{ response.installationState }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        reconciling: '{{ response.reconciling }}'
        secureSourceManagerInstanceConfig: '{{ response.secureSourceManagerInstanceConfig }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Fetch the list of branches or tags for a given repository.
  # ── gcp.developerconnect.projects.locations.connections.gitRepositoryLinks.fetchGitRefs [dependent] ──
  - discovery_id: gcp.developerconnect.projects.locations.connections.gitRepositoryLinks.fetchGitRefs
    for_each: gcp.developerconnect.projects.locations.connections.gitRepositoryLinks.list
    # python: svc.projects().locations().connections().gitRepositoryLinks().fetchGitRefs(**params).execute()
    calls:
      - action: projects.locations.connections.gitRepositoryLinks.fetchGitRefs
        params:
          gitRepositoryLink: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        refNames: '{{ response.refNames }}'
    # required_params: ['gitRepositoryLink']
  # Gets details of a single GitRepositoryLink.
  # ── gcp.developerconnect.projects.locations.connections.gitRepositoryLinks.get [dependent] ──
  - discovery_id: gcp.developerconnect.projects.locations.connections.gitRepositoryLinks.get
    for_each: gcp.developerconnect.projects.locations.connections.gitRepositoryLinks.list
    # python: svc.projects().locations().connections().gitRepositoryLinks().get(**params).execute()
    calls:
      - action: projects.locations.connections.gitRepositoryLinks.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        cloneUri: '{{ response.cloneUri }}'
        createTime: '{{ response.createTime }}'
        deleteTime: '{{ response.deleteTime }}'
        gitProxyUri: '{{ response.gitProxyUri }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        reconciling: '{{ response.reconciling }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
        webhookId: '{{ response.webhookId }}'
    # required_params: ['name']
  # Gets a single Deployment Event.
  # ── gcp.developerconnect.projects.locations.insightsConfigs.deploymentEvents.get [dependent] ──
  - discovery_id: gcp.developerconnect.projects.locations.insightsConfigs.deploymentEvents.get
    for_each: gcp.developerconnect.projects.locations.insightsConfigs.deploymentEvents.list
    # python: svc.projects().locations().insightsConfigs().deploymentEvents().get(**params).execute()
    calls:
      - action: projects.locations.insightsConfigs.deploymentEvents.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        artifactDeployments: '{{ response.artifactDeployments }}'
        createTime: '{{ response.createTime }}'
        deployTime: '{{ response.deployTime }}'
        name: '{{ response.name }}'
        runtimeConfig: '{{ response.runtimeConfig }}'
        runtimeDeploymentUri: '{{ response.runtimeDeploymentUri }}'
        state: '{{ response.state }}'
        undeployTime: '{{ response.undeployTime }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets details of a single Insight.
  # ── gcp.developerconnect.projects.locations.insightsConfigs.get [dependent] ──
  - discovery_id: gcp.developerconnect.projects.locations.insightsConfigs.get
    for_each: gcp.developerconnect.projects.locations.insightsConfigs.list
    # python: svc.projects().locations().insightsConfigs().get(**params).execute()
    calls:
      - action: projects.locations.insightsConfigs.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        appHubApplication: '{{ response.appHubApplication }}'
        artifactConfigs: '{{ response.artifactConfigs }}'
        createTime: '{{ response.createTime }}'
        errors: '{{ response.errors }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        projects: '{{ response.projects }}'
        reconciling: '{{ response.reconciling }}'
        runtimeConfigs: '{{ response.runtimeConfigs }}'
        state: '{{ response.state }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.developerconnect.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.developerconnect.projects.locations.operations.get
    for_each: gcp.developerconnect.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
