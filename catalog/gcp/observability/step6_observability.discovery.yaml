# ============================================================
# Discovery YAML — observability (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 23 | independent: 14 | dependent: 9
# ops with fields: 23/23 | dep ops with chain: 9/9
# ============================================================
version: '1.0'
provider: gcp
service: observability

services:
  client: observability
  module: "googleapiclient.discovery.build('observability', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Gets information about a location.
  # ── gcp.observability.folders.locations.get ──
  - discovery_id: gcp.observability.folders.locations.get
    # python: svc.folders().locations().get(**params).execute()
    calls:
      - action: folders.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.observability.folders.locations.list ──
  - discovery_id: gcp.observability.folders.locations.list
    # python: svc.folders().locations().list(**params).execute()
    calls:
      - action: folders.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.observability.folders.locations.operations.list ──
  - discovery_id: gcp.observability.folders.locations.operations.list
    # python: svc.folders().locations().operations().list(**params).execute()
    calls:
      - action: folders.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Gets information about a location.
  # ── gcp.observability.organizations.locations.get ──
  - discovery_id: gcp.observability.organizations.locations.get
    # python: svc.organizations().locations().get(**params).execute()
    calls:
      - action: organizations.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.observability.organizations.locations.list ──
  - discovery_id: gcp.observability.organizations.locations.list
    # python: svc.organizations().locations().list(**params).execute()
    calls:
      - action: organizations.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.observability.organizations.locations.operations.list ──
  - discovery_id: gcp.observability.organizations.locations.operations.list
    # python: svc.organizations().locations().operations().list(**params).execute()
    calls:
      - action: organizations.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # List links of a dataset.
  # ── gcp.observability.projects.locations.buckets.datasets.links.list ──
  - discovery_id: gcp.observability.projects.locations.buckets.datasets.links.list
    # python: svc.projects().locations().buckets().datasets().links().list(**params).execute()
    calls:
      - action: projects.locations.buckets.datasets.links.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.links }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        name: '{{ item.name }}'
  # List datasets of a bucket.
  # ── gcp.observability.projects.locations.buckets.datasets.list ──
  - discovery_id: gcp.observability.projects.locations.buckets.datasets.list
    # python: svc.projects().locations().buckets().datasets().list(**params).execute()
    calls:
      - action: projects.locations.buckets.datasets.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.datasets }}'
      item:
        createTime: '{{ item.createTime }}'
        deleteTime: '{{ item.deleteTime }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        name: '{{ item.name }}'
        purgeTime: '{{ item.purgeTime }}'
  # List views of a dataset.
  # ── gcp.observability.projects.locations.buckets.datasets.views.list ──
  - discovery_id: gcp.observability.projects.locations.buckets.datasets.views.list
    # python: svc.projects().locations().buckets().datasets().views().list(**params).execute()
    calls:
      - action: projects.locations.buckets.datasets.views.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.views }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        name: '{{ item.name }}'
        updateTime: '{{ item.updateTime }}'
  # List buckets of a project in a particular location.
  # ── gcp.observability.projects.locations.buckets.list ──
  - discovery_id: gcp.observability.projects.locations.buckets.list
    # python: svc.projects().locations().buckets().list(**params).execute()
    calls:
      - action: projects.locations.buckets.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.buckets }}'
      item:
        cmekSettings: '{{ item.cmekSettings }}'
        createTime: '{{ item.createTime }}'
        deleteTime: '{{ item.deleteTime }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        name: '{{ item.name }}'
        purgeTime: '{{ item.purgeTime }}'
        updateTime: '{{ item.updateTime }}'
  # Gets information about a location.
  # ── gcp.observability.projects.locations.get ──
  - discovery_id: gcp.observability.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.observability.projects.locations.list ──
  - discovery_id: gcp.observability.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.observability.projects.locations.operations.list ──
  - discovery_id: gcp.observability.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # List TraceScopes of a project in a particular location.
  # ── gcp.observability.projects.locations.traceScopes.list ──
  - discovery_id: gcp.observability.projects.locations.traceScopes.list
    # python: svc.projects().locations().traceScopes().list(**params).execute()
    calls:
      - action: projects.locations.traceScopes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.traceScopes }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        name: '{{ item.name }}'
        resourceNames: '{{ item.resourceNames }}'
        updateTime: '{{ item.updateTime }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.observability.folders.locations.operations.get [dependent] ──
  - discovery_id: gcp.observability.folders.locations.operations.get
    for_each: gcp.observability.projects.locations.operations.list
    # python: svc.folders().locations().operations().get(**params).execute()
    calls:
      - action: folders.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.observability.organizations.locations.operations.get [dependent] ──
  - discovery_id: gcp.observability.organizations.locations.operations.get
    for_each: gcp.observability.projects.locations.operations.list
    # python: svc.organizations().locations().operations().get(**params).execute()
    calls:
      - action: organizations.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Get a dataset.
  # ── gcp.observability.projects.locations.buckets.datasets.get [dependent] ──
  - discovery_id: gcp.observability.projects.locations.buckets.datasets.get
    for_each: gcp.observability.projects.locations.buckets.datasets.list
    # python: svc.projects().locations().buckets().datasets().get(**params).execute()
    calls:
      - action: projects.locations.buckets.datasets.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        deleteTime: '{{ response.deleteTime }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        name: '{{ response.name }}'
        purgeTime: '{{ response.purgeTime }}'
    # required_params: ['name']
  # Get a link.
  # ── gcp.observability.projects.locations.buckets.datasets.links.get [dependent] ──
  - discovery_id: gcp.observability.projects.locations.buckets.datasets.links.get
    for_each: gcp.observability.projects.locations.buckets.datasets.links.list
    # python: svc.projects().locations().buckets().datasets().links().get(**params).execute()
    calls:
      - action: projects.locations.buckets.datasets.links.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Get a view.
  # ── gcp.observability.projects.locations.buckets.datasets.views.get [dependent] ──
  - discovery_id: gcp.observability.projects.locations.buckets.datasets.views.get
    for_each: gcp.observability.projects.locations.buckets.datasets.views.list
    # python: svc.projects().locations().buckets().datasets().views().get(**params).execute()
    calls:
      - action: projects.locations.buckets.datasets.views.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        name: '{{ response.name }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Get bucket resource.
  # ── gcp.observability.projects.locations.buckets.get [dependent] ──
  - discovery_id: gcp.observability.projects.locations.buckets.get
    for_each: gcp.observability.projects.locations.buckets.list
    # python: svc.projects().locations().buckets().get(**params).execute()
    calls:
      - action: projects.locations.buckets.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        cmekSettings: '{{ response.cmekSettings }}'
        createTime: '{{ response.createTime }}'
        deleteTime: '{{ response.deleteTime }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        name: '{{ response.name }}'
        purgeTime: '{{ response.purgeTime }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.observability.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.observability.projects.locations.operations.get
    for_each: gcp.observability.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets details of a single Scope.
  # ── gcp.observability.projects.locations.scopes.get [dependent] ──
  - discovery_id: gcp.observability.projects.locations.scopes.get
    for_each: gcp.observability.projects.locations.traceScopes.list
    # python: svc.projects().locations().scopes().get(**params).execute()
    calls:
      - action: projects.locations.scopes.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        logScope: '{{ response.logScope }}'
        name: '{{ response.name }}'
        traceScope: '{{ response.traceScope }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Get TraceScope resource.
  # ── gcp.observability.projects.locations.traceScopes.get [dependent] ──
  - discovery_id: gcp.observability.projects.locations.traceScopes.get
    for_each: gcp.observability.projects.locations.traceScopes.list
    # python: svc.projects().locations().traceScopes().get(**params).execute()
    calls:
      - action: projects.locations.traceScopes.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        name: '{{ response.name }}'
        resourceNames: '{{ response.resourceNames }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
