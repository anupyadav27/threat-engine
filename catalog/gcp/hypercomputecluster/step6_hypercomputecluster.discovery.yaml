# ============================================================
# Discovery YAML — hypercomputecluster (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 6 | independent: 4 | dependent: 2
# ops with fields: 6/6 | dep ops with chain: 2/2
# ============================================================
version: '1.0'
provider: gcp
service: hypercomputecluster

services:
  client: hypercomputecluster
  module: "googleapiclient.discovery.build('hypercomputecluster', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists Clusters in a given project and location.
  # ── gcp.hypercomputecluster.projects.locations.clusters.list ──
  - discovery_id: gcp.hypercomputecluster.projects.locations.clusters.list
    # python: svc.projects().locations().clusters().list(**params).execute()
    calls:
      - action: projects.locations.clusters.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.clusters }}'
      item:
        computeResources: '{{ item.computeResources }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        networkResources: '{{ item.networkResources }}'
        orchestrator: '{{ item.orchestrator }}'
        reconciling: '{{ item.reconciling }}'
        storageResources: '{{ item.storageResources }}'
        updateTime: '{{ item.updateTime }}'
  # Gets information about a location.
  # ── gcp.hypercomputecluster.projects.locations.get ──
  - discovery_id: gcp.hypercomputecluster.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.hypercomputecluster.projects.locations.list ──
  - discovery_id: gcp.hypercomputecluster.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.hypercomputecluster.projects.locations.operations.list ──
  - discovery_id: gcp.hypercomputecluster.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets details of a single Cluster.
  # ── gcp.hypercomputecluster.projects.locations.clusters.get [dependent] ──
  - discovery_id: gcp.hypercomputecluster.projects.locations.clusters.get
    for_each: gcp.hypercomputecluster.projects.locations.clusters.list
    # python: svc.projects().locations().clusters().get(**params).execute()
    calls:
      - action: projects.locations.clusters.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        computeResources: '{{ response.computeResources }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        networkResources: '{{ response.networkResources }}'
        orchestrator: '{{ response.orchestrator }}'
        reconciling: '{{ response.reconciling }}'
        storageResources: '{{ response.storageResources }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.hypercomputecluster.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.hypercomputecluster.projects.locations.operations.get
    for_each: gcp.hypercomputecluster.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
