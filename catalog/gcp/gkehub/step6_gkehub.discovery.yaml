# ============================================================
# Discovery YAML — gkehub (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 27 | independent: 13 | dependent: 14
# ops with fields: 27/27 | dep ops with chain: 14/14
# ============================================================
version: '1.0'
provider: gcp
service: gkehub

services:
  client: gkehub
  module: "googleapiclient.discovery.build('gkehub', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Returns all fleets within an organization or a project that the caller has access to.
  # ── gcp.gkehub.organizations.locations.fleets.list ──
  - discovery_id: gcp.gkehub.organizations.locations.fleets.list
    # python: svc.organizations().locations().fleets().list(**params).execute()
    calls:
      - action: organizations.locations.fleets.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.fleets }}'
      item:
        createTime: '{{ item.createTime }}'
        defaultClusterConfig: '{{ item.defaultClusterConfig }}'
        deleteTime: '{{ item.deleteTime }}'
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # Lists Features in a given project and location.
  # ── gcp.gkehub.projects.locations.features.list ──
  - discovery_id: gcp.gkehub.projects.locations.features.list
    # python: svc.projects().locations().features().list(**params).execute()
    calls:
      - action: projects.locations.features.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.features }}'
      item:
        createTime: '{{ item.createTime }}'
        deleteTime: '{{ item.deleteTime }}'
        fleetDefaultMemberConfig: '{{ item.fleetDefaultMemberConfig }}'
        labels: '{{ item.labels }}'
        membershipSpecs: '{{ item.membershipSpecs }}'
        membershipStates: '{{ item.membershipStates }}'
        name: '{{ item.name }}'
        resourceState: '{{ item.resourceState }}'
        scopeSpecs: '{{ item.scopeSpecs }}'
        scopeStates: '{{ item.scopeStates }}'
        spec: '{{ item.spec }}'
        state: '{{ item.state }}'
        updateTime: '{{ item.updateTime }}'
  # Returns all fleets within an organization or a project that the caller has access to.
  # ── gcp.gkehub.projects.locations.fleets.list ──
  - discovery_id: gcp.gkehub.projects.locations.fleets.list
    # python: svc.projects().locations().fleets().list(**params).execute()
    calls:
      - action: projects.locations.fleets.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.fleets }}'
      item:
        createTime: '{{ item.createTime }}'
        defaultClusterConfig: '{{ item.defaultClusterConfig }}'
        deleteTime: '{{ item.deleteTime }}'
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # Gets information about a location.
  # ── gcp.gkehub.projects.locations.get ──
  - discovery_id: gcp.gkehub.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.gkehub.projects.locations.list ──
  - discovery_id: gcp.gkehub.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists MembershipBindings.
  # ── gcp.gkehub.projects.locations.memberships.bindings.list ──
  - discovery_id: gcp.gkehub.projects.locations.memberships.bindings.list
    # python: svc.projects().locations().memberships().bindings().list(**params).execute()
    calls:
      - action: projects.locations.memberships.bindings.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.bindings }}'
      item:
        createTime: '{{ item.createTime }}'
        deleteTime: '{{ item.deleteTime }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        scope: '{{ item.scope }}'
        state: '{{ item.state }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # Lists Memberships in a given project and location.
  # ── gcp.gkehub.projects.locations.memberships.list ──
  - discovery_id: gcp.gkehub.projects.locations.memberships.list
    # python: svc.projects().locations().memberships().list(**params).execute()
    calls:
      - action: projects.locations.memberships.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.memberships }}'
      item:
        authority: '{{ item.authority }}'
        clusterTier: '{{ item.clusterTier }}'
        createTime: '{{ item.createTime }}'
        deleteTime: '{{ item.deleteTime }}'
        description: '{{ item.description }}'
        endpoint: '{{ item.endpoint }}'
        externalId: '{{ item.externalId }}'
        labels: '{{ item.labels }}'
        lastConnectionTime: '{{ item.lastConnectionTime }}'
        membershipType: '{{ item.membershipType }}'
        monitoringConfig: '{{ item.monitoringConfig }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
        uniqueId: '{{ item.uniqueId }}'
        updateTime: '{{ item.updateTime }}'
  # Lists all Membership RBACRoleBindings.
  # ── gcp.gkehub.projects.locations.memberships.rbacrolebindings.list ──
  - discovery_id: gcp.gkehub.projects.locations.memberships.rbacrolebindings.list
    # python: svc.projects().locations().memberships().rbacrolebindings().list(**params).execute()
    calls:
      - action: projects.locations.memberships.rbacrolebindings.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.rbacrolebindings }}'
      item:
        createTime: '{{ item.createTime }}'
        deleteTime: '{{ item.deleteTime }}'
        group: '{{ item.group }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        role: '{{ item.role }}'
        state: '{{ item.state }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
        user: '{{ item.user }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.gkehub.projects.locations.operations.list ──
  - discovery_id: gcp.gkehub.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists Scopes.
  # ── gcp.gkehub.projects.locations.scopes.list ──
  - discovery_id: gcp.gkehub.projects.locations.scopes.list
    # python: svc.projects().locations().scopes().list(**params).execute()
    calls:
      - action: projects.locations.scopes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.scopes }}'
      item:
        createTime: '{{ item.createTime }}'
        deleteTime: '{{ item.deleteTime }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        namespaceLabels: '{{ item.namespaceLabels }}'
        state: '{{ item.state }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # Lists permitted Scopes.
  # ── gcp.gkehub.projects.locations.scopes.listPermitted ──
  - discovery_id: gcp.gkehub.projects.locations.scopes.listPermitted
    # python: svc.projects().locations().scopes().listPermitted(**params).execute()
    calls:
      - action: projects.locations.scopes.listPermitted
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.scopes }}'
      item:
        createTime: '{{ item.createTime }}'
        deleteTime: '{{ item.deleteTime }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        namespaceLabels: '{{ item.namespaceLabels }}'
        state: '{{ item.state }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # Lists fleet namespaces.
  # ── gcp.gkehub.projects.locations.scopes.namespaces.list ──
  - discovery_id: gcp.gkehub.projects.locations.scopes.namespaces.list
    # python: svc.projects().locations().scopes().namespaces().list(**params).execute()
    calls:
      - action: projects.locations.scopes.namespaces.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.namespaces }}'
      item:
        createTime: '{{ item.createTime }}'
        deleteTime: '{{ item.deleteTime }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        namespaceLabels: '{{ item.namespaceLabels }}'
        scope: '{{ item.scope }}'
        state: '{{ item.state }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # Lists all Scope RBACRoleBindings.
  # ── gcp.gkehub.projects.locations.scopes.rbacrolebindings.list ──
  - discovery_id: gcp.gkehub.projects.locations.scopes.rbacrolebindings.list
    # python: svc.projects().locations().scopes().rbacrolebindings().list(**params).execute()
    calls:
      - action: projects.locations.scopes.rbacrolebindings.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.rbacrolebindings }}'
      item:
        createTime: '{{ item.createTime }}'
        deleteTime: '{{ item.deleteTime }}'
        group: '{{ item.group }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        role: '{{ item.role }}'
        state: '{{ item.state }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
        user: '{{ item.user }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets details of a single Feature.
  # ── gcp.gkehub.projects.locations.features.get [dependent] ──
  - discovery_id: gcp.gkehub.projects.locations.features.get
    for_each: gcp.gkehub.projects.locations.features.list
    # python: svc.projects().locations().features().get(**params).execute()
    calls:
      - action: projects.locations.features.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        deleteTime: '{{ response.deleteTime }}'
        fleetDefaultMemberConfig: '{{ response.fleetDefaultMemberConfig }}'
        labels: '{{ response.labels }}'
        membershipSpecs: '{{ response.membershipSpecs }}'
        membershipStates: '{{ response.membershipStates }}'
        name: '{{ response.name }}'
        resourceState: '{{ response.resourceState }}'
        scopeSpecs: '{{ response.scopeSpecs }}'
        scopeStates: '{{ response.scopeStates }}'
        spec: '{{ response.spec }}'
        state: '{{ response.state }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.gkehub.projects.locations.features.getIamPolicy [dependent] ──
  - discovery_id: gcp.gkehub.projects.locations.features.getIamPolicy
    for_each: gcp.gkehub.projects.locations.features.list
    # python: svc.projects().locations().features().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.features.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Returns the details of a fleet.
  # ── gcp.gkehub.projects.locations.fleets.get [dependent] ──
  - discovery_id: gcp.gkehub.projects.locations.fleets.get
    for_each: gcp.gkehub.organizations.locations.fleets.list
    # python: svc.projects().locations().fleets().get(**params).execute()
    calls:
      - action: projects.locations.fleets.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        defaultClusterConfig: '{{ response.defaultClusterConfig }}'
        deleteTime: '{{ response.deleteTime }}'
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Returns the details of a MembershipBinding.
  # ── gcp.gkehub.projects.locations.memberships.bindings.get [dependent] ──
  - discovery_id: gcp.gkehub.projects.locations.memberships.bindings.get
    for_each: gcp.gkehub.projects.locations.memberships.bindings.list
    # python: svc.projects().locations().memberships().bindings().get(**params).execute()
    calls:
      - action: projects.locations.memberships.bindings.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        deleteTime: '{{ response.deleteTime }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        scope: '{{ response.scope }}'
        state: '{{ response.state }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Generates the manifest for deployment of the GKE connect agent. **This method is used internally by 
  # ── gcp.gkehub.projects.locations.memberships.generateConnectManifest [dependent] ──
  - discovery_id: gcp.gkehub.projects.locations.memberships.generateConnectManifest
    for_each: gcp.gkehub.projects.locations.memberships.list
    # python: svc.projects().locations().memberships().generateConnectManifest(**params).execute()
    calls:
      - action: projects.locations.memberships.generateConnectManifest
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        manifest: '{{ response.manifest }}'
    # required_params: ['name']
  # Gets the details of a Membership.
  # ── gcp.gkehub.projects.locations.memberships.get [dependent] ──
  - discovery_id: gcp.gkehub.projects.locations.memberships.get
    for_each: gcp.gkehub.projects.locations.memberships.list
    # python: svc.projects().locations().memberships().get(**params).execute()
    calls:
      - action: projects.locations.memberships.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        authority: '{{ response.authority }}'
        clusterTier: '{{ response.clusterTier }}'
        createTime: '{{ response.createTime }}'
        deleteTime: '{{ response.deleteTime }}'
        description: '{{ response.description }}'
        endpoint: '{{ response.endpoint }}'
        externalId: '{{ response.externalId }}'
        labels: '{{ response.labels }}'
        lastConnectionTime: '{{ response.lastConnectionTime }}'
        membershipType: '{{ response.membershipType }}'
        monitoringConfig: '{{ response.monitoringConfig }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
        uniqueId: '{{ response.uniqueId }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.gkehub.projects.locations.memberships.getIamPolicy [dependent] ──
  - discovery_id: gcp.gkehub.projects.locations.memberships.getIamPolicy
    for_each: gcp.gkehub.projects.locations.memberships.list
    # python: svc.projects().locations().memberships().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.memberships.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Returns the details of a Membership RBACRoleBinding.
  # ── gcp.gkehub.projects.locations.memberships.rbacrolebindings.get [dependent] ──
  - discovery_id: gcp.gkehub.projects.locations.memberships.rbacrolebindings.get
    for_each: gcp.gkehub.projects.locations.memberships.rbacrolebindings.list
    # python: svc.projects().locations().memberships().rbacrolebindings().get(**params).execute()
    calls:
      - action: projects.locations.memberships.rbacrolebindings.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        deleteTime: '{{ response.deleteTime }}'
        group: '{{ response.group }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        role: '{{ response.role }}'
        state: '{{ response.state }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
        user: '{{ response.user }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.gkehub.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.gkehub.projects.locations.operations.get
    for_each: gcp.gkehub.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Returns the details of a Scope.
  # ── gcp.gkehub.projects.locations.scopes.get [dependent] ──
  - discovery_id: gcp.gkehub.projects.locations.scopes.get
    for_each: gcp.gkehub.projects.locations.scopes.listPermitted
    # python: svc.projects().locations().scopes().get(**params).execute()
    calls:
      - action: projects.locations.scopes.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        deleteTime: '{{ response.deleteTime }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        namespaceLabels: '{{ response.namespaceLabels }}'
        state: '{{ response.state }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.gkehub.projects.locations.scopes.getIamPolicy [dependent] ──
  - discovery_id: gcp.gkehub.projects.locations.scopes.getIamPolicy
    for_each: gcp.gkehub.projects.locations.scopes.listPermitted
    # python: svc.projects().locations().scopes().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.scopes.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Lists Memberships bound to a Scope. The response includes relevant Memberships from all regions.
  # ── gcp.gkehub.projects.locations.scopes.listMemberships [dependent] ──
  - discovery_id: gcp.gkehub.projects.locations.scopes.listMemberships
    for_each: gcp.gkehub.projects.locations.scopes.listPermitted
    # python: svc.projects().locations().scopes().listMemberships(**params).execute()
    calls:
      - action: projects.locations.scopes.listMemberships
        params:
          scopeName: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.scopes }}'
      item:
        authority: '{{ item.authority }}'
        clusterTier: '{{ item.clusterTier }}'
        createTime: '{{ item.createTime }}'
        deleteTime: '{{ item.deleteTime }}'
        description: '{{ item.description }}'
        endpoint: '{{ item.endpoint }}'
        externalId: '{{ item.externalId }}'
        labels: '{{ item.labels }}'
        lastConnectionTime: '{{ item.lastConnectionTime }}'
        membershipType: '{{ item.membershipType }}'
        monitoringConfig: '{{ item.monitoringConfig }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
        uniqueId: '{{ item.uniqueId }}'
        updateTime: '{{ item.updateTime }}'
    # required_params: ['scopeName']
  # Returns the details of a fleet namespace.
  # ── gcp.gkehub.projects.locations.scopes.namespaces.get [dependent] ──
  - discovery_id: gcp.gkehub.projects.locations.scopes.namespaces.get
    for_each: gcp.gkehub.projects.locations.scopes.namespaces.list
    # python: svc.projects().locations().scopes().namespaces().get(**params).execute()
    calls:
      - action: projects.locations.scopes.namespaces.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        deleteTime: '{{ response.deleteTime }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        namespaceLabels: '{{ response.namespaceLabels }}'
        scope: '{{ response.scope }}'
        state: '{{ response.state }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Returns the details of a Scope RBACRoleBinding.
  # ── gcp.gkehub.projects.locations.scopes.rbacrolebindings.get [dependent] ──
  - discovery_id: gcp.gkehub.projects.locations.scopes.rbacrolebindings.get
    for_each: gcp.gkehub.projects.locations.memberships.rbacrolebindings.list
    # python: svc.projects().locations().scopes().rbacrolebindings().get(**params).execute()
    calls:
      - action: projects.locations.scopes.rbacrolebindings.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        deleteTime: '{{ response.deleteTime }}'
        group: '{{ response.group }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        role: '{{ response.role }}'
        state: '{{ response.state }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
        user: '{{ response.user }}'
    # required_params: ['name']
