# ============================================================
# Discovery YAML — iam (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 45 | independent: 16 | dependent: 29
# ops with fields: 45/45 | dep ops with chain: 29/29
# ============================================================
version: '1.0'
provider: gcp
service: iam

services:
  client: iam
  module: "googleapiclient.discovery.build('iam', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists all non-deleted WorkforcePools under the specified parent. If `show_deleted` is set to `true`,
  # ── gcp.iam.locations.workforcePools.list ──
  - discovery_id: gcp.iam.locations.workforcePools.list
    # python: svc.locations().workforcePools().list(**params).execute()
    calls:
      - action: locations.workforcePools.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.workforcePools }}'
      item:
        accessRestrictions: '{{ item.accessRestrictions }}'
        description: '{{ item.description }}'
        disabled: '{{ item.disabled }}'
        displayName: '{{ item.displayName }}'
        expireTime: '{{ item.expireTime }}'
        name: '{{ item.name }}'
        parent: '{{ item.parent }}'
        sessionDuration: '{{ item.sessionDuration }}'
        state: '{{ item.state }}'
  # Lists all non-deleted WorkforcePoolProviderKeys in a WorkforcePoolProvider. If `show_deleted` is set
  # ── gcp.iam.locations.workforcePools.providers.keys.list ──
  - discovery_id: gcp.iam.locations.workforcePools.providers.keys.list
    # python: svc.locations().workforcePools().providers().keys().list(**params).execute()
    calls:
      - action: locations.workforcePools.providers.keys.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.keys }}'
      item:
        expireTime: '{{ item.expireTime }}'
        keyData: '{{ item.keyData }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
        use: '{{ item.use }}'
  # Lists all non-deleted WorkforcePoolProviders in a WorkforcePool. If `show_deleted` is set to `true`,
  # ── gcp.iam.locations.workforcePools.providers.list ──
  - discovery_id: gcp.iam.locations.workforcePools.providers.list
    # python: svc.locations().workforcePools().providers().list(**params).execute()
    calls:
      - action: locations.workforcePools.providers.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.providers }}'
      item:
        attributeCondition: '{{ item.attributeCondition }}'
        attributeMapping: '{{ item.attributeMapping }}'
        description: '{{ item.description }}'
        detailedAuditLogging: '{{ item.detailedAuditLogging }}'
        disabled: '{{ item.disabled }}'
        displayName: '{{ item.displayName }}'
        expireTime: '{{ item.expireTime }}'
        extendedAttributesOauth2Client: '{{ item.extendedAttributesOauth2Client }}'
        extraAttributesOauth2Client: '{{ item.extraAttributesOauth2Client }}'
        name: '{{ item.name }}'
        oidc: '{{ item.oidc }}'
        saml: '{{ item.saml }}'
        scimUsage: '{{ item.scimUsage }}'
        state: '{{ item.state }}'
  # Gemini Enterprise only. Lists all non-deleted WorkforcePoolProviderScimTenants in a WorkforcePoolPro
  # ── gcp.iam.locations.workforcePools.providers.scimTenants.list ──
  - discovery_id: gcp.iam.locations.workforcePools.providers.scimTenants.list
    # python: svc.locations().workforcePools().providers().scimTenants().list(**params).execute()
    calls:
      - action: locations.workforcePools.providers.scimTenants.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.scimTenants }}'
      item:
        baseUri: '{{ item.baseUri }}'
        claimMapping: '{{ item.claimMapping }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        name: '{{ item.name }}'
        purgeTime: '{{ item.purgeTime }}'
        serviceAgent: '{{ item.serviceAgent }}'
        state: '{{ item.state }}'
  # Gemini Enterprise only. Lists all non-deleted WorkforcePoolProviderScimTokenss in a WorkforcePoolPro
  # ── gcp.iam.locations.workforcePools.providers.scimTenants.tokens.list ──
  - discovery_id: gcp.iam.locations.workforcePools.providers.scimTenants.tokens.list
    # python: svc.locations().workforcePools().providers().scimTenants().tokens().list(**params).execute()
    calls:
      - action: locations.workforcePools.providers.scimTenants.tokens.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.tokens }}'
      item:
        displayName: '{{ item.displayName }}'
        name: '{{ item.name }}'
        securityToken: '{{ item.securityToken }}'
        state: '{{ item.state }}'
  # Lists every predefined Role that IAM supports, or every custom role that is defined for an organizat
  # ── gcp.iam.organizations.roles.list ──
  - discovery_id: gcp.iam.organizations.roles.list
    # python: svc.organizations().roles().list(**params).execute()
    calls:
      - action: organizations.roles.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.roles }}'
      item:
        deleted: '{{ item.deleted }}'
        description: '{{ item.description }}'
        includedPermissions: '{{ item.includedPermissions }}'
        name: '{{ item.name }}'
        stage: '{{ item.stage }}'
        title: '{{ item.title }}'
  # Lists all OauthClientCredentials in an OauthClient.
  # ── gcp.iam.projects.locations.oauthClients.credentials.list ──
  - discovery_id: gcp.iam.projects.locations.oauthClients.credentials.list
    # python: svc.projects().locations().oauthClients().credentials().list(**params).execute()
    calls:
      - action: projects.locations.oauthClients.credentials.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.credentials }}'
      item:
        clientSecret: '{{ item.clientSecret }}'
        disabled: '{{ item.disabled }}'
        displayName: '{{ item.displayName }}'
        name: '{{ item.name }}'
  # Lists all non-deleted OauthClients in a project. If `show_deleted` is set to `true`, then deleted Oa
  # ── gcp.iam.projects.locations.oauthClients.list ──
  - discovery_id: gcp.iam.projects.locations.oauthClients.list
    # python: svc.projects().locations().oauthClients().list(**params).execute()
    calls:
      - action: projects.locations.oauthClients.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.oauthClients }}'
      item:
        allowedGrantTypes: '{{ item.allowedGrantTypes }}'
        allowedRedirectUris: '{{ item.allowedRedirectUris }}'
        allowedScopes: '{{ item.allowedScopes }}'
        clientId: '{{ item.clientId }}'
        clientType: '{{ item.clientType }}'
        description: '{{ item.description }}'
        disabled: '{{ item.disabled }}'
        displayName: '{{ item.displayName }}'
        expireTime: '{{ item.expireTime }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
  # Lists all non-deleted WorkloadIdentityPools in a project. If `show_deleted` is set to `true`, then d
  # ── gcp.iam.projects.locations.workloadIdentityPools.list ──
  - discovery_id: gcp.iam.projects.locations.workloadIdentityPools.list
    # python: svc.projects().locations().workloadIdentityPools().list(**params).execute()
    calls:
      - action: projects.locations.workloadIdentityPools.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.workloadIdentityPools }}'
      item:
        description: '{{ item.description }}'
        disabled: '{{ item.disabled }}'
        displayName: '{{ item.displayName }}'
        expireTime: '{{ item.expireTime }}'
        inlineCertificateIssuanceConfig: '{{ item.inlineCertificateIssuanceConfig }}'
        inlineTrustConfig: '{{ item.inlineTrustConfig }}'
        mode: '{{ item.mode }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
  # Lists all non-deleted WorkloadIdentityPoolNamespaces in a workload identity pool. If `show_deleted` 
  # ── gcp.iam.projects.locations.workloadIdentityPools.namespaces.list ──
  - discovery_id: gcp.iam.projects.locations.workloadIdentityPools.namespaces.list
    # python: svc.projects().locations().workloadIdentityPools().namespaces().list(**params).execute()
    calls:
      - action: projects.locations.workloadIdentityPools.namespaces.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.namespaces }}'
      item:
        description: '{{ item.description }}'
        disabled: '{{ item.disabled }}'
        expireTime: '{{ item.expireTime }}'
        name: '{{ item.name }}'
        ownerService: '{{ item.ownerService }}'
        state: '{{ item.state }}'
  # Lists all non-deleted WorkloadIdentityPoolManagedIdentitys in a namespace. If `show_deleted` is set 
  # ── gcp.iam.projects.locations.workloadIdentityPools.namespaces.managedIdentities.list ──
  - discovery_id: gcp.iam.projects.locations.workloadIdentityPools.namespaces.managedIdentities.list
    # python: svc.projects().locations().workloadIdentityPools().namespaces().managedIdentities().list(**params).execute()
    calls:
      - action: projects.locations.workloadIdentityPools.namespaces.managedIdentities.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.managedIdentities }}'
      item:
        description: '{{ item.description }}'
        disabled: '{{ item.disabled }}'
        expireTime: '{{ item.expireTime }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
  # Lists all non-deleted WorkloadIdentityPoolProviderKeys in a project. If show_deleted is set to `true
  # ── gcp.iam.projects.locations.workloadIdentityPools.providers.keys.list ──
  - discovery_id: gcp.iam.projects.locations.workloadIdentityPools.providers.keys.list
    # python: svc.projects().locations().workloadIdentityPools().providers().keys().list(**params).execute()
    calls:
      - action: projects.locations.workloadIdentityPools.providers.keys.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.keys }}'
      item:
        expireTime: '{{ item.expireTime }}'
        keyData: '{{ item.keyData }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
        use: '{{ item.use }}'
  # Lists all non-deleted WorkloadIdentityPoolProviders in a WorkloadIdentityPool. If `show_deleted` is 
  # ── gcp.iam.projects.locations.workloadIdentityPools.providers.list ──
  - discovery_id: gcp.iam.projects.locations.workloadIdentityPools.providers.list
    # python: svc.projects().locations().workloadIdentityPools().providers().list(**params).execute()
    calls:
      - action: projects.locations.workloadIdentityPools.providers.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.providers }}'
      item:
        attributeCondition: '{{ item.attributeCondition }}'
        attributeMapping: '{{ item.attributeMapping }}'
        aws: '{{ item.aws }}'
        description: '{{ item.description }}'
        disabled: '{{ item.disabled }}'
        displayName: '{{ item.displayName }}'
        expireTime: '{{ item.expireTime }}'
        name: '{{ item.name }}'
        oidc: '{{ item.oidc }}'
        saml: '{{ item.saml }}'
        state: '{{ item.state }}'
        x509: '{{ item.x509 }}'
  # Lists every predefined Role that IAM supports, or every custom role that is defined for an organizat
  # ── gcp.iam.projects.roles.list ──
  - discovery_id: gcp.iam.projects.roles.list
    # python: svc.projects().roles().list(**params).execute()
    calls:
      - action: projects.roles.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.roles }}'
      item:
        deleted: '{{ item.deleted }}'
        description: '{{ item.description }}'
        includedPermissions: '{{ item.includedPermissions }}'
        name: '{{ item.name }}'
        stage: '{{ item.stage }}'
        title: '{{ item.title }}'
  # Lists every ServiceAccount that belongs to a specific project.
  # ── gcp.iam.projects.serviceAccounts.list ──
  - discovery_id: gcp.iam.projects.serviceAccounts.list
    # python: svc.projects().serviceAccounts().list(**params).execute()
    calls:
      - action: projects.serviceAccounts.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.serviceAccounts }}'
      item:
        description: '{{ item.description }}'
        disabled: '{{ item.disabled }}'
        displayName: '{{ item.displayName }}'
        email: '{{ item.email }}'
        name: '{{ item.name }}'
        oauth2ClientId: '{{ item.oauth2ClientId }}'
        projectId: '{{ item.projectId }}'
        uniqueId: '{{ item.uniqueId }}'
  # Lists every predefined Role that IAM supports, or every custom role that is defined for an organizat
  # ── gcp.iam.roles.list ──
  - discovery_id: gcp.iam.roles.list
    # python: svc.roles().list(**params).execute()
    calls:
      - action: roles.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.roles }}'
      item:
        deleted: '{{ item.deleted }}'
        description: '{{ item.description }}'
        includedPermissions: '{{ item.includedPermissions }}'
        name: '{{ item.name }}'
        stage: '{{ item.stage }}'
        title: '{{ item.title }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets an individual WorkforcePool.
  # ── gcp.iam.locations.workforcePools.get [dependent] ──
  - discovery_id: gcp.iam.locations.workforcePools.get
    for_each: gcp.iam.locations.workforcePools.list
    # python: svc.locations().workforcePools().get(**params).execute()
    calls:
      - action: locations.workforcePools.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        accessRestrictions: '{{ response.accessRestrictions }}'
        description: '{{ response.description }}'
        disabled: '{{ response.disabled }}'
        displayName: '{{ response.displayName }}'
        expireTime: '{{ response.expireTime }}'
        name: '{{ response.name }}'
        parent: '{{ response.parent }}'
        sessionDuration: '{{ response.sessionDuration }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.iam.locations.workforcePools.operations.get [dependent] ──
  - discovery_id: gcp.iam.locations.workforcePools.operations.get
    for_each: gcp.iam.locations.workforcePools.list
    # python: svc.locations().workforcePools().operations().get(**params).execute()
    calls:
      - action: locations.workforcePools.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets an individual WorkforcePoolProvider.
  # ── gcp.iam.locations.workforcePools.providers.get [dependent] ──
  - discovery_id: gcp.iam.locations.workforcePools.providers.get
    for_each: gcp.iam.projects.locations.workloadIdentityPools.providers.list
    # python: svc.locations().workforcePools().providers().get(**params).execute()
    calls:
      - action: locations.workforcePools.providers.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        attributeCondition: '{{ response.attributeCondition }}'
        attributeMapping: '{{ response.attributeMapping }}'
        description: '{{ response.description }}'
        detailedAuditLogging: '{{ response.detailedAuditLogging }}'
        disabled: '{{ response.disabled }}'
        displayName: '{{ response.displayName }}'
        expireTime: '{{ response.expireTime }}'
        extendedAttributesOauth2Client: '{{ response.extendedAttributesOauth2Client }}'
        extraAttributesOauth2Client: '{{ response.extraAttributesOauth2Client }}'
        name: '{{ response.name }}'
        oidc: '{{ response.oidc }}'
        saml: '{{ response.saml }}'
        scimUsage: '{{ response.scimUsage }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Gets a WorkforcePoolProviderKey.
  # ── gcp.iam.locations.workforcePools.providers.keys.get [dependent] ──
  - discovery_id: gcp.iam.locations.workforcePools.providers.keys.get
    for_each: gcp.iam.projects.serviceAccounts.keys.list
    # python: svc.locations().workforcePools().providers().keys().get(**params).execute()
    calls:
      - action: locations.workforcePools.providers.keys.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        expireTime: '{{ response.expireTime }}'
        keyData: '{{ response.keyData }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
        use: '{{ response.use }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.iam.locations.workforcePools.providers.keys.operations.get [dependent] ──
  - discovery_id: gcp.iam.locations.workforcePools.providers.keys.operations.get
    for_each: gcp.iam.projects.serviceAccounts.keys.list
    # python: svc.locations().workforcePools().providers().keys().operations().get(**params).execute()
    calls:
      - action: locations.workforcePools.providers.keys.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.iam.locations.workforcePools.providers.operations.get [dependent] ──
  - discovery_id: gcp.iam.locations.workforcePools.providers.operations.get
    for_each: gcp.iam.projects.locations.workloadIdentityPools.providers.list
    # python: svc.locations().workforcePools().providers().operations().get(**params).execute()
    calls:
      - action: locations.workforcePools.providers.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gemini Enterprise only. Gets an individual WorkforcePoolProviderScimTenant.
  # ── gcp.iam.locations.workforcePools.providers.scimTenants.get [dependent] ──
  - discovery_id: gcp.iam.locations.workforcePools.providers.scimTenants.get
    for_each: gcp.iam.locations.workforcePools.providers.scimTenants.list
    # python: svc.locations().workforcePools().providers().scimTenants().get(**params).execute()
    calls:
      - action: locations.workforcePools.providers.scimTenants.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        baseUri: '{{ response.baseUri }}'
        claimMapping: '{{ response.claimMapping }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        name: '{{ response.name }}'
        purgeTime: '{{ response.purgeTime }}'
        serviceAgent: '{{ response.serviceAgent }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Gemini Enterprise only. Gets an individual WorkforcePoolProviderScimToken.
  # ── gcp.iam.locations.workforcePools.providers.scimTenants.tokens.get [dependent] ──
  - discovery_id: gcp.iam.locations.workforcePools.providers.scimTenants.tokens.get
    for_each: gcp.iam.locations.workforcePools.providers.scimTenants.tokens.list
    # python: svc.locations().workforcePools().providers().scimTenants().tokens().get(**params).execute()
    calls:
      - action: locations.workforcePools.providers.scimTenants.tokens.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        name: '{{ response.name }}'
        securityToken: '{{ response.securityToken }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.iam.locations.workforcePools.subjects.operations.get [dependent] ──
  - discovery_id: gcp.iam.locations.workforcePools.subjects.operations.get
    for_each: gcp.iam.locations.workforcePools.list
    # python: svc.locations().workforcePools().subjects().operations().get(**params).execute()
    calls:
      - action: locations.workforcePools.subjects.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets the definition of a Role.
  # ── gcp.iam.organizations.roles.get [dependent] ──
  - discovery_id: gcp.iam.organizations.roles.get
    for_each: gcp.iam.projects.roles.list
    # python: svc.organizations().roles().get(**params).execute()
    calls:
      - action: organizations.roles.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        deleted: '{{ response.deleted }}'
        description: '{{ response.description }}'
        includedPermissions: '{{ response.includedPermissions }}'
        name: '{{ response.name }}'
        stage: '{{ response.stage }}'
        title: '{{ response.title }}'
    # required_params: ['name']
  # Gets an individual OauthClientCredential.
  # ── gcp.iam.projects.locations.oauthClients.credentials.get [dependent] ──
  - discovery_id: gcp.iam.projects.locations.oauthClients.credentials.get
    for_each: gcp.iam.projects.locations.oauthClients.credentials.list
    # python: svc.projects().locations().oauthClients().credentials().get(**params).execute()
    calls:
      - action: projects.locations.oauthClients.credentials.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        clientSecret: '{{ response.clientSecret }}'
        disabled: '{{ response.disabled }}'
        displayName: '{{ response.displayName }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Gets an individual OauthClient.
  # ── gcp.iam.projects.locations.oauthClients.get [dependent] ──
  - discovery_id: gcp.iam.projects.locations.oauthClients.get
    for_each: gcp.iam.projects.locations.oauthClients.list
    # python: svc.projects().locations().oauthClients().get(**params).execute()
    calls:
      - action: projects.locations.oauthClients.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        allowedGrantTypes: '{{ response.allowedGrantTypes }}'
        allowedRedirectUris: '{{ response.allowedRedirectUris }}'
        allowedScopes: '{{ response.allowedScopes }}'
        clientId: '{{ response.clientId }}'
        clientType: '{{ response.clientType }}'
        description: '{{ response.description }}'
        disabled: '{{ response.disabled }}'
        displayName: '{{ response.displayName }}'
        expireTime: '{{ response.expireTime }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Gets an individual WorkloadIdentityPool.
  # ── gcp.iam.projects.locations.workloadIdentityPools.get [dependent] ──
  - discovery_id: gcp.iam.projects.locations.workloadIdentityPools.get
    for_each: gcp.iam.projects.locations.workloadIdentityPools.list
    # python: svc.projects().locations().workloadIdentityPools().get(**params).execute()
    calls:
      - action: projects.locations.workloadIdentityPools.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        description: '{{ response.description }}'
        disabled: '{{ response.disabled }}'
        displayName: '{{ response.displayName }}'
        expireTime: '{{ response.expireTime }}'
        inlineCertificateIssuanceConfig: '{{ response.inlineCertificateIssuanceConfig }}'
        inlineTrustConfig: '{{ response.inlineTrustConfig }}'
        mode: '{{ response.mode }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Gets an individual WorkloadIdentityPoolNamespace.
  # ── gcp.iam.projects.locations.workloadIdentityPools.namespaces.get [dependent] ──
  - discovery_id: gcp.iam.projects.locations.workloadIdentityPools.namespaces.get
    for_each: gcp.iam.projects.locations.workloadIdentityPools.namespaces.list
    # python: svc.projects().locations().workloadIdentityPools().namespaces().get(**params).execute()
    calls:
      - action: projects.locations.workloadIdentityPools.namespaces.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        description: '{{ response.description }}'
        disabled: '{{ response.disabled }}'
        expireTime: '{{ response.expireTime }}'
        name: '{{ response.name }}'
        ownerService: '{{ response.ownerService }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Gets an individual WorkloadIdentityPoolManagedIdentity.
  # ── gcp.iam.projects.locations.workloadIdentityPools.namespaces.managedIdentities.get [dependent] ──
  - discovery_id: gcp.iam.projects.locations.workloadIdentityPools.namespaces.managedIdentities.get
    for_each: gcp.iam.projects.locations.workloadIdentityPools.namespaces.managedIdentities.listAttestationRules
    # python: svc.projects().locations().workloadIdentityPools().namespaces().managedIdentities().get(**params).execute()
    calls:
      - action: projects.locations.workloadIdentityPools.namespaces.managedIdentities.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        description: '{{ response.description }}'
        disabled: '{{ response.disabled }}'
        expireTime: '{{ response.expireTime }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # List all AttestationRule on a WorkloadIdentityPoolManagedIdentity.
  # ── gcp.iam.projects.locations.workloadIdentityPools.namespaces.managedIdentities.listAttestationRules [dependent] ──
  - discovery_id: gcp.iam.projects.locations.workloadIdentityPools.namespaces.managedIdentities.listAttestationRules
    for_each: gcp.iam.projects.locations.workloadIdentityPools.namespaces.list
    # python: svc.projects().locations().workloadIdentityPools().namespaces().managedIdentities().listAttestationRules(**params).execute()
    calls:
      - action: projects.locations.workloadIdentityPools.namespaces.managedIdentities.listAttestationRules
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.managedIdentities }}'
      item:
        googleCloudResource: '{{ item.googleCloudResource }}'
    # required_params: ['resource']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.iam.projects.locations.workloadIdentityPools.namespaces.managedIdentities.operations.get [dependent] ──
  - discovery_id: gcp.iam.projects.locations.workloadIdentityPools.namespaces.managedIdentities.operations.get
    for_each: gcp.iam.projects.locations.workloadIdentityPools.namespaces.managedIdentities.listAttestationRules
    # python: svc.projects().locations().workloadIdentityPools().namespaces().managedIdentities().operations().get(**params).execute()
    calls:
      - action: projects.locations.workloadIdentityPools.namespaces.managedIdentities.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.iam.projects.locations.workloadIdentityPools.namespaces.managedIdentities.workloadSources.operations.get [dependent] ──
  - discovery_id: gcp.iam.projects.locations.workloadIdentityPools.namespaces.managedIdentities.workloadSources.operations.get
    for_each: gcp.iam.projects.locations.workloadIdentityPools.namespaces.managedIdentities.listAttestationRules
    # python: svc.projects().locations().workloadIdentityPools().namespaces().managedIdentities().workloadSources().operations().get(**params).execute()
    calls:
      - action: projects.locations.workloadIdentityPools.namespaces.managedIdentities.workloadSources.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.iam.projects.locations.workloadIdentityPools.namespaces.operations.get [dependent] ──
  - discovery_id: gcp.iam.projects.locations.workloadIdentityPools.namespaces.operations.get
    for_each: gcp.iam.projects.locations.workloadIdentityPools.namespaces.list
    # python: svc.projects().locations().workloadIdentityPools().namespaces().operations().get(**params).execute()
    calls:
      - action: projects.locations.workloadIdentityPools.namespaces.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.iam.projects.locations.workloadIdentityPools.operations.get [dependent] ──
  - discovery_id: gcp.iam.projects.locations.workloadIdentityPools.operations.get
    for_each: gcp.iam.projects.locations.workloadIdentityPools.list
    # python: svc.projects().locations().workloadIdentityPools().operations().get(**params).execute()
    calls:
      - action: projects.locations.workloadIdentityPools.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets an individual WorkloadIdentityPoolProvider.
  # ── gcp.iam.projects.locations.workloadIdentityPools.providers.get [dependent] ──
  - discovery_id: gcp.iam.projects.locations.workloadIdentityPools.providers.get
    for_each: gcp.iam.projects.locations.workloadIdentityPools.providers.list
    # python: svc.projects().locations().workloadIdentityPools().providers().get(**params).execute()
    calls:
      - action: projects.locations.workloadIdentityPools.providers.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        attributeCondition: '{{ response.attributeCondition }}'
        attributeMapping: '{{ response.attributeMapping }}'
        aws: '{{ response.aws }}'
        description: '{{ response.description }}'
        disabled: '{{ response.disabled }}'
        displayName: '{{ response.displayName }}'
        expireTime: '{{ response.expireTime }}'
        name: '{{ response.name }}'
        oidc: '{{ response.oidc }}'
        saml: '{{ response.saml }}'
        state: '{{ response.state }}'
        x509: '{{ response.x509 }}'
    # required_params: ['name']
  # Gets an individual WorkloadIdentityPoolProviderKey.
  # ── gcp.iam.projects.locations.workloadIdentityPools.providers.keys.get [dependent] ──
  - discovery_id: gcp.iam.projects.locations.workloadIdentityPools.providers.keys.get
    for_each: gcp.iam.projects.serviceAccounts.keys.list
    # python: svc.projects().locations().workloadIdentityPools().providers().keys().get(**params).execute()
    calls:
      - action: projects.locations.workloadIdentityPools.providers.keys.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        expireTime: '{{ response.expireTime }}'
        keyData: '{{ response.keyData }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
        use: '{{ response.use }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.iam.projects.locations.workloadIdentityPools.providers.keys.operations.get [dependent] ──
  - discovery_id: gcp.iam.projects.locations.workloadIdentityPools.providers.keys.operations.get
    for_each: gcp.iam.projects.serviceAccounts.keys.list
    # python: svc.projects().locations().workloadIdentityPools().providers().keys().operations().get(**params).execute()
    calls:
      - action: projects.locations.workloadIdentityPools.providers.keys.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.iam.projects.locations.workloadIdentityPools.providers.operations.get [dependent] ──
  - discovery_id: gcp.iam.projects.locations.workloadIdentityPools.providers.operations.get
    for_each: gcp.iam.projects.locations.workloadIdentityPools.providers.list
    # python: svc.projects().locations().workloadIdentityPools().providers().operations().get(**params).execute()
    calls:
      - action: projects.locations.workloadIdentityPools.providers.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets the definition of a Role.
  # ── gcp.iam.projects.roles.get [dependent] ──
  - discovery_id: gcp.iam.projects.roles.get
    for_each: gcp.iam.projects.roles.list
    # python: svc.projects().roles().get(**params).execute()
    calls:
      - action: projects.roles.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        deleted: '{{ response.deleted }}'
        description: '{{ response.description }}'
        includedPermissions: '{{ response.includedPermissions }}'
        name: '{{ response.name }}'
        stage: '{{ response.stage }}'
        title: '{{ response.title }}'
    # required_params: ['name']
  # Gets a ServiceAccount.
  # ── gcp.iam.projects.serviceAccounts.get [dependent] ──
  - discovery_id: gcp.iam.projects.serviceAccounts.get
    for_each: gcp.iam.projects.serviceAccounts.list
    # python: svc.projects().serviceAccounts().get(**params).execute()
    calls:
      - action: projects.serviceAccounts.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        description: '{{ response.description }}'
        disabled: '{{ response.disabled }}'
        displayName: '{{ response.displayName }}'
        email: '{{ response.email }}'
        name: '{{ response.name }}'
        oauth2ClientId: '{{ response.oauth2ClientId }}'
        projectId: '{{ response.projectId }}'
        uniqueId: '{{ response.uniqueId }}'
    # required_params: ['name']
  # Gets a ServiceAccountKey.
  # ── gcp.iam.projects.serviceAccounts.keys.get [dependent] ──
  - discovery_id: gcp.iam.projects.serviceAccounts.keys.get
    for_each: gcp.iam.projects.serviceAccounts.keys.list
    # python: svc.projects().serviceAccounts().keys().get(**params).execute()
    calls:
      - action: projects.serviceAccounts.keys.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        disableReason: '{{ response.disableReason }}'
        disabled: '{{ response.disabled }}'
        extendedStatus: '{{ response.extendedStatus }}'
        keyAlgorithm: '{{ response.keyAlgorithm }}'
        keyOrigin: '{{ response.keyOrigin }}'
        keyType: '{{ response.keyType }}'
        name: '{{ response.name }}'
        privateKeyData: '{{ response.privateKeyData }}'
        privateKeyType: '{{ response.privateKeyType }}'
        publicKeyData: '{{ response.publicKeyData }}'
        validAfterTime: '{{ response.validAfterTime }}'
        validBeforeTime: '{{ response.validBeforeTime }}'
    # required_params: ['name']
  # Lists every ServiceAccountKey for a service account.
  # ── gcp.iam.projects.serviceAccounts.keys.list [dependent] ──
  - discovery_id: gcp.iam.projects.serviceAccounts.keys.list
    for_each: gcp.iam.projects.serviceAccounts.list
    # python: svc.projects().serviceAccounts().keys().list(**params).execute()
    calls:
      - action: projects.serviceAccounts.keys.list
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.keys }}'
      item:
        disableReason: '{{ item.disableReason }}'
        disabled: '{{ item.disabled }}'
        extendedStatus: '{{ item.extendedStatus }}'
        keyAlgorithm: '{{ item.keyAlgorithm }}'
        keyOrigin: '{{ item.keyOrigin }}'
        keyType: '{{ item.keyType }}'
        name: '{{ item.name }}'
        privateKeyData: '{{ item.privateKeyData }}'
        privateKeyType: '{{ item.privateKeyType }}'
        publicKeyData: '{{ item.publicKeyData }}'
        validAfterTime: '{{ item.validAfterTime }}'
        validBeforeTime: '{{ item.validBeforeTime }}'
    # required_params: ['name']
  # Gets the definition of a Role.
  # ── gcp.iam.roles.get [dependent] ──
  - discovery_id: gcp.iam.roles.get
    for_each: gcp.iam.projects.roles.list
    # python: svc.roles().get(**params).execute()
    calls:
      - action: roles.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        deleted: '{{ response.deleted }}'
        description: '{{ response.description }}'
        includedPermissions: '{{ response.includedPermissions }}'
        name: '{{ response.name }}'
        stage: '{{ response.stage }}'
        title: '{{ response.title }}'
    # required_params: ['name']
