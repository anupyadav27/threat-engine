# ============================================================
# Discovery YAML — securityposture (v1)
# Generated: 2026-02-21T03:06:41Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 13 | independent: 7 | dependent: 6
# ops with fields: 13/13 | dep ops with chain: 6/6
# ============================================================
version: '1.0'
provider: gcp
service: securityposture

services:
  client: securityposture
  module: "googleapiclient.discovery.build('securityposture', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.securityposture.organizations.locations.operations.list ──
  - discovery_id: gcp.securityposture.organizations.locations.operations.list
    # python: svc.organizations().locations().operations().list(**params).execute()
    calls:
      - action: organizations.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists every PostureDeployment in a project and location.
  # ── gcp.securityposture.organizations.locations.postureDeployments.list ──
  - discovery_id: gcp.securityposture.organizations.locations.postureDeployments.list
    # python: svc.organizations().locations().postureDeployments().list(**params).execute()
    calls:
      - action: organizations.locations.postureDeployments.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.postureDeployments }}'
      item:
        annotations: '{{ item.annotations }}'
        categories: '{{ item.categories }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        desiredPostureId: '{{ item.desiredPostureId }}'
        desiredPostureRevisionId: '{{ item.desiredPostureRevisionId }}'
        failureMessage: '{{ item.failureMessage }}'
        name: '{{ item.name }}'
        postureId: '{{ item.postureId }}'
        postureRevisionId: '{{ item.postureRevisionId }}'
        reconciling: '{{ item.reconciling }}'
        state: '{{ item.state }}'
        targetResource: '{{ item.targetResource }}'
        updateTime: '{{ item.updateTime }}'
  # Lists every PostureTemplate in a given organization and location.
  # ── gcp.securityposture.organizations.locations.postureTemplates.list ──
  - discovery_id: gcp.securityposture.organizations.locations.postureTemplates.list
    # python: svc.organizations().locations().postureTemplates().list(**params).execute()
    calls:
      - action: organizations.locations.postureTemplates.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.postureTemplates }}'
      item:
        categories: '{{ item.categories }}'
        description: '{{ item.description }}'
        name: '{{ item.name }}'
        policySets: '{{ item.policySets }}'
        revisionId: '{{ item.revisionId }}'
        state: '{{ item.state }}'
  # Lists the most recent revisions of all Posture resources in a specified organization and location.
  # ── gcp.securityposture.organizations.locations.postures.list ──
  - discovery_id: gcp.securityposture.organizations.locations.postures.list
    # python: svc.organizations().locations().postures().list(**params).execute()
    calls:
      - action: organizations.locations.postures.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.postures }}'
      item:
        annotations: '{{ item.annotations }}'
        categories: '{{ item.categories }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        name: '{{ item.name }}'
        policySets: '{{ item.policySets }}'
        reconciling: '{{ item.reconciling }}'
        revisionId: '{{ item.revisionId }}'
        state: '{{ item.state }}'
        updateTime: '{{ item.updateTime }}'
  # Lists every Report in a given organization and location.
  # ── gcp.securityposture.organizations.locations.reports.list ──
  - discovery_id: gcp.securityposture.organizations.locations.reports.list
    # python: svc.organizations().locations().reports().list(**params).execute()
    calls:
      - action: organizations.locations.reports.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.reports }}'
      item:
        createTime: '{{ item.createTime }}'
        iacValidationReport: '{{ item.iacValidationReport }}'
        name: '{{ item.name }}'
        updateTime: '{{ item.updateTime }}'
  # Gets information about a location.
  # ── gcp.securityposture.projects.locations.get ──
  - discovery_id: gcp.securityposture.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.securityposture.projects.locations.list ──
  - discovery_id: gcp.securityposture.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.securityposture.organizations.locations.operations.get [dependent] ──
  - discovery_id: gcp.securityposture.organizations.locations.operations.get
    for_each: gcp.securityposture.organizations.locations.operations.list
    # python: svc.organizations().locations().operations().get(**params).execute()
    calls:
      - action: organizations.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets details for a PostureDeployment.
  # ── gcp.securityposture.organizations.locations.postureDeployments.get [dependent] ──
  - discovery_id: gcp.securityposture.organizations.locations.postureDeployments.get
    for_each: gcp.securityposture.organizations.locations.postureDeployments.list
    # python: svc.organizations().locations().postureDeployments().get(**params).execute()
    calls:
      - action: organizations.locations.postureDeployments.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        categories: '{{ response.categories }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        desiredPostureId: '{{ response.desiredPostureId }}'
        desiredPostureRevisionId: '{{ response.desiredPostureRevisionId }}'
        failureMessage: '{{ response.failureMessage }}'
        name: '{{ response.name }}'
        postureId: '{{ response.postureId }}'
        postureRevisionId: '{{ response.postureRevisionId }}'
        reconciling: '{{ response.reconciling }}'
        state: '{{ response.state }}'
        targetResource: '{{ response.targetResource }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets a single revision of a PostureTemplate.
  # ── gcp.securityposture.organizations.locations.postureTemplates.get [dependent] ──
  - discovery_id: gcp.securityposture.organizations.locations.postureTemplates.get
    for_each: gcp.securityposture.organizations.locations.postureTemplates.list
    # python: svc.organizations().locations().postureTemplates().get(**params).execute()
    calls:
      - action: organizations.locations.postureTemplates.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        categories: '{{ response.categories }}'
        description: '{{ response.description }}'
        name: '{{ response.name }}'
        policySets: '{{ response.policySets }}'
        revisionId: '{{ response.revisionId }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Gets a single revision of a Posture.
  # ── gcp.securityposture.organizations.locations.postures.get [dependent] ──
  - discovery_id: gcp.securityposture.organizations.locations.postures.get
    for_each: gcp.securityposture.organizations.locations.postures.list
    # python: svc.organizations().locations().postures().get(**params).execute()
    calls:
      - action: organizations.locations.postures.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        categories: '{{ response.categories }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        name: '{{ response.name }}'
        policySets: '{{ response.policySets }}'
        reconciling: '{{ response.reconciling }}'
        revisionId: '{{ response.revisionId }}'
        state: '{{ response.state }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Lists all revisions of a single Posture.
  # ── gcp.securityposture.organizations.locations.postures.listRevisions [dependent] ──
  - discovery_id: gcp.securityposture.organizations.locations.postures.listRevisions
    for_each: gcp.securityposture.organizations.locations.postures.list
    # python: svc.organizations().locations().postures().listRevisions(**params).execute()
    calls:
      - action: organizations.locations.postures.listRevisions
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.postures }}'
      item:
        annotations: '{{ item.annotations }}'
        categories: '{{ item.categories }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        name: '{{ item.name }}'
        policySets: '{{ item.policySets }}'
        reconciling: '{{ item.reconciling }}'
        revisionId: '{{ item.revisionId }}'
        state: '{{ item.state }}'
        updateTime: '{{ item.updateTime }}'
    # required_params: ['name']
  # Gets details for a Report.
  # ── gcp.securityposture.organizations.locations.reports.get [dependent] ──
  - discovery_id: gcp.securityposture.organizations.locations.reports.get
    for_each: gcp.securityposture.organizations.locations.reports.list
    # python: svc.organizations().locations().reports().get(**params).execute()
    calls:
      - action: organizations.locations.reports.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        iacValidationReport: '{{ response.iacValidationReport }}'
        name: '{{ response.name }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
