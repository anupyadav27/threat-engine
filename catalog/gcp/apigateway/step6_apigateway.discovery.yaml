# ============================================================
# Discovery YAML — apigateway (v1)
# Generated: 2026-02-21T03:06:38Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 13 | independent: 6 | dependent: 7
# ops with fields: 13/13 | dep ops with chain: 7/7
# ============================================================
version: '1.0'
provider: gcp
service: apigateway

services:
  client: apigateway
  module: "googleapiclient.discovery.build('apigateway', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists ApiConfigs in a given project and location.
  # ── gcp.apigateway.projects.locations.apis.configs.list ──
  - discovery_id: gcp.apigateway.projects.locations.apis.configs.list
    # python: svc.projects().locations().apis().configs().list(**params).execute()
    calls:
      - action: projects.locations.apis.configs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.configs }}'
      item:
        createTime: '{{ item.createTime }}'
        displayName: '{{ item.displayName }}'
        gatewayServiceAccount: '{{ item.gatewayServiceAccount }}'
        grpcServices: '{{ item.grpcServices }}'
        labels: '{{ item.labels }}'
        managedServiceConfigs: '{{ item.managedServiceConfigs }}'
        name: '{{ item.name }}'
        openapiDocuments: '{{ item.openapiDocuments }}'
        serviceConfigId: '{{ item.serviceConfigId }}'
        state: '{{ item.state }}'
        updateTime: '{{ item.updateTime }}'
  # Lists Apis in a given project and location.
  # ── gcp.apigateway.projects.locations.apis.list ──
  - discovery_id: gcp.apigateway.projects.locations.apis.list
    # python: svc.projects().locations().apis().list(**params).execute()
    calls:
      - action: projects.locations.apis.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.apis }}'
      item:
        createTime: '{{ item.createTime }}'
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        managedService: '{{ item.managedService }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
        updateTime: '{{ item.updateTime }}'
  # Lists Gateways in a given project and location.
  # ── gcp.apigateway.projects.locations.gateways.list ──
  - discovery_id: gcp.apigateway.projects.locations.gateways.list
    # python: svc.projects().locations().gateways().list(**params).execute()
    calls:
      - action: projects.locations.gateways.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.gateways }}'
      item:
        apiConfig: '{{ item.apiConfig }}'
        createTime: '{{ item.createTime }}'
        defaultHostname: '{{ item.defaultHostname }}'
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
        updateTime: '{{ item.updateTime }}'
  # Gets information about a location.
  # ── gcp.apigateway.projects.locations.get ──
  - discovery_id: gcp.apigateway.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.apigateway.projects.locations.list ──
  - discovery_id: gcp.apigateway.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.apigateway.projects.locations.operations.list ──
  - discovery_id: gcp.apigateway.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets details of a single ApiConfig.
  # ── gcp.apigateway.projects.locations.apis.configs.get [dependent] ──
  - discovery_id: gcp.apigateway.projects.locations.apis.configs.get
    for_each: gcp.apigateway.projects.locations.apis.configs.list
    # python: svc.projects().locations().apis().configs().get(**params).execute()
    calls:
      - action: projects.locations.apis.configs.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        displayName: '{{ response.displayName }}'
        gatewayServiceAccount: '{{ response.gatewayServiceAccount }}'
        grpcServices: '{{ response.grpcServices }}'
        labels: '{{ response.labels }}'
        managedServiceConfigs: '{{ response.managedServiceConfigs }}'
        name: '{{ response.name }}'
        openapiDocuments: '{{ response.openapiDocuments }}'
        serviceConfigId: '{{ response.serviceConfigId }}'
        state: '{{ response.state }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.apigateway.projects.locations.apis.configs.getIamPolicy [dependent] ──
  - discovery_id: gcp.apigateway.projects.locations.apis.configs.getIamPolicy
    for_each: gcp.apigateway.projects.locations.apis.configs.list
    # python: svc.projects().locations().apis().configs().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.apis.configs.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Gets details of a single Api.
  # ── gcp.apigateway.projects.locations.apis.get [dependent] ──
  - discovery_id: gcp.apigateway.projects.locations.apis.get
    for_each: gcp.apigateway.projects.locations.apis.list
    # python: svc.projects().locations().apis().get(**params).execute()
    calls:
      - action: projects.locations.apis.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        managedService: '{{ response.managedService }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.apigateway.projects.locations.apis.getIamPolicy [dependent] ──
  - discovery_id: gcp.apigateway.projects.locations.apis.getIamPolicy
    for_each: gcp.apigateway.projects.locations.apis.list
    # python: svc.projects().locations().apis().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.apis.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Gets details of a single Gateway.
  # ── gcp.apigateway.projects.locations.gateways.get [dependent] ──
  - discovery_id: gcp.apigateway.projects.locations.gateways.get
    for_each: gcp.apigateway.projects.locations.gateways.list
    # python: svc.projects().locations().gateways().get(**params).execute()
    calls:
      - action: projects.locations.gateways.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiConfig: '{{ response.apiConfig }}'
        createTime: '{{ response.createTime }}'
        defaultHostname: '{{ response.defaultHostname }}'
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.apigateway.projects.locations.gateways.getIamPolicy [dependent] ──
  - discovery_id: gcp.apigateway.projects.locations.gateways.getIamPolicy
    for_each: gcp.apigateway.projects.locations.gateways.list
    # python: svc.projects().locations().gateways().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.gateways.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.apigateway.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.apigateway.projects.locations.operations.get
    for_each: gcp.apigateway.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
