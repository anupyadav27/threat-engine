# ============================================================
# Discovery YAML — logging (v2)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 111 | independent: 68 | dependent: 43
# ops with fields: 102/111 | dep ops with chain: 38/43
# ============================================================
version: '1.0'
provider: gcp
service: logging

services:
  client: logging
  module: "googleapiclient.discovery.build('logging', 'v2')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists all the exclusions on the _Default sink in a parent resource.
  # ── gcp.logging.billingAccounts.exclusions.list ──
  - discovery_id: gcp.logging.billingAccounts.exclusions.list
    # python: svc.billingAccounts().exclusions().list(**params).execute()
    calls:
      - action: billingAccounts.exclusions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.exclusions }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        disabled: '{{ item.disabled }}'
        filter: '{{ item.filter }}'
        name: '{{ item.name }}'
        updateTime: '{{ item.updateTime }}'
  # Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configure
  # ── gcp.logging.billingAccounts.getCmekSettings ──
  - discovery_id: gcp.logging.billingAccounts.getCmekSettings
    # python: svc.billingAccounts().getCmekSettings(**params).execute()
    calls:
      - action: billingAccounts.getCmekSettings
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        kmsKeyName: '{{ response.kmsKeyName }}'
        kmsKeyVersionName: '{{ response.kmsKeyVersionName }}'
        name: '{{ response.name }}'
        serviceAccountId: '{{ response.serviceAccountId }}'
  # Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, 
  # ── gcp.logging.billingAccounts.getSettings ──
  - discovery_id: gcp.logging.billingAccounts.getSettings
    # python: svc.billingAccounts().getSettings(**params).execute()
    calls:
      - action: billingAccounts.getSettings
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        defaultSinkConfig: '{{ response.defaultSinkConfig }}'
        disableDefaultSink: '{{ response.disableDefaultSink }}'
        kmsKeyName: '{{ response.kmsKeyName }}'
        kmsServiceAccountId: '{{ response.kmsServiceAccountId }}'
        loggingServiceAccountId: '{{ response.loggingServiceAccountId }}'
        name: '{{ response.name }}'
        storageLocation: '{{ response.storageLocation }}'
  # Lists links.
  # ── gcp.logging.billingAccounts.locations.buckets.links.list ──
  - discovery_id: gcp.logging.billingAccounts.locations.buckets.links.list
    # python: svc.billingAccounts().locations().buckets().links().list(**params).execute()
    calls:
      - action: billingAccounts.locations.buckets.links.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.links }}'
      item:
        bigqueryDataset: '{{ item.bigqueryDataset }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        lifecycleState: '{{ item.lifecycleState }}'
        name: '{{ item.name }}'
  # Lists log buckets.
  # ── gcp.logging.billingAccounts.locations.buckets.list ──
  - discovery_id: gcp.logging.billingAccounts.locations.buckets.list
    # python: svc.billingAccounts().locations().buckets().list(**params).execute()
    calls:
      - action: billingAccounts.locations.buckets.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.buckets }}'
      item:
        analyticsEnabled: '{{ item.analyticsEnabled }}'
        cmekSettings: '{{ item.cmekSettings }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        indexConfigs: '{{ item.indexConfigs }}'
        lifecycleState: '{{ item.lifecycleState }}'
        locked: '{{ item.locked }}'
        name: '{{ item.name }}'
        restrictedFields: '{{ item.restrictedFields }}'
        retentionDays: '{{ item.retentionDays }}'
        updateTime: '{{ item.updateTime }}'
  # Lists views on a log bucket.
  # ── gcp.logging.billingAccounts.locations.buckets.views.list ──
  - discovery_id: gcp.logging.billingAccounts.locations.buckets.views.list
    # python: svc.billingAccounts().locations().buckets().views().list(**params).execute()
    calls:
      - action: billingAccounts.locations.buckets.views.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.views }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        filter: '{{ item.filter }}'
        name: '{{ item.name }}'
        updateTime: '{{ item.updateTime }}'
  # Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries
  # ── gcp.logging.billingAccounts.locations.buckets.views.logs.list ──
  - discovery_id: gcp.logging.billingAccounts.locations.buckets.views.logs.list
    # python: svc.billingAccounts().locations().buckets().views().logs().list(**params).execute()
    calls:
      - action: billingAccounts.locations.buckets.views.logs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.logs }}'
  # Gets information about a location.
  # ── gcp.logging.billingAccounts.locations.get ──
  - discovery_id: gcp.logging.billingAccounts.locations.get
    # python: svc.billingAccounts().locations().get(**params).execute()
    calls:
      - action: billingAccounts.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.logging.billingAccounts.locations.list ──
  - discovery_id: gcp.logging.billingAccounts.locations.list
    # python: svc.billingAccounts().locations().list(**params).execute()
    calls:
      - action: billingAccounts.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.logging.billingAccounts.locations.operations.list ──
  - discovery_id: gcp.logging.billingAccounts.locations.operations.list
    # python: svc.billingAccounts().locations().operations().list(**params).execute()
    calls:
      - action: billingAccounts.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists the RecentQueries that were created by the user making the request.
  # ── gcp.logging.billingAccounts.locations.recentQueries.list ──
  - discovery_id: gcp.logging.billingAccounts.locations.recentQueries.list
    # python: svc.billingAccounts().locations().recentQueries().list(**params).execute()
    calls:
      - action: billingAccounts.locations.recentQueries.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.recentQueries }}'
      item:
        lastRunTime: '{{ item.lastRunTime }}'
        loggingQuery: '{{ item.loggingQuery }}'
        name: '{{ item.name }}'
        opsAnalyticsQuery: '{{ item.opsAnalyticsQuery }}'
  # Lists the SavedQueries that were created by the user making the request.
  # ── gcp.logging.billingAccounts.locations.savedQueries.list ──
  - discovery_id: gcp.logging.billingAccounts.locations.savedQueries.list
    # python: svc.billingAccounts().locations().savedQueries().list(**params).execute()
    calls:
      - action: billingAccounts.locations.savedQueries.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.savedQueries }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        loggingQuery: '{{ item.loggingQuery }}'
        name: '{{ item.name }}'
        opsAnalyticsQuery: '{{ item.opsAnalyticsQuery }}'
        updateTime: '{{ item.updateTime }}'
        visibility: '{{ item.visibility }}'
  # Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries
  # ── gcp.logging.billingAccounts.logs.list ──
  - discovery_id: gcp.logging.billingAccounts.logs.list
    # python: svc.billingAccounts().logs().list(**params).execute()
    calls:
      - action: billingAccounts.logs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.logs }}'
  # Lists sinks.
  # ── gcp.logging.billingAccounts.sinks.list ──
  - discovery_id: gcp.logging.billingAccounts.sinks.list
    # python: svc.billingAccounts().sinks().list(**params).execute()
    calls:
      - action: billingAccounts.sinks.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.sinks }}'
      item:
        bigqueryOptions: '{{ item.bigqueryOptions }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        destination: '{{ item.destination }}'
        disabled: '{{ item.disabled }}'
        exclusions: '{{ item.exclusions }}'
        filter: '{{ item.filter }}'
        includeChildren: '{{ item.includeChildren }}'
        interceptChildren: '{{ item.interceptChildren }}'
        name: '{{ item.name }}'
        outputVersionFormat: '{{ item.outputVersionFormat }}'
        resourceName: '{{ item.resourceName }}'
        updateTime: '{{ item.updateTime }}'
        writerIdentity: '{{ item.writerIdentity }}'
  # Lists log entries. Use this method to retrieve log entries that originated from a project/folder/org
  # ── gcp.logging.entries.list ──
  - discovery_id: gcp.logging.entries.list
    # python: svc.entries().list(**params).execute()
    calls:
      - action: entries.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.entries }}'
      item:
        apphub: '{{ item.apphub }}'
        apphubDestination: '{{ item.apphubDestination }}'
        apphubSource: '{{ item.apphubSource }}'
        errorGroups: '{{ item.errorGroups }}'
        httpRequest: '{{ item.httpRequest }}'
        insertId: '{{ item.insertId }}'
        jsonPayload: '{{ item.jsonPayload }}'
        labels: '{{ item.labels }}'
        logName: '{{ item.logName }}'
        metadata: '{{ item.metadata }}'
        operation: '{{ item.operation }}'
        protoPayload: '{{ item.protoPayload }}'
        receiveTimestamp: '{{ item.receiveTimestamp }}'
        resource: '{{ item.resource }}'
        severity: '{{ item.severity }}'
        sourceLocation: '{{ item.sourceLocation }}'
        spanId: '{{ item.spanId }}'
        split: '{{ item.split }}'
        textPayload: '{{ item.textPayload }}'
        timestamp: '{{ item.timestamp }}'
        trace: '{{ item.trace }}'
        traceSampled: '{{ item.traceSampled }}'
  # Lists all the exclusions on the _Default sink in a parent resource.
  # ── gcp.logging.exclusions.list ──
  - discovery_id: gcp.logging.exclusions.list
    # python: svc.exclusions().list(**params).execute()
    calls:
      - action: exclusions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.exclusions }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        disabled: '{{ item.disabled }}'
        filter: '{{ item.filter }}'
        name: '{{ item.name }}'
        updateTime: '{{ item.updateTime }}'
  # Lists all the exclusions on the _Default sink in a parent resource.
  # ── gcp.logging.folders.exclusions.list ──
  - discovery_id: gcp.logging.folders.exclusions.list
    # python: svc.folders().exclusions().list(**params).execute()
    calls:
      - action: folders.exclusions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.exclusions }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        disabled: '{{ item.disabled }}'
        filter: '{{ item.filter }}'
        name: '{{ item.name }}'
        updateTime: '{{ item.updateTime }}'
  # Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configure
  # ── gcp.logging.folders.getCmekSettings ──
  - discovery_id: gcp.logging.folders.getCmekSettings
    # python: svc.folders().getCmekSettings(**params).execute()
    calls:
      - action: folders.getCmekSettings
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        kmsKeyName: '{{ response.kmsKeyName }}'
        kmsKeyVersionName: '{{ response.kmsKeyVersionName }}'
        name: '{{ response.name }}'
        serviceAccountId: '{{ response.serviceAccountId }}'
  # Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, 
  # ── gcp.logging.folders.getSettings ──
  - discovery_id: gcp.logging.folders.getSettings
    # python: svc.folders().getSettings(**params).execute()
    calls:
      - action: folders.getSettings
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        defaultSinkConfig: '{{ response.defaultSinkConfig }}'
        disableDefaultSink: '{{ response.disableDefaultSink }}'
        kmsKeyName: '{{ response.kmsKeyName }}'
        kmsServiceAccountId: '{{ response.kmsServiceAccountId }}'
        loggingServiceAccountId: '{{ response.loggingServiceAccountId }}'
        name: '{{ response.name }}'
        storageLocation: '{{ response.storageLocation }}'
  # Lists links.
  # ── gcp.logging.folders.locations.buckets.links.list ──
  - discovery_id: gcp.logging.folders.locations.buckets.links.list
    # python: svc.folders().locations().buckets().links().list(**params).execute()
    calls:
      - action: folders.locations.buckets.links.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.links }}'
      item:
        bigqueryDataset: '{{ item.bigqueryDataset }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        lifecycleState: '{{ item.lifecycleState }}'
        name: '{{ item.name }}'
  # Lists log buckets.
  # ── gcp.logging.folders.locations.buckets.list ──
  - discovery_id: gcp.logging.folders.locations.buckets.list
    # python: svc.folders().locations().buckets().list(**params).execute()
    calls:
      - action: folders.locations.buckets.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.buckets }}'
      item:
        analyticsEnabled: '{{ item.analyticsEnabled }}'
        cmekSettings: '{{ item.cmekSettings }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        indexConfigs: '{{ item.indexConfigs }}'
        lifecycleState: '{{ item.lifecycleState }}'
        locked: '{{ item.locked }}'
        name: '{{ item.name }}'
        restrictedFields: '{{ item.restrictedFields }}'
        retentionDays: '{{ item.retentionDays }}'
        updateTime: '{{ item.updateTime }}'
  # Lists views on a log bucket.
  # ── gcp.logging.folders.locations.buckets.views.list ──
  - discovery_id: gcp.logging.folders.locations.buckets.views.list
    # python: svc.folders().locations().buckets().views().list(**params).execute()
    calls:
      - action: folders.locations.buckets.views.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.views }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        filter: '{{ item.filter }}'
        name: '{{ item.name }}'
        updateTime: '{{ item.updateTime }}'
  # Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries
  # ── gcp.logging.folders.locations.buckets.views.logs.list ──
  - discovery_id: gcp.logging.folders.locations.buckets.views.logs.list
    # python: svc.folders().locations().buckets().views().logs().list(**params).execute()
    calls:
      - action: folders.locations.buckets.views.logs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.logs }}'
  # Gets information about a location.
  # ── gcp.logging.folders.locations.get ──
  - discovery_id: gcp.logging.folders.locations.get
    # python: svc.folders().locations().get(**params).execute()
    calls:
      - action: folders.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.logging.folders.locations.list ──
  - discovery_id: gcp.logging.folders.locations.list
    # python: svc.folders().locations().list(**params).execute()
    calls:
      - action: folders.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists log scopes.
  # ── gcp.logging.folders.locations.logScopes.list ──
  - discovery_id: gcp.logging.folders.locations.logScopes.list
    # python: svc.folders().locations().logScopes().list(**params).execute()
    calls:
      - action: folders.locations.logScopes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.logScopes }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        name: '{{ item.name }}'
        resourceNames: '{{ item.resourceNames }}'
        updateTime: '{{ item.updateTime }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.logging.folders.locations.operations.list ──
  - discovery_id: gcp.logging.folders.locations.operations.list
    # python: svc.folders().locations().operations().list(**params).execute()
    calls:
      - action: folders.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists the RecentQueries that were created by the user making the request.
  # ── gcp.logging.folders.locations.recentQueries.list ──
  - discovery_id: gcp.logging.folders.locations.recentQueries.list
    # python: svc.folders().locations().recentQueries().list(**params).execute()
    calls:
      - action: folders.locations.recentQueries.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.recentQueries }}'
      item:
        lastRunTime: '{{ item.lastRunTime }}'
        loggingQuery: '{{ item.loggingQuery }}'
        name: '{{ item.name }}'
        opsAnalyticsQuery: '{{ item.opsAnalyticsQuery }}'
  # Lists the SavedQueries that were created by the user making the request.
  # ── gcp.logging.folders.locations.savedQueries.list ──
  - discovery_id: gcp.logging.folders.locations.savedQueries.list
    # python: svc.folders().locations().savedQueries().list(**params).execute()
    calls:
      - action: folders.locations.savedQueries.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.savedQueries }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        loggingQuery: '{{ item.loggingQuery }}'
        name: '{{ item.name }}'
        opsAnalyticsQuery: '{{ item.opsAnalyticsQuery }}'
        updateTime: '{{ item.updateTime }}'
        visibility: '{{ item.visibility }}'
  # Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries
  # ── gcp.logging.folders.logs.list ──
  - discovery_id: gcp.logging.folders.logs.list
    # python: svc.folders().logs().list(**params).execute()
    calls:
      - action: folders.logs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.logs }}'
  # Lists sinks.
  # ── gcp.logging.folders.sinks.list ──
  - discovery_id: gcp.logging.folders.sinks.list
    # python: svc.folders().sinks().list(**params).execute()
    calls:
      - action: folders.sinks.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.sinks }}'
      item:
        bigqueryOptions: '{{ item.bigqueryOptions }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        destination: '{{ item.destination }}'
        disabled: '{{ item.disabled }}'
        exclusions: '{{ item.exclusions }}'
        filter: '{{ item.filter }}'
        includeChildren: '{{ item.includeChildren }}'
        interceptChildren: '{{ item.interceptChildren }}'
        name: '{{ item.name }}'
        outputVersionFormat: '{{ item.outputVersionFormat }}'
        resourceName: '{{ item.resourceName }}'
        updateTime: '{{ item.updateTime }}'
        writerIdentity: '{{ item.writerIdentity }}'
  # Lists links.
  # ── gcp.logging.locations.buckets.links.list ──
  - discovery_id: gcp.logging.locations.buckets.links.list
    # python: svc.locations().buckets().links().list(**params).execute()
    calls:
      - action: locations.buckets.links.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.links }}'
      item:
        bigqueryDataset: '{{ item.bigqueryDataset }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        lifecycleState: '{{ item.lifecycleState }}'
        name: '{{ item.name }}'
  # Lists log buckets.
  # ── gcp.logging.locations.buckets.list ──
  - discovery_id: gcp.logging.locations.buckets.list
    # python: svc.locations().buckets().list(**params).execute()
    calls:
      - action: locations.buckets.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.buckets }}'
      item:
        analyticsEnabled: '{{ item.analyticsEnabled }}'
        cmekSettings: '{{ item.cmekSettings }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        indexConfigs: '{{ item.indexConfigs }}'
        lifecycleState: '{{ item.lifecycleState }}'
        locked: '{{ item.locked }}'
        name: '{{ item.name }}'
        restrictedFields: '{{ item.restrictedFields }}'
        retentionDays: '{{ item.retentionDays }}'
        updateTime: '{{ item.updateTime }}'
  # Lists views on a log bucket.
  # ── gcp.logging.locations.buckets.views.list ──
  - discovery_id: gcp.logging.locations.buckets.views.list
    # python: svc.locations().buckets().views().list(**params).execute()
    calls:
      - action: locations.buckets.views.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.views }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        filter: '{{ item.filter }}'
        name: '{{ item.name }}'
        updateTime: '{{ item.updateTime }}'
  # Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries
  # ── gcp.logging.logs.list ──
  - discovery_id: gcp.logging.logs.list
    # python: svc.logs().list(**params).execute()
    calls:
      - action: logs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.logs }}'
  # Lists the descriptors for monitored resource types used by Logging.
  # ── gcp.logging.monitoredResourceDescriptors.list ──
  - discovery_id: gcp.logging.monitoredResourceDescriptors.list
    # python: svc.monitoredResourceDescriptors().list(**params).execute()
    calls:
      - action: monitoredResourceDescriptors.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.monitoredResourceDescriptors }}'
      item:
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        launchStage: '{{ item.launchStage }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
  # Lists all the exclusions on the _Default sink in a parent resource.
  # ── gcp.logging.organizations.exclusions.list ──
  - discovery_id: gcp.logging.organizations.exclusions.list
    # python: svc.organizations().exclusions().list(**params).execute()
    calls:
      - action: organizations.exclusions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.exclusions }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        disabled: '{{ item.disabled }}'
        filter: '{{ item.filter }}'
        name: '{{ item.name }}'
        updateTime: '{{ item.updateTime }}'
  # Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configure
  # ── gcp.logging.organizations.getCmekSettings ──
  - discovery_id: gcp.logging.organizations.getCmekSettings
    # python: svc.organizations().getCmekSettings(**params).execute()
    calls:
      - action: organizations.getCmekSettings
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        kmsKeyName: '{{ response.kmsKeyName }}'
        kmsKeyVersionName: '{{ response.kmsKeyVersionName }}'
        name: '{{ response.name }}'
        serviceAccountId: '{{ response.serviceAccountId }}'
  # Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, 
  # ── gcp.logging.organizations.getSettings ──
  - discovery_id: gcp.logging.organizations.getSettings
    # python: svc.organizations().getSettings(**params).execute()
    calls:
      - action: organizations.getSettings
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        defaultSinkConfig: '{{ response.defaultSinkConfig }}'
        disableDefaultSink: '{{ response.disableDefaultSink }}'
        kmsKeyName: '{{ response.kmsKeyName }}'
        kmsServiceAccountId: '{{ response.kmsServiceAccountId }}'
        loggingServiceAccountId: '{{ response.loggingServiceAccountId }}'
        name: '{{ response.name }}'
        storageLocation: '{{ response.storageLocation }}'
  # Lists links.
  # ── gcp.logging.organizations.locations.buckets.links.list ──
  - discovery_id: gcp.logging.organizations.locations.buckets.links.list
    # python: svc.organizations().locations().buckets().links().list(**params).execute()
    calls:
      - action: organizations.locations.buckets.links.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.links }}'
      item:
        bigqueryDataset: '{{ item.bigqueryDataset }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        lifecycleState: '{{ item.lifecycleState }}'
        name: '{{ item.name }}'
  # Lists log buckets.
  # ── gcp.logging.organizations.locations.buckets.list ──
  - discovery_id: gcp.logging.organizations.locations.buckets.list
    # python: svc.organizations().locations().buckets().list(**params).execute()
    calls:
      - action: organizations.locations.buckets.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.buckets }}'
      item:
        analyticsEnabled: '{{ item.analyticsEnabled }}'
        cmekSettings: '{{ item.cmekSettings }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        indexConfigs: '{{ item.indexConfigs }}'
        lifecycleState: '{{ item.lifecycleState }}'
        locked: '{{ item.locked }}'
        name: '{{ item.name }}'
        restrictedFields: '{{ item.restrictedFields }}'
        retentionDays: '{{ item.retentionDays }}'
        updateTime: '{{ item.updateTime }}'
  # Lists views on a log bucket.
  # ── gcp.logging.organizations.locations.buckets.views.list ──
  - discovery_id: gcp.logging.organizations.locations.buckets.views.list
    # python: svc.organizations().locations().buckets().views().list(**params).execute()
    calls:
      - action: organizations.locations.buckets.views.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.views }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        filter: '{{ item.filter }}'
        name: '{{ item.name }}'
        updateTime: '{{ item.updateTime }}'
  # Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries
  # ── gcp.logging.organizations.locations.buckets.views.logs.list ──
  - discovery_id: gcp.logging.organizations.locations.buckets.views.logs.list
    # python: svc.organizations().locations().buckets().views().logs().list(**params).execute()
    calls:
      - action: organizations.locations.buckets.views.logs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.logs }}'
  # Gets information about a location.
  # ── gcp.logging.organizations.locations.get ──
  - discovery_id: gcp.logging.organizations.locations.get
    # python: svc.organizations().locations().get(**params).execute()
    calls:
      - action: organizations.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.logging.organizations.locations.list ──
  - discovery_id: gcp.logging.organizations.locations.list
    # python: svc.organizations().locations().list(**params).execute()
    calls:
      - action: organizations.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists log scopes.
  # ── gcp.logging.organizations.locations.logScopes.list ──
  - discovery_id: gcp.logging.organizations.locations.logScopes.list
    # python: svc.organizations().locations().logScopes().list(**params).execute()
    calls:
      - action: organizations.locations.logScopes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.logScopes }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        name: '{{ item.name }}'
        resourceNames: '{{ item.resourceNames }}'
        updateTime: '{{ item.updateTime }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.logging.organizations.locations.operations.list ──
  - discovery_id: gcp.logging.organizations.locations.operations.list
    # python: svc.organizations().locations().operations().list(**params).execute()
    calls:
      - action: organizations.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists the RecentQueries that were created by the user making the request.
  # ── gcp.logging.organizations.locations.recentQueries.list ──
  - discovery_id: gcp.logging.organizations.locations.recentQueries.list
    # python: svc.organizations().locations().recentQueries().list(**params).execute()
    calls:
      - action: organizations.locations.recentQueries.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.recentQueries }}'
      item:
        lastRunTime: '{{ item.lastRunTime }}'
        loggingQuery: '{{ item.loggingQuery }}'
        name: '{{ item.name }}'
        opsAnalyticsQuery: '{{ item.opsAnalyticsQuery }}'
  # Lists the SavedQueries that were created by the user making the request.
  # ── gcp.logging.organizations.locations.savedQueries.list ──
  - discovery_id: gcp.logging.organizations.locations.savedQueries.list
    # python: svc.organizations().locations().savedQueries().list(**params).execute()
    calls:
      - action: organizations.locations.savedQueries.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.savedQueries }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        loggingQuery: '{{ item.loggingQuery }}'
        name: '{{ item.name }}'
        opsAnalyticsQuery: '{{ item.opsAnalyticsQuery }}'
        updateTime: '{{ item.updateTime }}'
        visibility: '{{ item.visibility }}'
  # Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries
  # ── gcp.logging.organizations.logs.list ──
  - discovery_id: gcp.logging.organizations.logs.list
    # python: svc.organizations().logs().list(**params).execute()
    calls:
      - action: organizations.logs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.logs }}'
  # Lists sinks.
  # ── gcp.logging.organizations.sinks.list ──
  - discovery_id: gcp.logging.organizations.sinks.list
    # python: svc.organizations().sinks().list(**params).execute()
    calls:
      - action: organizations.sinks.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.sinks }}'
      item:
        bigqueryOptions: '{{ item.bigqueryOptions }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        destination: '{{ item.destination }}'
        disabled: '{{ item.disabled }}'
        exclusions: '{{ item.exclusions }}'
        filter: '{{ item.filter }}'
        includeChildren: '{{ item.includeChildren }}'
        interceptChildren: '{{ item.interceptChildren }}'
        name: '{{ item.name }}'
        outputVersionFormat: '{{ item.outputVersionFormat }}'
        resourceName: '{{ item.resourceName }}'
        updateTime: '{{ item.updateTime }}'
        writerIdentity: '{{ item.writerIdentity }}'
  # Lists all the exclusions on the _Default sink in a parent resource.
  # ── gcp.logging.projects.exclusions.list ──
  - discovery_id: gcp.logging.projects.exclusions.list
    # python: svc.projects().exclusions().list(**params).execute()
    calls:
      - action: projects.exclusions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.exclusions }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        disabled: '{{ item.disabled }}'
        filter: '{{ item.filter }}'
        name: '{{ item.name }}'
        updateTime: '{{ item.updateTime }}'
  # Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configure
  # ── gcp.logging.projects.getCmekSettings ──
  - discovery_id: gcp.logging.projects.getCmekSettings
    # python: svc.projects().getCmekSettings(**params).execute()
    calls:
      - action: projects.getCmekSettings
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        kmsKeyName: '{{ response.kmsKeyName }}'
        kmsKeyVersionName: '{{ response.kmsKeyVersionName }}'
        name: '{{ response.name }}'
        serviceAccountId: '{{ response.serviceAccountId }}'
  # Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, 
  # ── gcp.logging.projects.getSettings ──
  - discovery_id: gcp.logging.projects.getSettings
    # python: svc.projects().getSettings(**params).execute()
    calls:
      - action: projects.getSettings
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        defaultSinkConfig: '{{ response.defaultSinkConfig }}'
        disableDefaultSink: '{{ response.disableDefaultSink }}'
        kmsKeyName: '{{ response.kmsKeyName }}'
        kmsServiceAccountId: '{{ response.kmsServiceAccountId }}'
        loggingServiceAccountId: '{{ response.loggingServiceAccountId }}'
        name: '{{ response.name }}'
        storageLocation: '{{ response.storageLocation }}'
  # Lists links.
  # ── gcp.logging.projects.locations.buckets.links.list ──
  - discovery_id: gcp.logging.projects.locations.buckets.links.list
    # python: svc.projects().locations().buckets().links().list(**params).execute()
    calls:
      - action: projects.locations.buckets.links.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.links }}'
      item:
        bigqueryDataset: '{{ item.bigqueryDataset }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        lifecycleState: '{{ item.lifecycleState }}'
        name: '{{ item.name }}'
  # Lists log buckets.
  # ── gcp.logging.projects.locations.buckets.list ──
  - discovery_id: gcp.logging.projects.locations.buckets.list
    # python: svc.projects().locations().buckets().list(**params).execute()
    calls:
      - action: projects.locations.buckets.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.buckets }}'
      item:
        analyticsEnabled: '{{ item.analyticsEnabled }}'
        cmekSettings: '{{ item.cmekSettings }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        indexConfigs: '{{ item.indexConfigs }}'
        lifecycleState: '{{ item.lifecycleState }}'
        locked: '{{ item.locked }}'
        name: '{{ item.name }}'
        restrictedFields: '{{ item.restrictedFields }}'
        retentionDays: '{{ item.retentionDays }}'
        updateTime: '{{ item.updateTime }}'
  # Lists views on a log bucket.
  # ── gcp.logging.projects.locations.buckets.views.list ──
  - discovery_id: gcp.logging.projects.locations.buckets.views.list
    # python: svc.projects().locations().buckets().views().list(**params).execute()
    calls:
      - action: projects.locations.buckets.views.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.views }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        filter: '{{ item.filter }}'
        name: '{{ item.name }}'
        updateTime: '{{ item.updateTime }}'
  # Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries
  # ── gcp.logging.projects.locations.buckets.views.logs.list ──
  - discovery_id: gcp.logging.projects.locations.buckets.views.logs.list
    # python: svc.projects().locations().buckets().views().logs().list(**params).execute()
    calls:
      - action: projects.locations.buckets.views.logs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.logs }}'
  # Gets information about a location.
  # ── gcp.logging.projects.locations.get ──
  - discovery_id: gcp.logging.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.logging.projects.locations.list ──
  - discovery_id: gcp.logging.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists log scopes.
  # ── gcp.logging.projects.locations.logScopes.list ──
  - discovery_id: gcp.logging.projects.locations.logScopes.list
    # python: svc.projects().locations().logScopes().list(**params).execute()
    calls:
      - action: projects.locations.logScopes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.logScopes }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        name: '{{ item.name }}'
        resourceNames: '{{ item.resourceNames }}'
        updateTime: '{{ item.updateTime }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.logging.projects.locations.operations.list ──
  - discovery_id: gcp.logging.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists the RecentQueries that were created by the user making the request.
  # ── gcp.logging.projects.locations.recentQueries.list ──
  - discovery_id: gcp.logging.projects.locations.recentQueries.list
    # python: svc.projects().locations().recentQueries().list(**params).execute()
    calls:
      - action: projects.locations.recentQueries.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.recentQueries }}'
      item:
        lastRunTime: '{{ item.lastRunTime }}'
        loggingQuery: '{{ item.loggingQuery }}'
        name: '{{ item.name }}'
        opsAnalyticsQuery: '{{ item.opsAnalyticsQuery }}'
  # Lists the SavedQueries that were created by the user making the request.
  # ── gcp.logging.projects.locations.savedQueries.list ──
  - discovery_id: gcp.logging.projects.locations.savedQueries.list
    # python: svc.projects().locations().savedQueries().list(**params).execute()
    calls:
      - action: projects.locations.savedQueries.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.savedQueries }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        loggingQuery: '{{ item.loggingQuery }}'
        name: '{{ item.name }}'
        opsAnalyticsQuery: '{{ item.opsAnalyticsQuery }}'
        updateTime: '{{ item.updateTime }}'
        visibility: '{{ item.visibility }}'
  # Lists the logs in projects, organizations, folders, or billing accounts. Only logs that have entries
  # ── gcp.logging.projects.logs.list ──
  - discovery_id: gcp.logging.projects.logs.list
    # python: svc.projects().logs().list(**params).execute()
    calls:
      - action: projects.logs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.logs }}'
  # Lists logs-based metrics.
  # ── gcp.logging.projects.metrics.list ──
  - discovery_id: gcp.logging.projects.metrics.list
    # python: svc.projects().metrics().list(**params).execute()
    calls:
      - action: projects.metrics.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.metrics }}'
      item:
        bucketName: '{{ item.bucketName }}'
        bucketOptions: '{{ item.bucketOptions }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        disabled: '{{ item.disabled }}'
        filter: '{{ item.filter }}'
        labelExtractors: '{{ item.labelExtractors }}'
        metricDescriptor: '{{ item.metricDescriptor }}'
        name: '{{ item.name }}'
        resourceName: '{{ item.resourceName }}'
        updateTime: '{{ item.updateTime }}'
        valueExtractor: '{{ item.valueExtractor }}'
        version: '{{ item.version }}'
  # Lists sinks.
  # ── gcp.logging.projects.sinks.list ──
  - discovery_id: gcp.logging.projects.sinks.list
    # python: svc.projects().sinks().list(**params).execute()
    calls:
      - action: projects.sinks.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.sinks }}'
      item:
        bigqueryOptions: '{{ item.bigqueryOptions }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        destination: '{{ item.destination }}'
        disabled: '{{ item.disabled }}'
        exclusions: '{{ item.exclusions }}'
        filter: '{{ item.filter }}'
        includeChildren: '{{ item.includeChildren }}'
        interceptChildren: '{{ item.interceptChildren }}'
        name: '{{ item.name }}'
        outputVersionFormat: '{{ item.outputVersionFormat }}'
        resourceName: '{{ item.resourceName }}'
        updateTime: '{{ item.updateTime }}'
        writerIdentity: '{{ item.writerIdentity }}'
  # Lists sinks.
  # ── gcp.logging.sinks.list ──
  - discovery_id: gcp.logging.sinks.list
    # python: svc.sinks().list(**params).execute()
    calls:
      - action: sinks.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.sinks }}'
      item:
        bigqueryOptions: '{{ item.bigqueryOptions }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        destination: '{{ item.destination }}'
        disabled: '{{ item.disabled }}'
        exclusions: '{{ item.exclusions }}'
        filter: '{{ item.filter }}'
        includeChildren: '{{ item.includeChildren }}'
        interceptChildren: '{{ item.interceptChildren }}'
        name: '{{ item.name }}'
        outputVersionFormat: '{{ item.outputVersionFormat }}'
        resourceName: '{{ item.resourceName }}'
        updateTime: '{{ item.updateTime }}'
        writerIdentity: '{{ item.writerIdentity }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets the description of an exclusion in the _Default sink.
  # ── gcp.logging.billingAccounts.exclusions.get [dependent] ──
  - discovery_id: gcp.logging.billingAccounts.exclusions.get
    for_each: gcp.logging.billingAccounts.exclusions.list
    # python: svc.billingAccounts().exclusions().get(**params).execute()
    calls:
      - action: billingAccounts.exclusions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        disabled: '{{ response.disabled }}'
        filter: '{{ response.filter }}'
        name: '{{ response.name }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets a log bucket.
  # ── gcp.logging.billingAccounts.locations.buckets.get [dependent] ──
  - discovery_id: gcp.logging.billingAccounts.locations.buckets.get
    for_each: gcp.logging.billingAccounts.locations.buckets.list
    # python: svc.billingAccounts().locations().buckets().get(**params).execute()
    calls:
      - action: billingAccounts.locations.buckets.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        analyticsEnabled: '{{ response.analyticsEnabled }}'
        cmekSettings: '{{ response.cmekSettings }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        indexConfigs: '{{ response.indexConfigs }}'
        lifecycleState: '{{ response.lifecycleState }}'
        locked: '{{ response.locked }}'
        name: '{{ response.name }}'
        restrictedFields: '{{ response.restrictedFields }}'
        retentionDays: '{{ response.retentionDays }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets a link.
  # ── gcp.logging.billingAccounts.locations.buckets.links.get [dependent] ──
  - discovery_id: gcp.logging.billingAccounts.locations.buckets.links.get
    for_each: gcp.logging.billingAccounts.locations.buckets.links.list
    # python: svc.billingAccounts().locations().buckets().links().get(**params).execute()
    calls:
      - action: billingAccounts.locations.buckets.links.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        bigqueryDataset: '{{ response.bigqueryDataset }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        lifecycleState: '{{ response.lifecycleState }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Gets a view on a log bucket.
  # ── gcp.logging.billingAccounts.locations.buckets.views.get [dependent] ──
  - discovery_id: gcp.logging.billingAccounts.locations.buckets.views.get
    for_each: gcp.logging.billingAccounts.locations.buckets.views.list
    # python: svc.billingAccounts().locations().buckets().views().get(**params).execute()
    calls:
      - action: billingAccounts.locations.buckets.views.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        filter: '{{ response.filter }}'
        name: '{{ response.name }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.logging.billingAccounts.locations.operations.get [dependent] ──
  - discovery_id: gcp.logging.billingAccounts.locations.operations.get
    for_each: gcp.logging.billingAccounts.locations.operations.list
    # python: svc.billingAccounts().locations().operations().get(**params).execute()
    calls:
      - action: billingAccounts.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Returns all data associated with the requested query.
  # ── gcp.logging.billingAccounts.locations.savedQueries.get [dependent] ──
  - discovery_id: gcp.logging.billingAccounts.locations.savedQueries.get
    for_each: gcp.logging.billingAccounts.locations.savedQueries.list
    # python: svc.billingAccounts().locations().savedQueries().get(**params).execute()
    calls:
      - action: billingAccounts.locations.savedQueries.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        loggingQuery: '{{ response.loggingQuery }}'
        name: '{{ response.name }}'
        opsAnalyticsQuery: '{{ response.opsAnalyticsQuery }}'
        updateTime: '{{ response.updateTime }}'
        visibility: '{{ response.visibility }}'
    # required_params: ['name']
  # Gets a sink.
  # ── gcp.logging.billingAccounts.sinks.get [dependent] ──
  - discovery_id: gcp.logging.billingAccounts.sinks.get
    for_each: gcp.logging.billingAccounts.sinks.list
    # python: svc.billingAccounts().sinks().get(**params).execute()
    calls:
      - action: billingAccounts.sinks.get
        params:
          sinkName: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        bigqueryOptions: '{{ response.bigqueryOptions }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        destination: '{{ response.destination }}'
        disabled: '{{ response.disabled }}'
        exclusions: '{{ response.exclusions }}'
        filter: '{{ response.filter }}'
        includeChildren: '{{ response.includeChildren }}'
        interceptChildren: '{{ response.interceptChildren }}'
        name: '{{ response.name }}'
        outputVersionFormat: '{{ response.outputVersionFormat }}'
        resourceName: '{{ response.resourceName }}'
        updateTime: '{{ response.updateTime }}'
        writerIdentity: '{{ response.writerIdentity }}'
    # required_params: ['sinkName']
  # Gets the description of an exclusion in the _Default sink.
  # ── gcp.logging.exclusions.get [dependent] ──
  - discovery_id: gcp.logging.exclusions.get
    for_each: gcp.logging.billingAccounts.exclusions.list
    # python: svc.exclusions().get(**params).execute()
    calls:
      - action: exclusions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        disabled: '{{ response.disabled }}'
        filter: '{{ response.filter }}'
        name: '{{ response.name }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the description of an exclusion in the _Default sink.
  # ── gcp.logging.folders.exclusions.get [dependent] ──
  - discovery_id: gcp.logging.folders.exclusions.get
    for_each: gcp.logging.billingAccounts.exclusions.list
    # python: svc.folders().exclusions().get(**params).execute()
    calls:
      - action: folders.exclusions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        disabled: '{{ response.disabled }}'
        filter: '{{ response.filter }}'
        name: '{{ response.name }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets a log bucket.
  # ── gcp.logging.folders.locations.buckets.get [dependent] ──
  - discovery_id: gcp.logging.folders.locations.buckets.get
    for_each: gcp.logging.billingAccounts.locations.buckets.list
    # python: svc.folders().locations().buckets().get(**params).execute()
    calls:
      - action: folders.locations.buckets.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        analyticsEnabled: '{{ response.analyticsEnabled }}'
        cmekSettings: '{{ response.cmekSettings }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        indexConfigs: '{{ response.indexConfigs }}'
        lifecycleState: '{{ response.lifecycleState }}'
        locked: '{{ response.locked }}'
        name: '{{ response.name }}'
        restrictedFields: '{{ response.restrictedFields }}'
        retentionDays: '{{ response.retentionDays }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets a link.
  # ── gcp.logging.folders.locations.buckets.links.get [dependent] ──
  - discovery_id: gcp.logging.folders.locations.buckets.links.get
    for_each: gcp.logging.billingAccounts.locations.buckets.links.list
    # python: svc.folders().locations().buckets().links().get(**params).execute()
    calls:
      - action: folders.locations.buckets.links.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        bigqueryDataset: '{{ response.bigqueryDataset }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        lifecycleState: '{{ response.lifecycleState }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Gets a view on a log bucket.
  # ── gcp.logging.folders.locations.buckets.views.get [dependent] ──
  - discovery_id: gcp.logging.folders.locations.buckets.views.get
    for_each: gcp.logging.billingAccounts.locations.buckets.views.list
    # python: svc.folders().locations().buckets().views().get(**params).execute()
    calls:
      - action: folders.locations.buckets.views.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        filter: '{{ response.filter }}'
        name: '{{ response.name }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets a log scope.
  # ── gcp.logging.folders.locations.logScopes.get [dependent] ──
  - discovery_id: gcp.logging.folders.locations.logScopes.get
    for_each: gcp.logging.organizations.locations.logScopes.list
    # python: svc.folders().locations().logScopes().get(**params).execute()
    calls:
      - action: folders.locations.logScopes.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        name: '{{ response.name }}'
        resourceNames: '{{ response.resourceNames }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.logging.folders.locations.operations.get [dependent] ──
  - discovery_id: gcp.logging.folders.locations.operations.get
    for_each: gcp.logging.billingAccounts.locations.operations.list
    # python: svc.folders().locations().operations().get(**params).execute()
    calls:
      - action: folders.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Returns all data associated with the requested query.
  # ── gcp.logging.folders.locations.savedQueries.get [dependent] ──
  - discovery_id: gcp.logging.folders.locations.savedQueries.get
    for_each: gcp.logging.billingAccounts.locations.savedQueries.list
    # python: svc.folders().locations().savedQueries().get(**params).execute()
    calls:
      - action: folders.locations.savedQueries.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        loggingQuery: '{{ response.loggingQuery }}'
        name: '{{ response.name }}'
        opsAnalyticsQuery: '{{ response.opsAnalyticsQuery }}'
        updateTime: '{{ response.updateTime }}'
        visibility: '{{ response.visibility }}'
    # required_params: ['name']
  # Gets a sink.
  # ── gcp.logging.folders.sinks.get [dependent] ──
  - discovery_id: gcp.logging.folders.sinks.get
    for_each: gcp.logging.billingAccounts.sinks.list
    # python: svc.folders().sinks().get(**params).execute()
    calls:
      - action: folders.sinks.get
        params:
          sinkName: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        bigqueryOptions: '{{ response.bigqueryOptions }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        destination: '{{ response.destination }}'
        disabled: '{{ response.disabled }}'
        exclusions: '{{ response.exclusions }}'
        filter: '{{ response.filter }}'
        includeChildren: '{{ response.includeChildren }}'
        interceptChildren: '{{ response.interceptChildren }}'
        name: '{{ response.name }}'
        outputVersionFormat: '{{ response.outputVersionFormat }}'
        resourceName: '{{ response.resourceName }}'
        updateTime: '{{ response.updateTime }}'
        writerIdentity: '{{ response.writerIdentity }}'
    # required_params: ['sinkName']
  # Gets a log bucket.
  # ── gcp.logging.locations.buckets.get [dependent] ──
  - discovery_id: gcp.logging.locations.buckets.get
    for_each: gcp.logging.billingAccounts.locations.buckets.list
    # python: svc.locations().buckets().get(**params).execute()
    calls:
      - action: locations.buckets.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        analyticsEnabled: '{{ response.analyticsEnabled }}'
        cmekSettings: '{{ response.cmekSettings }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        indexConfigs: '{{ response.indexConfigs }}'
        lifecycleState: '{{ response.lifecycleState }}'
        locked: '{{ response.locked }}'
        name: '{{ response.name }}'
        restrictedFields: '{{ response.restrictedFields }}'
        retentionDays: '{{ response.retentionDays }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets a link.
  # ── gcp.logging.locations.buckets.links.get [dependent] ──
  - discovery_id: gcp.logging.locations.buckets.links.get
    for_each: gcp.logging.billingAccounts.locations.buckets.links.list
    # python: svc.locations().buckets().links().get(**params).execute()
    calls:
      - action: locations.buckets.links.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        bigqueryDataset: '{{ response.bigqueryDataset }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        lifecycleState: '{{ response.lifecycleState }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Gets a view on a log bucket.
  # ── gcp.logging.locations.buckets.views.get [dependent] ──
  - discovery_id: gcp.logging.locations.buckets.views.get
    for_each: gcp.logging.billingAccounts.locations.buckets.views.list
    # python: svc.locations().buckets().views().get(**params).execute()
    calls:
      - action: locations.buckets.views.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        filter: '{{ response.filter }}'
        name: '{{ response.name }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets information about a location.
  # ── gcp.logging.locations.get [dependent] ──
  - discovery_id: gcp.logging.locations.get
    # python: svc.locations().get(**params).execute()
    calls:
      - action: locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.logging.locations.list [dependent] ──
  - discovery_id: gcp.logging.locations.list
    # python: svc.locations().list(**params).execute()
    calls:
      - action: locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.logging.locations.operations.get [dependent] ──
  - discovery_id: gcp.logging.locations.operations.get
    for_each: gcp.logging.billingAccounts.locations.operations.list
    # python: svc.locations().operations().get(**params).execute()
    calls:
      - action: locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.logging.locations.operations.list [dependent] ──
  - discovery_id: gcp.logging.locations.operations.list
    # python: svc.locations().operations().list(**params).execute()
    calls:
      - action: locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
    # required_params: ['name']
  # Gets the description of an exclusion in the _Default sink.
  # ── gcp.logging.organizations.exclusions.get [dependent] ──
  - discovery_id: gcp.logging.organizations.exclusions.get
    for_each: gcp.logging.billingAccounts.exclusions.list
    # python: svc.organizations().exclusions().get(**params).execute()
    calls:
      - action: organizations.exclusions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        disabled: '{{ response.disabled }}'
        filter: '{{ response.filter }}'
        name: '{{ response.name }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets a log bucket.
  # ── gcp.logging.organizations.locations.buckets.get [dependent] ──
  - discovery_id: gcp.logging.organizations.locations.buckets.get
    for_each: gcp.logging.billingAccounts.locations.buckets.list
    # python: svc.organizations().locations().buckets().get(**params).execute()
    calls:
      - action: organizations.locations.buckets.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        analyticsEnabled: '{{ response.analyticsEnabled }}'
        cmekSettings: '{{ response.cmekSettings }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        indexConfigs: '{{ response.indexConfigs }}'
        lifecycleState: '{{ response.lifecycleState }}'
        locked: '{{ response.locked }}'
        name: '{{ response.name }}'
        restrictedFields: '{{ response.restrictedFields }}'
        retentionDays: '{{ response.retentionDays }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets a link.
  # ── gcp.logging.organizations.locations.buckets.links.get [dependent] ──
  - discovery_id: gcp.logging.organizations.locations.buckets.links.get
    for_each: gcp.logging.billingAccounts.locations.buckets.links.list
    # python: svc.organizations().locations().buckets().links().get(**params).execute()
    calls:
      - action: organizations.locations.buckets.links.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        bigqueryDataset: '{{ response.bigqueryDataset }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        lifecycleState: '{{ response.lifecycleState }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Gets a view on a log bucket.
  # ── gcp.logging.organizations.locations.buckets.views.get [dependent] ──
  - discovery_id: gcp.logging.organizations.locations.buckets.views.get
    for_each: gcp.logging.billingAccounts.locations.buckets.views.list
    # python: svc.organizations().locations().buckets().views().get(**params).execute()
    calls:
      - action: organizations.locations.buckets.views.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        filter: '{{ response.filter }}'
        name: '{{ response.name }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets a log scope.
  # ── gcp.logging.organizations.locations.logScopes.get [dependent] ──
  - discovery_id: gcp.logging.organizations.locations.logScopes.get
    for_each: gcp.logging.organizations.locations.logScopes.list
    # python: svc.organizations().locations().logScopes().get(**params).execute()
    calls:
      - action: organizations.locations.logScopes.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        name: '{{ response.name }}'
        resourceNames: '{{ response.resourceNames }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.logging.organizations.locations.operations.get [dependent] ──
  - discovery_id: gcp.logging.organizations.locations.operations.get
    for_each: gcp.logging.billingAccounts.locations.operations.list
    # python: svc.organizations().locations().operations().get(**params).execute()
    calls:
      - action: organizations.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Returns all data associated with the requested query.
  # ── gcp.logging.organizations.locations.savedQueries.get [dependent] ──
  - discovery_id: gcp.logging.organizations.locations.savedQueries.get
    for_each: gcp.logging.billingAccounts.locations.savedQueries.list
    # python: svc.organizations().locations().savedQueries().get(**params).execute()
    calls:
      - action: organizations.locations.savedQueries.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        loggingQuery: '{{ response.loggingQuery }}'
        name: '{{ response.name }}'
        opsAnalyticsQuery: '{{ response.opsAnalyticsQuery }}'
        updateTime: '{{ response.updateTime }}'
        visibility: '{{ response.visibility }}'
    # required_params: ['name']
  # Gets a sink.
  # ── gcp.logging.organizations.sinks.get [dependent] ──
  - discovery_id: gcp.logging.organizations.sinks.get
    for_each: gcp.logging.billingAccounts.sinks.list
    # python: svc.organizations().sinks().get(**params).execute()
    calls:
      - action: organizations.sinks.get
        params:
          sinkName: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        bigqueryOptions: '{{ response.bigqueryOptions }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        destination: '{{ response.destination }}'
        disabled: '{{ response.disabled }}'
        exclusions: '{{ response.exclusions }}'
        filter: '{{ response.filter }}'
        includeChildren: '{{ response.includeChildren }}'
        interceptChildren: '{{ response.interceptChildren }}'
        name: '{{ response.name }}'
        outputVersionFormat: '{{ response.outputVersionFormat }}'
        resourceName: '{{ response.resourceName }}'
        updateTime: '{{ response.updateTime }}'
        writerIdentity: '{{ response.writerIdentity }}'
    # required_params: ['sinkName']
  # Gets the description of an exclusion in the _Default sink.
  # ── gcp.logging.projects.exclusions.get [dependent] ──
  - discovery_id: gcp.logging.projects.exclusions.get
    for_each: gcp.logging.billingAccounts.exclusions.list
    # python: svc.projects().exclusions().get(**params).execute()
    calls:
      - action: projects.exclusions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        disabled: '{{ response.disabled }}'
        filter: '{{ response.filter }}'
        name: '{{ response.name }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets a log bucket.
  # ── gcp.logging.projects.locations.buckets.get [dependent] ──
  - discovery_id: gcp.logging.projects.locations.buckets.get
    for_each: gcp.logging.billingAccounts.locations.buckets.list
    # python: svc.projects().locations().buckets().get(**params).execute()
    calls:
      - action: projects.locations.buckets.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        analyticsEnabled: '{{ response.analyticsEnabled }}'
        cmekSettings: '{{ response.cmekSettings }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        indexConfigs: '{{ response.indexConfigs }}'
        lifecycleState: '{{ response.lifecycleState }}'
        locked: '{{ response.locked }}'
        name: '{{ response.name }}'
        restrictedFields: '{{ response.restrictedFields }}'
        retentionDays: '{{ response.retentionDays }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets a link.
  # ── gcp.logging.projects.locations.buckets.links.get [dependent] ──
  - discovery_id: gcp.logging.projects.locations.buckets.links.get
    for_each: gcp.logging.billingAccounts.locations.buckets.links.list
    # python: svc.projects().locations().buckets().links().get(**params).execute()
    calls:
      - action: projects.locations.buckets.links.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        bigqueryDataset: '{{ response.bigqueryDataset }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        lifecycleState: '{{ response.lifecycleState }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Gets a view on a log bucket.
  # ── gcp.logging.projects.locations.buckets.views.get [dependent] ──
  - discovery_id: gcp.logging.projects.locations.buckets.views.get
    for_each: gcp.logging.billingAccounts.locations.buckets.views.list
    # python: svc.projects().locations().buckets().views().get(**params).execute()
    calls:
      - action: projects.locations.buckets.views.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        filter: '{{ response.filter }}'
        name: '{{ response.name }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets a log scope.
  # ── gcp.logging.projects.locations.logScopes.get [dependent] ──
  - discovery_id: gcp.logging.projects.locations.logScopes.get
    for_each: gcp.logging.organizations.locations.logScopes.list
    # python: svc.projects().locations().logScopes().get(**params).execute()
    calls:
      - action: projects.locations.logScopes.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        name: '{{ response.name }}'
        resourceNames: '{{ response.resourceNames }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.logging.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.logging.projects.locations.operations.get
    for_each: gcp.logging.billingAccounts.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Returns all data associated with the requested query.
  # ── gcp.logging.projects.locations.savedQueries.get [dependent] ──
  - discovery_id: gcp.logging.projects.locations.savedQueries.get
    for_each: gcp.logging.billingAccounts.locations.savedQueries.list
    # python: svc.projects().locations().savedQueries().get(**params).execute()
    calls:
      - action: projects.locations.savedQueries.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        displayName: '{{ response.displayName }}'
        loggingQuery: '{{ response.loggingQuery }}'
        name: '{{ response.name }}'
        opsAnalyticsQuery: '{{ response.opsAnalyticsQuery }}'
        updateTime: '{{ response.updateTime }}'
        visibility: '{{ response.visibility }}'
    # required_params: ['name']
  # Gets a logs-based metric.
  # ── gcp.logging.projects.metrics.get [dependent] ──
  - discovery_id: gcp.logging.projects.metrics.get
    for_each: gcp.logging.projects.metrics.list
    # python: svc.projects().metrics().get(**params).execute()
    calls:
      - action: projects.metrics.get
        params:
          metricName: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        bucketName: '{{ response.bucketName }}'
        bucketOptions: '{{ response.bucketOptions }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        disabled: '{{ response.disabled }}'
        filter: '{{ response.filter }}'
        labelExtractors: '{{ response.labelExtractors }}'
        metricDescriptor: '{{ response.metricDescriptor }}'
        name: '{{ response.name }}'
        resourceName: '{{ response.resourceName }}'
        updateTime: '{{ response.updateTime }}'
        valueExtractor: '{{ response.valueExtractor }}'
        version: '{{ response.version }}'
    # required_params: ['metricName']
  # Gets a sink.
  # ── gcp.logging.projects.sinks.get [dependent] ──
  - discovery_id: gcp.logging.projects.sinks.get
    for_each: gcp.logging.billingAccounts.sinks.list
    # python: svc.projects().sinks().get(**params).execute()
    calls:
      - action: projects.sinks.get
        params:
          sinkName: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        bigqueryOptions: '{{ response.bigqueryOptions }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        destination: '{{ response.destination }}'
        disabled: '{{ response.disabled }}'
        exclusions: '{{ response.exclusions }}'
        filter: '{{ response.filter }}'
        includeChildren: '{{ response.includeChildren }}'
        interceptChildren: '{{ response.interceptChildren }}'
        name: '{{ response.name }}'
        outputVersionFormat: '{{ response.outputVersionFormat }}'
        resourceName: '{{ response.resourceName }}'
        updateTime: '{{ response.updateTime }}'
        writerIdentity: '{{ response.writerIdentity }}'
    # required_params: ['sinkName']
  # Gets a sink.
  # ── gcp.logging.sinks.get [dependent] ──
  - discovery_id: gcp.logging.sinks.get
    for_each: gcp.logging.billingAccounts.sinks.list
    # python: svc.sinks().get(**params).execute()
    calls:
      - action: sinks.get
        params:
          sinkName: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        bigqueryOptions: '{{ response.bigqueryOptions }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        destination: '{{ response.destination }}'
        disabled: '{{ response.disabled }}'
        exclusions: '{{ response.exclusions }}'
        filter: '{{ response.filter }}'
        includeChildren: '{{ response.includeChildren }}'
        interceptChildren: '{{ response.interceptChildren }}'
        name: '{{ response.name }}'
        outputVersionFormat: '{{ response.outputVersionFormat }}'
        resourceName: '{{ response.resourceName }}'
        updateTime: '{{ response.updateTime }}'
        writerIdentity: '{{ response.writerIdentity }}'
    # required_params: ['sinkName']
  # Gets the Logging CMEK settings for the given resource.Note: CMEK for the Log Router can be configure
  # ── gcp.logging.v2.getCmekSettings [dependent] ──
  - discovery_id: gcp.logging.v2.getCmekSettings
    # python: svc.v2().getCmekSettings(**params).execute()
    calls:
      - action: v2.getCmekSettings
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        kmsKeyName: '{{ response.kmsKeyName }}'
        kmsKeyVersionName: '{{ response.kmsKeyVersionName }}'
        name: '{{ response.name }}'
        serviceAccountId: '{{ response.serviceAccountId }}'
    # required_params: ['name']
  # Gets the settings for the given resource.Note: Settings can be retrieved for Google Cloud projects, 
  # ── gcp.logging.v2.getSettings [dependent] ──
  - discovery_id: gcp.logging.v2.getSettings
    # python: svc.v2().getSettings(**params).execute()
    calls:
      - action: v2.getSettings
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        defaultSinkConfig: '{{ response.defaultSinkConfig }}'
        disableDefaultSink: '{{ response.disableDefaultSink }}'
        kmsKeyName: '{{ response.kmsKeyName }}'
        kmsServiceAccountId: '{{ response.kmsServiceAccountId }}'
        loggingServiceAccountId: '{{ response.loggingServiceAccountId }}'
        name: '{{ response.name }}'
        storageLocation: '{{ response.storageLocation }}'
    # required_params: ['name']
