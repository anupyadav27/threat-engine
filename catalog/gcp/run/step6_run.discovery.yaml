# ============================================================
# Discovery YAML — run (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 40 | independent: 20 | dependent: 20
# ops with fields: 40/40 | dep ops with chain: 20/20
# ============================================================
version: '1.0'
provider: gcp
service: run

services:
  client: run
  module: "googleapiclient.discovery.build('run', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # List authorized domains.
  # ── gcp.run.namespaces.authorizeddomains.list ──
  - discovery_id: gcp.run.namespaces.authorizeddomains.list
    # python: svc.namespaces().authorizeddomains().list(**params).execute()
    calls:
      - action: namespaces.authorizeddomains.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.authorizeddomains }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
  # List configurations. Results are sorted by creation time, descending.
  # ── gcp.run.namespaces.configurations.list ──
  - discovery_id: gcp.run.namespaces.configurations.list
    # python: svc.namespaces().configurations().list(**params).execute()
    calls:
      - action: namespaces.configurations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.configurations }}'
      item:
        spec: '{{ item.spec }}'
        status: '{{ item.status }}'
  # List all domain mappings.
  # ── gcp.run.namespaces.domainmappings.list ──
  - discovery_id: gcp.run.namespaces.domainmappings.list
    # python: svc.namespaces().domainmappings().list(**params).execute()
    calls:
      - action: namespaces.domainmappings.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.domainmappings }}'
      item:
        spec: '{{ item.spec }}'
        status: '{{ item.status }}'
  # List executions. Results are sorted by creation time, descending.
  # ── gcp.run.namespaces.executions.list ──
  - discovery_id: gcp.run.namespaces.executions.list
    # python: svc.namespaces().executions().list(**params).execute()
    calls:
      - action: namespaces.executions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.executions }}'
      item:
        spec: '{{ item.spec }}'
        status: '{{ item.status }}'
  # List Instances. Results are sorted by creation time, descending.
  # ── gcp.run.namespaces.instances.list ──
  - discovery_id: gcp.run.namespaces.instances.list
    # python: svc.namespaces().instances().list(**params).execute()
    calls:
      - action: namespaces.instances.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.instances }}'
      item:
        spec: '{{ item.spec }}'
        status: '{{ item.status }}'
  # List jobs. Results are sorted by creation time, descending.
  # ── gcp.run.namespaces.jobs.list ──
  - discovery_id: gcp.run.namespaces.jobs.list
    # python: svc.namespaces().jobs().list(**params).execute()
    calls:
      - action: namespaces.jobs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.jobs }}'
      item:
        spec: '{{ item.spec }}'
        status: '{{ item.status }}'
  # List revisions. Results are sorted by creation time, descending.
  # ── gcp.run.namespaces.revisions.list ──
  - discovery_id: gcp.run.namespaces.revisions.list
    # python: svc.namespaces().revisions().list(**params).execute()
    calls:
      - action: namespaces.revisions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.revisions }}'
      item:
        spec: '{{ item.spec }}'
        status: '{{ item.status }}'
  # List routes. Results are sorted by creation time, descending.
  # ── gcp.run.namespaces.routes.list ──
  - discovery_id: gcp.run.namespaces.routes.list
    # python: svc.namespaces().routes().list(**params).execute()
    calls:
      - action: namespaces.routes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.routes }}'
      item:
        spec: '{{ item.spec }}'
        status: '{{ item.status }}'
  # Lists services for the given project and region. Results are sorted by creation time, descending.
  # ── gcp.run.namespaces.services.list ──
  - discovery_id: gcp.run.namespaces.services.list
    # python: svc.namespaces().services().list(**params).execute()
    calls:
      - action: namespaces.services.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.services }}'
      item:
        spec: '{{ item.spec }}'
        status: '{{ item.status }}'
  # List tasks.
  # ── gcp.run.namespaces.tasks.list ──
  - discovery_id: gcp.run.namespaces.tasks.list
    # python: svc.namespaces().tasks().list(**params).execute()
    calls:
      - action: namespaces.tasks.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.tasks }}'
      item:
        spec: '{{ item.spec }}'
        status: '{{ item.status }}'
  # Lists worker pools for the given project and region. Results are sorted by creation time, descending
  # ── gcp.run.namespaces.workerpools.list ──
  - discovery_id: gcp.run.namespaces.workerpools.list
    # python: svc.namespaces().workerpools().list(**params).execute()
    calls:
      - action: namespaces.workerpools.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.workerpools }}'
      item:
        spec: '{{ item.spec }}'
        status: '{{ item.status }}'
  # List authorized domains.
  # ── gcp.run.projects.authorizeddomains.list ──
  - discovery_id: gcp.run.projects.authorizeddomains.list
    # python: svc.projects().authorizeddomains().list(**params).execute()
    calls:
      - action: projects.authorizeddomains.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.authorizeddomains }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
  # List authorized domains.
  # ── gcp.run.projects.locations.authorizeddomains.list ──
  - discovery_id: gcp.run.projects.locations.authorizeddomains.list
    # python: svc.projects().locations().authorizeddomains().list(**params).execute()
    calls:
      - action: projects.locations.authorizeddomains.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.authorizeddomains }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
  # List configurations. Results are sorted by creation time, descending.
  # ── gcp.run.projects.locations.configurations.list ──
  - discovery_id: gcp.run.projects.locations.configurations.list
    # python: svc.projects().locations().configurations().list(**params).execute()
    calls:
      - action: projects.locations.configurations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.configurations }}'
      item:
        spec: '{{ item.spec }}'
        status: '{{ item.status }}'
  # List all domain mappings.
  # ── gcp.run.projects.locations.domainmappings.list ──
  - discovery_id: gcp.run.projects.locations.domainmappings.list
    # python: svc.projects().locations().domainmappings().list(**params).execute()
    calls:
      - action: projects.locations.domainmappings.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.domainmappings }}'
      item:
        spec: '{{ item.spec }}'
        status: '{{ item.status }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.run.projects.locations.list ──
  - discovery_id: gcp.run.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.run.projects.locations.operations.list ──
  - discovery_id: gcp.run.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # List revisions. Results are sorted by creation time, descending.
  # ── gcp.run.projects.locations.revisions.list ──
  - discovery_id: gcp.run.projects.locations.revisions.list
    # python: svc.projects().locations().revisions().list(**params).execute()
    calls:
      - action: projects.locations.revisions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.revisions }}'
      item:
        spec: '{{ item.spec }}'
        status: '{{ item.status }}'
  # List routes. Results are sorted by creation time, descending.
  # ── gcp.run.projects.locations.routes.list ──
  - discovery_id: gcp.run.projects.locations.routes.list
    # python: svc.projects().locations().routes().list(**params).execute()
    calls:
      - action: projects.locations.routes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.routes }}'
      item:
        spec: '{{ item.spec }}'
        status: '{{ item.status }}'
  # Lists services for the given project and region. Results are sorted by creation time, descending.
  # ── gcp.run.projects.locations.services.list ──
  - discovery_id: gcp.run.projects.locations.services.list
    # python: svc.projects().locations().services().list(**params).execute()
    calls:
      - action: projects.locations.services.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.services }}'
      item:
        spec: '{{ item.spec }}'
        status: '{{ item.status }}'

  # ════ DEPENDENT (enrich) operations ════
  # Get information about a configuration.
  # ── gcp.run.namespaces.configurations.get [dependent] ──
  - discovery_id: gcp.run.namespaces.configurations.get
    for_each: gcp.run.projects.locations.configurations.list
    # python: svc.namespaces().configurations().get(**params).execute()
    calls:
      - action: namespaces.configurations.get
        params:
          name: '{{ item.kind }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiVersion: '{{ response.apiVersion }}'
        metadata: '{{ response.metadata }}'
        spec: '{{ response.spec }}'
        status: '{{ response.status }}'
    # required_params: ['name']
  # Get information about a domain mapping.
  # ── gcp.run.namespaces.domainmappings.get [dependent] ──
  - discovery_id: gcp.run.namespaces.domainmappings.get
    for_each: gcp.run.projects.locations.domainmappings.list
    # python: svc.namespaces().domainmappings().get(**params).execute()
    calls:
      - action: namespaces.domainmappings.get
        params:
          name: '{{ item.metadata }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiVersion: '{{ response.apiVersion }}'
        metadata: '{{ response.metadata }}'
        spec: '{{ response.spec }}'
        status: '{{ response.status }}'
    # required_params: ['name']
  # Get information about an execution.
  # ── gcp.run.namespaces.executions.get [dependent] ──
  - discovery_id: gcp.run.namespaces.executions.get
    for_each: gcp.run.namespaces.executions.list
    # python: svc.namespaces().executions().get(**params).execute()
    calls:
      - action: namespaces.executions.get
        params:
          name: '{{ item.metadata }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiVersion: '{{ response.apiVersion }}'
        metadata: '{{ response.metadata }}'
        spec: '{{ response.spec }}'
        status: '{{ response.status }}'
    # required_params: ['name']
  # Get an Instance.
  # ── gcp.run.namespaces.instances.get [dependent] ──
  - discovery_id: gcp.run.namespaces.instances.get
    for_each: gcp.run.namespaces.instances.list
    # python: svc.namespaces().instances().get(**params).execute()
    calls:
      - action: namespaces.instances.get
        params:
          name: '{{ item.kind }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiVersion: '{{ response.apiVersion }}'
        metadata: '{{ response.metadata }}'
        spec: '{{ response.spec }}'
        status: '{{ response.status }}'
    # required_params: ['name']
  # Get information about a job.
  # ── gcp.run.namespaces.jobs.get [dependent] ──
  - discovery_id: gcp.run.namespaces.jobs.get
    for_each: gcp.run.namespaces.jobs.list
    # python: svc.namespaces().jobs().get(**params).execute()
    calls:
      - action: namespaces.jobs.get
        params:
          name: '{{ item.spec }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiVersion: '{{ response.apiVersion }}'
        metadata: '{{ response.metadata }}'
        spec: '{{ response.spec }}'
        status: '{{ response.status }}'
    # required_params: ['name']
  # Get information about a revision.
  # ── gcp.run.namespaces.revisions.get [dependent] ──
  - discovery_id: gcp.run.namespaces.revisions.get
    for_each: gcp.run.projects.locations.revisions.list
    # python: svc.namespaces().revisions().get(**params).execute()
    calls:
      - action: namespaces.revisions.get
        params:
          name: '{{ item.spec }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiVersion: '{{ response.apiVersion }}'
        metadata: '{{ response.metadata }}'
        spec: '{{ response.spec }}'
        status: '{{ response.status }}'
    # required_params: ['name']
  # Get information about a route.
  # ── gcp.run.namespaces.routes.get [dependent] ──
  - discovery_id: gcp.run.namespaces.routes.get
    for_each: gcp.run.projects.locations.routes.list
    # python: svc.namespaces().routes().get(**params).execute()
    calls:
      - action: namespaces.routes.get
        params:
          name: '{{ item.status }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiVersion: '{{ response.apiVersion }}'
        metadata: '{{ response.metadata }}'
        spec: '{{ response.spec }}'
        status: '{{ response.status }}'
    # required_params: ['name']
  # Gets information about a service.
  # ── gcp.run.namespaces.services.get [dependent] ──
  - discovery_id: gcp.run.namespaces.services.get
    for_each: gcp.run.projects.locations.services.list
    # python: svc.namespaces().services().get(**params).execute()
    calls:
      - action: namespaces.services.get
        params:
          name: '{{ item.kind }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiVersion: '{{ response.apiVersion }}'
        metadata: '{{ response.metadata }}'
        spec: '{{ response.spec }}'
        status: '{{ response.status }}'
    # required_params: ['name']
  # Get information about a task.
  # ── gcp.run.namespaces.tasks.get [dependent] ──
  - discovery_id: gcp.run.namespaces.tasks.get
    for_each: gcp.run.namespaces.tasks.list
    # python: svc.namespaces().tasks().get(**params).execute()
    calls:
      - action: namespaces.tasks.get
        params:
          name: '{{ item.metadata }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiVersion: '{{ response.apiVersion }}'
        metadata: '{{ response.metadata }}'
        spec: '{{ response.spec }}'
        status: '{{ response.status }}'
    # required_params: ['name']
  # Gets information about a worker pool.
  # ── gcp.run.namespaces.workerpools.get [dependent] ──
  - discovery_id: gcp.run.namespaces.workerpools.get
    for_each: gcp.run.namespaces.workerpools.list
    # python: svc.namespaces().workerpools().get(**params).execute()
    calls:
      - action: namespaces.workerpools.get
        params:
          name: '{{ item.metadata }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiVersion: '{{ response.apiVersion }}'
        metadata: '{{ response.metadata }}'
        spec: '{{ response.spec }}'
        status: '{{ response.status }}'
    # required_params: ['name']
  # Get information about a configuration.
  # ── gcp.run.projects.locations.configurations.get [dependent] ──
  - discovery_id: gcp.run.projects.locations.configurations.get
    for_each: gcp.run.projects.locations.configurations.list
    # python: svc.projects().locations().configurations().get(**params).execute()
    calls:
      - action: projects.locations.configurations.get
        params:
          name: '{{ item.kind }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiVersion: '{{ response.apiVersion }}'
        metadata: '{{ response.metadata }}'
        spec: '{{ response.spec }}'
        status: '{{ response.status }}'
    # required_params: ['name']
  # Get information about a domain mapping.
  # ── gcp.run.projects.locations.domainmappings.get [dependent] ──
  - discovery_id: gcp.run.projects.locations.domainmappings.get
    for_each: gcp.run.projects.locations.domainmappings.list
    # python: svc.projects().locations().domainmappings().get(**params).execute()
    calls:
      - action: projects.locations.domainmappings.get
        params:
          name: '{{ item.metadata }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiVersion: '{{ response.apiVersion }}'
        metadata: '{{ response.metadata }}'
        spec: '{{ response.spec }}'
        status: '{{ response.status }}'
    # required_params: ['name']
  # Get the IAM Access Control policy currently in effect for the given instance. This result does not i
  # ── gcp.run.projects.locations.instances.getIamPolicy [dependent] ──
  - discovery_id: gcp.run.projects.locations.instances.getIamPolicy
    for_each: gcp.run.namespaces.instances.list
    # python: svc.projects().locations().instances().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.instances.getIamPolicy
        params:
          resource: '{{ item.kind }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Get the IAM Access Control policy currently in effect for the given job. This result does not includ
  # ── gcp.run.projects.locations.jobs.getIamPolicy [dependent] ──
  - discovery_id: gcp.run.projects.locations.jobs.getIamPolicy
    for_each: gcp.run.namespaces.jobs.list
    # python: svc.projects().locations().jobs().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.jobs.getIamPolicy
        params:
          resource: '{{ item.spec }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.run.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.run.projects.locations.operations.get
    for_each: gcp.run.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Get information about a revision.
  # ── gcp.run.projects.locations.revisions.get [dependent] ──
  - discovery_id: gcp.run.projects.locations.revisions.get
    for_each: gcp.run.projects.locations.revisions.list
    # python: svc.projects().locations().revisions().get(**params).execute()
    calls:
      - action: projects.locations.revisions.get
        params:
          name: '{{ item.spec }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiVersion: '{{ response.apiVersion }}'
        metadata: '{{ response.metadata }}'
        spec: '{{ response.spec }}'
        status: '{{ response.status }}'
    # required_params: ['name']
  # Get information about a route.
  # ── gcp.run.projects.locations.routes.get [dependent] ──
  - discovery_id: gcp.run.projects.locations.routes.get
    for_each: gcp.run.projects.locations.routes.list
    # python: svc.projects().locations().routes().get(**params).execute()
    calls:
      - action: projects.locations.routes.get
        params:
          name: '{{ item.status }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiVersion: '{{ response.apiVersion }}'
        metadata: '{{ response.metadata }}'
        spec: '{{ response.spec }}'
        status: '{{ response.status }}'
    # required_params: ['name']
  # Gets information about a service.
  # ── gcp.run.projects.locations.services.get [dependent] ──
  - discovery_id: gcp.run.projects.locations.services.get
    for_each: gcp.run.projects.locations.services.list
    # python: svc.projects().locations().services().get(**params).execute()
    calls:
      - action: projects.locations.services.get
        params:
          name: '{{ item.kind }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        apiVersion: '{{ response.apiVersion }}'
        metadata: '{{ response.metadata }}'
        spec: '{{ response.spec }}'
        status: '{{ response.status }}'
    # required_params: ['name']
  # Gets the IAM Access Control policy currently in effect for the given Cloud Run service. This result 
  # ── gcp.run.projects.locations.services.getIamPolicy [dependent] ──
  - discovery_id: gcp.run.projects.locations.services.getIamPolicy
    for_each: gcp.run.projects.locations.services.list
    # python: svc.projects().locations().services().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.services.getIamPolicy
        params:
          resource: '{{ item.kind }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Get the IAM Access Control policy currently in effect for the given worker pool. This result does no
  # ── gcp.run.projects.locations.workerpools.getIamPolicy [dependent] ──
  - discovery_id: gcp.run.projects.locations.workerpools.getIamPolicy
    for_each: gcp.run.namespaces.workerpools.list
    # python: svc.projects().locations().workerpools().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.workerpools.getIamPolicy
        params:
          resource: '{{ item.metadata }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
