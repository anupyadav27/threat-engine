# ============================================================
# Discovery YAML — contactcenteraiplatform (v1)
# Generated: 2026-02-21T03:06:39Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 6 | independent: 6 | dependent: 0
# ops with fields: 0/6 | dep ops with chain: 0/0
# ============================================================
version: '1.0'
provider: gcp
service: contactcenteraiplatform

services:
  client: contactcenteraiplatform
  module: "googleapiclient.discovery.build('contactcenteraiplatform', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Gets details of a single ContactCenter.
  # ── gcp.contactcenteraiplatform.contactCenters.get ──
  - discovery_id: gcp.contactcenteraiplatform.contactCenters.get
    calls:
      - action: contactCenters.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
  # Lists ContactCenters in a given project and location.
  # ── gcp.contactcenteraiplatform.contactCenters.list ──
  - discovery_id: gcp.contactcenteraiplatform.contactCenters.list
    calls:
      - action: contactCenters.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.contactCenters }}'
  # Gets information about a location.
  # ── gcp.contactcenteraiplatform.locations.get ──
  - discovery_id: gcp.contactcenteraiplatform.locations.get
    calls:
      - action: locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.contactcenteraiplatform.locations.list ──
  - discovery_id: gcp.contactcenteraiplatform.locations.list
    calls:
      - action: locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.contactcenteraiplatform.operations.get ──
  - discovery_id: gcp.contactcenteraiplatform.operations.get
    calls:
      - action: operations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.contactcenteraiplatform.operations.list ──
  - discovery_id: gcp.contactcenteraiplatform.operations.list
    calls:
      - action: operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
