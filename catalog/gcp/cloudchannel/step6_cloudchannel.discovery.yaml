# ============================================================
# Discovery YAML — cloudchannel (v1)
# Generated: 2026-02-21T03:06:39Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 27 | independent: 18 | dependent: 9
# ops with fields: 25/27 | dep ops with chain: 7/9
# ============================================================
version: '1.0'
provider: gcp
service: cloudchannel

services:
  client: cloudchannel
  module: "googleapiclient.discovery.build('cloudchannel', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists information about how a Reseller modifies their bill before sending it to a ChannelPartner. Po
  # ── gcp.cloudchannel.accounts.channelPartnerLinks.channelPartnerRepricingConfigs.list ──
  - discovery_id: gcp.cloudchannel.accounts.channelPartnerLinks.channelPartnerRepricingConfigs.list
    # python: svc.accounts().channelPartnerLinks().channelPartnerRepricingConfigs().list(**params).execute()
    calls:
      - action: accounts.channelPartnerLinks.channelPartnerRepricingConfigs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.channelPartnerRepricingConfigs }}'
      item:
        name: '{{ item.name }}'
        repricingConfig: '{{ item.repricingConfig }}'
        updateTime: '{{ item.updateTime }}'
  # List Customers. Possible error codes: * PERMISSION_DENIED: The reseller account making the request i
  # ── gcp.cloudchannel.accounts.channelPartnerLinks.customers.list ──
  - discovery_id: gcp.cloudchannel.accounts.channelPartnerLinks.customers.list
    # python: svc.accounts().channelPartnerLinks().customers().list(**params).execute()
    calls:
      - action: accounts.channelPartnerLinks.customers.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.customers }}'
      item:
        alternateEmail: '{{ item.alternateEmail }}'
        channelPartnerId: '{{ item.channelPartnerId }}'
        cloudIdentityId: '{{ item.cloudIdentityId }}'
        cloudIdentityInfo: '{{ item.cloudIdentityInfo }}'
        correlationId: '{{ item.correlationId }}'
        createTime: '{{ item.createTime }}'
        customerAttestationState: '{{ item.customerAttestationState }}'
        domain: '{{ item.domain }}'
        languageCode: '{{ item.languageCode }}'
        name: '{{ item.name }}'
        orgDisplayName: '{{ item.orgDisplayName }}'
        orgPostalAddress: '{{ item.orgPostalAddress }}'
        primaryContactInfo: '{{ item.primaryContactInfo }}'
        updateTime: '{{ item.updateTime }}'
  # List ChannelPartnerLinks belonging to a distributor. You must be a distributor to call this method. 
  # ── gcp.cloudchannel.accounts.channelPartnerLinks.list ──
  - discovery_id: gcp.cloudchannel.accounts.channelPartnerLinks.list
    # python: svc.accounts().channelPartnerLinks().list(**params).execute()
    calls:
      - action: accounts.channelPartnerLinks.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.channelPartnerLinks }}'
      item:
        channelPartnerCloudIdentityInfo: '{{ item.channelPartnerCloudIdentityInfo }}'
        createTime: '{{ item.createTime }}'
        inviteLinkUri: '{{ item.inviteLinkUri }}'
        linkState: '{{ item.linkState }}'
        name: '{{ item.name }}'
        publicId: '{{ item.publicId }}'
        resellerCloudIdentityId: '{{ item.resellerCloudIdentityId }}'
        updateTime: '{{ item.updateTime }}'
  # Lists information about how a Reseller modifies their bill before sending it to a Customer. Possible
  # ── gcp.cloudchannel.accounts.customers.customerRepricingConfigs.list ──
  - discovery_id: gcp.cloudchannel.accounts.customers.customerRepricingConfigs.list
    # python: svc.accounts().customers().customerRepricingConfigs().list(**params).execute()
    calls:
      - action: accounts.customers.customerRepricingConfigs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.customerRepricingConfigs }}'
      item:
        name: '{{ item.name }}'
        repricingConfig: '{{ item.repricingConfig }}'
        updateTime: '{{ item.updateTime }}'
  # Lists Entitlements belonging to a customer. Possible error codes: * PERMISSION_DENIED: The customer 
  # ── gcp.cloudchannel.accounts.customers.entitlements.list ──
  - discovery_id: gcp.cloudchannel.accounts.customers.entitlements.list
    # python: svc.accounts().customers().entitlements().list(**params).execute()
    calls:
      - action: accounts.customers.entitlements.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.entitlements }}'
      item:
        associationInfo: '{{ item.associationInfo }}'
        billingAccount: '{{ item.billingAccount }}'
        commitmentSettings: '{{ item.commitmentSettings }}'
        createTime: '{{ item.createTime }}'
        name: '{{ item.name }}'
        offer: '{{ item.offer }}'
        parameters: '{{ item.parameters }}'
        priceReferenceId: '{{ item.priceReferenceId }}'
        provisionedService: '{{ item.provisionedService }}'
        provisioningState: '{{ item.provisioningState }}'
        purchaseOrderId: '{{ item.purchaseOrderId }}'
        suspensionReasons: '{{ item.suspensionReasons }}'
        trialSettings: '{{ item.trialSettings }}'
        updateTime: '{{ item.updateTime }}'
  # List entitlement history. Possible error codes: * PERMISSION_DENIED: The reseller account making the
  # ── gcp.cloudchannel.accounts.customers.entitlements.listEntitlementChanges ──
  - discovery_id: gcp.cloudchannel.accounts.customers.entitlements.listEntitlementChanges
    # python: svc.accounts().customers().entitlements().listEntitlementChanges(**params).execute()
    calls:
      - action: accounts.customers.entitlements.listEntitlementChanges
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.entitlements }}'
      item:
        activationReason: '{{ item.activationReason }}'
        cancellationReason: '{{ item.cancellationReason }}'
        changeType: '{{ item.changeType }}'
        createTime: '{{ item.createTime }}'
        entitlement: '{{ item.entitlement }}'
        offer: '{{ item.offer }}'
        operator: '{{ item.operator }}'
        operatorType: '{{ item.operatorType }}'
        otherChangeReason: '{{ item.otherChangeReason }}'
        parameters: '{{ item.parameters }}'
        provisionedService: '{{ item.provisionedService }}'
        suspensionReason: '{{ item.suspensionReason }}'
  # Returns the requested Customer resource. Possible error codes: * PERMISSION_DENIED: The reseller acc
  # ── gcp.cloudchannel.accounts.customers.get ──
  - discovery_id: gcp.cloudchannel.accounts.customers.get
    # python: svc.accounts().customers().get(**params).execute()
    calls:
      - action: accounts.customers.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        alternateEmail: '{{ response.alternateEmail }}'
        channelPartnerId: '{{ response.channelPartnerId }}'
        cloudIdentityId: '{{ response.cloudIdentityId }}'
        cloudIdentityInfo: '{{ response.cloudIdentityInfo }}'
        correlationId: '{{ response.correlationId }}'
        createTime: '{{ response.createTime }}'
        customerAttestationState: '{{ response.customerAttestationState }}'
        domain: '{{ response.domain }}'
        languageCode: '{{ response.languageCode }}'
        name: '{{ response.name }}'
        orgDisplayName: '{{ response.orgDisplayName }}'
        orgPostalAddress: '{{ response.orgPostalAddress }}'
        primaryContactInfo: '{{ response.primaryContactInfo }}'
        updateTime: '{{ response.updateTime }}'
  # List Customers. Possible error codes: * PERMISSION_DENIED: The reseller account making the request i
  # ── gcp.cloudchannel.accounts.customers.list ──
  - discovery_id: gcp.cloudchannel.accounts.customers.list
    # python: svc.accounts().customers().list(**params).execute()
    calls:
      - action: accounts.customers.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.customers }}'
      item:
        alternateEmail: '{{ item.alternateEmail }}'
        channelPartnerId: '{{ item.channelPartnerId }}'
        cloudIdentityId: '{{ item.cloudIdentityId }}'
        cloudIdentityInfo: '{{ item.cloudIdentityInfo }}'
        correlationId: '{{ item.correlationId }}'
        createTime: '{{ item.createTime }}'
        customerAttestationState: '{{ item.customerAttestationState }}'
        domain: '{{ item.domain }}'
        languageCode: '{{ item.languageCode }}'
        name: '{{ item.name }}'
        orgDisplayName: '{{ item.orgDisplayName }}'
        orgPostalAddress: '{{ item.orgPostalAddress }}'
        primaryContactInfo: '{{ item.primaryContactInfo }}'
        updateTime: '{{ item.updateTime }}'
  # Lists the following: * Offers that you can purchase for a customer. * Offers that you can change for
  # ── gcp.cloudchannel.accounts.customers.listPurchasableOffers ──
  - discovery_id: gcp.cloudchannel.accounts.customers.listPurchasableOffers
    # python: svc.accounts().customers().listPurchasableOffers(**params).execute()
    calls:
      - action: accounts.customers.listPurchasableOffers
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.customers }}'
      item:
        offer: '{{ item.offer }}'
        priceReferenceId: '{{ item.priceReferenceId }}'
  # Lists the following: * SKUs that you can purchase for a customer * SKUs that you can upgrade or down
  # ── gcp.cloudchannel.accounts.customers.listPurchasableSkus ──
  - discovery_id: gcp.cloudchannel.accounts.customers.listPurchasableSkus
    # python: svc.accounts().customers().listPurchasableSkus(**params).execute()
    calls:
      - action: accounts.customers.listPurchasableSkus
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.customers }}'
      item:
        sku: '{{ item.sku }}'
  # Lists the billing accounts that are eligible to purchase particular SKUs for a given customer. Possi
  # ── gcp.cloudchannel.accounts.customers.queryEligibleBillingAccounts ──
  - discovery_id: gcp.cloudchannel.accounts.customers.queryEligibleBillingAccounts
    # python: svc.accounts().customers().queryEligibleBillingAccounts(**params).execute()
    calls:
      - action: accounts.customers.queryEligibleBillingAccounts
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        skuPurchaseGroups: '{{ response.skuPurchaseGroups }}'
  # Lists service accounts with subscriber privileges on the Pub/Sub topic created for this Channel Serv
  # ── gcp.cloudchannel.accounts.listSubscribers ──
  - discovery_id: gcp.cloudchannel.accounts.listSubscribers
    # python: svc.accounts().listSubscribers(**params).execute()
    calls:
      - action: accounts.listSubscribers
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.accounts }}'
  # Lists the Offers the reseller can sell. Possible error codes: * INVALID_ARGUMENT: Required request p
  # ── gcp.cloudchannel.accounts.offers.list ──
  - discovery_id: gcp.cloudchannel.accounts.offers.list
    # python: svc.accounts().offers().list(**params).execute()
    calls:
      - action: accounts.offers.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.offers }}'
      item:
        constraints: '{{ item.constraints }}'
        dealCode: '{{ item.dealCode }}'
        endTime: '{{ item.endTime }}'
        marketingInfo: '{{ item.marketingInfo }}'
        name: '{{ item.name }}'
        parameterDefinitions: '{{ item.parameterDefinitions }}'
        plan: '{{ item.plan }}'
        priceByResources: '{{ item.priceByResources }}'
        sku: '{{ item.sku }}'
        startTime: '{{ item.startTime }}'
  # Lists the reports that RunReportJob can run. These reports include an ID, a description, and the lis
  # ── gcp.cloudchannel.accounts.reports.list ──
  - discovery_id: gcp.cloudchannel.accounts.reports.list
    # python: svc.accounts().reports().list(**params).execute()
    calls:
      - action: accounts.reports.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.reports }}'
      item:
        columns: '{{ item.columns }}'
        description: '{{ item.description }}'
        displayName: '{{ item.displayName }}'
        name: '{{ item.name }}'
  # Lists the Billable SKUs in a given SKU group. Possible error codes: PERMISSION_DENIED: If the accoun
  # ── gcp.cloudchannel.accounts.skuGroups.billableSkus.list ──
  - discovery_id: gcp.cloudchannel.accounts.skuGroups.billableSkus.list
    # python: svc.accounts().skuGroups().billableSkus().list(**params).execute()
    calls:
      - action: accounts.skuGroups.billableSkus.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.billableSkus }}'
      item:
        service: '{{ item.service }}'
        serviceDisplayName: '{{ item.serviceDisplayName }}'
        sku: '{{ item.sku }}'
        skuDisplayName: '{{ item.skuDisplayName }}'
  # Lists the Rebilling supported SKU groups the account is authorized to sell. Reference: https://cloud
  # ── gcp.cloudchannel.accounts.skuGroups.list ──
  - discovery_id: gcp.cloudchannel.accounts.skuGroups.list
    # python: svc.accounts().skuGroups().list(**params).execute()
    calls:
      - action: accounts.skuGroups.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.skuGroups }}'
      item:
        displayName: '{{ item.displayName }}'
        name: '{{ item.name }}'
  # Lists the Products the reseller is authorized to sell. Possible error codes: * INVALID_ARGUMENT: Req
  # ── gcp.cloudchannel.products.list ──
  - discovery_id: gcp.cloudchannel.products.list
    # python: svc.products().list(**params).execute()
    calls:
      - action: products.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.products }}'
      item:
        marketingInfo: '{{ item.marketingInfo }}'
        name: '{{ item.name }}'
  # Lists the SKUs for a product the reseller is authorized to sell. Possible error codes: * INVALID_ARG
  # ── gcp.cloudchannel.products.skus.list ──
  - discovery_id: gcp.cloudchannel.products.skus.list
    # python: svc.products().skus().list(**params).execute()
    calls:
      - action: products.skus.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.skus }}'
      item:
        marketingInfo: '{{ item.marketingInfo }}'
        name: '{{ item.name }}'
        product: '{{ item.product }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets information about how a Distributor modifies their bill before sending it to a ChannelPartner. 
  # ── gcp.cloudchannel.accounts.channelPartnerLinks.channelPartnerRepricingConfigs.get [dependent] ──
  - discovery_id: gcp.cloudchannel.accounts.channelPartnerLinks.channelPartnerRepricingConfigs.get
    for_each: gcp.cloudchannel.accounts.channelPartnerLinks.channelPartnerRepricingConfigs.list
    # python: svc.accounts().channelPartnerLinks().channelPartnerRepricingConfigs().get(**params).execute()
    calls:
      - action: accounts.channelPartnerLinks.channelPartnerRepricingConfigs.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        repricingConfig: '{{ response.repricingConfig }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Returns the requested Customer resource. Possible error codes: * PERMISSION_DENIED: The reseller acc
  # ── gcp.cloudchannel.accounts.channelPartnerLinks.customers.get [dependent] ──
  - discovery_id: gcp.cloudchannel.accounts.channelPartnerLinks.customers.get
    for_each: gcp.cloudchannel.accounts.channelPartnerLinks.list
    # python: svc.accounts().channelPartnerLinks().customers().get(**params).execute()
    calls:
      - action: accounts.channelPartnerLinks.customers.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        alternateEmail: '{{ response.alternateEmail }}'
        channelPartnerId: '{{ response.channelPartnerId }}'
        cloudIdentityId: '{{ response.cloudIdentityId }}'
        cloudIdentityInfo: '{{ response.cloudIdentityInfo }}'
        correlationId: '{{ response.correlationId }}'
        createTime: '{{ response.createTime }}'
        customerAttestationState: '{{ response.customerAttestationState }}'
        domain: '{{ response.domain }}'
        languageCode: '{{ response.languageCode }}'
        name: '{{ response.name }}'
        orgDisplayName: '{{ response.orgDisplayName }}'
        orgPostalAddress: '{{ response.orgPostalAddress }}'
        primaryContactInfo: '{{ response.primaryContactInfo }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Returns the requested ChannelPartnerLink resource. You must be a distributor to call this method. Po
  # ── gcp.cloudchannel.accounts.channelPartnerLinks.get [dependent] ──
  - discovery_id: gcp.cloudchannel.accounts.channelPartnerLinks.get
    for_each: gcp.cloudchannel.accounts.channelPartnerLinks.list
    # python: svc.accounts().channelPartnerLinks().get(**params).execute()
    calls:
      - action: accounts.channelPartnerLinks.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        channelPartnerCloudIdentityInfo: '{{ response.channelPartnerCloudIdentityInfo }}'
        createTime: '{{ response.createTime }}'
        inviteLinkUri: '{{ response.inviteLinkUri }}'
        linkState: '{{ response.linkState }}'
        name: '{{ response.name }}'
        publicId: '{{ response.publicId }}'
        resellerCloudIdentityId: '{{ response.resellerCloudIdentityId }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets information about how a Reseller modifies their bill before sending it to a Customer. Possible 
  # ── gcp.cloudchannel.accounts.customers.customerRepricingConfigs.get [dependent] ──
  - discovery_id: gcp.cloudchannel.accounts.customers.customerRepricingConfigs.get
    for_each: gcp.cloudchannel.accounts.customers.customerRepricingConfigs.list
    # python: svc.accounts().customers().customerRepricingConfigs().get(**params).execute()
    calls:
      - action: accounts.customers.customerRepricingConfigs.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        repricingConfig: '{{ response.repricingConfig }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Returns the requested Entitlement resource. Possible error codes: * PERMISSION_DENIED: The customer 
  # ── gcp.cloudchannel.accounts.customers.entitlements.get [dependent] ──
  - discovery_id: gcp.cloudchannel.accounts.customers.entitlements.get
    for_each: gcp.cloudchannel.accounts.customers.entitlements.list
    # python: svc.accounts().customers().entitlements().get(**params).execute()
    calls:
      - action: accounts.customers.entitlements.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        associationInfo: '{{ response.associationInfo }}'
        billingAccount: '{{ response.billingAccount }}'
        commitmentSettings: '{{ response.commitmentSettings }}'
        createTime: '{{ response.createTime }}'
        name: '{{ response.name }}'
        offer: '{{ response.offer }}'
        parameters: '{{ response.parameters }}'
        priceReferenceId: '{{ response.priceReferenceId }}'
        provisionedService: '{{ response.provisionedService }}'
        provisioningState: '{{ response.provisioningState }}'
        purchaseOrderId: '{{ response.purchaseOrderId }}'
        suspensionReasons: '{{ response.suspensionReasons }}'
        trialSettings: '{{ response.trialSettings }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Returns the requested Offer resource. Possible error codes: * PERMISSION_DENIED: The entitlement doe
  # ── gcp.cloudchannel.accounts.customers.entitlements.lookupOffer [dependent] ──
  - discovery_id: gcp.cloudchannel.accounts.customers.entitlements.lookupOffer
    for_each: gcp.cloudchannel.accounts.customers.entitlements.list
    # python: svc.accounts().customers().entitlements().lookupOffer(**params).execute()
    calls:
      - action: accounts.customers.entitlements.lookupOffer
        params:
          entitlement: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        constraints: '{{ response.constraints }}'
        dealCode: '{{ response.dealCode }}'
        endTime: '{{ response.endTime }}'
        marketingInfo: '{{ response.marketingInfo }}'
        name: '{{ response.name }}'
        parameterDefinitions: '{{ response.parameterDefinitions }}'
        plan: '{{ response.plan }}'
        priceByResources: '{{ response.priceByResources }}'
        sku: '{{ response.sku }}'
        startTime: '{{ response.startTime }}'
    # required_params: ['entitlement']
  # Lists service accounts with subscriber privileges on the Pub/Sub topic created for this Channel Serv
  # ── gcp.cloudchannel.integrators.listSubscribers [dependent] ──
  - discovery_id: gcp.cloudchannel.integrators.listSubscribers
    # python: svc.integrators().listSubscribers(**params).execute()
    calls:
      - action: integrators.listSubscribers
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.integrators }}'
    # required_params: ['integrator']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.cloudchannel.operations.get [dependent] ──
  - discovery_id: gcp.cloudchannel.operations.get
    for_each: gcp.cloudchannel.operations.list
    # python: svc.operations().get(**params).execute()
    calls:
      - action: operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.cloudchannel.operations.list [dependent] ──
  - discovery_id: gcp.cloudchannel.operations.list
    # python: svc.operations().list(**params).execute()
    calls:
      - action: operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
    # required_params: ['name']
