# ============================================================
# Discovery YAML — networkmanagement (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 17 | independent: 11 | dependent: 6
# ops with fields: 17/17 | dep ops with chain: 6/6
# ============================================================
version: '1.0'
provider: gcp
service: networkmanagement

services:
  client: networkmanagement
  module: "googleapiclient.discovery.build('networkmanagement', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Gets information about a location.
  # ── gcp.networkmanagement.organizations.locations.get ──
  - discovery_id: gcp.networkmanagement.organizations.locations.get
    # python: svc.organizations().locations().get(**params).execute()
    calls:
      - action: organizations.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.networkmanagement.organizations.locations.global.operations.list ──
  - discovery_id: gcp.networkmanagement.organizations.locations.global.operations.list
    # python: svc.organizations().locations().global().operations().list(**params).execute()
    calls:
      - action: organizations.locations.global.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.networkmanagement.organizations.locations.list ──
  - discovery_id: gcp.networkmanagement.organizations.locations.list
    # python: svc.organizations().locations().list(**params).execute()
    calls:
      - action: organizations.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists all `VpcFlowLogsConfigs` in a given organization.
  # ── gcp.networkmanagement.organizations.locations.vpcFlowLogsConfigs.list ──
  - discovery_id: gcp.networkmanagement.organizations.locations.vpcFlowLogsConfigs.list
    # python: svc.organizations().locations().vpcFlowLogsConfigs().list(**params).execute()
    calls:
      - action: organizations.locations.vpcFlowLogsConfigs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.vpcFlowLogsConfigs }}'
      item:
        aggregationInterval: '{{ item.aggregationInterval }}'
        createTime: '{{ item.createTime }}'
        crossProjectMetadata: '{{ item.crossProjectMetadata }}'
        description: '{{ item.description }}'
        filterExpr: '{{ item.filterExpr }}'
        flowSampling: '{{ item.flowSampling }}'
        interconnectAttachment: '{{ item.interconnectAttachment }}'
        labels: '{{ item.labels }}'
        metadata: '{{ item.metadata }}'
        metadataFields: '{{ item.metadataFields }}'
        name: '{{ item.name }}'
        network: '{{ item.network }}'
        state: '{{ item.state }}'
        subnet: '{{ item.subnet }}'
        targetResourceState: '{{ item.targetResourceState }}'
        updateTime: '{{ item.updateTime }}'
        vpnTunnel: '{{ item.vpnTunnel }}'
  # Gets information about a location.
  # ── gcp.networkmanagement.projects.locations.get ──
  - discovery_id: gcp.networkmanagement.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists all Connectivity Tests owned by a project.
  # ── gcp.networkmanagement.projects.locations.global.connectivityTests.list ──
  - discovery_id: gcp.networkmanagement.projects.locations.global.connectivityTests.list
    # python: svc.projects().locations().global().connectivityTests().list(**params).execute()
    calls:
      - action: projects.locations.global.connectivityTests.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.connectivityTests }}'
      item:
        bypassFirewallChecks: '{{ item.bypassFirewallChecks }}'
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        destination: '{{ item.destination }}'
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        probingDetails: '{{ item.probingDetails }}'
        protocol: '{{ item.protocol }}'
        reachabilityDetails: '{{ item.reachabilityDetails }}'
        relatedProjects: '{{ item.relatedProjects }}'
        returnReachabilityDetails: '{{ item.returnReachabilityDetails }}'
        roundTrip: '{{ item.roundTrip }}'
        source: '{{ item.source }}'
        updateTime: '{{ item.updateTime }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.networkmanagement.projects.locations.global.operations.list ──
  - discovery_id: gcp.networkmanagement.projects.locations.global.operations.list
    # python: svc.projects().locations().global().operations().list(**params).execute()
    calls:
      - action: projects.locations.global.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.networkmanagement.projects.locations.list ──
  - discovery_id: gcp.networkmanagement.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists all `VpcFlowLogsConfigs` in a given project.
  # ── gcp.networkmanagement.projects.locations.vpcFlowLogsConfigs.list ──
  - discovery_id: gcp.networkmanagement.projects.locations.vpcFlowLogsConfigs.list
    # python: svc.projects().locations().vpcFlowLogsConfigs().list(**params).execute()
    calls:
      - action: projects.locations.vpcFlowLogsConfigs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.vpcFlowLogsConfigs }}'
      item:
        aggregationInterval: '{{ item.aggregationInterval }}'
        createTime: '{{ item.createTime }}'
        crossProjectMetadata: '{{ item.crossProjectMetadata }}'
        description: '{{ item.description }}'
        filterExpr: '{{ item.filterExpr }}'
        flowSampling: '{{ item.flowSampling }}'
        interconnectAttachment: '{{ item.interconnectAttachment }}'
        labels: '{{ item.labels }}'
        metadata: '{{ item.metadata }}'
        metadataFields: '{{ item.metadataFields }}'
        name: '{{ item.name }}'
        network: '{{ item.network }}'
        state: '{{ item.state }}'
        subnet: '{{ item.subnet }}'
        targetResourceState: '{{ item.targetResourceState }}'
        updateTime: '{{ item.updateTime }}'
        vpnTunnel: '{{ item.vpnTunnel }}'
  # QueryOrgVpcFlowLogsConfigs returns a list of all organization-level VPC Flow Logs configurations app
  # ── gcp.networkmanagement.projects.locations.vpcFlowLogsConfigs.queryOrgVpcFlowLogsConfigs ──
  - discovery_id: gcp.networkmanagement.projects.locations.vpcFlowLogsConfigs.queryOrgVpcFlowLogsConfigs
    # python: svc.projects().locations().vpcFlowLogsConfigs().queryOrgVpcFlowLogsConfigs(**params).execute()
    calls:
      - action: projects.locations.vpcFlowLogsConfigs.queryOrgVpcFlowLogsConfigs
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        vpcFlowLogsConfigs: '{{ response.vpcFlowLogsConfigs }}'
  # ShowEffectiveFlowLogsConfigs returns a list of all VPC Flow Logs configurations applicable to a spec
  # ── gcp.networkmanagement.projects.locations.vpcFlowLogsConfigs.showEffectiveFlowLogsConfigs ──
  - discovery_id: gcp.networkmanagement.projects.locations.vpcFlowLogsConfigs.showEffectiveFlowLogsConfigs
    # python: svc.projects().locations().vpcFlowLogsConfigs().showEffectiveFlowLogsConfigs(**params).execute()
    calls:
      - action: projects.locations.vpcFlowLogsConfigs.showEffectiveFlowLogsConfigs
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        effectiveFlowLogsConfigs: '{{ response.effectiveFlowLogsConfigs }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.networkmanagement.organizations.locations.global.operations.get [dependent] ──
  - discovery_id: gcp.networkmanagement.organizations.locations.global.operations.get
    for_each: gcp.networkmanagement.projects.locations.global.operations.list
    # python: svc.organizations().locations().global().operations().get(**params).execute()
    calls:
      - action: organizations.locations.global.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets the details of a specific `VpcFlowLogsConfig`.
  # ── gcp.networkmanagement.organizations.locations.vpcFlowLogsConfigs.get [dependent] ──
  - discovery_id: gcp.networkmanagement.organizations.locations.vpcFlowLogsConfigs.get
    for_each: gcp.networkmanagement.projects.locations.vpcFlowLogsConfigs.list
    # python: svc.organizations().locations().vpcFlowLogsConfigs().get(**params).execute()
    calls:
      - action: organizations.locations.vpcFlowLogsConfigs.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        aggregationInterval: '{{ response.aggregationInterval }}'
        createTime: '{{ response.createTime }}'
        crossProjectMetadata: '{{ response.crossProjectMetadata }}'
        description: '{{ response.description }}'
        filterExpr: '{{ response.filterExpr }}'
        flowSampling: '{{ response.flowSampling }}'
        interconnectAttachment: '{{ response.interconnectAttachment }}'
        labels: '{{ response.labels }}'
        metadata: '{{ response.metadata }}'
        metadataFields: '{{ response.metadataFields }}'
        name: '{{ response.name }}'
        network: '{{ response.network }}'
        state: '{{ response.state }}'
        subnet: '{{ response.subnet }}'
        targetResourceState: '{{ response.targetResourceState }}'
        updateTime: '{{ response.updateTime }}'
        vpnTunnel: '{{ response.vpnTunnel }}'
    # required_params: ['name']
  # Gets the details of a specific Connectivity Test.
  # ── gcp.networkmanagement.projects.locations.global.connectivityTests.get [dependent] ──
  - discovery_id: gcp.networkmanagement.projects.locations.global.connectivityTests.get
    for_each: gcp.networkmanagement.projects.locations.global.connectivityTests.list
    # python: svc.projects().locations().global().connectivityTests().get(**params).execute()
    calls:
      - action: projects.locations.global.connectivityTests.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        bypassFirewallChecks: '{{ response.bypassFirewallChecks }}'
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        destination: '{{ response.destination }}'
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        probingDetails: '{{ response.probingDetails }}'
        protocol: '{{ response.protocol }}'
        reachabilityDetails: '{{ response.reachabilityDetails }}'
        relatedProjects: '{{ response.relatedProjects }}'
        returnReachabilityDetails: '{{ response.returnReachabilityDetails }}'
        roundTrip: '{{ response.roundTrip }}'
        source: '{{ response.source }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.networkmanagement.projects.locations.global.connectivityTests.getIamPolicy [dependent] ──
  - discovery_id: gcp.networkmanagement.projects.locations.global.connectivityTests.getIamPolicy
    for_each: gcp.networkmanagement.projects.locations.global.connectivityTests.list
    # python: svc.projects().locations().global().connectivityTests().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.global.connectivityTests.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.networkmanagement.projects.locations.global.operations.get [dependent] ──
  - discovery_id: gcp.networkmanagement.projects.locations.global.operations.get
    for_each: gcp.networkmanagement.projects.locations.global.operations.list
    # python: svc.projects().locations().global().operations().get(**params).execute()
    calls:
      - action: projects.locations.global.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets the details of a specific `VpcFlowLogsConfig`.
  # ── gcp.networkmanagement.projects.locations.vpcFlowLogsConfigs.get [dependent] ──
  - discovery_id: gcp.networkmanagement.projects.locations.vpcFlowLogsConfigs.get
    for_each: gcp.networkmanagement.projects.locations.vpcFlowLogsConfigs.list
    # python: svc.projects().locations().vpcFlowLogsConfigs().get(**params).execute()
    calls:
      - action: projects.locations.vpcFlowLogsConfigs.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        aggregationInterval: '{{ response.aggregationInterval }}'
        createTime: '{{ response.createTime }}'
        crossProjectMetadata: '{{ response.crossProjectMetadata }}'
        description: '{{ response.description }}'
        filterExpr: '{{ response.filterExpr }}'
        flowSampling: '{{ response.flowSampling }}'
        interconnectAttachment: '{{ response.interconnectAttachment }}'
        labels: '{{ response.labels }}'
        metadata: '{{ response.metadata }}'
        metadataFields: '{{ response.metadataFields }}'
        name: '{{ response.name }}'
        network: '{{ response.network }}'
        state: '{{ response.state }}'
        subnet: '{{ response.subnet }}'
        targetResourceState: '{{ response.targetResourceState }}'
        updateTime: '{{ response.updateTime }}'
        vpnTunnel: '{{ response.vpnTunnel }}'
    # required_params: ['name']
