# ============================================================
# Discovery YAML — searchads360 (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 5 | independent: 5 | dependent: 0
# ops with fields: 0/5 | dep ops with chain: 0/0
# ============================================================
version: '1.0'
provider: gcp
service: searchads360

services:
  client: searchads360
  module: "googleapiclient.discovery.build('searchads360', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Returns the requested custom column in full detail.
  # ── gcp.searchads360.customColumns.get ──
  - discovery_id: gcp.searchads360.customColumns.get
    calls:
      - action: customColumns.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
  # Returns all the custom columns associated with the customer in full detail.
  # ── gcp.searchads360.customColumns.list ──
  - discovery_id: gcp.searchads360.customColumns.list
    calls:
      - action: customColumns.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.customColumns }}'
  # Returns all rows that match the search query. List of thrown errors: [AuthenticationError]() [Author
  # ── gcp.searchads360.searchAds360.search ──
  - discovery_id: gcp.searchads360.searchAds360.search
    calls:
      - action: searchAds360.search
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.searchAds360 }}'
  # Returns just the requested field. List of thrown errors: [AuthenticationError]() [AuthorizationError
  # ── gcp.searchads360.searchAds360Fields.get ──
  - discovery_id: gcp.searchads360.searchAds360Fields.get
    calls:
      - action: searchAds360Fields.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
  # Returns all fields that match the search [query](/search-ads/reporting/concepts/field-service#use_a_
  # ── gcp.searchads360.searchAds360Fields.search ──
  - discovery_id: gcp.searchads360.searchAds360Fields.search
    calls:
      - action: searchAds360Fields.search
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.searchAds360Fields }}'
