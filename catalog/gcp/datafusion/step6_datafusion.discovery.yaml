# ============================================================
# Discovery YAML — datafusion (v1)
# Generated: 2026-02-21T03:06:39Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 9 | independent: 6 | dependent: 3
# ops with fields: 9/9 | dep ops with chain: 3/3
# ============================================================
version: '1.0'
provider: gcp
service: datafusion

services:
  client: datafusion
  module: "googleapiclient.discovery.build('datafusion', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Gets information about a location.
  # ── gcp.datafusion.projects.locations.get ──
  - discovery_id: gcp.datafusion.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists DNS peerings for a given resource.
  # ── gcp.datafusion.projects.locations.instances.dnsPeerings.list ──
  - discovery_id: gcp.datafusion.projects.locations.instances.dnsPeerings.list
    # python: svc.projects().locations().instances().dnsPeerings().list(**params).execute()
    calls:
      - action: projects.locations.instances.dnsPeerings.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.dnsPeerings }}'
      item:
        description: '{{ item.description }}'
        domain: '{{ item.domain }}'
        name: '{{ item.name }}'
        targetNetwork: '{{ item.targetNetwork }}'
        targetProject: '{{ item.targetProject }}'
  # Lists Data Fusion instances in the specified project and location.
  # ── gcp.datafusion.projects.locations.instances.list ──
  - discovery_id: gcp.datafusion.projects.locations.instances.list
    # python: svc.projects().locations().instances().list(**params).execute()
    calls:
      - action: projects.locations.instances.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.instances }}'
      item:
        accelerators: '{{ item.accelerators }}'
        apiEndpoint: '{{ item.apiEndpoint }}'
        availableVersion: '{{ item.availableVersion }}'
        createTime: '{{ item.createTime }}'
        cryptoKeyConfig: '{{ item.cryptoKeyConfig }}'
        dataplexDataLineageIntegrationEnabled: '{{ item.dataplexDataLineageIntegrationEnabled }}'
        dataprocServiceAccount: '{{ item.dataprocServiceAccount }}'
        description: '{{ item.description }}'
        disabledReason: '{{ item.disabledReason }}'
        displayName: '{{ item.displayName }}'
        enableRbac: '{{ item.enableRbac }}'
        enableStackdriverLogging: '{{ item.enableStackdriverLogging }}'
        enableStackdriverMonitoring: '{{ item.enableStackdriverMonitoring }}'
        enableZoneSeparation: '{{ item.enableZoneSeparation }}'
        eventPublishConfig: '{{ item.eventPublishConfig }}'
        gcsBucket: '{{ item.gcsBucket }}'
        labels: '{{ item.labels }}'
        loggingConfig: '{{ item.loggingConfig }}'
        maintenanceEvents: '{{ item.maintenanceEvents }}'
        maintenancePolicy: '{{ item.maintenancePolicy }}'
        monitoringConfig: '{{ item.monitoringConfig }}'
        name: '{{ item.name }}'
        networkConfig: '{{ item.networkConfig }}'
        options: '{{ item.options }}'
        p4ServiceAccount: '{{ item.p4ServiceAccount }}'
        patchRevision: '{{ item.patchRevision }}'
        privateInstance: '{{ item.privateInstance }}'
        satisfiesPzi: '{{ item.satisfiesPzi }}'
        satisfiesPzs: '{{ item.satisfiesPzs }}'
        serviceAccount: '{{ item.serviceAccount }}'
        serviceEndpoint: '{{ item.serviceEndpoint }}'
        state: '{{ item.state }}'
        stateMessage: '{{ item.stateMessage }}'
        tags: '{{ item.tags }}'
        tenantProjectId: '{{ item.tenantProjectId }}'
        type: '{{ item.type }}'
        updateTime: '{{ item.updateTime }}'
        version: '{{ item.version }}'
        workforceIdentityServiceEndpoint: '{{ item.workforceIdentityServiceEndpoint }}'
        zone: '{{ item.zone }}'
  # Lists information about the supported locations for this service.
  # ── gcp.datafusion.projects.locations.list ──
  - discovery_id: gcp.datafusion.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.datafusion.projects.locations.operations.list ──
  - discovery_id: gcp.datafusion.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists possible versions for Data Fusion instances in the specified project and location.
  # ── gcp.datafusion.projects.locations.versions.list ──
  - discovery_id: gcp.datafusion.projects.locations.versions.list
    # python: svc.projects().locations().versions().list(**params).execute()
    calls:
      - action: projects.locations.versions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.versions }}'
      item:
        availableFeatures: '{{ item.availableFeatures }}'
        defaultVersion: '{{ item.defaultVersion }}'
        type: '{{ item.type }}'
        versionNumber: '{{ item.versionNumber }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets details of a single Data Fusion instance.
  # ── gcp.datafusion.projects.locations.instances.get [dependent] ──
  - discovery_id: gcp.datafusion.projects.locations.instances.get
    for_each: gcp.datafusion.projects.locations.instances.list
    # python: svc.projects().locations().instances().get(**params).execute()
    calls:
      - action: projects.locations.instances.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        accelerators: '{{ response.accelerators }}'
        apiEndpoint: '{{ response.apiEndpoint }}'
        availableVersion: '{{ response.availableVersion }}'
        createTime: '{{ response.createTime }}'
        cryptoKeyConfig: '{{ response.cryptoKeyConfig }}'
        dataplexDataLineageIntegrationEnabled: '{{ response.dataplexDataLineageIntegrationEnabled }}'
        dataprocServiceAccount: '{{ response.dataprocServiceAccount }}'
        description: '{{ response.description }}'
        disabledReason: '{{ response.disabledReason }}'
        displayName: '{{ response.displayName }}'
        enableRbac: '{{ response.enableRbac }}'
        enableStackdriverLogging: '{{ response.enableStackdriverLogging }}'
        enableStackdriverMonitoring: '{{ response.enableStackdriverMonitoring }}'
        enableZoneSeparation: '{{ response.enableZoneSeparation }}'
        eventPublishConfig: '{{ response.eventPublishConfig }}'
        gcsBucket: '{{ response.gcsBucket }}'
        labels: '{{ response.labels }}'
        loggingConfig: '{{ response.loggingConfig }}'
        maintenanceEvents: '{{ response.maintenanceEvents }}'
        maintenancePolicy: '{{ response.maintenancePolicy }}'
        monitoringConfig: '{{ response.monitoringConfig }}'
        name: '{{ response.name }}'
        networkConfig: '{{ response.networkConfig }}'
        options: '{{ response.options }}'
        p4ServiceAccount: '{{ response.p4ServiceAccount }}'
        patchRevision: '{{ response.patchRevision }}'
        privateInstance: '{{ response.privateInstance }}'
        satisfiesPzi: '{{ response.satisfiesPzi }}'
        satisfiesPzs: '{{ response.satisfiesPzs }}'
        serviceAccount: '{{ response.serviceAccount }}'
        serviceEndpoint: '{{ response.serviceEndpoint }}'
        state: '{{ response.state }}'
        stateMessage: '{{ response.stateMessage }}'
        tags: '{{ response.tags }}'
        tenantProjectId: '{{ response.tenantProjectId }}'
        type: '{{ response.type }}'
        updateTime: '{{ response.updateTime }}'
        version: '{{ response.version }}'
        workforceIdentityServiceEndpoint: '{{ response.workforceIdentityServiceEndpoint }}'
        zone: '{{ response.zone }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.datafusion.projects.locations.instances.getIamPolicy [dependent] ──
  - discovery_id: gcp.datafusion.projects.locations.instances.getIamPolicy
    for_each: gcp.datafusion.projects.locations.instances.list
    # python: svc.projects().locations().instances().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.instances.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.datafusion.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.datafusion.projects.locations.operations.get
    for_each: gcp.datafusion.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
