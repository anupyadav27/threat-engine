# ============================================================
# Discovery YAML — oracledatabase (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 37 | independent: 26 | dependent: 11
# ops with fields: 37/37 | dep ops with chain: 11/11
# ============================================================
version: '1.0'
provider: gcp
service: oracledatabase

services:
  client: oracledatabase
  module: "googleapiclient.discovery.build('oracledatabase', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists the long-term and automatic backups of an Autonomous Database.
  # ── gcp.oracledatabase.projects.locations.autonomousDatabaseBackups.list ──
  - discovery_id: gcp.oracledatabase.projects.locations.autonomousDatabaseBackups.list
    # python: svc.projects().locations().autonomousDatabaseBackups().list(**params).execute()
    calls:
      - action: projects.locations.autonomousDatabaseBackups.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.autonomousDatabaseBackups }}'
      item:
        autonomousDatabase: '{{ item.autonomousDatabase }}'
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        properties: '{{ item.properties }}'
  # Lists Autonomous Database Character Sets in a given project and location.
  # ── gcp.oracledatabase.projects.locations.autonomousDatabaseCharacterSets.list ──
  - discovery_id: gcp.oracledatabase.projects.locations.autonomousDatabaseCharacterSets.list
    # python: svc.projects().locations().autonomousDatabaseCharacterSets().list(**params).execute()
    calls:
      - action: projects.locations.autonomousDatabaseCharacterSets.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.autonomousDatabaseCharacterSets }}'
      item:
        characterSet: '{{ item.characterSet }}'
        characterSetType: '{{ item.characterSetType }}'
        name: '{{ item.name }}'
  # Lists the Autonomous Databases in a given project and location.
  # ── gcp.oracledatabase.projects.locations.autonomousDatabases.list ──
  - discovery_id: gcp.oracledatabase.projects.locations.autonomousDatabases.list
    # python: svc.projects().locations().autonomousDatabases().list(**params).execute()
    calls:
      - action: projects.locations.autonomousDatabases.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.autonomousDatabases }}'
      item:
        adminPassword: '{{ item.adminPassword }}'
        cidr: '{{ item.cidr }}'
        createTime: '{{ item.createTime }}'
        database: '{{ item.database }}'
        disasterRecoverySupportedLocations: '{{ item.disasterRecoverySupportedLocations }}'
        displayName: '{{ item.displayName }}'
        entitlementId: '{{ item.entitlementId }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        network: '{{ item.network }}'
        odbNetwork: '{{ item.odbNetwork }}'
        odbSubnet: '{{ item.odbSubnet }}'
        peerAutonomousDatabases: '{{ item.peerAutonomousDatabases }}'
        properties: '{{ item.properties }}'
        sourceConfig: '{{ item.sourceConfig }}'
  # Lists all the available Autonomous Database versions for a project and location.
  # ── gcp.oracledatabase.projects.locations.autonomousDbVersions.list ──
  - discovery_id: gcp.oracledatabase.projects.locations.autonomousDbVersions.list
    # python: svc.projects().locations().autonomousDbVersions().list(**params).execute()
    calls:
      - action: projects.locations.autonomousDbVersions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.autonomousDbVersions }}'
      item:
        dbWorkload: '{{ item.dbWorkload }}'
        name: '{{ item.name }}'
        version: '{{ item.version }}'
        workloadUri: '{{ item.workloadUri }}'
  # Lists the database servers of an Exadata Infrastructure instance.
  # ── gcp.oracledatabase.projects.locations.cloudExadataInfrastructures.dbServers.list ──
  - discovery_id: gcp.oracledatabase.projects.locations.cloudExadataInfrastructures.dbServers.list
    # python: svc.projects().locations().cloudExadataInfrastructures().dbServers().list(**params).execute()
    calls:
      - action: projects.locations.cloudExadataInfrastructures.dbServers.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.dbServers }}'
      item:
        displayName: '{{ item.displayName }}'
        name: '{{ item.name }}'
        properties: '{{ item.properties }}'
  # Lists Exadata Infrastructures in a given project and location.
  # ── gcp.oracledatabase.projects.locations.cloudExadataInfrastructures.list ──
  - discovery_id: gcp.oracledatabase.projects.locations.cloudExadataInfrastructures.list
    # python: svc.projects().locations().cloudExadataInfrastructures().list(**params).execute()
    calls:
      - action: projects.locations.cloudExadataInfrastructures.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.cloudExadataInfrastructures }}'
      item:
        createTime: '{{ item.createTime }}'
        displayName: '{{ item.displayName }}'
        entitlementId: '{{ item.entitlementId }}'
        gcpOracleZone: '{{ item.gcpOracleZone }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        properties: '{{ item.properties }}'
  # Lists the database nodes of a VM Cluster.
  # ── gcp.oracledatabase.projects.locations.cloudVmClusters.dbNodes.list ──
  - discovery_id: gcp.oracledatabase.projects.locations.cloudVmClusters.dbNodes.list
    # python: svc.projects().locations().cloudVmClusters().dbNodes().list(**params).execute()
    calls:
      - action: projects.locations.cloudVmClusters.dbNodes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.dbNodes }}'
      item:
        name: '{{ item.name }}'
        properties: '{{ item.properties }}'
  # Lists the VM Clusters in a given project and location.
  # ── gcp.oracledatabase.projects.locations.cloudVmClusters.list ──
  - discovery_id: gcp.oracledatabase.projects.locations.cloudVmClusters.list
    # python: svc.projects().locations().cloudVmClusters().list(**params).execute()
    calls:
      - action: projects.locations.cloudVmClusters.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.cloudVmClusters }}'
      item:
        backupOdbSubnet: '{{ item.backupOdbSubnet }}'
        backupSubnetCidr: '{{ item.backupSubnetCidr }}'
        cidr: '{{ item.cidr }}'
        createTime: '{{ item.createTime }}'
        displayName: '{{ item.displayName }}'
        exadataInfrastructure: '{{ item.exadataInfrastructure }}'
        gcpOracleZone: '{{ item.gcpOracleZone }}'
        identityConnector: '{{ item.identityConnector }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        network: '{{ item.network }}'
        odbNetwork: '{{ item.odbNetwork }}'
        odbSubnet: '{{ item.odbSubnet }}'
        properties: '{{ item.properties }}'
  # List DatabaseCharacterSets for the given project and location.
  # ── gcp.oracledatabase.projects.locations.databaseCharacterSets.list ──
  - discovery_id: gcp.oracledatabase.projects.locations.databaseCharacterSets.list
    # python: svc.projects().locations().databaseCharacterSets().list(**params).execute()
    calls:
      - action: projects.locations.databaseCharacterSets.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.databaseCharacterSets }}'
      item:
        characterSet: '{{ item.characterSet }}'
        characterSetType: '{{ item.characterSetType }}'
        name: '{{ item.name }}'
  # Lists all the Databases for the given project, location and DbSystem.
  # ── gcp.oracledatabase.projects.locations.databases.list ──
  - discovery_id: gcp.oracledatabase.projects.locations.databases.list
    # python: svc.projects().locations().databases().list(**params).execute()
    calls:
      - action: projects.locations.databases.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.databases }}'
      item:
        adminPassword: '{{ item.adminPassword }}'
        characterSet: '{{ item.characterSet }}'
        createTime: '{{ item.createTime }}'
        databaseId: '{{ item.databaseId }}'
        dbHomeName: '{{ item.dbHomeName }}'
        dbName: '{{ item.dbName }}'
        dbUniqueName: '{{ item.dbUniqueName }}'
        gcpOracleZone: '{{ item.gcpOracleZone }}'
        name: '{{ item.name }}'
        ncharacterSet: '{{ item.ncharacterSet }}'
        ociUrl: '{{ item.ociUrl }}'
        opsInsightsStatus: '{{ item.opsInsightsStatus }}'
        properties: '{{ item.properties }}'
        tdeWalletPassword: '{{ item.tdeWalletPassword }}'
  # Lists all the DbSystemInitialStorageSizes for the given project and location.
  # ── gcp.oracledatabase.projects.locations.dbSystemInitialStorageSizes.list ──
  - discovery_id: gcp.oracledatabase.projects.locations.dbSystemInitialStorageSizes.list
    # python: svc.projects().locations().dbSystemInitialStorageSizes().list(**params).execute()
    calls:
      - action: projects.locations.dbSystemInitialStorageSizes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.dbSystemInitialStorageSizes }}'
      item:
        name: '{{ item.name }}'
        properties: '{{ item.properties }}'
  # Lists the database system shapes available for the project and location.
  # ── gcp.oracledatabase.projects.locations.dbSystemShapes.list ──
  - discovery_id: gcp.oracledatabase.projects.locations.dbSystemShapes.list
    # python: svc.projects().locations().dbSystemShapes().list(**params).execute()
    calls:
      - action: projects.locations.dbSystemShapes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.dbSystemShapes }}'
      item:
        availableCoreCountPerNode: '{{ item.availableCoreCountPerNode }}'
        availableDataStorageTb: '{{ item.availableDataStorageTb }}'
        availableMemoryPerNodeGb: '{{ item.availableMemoryPerNodeGb }}'
        maxNodeCount: '{{ item.maxNodeCount }}'
        maxStorageCount: '{{ item.maxStorageCount }}'
        minCoreCountPerNode: '{{ item.minCoreCountPerNode }}'
        minDbNodeStoragePerNodeGb: '{{ item.minDbNodeStoragePerNodeGb }}'
        minMemoryPerNodeGb: '{{ item.minMemoryPerNodeGb }}'
        minNodeCount: '{{ item.minNodeCount }}'
        minStorageCount: '{{ item.minStorageCount }}'
        name: '{{ item.name }}'
        shape: '{{ item.shape }}'
  # Lists all the DbSystems for the given project and location.
  # ── gcp.oracledatabase.projects.locations.dbSystems.list ──
  - discovery_id: gcp.oracledatabase.projects.locations.dbSystems.list
    # python: svc.projects().locations().dbSystems().list(**params).execute()
    calls:
      - action: projects.locations.dbSystems.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.dbSystems }}'
      item:
        createTime: '{{ item.createTime }}'
        displayName: '{{ item.displayName }}'
        entitlementId: '{{ item.entitlementId }}'
        gcpOracleZone: '{{ item.gcpOracleZone }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        ociUrl: '{{ item.ociUrl }}'
        odbNetwork: '{{ item.odbNetwork }}'
        odbSubnet: '{{ item.odbSubnet }}'
        properties: '{{ item.properties }}'
  # List DbVersions for the given project and location.
  # ── gcp.oracledatabase.projects.locations.dbVersions.list ──
  - discovery_id: gcp.oracledatabase.projects.locations.dbVersions.list
    # python: svc.projects().locations().dbVersions().list(**params).execute()
    calls:
      - action: projects.locations.dbVersions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.dbVersions }}'
      item:
        name: '{{ item.name }}'
        properties: '{{ item.properties }}'
  # Lists the entitlements in a given project.
  # ── gcp.oracledatabase.projects.locations.entitlements.list ──
  - discovery_id: gcp.oracledatabase.projects.locations.entitlements.list
    # python: svc.projects().locations().entitlements().list(**params).execute()
    calls:
      - action: projects.locations.entitlements.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.entitlements }}'
      item:
        cloudAccountDetails: '{{ item.cloudAccountDetails }}'
        entitlementId: '{{ item.entitlementId }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
  # Lists the database nodes of a VM Cluster.
  # ── gcp.oracledatabase.projects.locations.exadbVmClusters.dbNodes.list ──
  - discovery_id: gcp.oracledatabase.projects.locations.exadbVmClusters.dbNodes.list
    # python: svc.projects().locations().exadbVmClusters().dbNodes().list(**params).execute()
    calls:
      - action: projects.locations.exadbVmClusters.dbNodes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.dbNodes }}'
      item:
        name: '{{ item.name }}'
        properties: '{{ item.properties }}'
  # Lists all the Exadb (Exascale) VM Clusters for the given project and location.
  # ── gcp.oracledatabase.projects.locations.exadbVmClusters.list ──
  - discovery_id: gcp.oracledatabase.projects.locations.exadbVmClusters.list
    # python: svc.projects().locations().exadbVmClusters().list(**params).execute()
    calls:
      - action: projects.locations.exadbVmClusters.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.exadbVmClusters }}'
      item:
        backupOdbSubnet: '{{ item.backupOdbSubnet }}'
        createTime: '{{ item.createTime }}'
        displayName: '{{ item.displayName }}'
        entitlementId: '{{ item.entitlementId }}'
        gcpOracleZone: '{{ item.gcpOracleZone }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        odbNetwork: '{{ item.odbNetwork }}'
        odbSubnet: '{{ item.odbSubnet }}'
        properties: '{{ item.properties }}'
  # Lists all the ExascaleDB Storage Vaults for the given project and location.
  # ── gcp.oracledatabase.projects.locations.exascaleDbStorageVaults.list ──
  - discovery_id: gcp.oracledatabase.projects.locations.exascaleDbStorageVaults.list
    # python: svc.projects().locations().exascaleDbStorageVaults().list(**params).execute()
    calls:
      - action: projects.locations.exascaleDbStorageVaults.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.exascaleDbStorageVaults }}'
      item:
        createTime: '{{ item.createTime }}'
        displayName: '{{ item.displayName }}'
        entitlementId: '{{ item.entitlementId }}'
        gcpOracleZone: '{{ item.gcpOracleZone }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        properties: '{{ item.properties }}'
  # Gets information about a location.
  # ── gcp.oracledatabase.projects.locations.get ──
  - discovery_id: gcp.oracledatabase.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists all the valid Oracle Grid Infrastructure (GI) versions for the given project and location.
  # ── gcp.oracledatabase.projects.locations.giVersions.list ──
  - discovery_id: gcp.oracledatabase.projects.locations.giVersions.list
    # python: svc.projects().locations().giVersions().list(**params).execute()
    calls:
      - action: projects.locations.giVersions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.giVersions }}'
      item:
        name: '{{ item.name }}'
        version: '{{ item.version }}'
  # Lists all the valid minor versions for the given project, location, gi version and shape family.
  # ── gcp.oracledatabase.projects.locations.giVersions.minorVersions.list ──
  - discovery_id: gcp.oracledatabase.projects.locations.giVersions.minorVersions.list
    # python: svc.projects().locations().giVersions().minorVersions().list(**params).execute()
    calls:
      - action: projects.locations.giVersions.minorVersions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.minorVersions }}'
      item:
        gridImageId: '{{ item.gridImageId }}'
        name: '{{ item.name }}'
        version: '{{ item.version }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.oracledatabase.projects.locations.list ──
  - discovery_id: gcp.oracledatabase.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists the ODB Networks in a given project and location.
  # ── gcp.oracledatabase.projects.locations.odbNetworks.list ──
  - discovery_id: gcp.oracledatabase.projects.locations.odbNetworks.list
    # python: svc.projects().locations().odbNetworks().list(**params).execute()
    calls:
      - action: projects.locations.odbNetworks.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.odbNetworks }}'
      item:
        createTime: '{{ item.createTime }}'
        entitlementId: '{{ item.entitlementId }}'
        gcpOracleZone: '{{ item.gcpOracleZone }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        network: '{{ item.network }}'
        state: '{{ item.state }}'
  # Lists all the ODB Subnets in a given ODB Network.
  # ── gcp.oracledatabase.projects.locations.odbNetworks.odbSubnets.list ──
  - discovery_id: gcp.oracledatabase.projects.locations.odbNetworks.odbSubnets.list
    # python: svc.projects().locations().odbNetworks().odbSubnets().list(**params).execute()
    calls:
      - action: projects.locations.odbNetworks.odbSubnets.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.odbSubnets }}'
      item:
        cidrRange: '{{ item.cidrRange }}'
        createTime: '{{ item.createTime }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        purpose: '{{ item.purpose }}'
        state: '{{ item.state }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.oracledatabase.projects.locations.operations.list ──
  - discovery_id: gcp.oracledatabase.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists all the PluggableDatabases for the given project, location and Container Database.
  # ── gcp.oracledatabase.projects.locations.pluggableDatabases.list ──
  - discovery_id: gcp.oracledatabase.projects.locations.pluggableDatabases.list
    # python: svc.projects().locations().pluggableDatabases().list(**params).execute()
    calls:
      - action: projects.locations.pluggableDatabases.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.pluggableDatabases }}'
      item:
        createTime: '{{ item.createTime }}'
        name: '{{ item.name }}'
        ociUrl: '{{ item.ociUrl }}'
        properties: '{{ item.properties }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets the details of a single Autonomous Database.
  # ── gcp.oracledatabase.projects.locations.autonomousDatabases.get [dependent] ──
  - discovery_id: gcp.oracledatabase.projects.locations.autonomousDatabases.get
    for_each: gcp.oracledatabase.projects.locations.autonomousDatabases.list
    # python: svc.projects().locations().autonomousDatabases().get(**params).execute()
    calls:
      - action: projects.locations.autonomousDatabases.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        adminPassword: '{{ response.adminPassword }}'
        cidr: '{{ response.cidr }}'
        createTime: '{{ response.createTime }}'
        database: '{{ response.database }}'
        disasterRecoverySupportedLocations: '{{ response.disasterRecoverySupportedLocations }}'
        displayName: '{{ response.displayName }}'
        entitlementId: '{{ response.entitlementId }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        network: '{{ response.network }}'
        odbNetwork: '{{ response.odbNetwork }}'
        odbSubnet: '{{ response.odbSubnet }}'
        peerAutonomousDatabases: '{{ response.peerAutonomousDatabases }}'
        properties: '{{ response.properties }}'
        sourceConfig: '{{ response.sourceConfig }}'
    # required_params: ['name']
  # Gets details of a single Exadata Infrastructure.
  # ── gcp.oracledatabase.projects.locations.cloudExadataInfrastructures.get [dependent] ──
  - discovery_id: gcp.oracledatabase.projects.locations.cloudExadataInfrastructures.get
    for_each: gcp.oracledatabase.projects.locations.cloudExadataInfrastructures.list
    # python: svc.projects().locations().cloudExadataInfrastructures().get(**params).execute()
    calls:
      - action: projects.locations.cloudExadataInfrastructures.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        displayName: '{{ response.displayName }}'
        entitlementId: '{{ response.entitlementId }}'
        gcpOracleZone: '{{ response.gcpOracleZone }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        properties: '{{ response.properties }}'
    # required_params: ['name']
  # Gets details of a single VM Cluster.
  # ── gcp.oracledatabase.projects.locations.cloudVmClusters.get [dependent] ──
  - discovery_id: gcp.oracledatabase.projects.locations.cloudVmClusters.get
    for_each: gcp.oracledatabase.projects.locations.cloudVmClusters.list
    # python: svc.projects().locations().cloudVmClusters().get(**params).execute()
    calls:
      - action: projects.locations.cloudVmClusters.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        backupOdbSubnet: '{{ response.backupOdbSubnet }}'
        backupSubnetCidr: '{{ response.backupSubnetCidr }}'
        cidr: '{{ response.cidr }}'
        createTime: '{{ response.createTime }}'
        displayName: '{{ response.displayName }}'
        exadataInfrastructure: '{{ response.exadataInfrastructure }}'
        gcpOracleZone: '{{ response.gcpOracleZone }}'
        identityConnector: '{{ response.identityConnector }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        network: '{{ response.network }}'
        odbNetwork: '{{ response.odbNetwork }}'
        odbSubnet: '{{ response.odbSubnet }}'
        properties: '{{ response.properties }}'
    # required_params: ['name']
  # Gets details of a single Database.
  # ── gcp.oracledatabase.projects.locations.databases.get [dependent] ──
  - discovery_id: gcp.oracledatabase.projects.locations.databases.get
    for_each: gcp.oracledatabase.projects.locations.databases.list
    # python: svc.projects().locations().databases().get(**params).execute()
    calls:
      - action: projects.locations.databases.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        adminPassword: '{{ response.adminPassword }}'
        characterSet: '{{ response.characterSet }}'
        createTime: '{{ response.createTime }}'
        databaseId: '{{ response.databaseId }}'
        dbHomeName: '{{ response.dbHomeName }}'
        dbName: '{{ response.dbName }}'
        dbUniqueName: '{{ response.dbUniqueName }}'
        gcpOracleZone: '{{ response.gcpOracleZone }}'
        name: '{{ response.name }}'
        ncharacterSet: '{{ response.ncharacterSet }}'
        ociUrl: '{{ response.ociUrl }}'
        opsInsightsStatus: '{{ response.opsInsightsStatus }}'
        properties: '{{ response.properties }}'
        tdeWalletPassword: '{{ response.tdeWalletPassword }}'
    # required_params: ['name']
  # Gets details of a single DbSystem.
  # ── gcp.oracledatabase.projects.locations.dbSystems.get [dependent] ──
  - discovery_id: gcp.oracledatabase.projects.locations.dbSystems.get
    for_each: gcp.oracledatabase.projects.locations.dbSystems.list
    # python: svc.projects().locations().dbSystems().get(**params).execute()
    calls:
      - action: projects.locations.dbSystems.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        displayName: '{{ response.displayName }}'
        entitlementId: '{{ response.entitlementId }}'
        gcpOracleZone: '{{ response.gcpOracleZone }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        ociUrl: '{{ response.ociUrl }}'
        odbNetwork: '{{ response.odbNetwork }}'
        odbSubnet: '{{ response.odbSubnet }}'
        properties: '{{ response.properties }}'
    # required_params: ['name']
  # Gets details of a single Exadb (Exascale) VM Cluster.
  # ── gcp.oracledatabase.projects.locations.exadbVmClusters.get [dependent] ──
  - discovery_id: gcp.oracledatabase.projects.locations.exadbVmClusters.get
    for_each: gcp.oracledatabase.projects.locations.exadbVmClusters.list
    # python: svc.projects().locations().exadbVmClusters().get(**params).execute()
    calls:
      - action: projects.locations.exadbVmClusters.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        backupOdbSubnet: '{{ response.backupOdbSubnet }}'
        createTime: '{{ response.createTime }}'
        displayName: '{{ response.displayName }}'
        entitlementId: '{{ response.entitlementId }}'
        gcpOracleZone: '{{ response.gcpOracleZone }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        odbNetwork: '{{ response.odbNetwork }}'
        odbSubnet: '{{ response.odbSubnet }}'
        properties: '{{ response.properties }}'
    # required_params: ['name']
  # Gets details of a single ExascaleDB Storage Vault.
  # ── gcp.oracledatabase.projects.locations.exascaleDbStorageVaults.get [dependent] ──
  - discovery_id: gcp.oracledatabase.projects.locations.exascaleDbStorageVaults.get
    for_each: gcp.oracledatabase.projects.locations.exascaleDbStorageVaults.list
    # python: svc.projects().locations().exascaleDbStorageVaults().get(**params).execute()
    calls:
      - action: projects.locations.exascaleDbStorageVaults.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        displayName: '{{ response.displayName }}'
        entitlementId: '{{ response.entitlementId }}'
        gcpOracleZone: '{{ response.gcpOracleZone }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        properties: '{{ response.properties }}'
    # required_params: ['name']
  # Gets details of a single ODB Network.
  # ── gcp.oracledatabase.projects.locations.odbNetworks.get [dependent] ──
  - discovery_id: gcp.oracledatabase.projects.locations.odbNetworks.get
    for_each: gcp.oracledatabase.projects.locations.odbNetworks.list
    # python: svc.projects().locations().odbNetworks().get(**params).execute()
    calls:
      - action: projects.locations.odbNetworks.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        entitlementId: '{{ response.entitlementId }}'
        gcpOracleZone: '{{ response.gcpOracleZone }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        network: '{{ response.network }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Gets details of a single ODB Subnet.
  # ── gcp.oracledatabase.projects.locations.odbNetworks.odbSubnets.get [dependent] ──
  - discovery_id: gcp.oracledatabase.projects.locations.odbNetworks.odbSubnets.get
    for_each: gcp.oracledatabase.projects.locations.odbNetworks.odbSubnets.list
    # python: svc.projects().locations().odbNetworks().odbSubnets().get(**params).execute()
    calls:
      - action: projects.locations.odbNetworks.odbSubnets.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        cidrRange: '{{ response.cidrRange }}'
        createTime: '{{ response.createTime }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        purpose: '{{ response.purpose }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.oracledatabase.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.oracledatabase.projects.locations.operations.get
    for_each: gcp.oracledatabase.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets details of a single PluggableDatabase.
  # ── gcp.oracledatabase.projects.locations.pluggableDatabases.get [dependent] ──
  - discovery_id: gcp.oracledatabase.projects.locations.pluggableDatabases.get
    for_each: gcp.oracledatabase.projects.locations.pluggableDatabases.list
    # python: svc.projects().locations().pluggableDatabases().get(**params).execute()
    calls:
      - action: projects.locations.pluggableDatabases.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        name: '{{ response.name }}'
        ociUrl: '{{ response.ociUrl }}'
        properties: '{{ response.properties }}'
    # required_params: ['name']
