# ============================================================
# Discovery YAML — metastore (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 19 | independent: 8 | dependent: 11
# ops with fields: 19/19 | dep ops with chain: 11/11
# ============================================================
version: '1.0'
provider: gcp
service: metastore

services:
  client: metastore
  module: "googleapiclient.discovery.build('metastore', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists federations in a project and location.
  # ── gcp.metastore.projects.locations.federations.list ──
  - discovery_id: gcp.metastore.projects.locations.federations.list
    # python: svc.projects().locations().federations().list(**params).execute()
    calls:
      - action: projects.locations.federations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.federations }}'
      item:
        backendMetastores: '{{ item.backendMetastores }}'
        createTime: '{{ item.createTime }}'
        endpointUri: '{{ item.endpointUri }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
        stateMessage: '{{ item.stateMessage }}'
        tags: '{{ item.tags }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
        version: '{{ item.version }}'
  # Gets information about a location.
  # ── gcp.metastore.projects.locations.get ──
  - discovery_id: gcp.metastore.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.metastore.projects.locations.list ──
  - discovery_id: gcp.metastore.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.metastore.projects.locations.operations.list ──
  - discovery_id: gcp.metastore.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists backups in a service.
  # ── gcp.metastore.projects.locations.services.backups.list ──
  - discovery_id: gcp.metastore.projects.locations.services.backups.list
    # python: svc.projects().locations().services().backups().list(**params).execute()
    calls:
      - action: projects.locations.services.backups.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.backups }}'
      item:
        createTime: '{{ item.createTime }}'
        description: '{{ item.description }}'
        endTime: '{{ item.endTime }}'
        name: '{{ item.name }}'
        restoringServices: '{{ item.restoringServices }}'
        serviceRevision: '{{ item.serviceRevision }}'
        state: '{{ item.state }}'
  # Lists services in a project and location.
  # ── gcp.metastore.projects.locations.services.list ──
  - discovery_id: gcp.metastore.projects.locations.services.list
    # python: svc.projects().locations().services().list(**params).execute()
    calls:
      - action: projects.locations.services.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.services }}'
      item:
        artifactGcsUri: '{{ item.artifactGcsUri }}'
        createTime: '{{ item.createTime }}'
        databaseType: '{{ item.databaseType }}'
        deletionProtection: '{{ item.deletionProtection }}'
        encryptionConfig: '{{ item.encryptionConfig }}'
        endpointUri: '{{ item.endpointUri }}'
        hiveMetastoreConfig: '{{ item.hiveMetastoreConfig }}'
        labels: '{{ item.labels }}'
        maintenanceWindow: '{{ item.maintenanceWindow }}'
        metadataIntegration: '{{ item.metadataIntegration }}'
        metadataManagementActivity: '{{ item.metadataManagementActivity }}'
        name: '{{ item.name }}'
        network: '{{ item.network }}'
        networkConfig: '{{ item.networkConfig }}'
        port: '{{ item.port }}'
        releaseChannel: '{{ item.releaseChannel }}'
        scalingConfig: '{{ item.scalingConfig }}'
        scheduledBackup: '{{ item.scheduledBackup }}'
        state: '{{ item.state }}'
        stateMessage: '{{ item.stateMessage }}'
        tags: '{{ item.tags }}'
        telemetryConfig: '{{ item.telemetryConfig }}'
        tier: '{{ item.tier }}'
        uid: '{{ item.uid }}'
        updateTime: '{{ item.updateTime }}'
  # Lists imports in a service.
  # ── gcp.metastore.projects.locations.services.metadataImports.list ──
  - discovery_id: gcp.metastore.projects.locations.services.metadataImports.list
    # python: svc.projects().locations().services().metadataImports().list(**params).execute()
    calls:
      - action: projects.locations.services.metadataImports.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.metadataImports }}'
      item:
        createTime: '{{ item.createTime }}'
        databaseDump: '{{ item.databaseDump }}'
        description: '{{ item.description }}'
        endTime: '{{ item.endTime }}'
        name: '{{ item.name }}'
        state: '{{ item.state }}'
        updateTime: '{{ item.updateTime }}'
  # Lists migration executions on a service.
  # ── gcp.metastore.projects.locations.services.migrationExecutions.list ──
  - discovery_id: gcp.metastore.projects.locations.services.migrationExecutions.list
    # python: svc.projects().locations().services().migrationExecutions().list(**params).execute()
    calls:
      - action: projects.locations.services.migrationExecutions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.migrationExecutions }}'
      item:
        cloudSqlMigrationConfig: '{{ item.cloudSqlMigrationConfig }}'
        createTime: '{{ item.createTime }}'
        endTime: '{{ item.endTime }}'
        name: '{{ item.name }}'
        phase: '{{ item.phase }}'
        state: '{{ item.state }}'
        stateMessage: '{{ item.stateMessage }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets the details of a single federation.
  # ── gcp.metastore.projects.locations.federations.get [dependent] ──
  - discovery_id: gcp.metastore.projects.locations.federations.get
    for_each: gcp.metastore.projects.locations.federations.list
    # python: svc.projects().locations().federations().get(**params).execute()
    calls:
      - action: projects.locations.federations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        backendMetastores: '{{ response.backendMetastores }}'
        createTime: '{{ response.createTime }}'
        endpointUri: '{{ response.endpointUri }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
        stateMessage: '{{ response.stateMessage }}'
        tags: '{{ response.tags }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
        version: '{{ response.version }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.metastore.projects.locations.federations.getIamPolicy [dependent] ──
  - discovery_id: gcp.metastore.projects.locations.federations.getIamPolicy
    for_each: gcp.metastore.projects.locations.federations.list
    # python: svc.projects().locations().federations().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.federations.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.metastore.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.metastore.projects.locations.operations.get
    for_each: gcp.metastore.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets details of a single backup.
  # ── gcp.metastore.projects.locations.services.backups.get [dependent] ──
  - discovery_id: gcp.metastore.projects.locations.services.backups.get
    for_each: gcp.metastore.projects.locations.services.backups.list
    # python: svc.projects().locations().services().backups().get(**params).execute()
    calls:
      - action: projects.locations.services.backups.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        description: '{{ response.description }}'
        endTime: '{{ response.endTime }}'
        name: '{{ response.name }}'
        restoringServices: '{{ response.restoringServices }}'
        serviceRevision: '{{ response.serviceRevision }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.metastore.projects.locations.services.backups.getIamPolicy [dependent] ──
  - discovery_id: gcp.metastore.projects.locations.services.backups.getIamPolicy
    for_each: gcp.metastore.projects.locations.services.backups.list
    # python: svc.projects().locations().services().backups().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.services.backups.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.metastore.projects.locations.services.databases.getIamPolicy [dependent] ──
  - discovery_id: gcp.metastore.projects.locations.services.databases.getIamPolicy
    for_each: gcp.metastore.projects.locations.services.list
    # python: svc.projects().locations().services().databases().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.services.databases.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.metastore.projects.locations.services.databases.tables.getIamPolicy [dependent] ──
  - discovery_id: gcp.metastore.projects.locations.services.databases.tables.getIamPolicy
    for_each: gcp.metastore.projects.locations.services.list
    # python: svc.projects().locations().services().databases().tables().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.services.databases.tables.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Gets the details of a single service.
  # ── gcp.metastore.projects.locations.services.get [dependent] ──
  - discovery_id: gcp.metastore.projects.locations.services.get
    for_each: gcp.metastore.projects.locations.services.list
    # python: svc.projects().locations().services().get(**params).execute()
    calls:
      - action: projects.locations.services.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        artifactGcsUri: '{{ response.artifactGcsUri }}'
        createTime: '{{ response.createTime }}'
        databaseType: '{{ response.databaseType }}'
        deletionProtection: '{{ response.deletionProtection }}'
        encryptionConfig: '{{ response.encryptionConfig }}'
        endpointUri: '{{ response.endpointUri }}'
        hiveMetastoreConfig: '{{ response.hiveMetastoreConfig }}'
        labels: '{{ response.labels }}'
        maintenanceWindow: '{{ response.maintenanceWindow }}'
        metadataIntegration: '{{ response.metadataIntegration }}'
        metadataManagementActivity: '{{ response.metadataManagementActivity }}'
        name: '{{ response.name }}'
        network: '{{ response.network }}'
        networkConfig: '{{ response.networkConfig }}'
        port: '{{ response.port }}'
        releaseChannel: '{{ response.releaseChannel }}'
        scalingConfig: '{{ response.scalingConfig }}'
        scheduledBackup: '{{ response.scheduledBackup }}'
        state: '{{ response.state }}'
        stateMessage: '{{ response.stateMessage }}'
        tags: '{{ response.tags }}'
        telemetryConfig: '{{ response.telemetryConfig }}'
        tier: '{{ response.tier }}'
        uid: '{{ response.uid }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the access control policy for a resource. Returns an empty policy if the resource exists and do
  # ── gcp.metastore.projects.locations.services.getIamPolicy [dependent] ──
  - discovery_id: gcp.metastore.projects.locations.services.getIamPolicy
    for_each: gcp.metastore.projects.locations.services.list
    # python: svc.projects().locations().services().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.services.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Gets details of a single import.
  # ── gcp.metastore.projects.locations.services.metadataImports.get [dependent] ──
  - discovery_id: gcp.metastore.projects.locations.services.metadataImports.get
    for_each: gcp.metastore.projects.locations.services.metadataImports.list
    # python: svc.projects().locations().services().metadataImports().get(**params).execute()
    calls:
      - action: projects.locations.services.metadataImports.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        databaseDump: '{{ response.databaseDump }}'
        description: '{{ response.description }}'
        endTime: '{{ response.endTime }}'
        name: '{{ response.name }}'
        state: '{{ response.state }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets details of a single migration execution.
  # ── gcp.metastore.projects.locations.services.migrationExecutions.get [dependent] ──
  - discovery_id: gcp.metastore.projects.locations.services.migrationExecutions.get
    for_each: gcp.metastore.projects.locations.services.migrationExecutions.list
    # python: svc.projects().locations().services().migrationExecutions().get(**params).execute()
    calls:
      - action: projects.locations.services.migrationExecutions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        cloudSqlMigrationConfig: '{{ response.cloudSqlMigrationConfig }}'
        createTime: '{{ response.createTime }}'
        endTime: '{{ response.endTime }}'
        name: '{{ response.name }}'
        phase: '{{ response.phase }}'
        state: '{{ response.state }}'
        stateMessage: '{{ response.stateMessage }}'
    # required_params: ['name']
