# ============================================================
# Discovery YAML — secretmanager (v1)
# Generated: 2026-02-21T03:06:41Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 16 | independent: 7 | dependent: 9
# ops with fields: 16/16 | dep ops with chain: 9/9
# ============================================================
version: '1.0'
provider: gcp
service: secretmanager

services:
  client: secretmanager
  module: "googleapiclient.discovery.build('secretmanager', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Gets information about a location.
  # ── gcp.secretmanager.projects.locations.get ──
  - discovery_id: gcp.secretmanager.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.secretmanager.projects.locations.list ──
  - discovery_id: gcp.secretmanager.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.secretmanager.projects.locations.operations.list ──
  - discovery_id: gcp.secretmanager.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists Secrets.
  # ── gcp.secretmanager.projects.locations.secrets.list ──
  - discovery_id: gcp.secretmanager.projects.locations.secrets.list
    # python: svc.projects().locations().secrets().list(**params).execute()
    calls:
      - action: projects.locations.secrets.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.secrets }}'
      item:
        annotations: '{{ item.annotations }}'
        createTime: '{{ item.createTime }}'
        customerManagedEncryption: '{{ item.customerManagedEncryption }}'
        expireTime: '{{ item.expireTime }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        replication: '{{ item.replication }}'
        rotation: '{{ item.rotation }}'
        tags: '{{ item.tags }}'
        topics: '{{ item.topics }}'
        ttl: '{{ item.ttl }}'
        versionAliases: '{{ item.versionAliases }}'
        versionDestroyTtl: '{{ item.versionDestroyTtl }}'
  # Lists SecretVersions. This call does not return secret data.
  # ── gcp.secretmanager.projects.locations.secrets.versions.list ──
  - discovery_id: gcp.secretmanager.projects.locations.secrets.versions.list
    # python: svc.projects().locations().secrets().versions().list(**params).execute()
    calls:
      - action: projects.locations.secrets.versions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.versions }}'
      item:
        clientSpecifiedPayloadChecksum: '{{ item.clientSpecifiedPayloadChecksum }}'
        createTime: '{{ item.createTime }}'
        customerManagedEncryption: '{{ item.customerManagedEncryption }}'
        destroyTime: '{{ item.destroyTime }}'
        name: '{{ item.name }}'
        replicationStatus: '{{ item.replicationStatus }}'
        scheduledDestroyTime: '{{ item.scheduledDestroyTime }}'
        state: '{{ item.state }}'
  # Lists Secrets.
  # ── gcp.secretmanager.projects.secrets.list ──
  - discovery_id: gcp.secretmanager.projects.secrets.list
    # python: svc.projects().secrets().list(**params).execute()
    calls:
      - action: projects.secrets.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.secrets }}'
      item:
        annotations: '{{ item.annotations }}'
        createTime: '{{ item.createTime }}'
        customerManagedEncryption: '{{ item.customerManagedEncryption }}'
        expireTime: '{{ item.expireTime }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        replication: '{{ item.replication }}'
        rotation: '{{ item.rotation }}'
        tags: '{{ item.tags }}'
        topics: '{{ item.topics }}'
        ttl: '{{ item.ttl }}'
        versionAliases: '{{ item.versionAliases }}'
        versionDestroyTtl: '{{ item.versionDestroyTtl }}'
  # Lists SecretVersions. This call does not return secret data.
  # ── gcp.secretmanager.projects.secrets.versions.list ──
  - discovery_id: gcp.secretmanager.projects.secrets.versions.list
    # python: svc.projects().secrets().versions().list(**params).execute()
    calls:
      - action: projects.secrets.versions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.versions }}'
      item:
        clientSpecifiedPayloadChecksum: '{{ item.clientSpecifiedPayloadChecksum }}'
        createTime: '{{ item.createTime }}'
        customerManagedEncryption: '{{ item.customerManagedEncryption }}'
        destroyTime: '{{ item.destroyTime }}'
        name: '{{ item.name }}'
        replicationStatus: '{{ item.replicationStatus }}'
        scheduledDestroyTime: '{{ item.scheduledDestroyTime }}'
        state: '{{ item.state }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.secretmanager.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.secretmanager.projects.locations.operations.get
    for_each: gcp.secretmanager.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets metadata for a given Secret.
  # ── gcp.secretmanager.projects.locations.secrets.get [dependent] ──
  - discovery_id: gcp.secretmanager.projects.locations.secrets.get
    for_each: gcp.secretmanager.projects.secrets.list
    # python: svc.projects().locations().secrets().get(**params).execute()
    calls:
      - action: projects.locations.secrets.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        createTime: '{{ response.createTime }}'
        customerManagedEncryption: '{{ response.customerManagedEncryption }}'
        expireTime: '{{ response.expireTime }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        replication: '{{ response.replication }}'
        rotation: '{{ response.rotation }}'
        tags: '{{ response.tags }}'
        topics: '{{ response.topics }}'
        ttl: '{{ response.ttl }}'
        versionAliases: '{{ response.versionAliases }}'
        versionDestroyTtl: '{{ response.versionDestroyTtl }}'
    # required_params: ['name']
  # Gets the access control policy for a secret. Returns empty policy if the secret exists and does not 
  # ── gcp.secretmanager.projects.locations.secrets.getIamPolicy [dependent] ──
  - discovery_id: gcp.secretmanager.projects.locations.secrets.getIamPolicy
    for_each: gcp.secretmanager.projects.secrets.list
    # python: svc.projects().locations().secrets().getIamPolicy(**params).execute()
    calls:
      - action: projects.locations.secrets.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Accesses a SecretVersion. This call returns the secret data. `projects/*/secrets/*/versions/latest` 
  # ── gcp.secretmanager.projects.locations.secrets.versions.access [dependent] ──
  - discovery_id: gcp.secretmanager.projects.locations.secrets.versions.access
    for_each: gcp.secretmanager.projects.secrets.versions.list
    # python: svc.projects().locations().secrets().versions().access(**params).execute()
    calls:
      - action: projects.locations.secrets.versions.access
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        payload: '{{ response.payload }}'
    # required_params: ['name']
  # Gets metadata for a SecretVersion. `projects/*/secrets/*/versions/latest` is an alias to the most re
  # ── gcp.secretmanager.projects.locations.secrets.versions.get [dependent] ──
  - discovery_id: gcp.secretmanager.projects.locations.secrets.versions.get
    for_each: gcp.secretmanager.projects.secrets.versions.list
    # python: svc.projects().locations().secrets().versions().get(**params).execute()
    calls:
      - action: projects.locations.secrets.versions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        clientSpecifiedPayloadChecksum: '{{ response.clientSpecifiedPayloadChecksum }}'
        createTime: '{{ response.createTime }}'
        customerManagedEncryption: '{{ response.customerManagedEncryption }}'
        destroyTime: '{{ response.destroyTime }}'
        name: '{{ response.name }}'
        replicationStatus: '{{ response.replicationStatus }}'
        scheduledDestroyTime: '{{ response.scheduledDestroyTime }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Gets metadata for a given Secret.
  # ── gcp.secretmanager.projects.secrets.get [dependent] ──
  - discovery_id: gcp.secretmanager.projects.secrets.get
    for_each: gcp.secretmanager.projects.secrets.list
    # python: svc.projects().secrets().get(**params).execute()
    calls:
      - action: projects.secrets.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        annotations: '{{ response.annotations }}'
        createTime: '{{ response.createTime }}'
        customerManagedEncryption: '{{ response.customerManagedEncryption }}'
        expireTime: '{{ response.expireTime }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        replication: '{{ response.replication }}'
        rotation: '{{ response.rotation }}'
        tags: '{{ response.tags }}'
        topics: '{{ response.topics }}'
        ttl: '{{ response.ttl }}'
        versionAliases: '{{ response.versionAliases }}'
        versionDestroyTtl: '{{ response.versionDestroyTtl }}'
    # required_params: ['name']
  # Gets the access control policy for a secret. Returns empty policy if the secret exists and does not 
  # ── gcp.secretmanager.projects.secrets.getIamPolicy [dependent] ──
  - discovery_id: gcp.secretmanager.projects.secrets.getIamPolicy
    for_each: gcp.secretmanager.projects.secrets.list
    # python: svc.projects().secrets().getIamPolicy(**params).execute()
    calls:
      - action: projects.secrets.getIamPolicy
        params:
          resource: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        auditConfigs: '{{ response.auditConfigs }}'
        bindings: '{{ response.bindings }}'
        version: '{{ response.version }}'
    # required_params: ['resource']
  # Accesses a SecretVersion. This call returns the secret data. `projects/*/secrets/*/versions/latest` 
  # ── gcp.secretmanager.projects.secrets.versions.access [dependent] ──
  - discovery_id: gcp.secretmanager.projects.secrets.versions.access
    for_each: gcp.secretmanager.projects.secrets.versions.list
    # python: svc.projects().secrets().versions().access(**params).execute()
    calls:
      - action: projects.secrets.versions.access
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        payload: '{{ response.payload }}'
    # required_params: ['name']
  # Gets metadata for a SecretVersion. `projects/*/secrets/*/versions/latest` is an alias to the most re
  # ── gcp.secretmanager.projects.secrets.versions.get [dependent] ──
  - discovery_id: gcp.secretmanager.projects.secrets.versions.get
    for_each: gcp.secretmanager.projects.secrets.versions.list
    # python: svc.projects().secrets().versions().get(**params).execute()
    calls:
      - action: projects.secrets.versions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        clientSpecifiedPayloadChecksum: '{{ response.clientSpecifiedPayloadChecksum }}'
        createTime: '{{ response.createTime }}'
        customerManagedEncryption: '{{ response.customerManagedEncryption }}'
        destroyTime: '{{ response.destroyTime }}'
        name: '{{ response.name }}'
        replicationStatus: '{{ response.replicationStatus }}'
        scheduledDestroyTime: '{{ response.scheduledDestroyTime }}'
        state: '{{ response.state }}'
    # required_params: ['name']
