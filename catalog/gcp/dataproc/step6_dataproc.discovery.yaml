# ============================================================
# Discovery YAML — dataproc (v1)
# Generated: 2026-02-21T03:06:39Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 61 | independent: 13 | dependent: 48
# ops with fields: 61/61 | dep ops with chain: 48/48
# ============================================================
version: '1.0'
provider: gcp
service: dataproc

services:
  client: dataproc
  module: "googleapiclient.discovery.build('dataproc', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists autoscaling policies in the project.
  # ── gcp.dataproc.projects.locations.autoscalingPolicies.list ──
  - discovery_id: gcp.dataproc.projects.locations.autoscalingPolicies.list
    # python: svc.projects().locations().autoscalingPolicies().list(**params).execute()
    calls:
      - action: projects.locations.autoscalingPolicies.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.autoscalingPolicies }}'
      item:
        basicAlgorithm: '{{ item.basicAlgorithm }}'
        clusterType: '{{ item.clusterType }}'
        id: '{{ item.id }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        secondaryWorkerConfig: '{{ item.secondaryWorkerConfig }}'
        workerConfig: '{{ item.workerConfig }}'
  # Lists batch workloads.
  # ── gcp.dataproc.projects.locations.batches.list ──
  - discovery_id: gcp.dataproc.projects.locations.batches.list
    # python: svc.projects().locations().batches().list(**params).execute()
    calls:
      - action: projects.locations.batches.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.batches }}'
      item:
        createTime: '{{ item.createTime }}'
        creator: '{{ item.creator }}'
        environmentConfig: '{{ item.environmentConfig }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        operation: '{{ item.operation }}'
        pysparkBatch: '{{ item.pysparkBatch }}'
        runtimeConfig: '{{ item.runtimeConfig }}'
        runtimeInfo: '{{ item.runtimeInfo }}'
        sparkBatch: '{{ item.sparkBatch }}'
        sparkRBatch: '{{ item.sparkRBatch }}'
        sparkSqlBatch: '{{ item.sparkSqlBatch }}'
        state: '{{ item.state }}'
        stateHistory: '{{ item.stateHistory }}'
        stateMessage: '{{ item.stateMessage }}'
        stateTime: '{{ item.stateTime }}'
        uuid: '{{ item.uuid }}'
  # Obtain high level information and list of Spark Applications corresponding to a batch
  # ── gcp.dataproc.projects.locations.batches.sparkApplications.search ──
  - discovery_id: gcp.dataproc.projects.locations.batches.sparkApplications.search
    # python: svc.projects().locations().batches().sparkApplications().search(**params).execute()
    calls:
      - action: projects.locations.batches.sparkApplications.search
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.sparkApplications }}'
      item:
        application: '{{ item.application }}'
        name: '{{ item.name }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.dataproc.projects.locations.operations.list ──
  - discovery_id: gcp.dataproc.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists session templates.
  # ── gcp.dataproc.projects.locations.sessionTemplates.list ──
  - discovery_id: gcp.dataproc.projects.locations.sessionTemplates.list
    # python: svc.projects().locations().sessionTemplates().list(**params).execute()
    calls:
      - action: projects.locations.sessionTemplates.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.sessionTemplates }}'
      item:
        createTime: '{{ item.createTime }}'
        creator: '{{ item.creator }}'
        description: '{{ item.description }}'
        environmentConfig: '{{ item.environmentConfig }}'
        jupyterSession: '{{ item.jupyterSession }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        runtimeConfig: '{{ item.runtimeConfig }}'
        sparkConnectSession: '{{ item.sparkConnectSession }}'
        updateTime: '{{ item.updateTime }}'
        uuid: '{{ item.uuid }}'
  # Lists interactive sessions.
  # ── gcp.dataproc.projects.locations.sessions.list ──
  - discovery_id: gcp.dataproc.projects.locations.sessions.list
    # python: svc.projects().locations().sessions().list(**params).execute()
    calls:
      - action: projects.locations.sessions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.sessions }}'
      item:
        createTime: '{{ item.createTime }}'
        creator: '{{ item.creator }}'
        environmentConfig: '{{ item.environmentConfig }}'
        jupyterSession: '{{ item.jupyterSession }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        runtimeConfig: '{{ item.runtimeConfig }}'
        runtimeInfo: '{{ item.runtimeInfo }}'
        sessionTemplate: '{{ item.sessionTemplate }}'
        sparkConnectSession: '{{ item.sparkConnectSession }}'
        state: '{{ item.state }}'
        stateHistory: '{{ item.stateHistory }}'
        stateMessage: '{{ item.stateMessage }}'
        stateTime: '{{ item.stateTime }}'
        user: '{{ item.user }}'
        uuid: '{{ item.uuid }}'
  # Obtain high level information and list of Spark Applications corresponding to a batch
  # ── gcp.dataproc.projects.locations.sessions.sparkApplications.search ──
  - discovery_id: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().sessions().sparkApplications().search(**params).execute()
    calls:
      - action: projects.locations.sessions.sparkApplications.search
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.sparkApplications }}'
      item:
        application: '{{ item.application }}'
        name: '{{ item.name }}'
  # Lists workflows that match the specified filter in the request.
  # ── gcp.dataproc.projects.locations.workflowTemplates.list ──
  - discovery_id: gcp.dataproc.projects.locations.workflowTemplates.list
    # python: svc.projects().locations().workflowTemplates().list(**params).execute()
    calls:
      - action: projects.locations.workflowTemplates.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.workflowTemplates }}'
      item:
        createTime: '{{ item.createTime }}'
        dagTimeout: '{{ item.dagTimeout }}'
        encryptionConfig: '{{ item.encryptionConfig }}'
        id: '{{ item.id }}'
        jobs: '{{ item.jobs }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        parameters: '{{ item.parameters }}'
        placement: '{{ item.placement }}'
        updateTime: '{{ item.updateTime }}'
        version: '{{ item.version }}'
  # Lists autoscaling policies in the project.
  # ── gcp.dataproc.projects.regions.autoscalingPolicies.list ──
  - discovery_id: gcp.dataproc.projects.regions.autoscalingPolicies.list
    # python: svc.projects().regions().autoscalingPolicies().list(**params).execute()
    calls:
      - action: projects.regions.autoscalingPolicies.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.autoscalingPolicies }}'
      item:
        basicAlgorithm: '{{ item.basicAlgorithm }}'
        clusterType: '{{ item.clusterType }}'
        id: '{{ item.id }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        secondaryWorkerConfig: '{{ item.secondaryWorkerConfig }}'
        workerConfig: '{{ item.workerConfig }}'
  # Lists all regions/{region}/clusters in a project alphabetically.
  # ── gcp.dataproc.projects.regions.clusters.list ──
  - discovery_id: gcp.dataproc.projects.regions.clusters.list
    # python: svc.projects().regions().clusters().list(**params).execute()
    calls:
      - action: projects.regions.clusters.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.clusters }}'
      item:
        clusterName: '{{ item.clusterName }}'
        clusterUuid: '{{ item.clusterUuid }}'
        config: '{{ item.config }}'
        labels: '{{ item.labels }}'
        metrics: '{{ item.metrics }}'
        projectId: '{{ item.projectId }}'
        status: '{{ item.status }}'
        statusHistory: '{{ item.statusHistory }}'
        virtualClusterConfig: '{{ item.virtualClusterConfig }}'
  # Lists regions/{region}/jobs in a project.
  # ── gcp.dataproc.projects.regions.jobs.list ──
  - discovery_id: gcp.dataproc.projects.regions.jobs.list
    # python: svc.projects().regions().jobs().list(**params).execute()
    calls:
      - action: projects.regions.jobs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.jobs }}'
      item:
        done: '{{ item.done }}'
        driverControlFilesUri: '{{ item.driverControlFilesUri }}'
        driverOutputResourceUri: '{{ item.driverOutputResourceUri }}'
        driverSchedulingConfig: '{{ item.driverSchedulingConfig }}'
        flinkJob: '{{ item.flinkJob }}'
        hadoopJob: '{{ item.hadoopJob }}'
        hiveJob: '{{ item.hiveJob }}'
        jobUuid: '{{ item.jobUuid }}'
        labels: '{{ item.labels }}'
        pigJob: '{{ item.pigJob }}'
        placement: '{{ item.placement }}'
        prestoJob: '{{ item.prestoJob }}'
        pysparkJob: '{{ item.pysparkJob }}'
        reference: '{{ item.reference }}'
        scheduling: '{{ item.scheduling }}'
        sparkJob: '{{ item.sparkJob }}'
        sparkRJob: '{{ item.sparkRJob }}'
        sparkSqlJob: '{{ item.sparkSqlJob }}'
        status: '{{ item.status }}'
        statusHistory: '{{ item.statusHistory }}'
        trinoJob: '{{ item.trinoJob }}'
        yarnApplications: '{{ item.yarnApplications }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.dataproc.projects.regions.operations.list ──
  - discovery_id: gcp.dataproc.projects.regions.operations.list
    # python: svc.projects().regions().operations().list(**params).execute()
    calls:
      - action: projects.regions.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists workflows that match the specified filter in the request.
  # ── gcp.dataproc.projects.regions.workflowTemplates.list ──
  - discovery_id: gcp.dataproc.projects.regions.workflowTemplates.list
    # python: svc.projects().regions().workflowTemplates().list(**params).execute()
    calls:
      - action: projects.regions.workflowTemplates.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.workflowTemplates }}'
      item:
        createTime: '{{ item.createTime }}'
        dagTimeout: '{{ item.dagTimeout }}'
        encryptionConfig: '{{ item.encryptionConfig }}'
        id: '{{ item.id }}'
        jobs: '{{ item.jobs }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        parameters: '{{ item.parameters }}'
        placement: '{{ item.placement }}'
        updateTime: '{{ item.updateTime }}'
        version: '{{ item.version }}'

  # ════ DEPENDENT (enrich) operations ════
  # Retrieves autoscaling policy.
  # ── gcp.dataproc.projects.locations.autoscalingPolicies.get [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.autoscalingPolicies.get
    for_each: gcp.dataproc.projects.regions.autoscalingPolicies.list
    # python: svc.projects().locations().autoscalingPolicies().get(**params).execute()
    calls:
      - action: projects.locations.autoscalingPolicies.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        basicAlgorithm: '{{ response.basicAlgorithm }}'
        clusterType: '{{ response.clusterType }}'
        id: '{{ response.id }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        secondaryWorkerConfig: '{{ response.secondaryWorkerConfig }}'
        workerConfig: '{{ response.workerConfig }}'
    # required_params: ['name']
  # Gets the batch workload resource representation.
  # ── gcp.dataproc.projects.locations.batches.get [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.batches.get
    for_each: gcp.dataproc.projects.locations.batches.list
    # python: svc.projects().locations().batches().get(**params).execute()
    calls:
      - action: projects.locations.batches.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        creator: '{{ response.creator }}'
        environmentConfig: '{{ response.environmentConfig }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        operation: '{{ response.operation }}'
        pysparkBatch: '{{ response.pysparkBatch }}'
        runtimeConfig: '{{ response.runtimeConfig }}'
        runtimeInfo: '{{ response.runtimeInfo }}'
        sparkBatch: '{{ response.sparkBatch }}'
        sparkRBatch: '{{ response.sparkRBatch }}'
        sparkSqlBatch: '{{ response.sparkSqlBatch }}'
        state: '{{ response.state }}'
        stateHistory: '{{ response.stateHistory }}'
        stateMessage: '{{ response.stateMessage }}'
        stateTime: '{{ response.stateTime }}'
        uuid: '{{ response.uuid }}'
    # required_params: ['name']
  # Obtain high level information corresponding to a single Spark Application.
  # ── gcp.dataproc.projects.locations.batches.sparkApplications.access [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.batches.sparkApplications.access
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().batches().sparkApplications().access(**params).execute()
    calls:
      - action: projects.locations.batches.sparkApplications.access
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        application: '{{ response.application }}'
    # required_params: ['name']
  # Obtain environment details for a Spark Application
  # ── gcp.dataproc.projects.locations.batches.sparkApplications.accessEnvironmentInfo [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.batches.sparkApplications.accessEnvironmentInfo
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().batches().sparkApplications().accessEnvironmentInfo(**params).execute()
    calls:
      - action: projects.locations.batches.sparkApplications.accessEnvironmentInfo
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        applicationEnvironmentInfo: '{{ response.applicationEnvironmentInfo }}'
    # required_params: ['name']
  # Obtain data corresponding to a spark job for a Spark Application.
  # ── gcp.dataproc.projects.locations.batches.sparkApplications.accessJob [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.batches.sparkApplications.accessJob
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().batches().sparkApplications().accessJob(**params).execute()
    calls:
      - action: projects.locations.batches.sparkApplications.accessJob
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        jobData: '{{ response.jobData }}'
    # required_params: ['name']
  # Obtain Spark Plan Graph for a Spark Application SQL execution. Limits the number of clusters returne
  # ── gcp.dataproc.projects.locations.batches.sparkApplications.accessSqlPlan [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.batches.sparkApplications.accessSqlPlan
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().batches().sparkApplications().accessSqlPlan(**params).execute()
    calls:
      - action: projects.locations.batches.sparkApplications.accessSqlPlan
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        sparkPlanGraph: '{{ response.sparkPlanGraph }}'
    # required_params: ['name']
  # Obtain data corresponding to a particular SQL Query for a Spark Application.
  # ── gcp.dataproc.projects.locations.batches.sparkApplications.accessSqlQuery [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.batches.sparkApplications.accessSqlQuery
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().batches().sparkApplications().accessSqlQuery(**params).execute()
    calls:
      - action: projects.locations.batches.sparkApplications.accessSqlQuery
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        executionData: '{{ response.executionData }}'
    # required_params: ['name']
  # Obtain data corresponding to a spark stage attempt for a Spark Application.
  # ── gcp.dataproc.projects.locations.batches.sparkApplications.accessStageAttempt [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.batches.sparkApplications.accessStageAttempt
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().batches().sparkApplications().accessStageAttempt(**params).execute()
    calls:
      - action: projects.locations.batches.sparkApplications.accessStageAttempt
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        stageData: '{{ response.stageData }}'
    # required_params: ['name']
  # Obtain RDD operation graph for a Spark Application Stage. Limits the number of clusters returned as 
  # ── gcp.dataproc.projects.locations.batches.sparkApplications.accessStageRddGraph [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.batches.sparkApplications.accessStageRddGraph
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().batches().sparkApplications().accessStageRddGraph(**params).execute()
    calls:
      - action: projects.locations.batches.sparkApplications.accessStageRddGraph
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        rddOperationGraph: '{{ response.rddOperationGraph }}'
    # required_params: ['name']
  # Obtain executor summary with respect to a spark stage attempt.
  # ── gcp.dataproc.projects.locations.batches.sparkApplications.searchExecutorStageSummary [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.batches.sparkApplications.searchExecutorStageSummary
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().batches().sparkApplications().searchExecutorStageSummary(**params).execute()
    calls:
      - action: projects.locations.batches.sparkApplications.searchExecutorStageSummary
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        sparkApplicationStageExecutors: '{{ response.sparkApplicationStageExecutors }}'
    # required_params: ['name']
  # Obtain data corresponding to executors for a Spark Application.
  # ── gcp.dataproc.projects.locations.batches.sparkApplications.searchExecutors [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.batches.sparkApplications.searchExecutors
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().batches().sparkApplications().searchExecutors(**params).execute()
    calls:
      - action: projects.locations.batches.sparkApplications.searchExecutors
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        sparkApplicationExecutors: '{{ response.sparkApplicationExecutors }}'
    # required_params: ['name']
  # Obtain list of spark jobs corresponding to a Spark Application.
  # ── gcp.dataproc.projects.locations.batches.sparkApplications.searchJobs [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.batches.sparkApplications.searchJobs
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().batches().sparkApplications().searchJobs(**params).execute()
    calls:
      - action: projects.locations.batches.sparkApplications.searchJobs
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        sparkApplicationJobs: '{{ response.sparkApplicationJobs }}'
    # required_params: ['name']
  # Obtain data corresponding to SQL Queries for a Spark Application.
  # ── gcp.dataproc.projects.locations.batches.sparkApplications.searchSqlQueries [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.batches.sparkApplications.searchSqlQueries
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().batches().sparkApplications().searchSqlQueries(**params).execute()
    calls:
      - action: projects.locations.batches.sparkApplications.searchSqlQueries
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        sparkApplicationSqlQueries: '{{ response.sparkApplicationSqlQueries }}'
    # required_params: ['name']
  # Obtain data corresponding to tasks for a spark stage attempt for a Spark Application.
  # ── gcp.dataproc.projects.locations.batches.sparkApplications.searchStageAttemptTasks [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.batches.sparkApplications.searchStageAttemptTasks
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().batches().sparkApplications().searchStageAttemptTasks(**params).execute()
    calls:
      - action: projects.locations.batches.sparkApplications.searchStageAttemptTasks
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        sparkApplicationStageAttemptTasks: '{{ response.sparkApplicationStageAttemptTasks }}'
    # required_params: ['name']
  # Obtain data corresponding to a spark stage attempts for a Spark Application.
  # ── gcp.dataproc.projects.locations.batches.sparkApplications.searchStageAttempts [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.batches.sparkApplications.searchStageAttempts
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().batches().sparkApplications().searchStageAttempts(**params).execute()
    calls:
      - action: projects.locations.batches.sparkApplications.searchStageAttempts
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        sparkApplicationStageAttempts: '{{ response.sparkApplicationStageAttempts }}'
    # required_params: ['name']
  # Obtain data corresponding to stages for a Spark Application.
  # ── gcp.dataproc.projects.locations.batches.sparkApplications.searchStages [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.batches.sparkApplications.searchStages
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().batches().sparkApplications().searchStages(**params).execute()
    calls:
      - action: projects.locations.batches.sparkApplications.searchStages
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        sparkApplicationStages: '{{ response.sparkApplicationStages }}'
    # required_params: ['name']
  # Obtain summary of Executor Summary for a Spark Application
  # ── gcp.dataproc.projects.locations.batches.sparkApplications.summarizeExecutors [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.batches.sparkApplications.summarizeExecutors
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().batches().sparkApplications().summarizeExecutors(**params).execute()
    calls:
      - action: projects.locations.batches.sparkApplications.summarizeExecutors
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        activeExecutorSummary: '{{ response.activeExecutorSummary }}'
        applicationId: '{{ response.applicationId }}'
        deadExecutorSummary: '{{ response.deadExecutorSummary }}'
        totalExecutorSummary: '{{ response.totalExecutorSummary }}'
    # required_params: ['name']
  # Obtain summary of Jobs for a Spark Application
  # ── gcp.dataproc.projects.locations.batches.sparkApplications.summarizeJobs [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.batches.sparkApplications.summarizeJobs
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().batches().sparkApplications().summarizeJobs(**params).execute()
    calls:
      - action: projects.locations.batches.sparkApplications.summarizeJobs
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        jobsSummary: '{{ response.jobsSummary }}'
    # required_params: ['name']
  # Obtain summary of Tasks for a Spark Application Stage Attempt
  # ── gcp.dataproc.projects.locations.batches.sparkApplications.summarizeStageAttemptTasks [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.batches.sparkApplications.summarizeStageAttemptTasks
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().batches().sparkApplications().summarizeStageAttemptTasks(**params).execute()
    calls:
      - action: projects.locations.batches.sparkApplications.summarizeStageAttemptTasks
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        stageAttemptTasksSummary: '{{ response.stageAttemptTasksSummary }}'
    # required_params: ['name']
  # Obtain summary of Stages for a Spark Application
  # ── gcp.dataproc.projects.locations.batches.sparkApplications.summarizeStages [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.batches.sparkApplications.summarizeStages
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().batches().sparkApplications().summarizeStages(**params).execute()
    calls:
      - action: projects.locations.batches.sparkApplications.summarizeStages
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        stagesSummary: '{{ response.stagesSummary }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.dataproc.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.operations.get
    for_each: gcp.dataproc.projects.regions.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets the resource representation for a session template.
  # ── gcp.dataproc.projects.locations.sessionTemplates.get [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.sessionTemplates.get
    for_each: gcp.dataproc.projects.locations.sessionTemplates.list
    # python: svc.projects().locations().sessionTemplates().get(**params).execute()
    calls:
      - action: projects.locations.sessionTemplates.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        creator: '{{ response.creator }}'
        description: '{{ response.description }}'
        environmentConfig: '{{ response.environmentConfig }}'
        jupyterSession: '{{ response.jupyterSession }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        runtimeConfig: '{{ response.runtimeConfig }}'
        sparkConnectSession: '{{ response.sparkConnectSession }}'
        updateTime: '{{ response.updateTime }}'
        uuid: '{{ response.uuid }}'
    # required_params: ['name']
  # Gets the resource representation for an interactive session.
  # ── gcp.dataproc.projects.locations.sessions.get [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.sessions.get
    for_each: gcp.dataproc.projects.locations.sessions.list
    # python: svc.projects().locations().sessions().get(**params).execute()
    calls:
      - action: projects.locations.sessions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        creator: '{{ response.creator }}'
        environmentConfig: '{{ response.environmentConfig }}'
        jupyterSession: '{{ response.jupyterSession }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        runtimeConfig: '{{ response.runtimeConfig }}'
        runtimeInfo: '{{ response.runtimeInfo }}'
        sessionTemplate: '{{ response.sessionTemplate }}'
        sparkConnectSession: '{{ response.sparkConnectSession }}'
        state: '{{ response.state }}'
        stateHistory: '{{ response.stateHistory }}'
        stateMessage: '{{ response.stateMessage }}'
        stateTime: '{{ response.stateTime }}'
        user: '{{ response.user }}'
        uuid: '{{ response.uuid }}'
    # required_params: ['name']
  # Obtain high level information corresponding to a single Spark Application.
  # ── gcp.dataproc.projects.locations.sessions.sparkApplications.access [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.sessions.sparkApplications.access
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().sessions().sparkApplications().access(**params).execute()
    calls:
      - action: projects.locations.sessions.sparkApplications.access
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        application: '{{ response.application }}'
    # required_params: ['name']
  # Obtain environment details for a Spark Application
  # ── gcp.dataproc.projects.locations.sessions.sparkApplications.accessEnvironmentInfo [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.sessions.sparkApplications.accessEnvironmentInfo
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().sessions().sparkApplications().accessEnvironmentInfo(**params).execute()
    calls:
      - action: projects.locations.sessions.sparkApplications.accessEnvironmentInfo
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        applicationEnvironmentInfo: '{{ response.applicationEnvironmentInfo }}'
    # required_params: ['name']
  # Obtain data corresponding to a spark job for a Spark Application.
  # ── gcp.dataproc.projects.locations.sessions.sparkApplications.accessJob [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.sessions.sparkApplications.accessJob
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().sessions().sparkApplications().accessJob(**params).execute()
    calls:
      - action: projects.locations.sessions.sparkApplications.accessJob
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        jobData: '{{ response.jobData }}'
    # required_params: ['name']
  # Obtain Spark Plan Graph for a Spark Application SQL execution. Limits the number of clusters returne
  # ── gcp.dataproc.projects.locations.sessions.sparkApplications.accessSqlPlan [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.sessions.sparkApplications.accessSqlPlan
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().sessions().sparkApplications().accessSqlPlan(**params).execute()
    calls:
      - action: projects.locations.sessions.sparkApplications.accessSqlPlan
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        sparkPlanGraph: '{{ response.sparkPlanGraph }}'
    # required_params: ['name']
  # Obtain data corresponding to a particular SQL Query for a Spark Application.
  # ── gcp.dataproc.projects.locations.sessions.sparkApplications.accessSqlQuery [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.sessions.sparkApplications.accessSqlQuery
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().sessions().sparkApplications().accessSqlQuery(**params).execute()
    calls:
      - action: projects.locations.sessions.sparkApplications.accessSqlQuery
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        executionData: '{{ response.executionData }}'
    # required_params: ['name']
  # Obtain data corresponding to a spark stage attempt for a Spark Application.
  # ── gcp.dataproc.projects.locations.sessions.sparkApplications.accessStageAttempt [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.sessions.sparkApplications.accessStageAttempt
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().sessions().sparkApplications().accessStageAttempt(**params).execute()
    calls:
      - action: projects.locations.sessions.sparkApplications.accessStageAttempt
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        stageData: '{{ response.stageData }}'
    # required_params: ['name']
  # Obtain RDD operation graph for a Spark Application Stage. Limits the number of clusters returned as 
  # ── gcp.dataproc.projects.locations.sessions.sparkApplications.accessStageRddGraph [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.sessions.sparkApplications.accessStageRddGraph
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().sessions().sparkApplications().accessStageRddGraph(**params).execute()
    calls:
      - action: projects.locations.sessions.sparkApplications.accessStageRddGraph
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        rddOperationGraph: '{{ response.rddOperationGraph }}'
    # required_params: ['name']
  # Obtain executor summary with respect to a spark stage attempt.
  # ── gcp.dataproc.projects.locations.sessions.sparkApplications.searchExecutorStageSummary [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.sessions.sparkApplications.searchExecutorStageSummary
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().sessions().sparkApplications().searchExecutorStageSummary(**params).execute()
    calls:
      - action: projects.locations.sessions.sparkApplications.searchExecutorStageSummary
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        sparkApplicationStageExecutors: '{{ response.sparkApplicationStageExecutors }}'
    # required_params: ['name']
  # Obtain data corresponding to executors for a Spark Application.
  # ── gcp.dataproc.projects.locations.sessions.sparkApplications.searchExecutors [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.sessions.sparkApplications.searchExecutors
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().sessions().sparkApplications().searchExecutors(**params).execute()
    calls:
      - action: projects.locations.sessions.sparkApplications.searchExecutors
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        sparkApplicationExecutors: '{{ response.sparkApplicationExecutors }}'
    # required_params: ['name']
  # Obtain list of spark jobs corresponding to a Spark Application.
  # ── gcp.dataproc.projects.locations.sessions.sparkApplications.searchJobs [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.sessions.sparkApplications.searchJobs
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().sessions().sparkApplications().searchJobs(**params).execute()
    calls:
      - action: projects.locations.sessions.sparkApplications.searchJobs
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        sparkApplicationJobs: '{{ response.sparkApplicationJobs }}'
    # required_params: ['name']
  # Obtain data corresponding to SQL Queries for a Spark Application.
  # ── gcp.dataproc.projects.locations.sessions.sparkApplications.searchSqlQueries [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.sessions.sparkApplications.searchSqlQueries
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().sessions().sparkApplications().searchSqlQueries(**params).execute()
    calls:
      - action: projects.locations.sessions.sparkApplications.searchSqlQueries
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        sparkApplicationSqlQueries: '{{ response.sparkApplicationSqlQueries }}'
    # required_params: ['name']
  # Obtain data corresponding to tasks for a spark stage attempt for a Spark Application.
  # ── gcp.dataproc.projects.locations.sessions.sparkApplications.searchStageAttemptTasks [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.sessions.sparkApplications.searchStageAttemptTasks
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().sessions().sparkApplications().searchStageAttemptTasks(**params).execute()
    calls:
      - action: projects.locations.sessions.sparkApplications.searchStageAttemptTasks
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        sparkApplicationStageAttemptTasks: '{{ response.sparkApplicationStageAttemptTasks }}'
    # required_params: ['name']
  # Obtain data corresponding to a spark stage attempts for a Spark Application.
  # ── gcp.dataproc.projects.locations.sessions.sparkApplications.searchStageAttempts [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.sessions.sparkApplications.searchStageAttempts
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().sessions().sparkApplications().searchStageAttempts(**params).execute()
    calls:
      - action: projects.locations.sessions.sparkApplications.searchStageAttempts
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        sparkApplicationStageAttempts: '{{ response.sparkApplicationStageAttempts }}'
    # required_params: ['name']
  # Obtain data corresponding to stages for a Spark Application.
  # ── gcp.dataproc.projects.locations.sessions.sparkApplications.searchStages [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.sessions.sparkApplications.searchStages
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().sessions().sparkApplications().searchStages(**params).execute()
    calls:
      - action: projects.locations.sessions.sparkApplications.searchStages
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        sparkApplicationStages: '{{ response.sparkApplicationStages }}'
    # required_params: ['name']
  # Obtain summary of Executor Summary for a Spark Application
  # ── gcp.dataproc.projects.locations.sessions.sparkApplications.summarizeExecutors [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.sessions.sparkApplications.summarizeExecutors
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().sessions().sparkApplications().summarizeExecutors(**params).execute()
    calls:
      - action: projects.locations.sessions.sparkApplications.summarizeExecutors
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        activeExecutorSummary: '{{ response.activeExecutorSummary }}'
        applicationId: '{{ response.applicationId }}'
        deadExecutorSummary: '{{ response.deadExecutorSummary }}'
        totalExecutorSummary: '{{ response.totalExecutorSummary }}'
    # required_params: ['name']
  # Obtain summary of Jobs for a Spark Application
  # ── gcp.dataproc.projects.locations.sessions.sparkApplications.summarizeJobs [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.sessions.sparkApplications.summarizeJobs
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().sessions().sparkApplications().summarizeJobs(**params).execute()
    calls:
      - action: projects.locations.sessions.sparkApplications.summarizeJobs
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        jobsSummary: '{{ response.jobsSummary }}'
    # required_params: ['name']
  # Obtain summary of Tasks for a Spark Application Stage Attempt
  # ── gcp.dataproc.projects.locations.sessions.sparkApplications.summarizeStageAttemptTasks [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.sessions.sparkApplications.summarizeStageAttemptTasks
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().sessions().sparkApplications().summarizeStageAttemptTasks(**params).execute()
    calls:
      - action: projects.locations.sessions.sparkApplications.summarizeStageAttemptTasks
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        stageAttemptTasksSummary: '{{ response.stageAttemptTasksSummary }}'
    # required_params: ['name']
  # Obtain summary of Stages for a Spark Application
  # ── gcp.dataproc.projects.locations.sessions.sparkApplications.summarizeStages [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.sessions.sparkApplications.summarizeStages
    for_each: gcp.dataproc.projects.locations.sessions.sparkApplications.search
    # python: svc.projects().locations().sessions().sparkApplications().summarizeStages(**params).execute()
    calls:
      - action: projects.locations.sessions.sparkApplications.summarizeStages
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        stagesSummary: '{{ response.stagesSummary }}'
    # required_params: ['name']
  # Retrieves the latest workflow template.Can retrieve previously instantiated template by specifying o
  # ── gcp.dataproc.projects.locations.workflowTemplates.get [dependent] ──
  - discovery_id: gcp.dataproc.projects.locations.workflowTemplates.get
    for_each: gcp.dataproc.projects.regions.workflowTemplates.list
    # python: svc.projects().locations().workflowTemplates().get(**params).execute()
    calls:
      - action: projects.locations.workflowTemplates.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        dagTimeout: '{{ response.dagTimeout }}'
        encryptionConfig: '{{ response.encryptionConfig }}'
        id: '{{ response.id }}'
        jobs: '{{ response.jobs }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        parameters: '{{ response.parameters }}'
        placement: '{{ response.placement }}'
        updateTime: '{{ response.updateTime }}'
        version: '{{ response.version }}'
    # required_params: ['name']
  # Retrieves autoscaling policy.
  # ── gcp.dataproc.projects.regions.autoscalingPolicies.get [dependent] ──
  - discovery_id: gcp.dataproc.projects.regions.autoscalingPolicies.get
    for_each: gcp.dataproc.projects.regions.autoscalingPolicies.list
    # python: svc.projects().regions().autoscalingPolicies().get(**params).execute()
    calls:
      - action: projects.regions.autoscalingPolicies.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        basicAlgorithm: '{{ response.basicAlgorithm }}'
        clusterType: '{{ response.clusterType }}'
        id: '{{ response.id }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        secondaryWorkerConfig: '{{ response.secondaryWorkerConfig }}'
        workerConfig: '{{ response.workerConfig }}'
    # required_params: ['name']
  # Gets the resource representation for a cluster in a project.
  # ── gcp.dataproc.projects.regions.clusters.get [dependent] ──
  - discovery_id: gcp.dataproc.projects.regions.clusters.get
    for_each: gcp.dataproc.projects.regions.clusters.list
    # python: svc.projects().regions().clusters().get(**params).execute()
    calls:
      - action: projects.regions.clusters.get
        params:
          clusterName: '{{ item.config }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        clusterName: '{{ response.clusterName }}'
        clusterUuid: '{{ response.clusterUuid }}'
        config: '{{ response.config }}'
        labels: '{{ response.labels }}'
        metrics: '{{ response.metrics }}'
        projectId: '{{ response.projectId }}'
        status: '{{ response.status }}'
        statusHistory: '{{ response.statusHistory }}'
        virtualClusterConfig: '{{ response.virtualClusterConfig }}'
    # required_params: ['clusterName', 'projectId', 'region']
  # Gets the resource representation for a node group in a cluster.
  # ── gcp.dataproc.projects.regions.clusters.nodeGroups.get [dependent] ──
  - discovery_id: gcp.dataproc.projects.regions.clusters.nodeGroups.get
    for_each: gcp.dataproc.projects.regions.clusters.list
    # python: svc.projects().regions().clusters().nodeGroups().get(**params).execute()
    calls:
      - action: projects.regions.clusters.nodeGroups.get
        params:
          name: '{{ item.config }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        nodeGroupConfig: '{{ response.nodeGroupConfig }}'
        roles: '{{ response.roles }}'
    # required_params: ['name']
  # Gets the resource representation for a job in a project.
  # ── gcp.dataproc.projects.regions.jobs.get [dependent] ──
  - discovery_id: gcp.dataproc.projects.regions.jobs.get
    for_each: gcp.dataproc.projects.regions.jobs.list
    # python: svc.projects().regions().jobs().get(**params).execute()
    calls:
      - action: projects.regions.jobs.get
        params:
          jobId: '{{ item.driverSchedulingConfig }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        driverControlFilesUri: '{{ response.driverControlFilesUri }}'
        driverOutputResourceUri: '{{ response.driverOutputResourceUri }}'
        driverSchedulingConfig: '{{ response.driverSchedulingConfig }}'
        flinkJob: '{{ response.flinkJob }}'
        hadoopJob: '{{ response.hadoopJob }}'
        hiveJob: '{{ response.hiveJob }}'
        jobUuid: '{{ response.jobUuid }}'
        labels: '{{ response.labels }}'
        pigJob: '{{ response.pigJob }}'
        placement: '{{ response.placement }}'
        prestoJob: '{{ response.prestoJob }}'
        pysparkJob: '{{ response.pysparkJob }}'
        reference: '{{ response.reference }}'
        scheduling: '{{ response.scheduling }}'
        sparkJob: '{{ response.sparkJob }}'
        sparkRJob: '{{ response.sparkRJob }}'
        sparkSqlJob: '{{ response.sparkSqlJob }}'
        status: '{{ response.status }}'
        statusHistory: '{{ response.statusHistory }}'
        trinoJob: '{{ response.trinoJob }}'
        yarnApplications: '{{ response.yarnApplications }}'
    # required_params: ['projectId', 'region', 'jobId']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.dataproc.projects.regions.operations.get [dependent] ──
  - discovery_id: gcp.dataproc.projects.regions.operations.get
    for_each: gcp.dataproc.projects.regions.operations.list
    # python: svc.projects().regions().operations().get(**params).execute()
    calls:
      - action: projects.regions.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Retrieves the latest workflow template.Can retrieve previously instantiated template by specifying o
  # ── gcp.dataproc.projects.regions.workflowTemplates.get [dependent] ──
  - discovery_id: gcp.dataproc.projects.regions.workflowTemplates.get
    for_each: gcp.dataproc.projects.regions.workflowTemplates.list
    # python: svc.projects().regions().workflowTemplates().get(**params).execute()
    calls:
      - action: projects.regions.workflowTemplates.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        dagTimeout: '{{ response.dagTimeout }}'
        encryptionConfig: '{{ response.encryptionConfig }}'
        id: '{{ response.id }}'
        jobs: '{{ response.jobs }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        parameters: '{{ response.parameters }}'
        placement: '{{ response.placement }}'
        updateTime: '{{ response.updateTime }}'
        version: '{{ response.version }}'
    # required_params: ['name']
