# ============================================================
# Discovery YAML — documentai (v1)
# Generated: 2026-02-21T03:06:40Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 18 | independent: 10 | dependent: 8
# ops with fields: 18/18 | dep ops with chain: 8/8
# ============================================================
version: '1.0'
provider: gcp
service: documentai

services:
  client: documentai
  module: "googleapiclient.discovery.build('documentai', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Fetches processor types. Note that we don't use ListProcessorTypes here, because it isn't paginated.
  # ── gcp.documentai.projects.locations.fetchProcessorTypes ──
  - discovery_id: gcp.documentai.projects.locations.fetchProcessorTypes
    # python: svc.projects().locations().fetchProcessorTypes(**params).execute()
    calls:
      - action: projects.locations.fetchProcessorTypes
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        processorTypes: '{{ response.processorTypes }}'
  # Gets information about a location.
  # ── gcp.documentai.projects.locations.get ──
  - discovery_id: gcp.documentai.projects.locations.get
    # python: svc.projects().locations().get(**params).execute()
    calls:
      - action: projects.locations.get
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        locationId: '{{ response.locationId }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
  # Lists information about the supported locations for this service. This method can be called in two w
  # ── gcp.documentai.projects.locations.list ──
  - discovery_id: gcp.documentai.projects.locations.list
    # python: svc.projects().locations().list(**params).execute()
    calls:
      - action: projects.locations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.locations }}'
      item:
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        locationId: '{{ item.locationId }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.documentai.projects.locations.operations.list ──
  - discovery_id: gcp.documentai.projects.locations.operations.list
    # python: svc.projects().locations().operations().list(**params).execute()
    calls:
      - action: projects.locations.operations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists the processor types that exist.
  # ── gcp.documentai.projects.locations.processorTypes.list ──
  - discovery_id: gcp.documentai.projects.locations.processorTypes.list
    # python: svc.projects().locations().processorTypes().list(**params).execute()
    calls:
      - action: projects.locations.processorTypes.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.processorTypes }}'
      item:
        allowCreation: '{{ item.allowCreation }}'
        availableLocations: '{{ item.availableLocations }}'
        category: '{{ item.category }}'
        launchStage: '{{ item.launchStage }}'
        name: '{{ item.name }}'
        sampleDocumentUris: '{{ item.sampleDocumentUris }}'
        type: '{{ item.type }}'
  # Lists all processors which belong to this project.
  # ── gcp.documentai.projects.locations.processors.list ──
  - discovery_id: gcp.documentai.projects.locations.processors.list
    # python: svc.projects().locations().processors().list(**params).execute()
    calls:
      - action: projects.locations.processors.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.processors }}'
      item:
        activeSchemaVersion: '{{ item.activeSchemaVersion }}'
        createTime: '{{ item.createTime }}'
        defaultProcessorVersion: '{{ item.defaultProcessorVersion }}'
        displayName: '{{ item.displayName }}'
        kmsKeyName: '{{ item.kmsKeyName }}'
        name: '{{ item.name }}'
        processEndpoint: '{{ item.processEndpoint }}'
        processorVersionAliases: '{{ item.processorVersionAliases }}'
        satisfiesPzi: '{{ item.satisfiesPzi }}'
        satisfiesPzs: '{{ item.satisfiesPzs }}'
        state: '{{ item.state }}'
        type: '{{ item.type }}'
  # Retrieves a set of evaluations for a given processor version.
  # ── gcp.documentai.projects.locations.processors.processorVersions.evaluations.list ──
  - discovery_id: gcp.documentai.projects.locations.processors.processorVersions.evaluations.list
    # python: svc.projects().locations().processors().processorVersions().evaluations().list(**params).execute()
    calls:
      - action: projects.locations.processors.processorVersions.evaluations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.evaluations }}'
      item:
        allEntitiesMetrics: '{{ item.allEntitiesMetrics }}'
        createTime: '{{ item.createTime }}'
        documentCounters: '{{ item.documentCounters }}'
        entityMetrics: '{{ item.entityMetrics }}'
        kmsKeyName: '{{ item.kmsKeyName }}'
        kmsKeyVersionName: '{{ item.kmsKeyVersionName }}'
        name: '{{ item.name }}'
        revisions: '{{ item.revisions }}'
  # Lists all versions of a processor.
  # ── gcp.documentai.projects.locations.processors.processorVersions.list ──
  - discovery_id: gcp.documentai.projects.locations.processors.processorVersions.list
    # python: svc.projects().locations().processors().processorVersions().list(**params).execute()
    calls:
      - action: projects.locations.processors.processorVersions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.processorVersions }}'
      item:
        createTime: '{{ item.createTime }}'
        deprecationInfo: '{{ item.deprecationInfo }}'
        displayName: '{{ item.displayName }}'
        documentSchema: '{{ item.documentSchema }}'
        genAiModelInfo: '{{ item.genAiModelInfo }}'
        googleManaged: '{{ item.googleManaged }}'
        kmsKeyName: '{{ item.kmsKeyName }}'
        kmsKeyVersionName: '{{ item.kmsKeyVersionName }}'
        latestEvaluation: '{{ item.latestEvaluation }}'
        modelType: '{{ item.modelType }}'
        name: '{{ item.name }}'
        satisfiesPzi: '{{ item.satisfiesPzi }}'
        satisfiesPzs: '{{ item.satisfiesPzs }}'
        state: '{{ item.state }}'
  # Lists Schemas.
  # ── gcp.documentai.projects.locations.schemas.list ──
  - discovery_id: gcp.documentai.projects.locations.schemas.list
    # python: svc.projects().locations().schemas().list(**params).execute()
    calls:
      - action: projects.locations.schemas.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.schemas }}'
      item:
        createTime: '{{ item.createTime }}'
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        updateTime: '{{ item.updateTime }}'
  # Lists SchemaVersions.
  # ── gcp.documentai.projects.locations.schemas.schemaVersions.list ──
  - discovery_id: gcp.documentai.projects.locations.schemas.schemaVersions.list
    # python: svc.projects().locations().schemas().schemaVersions().list(**params).execute()
    calls:
      - action: projects.locations.schemas.schemaVersions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.schemaVersions }}'
      item:
        createTime: '{{ item.createTime }}'
        displayName: '{{ item.displayName }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        schema: '{{ item.schema }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.documentai.projects.locations.operations.get [dependent] ──
  - discovery_id: gcp.documentai.projects.locations.operations.get
    for_each: gcp.documentai.projects.locations.operations.list
    # python: svc.projects().locations().operations().get(**params).execute()
    calls:
      - action: projects.locations.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Gets a processor type detail.
  # ── gcp.documentai.projects.locations.processorTypes.get [dependent] ──
  - discovery_id: gcp.documentai.projects.locations.processorTypes.get
    for_each: gcp.documentai.projects.locations.processorTypes.list
    # python: svc.projects().locations().processorTypes().get(**params).execute()
    calls:
      - action: projects.locations.processorTypes.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        allowCreation: '{{ response.allowCreation }}'
        availableLocations: '{{ response.availableLocations }}'
        category: '{{ response.category }}'
        launchStage: '{{ response.launchStage }}'
        name: '{{ response.name }}'
        sampleDocumentUris: '{{ response.sampleDocumentUris }}'
        type: '{{ response.type }}'
    # required_params: ['name']
  # Gets a processor detail.
  # ── gcp.documentai.projects.locations.processors.get [dependent] ──
  - discovery_id: gcp.documentai.projects.locations.processors.get
    for_each: gcp.documentai.projects.locations.processors.list
    # python: svc.projects().locations().processors().get(**params).execute()
    calls:
      - action: projects.locations.processors.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        activeSchemaVersion: '{{ response.activeSchemaVersion }}'
        createTime: '{{ response.createTime }}'
        defaultProcessorVersion: '{{ response.defaultProcessorVersion }}'
        displayName: '{{ response.displayName }}'
        kmsKeyName: '{{ response.kmsKeyName }}'
        name: '{{ response.name }}'
        processEndpoint: '{{ response.processEndpoint }}'
        processorVersionAliases: '{{ response.processorVersionAliases }}'
        satisfiesPzi: '{{ response.satisfiesPzi }}'
        satisfiesPzs: '{{ response.satisfiesPzs }}'
        state: '{{ response.state }}'
        type: '{{ response.type }}'
    # required_params: ['name']
  # Retrieves a specific evaluation.
  # ── gcp.documentai.projects.locations.processors.processorVersions.evaluations.get [dependent] ──
  - discovery_id: gcp.documentai.projects.locations.processors.processorVersions.evaluations.get
    for_each: gcp.documentai.projects.locations.processors.processorVersions.evaluations.list
    # python: svc.projects().locations().processors().processorVersions().evaluations().get(**params).execute()
    calls:
      - action: projects.locations.processors.processorVersions.evaluations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        allEntitiesMetrics: '{{ response.allEntitiesMetrics }}'
        createTime: '{{ response.createTime }}'
        documentCounters: '{{ response.documentCounters }}'
        entityMetrics: '{{ response.entityMetrics }}'
        kmsKeyName: '{{ response.kmsKeyName }}'
        kmsKeyVersionName: '{{ response.kmsKeyVersionName }}'
        name: '{{ response.name }}'
        revisions: '{{ response.revisions }}'
    # required_params: ['name']
  # Gets a processor version detail.
  # ── gcp.documentai.projects.locations.processors.processorVersions.get [dependent] ──
  - discovery_id: gcp.documentai.projects.locations.processors.processorVersions.get
    for_each: gcp.documentai.projects.locations.processors.processorVersions.list
    # python: svc.projects().locations().processors().processorVersions().get(**params).execute()
    calls:
      - action: projects.locations.processors.processorVersions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        deprecationInfo: '{{ response.deprecationInfo }}'
        displayName: '{{ response.displayName }}'
        documentSchema: '{{ response.documentSchema }}'
        genAiModelInfo: '{{ response.genAiModelInfo }}'
        googleManaged: '{{ response.googleManaged }}'
        kmsKeyName: '{{ response.kmsKeyName }}'
        kmsKeyVersionName: '{{ response.kmsKeyVersionName }}'
        latestEvaluation: '{{ response.latestEvaluation }}'
        modelType: '{{ response.modelType }}'
        name: '{{ response.name }}'
        satisfiesPzi: '{{ response.satisfiesPzi }}'
        satisfiesPzs: '{{ response.satisfiesPzs }}'
        state: '{{ response.state }}'
    # required_params: ['name']
  # Gets a schema.
  # ── gcp.documentai.projects.locations.schemas.get [dependent] ──
  - discovery_id: gcp.documentai.projects.locations.schemas.get
    for_each: gcp.documentai.projects.locations.schemas.list
    # python: svc.projects().locations().schemas().get(**params).execute()
    calls:
      - action: projects.locations.schemas.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets a schema version.
  # ── gcp.documentai.projects.locations.schemas.schemaVersions.get [dependent] ──
  - discovery_id: gcp.documentai.projects.locations.schemas.schemaVersions.get
    for_each: gcp.documentai.projects.locations.schemas.schemaVersions.list
    # python: svc.projects().locations().schemas().schemaVersions().get(**params).execute()
    calls:
      - action: projects.locations.schemas.schemaVersions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        displayName: '{{ response.displayName }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        schema: '{{ response.schema }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.documentai.projects.operations.get [dependent] ──
  - discovery_id: gcp.documentai.projects.operations.get
    for_each: gcp.documentai.projects.locations.operations.list
    # python: svc.projects().operations().get(**params).execute()
    calls:
      - action: projects.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
