# ============================================================
# Discovery YAML — spanner (v1)
# Generated: 2026-02-21T03:06:41Z
# Sources: step2 (ops) | step3 (dep chain) | step4/step5 (fields)
# read ops: 35 | independent: 12 | dependent: 23
# ops with fields: 35/35 | dep ops with chain: 23/23
# ============================================================
version: '1.0'
provider: gcp
service: spanner

services:
  client: spanner
  module: "googleapiclient.discovery.build('spanner', 'v1')"

# Anchors: fixed service-level parameters (caller provides values)
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # Lists the user-managed instance configuration long-running operations in the given project. An insta
  # ── gcp.spanner.projects.instanceConfigOperations.list ──
  - discovery_id: gcp.spanner.projects.instanceConfigOperations.list
    # python: svc.projects().instanceConfigOperations().list(**params).execute()
    calls:
      - action: projects.instanceConfigOperations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.instanceConfigOperations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists the supported instance configurations for a given project. Returns both Google-managed configu
  # ── gcp.spanner.projects.instanceConfigs.list ──
  - discovery_id: gcp.spanner.projects.instanceConfigs.list
    # python: svc.projects().instanceConfigs().list(**params).execute()
    calls:
      - action: projects.instanceConfigs.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.instanceConfigs }}'
      item:
        baseConfig: '{{ item.baseConfig }}'
        configType: '{{ item.configType }}'
        displayName: '{{ item.displayName }}'
        freeInstanceAvailability: '{{ item.freeInstanceAvailability }}'
        labels: '{{ item.labels }}'
        leaderOptions: '{{ item.leaderOptions }}'
        name: '{{ item.name }}'
        optionalReplicas: '{{ item.optionalReplicas }}'
        quorumType: '{{ item.quorumType }}'
        reconciling: '{{ item.reconciling }}'
        replicas: '{{ item.replicas }}'
        state: '{{ item.state }}'
        storageLimitPerProcessingUnit: '{{ item.storageLimitPerProcessingUnit }}'
  # Lists the backup long-running operations in the given instance. A backup operation has a name of the
  # ── gcp.spanner.projects.instances.backupOperations.list ──
  - discovery_id: gcp.spanner.projects.instances.backupOperations.list
    # python: svc.projects().instances().backupOperations().list(**params).execute()
    calls:
      - action: projects.instances.backupOperations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.backupOperations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists completed and pending backups. Backups returned are ordered by `create_time` in descending ord
  # ── gcp.spanner.projects.instances.backups.list ──
  - discovery_id: gcp.spanner.projects.instances.backups.list
    # python: svc.projects().instances().backups().list(**params).execute()
    calls:
      - action: projects.instances.backups.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.backups }}'
      item:
        backupSchedules: '{{ item.backupSchedules }}'
        createTime: '{{ item.createTime }}'
        database: '{{ item.database }}'
        databaseDialect: '{{ item.databaseDialect }}'
        encryptionInfo: '{{ item.encryptionInfo }}'
        encryptionInformation: '{{ item.encryptionInformation }}'
        exclusiveSizeBytes: '{{ item.exclusiveSizeBytes }}'
        expireTime: '{{ item.expireTime }}'
        freeableSizeBytes: '{{ item.freeableSizeBytes }}'
        incrementalBackupChainId: '{{ item.incrementalBackupChainId }}'
        instancePartitions: '{{ item.instancePartitions }}'
        maxExpireTime: '{{ item.maxExpireTime }}'
        minimumRestorableEdition: '{{ item.minimumRestorableEdition }}'
        name: '{{ item.name }}'
        oldestVersionTime: '{{ item.oldestVersionTime }}'
        referencingBackups: '{{ item.referencingBackups }}'
        referencingDatabases: '{{ item.referencingDatabases }}'
        sizeBytes: '{{ item.sizeBytes }}'
        state: '{{ item.state }}'
        versionTime: '{{ item.versionTime }}'
  # Lists database longrunning-operations. A database operation has a name of the form `projects//instan
  # ── gcp.spanner.projects.instances.databaseOperations.list ──
  - discovery_id: gcp.spanner.projects.instances.databaseOperations.list
    # python: svc.projects().instances().databaseOperations().list(**params).execute()
    calls:
      - action: projects.instances.databaseOperations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.databaseOperations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists all the backup schedules for the database.
  # ── gcp.spanner.projects.instances.databases.backupSchedules.list ──
  - discovery_id: gcp.spanner.projects.instances.databases.backupSchedules.list
    # python: svc.projects().instances().databases().backupSchedules().list(**params).execute()
    calls:
      - action: projects.instances.databases.backupSchedules.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.backupSchedules }}'
      item:
        encryptionConfig: '{{ item.encryptionConfig }}'
        fullBackupSpec: '{{ item.fullBackupSpec }}'
        incrementalBackupSpec: '{{ item.incrementalBackupSpec }}'
        name: '{{ item.name }}'
        retentionDuration: '{{ item.retentionDuration }}'
        spec: '{{ item.spec }}'
        updateTime: '{{ item.updateTime }}'
  # Lists Cloud Spanner database roles.
  # ── gcp.spanner.projects.instances.databases.databaseRoles.list ──
  - discovery_id: gcp.spanner.projects.instances.databases.databaseRoles.list
    # python: svc.projects().instances().databases().databaseRoles().list(**params).execute()
    calls:
      - action: projects.instances.databases.databaseRoles.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.databaseRoles }}'
      item:
        name: '{{ item.name }}'
  # Lists Cloud Spanner databases.
  # ── gcp.spanner.projects.instances.databases.list ──
  - discovery_id: gcp.spanner.projects.instances.databases.list
    # python: svc.projects().instances().databases().list(**params).execute()
    calls:
      - action: projects.instances.databases.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.databases }}'
      item:
        createTime: '{{ item.createTime }}'
        databaseDialect: '{{ item.databaseDialect }}'
        defaultLeader: '{{ item.defaultLeader }}'
        earliestVersionTime: '{{ item.earliestVersionTime }}'
        enableDropProtection: '{{ item.enableDropProtection }}'
        encryptionConfig: '{{ item.encryptionConfig }}'
        encryptionInfo: '{{ item.encryptionInfo }}'
        name: '{{ item.name }}'
        quorumInfo: '{{ item.quorumInfo }}'
        reconciling: '{{ item.reconciling }}'
        restoreInfo: '{{ item.restoreInfo }}'
        state: '{{ item.state }}'
        versionRetentionPeriod: '{{ item.versionRetentionPeriod }}'
  # Lists instance partition long-running operations in the given instance. An instance partition operat
  # ── gcp.spanner.projects.instances.instancePartitionOperations.list ──
  - discovery_id: gcp.spanner.projects.instances.instancePartitionOperations.list
    # python: svc.projects().instances().instancePartitionOperations().list(**params).execute()
    calls:
      - action: projects.instances.instancePartitionOperations.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.instancePartitionOperations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
  # Lists all instance partitions for the given instance.
  # ── gcp.spanner.projects.instances.instancePartitions.list ──
  - discovery_id: gcp.spanner.projects.instances.instancePartitions.list
    # python: svc.projects().instances().instancePartitions().list(**params).execute()
    calls:
      - action: projects.instances.instancePartitions.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.instancePartitions }}'
      item:
        autoscalingConfig: '{{ item.autoscalingConfig }}'
        config: '{{ item.config }}'
        createTime: '{{ item.createTime }}'
        displayName: '{{ item.displayName }}'
        name: '{{ item.name }}'
        nodeCount: '{{ item.nodeCount }}'
        processingUnits: '{{ item.processingUnits }}'
        referencingBackups: '{{ item.referencingBackups }}'
        referencingDatabases: '{{ item.referencingDatabases }}'
        state: '{{ item.state }}'
        updateTime: '{{ item.updateTime }}'
  # Lists all instances in the given project.
  # ── gcp.spanner.projects.instances.list ──
  - discovery_id: gcp.spanner.projects.instances.list
    # python: svc.projects().instances().list(**params).execute()
    calls:
      - action: projects.instances.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.instances }}'
      item:
        autoscalingConfig: '{{ item.autoscalingConfig }}'
        config: '{{ item.config }}'
        createTime: '{{ item.createTime }}'
        defaultBackupScheduleType: '{{ item.defaultBackupScheduleType }}'
        displayName: '{{ item.displayName }}'
        edition: '{{ item.edition }}'
        endpointUris: '{{ item.endpointUris }}'
        freeInstanceMetadata: '{{ item.freeInstanceMetadata }}'
        instanceType: '{{ item.instanceType }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        nodeCount: '{{ item.nodeCount }}'
        processingUnits: '{{ item.processingUnits }}'
        replicaComputeCapacity: '{{ item.replicaComputeCapacity }}'
        state: '{{ item.state }}'
        updateTime: '{{ item.updateTime }}'
  # Return available scans given a Database-specific resource name.
  # ── gcp.spanner.scans.list ──
  - discovery_id: gcp.spanner.scans.list
    # python: svc.scans().list(**params).execute()
    calls:
      - action: scans.list
        params: {}
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.scans }}'
      item:
        details: '{{ item.details }}'
        endTime: '{{ item.endTime }}'
        name: '{{ item.name }}'
        scanData: '{{ item.scanData }}'
        startTime: '{{ item.startTime }}'

  # ════ DEPENDENT (enrich) operations ════
  # Gets information about a particular instance configuration.
  # ── gcp.spanner.projects.instanceConfigs.get [dependent] ──
  - discovery_id: gcp.spanner.projects.instanceConfigs.get
    for_each: gcp.spanner.projects.instanceConfigs.list
    # python: svc.projects().instanceConfigs().get(**params).execute()
    calls:
      - action: projects.instanceConfigs.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        baseConfig: '{{ response.baseConfig }}'
        configType: '{{ response.configType }}'
        displayName: '{{ response.displayName }}'
        freeInstanceAvailability: '{{ response.freeInstanceAvailability }}'
        labels: '{{ response.labels }}'
        leaderOptions: '{{ response.leaderOptions }}'
        name: '{{ response.name }}'
        optionalReplicas: '{{ response.optionalReplicas }}'
        quorumType: '{{ response.quorumType }}'
        reconciling: '{{ response.reconciling }}'
        replicas: '{{ response.replicas }}'
        state: '{{ response.state }}'
        storageLimitPerProcessingUnit: '{{ response.storageLimitPerProcessingUnit }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.spanner.projects.instanceConfigs.operations.get [dependent] ──
  - discovery_id: gcp.spanner.projects.instanceConfigs.operations.get
    for_each: gcp.spanner.projects.instanceConfigs.operations.list
    # python: svc.projects().instanceConfigs().operations().get(**params).execute()
    calls:
      - action: projects.instanceConfigs.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.spanner.projects.instanceConfigs.operations.list [dependent] ──
  - discovery_id: gcp.spanner.projects.instanceConfigs.operations.list
    for_each: gcp.spanner.projects.instanceConfigs.list
    # python: svc.projects().instanceConfigs().operations().list(**params).execute()
    calls:
      - action: projects.instanceConfigs.operations.list
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.spanner.projects.instanceConfigs.ssdCaches.operations.get [dependent] ──
  - discovery_id: gcp.spanner.projects.instanceConfigs.ssdCaches.operations.get
    for_each: gcp.spanner.projects.instanceConfigs.operations.list
    # python: svc.projects().instanceConfigs().ssdCaches().operations().get(**params).execute()
    calls:
      - action: projects.instanceConfigs.ssdCaches.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.spanner.projects.instanceConfigs.ssdCaches.operations.list [dependent] ──
  - discovery_id: gcp.spanner.projects.instanceConfigs.ssdCaches.operations.list
    for_each: gcp.spanner.projects.instanceConfigs.list
    # python: svc.projects().instanceConfigs().ssdCaches().operations().list(**params).execute()
    calls:
      - action: projects.instanceConfigs.ssdCaches.operations.list
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
    # required_params: ['name']
  # Gets metadata on a pending or completed Backup.
  # ── gcp.spanner.projects.instances.backups.get [dependent] ──
  - discovery_id: gcp.spanner.projects.instances.backups.get
    for_each: gcp.spanner.projects.instances.backups.list
    # python: svc.projects().instances().backups().get(**params).execute()
    calls:
      - action: projects.instances.backups.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        backupSchedules: '{{ response.backupSchedules }}'
        createTime: '{{ response.createTime }}'
        database: '{{ response.database }}'
        databaseDialect: '{{ response.databaseDialect }}'
        encryptionInfo: '{{ response.encryptionInfo }}'
        encryptionInformation: '{{ response.encryptionInformation }}'
        exclusiveSizeBytes: '{{ response.exclusiveSizeBytes }}'
        expireTime: '{{ response.expireTime }}'
        freeableSizeBytes: '{{ response.freeableSizeBytes }}'
        incrementalBackupChainId: '{{ response.incrementalBackupChainId }}'
        instancePartitions: '{{ response.instancePartitions }}'
        maxExpireTime: '{{ response.maxExpireTime }}'
        minimumRestorableEdition: '{{ response.minimumRestorableEdition }}'
        name: '{{ response.name }}'
        oldestVersionTime: '{{ response.oldestVersionTime }}'
        referencingBackups: '{{ response.referencingBackups }}'
        referencingDatabases: '{{ response.referencingDatabases }}'
        sizeBytes: '{{ response.sizeBytes }}'
        state: '{{ response.state }}'
        versionTime: '{{ response.versionTime }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.spanner.projects.instances.backups.operations.get [dependent] ──
  - discovery_id: gcp.spanner.projects.instances.backups.operations.get
    for_each: gcp.spanner.projects.instanceConfigs.operations.list
    # python: svc.projects().instances().backups().operations().get(**params).execute()
    calls:
      - action: projects.instances.backups.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.spanner.projects.instances.backups.operations.list [dependent] ──
  - discovery_id: gcp.spanner.projects.instances.backups.operations.list
    for_each: gcp.spanner.projects.instances.backups.list
    # python: svc.projects().instances().backups().operations().list(**params).execute()
    calls:
      - action: projects.instances.backups.operations.list
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
    # required_params: ['name']
  # Gets backup schedule for the input schedule name.
  # ── gcp.spanner.projects.instances.databases.backupSchedules.get [dependent] ──
  - discovery_id: gcp.spanner.projects.instances.databases.backupSchedules.get
    for_each: gcp.spanner.projects.instances.databases.backupSchedules.list
    # python: svc.projects().instances().databases().backupSchedules().get(**params).execute()
    calls:
      - action: projects.instances.databases.backupSchedules.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        encryptionConfig: '{{ response.encryptionConfig }}'
        fullBackupSpec: '{{ response.fullBackupSpec }}'
        incrementalBackupSpec: '{{ response.incrementalBackupSpec }}'
        name: '{{ response.name }}'
        retentionDuration: '{{ response.retentionDuration }}'
        spec: '{{ response.spec }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the state of a Cloud Spanner database.
  # ── gcp.spanner.projects.instances.databases.get [dependent] ──
  - discovery_id: gcp.spanner.projects.instances.databases.get
    for_each: gcp.spanner.projects.instances.databases.list
    # python: svc.projects().instances().databases().get(**params).execute()
    calls:
      - action: projects.instances.databases.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        createTime: '{{ response.createTime }}'
        databaseDialect: '{{ response.databaseDialect }}'
        defaultLeader: '{{ response.defaultLeader }}'
        earliestVersionTime: '{{ response.earliestVersionTime }}'
        enableDropProtection: '{{ response.enableDropProtection }}'
        encryptionConfig: '{{ response.encryptionConfig }}'
        encryptionInfo: '{{ response.encryptionInfo }}'
        name: '{{ response.name }}'
        quorumInfo: '{{ response.quorumInfo }}'
        reconciling: '{{ response.reconciling }}'
        restoreInfo: '{{ response.restoreInfo }}'
        state: '{{ response.state }}'
        versionRetentionPeriod: '{{ response.versionRetentionPeriod }}'
    # required_params: ['name']
  # Returns the schema of a Cloud Spanner database as a list of formatted DDL statements. This method do
  # ── gcp.spanner.projects.instances.databases.getDdl [dependent] ──
  - discovery_id: gcp.spanner.projects.instances.databases.getDdl
    for_each: gcp.spanner.projects.instances.databases.list
    # python: svc.projects().instances().databases().getDdl(**params).execute()
    calls:
      - action: projects.instances.databases.getDdl
        params:
          database: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        protoDescriptors: '{{ response.protoDescriptors }}'
        statements: '{{ response.statements }}'
    # required_params: ['database']
  # Request a specific scan with Database-specific data for Cloud Key Visualizer.
  # ── gcp.spanner.projects.instances.databases.getScans [dependent] ──
  - discovery_id: gcp.spanner.projects.instances.databases.getScans
    for_each: gcp.spanner.projects.instances.databases.list
    # python: svc.projects().instances().databases().getScans(**params).execute()
    calls:
      - action: projects.instances.databases.getScans
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        details: '{{ response.details }}'
        endTime: '{{ response.endTime }}'
        name: '{{ response.name }}'
        scanData: '{{ response.scanData }}'
        startTime: '{{ response.startTime }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.spanner.projects.instances.databases.operations.get [dependent] ──
  - discovery_id: gcp.spanner.projects.instances.databases.operations.get
    for_each: gcp.spanner.projects.instanceConfigs.operations.list
    # python: svc.projects().instances().databases().operations().get(**params).execute()
    calls:
      - action: projects.instances.databases.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.spanner.projects.instances.databases.operations.list [dependent] ──
  - discovery_id: gcp.spanner.projects.instances.databases.operations.list
    for_each: gcp.spanner.projects.instances.databases.list
    # python: svc.projects().instances().databases().operations().list(**params).execute()
    calls:
      - action: projects.instances.databases.operations.list
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
    # required_params: ['name']
  # Gets a session. Returns `NOT_FOUND` if the session doesn't exist. This is mainly useful for determin
  # ── gcp.spanner.projects.instances.databases.sessions.get [dependent] ──
  - discovery_id: gcp.spanner.projects.instances.databases.sessions.get
    for_each: gcp.spanner.projects.instances.databases.sessions.list
    # python: svc.projects().instances().databases().sessions().get(**params).execute()
    calls:
      - action: projects.instances.databases.sessions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        approximateLastUseTime: '{{ response.approximateLastUseTime }}'
        createTime: '{{ response.createTime }}'
        creatorRole: '{{ response.creatorRole }}'
        labels: '{{ response.labels }}'
        multiplexed: '{{ response.multiplexed }}'
        name: '{{ response.name }}'
    # required_params: ['name']
  # Lists all sessions in a given database.
  # ── gcp.spanner.projects.instances.databases.sessions.list [dependent] ──
  - discovery_id: gcp.spanner.projects.instances.databases.sessions.list
    for_each: gcp.spanner.projects.instances.databases.list
    # python: svc.projects().instances().databases().sessions().list(**params).execute()
    calls:
      - action: projects.instances.databases.sessions.list
        params:
          database: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.sessions }}'
      item:
        approximateLastUseTime: '{{ item.approximateLastUseTime }}'
        createTime: '{{ item.createTime }}'
        creatorRole: '{{ item.creatorRole }}'
        labels: '{{ item.labels }}'
        multiplexed: '{{ item.multiplexed }}'
        name: '{{ item.name }}'
    # required_params: ['database']
  # Reads rows from the database using key lookups and scans, as a simple key/value style alternative to
  # ── gcp.spanner.projects.instances.databases.sessions.read [dependent] ──
  - discovery_id: gcp.spanner.projects.instances.databases.sessions.read
    for_each: gcp.spanner.projects.instances.databases.sessions.list
    # python: svc.projects().instances().databases().sessions().read(**params).execute()
    calls:
      - action: projects.instances.databases.sessions.read
        params:
          session: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        metadata: '{{ response.metadata }}'
        precommitToken: '{{ response.precommitToken }}'
        rows: '{{ response.rows }}'
        stats: '{{ response.stats }}'
    # required_params: ['session']
  # Gets information about a particular instance.
  # ── gcp.spanner.projects.instances.get [dependent] ──
  - discovery_id: gcp.spanner.projects.instances.get
    for_each: gcp.spanner.projects.instances.list
    # python: svc.projects().instances().get(**params).execute()
    calls:
      - action: projects.instances.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        autoscalingConfig: '{{ response.autoscalingConfig }}'
        config: '{{ response.config }}'
        createTime: '{{ response.createTime }}'
        defaultBackupScheduleType: '{{ response.defaultBackupScheduleType }}'
        displayName: '{{ response.displayName }}'
        edition: '{{ response.edition }}'
        endpointUris: '{{ response.endpointUris }}'
        freeInstanceMetadata: '{{ response.freeInstanceMetadata }}'
        instanceType: '{{ response.instanceType }}'
        labels: '{{ response.labels }}'
        name: '{{ response.name }}'
        nodeCount: '{{ response.nodeCount }}'
        processingUnits: '{{ response.processingUnits }}'
        replicaComputeCapacity: '{{ response.replicaComputeCapacity }}'
        state: '{{ response.state }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets information about a particular instance partition.
  # ── gcp.spanner.projects.instances.instancePartitions.get [dependent] ──
  - discovery_id: gcp.spanner.projects.instances.instancePartitions.get
    for_each: gcp.spanner.projects.instances.instancePartitions.list
    # python: svc.projects().instances().instancePartitions().get(**params).execute()
    calls:
      - action: projects.instances.instancePartitions.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        autoscalingConfig: '{{ response.autoscalingConfig }}'
        config: '{{ response.config }}'
        createTime: '{{ response.createTime }}'
        displayName: '{{ response.displayName }}'
        name: '{{ response.name }}'
        nodeCount: '{{ response.nodeCount }}'
        processingUnits: '{{ response.processingUnits }}'
        referencingBackups: '{{ response.referencingBackups }}'
        referencingDatabases: '{{ response.referencingDatabases }}'
        state: '{{ response.state }}'
        updateTime: '{{ response.updateTime }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.spanner.projects.instances.instancePartitions.operations.get [dependent] ──
  - discovery_id: gcp.spanner.projects.instances.instancePartitions.operations.get
    for_each: gcp.spanner.projects.instanceConfigs.operations.list
    # python: svc.projects().instances().instancePartitions().operations().get(**params).execute()
    calls:
      - action: projects.instances.instancePartitions.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.spanner.projects.instances.instancePartitions.operations.list [dependent] ──
  - discovery_id: gcp.spanner.projects.instances.instancePartitions.operations.list
    for_each: gcp.spanner.projects.instances.instancePartitions.list
    # python: svc.projects().instances().instancePartitions().operations().list(**params).execute()
    calls:
      - action: projects.instances.instancePartitions.operations.list
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
    # required_params: ['name']
  # Gets the latest state of a long-running operation. Clients can use this method to poll the operation
  # ── gcp.spanner.projects.instances.operations.get [dependent] ──
  - discovery_id: gcp.spanner.projects.instances.operations.get
    for_each: gcp.spanner.projects.instanceConfigs.operations.list
    # python: svc.projects().instances().operations().get(**params).execute()
    calls:
      - action: projects.instances.operations.get
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        done: '{{ response.done }}'
        error: '{{ response.error }}'
        metadata: '{{ response.metadata }}'
        name: '{{ response.name }}'
        response: '{{ response.response }}'
    # required_params: ['name']
  # Lists operations that match the specified filter in the request. If the server doesn't support this 
  # ── gcp.spanner.projects.instances.operations.list [dependent] ──
  - discovery_id: gcp.spanner.projects.instances.operations.list
    for_each: gcp.spanner.projects.instances.list
    # python: svc.projects().instances().operations().list(**params).execute()
    calls:
      - action: projects.instances.operations.list
        params:
          name: '{{ item.name }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.operations }}'
      item:
        done: '{{ item.done }}'
        error: '{{ item.error }}'
        metadata: '{{ item.metadata }}'
        name: '{{ item.name }}'
        response: '{{ item.response }}'
    # required_params: ['name']
