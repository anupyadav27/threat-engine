id: PAT-T1-011
version: 1
deprecated_at: null
description: >
  Service account or user bound to cluster-admin role grants unrestricted
  cluster-wide privileges — equivalent to root on every node.

tier: 1
severity_base: critical
confidence: confirmed

mitre_tactics:      [TA0004]
mitre_techniques:   [T1078.001]
tactic_chain_order: [privilege_escalation]

csps: [k8s]

entry:
  node_type: Resource
  resource_types: []
  conditions:
    is_admin_role: true

hops: []

scoring:
  posture_severity: critical
  active_severity:  critical
  path_length_bonus: false
