id: PAT-T1-012
version: 1
deprecated_at: null
description: >
  Pod running with privileged: true or hostPID/hostNetwork — gives the
  container direct access to the host kernel, bypassing all namespace isolation.

tier: 1
severity_base: critical
confidence: confirmed

mitre_tactics:      [TA0004]
mitre_techniques:   [T1611]
tactic_chain_order: [privilege_escalation]

csps: [k8s]

entry:
  node_type: Resource
  resource_types: []
  conditions:
    has_privileged_container: true

hops: []

scoring:
  posture_severity: critical
  active_severity:  critical
  path_length_bonus: false
