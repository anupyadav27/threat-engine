id: PAT-T1-013
version: 1
deprecated_at: null
description: >
  Kubernetes API server endpoint reachable from the internet without IP
  allowlisting — any credential leak or kube-apiserver CVE is immediately
  exploitable.

tier: 1
severity_base: critical
confidence: confirmed

mitre_tactics:      [TA0001]
mitre_techniques:   [T1190]
tactic_chain_order: [initial_access]

csps: [k8s]

entry:
  node_type: Resource
  resource_types: []
  conditions:
    internet_exposed: true

hops: []

scoring:
  posture_severity: critical
  active_severity:  critical
  path_length_bonus: false
