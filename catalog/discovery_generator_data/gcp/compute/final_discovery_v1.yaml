# ============================================================
# Discovery YAML — compute (final_discovery v1)
# Generated: 2026-04-16T09:26:47Z
# Source: gcp_master_field_catalog.csv
# Check rules: 391 | ops in scope: 23 (23 independent, 0 dependent)
# ============================================================
version: '1.0'
provider: gcp
service: compute

services:
  client: compute
  module: "googleapiclient.discovery.build('compute', 'v1')"

# Resource identifiers — used by inventory engine for asset dedup/linking
inventory_resource_identifiers:
  - resource_type: firewalls
    identifier_op: gcp.compute.firewalls.list
    identifier_field: items[].name
    item_var_path: item.name
    identifier_template: '{project}/global/firewalls/{firewall}'
  - resource_type: forwardingRules
    identifier_op: gcp.compute.forwardingRules.aggregatedList
    identifier_field: selfLink
    item_var_path: item.selfLink
    identifier_template: '{project}/regions/{region}/forwardingRules/{forwardingRule}'
  - resource_type: imageFamilyViews
    identifier_op: gcp.compute.images.list
    identifier_field: items[].name
    item_var_path: item.name
    identifier_template: '{project}/zones/{zone}/imageFamilyViews/{family}'
  - resource_type: instanceGroups
    identifier_op: gcp.compute.instanceGroups.aggregatedList
    identifier_field: selfLink
    item_var_path: item.selfLink
    identifier_template: '{project}/zones/{zone}/instanceGroups/{instanceGroup}'
  - resource_type: instanceTemplates
    identifier_op: gcp.compute.instanceTemplates.aggregatedList
    identifier_field: selfLink
    item_var_path: item.selfLink
    identifier_template: '{project}/global/instanceTemplates/{instanceTemplate}'
  - resource_type: publicAdvertisedPrefixes
    identifier_op: gcp.compute.addresses.aggregatedList
    identifier_field: selfLink
    item_var_path: item.selfLink
    identifier_template: '{project}/global/publicAdvertisedPrefixes/{publicAdvertisedPrefix}'
  - resource_type: regionSslCertificates
    identifier_op: gcp.compute.sslCertificates.aggregatedList
    identifier_field: selfLink
    item_var_path: item.selfLink
    identifier_template: '{project}/regions/{region}/sslCertificates/{sslCertificate}'
  - resource_type: regionTargetHttpsProxies
    identifier_op: gcp.compute.targetHttpsProxies.aggregatedList
    identifier_field: selfLink
    item_var_path: item.selfLink
    identifier_template: '{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}'
  - resource_type: routes
    identifier_op: gcp.compute.routes.list
    identifier_field: items[].name
    item_var_path: item.name
    identifier_template: '{project}/global/routes/{route}'
  - resource_type: snapshots
    identifier_op: gcp.compute.snapshots.list
    identifier_field: items[].name
    item_var_path: item.name
    identifier_template: '{project}/global/snapshots/{snapshot}'
  - resource_type: subnetworks
    identifier_op: gcp.compute.subnetworks.aggregatedList
    identifier_field: selfLink
    item_var_path: item.selfLink
    identifier_template: '{project}/regions/{region}/subnetworks/{subnetwork}'
  - resource_type: vpnTunnels
    identifier_op: gcp.compute.vpnTunnels.aggregatedList
    identifier_field: selfLink
    item_var_path: item.selfLink
    identifier_template: '{project}/regions/{region}/vpnTunnels/{vpnTunnel}'
  - resource_type: unknown
    identifier_op: gcp.compute.globalAddresses.list
    identifier_field: items[].name
    item_var_path: item.name
  - resource_type: unknown
    identifier_op: gcp.compute.networks.list
    identifier_field: items[].name
    item_var_path: item.name
  - resource_type: unknown
    identifier_op: gcp.compute.projects.get
    identifier_field: id
    item_var_path: item.id
  - resource_type: unknown
    identifier_op: gcp.compute.projects.get
    identifier_field: name
    item_var_path: item.name
  - resource_type: unknown
    identifier_op: gcp.compute.projects.get
    identifier_field: selfLink
    item_var_path: item.selfLink
  - resource_type: unknown
    identifier_op: gcp.compute.autoscalers.aggregatedList
    identifier_field: selfLink
    item_var_path: item.selfLink
  - resource_type: unknown
    identifier_op: gcp.compute.backendServices.aggregatedList
    identifier_field: selfLink
    item_var_path: item.selfLink
  - resource_type: unknown
    identifier_op: gcp.compute.disks.aggregatedList
    identifier_field: selfLink
    item_var_path: item.selfLink
  - resource_type: unknown
    identifier_op: gcp.compute.healthChecks.aggregatedList
    identifier_field: selfLink
    item_var_path: item.selfLink
  - resource_type: unknown
    identifier_op: gcp.compute.instances.aggregatedList
    identifier_field: selfLink
    item_var_path: item.selfLink
  - resource_type: unknown
    identifier_op: gcp.compute.reservations.aggregatedList
    identifier_field: selfLink
    item_var_path: item.selfLink
  - resource_type: unknown
    identifier_op: gcp.compute.securityPolicies.aggregatedList
    identifier_field: selfLink
    item_var_path: item.selfLink
  - resource_type: unknown
    identifier_op: gcp.compute.urlMaps.aggregatedList
    identifier_field: selfLink
    item_var_path: item.selfLink

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # ── gcp.compute.addresses.aggregatedList ──
  - discovery_id: gcp.compute.addresses.aggregatedList
    calls:
      - action: addresses.aggregatedList
        params:
          project: '{{ project_id }}'
        save_as: response
        on_error: continue
    emit:
      as: item

  # ════ INDEPENDENT (root) operations ════
  # ── gcp.compute.autoscalers.aggregatedList ──
  - discovery_id: gcp.compute.autoscalers.aggregatedList
    calls:
      - action: autoscalers.aggregatedList
        params:
          project: '{{ project_id }}'
        save_as: response
        on_error: continue
    emit:
      as: item

  # ════ INDEPENDENT (root) operations ════
  # ── gcp.compute.backendServices.aggregatedList ──
  - discovery_id: gcp.compute.backendServices.aggregatedList
    calls:
      - action: backendServices.aggregatedList
        params:
          project: '{{ project_id }}'
        save_as: response
        on_error: continue
    emit:
      as: item

  # ════ INDEPENDENT (root) operations ════
  # ── gcp.compute.disks.aggregatedList ──
  - discovery_id: gcp.compute.disks.aggregatedList
    calls:
      - action: disks.aggregatedList
        params:
          project: '{{ project_id }}'
        save_as: response
        on_error: continue
    emit:
      as: item

  # ════ INDEPENDENT (root) operations ════
  # ── gcp.compute.firewalls.list ──
  - discovery_id: gcp.compute.firewalls.list
    calls:
      - action: firewalls.list
        params:
          project: '{{ project_id }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        allowed: '{{ item.allowed }}'
        creationTimestamp: '{{ item.creationTimestamp }}'
        denied: '{{ item.denied }}'
        description: '{{ item.description }}'
        destinationRanges: '{{ item.destinationRanges }}'
        direction: '{{ item.direction }}'
        disabled: '{{ item.disabled }}'
        id: '{{ item.id }}'
        logConfig: '{{ item.logConfig }}'
        name: '{{ item.name }}'
        network: '{{ item.network }}'
        params: '{{ item.params }}'
        priority: '{{ item.priority }}'
        selfLink: '{{ item.selfLink }}'
        sourceRanges: '{{ item.sourceRanges }}'
        sourceServiceAccounts: '{{ item.sourceServiceAccounts }}'
        sourceTags: '{{ item.sourceTags }}'
        targetServiceAccounts: '{{ item.targetServiceAccounts }}'
        targetTags: '{{ item.targetTags }}'
        warning: '{{ item.warning }}'

  # ════ INDEPENDENT (root) operations ════
  # ── gcp.compute.forwardingRules.aggregatedList ──
  - discovery_id: gcp.compute.forwardingRules.aggregatedList
    calls:
      - action: forwardingRules.aggregatedList
        params:
          project: '{{ project_id }}'
        save_as: response
        on_error: continue
    emit:
      as: item

  # ════ INDEPENDENT (root) operations ════
  # ── gcp.compute.globalAddresses.list ──
  - discovery_id: gcp.compute.globalAddresses.list
    calls:
      - action: globalAddresses.list
        params:
          project: '{{ project_id }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        address: '{{ item.address }}'
        addressType: '{{ item.addressType }}'
        creationTimestamp: '{{ item.creationTimestamp }}'
        description: '{{ item.description }}'
        id: '{{ item.id }}'
        ipCollection: '{{ item.ipCollection }}'
        ipVersion: '{{ item.ipVersion }}'
        ipv6EndpointType: '{{ item.ipv6EndpointType }}'
        labelFingerprint: '{{ item.labelFingerprint }}'
        labels: '{{ item.labels }}'
        name: '{{ item.name }}'
        network: '{{ item.network }}'
        networkTier: '{{ item.networkTier }}'
        prefixLength: '{{ item.prefixLength }}'
        purpose: '{{ item.purpose }}'
        region: '{{ item.region }}'
        selfLink: '{{ item.selfLink }}'
        status: '{{ item.status }}'
        subnetwork: '{{ item.subnetwork }}'
        users: '{{ item.users }}'
        warning: '{{ item.warning }}'

  # ════ INDEPENDENT (root) operations ════
  # ── gcp.compute.healthChecks.aggregatedList ──
  - discovery_id: gcp.compute.healthChecks.aggregatedList
    calls:
      - action: healthChecks.aggregatedList
        params:
          project: '{{ project_id }}'
        save_as: response
        on_error: continue
    emit:
      as: item

  # ════ INDEPENDENT (root) operations ════
  # ── gcp.compute.images.list ──
  - discovery_id: gcp.compute.images.list
    calls:
      - action: images.list
        params:
          project: '{{ project_id }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        architecture: '{{ item.architecture }}'
        archiveSizeBytes: '{{ item.archiveSizeBytes }}'
        creationTimestamp: '{{ item.creationTimestamp }}'
        deprecated: '{{ item.deprecated }}'
        description: '{{ item.description }}'
        diskSizeGb: '{{ item.diskSizeGb }}'
        enableConfidentialCompute: '{{ item.enableConfidentialCompute }}'
        family: '{{ item.family }}'
        guestOsFeatures: '{{ item.guestOsFeatures }}'
        id: '{{ item.id }}'
        imageEncryptionKey: '{{ item.imageEncryptionKey }}'
        labelFingerprint: '{{ item.labelFingerprint }}'
        labels: '{{ item.labels }}'
        licenseCodes: '{{ item.licenseCodes }}'
        licenses: '{{ item.licenses }}'
        name: '{{ item.name }}'
        params: '{{ item.params }}'
        rawDisk: '{{ item.rawDisk }}'
        satisfiesPzi: '{{ item.satisfiesPzi }}'
        satisfiesPzs: '{{ item.satisfiesPzs }}'
        selfLink: '{{ item.selfLink }}'
        shieldedInstanceInitialState: '{{ item.shieldedInstanceInitialState }}'
        sourceDisk: '{{ item.sourceDisk }}'
        sourceDiskEncryptionKey: '{{ item.sourceDiskEncryptionKey }}'
        sourceDiskId: '{{ item.sourceDiskId }}'
        sourceImage: '{{ item.sourceImage }}'
        sourceImageEncryptionKey: '{{ item.sourceImageEncryptionKey }}'
        sourceImageId: '{{ item.sourceImageId }}'
        sourceSnapshot: '{{ item.sourceSnapshot }}'
        sourceSnapshotEncryptionKey: '{{ item.sourceSnapshotEncryptionKey }}'
        sourceSnapshotId: '{{ item.sourceSnapshotId }}'
        sourceType: '{{ item.sourceType }}'
        status: '{{ item.status }}'
        storageLocations: '{{ item.storageLocations }}'
        warning: '{{ item.warning }}'

  # ════ INDEPENDENT (root) operations ════
  # ── gcp.compute.instanceGroups.aggregatedList ──
  - discovery_id: gcp.compute.instanceGroups.aggregatedList
    calls:
      - action: instanceGroups.aggregatedList
        params:
          project: '{{ project_id }}'
        save_as: response
        on_error: continue
    emit:
      as: item

  # ════ INDEPENDENT (root) operations ════
  # ── gcp.compute.instanceTemplates.aggregatedList ──
  - discovery_id: gcp.compute.instanceTemplates.aggregatedList
    calls:
      - action: instanceTemplates.aggregatedList
        params:
          project: '{{ project_id }}'
        save_as: response
        on_error: continue
    emit:
      as: item

  # ════ INDEPENDENT (root) operations ════
  # ── gcp.compute.instances.aggregatedList ──
  - discovery_id: gcp.compute.instances.aggregatedList
    calls:
      - action: instances.aggregatedList
        params:
          project: '{{ project_id }}'
        save_as: response
        on_error: continue
    emit:
      as: item

  # ════ INDEPENDENT (root) operations ════
  # ── gcp.compute.networks.list ──
  - discovery_id: gcp.compute.networks.list
    calls:
      - action: networks.list
        params:
          project: '{{ project_id }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        IPv4Range: '{{ item.IPv4Range }}'
        autoCreateSubnetworks: '{{ item.autoCreateSubnetworks }}'
        creationTimestamp: '{{ item.creationTimestamp }}'
        description: '{{ item.description }}'
        diskEncryptionKey: '{{ item.diskEncryptionKey }}'
        dnsPolicy: '{{ item.dnsPolicy }}'
        enableUlaInternalIpv6: '{{ item.enableUlaInternalIpv6 }}'
        firewallPolicy: '{{ item.firewallPolicy }}'
        gatewayIPv4: '{{ item.gatewayIPv4 }}'
        id: '{{ item.id }}'
        internalIpv6Range: '{{ item.internalIpv6Range }}'
        mtu: '{{ item.mtu }}'
        name: '{{ item.name }}'
        nats: '{{ item.nats }}'
        networkFirewallPolicyEnforcementOrder: '{{ item.networkFirewallPolicyEnforcementOrder }}'
        networkProfile: '{{ item.networkProfile }}'
        params: '{{ item.params }}'
        peerings: '{{ item.peerings }}'
        routingConfig: '{{ item.routingConfig }}'
        selfLink: '{{ item.selfLink }}'
        selfLinkWithId: '{{ item.selfLinkWithId }}'
        subnetworks: '{{ item.subnetworks }}'
        warning: '{{ item.warning }}'

  # ════ INDEPENDENT (root) operations ════
  # ── gcp.compute.projects.get ──
  - discovery_id: gcp.compute.projects.get
    calls:
      - action: projects.get
        params:
          project: '{{ project_id }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        cloudArmorTier: '{{ response.cloudArmorTier }}'
        commonInstanceMetadata: '{{ response.commonInstanceMetadata }}'
        creationTimestamp: '{{ response.creationTimestamp }}'
        defaultNetworkTier: '{{ response.defaultNetworkTier }}'
        defaultServiceAccount: '{{ response.defaultServiceAccount }}'
        description: '{{ response.description }}'
        enabledFeatures: '{{ response.enabledFeatures }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        quotas: '{{ response.quotas }}'
        selfLink: '{{ response.selfLink }}'
        usageExportLocation: '{{ response.usageExportLocation }}'
        vmDnsSetting: '{{ response.vmDnsSetting }}'
        xpnProjectStatus: '{{ response.xpnProjectStatus }}'

  # ════ INDEPENDENT (root) operations ════
  # ── gcp.compute.reservations.aggregatedList ──
  - discovery_id: gcp.compute.reservations.aggregatedList
    calls:
      - action: reservations.aggregatedList
        params:
          project: '{{ project_id }}'
        save_as: response
        on_error: continue
    emit:
      as: item

  # ════ INDEPENDENT (root) operations ════
  # ── gcp.compute.routes.list ──
  - discovery_id: gcp.compute.routes.list
    calls:
      - action: routes.list
        params:
          project: '{{ project_id }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        asPaths: '{{ item.asPaths }}'
        creationTimestamp: '{{ item.creationTimestamp }}'
        description: '{{ item.description }}'
        destRange: '{{ item.destRange }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        network: '{{ item.network }}'
        nextHopGateway: '{{ item.nextHopGateway }}'
        nextHopHub: '{{ item.nextHopHub }}'
        nextHopIlb: '{{ item.nextHopIlb }}'
        nextHopInstance: '{{ item.nextHopInstance }}'
        nextHopInterRegionCost: '{{ item.nextHopInterRegionCost }}'
        nextHopInterconnectAttachment: '{{ item.nextHopInterconnectAttachment }}'
        nextHopIp: '{{ item.nextHopIp }}'
        nextHopMed: '{{ item.nextHopMed }}'
        nextHopNetwork: '{{ item.nextHopNetwork }}'
        nextHopOrigin: '{{ item.nextHopOrigin }}'
        nextHopPeering: '{{ item.nextHopPeering }}'
        nextHopVpnTunnel: '{{ item.nextHopVpnTunnel }}'
        params: '{{ item.params }}'
        priority: '{{ item.priority }}'
        routeStatus: '{{ item.routeStatus }}'
        routeType: '{{ item.routeType }}'
        selfLink: '{{ item.selfLink }}'
        tags: '{{ item.tags }}'
        warning: '{{ item.warning }}'

  # ════ INDEPENDENT (root) operations ════
  # ── gcp.compute.securityPolicies.aggregatedList ──
  - discovery_id: gcp.compute.securityPolicies.aggregatedList
    calls:
      - action: securityPolicies.aggregatedList
        params:
          project: '{{ project_id }}'
        save_as: response
        on_error: continue
    emit:
      as: item

  # ════ INDEPENDENT (root) operations ════
  # ── gcp.compute.snapshots.list ──
  - discovery_id: gcp.compute.snapshots.list
    calls:
      - action: snapshots.list
        params:
          project: '{{ project_id }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        architecture: '{{ item.architecture }}'
        autoCreated: '{{ item.autoCreated }}'
        chainName: '{{ item.chainName }}'
        creationSizeBytes: '{{ item.creationSizeBytes }}'
        creationTimestamp: '{{ item.creationTimestamp }}'
        description: '{{ item.description }}'
        diskSizeGb: '{{ item.diskSizeGb }}'
        downloadBytes: '{{ item.downloadBytes }}'
        enableConfidentialCompute: '{{ item.enableConfidentialCompute }}'
        guestFlush: '{{ item.guestFlush }}'
        guestOsFeatures: '{{ item.guestOsFeatures }}'
        id: '{{ item.id }}'
        labelFingerprint: '{{ item.labelFingerprint }}'
        labels: '{{ item.labels }}'
        licenseCodes: '{{ item.licenseCodes }}'
        licenses: '{{ item.licenses }}'
        locationHint: '{{ item.locationHint }}'
        name: '{{ item.name }}'
        params: '{{ item.params }}'
        satisfiesPzi: '{{ item.satisfiesPzi }}'
        satisfiesPzs: '{{ item.satisfiesPzs }}'
        selfLink: '{{ item.selfLink }}'
        snapshotEncryptionKey: '{{ item.snapshotEncryptionKey }}'
        snapshotType: '{{ item.snapshotType }}'
        sourceDisk: '{{ item.sourceDisk }}'
        sourceDiskEncryptionKey: '{{ item.sourceDiskEncryptionKey }}'
        sourceDiskForRecoveryCheckpoint: '{{ item.sourceDiskForRecoveryCheckpoint }}'
        sourceDiskId: '{{ item.sourceDiskId }}'
        sourceInstantSnapshot: '{{ item.sourceInstantSnapshot }}'
        sourceInstantSnapshotEncryptionKey: '{{ item.sourceInstantSnapshotEncryptionKey }}'
        sourceInstantSnapshotId: '{{ item.sourceInstantSnapshotId }}'
        sourceSnapshotSchedulePolicy: '{{ item.sourceSnapshotSchedulePolicy }}'
        sourceSnapshotSchedulePolicyId: '{{ item.sourceSnapshotSchedulePolicyId }}'
        status: '{{ item.status }}'
        storageBytes: '{{ item.storageBytes }}'
        storageBytesStatus: '{{ item.storageBytesStatus }}'
        storageLocations: '{{ item.storageLocations }}'
        warning: '{{ item.warning }}'

  # ════ INDEPENDENT (root) operations ════
  # ── gcp.compute.sslCertificates.aggregatedList ──
  - discovery_id: gcp.compute.sslCertificates.aggregatedList
    calls:
      - action: sslCertificates.aggregatedList
        params:
          project: '{{ project_id }}'
        save_as: response
        on_error: continue
    emit:
      as: item

  # ════ INDEPENDENT (root) operations ════
  # ── gcp.compute.subnetworks.aggregatedList ──
  - discovery_id: gcp.compute.subnetworks.aggregatedList
    calls:
      - action: subnetworks.aggregatedList
        params:
          project: '{{ project_id }}'
        save_as: response
        on_error: continue
    emit:
      as: item

  # ════ INDEPENDENT (root) operations ════
  # ── gcp.compute.targetHttpsProxies.aggregatedList ──
  - discovery_id: gcp.compute.targetHttpsProxies.aggregatedList
    calls:
      - action: targetHttpsProxies.aggregatedList
        params:
          project: '{{ project_id }}'
        save_as: response
        on_error: continue
    emit:
      as: item

  # ════ INDEPENDENT (root) operations ════
  # ── gcp.compute.urlMaps.aggregatedList ──
  - discovery_id: gcp.compute.urlMaps.aggregatedList
    calls:
      - action: urlMaps.aggregatedList
        params:
          project: '{{ project_id }}'
        save_as: response
        on_error: continue
    emit:
      as: item

  # ════ INDEPENDENT (root) operations ════
  # ── gcp.compute.vpnTunnels.aggregatedList ──
  - discovery_id: gcp.compute.vpnTunnels.aggregatedList
    calls:
      - action: vpnTunnels.aggregatedList
        params:
          project: '{{ project_id }}'
        save_as: response
        on_error: continue
    emit:
      as: item

