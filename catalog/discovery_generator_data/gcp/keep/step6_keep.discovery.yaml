version: '1.0'
provider: gcp
service: keep
services:
  client: keep
  module: googleapiclient.discovery.build('keep', 'v1')
anchors:
  project_id: null
  org_id: null
  folder_id: null
  location: null
  zone: null
  region: null
checks: []
discovery:
- discovery_id: gcp.keep.notes.list
  calls:
  - action: notes.list
    params: {}
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.notes }}'
    item:
      attachments: '{{ item.attachments }}'
      body: '{{ item.body }}'
      createTime: '{{ item.createTime }}'
      name: '{{ item.name }}'
      permissions: '{{ item.permissions }}'
      title: '{{ item.title }}'
      trashTime: '{{ item.trashTime }}'
      trashed: '{{ item.trashed }}'
      updateTime: '{{ item.updateTime }}'
  uid_template: '{item.name}'
- discovery_id: gcp.keep.media.download
  for_each: gcp.keep.notes.list
  calls:
  - action: media.download
    params:
      name: '{{ item.name }}'
    save_as: response
    on_error: continue
  emit:
    as: item
    item:
      mimeType: '{{ response.mimeType }}'
      name: '{{ response.name }}'
- discovery_id: gcp.keep.notes.get
  for_each: gcp.keep.notes.list
  calls:
  - action: notes.get
    params:
      name: '{{ item.name }}'
    save_as: response
    on_error: continue
  emit:
    as: item
    item:
      attachments: '{{ response.attachments }}'
      body: '{{ response.body }}'
      createTime: '{{ response.createTime }}'
      name: '{{ response.name }}'
      permissions: '{{ response.permissions }}'
      title: '{{ response.title }}'
      trashTime: '{{ response.trashTime }}'
      trashed: '{{ response.trashed }}'
      updateTime: '{{ response.updateTime }}'
