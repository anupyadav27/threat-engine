# ============================================================
# Discovery YAML — dataflow (final_discovery v1)
# Generated: 2026-04-16T09:26:47Z
# Source: gcp_master_field_catalog.csv
# Check rules: 62 | ops in scope: 2 (2 independent, 0 dependent)
# ============================================================
version: '1.0'
provider: gcp
service: dataflow

services:
  client: dataflow
  module: "googleapiclient.discovery.build('dataflow', 'v1')"

# Resource identifiers — used by inventory engine for asset dedup/linking
inventory_resource_identifiers:
  - resource_type: debug
    identifier_op: gcp.dataflow.projects.locations.jobs.list
    identifier_field: jobs[].name
    item_var_path: item.jobs[].name
    identifier_template: 'projects/{projectId}/jobs/{jobId}/debug/getConfig'
  - resource_type: unknown
    identifier_op: gcp.dataflow.projects.jobs.list
    identifier_field: jobs[].name
    item_var_path: item.jobs[].name

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # ── gcp.dataflow.projects.jobs.list ──
  - discovery_id: gcp.dataflow.projects.jobs.list
    calls:
      - action: projects.jobs.list
        params:
          projectId: '{{ project_id }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        __self__: '{{ item.__self__ }}'
        clientRequestId: '{{ item.clientRequestId }}'
        createTime: '{{ item.createTime }}'
        createdFromSnapshotId: '{{ item.createdFromSnapshotId }}'
        currentState: '{{ item.currentState }}'
        currentStateTime: '{{ item.currentStateTime }}'
        environment: '{{ item.environment }}'
        executionInfo: '{{ item.executionInfo }}'
        id: '{{ item.id }}'
        jobMetadata: '{{ item.jobMetadata }}'
        labels: '{{ item.labels }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        pausable: '{{ item.pausable }}'
        pipelineDescription: '{{ item.pipelineDescription }}'
        projectId: '{{ item.projectId }}'
        replaceJobId: '{{ item.replaceJobId }}'
        replacedByJobId: '{{ item.replacedByJobId }}'
        requestedState: '{{ item.requestedState }}'
        runtimeParameters: '{{ item.runtimeParameters }}'
        runtimeUpdatableParams: '{{ item.runtimeUpdatableParams }}'
        satisfiesPzi: '{{ item.satisfiesPzi }}'
        satisfiesPzs: '{{ item.satisfiesPzs }}'
        serviceResources: '{{ item.serviceResources }}'
        stageStates: '{{ item.stageStates }}'
        startTime: '{{ item.startTime }}'
        steps: '{{ item.steps }}'
        stepsLocation: '{{ item.stepsLocation }}'
        tempFiles: '{{ item.tempFiles }}'
        transformNameMapping: '{{ item.transformNameMapping }}'
        type: '{{ item.type }}'

  # ════ INDEPENDENT (root) operations ════
  # ── gcp.dataflow.projects.locations.jobs.list ──
  - discovery_id: gcp.dataflow.projects.locations.jobs.list
    calls:
      - action: projects.locations.jobs.list
        params:
          location: '{{ location }}'
          projectId: '{{ project_id }}'
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        clientRequestId: '{{ item.clientRequestId }}'
        createTime: '{{ item.createTime }}'
        createdFromSnapshotId: '{{ item.createdFromSnapshotId }}'
        currentState: '{{ item.currentState }}'
        currentStateTime: '{{ item.currentStateTime }}'
        environment: '{{ item.environment }}'
        executionInfo: '{{ item.executionInfo }}'
        id: '{{ item.id }}'
        jobMetadata: '{{ item.jobMetadata }}'
        labels: '{{ item.labels }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        pausable: '{{ item.pausable }}'
        pipelineDescription: '{{ item.pipelineDescription }}'
        projectId: '{{ item.projectId }}'
        replaceJobId: '{{ item.replaceJobId }}'
        replacedByJobId: '{{ item.replacedByJobId }}'
        requestedState: '{{ item.requestedState }}'
        runtimeUpdatableParams: '{{ item.runtimeUpdatableParams }}'
        satisfiesPzi: '{{ item.satisfiesPzi }}'
        satisfiesPzs: '{{ item.satisfiesPzs }}'
        serviceResources: '{{ item.serviceResources }}'
        stageStates: '{{ item.stageStates }}'
        startTime: '{{ item.startTime }}'
        steps: '{{ item.steps }}'
        stepsLocation: '{{ item.stepsLocation }}'
        tempFiles: '{{ item.tempFiles }}'
        transformNameMapping: '{{ item.transformNameMapping }}'
        type: '{{ item.type }}'

  # ════ INDEPENDENT (root) operations ════
  # ── gcp.dataflow.projects.locations.pipelines.list ──
  - discovery_id: gcp.dataflow.projects.locations.pipelines.list
    calls:
      - action: projects.locations.pipelines.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        createTime: '{{ item.createTime }}'
        displayName: '{{ item.displayName }}'
        name: '{{ item.name }}'
        pipelineDefinition: '{{ item.pipelineDefinition }}'
        schedules: '{{ item.schedules }}'
        state: '{{ item.state }}'
        type: '{{ item.type }}'
        updateTime: '{{ item.updateTime }}'

