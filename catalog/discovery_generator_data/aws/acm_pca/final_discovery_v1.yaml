# ==========================================================
# Discovery YAML — acm_pca (final_discovery v1)
# Generated from: aws_field_rule_catalog.csv
# Check rules: 1 | ops: 1 independent, 0 dependent
# ==========================================================
version: '1.0'
provider: aws
service: acm_pca

services:
  client: acm_pca
  module: "boto3.client('acm_pca')"

# Resource identifiers — used by inventory engine for asset dedup/linking
inventory_resource_identifiers:
  []

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # ── aws.acm_pca.list_certificate_authorities  ──
  - discovery_id: aws.acm_pca.list_certificate_authorities
    calls:
      - action: 
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        Status: '{{ item.Status }}'

