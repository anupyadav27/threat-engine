# discoveries_only_check: 2 kept / 8 removed from 10 total
# generated_from: step6_identitystore.discovery.yaml
# purpose: only discoveries needed by identitystore.checks.yaml for_each chains
version: '1.0'
provider: aws
service: identitystore
services:
  client: identitystore
  module: boto3.client
discovery:
- discovery_id: aws.identitystore.list_groups
  calls:
  - action: list_groups
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.Groups }}'
    item:
      CreatedAt: '{{ item.CreatedAt }}'
      CreatedBy: '{{ item.CreatedBy }}'
      Description: '{{ item.Description }}'
      DisplayName: '{{ item.DisplayName }}'
      ExternalIds: '{{ item.ExternalIds }}'
      GroupId: '{{ item.GroupId }}'
      IdentityStoreId: '{{ item.IdentityStoreId }}'
      UpdatedAt: '{{ item.UpdatedAt }}'
      UpdatedBy: '{{ item.UpdatedBy }}'
- discovery_id: aws.identitystore.list_users
  calls:
  - action: list_users
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.Users }}'
    item:
      Name: '{{ item.Name }}'
      Addresses: '{{ item.Addresses }}'
      Birthdate: '{{ item.Birthdate }}'
      CreatedAt: '{{ item.CreatedAt }}'
      CreatedBy: '{{ item.CreatedBy }}'
      DisplayName: '{{ item.DisplayName }}'
      Emails: '{{ item.Emails }}'
      ExternalIds: '{{ item.ExternalIds }}'
      IdentityStoreId: '{{ item.IdentityStoreId }}'
      Locale: '{{ item.Locale }}'
      NickName: '{{ item.NickName }}'
      PhoneNumbers: '{{ item.PhoneNumbers }}'
      Photos: '{{ item.Photos }}'
      PreferredLanguage: '{{ item.PreferredLanguage }}'
      ProfileUrl: '{{ item.ProfileUrl }}'
      Timezone: '{{ item.Timezone }}'
      Title: '{{ item.Title }}'
      UpdatedAt: '{{ item.UpdatedAt }}'
      UpdatedBy: '{{ item.UpdatedBy }}'
      UserId: '{{ item.UserId }}'
      UserName: '{{ item.UserName }}'
      UserStatus: '{{ item.UserStatus }}'
      UserType: '{{ item.UserType }}'
      Website: '{{ item.Website }}'
