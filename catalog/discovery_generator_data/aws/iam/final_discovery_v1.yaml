# ==========================================================
# Discovery YAML — iam (final_discovery v1)
# Generated from: aws_field_rule_catalog.csv
# Check rules: 107 | ops: 16 independent, 10 dependent
# ==========================================================
version: '1.0'
provider: aws
service: iam

services:
  client: iam
  module: "boto3.client('iam')"

# Resource identifiers — used by inventory engine for asset dedup/linking
inventory_resource_identifiers:
  - resource_type: user
    identifier_op: aws.iam.get_account_authorization_details
    identifier_field: Arn
    item_var_path: item.Arn
    identifier_template: '{arn}'
  - resource_type: user
    identifier_op: aws.iam.get_instance_profile
    identifier_field: Arn
    item_var_path: item.Arn
    identifier_template: '{arn}'
  - resource_type: user
    identifier_op: aws.iam.get_policy
    identifier_field: Arn
    item_var_path: item.Arn
    identifier_template: '{arn}'
  - resource_type: user
    identifier_op: aws.iam.get_role
    identifier_field: Arn
    item_var_path: item.Arn
    identifier_template: '{arn}'
  - resource_type: user
    identifier_op: aws.iam.list_groups_for_user
    identifier_field: Arn
    item_var_path: item.Arn
    identifier_template: '{arn}'
  - resource_type: user
    identifier_op: aws.iam.list_instance_profiles_for_role
    identifier_field: Arn
    item_var_path: item.Arn
    identifier_template: '{arn}'
  - resource_type: user
    identifier_op: aws.iam.list_open_id_connect_providers
    identifier_field: Arn
    item_var_path: item.Arn
    identifier_template: '{arn}'
  - resource_type: user
    identifier_op: aws.iam.list_policies
    identifier_field: Arn
    item_var_path: item.Arn
    identifier_template: '{arn}'
  - resource_type: user
    identifier_op: aws.iam.list_roles
    identifier_field: Arn
    item_var_path: item.Arn
    identifier_template: '{arn}'
  - resource_type: user
    identifier_op: aws.iam.list_saml_providers
    identifier_field: Arn
    item_var_path: item.Arn
    identifier_template: '{arn}'
  - resource_type: user
    identifier_op: aws.iam.list_server_certificates
    identifier_field: Arn
    item_var_path: item.Arn
    identifier_template: '{arn}'
  - resource_type: user
    identifier_op: aws.iam.list_users
    identifier_field: Arn
    item_var_path: item.Arn
    identifier_template: '{arn}'

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # ── aws.iam.get_account_authorization_details  ──
  - discovery_id: aws.iam.get_account_authorization_details
    calls:
      - action: get_account_authorization_details
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.UserDetailList }}'
      item:
        Arn: '{{ item.Arn }}'
        AttachedManagedPolicies: '{{ item.AttachedManagedPolicies }}'
        AttachedManagedPolicies.PolicyName: '{{ item.AttachedManagedPolicies.PolicyName }}'
        CreateDate: '{{ item.CreateDate }}'
        Fingerprint: '{{ item.Fingerprint }}'
        Group: '{{ item.Group }}'
        Group.GroupName: '{{ item.Group.GroupName }}'
        GroupDetailList: '{{ item.GroupDetailList }}'
        GroupList: '{{ item.GroupList }}'
        Path: '{{ item.Path }}'
        PermissionsBoundary: '{{ item.PermissionsBoundary }}'
        RoleDetailList: '{{ item.RoleDetailList }}'
        SSHPublicKey: '{{ item.SSHPublicKey }}'
        Statement: '{{ item.Statement }}'
        Tags: '{{ item.Tags }}'
        UserDetailList: '{{ item.UserDetailList }}'
        UserId: '{{ item.UserId }}'
        UserName: '{{ item.UserName }}'
        UserPolicyList: '{{ item.UserPolicyList }}'
        Version: '{{ item.Version }}'

  # ════ INDEPENDENT (root) operations ════
  # ── aws.iam.get_account_authorization_details_policies  ──
  - discovery_id: aws.iam.get_account_authorization_details_policies
    calls:
      - action: get_account_authorization_details
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Policies }}'
      item:
        Arn: '{{ item.Arn }}'
        AttachmentCount: '{{ item.AttachmentCount }}'
        CreateDate: '{{ item.CreateDate }}'
        IsAttachable: '{{ item.IsAttachable }}'
        Path: '{{ item.Path }}'
        PolicyId: '{{ item.PolicyId }}'
        PolicyName: '{{ item.PolicyName }}'
        PolicyVersionList: '{{ item.PolicyVersionList }}'
        UpdateDate: '{{ item.UpdateDate }}'

  # ════ INDEPENDENT (root) operations ════
  # ── aws.iam.get_account_password_policy  ──
  - discovery_id: aws.iam.get_account_password_policy
    calls:
      - action: get_account_password_policy
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        AllowUsersToChangePassword: '{{ item.AllowUsersToChangePassword }}'
        ExpirePasswords: '{{ item.ExpirePasswords }}'
        HardExpiry: '{{ item.HardExpiry }}'
        MaxPasswordAge: '{{ item.MaxPasswordAge }}'
        MinimumPasswordLength: '{{ item.MinimumPasswordLength }}'
        PasswordPolicy: '{{ item.PasswordPolicy }}'
        PasswordPolicy.ExpirePasswords: '{{ item.PasswordPolicy.ExpirePasswords }}'
        PasswordPolicy.MinimumPasswordLength: '{{ item.PasswordPolicy.MinimumPasswordLength }}'
        PasswordPolicy.PasswordReusePrevention: '{{ item.PasswordPolicy.PasswordReusePrevention }}'
        PasswordReusePrevention: '{{ item.PasswordReusePrevention }}'
        RequireLowercaseCharacters: '{{ item.RequireLowercaseCharacters }}'
        RequireNumbers: '{{ item.RequireNumbers }}'
        RequireSymbols: '{{ item.RequireSymbols }}'
        RequireUppercaseCharacters: '{{ item.RequireUppercaseCharacters }}'

  # ════ INDEPENDENT (root) operations ════
  # ── aws.iam.get_account_summary  ──
  - discovery_id: aws.iam.get_account_summary
    calls:
      - action: get_account_summary
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        SummaryMap: '{{ item.SummaryMap }}'
        SummaryMap.AccountAccessKeysPresent: '{{ item.SummaryMap.AccountAccessKeysPresent }}'
        SummaryMap.AccountMFAEnabled: '{{ item.SummaryMap.AccountMFAEnabled }}'

  # ════ INDEPENDENT (root) operations ════
  # ── aws.iam.get_policy  ──
  - discovery_id: aws.iam.get_policy
    calls:
      - action: get_policy
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        Arn: '{{ item.Arn }}'
        AttachmentCount: '{{ item.AttachmentCount }}'
        CreateDate: '{{ item.CreateDate }}'
        DefaultVersionId: '{{ item.DefaultVersionId }}'
        Description: '{{ item.Description }}'
        IsAttachable: '{{ item.IsAttachable }}'
        Path: '{{ item.Path }}'
        PermissionsBoundaryUsageCount: '{{ item.PermissionsBoundaryUsageCount }}'
        Policy: '{{ item.Policy }}'
        PolicyId: '{{ item.PolicyId }}'
        PolicyName: '{{ item.PolicyName }}'
        Tags: '{{ item.Tags }}'
        UpdateDate: '{{ item.UpdateDate }}'

  # ════ INDEPENDENT (root) operations ════
  # ── aws.iam.get_saml_provider  ──
  - discovery_id: aws.iam.get_saml_provider
    calls:
      - action: get_saml_provider
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        CreateDate: '{{ item.CreateDate }}'
        SAMLMetadataDocument: '{{ item.SAMLMetadataDocument }}'
        Tags: '{{ item.Tags }}'
        ValidUntil: '{{ item.ValidUntil }}'

  # ════ INDEPENDENT (root) operations ════
  # ── aws.iam.list_access_keys  ──
  - discovery_id: aws.iam.list_access_keys
    calls:
      - action: list_access_keys
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.AccessKeyMetadata }}'
      item:
        AccessKeyId: '{{ item.AccessKeyId }}'
        AccessKeyMetadata: '{{ item.AccessKeyMetadata }}'
        CreateDate: '{{ item.CreateDate }}'
        Status: '{{ item.Status }}'
        UserName: '{{ item.UserName }}'

  # ════ INDEPENDENT (root) operations ════
  # ── aws.iam.list_mfa_devices  ──
  - discovery_id: aws.iam.list_mfa_devices
    calls:
      - action: list_mfa_devices
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.MFADevices }}'
      item:
        Certifications: '{{ item.Certifications }}'
        EnableDate: '{{ item.EnableDate }}'
        MFADevices: '{{ item.MFADevices }}'
        SerialNumber: '{{ item.SerialNumber }}'
        UserName: '{{ item.UserName }}'

  # ════ INDEPENDENT (root) operations ════
  # ── aws.iam.list_open_id_connect_providers  ──
  - discovery_id: aws.iam.list_open_id_connect_providers
    calls:
      - action: list_open_id_connect_providers
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.OpenIDConnectProviderList }}'
      item:
        Arn: '{{ item.Arn }}'
        OpenIDConnectProviderArn: '{{ item.OpenIDConnectProviderArn }}'
        Tags: '{{ item.Tags }}'
        Url: '{{ item.Url }}'

  # ════ INDEPENDENT (root) operations ════
  # ── aws.iam.list_policies  ──
  - discovery_id: aws.iam.list_policies
    calls:
      - action: list_policies
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Policies }}'
      item:
        Arn: '{{ item.Arn }}'
        AttachmentCount: '{{ item.AttachmentCount }}'
        CreateDate: '{{ item.CreateDate }}'
        DefaultVersionId: '{{ item.DefaultVersionId }}'
        Description: '{{ item.Description }}'
        Document: '{{ item.Document }}'
        IsAttachable: '{{ item.IsAttachable }}'
        IsDefaultVersion: '{{ item.IsDefaultVersion }}'
        Path: '{{ item.Path }}'
        PermissionsBoundaryUsageCount: '{{ item.PermissionsBoundaryUsageCount }}'
        Policy: '{{ item.Policy }}'
        PolicyId: '{{ item.PolicyId }}'
        PolicyName: '{{ item.PolicyName }}'
        PolicyVersion: '{{ item.PolicyVersion }}'
        Tags: '{{ item.Tags }}'
        UpdateDate: '{{ item.UpdateDate }}'

  # ════ INDEPENDENT (root) operations ════
  # ── aws.iam.list_policy_versions  ──
  - discovery_id: aws.iam.list_policy_versions
    calls:
      - action: list_policy_versions
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Versions }}'
      item:
        CreateDate: '{{ item.CreateDate }}'
        Document: '{{ item.Document }}'
        IsDefaultVersion: '{{ item.IsDefaultVersion }}'
        VersionId: '{{ item.VersionId }}'

  # ════ INDEPENDENT (root) operations ════
  # ── aws.iam.list_roles  ──
  - discovery_id: aws.iam.list_roles
    calls:
      - action: list_roles
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Roles }}'
      item:
        Arn: '{{ item.Arn }}'
        AssumeRolePolicyDocument: '{{ item.AssumeRolePolicyDocument }}'
        CreateDate: '{{ item.CreateDate }}'
        Description: '{{ item.Description }}'
        MaxSessionDuration: '{{ item.MaxSessionDuration }}'
        Path: '{{ item.Path }}'
        PermissionsBoundary: '{{ item.PermissionsBoundary }}'
        Role: '{{ item.Role }}'
        RoleId: '{{ item.RoleId }}'
        RoleLastUsed: '{{ item.RoleLastUsed }}'
        RoleName: '{{ item.RoleName }}'
        Tags: '{{ item.Tags }}'

  # ════ INDEPENDENT (root) operations ════
  # ── aws.iam.list_saml_providers  ──
  - discovery_id: aws.iam.list_saml_providers
    calls:
      - action: list_saml_providers
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.SAMLProviderList }}'
      item:
        Arn: '{{ item.Arn }}'
        CreateDate: '{{ item.CreateDate }}'
        PrivateKeyList: '{{ item.PrivateKeyList }}'
        Tags: '{{ item.Tags }}'
        ValidUntil: '{{ item.ValidUntil }}'

  # ════ INDEPENDENT (root) operations ════
  # ── aws.iam.list_server_certificates  ──
  - discovery_id: aws.iam.list_server_certificates
    calls:
      - action: list_server_certificates
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.ServerCertificateMetadataList }}'
      item:
        Arn: '{{ item.Arn }}'
        Expiration: '{{ item.Expiration }}'
        Path: '{{ item.Path }}'
        ServerCertificateId: '{{ item.ServerCertificateId }}'
        ServerCertificateName: '{{ item.ServerCertificateName }}'
        UploadDate: '{{ item.UploadDate }}'

  # ════ INDEPENDENT (root) operations ════
  # ── aws.iam.list_signing_certificates  ──
  - discovery_id: aws.iam.list_signing_certificates
    calls:
      - action: list_signing_certificates
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Certificates }}'
      item:
        CertificateBody: '{{ item.CertificateBody }}'
        CertificateId: '{{ item.CertificateId }}'
        Status: '{{ item.Status }}'
        UploadDate: '{{ item.UploadDate }}'
        UserName: '{{ item.UserName }}'

  # ════ INDEPENDENT (root) operations ════
  # ── aws.iam.list_users  ──
  - discovery_id: aws.iam.list_users
    calls:
      - action: list_users
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Users }}'
      item:
        Arn: '{{ item.Arn }}'
        CreateDate: '{{ item.CreateDate }}'
        PasswordLastUsed: '{{ item.PasswordLastUsed }}'
        Path: '{{ item.Path }}'
        PermissionsBoundary: '{{ item.PermissionsBoundary }}'
        Tags: '{{ item.Tags }}'
        UserId: '{{ item.UserId }}'
        UserName: '{{ item.UserName }}'
        Users: '{{ item.Users }}'
        item: '{{ item }}'

  # ════ DEPENDENT (enrich) operations ════
  # ── aws.iam.get_group_policy [dependent] ──
  - discovery_id: aws.iam.get_group_policy
    for_each: aws.iam.list_groups
    calls:
      - action: get_group_policy
        params:
          GroupName: "{{ item.GroupName }}"
          PolicyName: "{{ item.PolicyName }}"
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        GroupName: '{{ item.GroupName }}'
        PolicyDocument: '{{ item.PolicyDocument }}'
        PolicyName: '{{ item.PolicyName }}'

  # ════ DEPENDENT (enrich) operations ════
  # ── aws.iam.get_instance_profile [dependent] ──
  - discovery_id: aws.iam.get_instance_profile
    for_each: aws.iam.list_instance_profiles
    calls:
      - action: get_instance_profile
        params:
          InstanceProfileName: "{{ item.InstanceProfileName }}"
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        Arn: '{{ item.Arn }}'
        CreateDate: '{{ item.CreateDate }}'
        InstanceProfileId: '{{ item.InstanceProfileId }}'
        InstanceProfileName: '{{ item.InstanceProfileName }}'
        Path: '{{ item.Path }}'
        Roles: '{{ item.Roles }}'
        Roles.AssumeRolePolicyDocument.Statement.Effect: '{{ item.Roles.AssumeRolePolicyDocument.Statement.Effect }}'
        Tags: '{{ item.Tags }}'

  # ════ DEPENDENT (enrich) operations ════
  # ── aws.iam.get_open_id_connect_provider [dependent] ──
  - discovery_id: aws.iam.get_open_id_connect_provider
    for_each: aws.iam.list_open_id_connect_providers
    calls:
      - action: get_open_id_connect_provider
        params:
          OpenIDConnectProviderArn: "{{ item.Arn }}"
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        ClientIDList: '{{ item.ClientIDList }}'
        CreateDate: '{{ item.CreateDate }}'
        Tags: '{{ item.Tags }}'
        ThumbprintList: '{{ item.ThumbprintList }}'
        Url: '{{ item.Url }}'

  # ════ DEPENDENT (enrich) operations ════
  # ── aws.iam.get_policy_version [dependent] ──
  - discovery_id: aws.iam.get_policy_version
    for_each: aws.iam.list_policies
    calls:
      - action: get_policy_version
        params:
          PolicyArn: "{{ item.Arn }}"
          VersionId: "{{ item.DefaultVersionId }}"
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        CreateDate: '{{ item.CreateDate }}'
        Document: '{{ item.Document }}'
        IsDefaultVersion: '{{ item.IsDefaultVersion }}'
        PolicyArn: '{{ item.PolicyArn }}'
        PolicyDocument: '{{ item.PolicyDocument }}'
        PolicyDocument.Statement.Action: '{{ item.PolicyDocument.Statement.Action }}'
        PolicyDocument.Statement.Resource: '{{ item.PolicyDocument.Statement.Resource }}'
        PolicyName: '{{ item.PolicyName }}'
        VersionId: '{{ item.VersionId }}'

  # ════ DEPENDENT (enrich) operations ════
  # ── aws.iam.get_role [dependent] ──
  - discovery_id: aws.iam.get_role
    for_each: aws.iam.list_roles
    calls:
      - action: get_role
        params:
          RoleName: "{{ item.RoleName }}"
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        Action: '{{ item.Action }}'
        Arn: '{{ item.Arn }}'
        AssumeRolePolicyDocument: '{{ item.AssumeRolePolicyDocument }}'
        Condition: '{{ item.Condition }}'
        Condition.StringEquals.sts:ExternalId: '{{ item.Condition.StringEquals.sts:ExternalId }}'
        CreateDate: '{{ item.CreateDate }}'
        Description: '{{ item.Description }}'
        Effect: '{{ item.Effect }}'
        MaxSessionDuration: '{{ item.MaxSessionDuration }}'
        Path: '{{ item.Path }}'
        PermissionsBoundary: '{{ item.PermissionsBoundary }}'
        Principal: '{{ item.Principal }}'
        Principal.AWS: '{{ item.Principal.AWS }}'
        Principal.Federated: '{{ item.Principal.Federated }}'
        RoleId: '{{ item.RoleId }}'
        RoleLastUsed: '{{ item.RoleLastUsed }}'
        RoleName: '{{ item.RoleName }}'
        Tags: '{{ item.Tags }}'

  # ════ DEPENDENT (enrich) operations ════
  # ── aws.iam.list_group_policies [dependent] ──
  - discovery_id: aws.iam.list_group_policies
    for_each: aws.iam.list_groups
    calls:
      - action: list_group_policies
        params:
          GroupName: "{{ item.GroupName }}"
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.PolicyNames }}'
      item:
        GroupName: '{{ item.GroupName }}'
        PolicyName: '{{ item.PolicyName }}'
        PolicyNames: '{{ item.PolicyNames }}'

  # ════ DEPENDENT (enrich) operations ════
  # ── aws.iam.list_groups_for_user [dependent] ──
  - discovery_id: aws.iam.list_groups_for_user
    for_each: aws.iam.list_users
    calls:
      - action: list_groups_for_user
        params:
          UserName: "{{ item.UserName }}"
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Groups }}'
      item:
        Arn: '{{ item.Arn }}'
        CreateDate: '{{ item.CreateDate }}'
        GroupId: '{{ item.GroupId }}'
        GroupName: '{{ item.GroupName }}'
        Groups: '{{ item.Groups }}'
        Path: '{{ item.Path }}'

  # ════ DEPENDENT (enrich) operations ════
  # ── aws.iam.list_instance_profiles_for_role [dependent] ──
  - discovery_id: aws.iam.list_instance_profiles_for_role
    for_each: aws.iam.list_roles
    calls:
      - action: list_instance_profiles_for_role
        params:
          RoleName: "{{ item.RoleName }}"
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.InstanceProfiles }}'
      item:
        Arn: '{{ item.Arn }}'
        CreateDate: '{{ item.CreateDate }}'
        InstanceProfileId: '{{ item.InstanceProfileId }}'
        InstanceProfileName: '{{ item.InstanceProfileName }}'
        Path: '{{ item.Path }}'
        Roles: '{{ item.Roles }}'
        Tags: '{{ item.Tags }}'

  # ════ DEPENDENT (enrich) operations ════
  # ── aws.iam.list_user_policies [dependent] ──
  - discovery_id: aws.iam.list_user_policies
    for_each: aws.iam.list_users
    calls:
      - action: list_user_policies
        params:
          UserName: "{{ item.UserName }}"
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.PolicyNames }}'
      item:
        PolicyName: '{{ item.PolicyName }}'
        PolicyNames: '{{ item.PolicyNames }}'
        UserName: '{{ item.UserName }}'

  # ════ DEPENDENT (enrich) operations ════
  # ── aws.iam.list_attached_user_policies [dependent] ──
  - discovery_id: aws.iam.list_attached_user_policies
    for_each: aws.iam.list_users
    calls:
      - action: list_attached_user_policies
        params:
          UserName: "{{ item.UserName }}"
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.AttachedPolicies }}'
      item:
        PolicyArn: '{{ item.PolicyArn }}'
        PolicyName: '{{ item.PolicyName }}'

