# discoveries_only_check: 1 kept / 14 removed from 15 total
# generated_from: step6_kinesisvideostreams.discovery.yaml
# purpose: only discoveries needed by kinesisvideostreams.checks.yaml for_each chains
version: '1.0'
provider: aws
service: kinesisvideostreams
services:
  client: kinesisvideostreams
  module: boto3.client
discovery:
- discovery_id: aws.kinesisvideostreams.describe_stream
  calls:
  - action: describe_stream
    save_as: response
    on_error: continue
  emit:
    as: item
    item:
      CreationTime: '{{ response.CreationTime }}'
      KmsKeyId: '{{ response.KmsKeyId }}'
      Status: '{{ response.Status }}'
      DataRetentionInHours: '{{ response.DataRetentionInHours }}'
      DeviceName: '{{ response.DeviceName }}'
      MediaType: '{{ response.MediaType }}'
      StreamARN: '{{ response.StreamARN }}'
      StreamName: '{{ response.StreamName }}'
      Version: '{{ response.Version }}'
