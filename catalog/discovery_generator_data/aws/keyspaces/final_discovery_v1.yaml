# ==========================================================
# Discovery YAML — keyspaces (final_discovery v1)
# Generated from: aws_field_rule_catalog.csv
# Check rules: 2 | ops: 1 independent, 0 dependent
# ==========================================================
version: '1.0'
provider: aws
service: keyspaces

services:
  client: keyspaces
  module: "boto3.client('keyspaces')"

# Resource identifiers — used by inventory engine for asset dedup/linking
inventory_resource_identifiers:
  []

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # ── aws.keyspaces.list_keyspaces  ──
  - discovery_id: aws.keyspaces.list_keyspaces
    calls:
      - action: list_keyspaces
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.keyspaces }}'
      item:
        EncryptionSpecification: '{{ item.EncryptionSpecification.EncryptionType }}'
        EncryptionSpecification.EncryptionType: '{{ item.EncryptionSpecification.EncryptionType }}'

