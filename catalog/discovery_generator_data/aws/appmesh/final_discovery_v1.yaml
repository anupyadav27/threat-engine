# ==========================================================
# Discovery YAML — appmesh (final_discovery v1)
# Generated from: aws_field_rule_catalog.csv
# Check rules: 19 | ops: 0 independent, 6 dependent
# ==========================================================
version: '1.0'
provider: aws
service: appmesh

services:
  client: appmesh
  module: "boto3.client('appmesh')"

# Resource identifiers — used by inventory engine for asset dedup/linking
inventory_resource_identifiers:
  []

checks: []

discovery:

  # ════ DEPENDENT (enrich) operations ════
  # ── aws.appmesh.describe_gateway_route [dependent] ──
  - discovery_id: aws.appmesh.describe_gateway_route
    for_each: aws.appmesh.list_meshes
    calls:
      - action: describe_gateway_route
        params:
          gatewayRouteName: "{{ item.gatewayRouteName }}"
          meshName: "{{ item.meshName }}"
          virtualGatewayName: "{{ item.virtualGatewayName }}"
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        Tags: '{{ item.Tags }}'
        gatewayRoute: '{{ item.gatewayRoute }}'
        gatewayRouteName: '{{ item.gatewayRouteName }}'
        meshName: '{{ item.meshName }}'
        metadata: '{{ item.metadata }}'
        spec: '{{ item.spec }}'
        status: '{{ item.status }}'
        virtualGatewayName: '{{ item.virtualGatewayName }}'

  # ════ DEPENDENT (enrich) operations ════
  # ── aws.appmesh.describe_mesh [dependent] ──
  - discovery_id: aws.appmesh.describe_mesh
    for_each: aws.appmesh.list_meshes
    calls:
      - action: describe_mesh
        params:
          meshName: "{{ item.meshName }}"
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        Spec.EgressFilter.Type: '{{ item.Spec.EgressFilter.Type }}'
        Tags: '{{ item.Tags }}'
        item: '{{ item }}'
        meshName: '{{ item.meshName }}'
        metadata: '{{ item.metadata }}'
        spec: '{{ item.spec }}'
        status: '{{ item.status }}'

  # ════ DEPENDENT (enrich) operations ════
  # ── aws.appmesh.describe_virtual_gateway [dependent] ──
  - discovery_id: aws.appmesh.describe_virtual_gateway
    for_each: aws.appmesh.list_meshes
    calls:
      - action: describe_virtual_gateway
        params:
          meshName: "{{ item.meshName }}"
          virtualGatewayName: "{{ item.virtualGatewayName }}"
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        Spec.BackendDefaults.ClientPolicy.Tls.Enforce: '{{ item.Spec.BackendDefaults.ClientPolicy.Tls.Enforce }}'
        Spec.Listeners[].HealthCheck: '{{ item.Spec.Listeners[].HealthCheck }}'
        Spec.Logging.AccessLog.File.Path: '{{ item.Spec.Logging.AccessLog.File.Path }}'
        Tags: '{{ item.Tags }}'
        meshName: '{{ item.meshName }}'
        metadata: '{{ item.metadata }}'
        spec: '{{ item.spec }}'
        status: '{{ item.status }}'
        virtualGateway: '{{ item.virtualGateway }}'
        virtualGatewayName: '{{ item.virtualGatewayName }}'

  # ════ DEPENDENT (enrich) operations ════
  # ── aws.appmesh.describe_virtual_node [dependent] ──
  - discovery_id: aws.appmesh.describe_virtual_node
    for_each: aws.appmesh.list_meshes
    calls:
      - action: describe_virtual_node
        params:
          meshName: "{{ item.meshName }}"
          virtualNodeName: "{{ item.virtualNodeName }}"
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        Spec.BackendDefaults.ClientPolicy.Tls.Enforce: '{{ item.Spec.BackendDefaults.ClientPolicy.Tls.Enforce }}'
        Spec.Backends[].ClientPolicy.Tls.Enforce: '{{ item.Spec.Backends[].ClientPolicy.Tls.Enforce }}'
        Spec.Listeners[].HealthCheck: '{{ item.Spec.Listeners[].HealthCheck }}'
        Spec.Listeners[].OutlierDetection: '{{ item.Spec.Listeners[].OutlierDetection }}'
        Spec.Logging.AccessLog.File.Path: '{{ item.Spec.Logging.AccessLog.File.Path }}'
        Tags: '{{ item.Tags }}'
        item: '{{ item }}'
        meshName: '{{ item.meshName }}'
        metadata: '{{ item.metadata }}'
        spec: '{{ item.spec }}'
        status: '{{ item.status }}'
        virtualNode: '{{ item.virtualNode }}'
        virtualNodeName: '{{ item.virtualNodeName }}'

  # ════ DEPENDENT (enrich) operations ════
  # ── aws.appmesh.describe_virtual_router [dependent] ──
  - discovery_id: aws.appmesh.describe_virtual_router
    for_each: aws.appmesh.list_meshes
    calls:
      - action: describe_virtual_router
        params:
          meshName: "{{ item.meshName }}"
          virtualRouterName: "{{ item.virtualRouterName }}"
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        Tags: '{{ item.Tags }}'
        meshName: '{{ item.meshName }}'
        metadata: '{{ item.metadata }}'
        spec: '{{ item.spec }}'
        status: '{{ item.status }}'
        virtualRouter: '{{ item.virtualRouter }}'
        virtualRouterName: '{{ item.virtualRouterName }}'

  # ════ DEPENDENT (enrich) operations ════
  # ── aws.appmesh.describe_virtual_service [dependent] ──
  - discovery_id: aws.appmesh.describe_virtual_service
    for_each: aws.appmesh.list_meshes
    calls:
      - action: describe_virtual_service
        params:
          meshName: "{{ item.meshName }}"
          virtualServiceName: "{{ item.virtualServiceName }}"
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        Tags: '{{ item.Tags }}'
        meshName: '{{ item.meshName }}'
        metadata: '{{ item.metadata }}'
        spec: '{{ item.spec }}'
        status: '{{ item.status }}'
        virtualService: '{{ item.virtualService }}'
        virtualServiceName: '{{ item.virtualServiceName }}'

