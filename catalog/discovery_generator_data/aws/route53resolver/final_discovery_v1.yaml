# ==========================================================
# Discovery YAML — route53resolver (final_discovery v1)
# Generated from: aws_field_rule_catalog.csv
# Check rules: 5 | ops: 2 independent, 0 dependent
# ==========================================================
version: '1.0'
provider: aws
service: route53resolver

services:
  client: route53resolver
  module: "boto3.client('route53resolver')"

# Resource identifiers — used by inventory engine for asset dedup/linking
inventory_resource_identifiers:
  []

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # ── aws.route53resolver.list_firewall_domain_lists  ──
  - discovery_id: aws.route53resolver.list_firewall_domain_lists
    calls:
      - action: 
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        Tags: '{{ item.Tags }}'

  # ════ INDEPENDENT (root) operations ════
  # ── aws.route53resolver.list_resolver_endpoints  ──
  - discovery_id: aws.route53resolver.list_resolver_endpoints
    calls:
      - action: 
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        Tags: '{{ item.Tags }}'

