# discoveries_only_check: 1 kept / 21 removed from 22 total
# generated_from: step6_eventbridge.discovery.yaml
# purpose: only discoveries needed by eventbridge.checks.yaml for_each chains
# cross-service refs (in other step6 files): aws.events.describe_rule, aws.events.list_api_destinations, aws.events.list_endpoints, aws.events.list_event_buses, aws.events.list_rules ... (+1 more)
version: '1.0'
provider: aws
service: eventbridge
services:
  client: eventbridge
  module: boto3.client
discovery:
- discovery_id: aws.eventbridge.describe_event_bus
  calls:
  - action: describe_event_bus
    save_as: response
    on_error: continue
  emit:
    as: item
    item:
      Arn: '{{ response.Arn }}'
      KmsKeyIdentifier: '{{ response.KmsKeyIdentifier }}'
      LogConfig: '{{ response.LogConfig }}'
      Statement: '{{ response.Statement }}'
      Version: '{{ response.Version }}'
