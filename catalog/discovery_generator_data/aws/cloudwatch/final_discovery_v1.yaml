# ==========================================================
# Discovery YAML — cloudwatch (final_discovery v1)
# Generated from: aws_field_rule_catalog.csv
# Check rules: 87 | ops: 7 independent, 1 dependent
# ==========================================================
version: '1.0'
provider: aws
service: cloudwatch

services:
  client: cloudwatch
  module: "boto3.client('cloudwatch')"

# Resource identifiers — used by inventory engine for asset dedup/linking
inventory_resource_identifiers:
  - resource_type: alarm
    identifier_op: aws.cloudwatch.describe_alarms
    identifier_field: AlarmArn
    item_var_path: item.AlarmArn
    identifier_template: '{alarmarn}'
  - resource_type: dashboard
    identifier_op: aws.cloudwatch.list_dashboards
    identifier_field: DashboardArn
    item_var_path: item.DashboardArn
    identifier_template: '{dashboardarn}'
  - resource_type: entry
    identifier_op: aws.cloudwatch.list_metric_streams
    identifier_field: Arn
    item_var_path: item.Arn
    identifier_template: '{arn}'

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # ── aws.cloudwatch.describe_alarm_history  ──
  - discovery_id: aws.cloudwatch.describe_alarm_history
    calls:
      - action: describe_alarm_history
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.AlarmHistoryItems }}'
      item:
        AlarmContributorAttributes: '{{ item.AlarmContributorAttributes }}'
        AlarmContributorId: '{{ item.AlarmContributorId }}'
        AlarmContributors: '{{ item.AlarmContributors }}'
        AlarmHistoryItems: '{{ item.AlarmHistoryItems }}'
        AlarmName: '{{ item.AlarmName }}'
        AlarmType: '{{ item.AlarmType }}'
        ContributorAttributes: '{{ item.ContributorAttributes }}'
        ContributorId: '{{ item.ContributorId }}'
        Contributors: '{{ item.Contributors }}'
        HistoryData: '{{ item.HistoryData }}'
        HistoryItemType: '{{ item.HistoryItemType }}'
        HistorySummary: '{{ item.HistorySummary }}'
        Timestamp: '{{ item.Timestamp }}'

  # ════ INDEPENDENT (root) operations ════
  # ── aws.cloudwatch.describe_alarms  ──
  - discovery_id: aws.cloudwatch.describe_alarms
    calls:
      - action: describe_alarms
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.CompositeAlarms }}'
      item:
        ActionsEnabled: '{{ item.ActionsEnabled }}'
        ActionsSuppressedBy: '{{ item.ActionsSuppressedBy }}'
        ActionsSuppressedReason: '{{ item.ActionsSuppressedReason }}'
        ActionsSuppressor: '{{ item.ActionsSuppressor }}'
        ActionsSuppressorExtensionPeriod: '{{ item.ActionsSuppressorExtensionPeriod }}'
        ActionsSuppressorWaitPeriod: '{{ item.ActionsSuppressorWaitPeriod }}'
        AlarmActions: '{{ item.AlarmActions }}'
        AlarmArn: '{{ item.AlarmArn }}'
        AlarmConfigurationUpdatedTimestamp: '{{ item.AlarmConfigurationUpdatedTimestamp }}'
        AlarmDescription: '{{ item.AlarmDescription }}'
        AlarmName: '{{ item.AlarmName }}'
        AlarmRule: '{{ item.AlarmRule }}'
        CompositeAlarms: '{{ item.CompositeAlarms }}'
        Enabled: '{{ item.Enabled }}'
        InsufficientDataActions: '{{ item.InsufficientDataActions }}'
        MetricAlarms: '{{ item.MetricAlarms }}'
        OKActions: '{{ item.OKActions }}'
        StateReason: '{{ item.StateReason }}'
        StateReasonData: '{{ item.StateReasonData }}'
        StateTransitionedTimestamp: '{{ item.StateTransitionedTimestamp }}'
        StateUpdatedTimestamp: '{{ item.StateUpdatedTimestamp }}'
        StateValue: '{{ item.StateValue }}'
        Status: '{{ item.Status }}'
        item: '{{ item }}'

  # ════ INDEPENDENT (root) operations ════
  # ── aws.cloudwatch.describe_anomaly_detectors  ──
  - discovery_id: aws.cloudwatch.describe_anomaly_detectors
    calls:
      - action: describe_anomaly_detectors
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.AnomalyDetectors }}'
      item:
        AnomalyDetectors: '{{ item.AnomalyDetectors }}'
        Configuration: '{{ item.Configuration }}'
        Dimensions: '{{ item.Dimensions }}'
        MetricCharacteristics: '{{ item.MetricCharacteristics }}'
        MetricMathAnomalyDetector: '{{ item.MetricMathAnomalyDetector }}'
        MetricName: '{{ item.MetricName }}'
        Namespace: '{{ item.Namespace }}'
        SingleMetricAnomalyDetector: '{{ item.SingleMetricAnomalyDetector }}'
        Stat: '{{ item.Stat }}'
        StateValue: '{{ item.StateValue }}'

  # ════ INDEPENDENT (root) operations ════
  # ── aws.cloudwatch.describe_insight_rules  ──
  - discovery_id: aws.cloudwatch.describe_insight_rules
    calls:
      - action: describe_insight_rules
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.InsightRules }}'
      item:
        ApplyOnTransformedLogs: '{{ item.ApplyOnTransformedLogs }}'
        Definition: '{{ item.Definition }}'
        InsightRules: '{{ item.InsightRules }}'
        ManagedRule: '{{ item.ManagedRule }}'
        Name: '{{ item.Name }}'
        Schema: '{{ item.Schema }}'
        State: '{{ item.State }}'

  # ════ INDEPENDENT (root) operations ════
  # ── aws.cloudwatch.list_dashboards  ──
  - discovery_id: aws.cloudwatch.list_dashboards
    calls:
      - action: list_dashboards
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.DashboardEntries }}'
      item:
        DashboardArn: '{{ item.DashboardArn }}'
        DashboardName: '{{ item.DashboardName }}'
        LastModified: '{{ item.LastModified }}'
        Size: '{{ item.Size }}'

  # ════ INDEPENDENT (root) operations ════
  # ── aws.cloudwatch.list_metric_streams  ──
  - discovery_id: aws.cloudwatch.list_metric_streams
    calls:
      - action: list_metric_streams
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Entries }}'
      item:
        Arn: '{{ item.Arn }}'
        CreationDate: '{{ item.CreationDate }}'
        FirehoseArn: '{{ item.FirehoseArn }}'
        LastUpdateDate: '{{ item.LastUpdateDate }}'
        Name: '{{ item.Name }}'
        OutputFormat: '{{ item.OutputFormat }}'
        State: '{{ item.State }}'

  # ════ INDEPENDENT (root) operations ════
  # ── aws.cloudwatch.list_tags_for_resource  ──
  - discovery_id: aws.cloudwatch.list_tags_for_resource
    calls:
      - action: list_tags_for_resource
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Tags }}'
      item:
        Key: '{{ item.Key }}'
        Value: '{{ item.Value }}'

  # ════ DEPENDENT (enrich) operations ════
  # ── aws.cloudwatch.describe_alarms_for_metric [dependent] ──
  - discovery_id: aws.cloudwatch.describe_alarms_for_metric
    for_each: aws.cloudwatch.describe_anomaly_detectors
    calls:
      - action: describe_alarms_for_metric
        params:
          MetricName: "{{ item.MetricName }}"
          Namespace: "{{ item.Namespace }}"
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.MetricAlarms }}'
      item:
        ActionsEnabled: '{{ item.ActionsEnabled }}'
        AlarmActions: '{{ item.AlarmActions }}'
        AlarmArn: '{{ item.AlarmArn }}'
        AlarmConfigurationUpdatedTimestamp: '{{ item.AlarmConfigurationUpdatedTimestamp }}'
        AlarmDescription: '{{ item.AlarmDescription }}'
        AlarmName: '{{ item.AlarmName }}'
        ComparisonOperator: '{{ item.ComparisonOperator }}'
        DatapointsToAlarm: '{{ item.DatapointsToAlarm }}'
        Dimensions: '{{ item.Dimensions }}'
        EvaluateLowSampleCountPercentile: '{{ item.EvaluateLowSampleCountPercentile }}'
        EvaluationPeriods: '{{ item.EvaluationPeriods }}'
        EvaluationState: '{{ item.EvaluationState }}'
        ExtendedStatistic: '{{ item.ExtendedStatistic }}'
        InsufficientDataActions: '{{ item.InsufficientDataActions }}'
        MetricName: '{{ item.MetricName }}'
        Metrics: '{{ item.Metrics }}'
        Namespace: '{{ item.Namespace }}'
        OKActions: '{{ item.OKActions }}'
        Period: '{{ item.Period }}'
        StateReason: '{{ item.StateReason }}'
        StateReasonData: '{{ item.StateReasonData }}'
        StateTransitionedTimestamp: '{{ item.StateTransitionedTimestamp }}'
        StateUpdatedTimestamp: '{{ item.StateUpdatedTimestamp }}'
        StateValue: '{{ item.StateValue }}'
        Statistic: '{{ item.Statistic }}'
        Threshold: '{{ item.Threshold }}'
        ThresholdMetricId: '{{ item.ThresholdMetricId }}'
        TreatMissingData: '{{ item.TreatMissingData }}'
        Unit: '{{ item.Unit }}'

