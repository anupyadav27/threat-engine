# ==========================================================
# Discovery YAML — kafkaconnect (final_discovery v1)
# Generated from: aws_field_rule_catalog.csv
# Check rules: 1 | ops: 1 independent, 0 dependent
# ==========================================================
version: '1.0'
provider: aws
service: kafkaconnect

services:
  client: kafkaconnect
  module: "boto3.client('kafkaconnect')"

# Resource identifiers — used by inventory engine for asset dedup/linking
inventory_resource_identifiers:
  []

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # ── aws.kafkaconnect.list_connectors  ──
  - discovery_id: aws.kafkaconnect.list_connectors
    calls:
      - action: 
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        logDelivery: '{{ item.logDelivery }}'

