# Auto-generated: only discoveries referenced by parameterstore check rules (+ dependency chain)
# Source catalogs: ssm
# 1 of N needed (cross-service references)
version: '1.0'
provider: aws
service: parameterstore
discovery:
  - calls:
    - action: describe_parameters
      on_error: continue
      save_as: response
    discovery_id: aws.ssm.describe_parameters
    emit:
      as: item
      item:
        ARN: '{{ item.ARN }}'
        AllowedPattern: '{{ item.AllowedPattern }}'
        DataType: '{{ item.DataType }}'
        Description: '{{ item.Description }}'
        KeyId: '{{ item.KeyId }}'
        LastModifiedDate: '{{ item.LastModifiedDate }}'
        LastModifiedUser: '{{ item.LastModifiedUser }}'
        Name: '{{ item.Name }}'
        Policies: '{{ item.Policies }}'
        Tier: '{{ item.Tier }}'
        Type: '{{ item.Type }}'
        Version: '{{ item.Version }}'
      items_for: '{{ response.Parameters }}'
