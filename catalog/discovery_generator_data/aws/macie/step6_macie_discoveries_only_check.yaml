# discoveries_only_check: 1 kept / 40 removed from 41 total
# generated_from: step6_macie.discovery.yaml
# purpose: only discoveries needed by macie.checks.yaml for_each chains
# cross-service refs (in other step6 files): aws.macie2.describe_buckets, aws.macie2.list_classification_jobs, aws.macie2.list_findings, aws.macie2.list_members
version: '1.0'
provider: aws
service: macie
services:
  client: macie
  module: boto3.client
discovery:
- discovery_id: aws.macie.list_findings
  calls:
  - action: list_findings
    save_as: response
    on_error: continue
  emit:
    as: item
    item:
      archived: '{{ response.archived }}'
      classificationDetails: '{{ response.classificationDetails }}'
      findings: '{{ response.findings }}'
      partition: '{{ response.partition }}'
      policyDetails: '{{ response.policyDetails }}'
      resourcesAffected: '{{ response.resourcesAffected }}'
      sample: '{{ response.sample }}'
      schemaVersion: '{{ response.schemaVersion }}'
      severity: '{{ response.severity }}'
      title: '{{ response.title }}'
