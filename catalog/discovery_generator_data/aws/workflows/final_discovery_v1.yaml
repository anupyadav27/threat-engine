# ==========================================================
# Discovery YAML — workflows (final_discovery v1)
# Generated from: aws_field_rule_catalog.csv
# Check rules: 6 | ops: 2 independent, 1 dependent
# ==========================================================
version: '1.0'
provider: aws
service: workflows

services:
  client: workflows
  module: "boto3.client('workflows')"

# Resource identifiers — used by inventory engine for asset dedup/linking
inventory_resource_identifiers:
  - resource_type: activity
    identifier_op: aws.workflows.list_activities
    identifier_field: activityArn
    item_var_path: item.activityArn
    identifier_template: '{activityarn}'
  - resource_type: role
    identifier_op: aws.workflows.list_state_machines
    identifier_field: stateMachineArn
    item_var_path: item.stateMachineArn
    identifier_template: '{statemachinearn}'

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # ── aws.workflows.list_activities  ──
  - discovery_id: aws.workflows.list_activities
    calls:
      - action: list_activities
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        activityArn: '{{ item.activityArn }}'
        creationDate: '{{ item.creationDate }}'
        encryptionConfiguration: '{{ item.encryptionConfiguration }}'
        encryptionConfiguration.kmsKeyId: '{{ item.encryptionConfiguration.kmsKeyId }}'
        input: '{{ item.input }}'
        kmsDataKeyReusePeriodSeconds: '{{ item.kmsDataKeyReusePeriodSeconds }}'
        kmsKeyId: '{{ item.kmsKeyId }}'
        name: '{{ item.name }}'
        taskToken: '{{ item.taskToken }}'

  # ════ INDEPENDENT (root) operations ════
  # ── aws.workflows.list_state_machines  ──
  - discovery_id: aws.workflows.list_state_machines
    calls:
      - action: list_state_machines
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        creationDate: '{{ item.creationDate }}'
        definition: '{{ item.definition }}'
        description: '{{ item.description }}'
        label: '{{ item.label }}'
        name: '{{ item.name }}'
        stateMachineArn: '{{ item.stateMachineArn }}'
        type: '{{ item.type }}'

  # ════ DEPENDENT (enrich) operations ════
  # ── aws.workflows.describe_state_machine [dependent] ──
  - discovery_id: aws.workflows.describe_state_machine
    for_each: aws.workflows.list_state_machines
    calls:
      - action: describe_state_machine
        params:
          stateMachineArn: "{{ item.stateMachineArn }}"
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        definition: '{{ item.definition }}'
        description: '{{ item.description }}'
        destinations: '{{ item.destinations }}'
        includeExecutionData: '{{ item.includeExecutionData }}'
        label: '{{ item.label }}'
        level: '{{ item.level }}'
        loggingConfiguration: '{{ item.loggingConfiguration }}'
        loggingConfiguration.level: '{{ item.loggingConfiguration.level }}'
        name: '{{ item.name }}'
        roleArn: '{{ item.roleArn }}'
        stateMachineArn: '{{ item.stateMachineArn }}'
        status: '{{ item.status }}'
        type: '{{ item.type }}'

