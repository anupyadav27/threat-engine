# ==========================================================
# Discovery YAML — fsx (final_discovery v1)
# Generated from: aws_field_rule_catalog.csv
# Check rules: 11 | ops: 3 independent, 0 dependent
# ==========================================================
version: '1.0'
provider: aws
service: fsx

services:
  client: fsx
  module: "boto3.client('fsx')"

# Resource identifiers — used by inventory engine for asset dedup/linking
inventory_resource_identifiers:
  - resource_type: resource
    identifier_op: aws.fsx.describe_file_systems
    identifier_field: ResourceARN
    item_var_path: item.ResourceARN
    identifier_template: '{resourcearn}'

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # ── aws.fsx.describe_backups  ──
  - discovery_id: aws.fsx.describe_backups
    calls:
      - action: describe_backups
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Backups }}'
      item:
        BackupId: '{{ item.BackupId }}'
        Backups: '{{ item.Backups }}'
        CreationTime: '{{ item.CreationTime }}'
        DirectoryInformation: '{{ item.DirectoryInformation }}'
        FailureDetails: '{{ item.FailureDetails }}'
        FileSystem: '{{ item.FileSystem }}'
        Key: '{{ item.Key }}'
        KmsKeyId: '{{ item.KmsKeyId }}'
        Lifecycle: '{{ item.Lifecycle }}'
        OwnerId: '{{ item.OwnerId }}'
        ProgressPercent: '{{ item.ProgressPercent }}'
        ResourceARN: '{{ item.ResourceARN }}'
        ResourceType: '{{ item.ResourceType }}'
        SizeInBytes: '{{ item.SizeInBytes }}'
        SourceBackupId: '{{ item.SourceBackupId }}'
        SourceBackupRegion: '{{ item.SourceBackupRegion }}'
        Tags: '{{ item.Tags }}'
        Type: '{{ item.Type }}'
        Value: '{{ item.Value }}'
        Volume: '{{ item.Volume }}'

  # ════ INDEPENDENT (root) operations ════
  # ── aws.fsx.describe_file_systems  ──
  - discovery_id: aws.fsx.describe_file_systems
    calls:
      - action: describe_file_systems
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.FileSystems }}'
      item:
        AdministrativeActions: '{{ item.AdministrativeActions }}'
        CreationTime: '{{ item.CreationTime }}'
        DNSName: '{{ item.DNSName }}'
        FailureDetails: '{{ item.FailureDetails }}'
        FileSystemId: '{{ item.FileSystemId }}'
        FileSystemType: '{{ item.FileSystemType }}'
        FileSystemTypeVersion: '{{ item.FileSystemTypeVersion }}'
        FileSystems: '{{ item.FileSystems }}'
        KmsKeyId: '{{ item.KmsKeyId }}'
        Lifecycle: '{{ item.Lifecycle }}'
        LustreConfiguration: '{{ item.LustreConfiguration }}'
        NetworkInterfaceIds: '{{ item.NetworkInterfaceIds }}'
        NetworkType: '{{ item.NetworkType }}'
        OntapConfiguration: '{{ item.OntapConfiguration }}'
        OpenZFSConfiguration: '{{ item.OpenZFSConfiguration }}'
        OwnerId: '{{ item.OwnerId }}'
        ResourceARN: '{{ item.ResourceARN }}'
        StorageCapacity: '{{ item.StorageCapacity }}'
        StorageType: '{{ item.StorageType }}'
        SubnetIds: '{{ item.SubnetIds }}'
        Tags: '{{ item.Tags }}'
        VpcId: '{{ item.VpcId }}'
        WindowsConfiguration: '{{ item.WindowsConfiguration }}'
        WindowsConfiguration.CopyTagsToBackups: '{{ item.WindowsConfiguration.CopyTagsToBackups }}'
        WindowsConfiguration.CopyTagsToVolumes: '{{ item.WindowsConfiguration.CopyTagsToVolumes }}'
        WindowsConfiguration.DeploymentType: '{{ item.WindowsConfiguration.DeploymentType }}'
        item: '{{ item }}'

  # ════ INDEPENDENT (root) operations ════
  # ── aws.fsx.describe_snapshots  ──
  - discovery_id: aws.fsx.describe_snapshots
    calls:
      - action: describe_snapshots
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.Snapshots }}'
      item:
        AdministrativeActions: '{{ item.AdministrativeActions }}'
        CreationTime: '{{ item.CreationTime }}'
        Lifecycle: '{{ item.Lifecycle }}'
        LifecycleTransitionReason: '{{ item.LifecycleTransitionReason }}'
        Name: '{{ item.Name }}'
        ResourceARN: '{{ item.ResourceARN }}'
        SnapshotId: '{{ item.SnapshotId }}'
        Snapshots: '{{ item.Snapshots }}'
        Tags: '{{ item.Tags }}'
        VolumeId: '{{ item.VolumeId }}'

