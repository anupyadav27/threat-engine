# Discovery YAML — iam_identity (IBM Cloud)
# Generated: 2026-02-21T04:45:39Z
version: '1.0'
provider: ibm
service: iam_identity
services:
  client: iam_identity
  module: ibm_platform_services.iam_identity
discovery:
  # ── get_api_keys_details ──
  - discovery_id: ibm.iam_identity.get-api-keys-details
    calls:
      - action: get-api-keys-details
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── list_account_settings_assignments ──
  - discovery_id: ibm.iam_identity.list-account-settings-assignments
    calls:
      - action: list-account-settings-assignments
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_account_settings_templates ──
  - discovery_id: ibm.iam_identity.list-account-settings-templates
    calls:
      - action: list-account-settings-templates
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_api_keys ──
  - discovery_id: ibm.iam_identity.list-api-keys
    calls:
      - action: list-api-keys
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_profile_templates ──
  - discovery_id: ibm.iam_identity.list-profile-templates
    calls:
      - action: list-profile-templates
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_service_id_group ──
  - discovery_id: ibm.iam_identity.list-service-id-group
    calls:
      - action: list-service-id-group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_service_ids ──
  - discovery_id: ibm.iam_identity.list-service-ids
    calls:
      - action: list-service-ids
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_trusted_profile_assignments ──
  - discovery_id: ibm.iam_identity.list-trusted-profile-assignments
    calls:
      - action: list-trusted-profile-assignments
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── Dependent ops ──
  # ── get_account_settings [dependent] ──
  - discovery_id: ibm.iam_identity.get-account-settings
    calls:
      - action: get-account-settings
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_account_settings_assignment [dependent] ──
  - discovery_id: ibm.iam_identity.get-account-settings-assignment
    calls:
      - action: get-account-settings-assignment
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_account_settings_template_version [dependent] ──
  - discovery_id: ibm.iam_identity.get-account-settings-template-version
    calls:
      - action: get-account-settings-template-version
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_all_preferences_on_scope_account [dependent] ──
  - discovery_id: ibm.iam_identity.get-all-preferences-on-scope-account
    calls:
      - action: get-all-preferences-on-scope-account
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_api_key [dependent] ──
  - discovery_id: ibm.iam_identity.get-api-key
    calls:
      - action: get-api-key
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_claim_rule [dependent] ──
  - discovery_id: ibm.iam_identity.get-claim-rule
    calls:
      - action: get-claim-rule
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_effective_account_settings [dependent] ──
  - discovery_id: ibm.iam_identity.get-effective-account-settings
    calls:
      - action: get-effective-account-settings
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_latest_account_settings_template_version [dependent] ──
  - discovery_id: ibm.iam_identity.get-latest-account-settings-template-version
    calls:
      - action: get-latest-account-settings-template-version
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_latest_profile_template_version [dependent] ──
  - discovery_id: ibm.iam_identity.get-latest-profile-template-version
    calls:
      - action: get-latest-profile-template-version
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_link [dependent] ──
  - discovery_id: ibm.iam_identity.get-link
    calls:
      - action: get-link
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_mfa_report [dependent] ──
  - discovery_id: ibm.iam_identity.get-mfa-report
    calls:
      - action: get-mfa-report
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_mfa_status [dependent] ──
  - discovery_id: ibm.iam_identity.get-mfa-status
    calls:
      - action: get-mfa-status
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_preferences_on_scope_account [dependent] ──
  - discovery_id: ibm.iam_identity.get-preferences-on-scope-account
    calls:
      - action: get-preferences-on-scope-account
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_profile [dependent] ──
  - discovery_id: ibm.iam_identity.get-profile
    calls:
      - action: get-profile
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_profile_identities [dependent] ──
  - discovery_id: ibm.iam_identity.get-profile-identities
    calls:
      - action: get-profile-identities
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_profile_identity [dependent] ──
  - discovery_id: ibm.iam_identity.get-profile-identity
    calls:
      - action: get-profile-identity
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_profile_template_version [dependent] ──
  - discovery_id: ibm.iam_identity.get-profile-template-version
    calls:
      - action: get-profile-template-version
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_report [dependent] ──
  - discovery_id: ibm.iam_identity.get-report
    calls:
      - action: get-report
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_service_id [dependent] ──
  - discovery_id: ibm.iam_identity.get-service-id
    calls:
      - action: get-service-id
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_service_id_group [dependent] ──
  - discovery_id: ibm.iam_identity.get-service-id-group
    calls:
      - action: get-service-id-group
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── get_trusted_profile_assignment [dependent] ──
  - discovery_id: ibm.iam_identity.get-trusted-profile-assignment
    calls:
      - action: get-trusted-profile-assignment
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        created_at: '{{ response.created_at }}'
        crn: '{{ response.crn }}'
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        status: '{{ response.status }}'
        tags: '{{ response.tags }}'
  # ── list_claim_rules [dependent] ──
  - discovery_id: ibm.iam_identity.list-claim-rules
    calls:
      - action: list-claim-rules
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_links [dependent] ──
  - discovery_id: ibm.iam_identity.list-links
    calls:
      - action: list-links
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_profiles [dependent] ──
  - discovery_id: ibm.iam_identity.list-profiles
    calls:
      - action: list-profiles
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_versions_of_account_settings_template [dependent] ──
  - discovery_id: ibm.iam_identity.list-versions-of-account-settings-template
    calls:
      - action: list-versions-of-account-settings-template
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
  # ── list_versions_of_profile_template [dependent] ──
  - discovery_id: ibm.iam_identity.list-versions-of-profile-template
    calls:
      - action: list-versions-of-profile-template
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        created_at: '{{ item.created_at }}'
        crn: '{{ item.crn }}'
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
