# Discovery YAML — stub (IBM Cloud)
# Generated: 2026-02-21T04:45:39Z
version: '1.0'
provider: ibm
service: stub
services:
  client: stub
  module: ibm_platform_services.stub
discovery:
  # ── list_stub ──
  - discovery_id: ibm.stub.list_stub
    calls:
      - action: list_stub
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
  # ── list_stub_resources ──
  - discovery_id: ibm.stub.list_stub_resources
    calls:
      - action: list_stub_resources
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
  # ── Dependent ops ──
  # ── get_stub [dependent] ──
  - discovery_id: ibm.stub.get_stub
    calls:
      - action: get_stub
        save_as: response
        on_error: continue
    emit:
      as: item
  # ── get_stub_details [dependent] ──
  - discovery_id: ibm.stub.get_stub_details
    calls:
      - action: get_stub_details
        save_as: response
        on_error: continue
    emit:
      as: item
