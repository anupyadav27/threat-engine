# ============================================================
# K8s Finalized Discovery — networkpolicy
# Generated: 2026-04-16T14:28:05Z
# Source: k8s_field_rule_catalog.csv
# Ops: 1 list  +  1 get  =  2 total
# ============================================================

version: '1.0'
provider: k8s
service: networkpolicy
services:
  client: networking_v1_api
  module: kubernetes.client
  api_class: NetworkingV1Api
discovery:
- discovery_id: k8s.networkpolicy.list
  calls:
  - action: list_network_policy_for_all_namespaces
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.items }}'
- discovery_id: k8s.networkpolicy.get
  for_each: k8s.networkpolicy.list
  calls:
  - action: read_namespaced_network_policy
    save_as: response
    on_error: continue
    params:
      name: '{{ item.metadata.name }}'
      namespace: '{{ item.metadata.namespace }}'
  emit:
    as: item
    item:
      network_policy: '{{ response }}'
