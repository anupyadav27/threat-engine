# ============================================================
# Discovery YAML — policy (final_discovery v1)
# Generated: 2026-04-16T09:36:43Z
# Check rules: 10 | ops in scope: 1
# ============================================================
version: '1.0'
provider: k8s
service: policy

services:
  client: core_v1_api
  module: kubernetes.client
  api_class: CoreV1Api

# Resource identifiers — used by inventory engine for asset dedup/linking
inventory_resource_identifiers:
  - resource_type: 
    identifier_op: k8s.policy.list_network_policy_for_all_namespaces
    identifier_field: metadata.name
    item_var_path: item.metadata.name
    identifier_template: '{name}'

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # ── k8s.policy.list_network_policy_for_all_namespaces ──
  - discovery_id: k8s.policy.list_network_policy_for_all_namespaces
    calls:
      - action: list_network_policy_for_all_namespaces
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        metadata: '{{ item.metadata }}'
        spec: '{{ item.spec }}'
        spec.egress: '{{ item.spec.egress }}'
        spec.ingress: '{{ item.spec.ingress }}'
        spec.podSelector: '{{ item.spec.podSelector }}'
        spec.policyTypes: '{{ item.spec.policyTypes }}'

