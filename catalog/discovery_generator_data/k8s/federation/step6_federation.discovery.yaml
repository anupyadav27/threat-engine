version: '1.0'
provider: k8s
service: federation
services:
  client: federation
  module: kubernetes.client
discovery:
- discovery_id: k8s.federation.list_namespace
  calls:
  - action: list_namespace
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
