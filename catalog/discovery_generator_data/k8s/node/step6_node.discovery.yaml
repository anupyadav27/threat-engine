version: '1.0'
provider: k8s
service: node
services:
  client: node
  module: kubernetes.client
discovery:
- discovery_id: k8s.node.list_node
  calls:
  - action: list_node
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
