# ============================================================
# K8s Finalized Discovery — pod_security
# Generated: 2026-04-16T14:28:05Z
# Source: k8s_field_rule_catalog.csv
# Ops: 2 list  +  0 get  =  2 total
# ============================================================

version: '1.0'
provider: k8s
service: pod_security
services:
  client: policy_v1_api
  module: kubernetes.client
  api_class: PolicyV1Api
discovery:
- discovery_id: k8s.pod_security.list_pod_for_all_namespaces
  calls:
  - action: list_pod_for_all_namespaces
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.items }}'
- discovery_id: k8s.pod_security.list_pod_security_policy
  calls:
  - action: list_pod_security_policy
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.items }}'
