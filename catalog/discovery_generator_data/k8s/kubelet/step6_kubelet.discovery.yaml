version: '1.0'
provider: k8s
service: kubelet
services:
  client: kubelet
  module: kubernetes.client
discovery:
- discovery_id: k8s.kubelet.list_node
  calls:
  - action: list_node
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
