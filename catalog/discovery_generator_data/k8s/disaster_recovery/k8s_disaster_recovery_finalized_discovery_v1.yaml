# ============================================================
# K8s Finalized Discovery — disaster_recovery
# Generated: 2026-04-16T14:28:05Z
# Source: k8s_field_rule_catalog.csv
# Ops: 3 list  +  0 get  =  3 total
# ============================================================

version: '1.0'
provider: k8s
service: disaster_recovery
services:
  client: core_v1_api
  module: kubernetes.client
  api_class: CoreV1Api
discovery:
- discovery_id: k8s.disaster_recovery.list_persistent_volume
  calls:
  - action: list_persistent_volume
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.items }}'
- discovery_id: k8s.disaster_recovery.list_persistent_volume_claim_for_all_namespaces
  calls:
  - action: list_persistent_volume_claim_for_all_namespaces
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.items }}'
- discovery_id: k8s.disaster_recovery.list_storage_class
  calls:
  - action: list_storage_class
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.items }}'
