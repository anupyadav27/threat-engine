version: '1.0'
provider: k8s
service: inventory
services:
  client: inventory
  module: kubernetes.client
discovery:
- discovery_id: k8s.inventory.list_pod_for_all_namespaces
  calls:
  - action: list_pod_for_all_namespaces
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: k8s.inventory.list_deployment_for_all_namespaces
  calls:
  - action: list_deployment_for_all_namespaces
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: k8s.inventory.list_node
  calls:
  - action: list_node
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
