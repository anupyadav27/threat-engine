# ============================================================
# Discovery YAML — storageclass (final_discovery v1)
# Generated: 2026-04-16T09:36:43Z
# Check rules: 3 | ops in scope: 1
# ============================================================
version: '1.0'
provider: k8s
service: storageclass

services:
  client: storage_v1_api
  module: kubernetes.client
  api_class: StorageV1Api

# Resource identifiers — used by inventory engine for asset dedup/linking
inventory_resource_identifiers:
  - resource_type: 
    identifier_op: k8s.storageclass.list
    identifier_field: metadata.name
    item_var_path: item.metadata.name
    identifier_template: '{name}'

checks: []

discovery:

  # ════ INDEPENDENT (root) operations ════
  # ── k8s.storageclass.list ──
  - discovery_id: k8s.storageclass.list
    calls:
      - action: list_storage_class
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.items }}'
      item:
        allowVolumeExpansion: '{{ item.allowVolumeExpansion }}'
        metadata.name: '{{ item.metadata.name }}'
        parameters: '{{ item.parameters }}'
        provisioner: '{{ item.provisioner }}'
        reclaimPolicy: '{{ item.reclaimPolicy }}'
        volumeBindingMode: '{{ item.volumeBindingMode }}'

