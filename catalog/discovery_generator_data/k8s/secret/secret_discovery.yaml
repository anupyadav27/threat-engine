version: '1.0'
provider: k8s
service: secret
services:
  client: secret
  module: boto3.client
discovery:
- discovery_id: k8s.secret.list
  calls:
  - action: list
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
    item:
      metadata: '{{ item.metadata }}'
      metadata.name: '{{ item.metadata.name }}'
      metadata.namespace: '{{ item.metadata.namespace }}'
      type: '{{ item.type }}'
      data: '{{ item.data }}'
      stringData: '{{ item.stringData }}'
      immutable: '{{ item.immutable }}'
- discovery_id: k8s.secret.get
  calls:
  - action: get
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
    item:
      metadata: '{{ item.metadata }}'
      metadata.name: '{{ item.metadata.name }}'
      metadata.namespace: '{{ item.metadata.namespace }}'
      type: '{{ item.type }}'
      data: '{{ item.data }}'
      stringData: '{{ item.stringData }}'
      immutable: '{{ item.immutable }}'
checks: []
