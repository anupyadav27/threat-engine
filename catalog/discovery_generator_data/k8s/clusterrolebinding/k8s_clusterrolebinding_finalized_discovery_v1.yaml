# ============================================================
# K8s Finalized Discovery — clusterrolebinding
# Generated: 2026-04-16T14:28:05Z
# Source: k8s_field_rule_catalog.csv
# Ops: 1 list  +  1 get  =  2 total
# ============================================================

version: '1.0'
provider: k8s
service: clusterrolebinding
services:
  client: rbac_authorization_v1_api
  module: kubernetes.client
  api_class: RbacAuthorizationV1Api
discovery:
- discovery_id: k8s.clusterrolebinding.list
  calls:
  - action: list_cluster_role_binding
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.items }}'
- discovery_id: k8s.clusterrolebinding.get
  for_each: k8s.clusterrolebinding.list
  calls:
  - action: read_cluster_role_binding
    save_as: response
    on_error: continue
    params:
      name: '{{ item.metadata.name }}'
  emit:
    as: item
    item:
      cluster_role_binding: '{{ response }}'
