version: '1.0'
provider: networking
engine: cisco_nxos
category: section_1
cis_section: '1'
description: >
  cisco_nxos CIS Section 1 — Exec Timeout for Console Sessions is set for less than 10

discovery:

  - discovery_id: cisco_nxos.section_1.exec_timeout_for_console_sessions_is_set_for_less
    description: Ensure Exec Timeout for Console Sessions is set for less than 10
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: exec_timeout_for_console_sessions_is_set_for_less
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_nxos.section_1.exec_timeout_for_remote_administrative_sessions
    description: Ensure Exec Timeout for Remote Administrative Sessions (VTY) is set to less than 10
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: exec_timeout_for_remote_administrative_sessions
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_nxos.section_1.set_the_maximum_number_of_vty_sessions
    description: Set the Maximum Number of VTY Sessions
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: set_the_maximum_number_of_vty_sessions
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_nxos.section_1.disable_the_telnet_feature
    description: Disable the Telnet Feature
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: disable_the_telnet_feature
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_nxos.section_1.enable_password_complexity_requirements_for_local
    description: Enable Password Complexity Requirements for Local Credentials
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: enable_password_complexity_requirements_for_local
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_nxos.section_1.set_password_lifetime_warning_time_and_grace_time
    description: Set password lifetime, warning time and grace time for local credentials
    calls:
      - action: run_command
        command: "To show the passphrase timers set per-user: switch# sho username \u003clocal userid\u003e passphrase timevalues Last passphrase change(Y-M-D): 2020-04-30 Passphrase lifetime: 99999 days after last passphrase change Passphrase warning time starts: 14 days before passphrase lifetime Passphrase Gracetime ends: 3 days after passphrase lifetime To show the global defaults for passphrase timers: switch# show userpassphrase timevalues passphrase default warningtime (in days): 10 passphrase default gracetime (in days): 10 passphrase default lifetime (in days): 180 To show both the global defaults as well as the settings for all local users: switch# sho run | i passphrase username asdf passphrase lifetime 99999 warntime 14 gracetime 3 username test passphrase lifetime 180 warntime 10 gracetime 10 userpassphrase default-warntime 10 userpassphrase default-gracetime 10 userpassphrase default-lifetime 180"
        save_as: result
    emit:
      as: item
      item:
        check: set_password_lifetime_warning_time_and_grace_time
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_nxos.section_1.set_password_length_for_local_credentials
    description: Set password length for local credentials
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: set_password_length_for_local_credentials
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_nxos.section_1.syslog_logging_is_configured
    description: Ensure Syslog Logging is configured
    calls:
      - action: run_command
        command: "To show the current logging server: switch# sho logging server Logging server: enabled {1.2.3.4} server severity: notifications server facility: local7 server VRF: default server port: 514 To show the logging source interface: CISNXOS9# sho logging source-interface Logging source-interface : enabled (Interface: mgmt0) To show the logging level: CISNXOS9# sho logging level Facility Default Severity Current Session Severity -------- ---------------- ------------------------ aaa 3 3 acllog 2 2 aclmgr 3 3 aclqos 5 5 adbm 2 2 arp 3 3 auth 0 0 authpriv 3 3 bootvar 5 5 callhome 2 2 capability 2 2 cdp 2 2 cert_enroll 2 2 cfs 3 3 clis 3 3 clk_mgr 2 2 confcheck 2 2 copp 2 2 cron 3 3 daemon 3 3 device_test 3 3 dhclient 2 2 dhcp_snoop 2 2 diag_port_lb 2 2 diagclient 2 2 diagmgr 2 2 ecp 5 5 eltm 2 2 eth_port_channel 5 5 ethpm 5 5 evmc 5 5 evms 2 2 feature-mgr 2 2 fs-daemon 2 2 ftp 3 3 ifmgr 5 5 igmp_1 5 5 interface-vlan 2 2 ip 3 3 ipfib 2 2 ipqosmgr 4 4 ipv6 3 3 kern 3 3 l2fm 2 2 l2pt 3 3 l3vm 5 5 lacp 2 2 licmgr 6 6 lldp 2 2 local0 3 3 local1 3 3 local2 3 3 local3 3 3 local4 3 3 local5 3 3 local6 3 3 local7 3 3 lpr 3 3 m2rib 2 2 m6rib 5 5 mail 3 3 mcm 2 2 mfdm 2 2 mmode 2 2 module 5 5 monitor 3 3 mrib 5 5 mvsh 2 2 news 3 3 ntp 2 2 otm 3 3 pfstat 2 2 pixm_gl 4 4 pixm_vl 4 4 platform 5 5 plcmgr 2 2 plugin 2 2 port-profile 2 2 radius 3 3 res_mgr 5 5 rpm 5 5 sal 2 2 scheduler 5 5 securityd 3 3 sflow 2 2 sksd 3 3 smm 4 4 snmpd 2 2 span 3 3 spm 2 2 stp 3 3 syslog 3 3 sysmgr 3 3 tamnw 2 2 telemetry 3 3 template_manager 2 2 u6rib 5 5 ufdm 3 3 urib 5 5 user 3 3 uucp 3 3 vdc_mgr 6 6 virtual-service 5 5 vlan_mgr 2 2 vshd 5 5 xbar 5 5 xmlma 3 3 0(emergencies) 1(alerts) 2(critical) 3(errors) 4(warnings) 5(notifications) 6(information) 7(debugging)"
        save_as: result
    emit:
      as: item
      item:
        check: syslog_logging_is_configured
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_nxos.section_1.configure_a_time_zone
    description: Configure a Time Zone
    calls:
      - action: run_command
        command: "To show the current timezone, show the running configuration and filter for the word \u0027timezone\u0027. switch(config)# sho run | i timezone clock timezone EST -5 0"
        save_as: result
    emit:
      as: item
      item:
        check: configure_a_time_zone
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_nxos.section_1.disable_power_on_auto_provisioning_poap
    description: Disable Power on Auto Provisioning (POAP)
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: disable_power_on_auto_provisioning_poap
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_nxos.section_1.disable_ipxe_pre_boot_execution_environment
    description: Disable iPXE (Pre-boot eXecution Environment)
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: disable_ipxe_pre_boot_execution_environment
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_nxos.section_1.configure_snmp_traps
    description: Configure SNMP Traps
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: configure_snmp_traps
        value: "{{ result.stdout }}"
        expected: ""

