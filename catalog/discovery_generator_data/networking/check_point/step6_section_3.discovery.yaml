version: '1.0'
provider: networking
engine: check_point
category: section_3
cis_section: '3'
description: >
  check_point CIS Section 3 — Enable the Firewall Stealth Rule

discovery:

  - discovery_id: check_point.section_3.enable_the_firewall_stealth_rule
    description: Enable the Firewall Stealth Rule
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: CHECK_POINT_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: enable_the_firewall_stealth_rule
        value: "{{ result }}"
        expected: ""

  - discovery_id: check_point.section_3.hit_count_is_enable_for_the_rules
    description: Ensure Hit count is Enable for the rules
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: CHECK_POINT_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: hit_count_is_enable_for_the_rules
        value: "{{ result }}"
        expected: ""

  - discovery_id: check_point.section_3.review_and_log_implied_rules
    description: Review and Log Implied Rules
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: CHECK_POINT_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: review_and_log_implied_rules
        value: "{{ result }}"
        expected: ""

  - discovery_id: check_point.section_3.drop_out_of_state_tcp_packets_is_enabled
    description: Ensure Drop Out of State TCP Packets is enabled
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: CHECK_POINT_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: drop_out_of_state_tcp_packets_is_enabled
        value: "{{ result }}"
        expected: ""

  - discovery_id: check_point.section_3.drop_out_of_state_icmp_packets_is_enabled
    description: Ensure Drop Out of State ICMP Packets is enabled
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: CHECK_POINT_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: drop_out_of_state_icmp_packets_is_enabled
        value: "{{ result }}"
        expected: ""

  - discovery_id: check_point.section_3.anti_spoofing_is_enabled_and_action_is_set_to
    description: Ensure Anti-Spoofing is enabled and action is set to Prevent for all Interfaces
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: CHECK_POINT_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: anti_spoofing_is_enabled_and_action_is_set_to
        value: "{{ result }}"
        expected: ""

  - discovery_id: check_point.section_3.disk_space_alert_is_set
    description: Ensure Disk Space Alert is set
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: CHECK_POINT_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: disk_space_alert_is_set
        value: "{{ result }}"
        expected: ""

  - discovery_id: check_point.section_3.accept_rip_is_not_enabled
    description: Ensure Accept RIP is not enabled
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: CHECK_POINT_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: accept_rip_is_not_enabled
        value: "{{ result }}"
        expected: ""

  - discovery_id: check_point.section_3.accept_domain_name_over_tcp_zone_transfer_is_not
    description: Ensure Accept Domain Name over TCP (Zone Transfer) is not enabled
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: CHECK_POINT_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: accept_domain_name_over_tcp_zone_transfer_is_not
        value: "{{ result }}"
        expected: ""

  - discovery_id: check_point.section_3.accept_domain_name_over_udp_queries_is_not_enabled
    description: Ensure Accept Domain Name over UDP (Queries) is not enabled
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: CHECK_POINT_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: accept_domain_name_over_udp_queries_is_not_enabled
        value: "{{ result }}"
        expected: ""

  - discovery_id: check_point.section_3.accept_icmp_requests_is_not_enabled
    description: Ensure Accept ICMP Requests is not enabled
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: CHECK_POINT_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: accept_icmp_requests_is_not_enabled
        value: "{{ result }}"
        expected: ""

  - discovery_id: check_point.section_3.allow_bi_directional_nat_is_enabled
    description: Ensure Allow bi-directional NAT is enabled
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: CHECK_POINT_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: allow_bi_directional_nat_is_enabled
        value: "{{ result }}"
        expected: ""

  - discovery_id: check_point.section_3.automatic_arp_configuration_nat_is_enabled
    description: Ensure Automatic ARP Configuration NAT is enabled
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: CHECK_POINT_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: automatic_arp_configuration_nat_is_enabled
        value: "{{ result }}"
        expected: ""

  - discovery_id: check_point.section_3.logging_is_enabled_for_track_options_of_global
    description: Ensure Logging is enabled for Track Options of Global Properties
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: CHECK_POINT_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: logging_is_enabled_for_track_options_of_global
        value: "{{ result }}"
        expected: ""

