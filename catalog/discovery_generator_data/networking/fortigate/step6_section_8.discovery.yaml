version: '1.0'
provider: networking
engine: fortigate
category: section_8
cis_section: '8'
description: >
  fortigate CIS Section 8 — Encrypt Log Transmission to FortiAnalyzer / FortiManager

discovery:

  - discovery_id: fortigate.section_8.encrypt_log_transmission_to_fortianalyzer
    description: Encrypt Log Transmission to FortiAnalyzer / FortiManager
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: encrypt_log_transmission_to_fortianalyzer
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: fortigate.section_8.centralized_logging_and_reporting
    description: Centralized Logging and Reporting
    calls:
      - action: run_command
        command: "Review log settings through the administrative web page go to Log \u0026 Report \u003e Log Settings and validate under \u0027Remote Logging and Archiving\u0027 that logs are being offloaded to another device."
        save_as: result
    emit:
      as: item
      item:
        check: centralized_logging_and_reporting
        value: "{{ result.stdout }}"
        expected: ""

