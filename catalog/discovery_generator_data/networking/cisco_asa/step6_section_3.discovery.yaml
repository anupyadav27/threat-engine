version: '1.0'
provider: networking
engine: cisco_asa
category: section_3
cis_section: '3'
description: >
  cisco_asa CIS Section 3 — DNS services are configured correctly

discovery:

  - discovery_id: cisco_asa.section_3.dns_services_are_configured_correctly
    description: Ensure DNS services are configured correctly
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: dns_services_are_configured_correctly
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_3.packet_fragments_are_restricted_for_untrusted
    description: Ensure packet fragments are restricted for untrusted interfaces
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: packet_fragments_are_restricted_for_untrusted
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_3.threat_detection_statistics_is_set_to_tcp
    description: Ensure 'threat-detection statistics' is set to 'tcp-intercept'
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: threat_detection_statistics_is_set_to_tcp
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_3.ip_verify_is_set_to_reverse_path_for_untrusted
    description: Ensure 'ip verify' is set to 'reverse-path' for untrusted interfaces
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: ip_verify_is_set_to_reverse_path_for_untrusted
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_3.security_level_is_set_to_0_for_internet_facing
    description: Ensure 'security-level' is set to '0' for Internet-facing interface
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: security_level_is_set_to_0_for_internet_facing
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_3.botnet_protection_is_enabled_for_untrusted
    description: Ensure Botnet protection is enabled for untrusted interfaces
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: botnet_protection_is_enabled_for_untrusted
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_3.activex_filtering_is_enabled
    description: Ensure ActiveX filtering is enabled
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: activex_filtering_is_enabled
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_3.java_applet_filtering_is_enabled
    description: Ensure Java applet filtering is enabled
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: java_applet_filtering_is_enabled
        value: "{{ result.stdout }}"
        expected: ""

