version: '1.0'
provider: networking
engine: cisco_asa
category: section_2
cis_section: '2'
description: >
  cisco_asa CIS Section 2 — 'OSPF authentication' is enabled

discovery:

  - discovery_id: cisco_asa.section_2.ospf_authentication_is_enabled
    description: Ensure 'OSPF authentication' is enabled
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: ospf_authentication_is_enabled
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_2.eigrp_authentication_is_enabled
    description: Ensure 'EIGRP authentication' is enabled
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: eigrp_authentication_is_enabled
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_2.bgp_authentication_is_enabled
    description: Ensure 'BGP authentication' is enabled
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: bgp_authentication_is_enabled
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_2.noproxyarp_is_enabled_for_untrusted_interfaces
    description: Ensure 'noproxyarp' is enabled for untrusted interfaces
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: noproxyarp_is_enabled_for_untrusted_interfaces
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_2.dns_guard_is_enabled
    description: Ensure 'DNS Guard' is enabled
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: dns_guard_is_enabled
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_2.dhcp_services_are_disabled_for_untrusted
    description: Ensure DHCP services are disabled for untrusted interfaces
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: dhcp_services_are_disabled_for_untrusted
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_2.icmp_is_restricted_for_untrusted_interfaces
    description: Ensure ICMP is restricted for untrusted interfaces
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: icmp_is_restricted_for_untrusted_interfaces
        value: "{{ result.stdout }}"
        expected: ""

