version: '1.0'
provider: networking
engine: cisco_asa
category: section_1
cis_section: '1'
description: >
  cisco_asa CIS Section 1 — 'Master Key Passphrase' is set

discovery:

  - discovery_id: cisco_asa.section_1.master_key_passphrase_is_set
    description: Ensure 'Master Key Passphrase' is set
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: master_key_passphrase_is_set
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.password_recovery_is_disabled
    description: Ensure 'Password Recovery' is disabled
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: password_recovery_is_disabled
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.password_policy_is_enabled
    description: Ensure 'Password Policy' is enabled
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: password_policy_is_enabled
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.domain_name_is_set
    description: Ensure 'Domain Name' is set
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: domain_name_is_set
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.host_name_is_set
    description: Ensure 'Host Name' is set
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: host_name_is_set
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.failover_is_enabled
    description: Ensure 'Failover' is enabled
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: failover_is_enabled
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.unused_interfaces_is_disable
    description: Ensure 'Unused Interfaces' is disable
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: unused_interfaces_is_disable
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.image_authenticity_is_correct
    description: Ensure 'Image Authenticity' is correct
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: image_authenticity_is_correct
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.aaa_local_authentication_max_failed_attempts_is
    description: Ensure 'aaa local authentication max failed attempts' is set to less than or equal to '3'
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: aaa_local_authentication_max_failed_attempts_is
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.known_default_accounts_do_not_exist
    description: Ensure known default accounts do not exist
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: known_default_accounts_do_not_exist
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.tacacs_radius_is_configured_correctly
    description: Ensure 'TACACS+/RADIUS' is configured correctly
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: tacacs_radius_is_configured_correctly
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.aaa_authentication_secure_http_client_is
    description: Ensure 'aaa authentication secure-http-client' is configured correctly
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: aaa_authentication_secure_http_client_is
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.aaa_command_authorization_is_configured_correctly
    description: Ensure 'aaa command authorization' is configured correctly
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: aaa_command_authorization_is_configured_correctly
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.ssh_source_restriction_is_set_to_an_authorized_ip
    description: Ensure 'SSH source restriction' is set to an authorized IP address
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: ssh_source_restriction_is_set_to_an_authorized_ip
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.ssh_version_2_is_enabled
    description: Ensure 'SSH version 2' is enabled
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: ssh_version_2_is_enabled
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.rsa_key_pair_is_greater_than_or_equal_to_2048_bits
    description: Ensure 'RSA key pair' is greater than or equal to 2048 bits
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: rsa_key_pair_is_greater_than_or_equal_to_2048_bits
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.scp_protocol_is_set_to_enable_for_files_transfers
    description: Ensure 'SCP protocol' is set to Enable for files transfers
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: scp_protocol_is_set_to_enable_for_files_transfers
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.http_source_restriction_is_set_to_an_authorized
    description: Ensure 'HTTP source restriction' is set to an authorized IP address
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: http_source_restriction_is_set_to_an_authorized
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.console_session_timeout_is_less_than_or_equal_to
    description: Ensure 'console session timeout' is less than or equal to '5' minutes
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: console_session_timeout_is_less_than_or_equal_to
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.ssh_session_timeout_is_less_than_or_equal_to_5
    description: Ensure 'SSH session timeout' is less than or equal to '5' minutes
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: ssh_session_timeout_is_less_than_or_equal_to_5
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.http_idle_timeout_is_less_than_or_equal_to_5
    description: Ensure 'HTTP idle timeout' is less than or equal to '5' minutes
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: http_idle_timeout_is_less_than_or_equal_to_5
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.ntp_authentication_is_enabled
    description: Ensure 'NTP authentication' is enabled
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: ntp_authentication_is_enabled
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.ntp_authentication_key_is_configured_correctly
    description: Ensure 'NTP authentication key' is configured correctly
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: ntp_authentication_key_is_configured_correctly
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.trusted_ntp_server_exists
    description: Ensure 'trusted NTP server' exists
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: trusted_ntp_server_exists
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.local_timezone_is_properly_configured
    description: Ensure 'local timezone' is properly configured
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: local_timezone_is_properly_configured
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.logging_is_enabled
    description: Ensure 'logging' is enabled
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: logging_is_enabled
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.logging_to_monitor_is_disabled
    description: Ensure 'logging to monitor' is disabled
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: logging_to_monitor_is_disabled
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.syslog_hosts_is_configured_correctly
    description: Ensure 'syslog hosts' is configured correctly
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: syslog_hosts_is_configured_correctly
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.logging_with_the_device_id_is_configured_correctly
    description: Ensure 'logging with the device ID' is configured correctly
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: logging_with_the_device_id_is_configured_correctly
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.logging_history_severity_level_is_set_to_greater
    description: Ensure 'logging history severity level' is set to greater than or equal to '5'
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: logging_history_severity_level_is_set_to_greater
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.logging_with_timestamps_is_enabled
    description: Ensure 'logging with timestamps' is enabled
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: logging_with_timestamps_is_enabled
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.logging_buffer_size_is_greater_than_or_equal_to
    description: Ensure 'logging buffer size' is greater than or equal to '524288' bytes (512kb)
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: logging_buffer_size_is_greater_than_or_equal_to
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.logging_buffered_severity_level_is_greater_than
    description: Ensure 'logging buffered severity level' is greater than or equal to '3'
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: logging_buffered_severity_level_is_greater_than
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.logging_trap_severity_level_is_greater_than_or
    description: Ensure 'logging trap severity level' is greater than or equal to '5'
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: logging_trap_severity_level_is_greater_than_or
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.email_logging_is_configured_for_critical_to
    description: Ensure email logging is configured for critical to emergency
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: email_logging_is_configured_for_critical_to
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.snmp_server_group_is_set_to_v3_priv
    description: Ensure 'snmp-server group' is set to 'v3 priv'
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: snmp_server_group_is_set_to_v3_priv
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.snmp_server_user_is_set_to_v3_auth_sha
    description: Ensure 'snmp-server user' is set to 'v3 auth SHA'
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: snmp_server_user_is_set_to_v3_auth_sha
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.snmp_server_host_is_set_to_version_3
    description: Ensure 'snmp-server host' is set to 'version 3'
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: snmp_server_host_is_set_to_version_3
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.snmp_traps_is_enabled
    description: Ensure 'SNMP traps' is enabled
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: snmp_traps_is_enabled
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: cisco_asa.section_1.snmp_community_string_is_not_the_default_string
    description: Ensure 'SNMP community string' is not the default string
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: snmp_community_string_is_not_the_default_string
        value: "{{ result.stdout }}"
        expected: ""

