version: '1.0'
provider: web_server
engine: tomcat
category: section_6
cis_section: '6'
description: >
  tomcat CIS Section 6 — Setup Client-cert Authentication

discovery:

  - discovery_id: tomcat.section_6.setup_client_cert_authentication
    description: Setup Client-cert Authentication
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: setup_client_cert_authentication
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: tomcat.section_6.sslenabled_is_set_to_true_for_sensitive_connectors
    description: Ensure SSLEnabled is set to True for Sensitive Connectors
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: sslenabled_is_set_to_true_for_sensitive_connectors
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: tomcat.section_6.scheme_is_set_accurately
    description: Ensure scheme is set accurately
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: scheme_is_set_accurately
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: tomcat.section_6.secure_is_set_to_true_only_for_ssl_enabled
    description: Ensure secure is set to true only for SSL-enabled Connectors
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: secure_is_set_to_true_only_for_ssl_enabled
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: tomcat.section_6.sslprotocol_is_configured_correctly_for_secure
    description: Ensure 'sslProtocol' is Configured Correctly for Secure Connectors
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: sslprotocol_is_configured_correctly_for_secure
        value: "{{ result.stdout }}"
        expected: ""

