version: '1.0'
provider: web_server
engine: tomcat
category: section_2
cis_section: '2'
description: >
  tomcat CIS Section 2 — Disable X-Powered-By HTTP Header and Rename the Server Value for all Connectors

discovery:

  - discovery_id: tomcat.section_2.disable_x_powered_by_http_header_and_rename_the
    description: Disable X-Powered-By HTTP Header and Rename the Server Value for all Connectors
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: disable_x_powered_by_http_header_and_rename_the
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: tomcat.section_2.disable_client_facing_stack_traces
    description: Disable client facing Stack Traces
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: disable_client_facing_stack_traces
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: tomcat.section_2.turn_off_trace
    description: Turn off TRACE
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: turn_off_trace
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: tomcat.section_2.sever_header_is_modified_to_prevent_information
    description: Ensure Sever Header is Modified To Prevent Information Disclosure
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: sever_header_is_modified_to_prevent_information
        value: "{{ result.stdout }}"
        expected: ""

