version: '1.0'
provider: web_server
engine: tomcat
category: section_5
cis_section: '5'
description: >
  tomcat CIS Section 5 — Use secure Realms

discovery:

  - discovery_id: tomcat.section_5.use_secure_realms
    description: Use secure Realms
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: use_secure_realms
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: tomcat.section_5.use_lockout_realms
    description: Use LockOut Realms
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: use_lockout_realms
        value: "{{ result.stdout }}"
        expected: ""

