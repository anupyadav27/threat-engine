version: '1.0'
provider: web_server
engine: tomcat
category: section_7
cis_section: '7'
description: >
  tomcat CIS Section 7 — Application specific logging

discovery:

  - discovery_id: tomcat.section_7.application_specific_logging
    description: Application specific logging
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: application_specific_logging
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: tomcat.section_7.specify_file_handler_in_logging_properties_files
    description: Specify file handler in logging.properties files
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: specify_file_handler_in_logging_properties_files
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: tomcat.section_7.classname_is_set_correctly_in_context_xml
    description: Ensure className is set correctly in context.xml
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: classname_is_set_correctly_in_context_xml
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: tomcat.section_7.pattern_in_context_xml_is_correct
    description: Ensure pattern in context.xml is correct
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: pattern_in_context_xml_is_correct
        value: "{{ result.stdout }}"
        expected: ""

  - discovery_id: tomcat.section_7.directory_in_logging_properties_is_a_secure
    description: Ensure directory in logging.properties is a secure location
    calls:
      - action: run_command
        command: ""
        save_as: result
    emit:
      as: item
      item:
        check: directory_in_logging_properties_is_a_secure
        value: "{{ result.stdout }}"
        expected: ""

