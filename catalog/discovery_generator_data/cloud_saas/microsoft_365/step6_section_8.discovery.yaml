version: '1.0'
provider: cloud_saas
engine: microsoft_365
category: section_8
cis_section: '8'
description: >
  microsoft_365 CIS Section 8 — (L2) Ensure external file sharing in Teams is enabled for only approved cloud storage services

discovery:

  - discovery_id: microsoft_365.section_8.l2_ensure_external_file_sharing_in_teams_is
    description: (L2) Ensure external file sharing in Teams is enabled for only approved cloud storage services
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l2_ensure_external_file_sharing_in_teams_is
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_8.l1_ensure_users_can_t_send_emails_to_a_channel
    description: (L1) Ensure users can't send emails to a channel email address
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_users_can_t_send_emails_to_a_channel
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_8.l2_ensure_external_domains_are_restricted_in_the
    description: (L2) Ensure external domains are restricted in the Teams admin center
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l2_ensure_external_domains_are_restricted_in_the
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_8.l1_ensure_communication_with_unmanaged_teams
    description: (L1) Ensure communication with unmanaged Teams users is disabled
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_communication_with_unmanaged_teams
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_8.l1_ensure_external_teams_users_cannot_initiate
    description: (L1) Ensure external Teams users cannot initiate conversations
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_external_teams_users_cannot_initiate
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_8.l1_ensure_communication_with_skype_users_is
    description: (L1) Ensure communication with Skype users is disabled
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_communication_with_skype_users_is
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_8.l1_ensure_only_people_in_my_org_can_bypass_the
    description: (L1) Ensure only people in my org can bypass the lobby
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_only_people_in_my_org_can_bypass_the
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_8.l1_ensure_users_dialing_in_can_t_bypass_the_lobby
    description: (L1) Ensure users dialing in can't bypass the lobby
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_users_dialing_in_can_t_bypass_the_lobby
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_8.l2_ensure_only_organizers_and_co_organizers_can
    description: (L2) Ensure only organizers and co-organizers can present
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l2_ensure_only_organizers_and_co_organizers_can
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_8.l1_ensure_external_participants_can_t_give_or
    description: (L1) Ensure external participants can't give or request control
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_external_participants_can_t_give_or
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_8.l2_ensure_external_meeting_chat_is_off
    description: (L2) Ensure external meeting chat is off
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l2_ensure_external_meeting_chat_is_off
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_8.l2_ensure_meeting_recording_is_off_by_default
    description: (L2) Ensure meeting recording is off by default
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l2_ensure_meeting_recording_is_off_by_default
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_8.l1_ensure_users_can_report_security_concerns_in
    description: (L1) Ensure users can report security concerns in Teams
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_users_can_report_security_concerns_in
        value: "{{ result }}"
        expected: ""

