version: '1.0'
provider: cloud_saas
engine: microsoft_365
category: section_1
cis_section: '1'
description: >
  microsoft_365 CIS Section 1 — (L1) Ensure that between two and four global admins are designated

discovery:

  - discovery_id: microsoft_365.section_1.l1_ensure_that_between_two_and_four_global_admins
    description: (L1) Ensure that between two and four global admins are designated
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_that_between_two_and_four_global_admins
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_1.l1_ensure_the_password_expiration_policy_is_set
    description: (L1) Ensure the 'Password expiration policy' is set to 'Set passwords to never expire (recommended)'
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_the_password_expiration_policy_is_set
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_1.l2_ensure_external_sharing_of_calendars_is_not
    description: (L2) Ensure 'External sharing' of calendars is not available
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l2_ensure_external_sharing_of_calendars_is_not
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_1.l1_ensure_internal_phishing_protection_for_forms
    description: (L1) Ensure internal phishing protection for Forms is enabled
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_internal_phishing_protection_for_forms
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_1.l2_ensure_third_party_storage_services_are
    description: (L2) Ensure 'third-party storage services' are restricted in 'Microsoft 365 on the web'
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l2_ensure_third_party_storage_services_are
        value: "{{ result }}"
        expected: ""

