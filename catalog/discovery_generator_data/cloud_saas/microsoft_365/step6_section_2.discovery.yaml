version: '1.0'
provider: cloud_saas
engine: microsoft_365
category: section_2
cis_section: '2'
description: >
  microsoft_365 CIS Section 2 — (L1) Ensure the Common Attachment Types Filter is enabled

discovery:

  - discovery_id: microsoft_365.section_2.l1_ensure_the_common_attachment_types_filter_is
    description: (L1) Ensure the Common Attachment Types Filter is enabled
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_the_common_attachment_types_filter_is
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_2.l1_ensure_notifications_for_internal_users
    description: (L1) Ensure notifications for internal users sending malware is Enabled
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_notifications_for_internal_users
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_2.l2_ensure_safe_attachments_policy_is_enabled
    description: (L2) Ensure Safe Attachments policy is enabled
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l2_ensure_safe_attachments_policy_is_enabled
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_2.l2_ensure_safe_attachments_for_sharepoint
    description: (L2) Ensure Safe Attachments for SharePoint, OneDrive, and Microsoft Teams is Enabled
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l2_ensure_safe_attachments_for_sharepoint
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_2.l1_ensure_exchange_online_spam_policies_are_set
    description: (L1) Ensure Exchange Online Spam Policies are set to notify administrators
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_exchange_online_spam_policies_are_set
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_2.l2_ensure_that_an_anti_phishing_policy_has_been
    description: (L2) Ensure that an anti-phishing policy has been created
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l2_ensure_that_an_anti_phishing_policy_has_been
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_2.l1_ensure_that_spf_records_are_published_for_all
    description: (L1) Ensure that SPF records are published for all Exchange Domains
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_that_spf_records_are_published_for_all
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_2.l1_ensure_that_dkim_is_enabled_for_all_exchange
    description: (L1) Ensure that DKIM is enabled for all Exchange Online Domains
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_that_dkim_is_enabled_for_all_exchange
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_2.l1_ensure_dmarc_records_for_all_exchange_online
    description: (L1) Ensure DMARC Records for all Exchange Online domains are published
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_dmarc_records_for_all_exchange_online
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_2.l2_ensure_comprehensive_attachment_filtering_is
    description: (L2) Ensure comprehensive attachment filtering is applied
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l2_ensure_comprehensive_attachment_filtering_is
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_2.l1_ensure_the_connection_filter_ip_allow_list_is
    description: (L1) Ensure the connection filter IP allow list is not used
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_the_connection_filter_ip_allow_list_is
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_2.l1_ensure_the_connection_filter_safe_list_is_off
    description: (L1) Ensure the connection filter safe list is off
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_the_connection_filter_safe_list_is_off
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_2.l1_ensure_inbound_anti_spam_policies_do_not
    description: (L1) Ensure inbound anti-spam policies do not contain allowed domains
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_inbound_anti_spam_policies_do_not
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_2.l1_ensure_priority_accounts_have_strict
    description: (L1) Ensure Priority accounts have 'Strict protection' presets applied
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_priority_accounts_have_strict
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_2.l1_ensure_zero_hour_auto_purge_for_microsoft
    description: (L1) Ensure Zero-hour auto purge for Microsoft Teams is on
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_zero_hour_auto_purge_for_microsoft
        value: "{{ result }}"
        expected: ""

