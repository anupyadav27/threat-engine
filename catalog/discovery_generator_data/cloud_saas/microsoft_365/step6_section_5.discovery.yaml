version: '1.0'
provider: cloud_saas
engine: microsoft_365
category: section_5
cis_section: '5'
description: >
  microsoft_365 CIS Section 5 — (L2) Ensure third party integrated applications are not allowed

discovery:

  - discovery_id: microsoft_365.section_5.l2_ensure_third_party_integrated_applications_are
    description: (L2) Ensure third party integrated applications are not allowed
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l2_ensure_third_party_integrated_applications_are
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_5.l1_ensure_a_dynamic_group_for_guest_users_is
    description: (L1) Ensure a dynamic group for guest users is created
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_a_dynamic_group_for_guest_users_is
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_5.l2_ensure_guest_user_invitations_are_limited_to
    description: (L2) Ensure guest user invitations are limited to the Guest Inviter role
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l2_ensure_guest_user_invitations_are_limited_to
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_5.l1_ensure_microsoft_authenticator_is_configured
    description: (L1) Ensure Microsoft Authenticator is configured to protect against MFA fatigue
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_microsoft_authenticator_is_configured
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_5.l1_ensure_custom_banned_passwords_lists_are_used
    description: (L1) Ensure custom banned passwords lists are used
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_custom_banned_passwords_lists_are_used
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_5.l1_ensure_password_protection_is_enabled_for_on
    description: (L1) Ensure password protection is enabled for on-prem Active Directory
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_password_protection_is_enabled_for_on
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_5.l1_ensure_all_member_users_are_mfa_capable
    description: (L1) Ensure all member users are 'MFA capable'
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_all_member_users_are_mfa_capable
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_5.l1_ensure_weak_authentication_methods_are_disabled
    description: (L1) Ensure weak authentication methods are disabled
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_weak_authentication_methods_are_disabled
        value: "{{ result }}"
        expected: ""

