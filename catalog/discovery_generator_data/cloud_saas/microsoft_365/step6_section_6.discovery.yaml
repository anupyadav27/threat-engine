version: '1.0'
provider: cloud_saas
engine: microsoft_365
category: section_6
cis_section: '6'
description: >
  microsoft_365 CIS Section 6 — (L1) Ensure all forms of mail forwarding are blocked and/or disabled

discovery:

  - discovery_id: microsoft_365.section_6.l1_ensure_all_forms_of_mail_forwarding_are
    description: (L1) Ensure all forms of mail forwarding are blocked and/or disabled
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_all_forms_of_mail_forwarding_are
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_6.l1_ensure_mail_transport_rules_do_not_whitelist
    description: (L1) Ensure mail transport rules do not whitelist specific domains
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_mail_transport_rules_do_not_whitelist
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_6.l1_ensure_email_from_external_senders_is
    description: (L1) Ensure email from external senders is identified
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_email_from_external_senders_is
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_6.l1_ensure_modern_authentication_for_exchange
    description: (L1) Ensure modern authentication for Exchange Online is enabled
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_modern_authentication_for_exchange
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_6.l1_ensure_mailtips_are_enabled_for_end_users
    description: (L1) Ensure MailTips are enabled for end users
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_mailtips_are_enabled_for_end_users
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_6.l2_ensure_additional_storage_providers_are
    description: (L2) Ensure additional storage providers are restricted in Outlook on the web
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l2_ensure_additional_storage_providers_are
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_6.l1_ensure_smtp_auth_is_disabled
    description: (L1) Ensure SMTP AUTH is disabled
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_smtp_auth_is_disabled
        value: "{{ result }}"
        expected: ""

