version: '1.0'
provider: cloud_saas
engine: microsoft_365
category: section_7
cis_section: '7'
description: >
  microsoft_365 CIS Section 7 — (L1) Ensure modern authentication for SharePoint applications is required

discovery:

  - discovery_id: microsoft_365.section_7.l1_ensure_modern_authentication_for_sharepoint
    description: (L1) Ensure modern authentication for SharePoint applications is required
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_modern_authentication_for_sharepoint
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_7.l1_ensure_external_content_sharing_is_restricted
    description: (L1) Ensure external content sharing is restricted
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_external_content_sharing_is_restricted
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_7.l2_ensure_onedrive_content_sharing_is_restricted
    description: (L2) Ensure OneDrive content sharing is restricted
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l2_ensure_onedrive_content_sharing_is_restricted
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_7.l2_ensure_that_sharepoint_guest_users_cannot
    description: (L2) Ensure that SharePoint guest users cannot share items they don't own
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l2_ensure_that_sharepoint_guest_users_cannot
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_7.l2_ensure_sharepoint_external_sharing_is_managed
    description: (L2) Ensure SharePoint external sharing is managed through domain whitelist/blacklists
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l2_ensure_sharepoint_external_sharing_is_managed
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_7.l1_ensure_link_sharing_is_restricted_in
    description: (L1) Ensure link sharing is restricted in SharePoint and OneDrive
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_link_sharing_is_restricted_in
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_7.l1_ensure_the_sharepoint_default_sharing_link
    description: (L1) Ensure the SharePoint default sharing link permission is set
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_the_sharepoint_default_sharing_link
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_7.l2_ensure_office_365_sharepoint_infected_files
    description: (L2) Ensure Office 365 SharePoint infected files are disallowed for download
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l2_ensure_office_365_sharepoint_infected_files
        value: "{{ result }}"
        expected: ""

  - discovery_id: microsoft_365.section_7.l2_ensure_onedrive_sync_is_restricted_for
    description: (L2) Ensure OneDrive sync is restricted for unmanaged devices
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l2_ensure_onedrive_sync_is_restricted_for
        value: "{{ result }}"
        expected: ""

