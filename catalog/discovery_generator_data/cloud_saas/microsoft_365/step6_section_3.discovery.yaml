version: '1.0'
provider: cloud_saas
engine: microsoft_365
category: section_3
cis_section: '3'
description: >
  microsoft_365 CIS Section 3 — (L1) Ensure DLP policies are enabled for Microsoft Teams

discovery:

  - discovery_id: microsoft_365.section_3.l1_ensure_dlp_policies_are_enabled_for_microsoft
    description: (L1) Ensure DLP policies are enabled for Microsoft Teams
    calls:
      - action: http_get
        endpoint: "# TODO: map to vendor API endpoint"
        auth_type: bearer_token
        base_url_var: MICROSOFT_365_API_BASE_URL
        save_as: result
    emit:
      as: item
      item:
        check: l1_ensure_dlp_policies_are_enabled_for_microsoft
        value: "{{ result }}"
        expected: ""

