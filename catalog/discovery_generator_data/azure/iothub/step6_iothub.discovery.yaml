# Discovery YAML — iothub (Azure)
# Generated: 2026-04-15T10:39:30Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: iothub
services:
  client: iothub
  module: azure.mgmt.iothub.v2019_07_01_preview
discovery:
  # ── iothubresource.list_by_resource_group [root] ──
  - discovery_id: azure.iothub.iothubresource.list_by_resource_group
    calls:
      - action: iothubresource.list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
        etag: '{{ item.etag }}'
  # ── iothubresource.list_by_subscription [root] ──
  - discovery_id: azure.iothub.iothubresource.list_by_subscription
    calls:
      - action: iothubresource.list_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
        etag: '{{ item.etag }}'
  # ── resourceprovidercommon.get_subscription_quota [root] ──
  - discovery_id: azure.iothub.resourceprovidercommon.get_subscription_quota
    calls:
      - action: resourceprovidercommon.get_subscription_quota
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        type: '{{ response.type }}'
        name: '{{ response.name }}'
        id: '{{ response.id }}'
        current_value: '{{ response.current_value }}'
        limit: '{{ response.limit }}'
        unit: '{{ response.unit }}'
  # ════════════════════════════════════════
  # Dependent operations (for_each + params)
  # ════════════════════════════════════════
  # ── certificates.list_by_iot_hub [dependent] ──
  - discovery_id: azure.iothub.certificates.list_by_iot_hub
    for_each: azure.iothub.iothubresource.list_by_subscription
    calls:
      - action: certificates.list_by_iot_hub
        save_as: response
        on_error: continue
        params:
          resource_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        etag: '{{ item.etag }}'
  # ── iothubresource.get_endpoint_health [dependent] ──
  - discovery_id: azure.iothub.iothubresource.get_endpoint_health
    for_each: azure.iothub.iothubresource.list_by_subscription
    calls:
      - action: iothubresource.get_endpoint_health
        save_as: response
        on_error: continue
        params:
          iot_hub_name: '{{ item.name }}'
    emit:
      as: item
      item:
        endpoint_id: '{{ response.endpoint_id }}'
        health_status: '{{ response.health_status }}'
  # ── iothubresource.get_event_hub_consumer_group [dependent] ──
  - discovery_id: azure.iothub.iothubresource.get_event_hub_consumer_group
    for_each: azure.iothub.iothubresource.list_by_subscription
    calls:
      - action: iothubresource.get_event_hub_consumer_group
        save_as: response
        on_error: continue
        params:
          resource_name: '{{ item.name }}'
          name: '{{ item.name }}'
    emit:
      as: item
      item:
        identity: '{{ response.identity }}'
        type: '{{ response.type }}'
        properties: '{{ response.properties }}'
        location: '{{ response.location }}'
        sku: '{{ response.sku }}'
        name: '{{ response.name }}'
        provisioning_state: '{{ response.provisioning_state }}'
        zones: '{{ response.zones }}'
        kind: '{{ response.kind }}'
        id: '{{ response.id }}'
        tags: '{{ response.tags }}'
  # ── iothubresource.get_job [dependent] ──
  - discovery_id: azure.iothub.iothubresource.get_job
    for_each: azure.iothub.iothubresource.list_by_subscription
    calls:
      - action: iothubresource.get_job
        save_as: response
        on_error: continue
        params:
          resource_name: '{{ item.name }}'
    emit:
      as: item
      item:
        identity: '{{ response.identity }}'
        type: '{{ response.type }}'
        properties: '{{ response.properties }}'
        location: '{{ response.location }}'
        sku: '{{ response.sku }}'
        name: '{{ response.name }}'
        provisioning_state: '{{ response.provisioning_state }}'
        zones: '{{ response.zones }}'
        kind: '{{ response.kind }}'
        id: '{{ response.id }}'
        tags: '{{ response.tags }}'
  # ── iothubresource.get_keys_for_key_name [dependent] ──
  - discovery_id: azure.iothub.iothubresource.get_keys_for_key_name
    for_each: azure.iothub.iothubresource.list_by_subscription
    calls:
      - action: iothubresource.get_keys_for_key_name
        save_as: response
        on_error: continue
        params:
          resource_name: '{{ item.name }}'
    emit:
      as: item
      item:
        identity: '{{ response.identity }}'
        type: '{{ response.type }}'
        properties: '{{ response.properties }}'
        location: '{{ response.location }}'
        sku: '{{ response.sku }}'
        name: '{{ response.name }}'
        provisioning_state: '{{ response.provisioning_state }}'
        zones: '{{ response.zones }}'
        kind: '{{ response.kind }}'
        id: '{{ response.id }}'
        tags: '{{ response.tags }}'
  # ── iothubresource.get_quota_metrics [dependent] ──
  - discovery_id: azure.iothub.iothubresource.get_quota_metrics
    for_each: azure.iothub.iothubresource.list_by_subscription
    calls:
      - action: iothubresource.get_quota_metrics
        save_as: response
        on_error: continue
        params:
          resource_name: '{{ item.name }}'
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        current_value: '{{ response.current_value }}'
        max_value: '{{ response.max_value }}'
  # ── iothubresource.get_stats [dependent] ──
  - discovery_id: azure.iothub.iothubresource.get_stats
    for_each: azure.iothub.iothubresource.list_by_subscription
    calls:
      - action: iothubresource.get_stats
        save_as: response
        on_error: continue
        params:
          resource_name: '{{ item.name }}'
    emit:
      as: item
      item:
        identity: '{{ response.identity }}'
        type: '{{ response.type }}'
        properties: '{{ response.properties }}'
        location: '{{ response.location }}'
        sku: '{{ response.sku }}'
        name: '{{ response.name }}'
        provisioning_state: '{{ response.provisioning_state }}'
        zones: '{{ response.zones }}'
        kind: '{{ response.kind }}'
        id: '{{ response.id }}'
        tags: '{{ response.tags }}'
  # ── iothubresource.get_valid_skus [dependent] ──
  - discovery_id: azure.iothub.iothubresource.get_valid_skus
    for_each: azure.iothub.iothubresource.list_by_subscription
    calls:
      - action: iothubresource.get_valid_skus
        save_as: response
        on_error: continue
        params:
          resource_name: '{{ item.name }}'
    emit:
      as: item
      item:
        sku: '{{ response.sku }}'
        capacity: '{{ response.capacity }}'
        resource_type: '{{ response.resource_type }}'
  # ── iothubresource.list_event_hub_consumer_groups [dependent] ──
  - discovery_id: azure.iothub.iothubresource.list_event_hub_consumer_groups
    for_each: azure.iothub.iothubresource.list_by_subscription
    calls:
      - action: iothubresource.list_event_hub_consumer_groups
        save_as: response
        on_error: continue
        params:
          resource_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        etag: '{{ item.etag }}'
  # ── iothubresource.list_jobs [dependent] ──
  - discovery_id: azure.iothub.iothubresource.list_jobs
    for_each: azure.iothub.iothubresource.list_by_subscription
    calls:
      - action: iothubresource.list_jobs
        save_as: response
        on_error: continue
        params:
          resource_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        id: '{{ item.id }}'
        end_time_utc: '{{ item.end_time_utc }}'
        failure_reason: '{{ item.failure_reason }}'
        job_id: '{{ item.job_id }}'
        parent_job_id: '{{ item.parent_job_id }}'
        start_time_utc: '{{ item.start_time_utc }}'
        status_message: '{{ item.status_message }}'
  # ── iothubresource.list_keys [dependent] ──
  - discovery_id: azure.iothub.iothubresource.list_keys
    for_each: azure.iothub.iothubresource.list_by_subscription
    calls:
      - action: iothubresource.list_keys
        save_as: response
        on_error: continue
        params:
          resource_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        key_name: '{{ item.key_name }}'
        primary_key: '{{ item.primary_key }}'
        rights: '{{ item.rights }}'
        secondary_key: '{{ item.secondary_key }}'
