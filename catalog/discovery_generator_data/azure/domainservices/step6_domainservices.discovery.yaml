# Discovery YAML — domainservices (Azure)
# Generated: 2026-04-15T10:39:30Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: domainservices
services:
  client: domainservices
  module: azure.mgmt.domainservices
discovery:
  # ── domainserviceoperations.DomainServiceOperations_List [root] ──
  - discovery_id: azure.domainservices.domainserviceoperations.domainserviceoperations_list
    calls:
      - action: domainserviceoperations.DomainServiceOperations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── domainservices.DomainServices_List [root] ──
  - discovery_id: azure.domainservices.domainservices.domainservices_list
    calls:
      - action: domainservices.DomainServices_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── oucontaineroperations.OuContainerOperations_List [root] ──
  - discovery_id: azure.domainservices.oucontaineroperations.oucontaineroperations_list
    calls:
      - action: oucontaineroperations.OuContainerOperations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.domainservices.domainservices.domainservices_get — required_params: ['id']
  # SKIP: azure.domainservices.oucontainer.oucontainer_get — required_params: ['id']
