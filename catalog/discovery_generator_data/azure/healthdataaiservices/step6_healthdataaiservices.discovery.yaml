# Discovery YAML — healthdataaiservices (Azure)
# Generated: 2026-04-15T10:39:30Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: healthdataaiservices
services:
  client: healthdataaiservices
  module: azure.mgmt.healthdataaiservices
discovery:
  # ── operations.Operations_List [root] ──
  - discovery_id: azure.healthdataaiservices.operations.operations_list
    calls:
      - action: operations.Operations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.healthdataaiservices.deidservices.deidservices_get — required_params: ['id']
  # SKIP: azure.healthdataaiservices.privateendpointconnections.privateendpointconnections_get — required_params: ['id']
