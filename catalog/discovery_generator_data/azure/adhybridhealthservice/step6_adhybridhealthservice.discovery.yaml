# Discovery YAML — adhybridhealthservice (Azure)
# Generated: 2026-04-15T10:39:29Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: adhybridhealthservice
services:
  client: adhybridhealthservice
  module: azure.mgmt.adhybridhealthservice
discovery:
  # ── addsservices.addsServices_list [root] ──
  - discovery_id: azure.adhybridhealthservice.addsservices.addsservices_list
    calls:
      - action: addsservices.addsServices_list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── operations.operations_list [root] ──
  - discovery_id: azure.adhybridhealthservice.operations.operations_list
    calls:
      - action: operations.operations_list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── services.services_list [root] ──
  - discovery_id: azure.adhybridhealthservice.services.services_list
    calls:
      - action: services.services_list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.adhybridhealthservice.addsservicemembers.addsservicemembers_get — required_params: ['id']
  # SKIP: azure.adhybridhealthservice.addsservices.addsservices_get — required_params: ['id']
  # SKIP: azure.adhybridhealthservice.addsservicesreplicationstatus.addsservicesreplicationstatus_get — required_params: ['id']
  # SKIP: azure.adhybridhealthservice.addsservicesuserpreference.addsservicesuserpreference_get — required_params: ['id']
  # SKIP: azure.adhybridhealthservice.configuration.configuration_get — required_params: ['id']
  # SKIP: azure.adhybridhealthservice.servicemembers.servicemembers_get — required_params: ['id']
  # SKIP: azure.adhybridhealthservice.services.services_get — required_params: ['id']
