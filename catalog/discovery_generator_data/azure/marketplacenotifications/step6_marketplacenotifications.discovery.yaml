# Discovery YAML — marketplacenotifications (Azure)
# Generated: 2026-04-15T10:39:30Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: marketplacenotifications
services:
  client: marketplacenotifications
  module: azure.mgmt.marketplacenotifications
discovery:

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.marketplacenotifications.notification.notification_get — required_params: ['id']
