# Discovery YAML — automanage (Azure)
# Generated: 2026-04-15T10:39:29Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: automanage
services:
  client: automanage
  module: azure.mgmt.automanage
discovery:
  # ── configurationprofileassignments.ConfigurationProfileAssignments_List [root] ──
  - discovery_id: azure.automanage.configurationprofileassignments.configurationprofileassignments_list
    calls:
      - action: configurationprofileassignments.ConfigurationProfileAssignments_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── operations.Operations_List [root] ──
  - discovery_id: azure.automanage.operations.operations_list
    calls:
      - action: operations.Operations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.automanage.bestpractices.bestpractices_get — required_params: ['id']
  # SKIP: azure.automanage.bestpracticesversions.bestpracticesversions_get — required_params: ['id']
  # SKIP: azure.automanage.configurationprofileassignments.configurationprofileassignments_get — required_params: ['id']
  # SKIP: azure.automanage.configurationprofilehciassignments.configurationprofilehciassignments_get — required_params: ['id']
  # SKIP: azure.automanage.configurationprofilehcrpassignments.configurationprofilehcrpassignments_get — required_params: ['id']
  # SKIP: azure.automanage.configurationprofiles.configurationprofiles_get — required_params: ['id']
  # SKIP: azure.automanage.configurationprofilesversions.configurationprofilesversions_get — required_params: ['id']
  # SKIP: azure.automanage.hcireports.hcireports_get — required_params: ['id']
  # SKIP: azure.automanage.hcrpreports.hcrpreports_get — required_params: ['id']
  # SKIP: azure.automanage.reports.reports_get — required_params: ['id']
  # SKIP: azure.automanage.serviceprincipals.serviceprincipals_get — required_params: ['id']
