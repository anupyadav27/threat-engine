# Discovery YAML — recoveryservicessiterecovery (Azure)
# Generated: 2026-04-15T10:39:31Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: recoveryservicessiterecovery
services:
  client: recoveryservicessiterecovery
  module: azure.mgmt.recoveryservicessiterecovery
discovery:
  # ── replicationalertsettings.list [root] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationalertsettings.list
    calls:
      - action: replicationalertsettings.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationappliances.list [root] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationappliances.list
    calls:
      - action: replicationappliances.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationevents.list [root] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationevents.list
    calls:
      - action: replicationevents.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationfabrics.list [root] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationfabrics.list
    calls:
      - action: replicationfabrics.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationjobs.list [root] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationjobs.list
    calls:
      - action: replicationjobs.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationmigrationitems.list [root] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationmigrationitems.list
    calls:
      - action: replicationmigrationitems.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationnetworkmappings.list [root] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationnetworkmappings.list
    calls:
      - action: replicationnetworkmappings.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationnetworks.list [root] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationnetworks.list
    calls:
      - action: replicationnetworks.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationpolicies.list [root] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationpolicies.list
    calls:
      - action: replicationpolicies.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationprotecteditems.list [root] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationprotecteditems.list
    calls:
      - action: replicationprotecteditems.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationprotectioncontainermappings.list [root] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationprotectioncontainermappings.list
    calls:
      - action: replicationprotectioncontainermappings.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationprotectioncontainers.list [root] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationprotectioncontainers.list
    calls:
      - action: replicationprotectioncontainers.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationprotectionintents.list [root] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationprotectionintents.list
    calls:
      - action: replicationprotectionintents.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationrecoveryplans.list [root] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationrecoveryplans.list
    calls:
      - action: replicationrecoveryplans.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationrecoveryservicesproviders.list [root] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationrecoveryservicesproviders.list
    calls:
      - action: replicationrecoveryservicesproviders.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationstorageclassificationmappings.list [root] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationstorageclassificationmappings.list
    calls:
      - action: replicationstorageclassificationmappings.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationstorageclassifications.list [root] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationstorageclassifications.list
    calls:
      - action: replicationstorageclassifications.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationvaultsetting.list [root] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationvaultsetting.list
    calls:
      - action: replicationvaultsetting.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationvcenters.list [root] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationvcenters.list
    calls:
      - action: replicationvcenters.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ════════════════════════════════════════
  # Dependent operations (for_each + params)
  # ════════════════════════════════════════
  # ── clusterrecoverypoints.list_by_replication_protection_cluster [dependent] ──
  - discovery_id: azure.recoveryservicessiterecovery.clusterrecoverypoints.list_by_replication_protection_cluster
    for_each: azure.recoveryservicessiterecovery.replicationprotectionclusters.list
    calls:
      - action: clusterrecoverypoints.list_by_replication_protection_cluster
        save_as: response
        on_error: continue
        params:
          resource_name: '{{ item.name }}'
          fabric_name: '{{ item.name }}'
          protection_container_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── migrationrecoverypoints.list_by_replication_migration_items [dependent] ──
  - discovery_id: azure.recoveryservicessiterecovery.migrationrecoverypoints.list_by_replication_migration_items
    for_each: azure.recoveryservicessiterecovery.replicationfabrics.list
    calls:
      - action: migrationrecoverypoints.list_by_replication_migration_items
        save_as: response
        on_error: continue
        params:
          fabric_name: '{{ item.name }}'
          protection_container_name: '{{ item.name }}'
          migration_item_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── recoverypoints.list_by_replication_protected_items [dependent] ──
  - discovery_id: azure.recoveryservicessiterecovery.recoverypoints.list_by_replication_protected_items
    for_each: azure.recoveryservicessiterecovery.replicationfabrics.list
    calls:
      - action: recoverypoints.list_by_replication_protected_items
        save_as: response
        on_error: continue
        params:
          fabric_name: '{{ item.name }}'
          protection_container_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationlogicalnetworks.list_by_replication_fabrics [dependent] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationlogicalnetworks.list_by_replication_fabrics
    for_each: azure.recoveryservicessiterecovery.replicationfabrics.list
    calls:
      - action: replicationlogicalnetworks.list_by_replication_fabrics
        save_as: response
        on_error: continue
        params:
          fabric_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationmigrationitems.list_by_replication_protection_containers [dependent] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationmigrationitems.list_by_replication_protection_containers
    for_each: azure.recoveryservicessiterecovery.replicationfabrics.list
    calls:
      - action: replicationmigrationitems.list_by_replication_protection_containers
        save_as: response
        on_error: continue
        params:
          fabric_name: '{{ item.name }}'
          protection_container_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationnetworkmappings.list_by_replication_networks [dependent] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationnetworkmappings.list_by_replication_networks
    for_each: azure.recoveryservicessiterecovery.replicationfabrics.list
    calls:
      - action: replicationnetworkmappings.list_by_replication_networks
        save_as: response
        on_error: continue
        params:
          fabric_name: '{{ item.name }}'
          network_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationnetworks.list_by_replication_fabrics [dependent] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationnetworks.list_by_replication_fabrics
    for_each: azure.recoveryservicessiterecovery.replicationfabrics.list
    calls:
      - action: replicationnetworks.list_by_replication_fabrics
        save_as: response
        on_error: continue
        params:
          fabric_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationprotectableitems.list_by_replication_protection_containers [dependent] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationprotectableitems.list_by_replication_protection_containers
    for_each: azure.recoveryservicessiterecovery.replicationfabrics.list
    calls:
      - action: replicationprotectableitems.list_by_replication_protection_containers
        save_as: response
        on_error: continue
        params:
          fabric_name: '{{ item.name }}'
          protection_container_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationprotecteditems.list_by_replication_protection_containers [dependent] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationprotecteditems.list_by_replication_protection_containers
    for_each: azure.recoveryservicessiterecovery.replicationfabrics.list
    calls:
      - action: replicationprotecteditems.list_by_replication_protection_containers
        save_as: response
        on_error: continue
        params:
          fabric_name: '{{ item.name }}'
          protection_container_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationprotectionclusters.get_operation_results [dependent] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationprotectionclusters.get_operation_results
    for_each: azure.recoveryservicessiterecovery.replicationprotectionclusters.list
    calls:
      - action: replicationprotectionclusters.get_operation_results
        save_as: response
        on_error: continue
        params:
          resource_name: '{{ item.name }}'
          fabric_name: '{{ item.name }}'
          protection_container_name: '{{ item.name }}'
          job_id: '{{ item.id }}'
    emit:
      as: item
      item:
        identity: '{{ response.identity }}'
        type: '{{ response.type }}'
        properties: '{{ response.properties }}'
        location: '{{ response.location }}'
        sku: '{{ response.sku }}'
        name: '{{ response.name }}'
        provisioning_state: '{{ response.provisioning_state }}'
        zones: '{{ response.zones }}'
        kind: '{{ response.kind }}'
        id: '{{ response.id }}'
        tags: '{{ response.tags }}'
  # ── replicationprotectionclusters.list_by_replication_protection_containers [dependent] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationprotectionclusters.list_by_replication_protection_containers
    for_each: azure.recoveryservicessiterecovery.replicationprotectionclusters.list
    calls:
      - action: replicationprotectionclusters.list_by_replication_protection_containers
        save_as: response
        on_error: continue
        params:
          resource_name: '{{ item.name }}'
          fabric_name: '{{ item.name }}'
          protection_container_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationprotectioncontainermappings.list_by_replication_protection_containers [dependent] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationprotectioncontainermappings.list_by_replication_protection_containers
    for_each: azure.recoveryservicessiterecovery.replicationfabrics.list
    calls:
      - action: replicationprotectioncontainermappings.list_by_replication_protection_containers
        save_as: response
        on_error: continue
        params:
          fabric_name: '{{ item.name }}'
          protection_container_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationprotectioncontainers.list_by_replication_fabrics [dependent] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationprotectioncontainers.list_by_replication_fabrics
    for_each: azure.recoveryservicessiterecovery.replicationfabrics.list
    calls:
      - action: replicationprotectioncontainers.list_by_replication_fabrics
        save_as: response
        on_error: continue
        params:
          fabric_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationrecoveryservicesproviders.list_by_replication_fabrics [dependent] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationrecoveryservicesproviders.list_by_replication_fabrics
    for_each: azure.recoveryservicessiterecovery.replicationfabrics.list
    calls:
      - action: replicationrecoveryservicesproviders.list_by_replication_fabrics
        save_as: response
        on_error: continue
        params:
          fabric_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationstorageclassificationmappings.list_by_replication_storage_classifications [dependent] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationstorageclassificationmappings.list_by_replication_storage_classifications
    for_each: azure.recoveryservicessiterecovery.replicationfabrics.list
    calls:
      - action: replicationstorageclassificationmappings.list_by_replication_storage_classifications
        save_as: response
        on_error: continue
        params:
          fabric_name: '{{ item.name }}'
          storage_classification_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationstorageclassifications.list_by_replication_fabrics [dependent] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationstorageclassifications.list_by_replication_fabrics
    for_each: azure.recoveryservicessiterecovery.replicationfabrics.list
    calls:
      - action: replicationstorageclassifications.list_by_replication_fabrics
        save_as: response
        on_error: continue
        params:
          fabric_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── replicationvcenters.list_by_replication_fabrics [dependent] ──
  - discovery_id: azure.recoveryservicessiterecovery.replicationvcenters.list_by_replication_fabrics
    for_each: azure.recoveryservicessiterecovery.replicationfabrics.list
    calls:
      - action: replicationvcenters.list_by_replication_fabrics
        save_as: response
        on_error: continue
        params:
          fabric_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── targetcomputesizes.list_by_replication_protected_items [dependent] ──
  - discovery_id: azure.recoveryservicessiterecovery.targetcomputesizes.list_by_replication_protected_items
    for_each: azure.recoveryservicessiterecovery.replicationfabrics.list
    calls:
      - action: targetcomputesizes.list_by_replication_protected_items
        save_as: response
        on_error: continue
        params:
          fabric_name: '{{ item.name }}'
          protection_container_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.recoveryservicessiterecovery.replicationeligibilityresults.list — required_params: ['virtual_machine_name']
  # SKIP: azure.recoveryservicessiterecovery.replicationprotectionclusters.list — required_params: ['resource_name']
