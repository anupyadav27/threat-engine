# Discovery YAML — quota (Azure)
# Generated: 2026-04-15T10:39:31Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: quota
services:
  client: quota
  module: azure.mgmt.quota
discovery:
  # ── quotaoperation.QuotaOperation_List [root] ──
  - discovery_id: azure.quota.quotaoperation.quotaoperation_list
    calls:
      - action: quotaoperation.QuotaOperation_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.quota.groupquotalimitsrequest.groupquotalimitsrequest_get — required_params: ['id']
  # SKIP: azure.quota.groupquotalocationsettings.groupquotalocationsettings_get — required_params: ['id']
  # SKIP: azure.quota.groupquotas.groupquotas_get — required_params: ['id']
  # SKIP: azure.quota.groupquotasubscriptionallocationrequest.groupquotasubscriptionallocationrequest_get — required_params: ['id']
  # SKIP: azure.quota.groupquotasubscriptionrequests.groupquotasubscriptionrequests_get — required_params: ['id']
  # SKIP: azure.quota.groupquotasubscriptions.groupquotasubscriptions_get — required_params: ['id']
  # SKIP: azure.quota.quota.quota_get — required_params: ['id']
  # SKIP: azure.quota.quotarequeststatus.quotarequeststatus_get — required_params: ['id']
  # SKIP: azure.quota.usages.usages_get — required_params: ['id']
