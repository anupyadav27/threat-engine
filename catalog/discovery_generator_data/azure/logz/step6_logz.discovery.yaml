# Discovery YAML — logz (Azure)
# Generated: 2026-04-15T10:39:30Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: logz
services:
  client: logz
  module: azure.mgmt.logz
discovery:
  # ── monitors.list_by_resource_group [root] ──
  - discovery_id: azure.logz.monitors.list_by_resource_group
    calls:
      - action: monitors.list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
        system_data: '{{ item.system_data }}'
  # ── monitors.list_by_subscription [root] ──
  - discovery_id: azure.logz.monitors.list_by_subscription
    calls:
      - action: monitors.list_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
        system_data: '{{ item.system_data }}'
  # ════════════════════════════════════════
  # Dependent operations (for_each + params)
  # ════════════════════════════════════════
  # ── monitor.list_vm_host_update [dependent] ──
  - discovery_id: azure.logz.monitor.list_vm_host_update
    for_each: azure.logz.monitors.list_by_subscription
    calls:
      - action: monitor.list_vm_host_update
        save_as: response
        on_error: continue
        params:
          monitor_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        agent_version: '{{ item.agent_version }}'
  # ── monitor.list_vm_hosts [dependent] ──
  - discovery_id: azure.logz.monitor.list_vm_hosts
    for_each: azure.logz.monitors.list_by_subscription
    calls:
      - action: monitor.list_vm_hosts
        save_as: response
        on_error: continue
        params:
          monitor_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        agent_version: '{{ item.agent_version }}'
  # ── monitors.list_monitored_resources [dependent] ──
  - discovery_id: azure.logz.monitors.list_monitored_resources
    for_each: azure.logz.monitors.list_by_subscription
    calls:
      - action: monitors.list_monitored_resources
        save_as: response
        on_error: continue
        params:
          monitor_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        reason_for_logs_status: '{{ item.reason_for_logs_status }}'
        reason_for_metrics_status: '{{ item.reason_for_metrics_status }}'
        sending_logs: '{{ item.sending_logs }}'
        sending_metrics: '{{ item.sending_metrics }}'
        system_data: '{{ item.system_data }}'
  # ── monitors.list_user_roles [dependent] ──
  - discovery_id: azure.logz.monitors.list_user_roles
    for_each: azure.logz.monitors.list_by_subscription
    calls:
      - action: monitors.list_user_roles
        save_as: response
        on_error: continue
        params:
          monitor_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        role: '{{ item.role }}'
  # ── singlesignon.list [dependent] ──
  - discovery_id: azure.logz.singlesignon.list
    for_each: azure.logz.monitors.list_by_subscription
    calls:
      - action: singlesignon.list
        save_as: response
        on_error: continue
        params:
          monitor_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── subaccount.list [dependent] ──
  - discovery_id: azure.logz.subaccount.list
    for_each: azure.logz.monitors.list_by_subscription
    calls:
      - action: subaccount.list
        save_as: response
        on_error: continue
        params:
          monitor_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── subaccount.list_monitored_resources [dependent] ──
  - discovery_id: azure.logz.subaccount.list_monitored_resources
    for_each: azure.logz.monitors.list_by_subscription
    calls:
      - action: subaccount.list_monitored_resources
        save_as: response
        on_error: continue
        params:
          monitor_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        reason_for_logs_status: '{{ item.reason_for_logs_status }}'
        reason_for_metrics_status: '{{ item.reason_for_metrics_status }}'
        sending_logs: '{{ item.sending_logs }}'
        sending_metrics: '{{ item.sending_metrics }}'
        system_data: '{{ item.system_data }}'
  # ── subaccount.list_vm_host_update [dependent] ──
  - discovery_id: azure.logz.subaccount.list_vm_host_update
    for_each: azure.logz.monitors.list_by_subscription
    calls:
      - action: subaccount.list_vm_host_update
        save_as: response
        on_error: continue
        params:
          monitor_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        agent_version: '{{ item.agent_version }}'
  # ── subaccount.list_vm_hosts [dependent] ──
  - discovery_id: azure.logz.subaccount.list_vm_hosts
    for_each: azure.logz.monitors.list_by_subscription
    calls:
      - action: subaccount.list_vm_hosts
        save_as: response
        on_error: continue
        params:
          monitor_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        agent_version: '{{ item.agent_version }}'
  # ── subaccounttagrules.list [dependent] ──
  - discovery_id: azure.logz.subaccounttagrules.list
    for_each: azure.logz.monitors.list_by_subscription
    calls:
      - action: subaccounttagrules.list
        save_as: response
        on_error: continue
        params:
          monitor_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── tagrules.list [dependent] ──
  - discovery_id: azure.logz.tagrules.list
    for_each: azure.logz.monitors.list_by_subscription
    calls:
      - action: tagrules.list
        save_as: response
        on_error: continue
        params:
          monitor_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
