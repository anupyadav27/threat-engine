# Discovery YAML — storageimportexport (Azure)
# Generated: 2026-04-15T10:39:31Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: storageimportexport
services:
  client: storageimportexport
  module: azure.mgmt.storageimportexport
discovery:
  # ── jobs.list_by_resource_group [root] ──
  - discovery_id: azure.storageimportexport.jobs.list_by_resource_group
    calls:
      - action: jobs.list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── jobs.list_by_subscription [root] ──
  - discovery_id: azure.storageimportexport.jobs.list_by_subscription
    calls:
      - action: jobs.list_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── locations.list [root] ──
  - discovery_id: azure.storageimportexport.locations.list
    calls:
      - action: locations.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        description: '{{ item.description }}'
        operation: '{{ item.operation }}'
        provider: '{{ item.provider }}'
        resource: '{{ item.resource }}'
  # ════════════════════════════════════════
  # Dependent operations (for_each + params)
  # ════════════════════════════════════════
  # ── bitlockerkeys.list [dependent] ──
  - discovery_id: azure.storageimportexport.bitlockerkeys.list
    for_each: azure.storageimportexport.jobs.list_by_subscription
    calls:
      - action: bitlockerkeys.list
        save_as: response
        on_error: continue
        params:
          job_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        description: '{{ item.description }}'
        operation: '{{ item.operation }}'
        provider: '{{ item.provider }}'
        resource: '{{ item.resource }}'
