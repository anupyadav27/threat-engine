# Discovery YAML — labservices (Azure)
# Generated: 2026-04-15T10:39:30Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: labservices
services:
  client: labservices
  module: azure.mgmt.labservices
discovery:
  # ── labplans.list_by_resource_group [root] ──
  - discovery_id: azure.labservices.labplans.list_by_resource_group
    calls:
      - action: labplans.list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
        auto_shutdown_profile: '{{ item.auto_shutdown_profile }}'
        connection_profile: '{{ item.connection_profile }}'
        description: '{{ item.description }}'
        lab_plan_id: '{{ item.lab_plan_id }}'
        network_profile: '{{ item.network_profile }}'
        roster_profile: '{{ item.roster_profile }}'
        security_profile: '{{ item.security_profile }}'
        state: '{{ item.state }}'
        system_data: '{{ item.system_data }}'
        title: '{{ item.title }}'
        virtual_machine_profile: '{{ item.virtual_machine_profile }}'
  # ── labplans.list_by_subscription [root] ──
  - discovery_id: azure.labservices.labplans.list_by_subscription
    calls:
      - action: labplans.list_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
        auto_shutdown_profile: '{{ item.auto_shutdown_profile }}'
        connection_profile: '{{ item.connection_profile }}'
        description: '{{ item.description }}'
        lab_plan_id: '{{ item.lab_plan_id }}'
        network_profile: '{{ item.network_profile }}'
        roster_profile: '{{ item.roster_profile }}'
        security_profile: '{{ item.security_profile }}'
        state: '{{ item.state }}'
        system_data: '{{ item.system_data }}'
        title: '{{ item.title }}'
        virtual_machine_profile: '{{ item.virtual_machine_profile }}'
  # ── labs.list_by_resource_group [root] ──
  - discovery_id: azure.labservices.labs.list_by_resource_group
    calls:
      - action: labs.list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
        auto_shutdown_profile: '{{ item.auto_shutdown_profile }}'
        connection_profile: '{{ item.connection_profile }}'
        description: '{{ item.description }}'
        lab_plan_id: '{{ item.lab_plan_id }}'
        network_profile: '{{ item.network_profile }}'
        roster_profile: '{{ item.roster_profile }}'
        security_profile: '{{ item.security_profile }}'
        state: '{{ item.state }}'
        system_data: '{{ item.system_data }}'
        title: '{{ item.title }}'
        virtual_machine_profile: '{{ item.virtual_machine_profile }}'
  # ── labs.list_by_subscription [root] ──
  - discovery_id: azure.labservices.labs.list_by_subscription
    calls:
      - action: labs.list_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
        auto_shutdown_profile: '{{ item.auto_shutdown_profile }}'
        connection_profile: '{{ item.connection_profile }}'
        description: '{{ item.description }}'
        lab_plan_id: '{{ item.lab_plan_id }}'
        network_profile: '{{ item.network_profile }}'
        roster_profile: '{{ item.roster_profile }}'
        security_profile: '{{ item.security_profile }}'
        state: '{{ item.state }}'
        system_data: '{{ item.system_data }}'
        title: '{{ item.title }}'
        virtual_machine_profile: '{{ item.virtual_machine_profile }}'
  # ── skus.list [root] ──
  - discovery_id: azure.labservices.skus.list
    calls:
      - action: skus.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        capabilities: '{{ item.capabilities }}'
        capacity: '{{ item.capacity }}'
        costs: '{{ item.costs }}'
        family: '{{ item.family }}'
        locations: '{{ item.locations }}'
        resource_type: '{{ item.resource_type }}'
        restrictions: '{{ item.restrictions }}'
        size: '{{ item.size }}'
        tier: '{{ item.tier }}'
  # ════════════════════════════════════════
  # Dependent operations (for_each + params)
  # ════════════════════════════════════════
  # ── images.list_by_lab_plan [dependent] ──
  - discovery_id: azure.labservices.images.list_by_lab_plan
    for_each: azure.labservices.labplans.list_by_subscription
    calls:
      - action: images.list_by_lab_plan
        save_as: response
        on_error: continue
        params:
          lab_plan_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        id: '{{ item.id }}'
        author: '{{ item.author }}'
        available_regions: '{{ item.available_regions }}'
        description: '{{ item.description }}'
        display_name: '{{ item.display_name }}'
        enabled_state: '{{ item.enabled_state }}'
        icon_url: '{{ item.icon_url }}'
        offer: '{{ item.offer }}'
        os_state: '{{ item.os_state }}'
        os_type: '{{ item.os_type }}'
        plan: '{{ item.plan }}'
        publisher: '{{ item.publisher }}'
        shared_gallery_id: '{{ item.shared_gallery_id }}'
        system_data: '{{ item.system_data }}'
        terms_status: '{{ item.terms_status }}'
        version: '{{ item.version }}'
  # ── schedules.list_by_lab [dependent] ──
  - discovery_id: azure.labservices.schedules.list_by_lab
    for_each: azure.labservices.labs.list_by_subscription
    calls:
      - action: schedules.list_by_lab
        save_as: response
        on_error: continue
        params:
          lab_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        id: '{{ item.id }}'
        claimed_by_user_id: '{{ item.claimed_by_user_id }}'
        connection_profile: '{{ item.connection_profile }}'
        state: '{{ item.state }}'
        system_data: '{{ item.system_data }}'
        vm_type: '{{ item.vm_type }}'
  # ── users.list_by_lab [dependent] ──
  - discovery_id: azure.labservices.users.list_by_lab
    for_each: azure.labservices.labs.list_by_subscription
    calls:
      - action: users.list_by_lab
        save_as: response
        on_error: continue
        params:
          lab_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        id: '{{ item.id }}'
        claimed_by_user_id: '{{ item.claimed_by_user_id }}'
        connection_profile: '{{ item.connection_profile }}'
        state: '{{ item.state }}'
        system_data: '{{ item.system_data }}'
        vm_type: '{{ item.vm_type }}'
  # ── virtualmachines.list_by_lab [dependent] ──
  - discovery_id: azure.labservices.virtualmachines.list_by_lab
    for_each: azure.labservices.labs.list_by_subscription
    calls:
      - action: virtualmachines.list_by_lab
        save_as: response
        on_error: continue
        params:
          lab_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        id: '{{ item.id }}'
        claimed_by_user_id: '{{ item.claimed_by_user_id }}'
        connection_profile: '{{ item.connection_profile }}'
        state: '{{ item.state }}'
        system_data: '{{ item.system_data }}'
        vm_type: '{{ item.vm_type }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.labservices.usages.list_by_location — required_params: ['location']
