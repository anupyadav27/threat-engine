# Discovery YAML — resourcemover (Azure)
# Generated: 2026-04-15T10:39:31Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: resourcemover
services:
  client: resourcemover
  module: azure.mgmt.resourcemover
discovery:

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.resourcemover.movecollections.movecollections_get — required_params: ['id']
  # SKIP: azure.resourcemover.moveresources.moveresources_get — required_params: ['id']
  # SKIP: azure.resourcemover.operationsdiscovery.operationsdiscovery_get — required_params: ['id']
  # SKIP: azure.resourcemover.unresolveddependencies.unresolveddependencies_get — required_params: ['id']
