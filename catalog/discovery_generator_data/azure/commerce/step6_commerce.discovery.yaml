# Discovery YAML — commerce (Azure)
# Generated: 2026-04-15T10:39:29Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: commerce
services:
  client: commerce
  module: azure.mgmt.commerce
discovery:

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.commerce.usageaggregates.list — required_params: ['reported_start_time', 'reported_end_time']
