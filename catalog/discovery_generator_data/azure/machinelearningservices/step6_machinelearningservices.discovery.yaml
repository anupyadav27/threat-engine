# Discovery YAML — machinelearningservices (Azure)
# Generated: 2026-04-15T10:39:30Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: machinelearningservices
services:
  client: machinelearningservices
  module: azure.mgmt.machinelearningservices
discovery:
  # ── azuremachinelearningworkspacesmixin.list_skus [root] ──
  - discovery_id: azure.machinelearningservices.azuremachinelearningworkspacesmixin.list_skus
    calls:
      - action: azuremachinelearningworkspacesmixin.list_skus
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── workspaces.list_by_resource_group [root] ──
  - discovery_id: azure.machinelearningservices.workspaces.list_by_resource_group
    calls:
      - action: workspaces.list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── workspaces.list_by_subscription [root] ──
  - discovery_id: azure.machinelearningservices.workspaces.list_by_subscription
    calls:
      - action: workspaces.list_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ════════════════════════════════════════
  # Dependent operations (for_each + params)
  # ════════════════════════════════════════
  # ── machinelearningcompute.list_by_workspace [dependent] ──
  - discovery_id: azure.machinelearningservices.machinelearningcompute.list_by_workspace
    for_each: azure.machinelearningservices.privatelinkresources.list_by_workspace
    calls:
      - action: machinelearningcompute.list_by_workspace
        save_as: response
        on_error: continue
        params:
          workspace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
        group_id: '{{ item.group_id }}'
        required_members: '{{ item.required_members }}'
        required_zone_names: '{{ item.required_zone_names }}'
  # ── machinelearningcompute.list_keys [dependent] ──
  - discovery_id: azure.machinelearningservices.machinelearningcompute.list_keys
    for_each: azure.machinelearningservices.privatelinkresources.list_by_workspace
    calls:
      - action: machinelearningcompute.list_keys
        save_as: response
        on_error: continue
        params:
          workspace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── machinelearningcompute.list_nodes [dependent] ──
  - discovery_id: azure.machinelearningservices.machinelearningcompute.list_nodes
    for_each: azure.machinelearningservices.privatelinkresources.list_by_workspace
    calls:
      - action: machinelearningcompute.list_nodes
        save_as: response
        on_error: continue
        params:
          workspace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── workspaceconnections.list [dependent] ──
  - discovery_id: azure.machinelearningservices.workspaceconnections.list
    for_each: azure.machinelearningservices.privatelinkresources.list_by_workspace
    calls:
      - action: workspaceconnections.list
        save_as: response
        on_error: continue
        params:
          workspace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── workspacefeatures.list [dependent] ──
  - discovery_id: azure.machinelearningservices.workspacefeatures.list
    for_each: azure.machinelearningservices.privatelinkresources.list_by_workspace
    calls:
      - action: workspacefeatures.list
        save_as: response
        on_error: continue
        params:
          workspace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── workspaces.list_keys [dependent] ──
  - discovery_id: azure.machinelearningservices.workspaces.list_keys
    for_each: azure.machinelearningservices.privatelinkresources.list_by_workspace
    calls:
      - action: workspaces.list_keys
        save_as: response
        on_error: continue
        params:
          workspace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.machinelearningservices.privatelinkresources.list_by_workspace — required_params: ['workspace_name']
  # SKIP: azure.machinelearningservices.quotas.list — required_params: ['location']
  # SKIP: azure.machinelearningservices.usages.list — required_params: ['location']
  # SKIP: azure.machinelearningservices.virtualmachinesizes.list — required_params: ['location']
