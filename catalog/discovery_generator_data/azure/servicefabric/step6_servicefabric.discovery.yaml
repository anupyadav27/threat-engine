# Discovery YAML — servicefabric (Azure)
# Generated: 2026-04-15T10:39:31Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: servicefabric
services:
  client: servicefabric
  module: azure.mgmt.servicefabric
discovery:
  # ── clusters.list [root] ──
  - discovery_id: azure.servicefabric.clusters.list
    calls:
      - action: clusters.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
        correlation_scheme: '{{ item.correlation_scheme }}'
        default_move_cost: '{{ item.default_move_cost }}'
        etag: '{{ item.etag }}'
        partition_description: '{{ item.partition_description }}'
        placement_constraints: '{{ item.placement_constraints }}'
        service_dns_name: '{{ item.service_dns_name }}'
        service_kind: '{{ item.service_kind }}'
        service_load_metrics: '{{ item.service_load_metrics }}'
        service_package_activation_mode: '{{ item.service_package_activation_mode }}'
        service_placement_policies: '{{ item.service_placement_policies }}'
        service_type_name: '{{ item.service_type_name }}'
        system_data: '{{ item.system_data }}'
  # ── clusters.list_by_resource_group [root] ──
  - discovery_id: azure.servicefabric.clusters.list_by_resource_group
    calls:
      - action: clusters.list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
        add_on_features: '{{ item.add_on_features }}'
        application_type_versions_cleanup_policy: '{{ item.application_type_versions_cleanup_policy }}'
        available_cluster_versions: '{{ item.available_cluster_versions }}'
        azure_active_directory: '{{ item.azure_active_directory }}'
        certificate: '{{ item.certificate }}'
        certificate_common_names: '{{ item.certificate_common_names }}'
        client_certificate_common_names: '{{ item.client_certificate_common_names }}'
        client_certificate_thumbprints: '{{ item.client_certificate_thumbprints }}'
        cluster_code_version: '{{ item.cluster_code_version }}'
        cluster_endpoint: '{{ item.cluster_endpoint }}'
        cluster_id: '{{ item.cluster_id }}'
        cluster_state: '{{ item.cluster_state }}'
        diagnostics_storage_account_config: '{{ item.diagnostics_storage_account_config }}'
        etag: '{{ item.etag }}'
        event_store_service_enabled: '{{ item.event_store_service_enabled }}'
        fabric_settings: '{{ item.fabric_settings }}'
        infrastructure_service_manager: '{{ item.infrastructure_service_manager }}'
        management_endpoint: '{{ item.management_endpoint }}'
        node_types: '{{ item.node_types }}'
        notifications: '{{ item.notifications }}'
        reliability_level: '{{ item.reliability_level }}'
        reverse_proxy_certificate: '{{ item.reverse_proxy_certificate }}'
        reverse_proxy_certificate_common_names: '{{ item.reverse_proxy_certificate_common_names }}'
        sf_zonal_upgrade_mode: '{{ item.sf_zonal_upgrade_mode }}'
        system_data: '{{ item.system_data }}'
        upgrade_description: '{{ item.upgrade_description }}'
        upgrade_mode: '{{ item.upgrade_mode }}'
        upgrade_pause_end_timestamp_utc: '{{ item.upgrade_pause_end_timestamp_utc }}'
        upgrade_pause_start_timestamp_utc: '{{ item.upgrade_pause_start_timestamp_utc }}'
        upgrade_wave: '{{ item.upgrade_wave }}'
        vm_image: '{{ item.vm_image }}'
        vmss_zonal_upgrade_mode: '{{ item.vmss_zonal_upgrade_mode }}'
        wave_upgrade_paused: '{{ item.wave_upgrade_paused }}'
  # ════════════════════════════════════════
  # Dependent operations (for_each + params)
  # ════════════════════════════════════════
  # ── applications.list [dependent] ──
  - discovery_id: azure.servicefabric.applications.list
    for_each: azure.servicefabric.clusters.list
    calls:
      - action: applications.list
        save_as: response
        on_error: continue
        params:
          cluster_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
        correlation_scheme: '{{ item.correlation_scheme }}'
        default_move_cost: '{{ item.default_move_cost }}'
        etag: '{{ item.etag }}'
        partition_description: '{{ item.partition_description }}'
        placement_constraints: '{{ item.placement_constraints }}'
        service_dns_name: '{{ item.service_dns_name }}'
        service_kind: '{{ item.service_kind }}'
        service_load_metrics: '{{ item.service_load_metrics }}'
        service_package_activation_mode: '{{ item.service_package_activation_mode }}'
        service_placement_policies: '{{ item.service_placement_policies }}'
        service_type_name: '{{ item.service_type_name }}'
        system_data: '{{ item.system_data }}'
  # ── applicationtypes.list [dependent] ──
  - discovery_id: azure.servicefabric.applicationtypes.list
    for_each: azure.servicefabric.clusters.list
    calls:
      - action: applicationtypes.list
        save_as: response
        on_error: continue
        params:
          cluster_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
        correlation_scheme: '{{ item.correlation_scheme }}'
        default_move_cost: '{{ item.default_move_cost }}'
        etag: '{{ item.etag }}'
        partition_description: '{{ item.partition_description }}'
        placement_constraints: '{{ item.placement_constraints }}'
        service_dns_name: '{{ item.service_dns_name }}'
        service_kind: '{{ item.service_kind }}'
        service_load_metrics: '{{ item.service_load_metrics }}'
        service_package_activation_mode: '{{ item.service_package_activation_mode }}'
        service_placement_policies: '{{ item.service_placement_policies }}'
        service_type_name: '{{ item.service_type_name }}'
        system_data: '{{ item.system_data }}'
  # ── applicationtypeversions.list [dependent] ──
  - discovery_id: azure.servicefabric.applicationtypeversions.list
    for_each: azure.servicefabric.clusters.list
    calls:
      - action: applicationtypeversions.list
        save_as: response
        on_error: continue
        params:
          cluster_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
        correlation_scheme: '{{ item.correlation_scheme }}'
        default_move_cost: '{{ item.default_move_cost }}'
        etag: '{{ item.etag }}'
        partition_description: '{{ item.partition_description }}'
        placement_constraints: '{{ item.placement_constraints }}'
        service_dns_name: '{{ item.service_dns_name }}'
        service_kind: '{{ item.service_kind }}'
        service_load_metrics: '{{ item.service_load_metrics }}'
        service_package_activation_mode: '{{ item.service_package_activation_mode }}'
        service_placement_policies: '{{ item.service_placement_policies }}'
        service_type_name: '{{ item.service_type_name }}'
        system_data: '{{ item.system_data }}'
  # ── clusters.list_upgradable_versions [dependent] ──
  - discovery_id: azure.servicefabric.clusters.list_upgradable_versions
    for_each: azure.servicefabric.clusters.list
    calls:
      - action: clusters.list_upgradable_versions
        save_as: response
        on_error: continue
        params:
          cluster_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── clusterversions.get_by_environment [dependent] ──
  - discovery_id: azure.servicefabric.clusterversions.get_by_environment
    for_each: azure.servicefabric.clusterversions.list
    calls:
      - action: clusterversions.get_by_environment
        save_as: response
        on_error: continue
        params:
          location: '{{ item.location }}'
          environment: '{{ item.environment }}'
    emit:
      as: item
      item:
        type: '{{ response.type }}'
        name: '{{ response.name }}'
        id: '{{ response.id }}'
        code_version: '{{ response.code_version }}'
        environment: '{{ response.environment }}'
        support_expiry_utc: '{{ response.support_expiry_utc }}'
  # ── clusterversions.list_by_environment [dependent] ──
  - discovery_id: azure.servicefabric.clusterversions.list_by_environment
    for_each: azure.servicefabric.clusterversions.list
    calls:
      - action: clusterversions.list_by_environment
        save_as: response
        on_error: continue
        params:
          location: '{{ item.location }}'
          environment: '{{ item.environment }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        code_version: '{{ item.code_version }}'
        environment: '{{ item.environment }}'
        support_expiry_utc: '{{ item.support_expiry_utc }}'
  # ── services.list [dependent] ──
  - discovery_id: azure.servicefabric.services.list
    for_each: azure.servicefabric.clusters.list
    calls:
      - action: services.list
        save_as: response
        on_error: continue
        params:
          cluster_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
        correlation_scheme: '{{ item.correlation_scheme }}'
        default_move_cost: '{{ item.default_move_cost }}'
        etag: '{{ item.etag }}'
        partition_description: '{{ item.partition_description }}'
        placement_constraints: '{{ item.placement_constraints }}'
        service_dns_name: '{{ item.service_dns_name }}'
        service_kind: '{{ item.service_kind }}'
        service_load_metrics: '{{ item.service_load_metrics }}'
        service_package_activation_mode: '{{ item.service_package_activation_mode }}'
        service_placement_policies: '{{ item.service_placement_policies }}'
        service_type_name: '{{ item.service_type_name }}'
        system_data: '{{ item.system_data }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.servicefabric.clusterversions.list — required_params: ['location']
