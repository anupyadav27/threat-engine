# Discovery YAML — solutions (Azure)
# Generated: 2026-04-15T10:39:31Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: solutions
services:
  client: solutions
  module: azure.mgmt.solutions
discovery:

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.solutions.applicationdefinitions.applicationdefinitions_get — required_params: ['id']
  # SKIP: azure.solutions.applications.applications_get — required_params: ['id']
  # SKIP: azure.solutions.jitrequests.jitrequests_get — required_params: ['id']
