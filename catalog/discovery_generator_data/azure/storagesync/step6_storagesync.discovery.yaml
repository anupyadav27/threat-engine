# Discovery YAML — storagesync (Azure)
# Generated: 2026-04-15T10:39:31Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: storagesync
services:
  client: storagesync
  module: azure.mgmt.storagesync
discovery:
  # ── storagesyncservices.list_by_resource_group [root] ──
  - discovery_id: azure.storagesync.storagesyncservices.list_by_resource_group
    calls:
      - action: storagesyncservices.list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── storagesyncservices.list_by_subscription [root] ──
  - discovery_id: azure.storagesync.storagesyncservices.list_by_subscription
    calls:
      - action: storagesyncservices.list_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ════════════════════════════════════════
  # Dependent operations (for_each + params)
  # ════════════════════════════════════════
  # ── cloudendpoints.list_by_sync_group [dependent] ──
  - discovery_id: azure.storagesync.cloudendpoints.list_by_sync_group
    for_each: azure.storagesync.privatelinkresources.list_by_storage_sync_service
    calls:
      - action: cloudendpoints.list_by_sync_group
        save_as: response
        on_error: continue
        params:
          storage_sync_service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── privateendpointconnections.list_by_storage_sync_service [dependent] ──
  - discovery_id: azure.storagesync.privateendpointconnections.list_by_storage_sync_service
    for_each: azure.storagesync.privatelinkresources.list_by_storage_sync_service
    calls:
      - action: privateendpointconnections.list_by_storage_sync_service
        save_as: response
        on_error: continue
        params:
          storage_sync_service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── registeredservers.list_by_storage_sync_service [dependent] ──
  - discovery_id: azure.storagesync.registeredservers.list_by_storage_sync_service
    for_each: azure.storagesync.privatelinkresources.list_by_storage_sync_service
    calls:
      - action: registeredservers.list_by_storage_sync_service
        save_as: response
        on_error: continue
        params:
          storage_sync_service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── serverendpoints.list_by_sync_group [dependent] ──
  - discovery_id: azure.storagesync.serverendpoints.list_by_sync_group
    for_each: azure.storagesync.privatelinkresources.list_by_storage_sync_service
    calls:
      - action: serverendpoints.list_by_sync_group
        save_as: response
        on_error: continue
        params:
          storage_sync_service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── syncgroups.list_by_storage_sync_service [dependent] ──
  - discovery_id: azure.storagesync.syncgroups.list_by_storage_sync_service
    for_each: azure.storagesync.privatelinkresources.list_by_storage_sync_service
    calls:
      - action: syncgroups.list_by_storage_sync_service
        save_as: response
        on_error: continue
        params:
          storage_sync_service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── workflows.list_by_storage_sync_service [dependent] ──
  - discovery_id: azure.storagesync.workflows.list_by_storage_sync_service
    for_each: azure.storagesync.privatelinkresources.list_by_storage_sync_service
    calls:
      - action: workflows.list_by_storage_sync_service
        save_as: response
        on_error: continue
        params:
          storage_sync_service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.storagesync.privatelinkresources.list_by_storage_sync_service — required_params: ['storage_sync_service_name']
