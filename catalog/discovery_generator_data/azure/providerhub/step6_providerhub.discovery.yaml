# Discovery YAML — providerhub (Azure)
# Generated: 2026-04-15T10:39:31Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: providerhub
services:
  client: providerhub
  module: azure.mgmt.providerhub
discovery:
  # ── operations.Operations_List [root] ──
  - discovery_id: azure.providerhub.operations.operations_list
    calls:
      - action: operations.Operations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── providerregistrations.ProviderRegistrations_List [root] ──
  - discovery_id: azure.providerhub.providerregistrations.providerregistrations_list
    calls:
      - action: providerregistrations.ProviderRegistrations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.providerhub.authorizedapplications.authorizedapplications_get — required_params: ['id']
  # SKIP: azure.providerhub.customrollouts.customrollouts_get — required_params: ['id']
  # SKIP: azure.providerhub.defaultrollouts.defaultrollouts_get — required_params: ['id']
  # SKIP: azure.providerhub.newregionfrontloadrelease.newregionfrontloadrelease_get — required_params: ['id']
  # SKIP: azure.providerhub.notificationregistrations.notificationregistrations_get — required_params: ['id']
  # SKIP: azure.providerhub.providermonitorsettings.providermonitorsettings_get — required_params: ['id']
  # SKIP: azure.providerhub.providerregistrations.providerregistrations_get — required_params: ['id']
  # SKIP: azure.providerhub.resourcetyperegistrations.resourcetyperegistrations_get — required_params: ['id']
  # SKIP: azure.providerhub.skus.skus_get — required_params: ['id']
