# Discovery YAML — dnsresolver (Azure)
# Generated: 2026-04-15T10:39:30Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: dnsresolver
services:
  client: dnsresolver
  module: azure.mgmt.dnsresolver
discovery:
  # ── dnsforwardingrulesets.DnsForwardingRulesets_List [root] ──
  - discovery_id: azure.dnsresolver.dnsforwardingrulesets.dnsforwardingrulesets_list
    calls:
      - action: dnsforwardingrulesets.DnsForwardingRulesets_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── dnsresolverdomainlists.DnsResolverDomainLists_List [root] ──
  - discovery_id: azure.dnsresolver.dnsresolverdomainlists.dnsresolverdomainlists_list
    calls:
      - action: dnsresolverdomainlists.DnsResolverDomainLists_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── dnsresolverpolicies.DnsResolverPolicies_List [root] ──
  - discovery_id: azure.dnsresolver.dnsresolverpolicies.dnsresolverpolicies_list
    calls:
      - action: dnsresolverpolicies.DnsResolverPolicies_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── dnsresolvers.DnsResolvers_List [root] ──
  - discovery_id: azure.dnsresolver.dnsresolvers.dnsresolvers_list
    calls:
      - action: dnsresolvers.DnsResolvers_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.dnsresolver.dnsforwardingrulesets.dnsforwardingrulesets_get — required_params: ['id']
  # SKIP: azure.dnsresolver.dnsresolverpolicies.dnsresolverpolicies_get — required_params: ['id']
  # SKIP: azure.dnsresolver.dnsresolverpolicyvirtualnetworklinks.dnsresolverpolicyvirtualnetworklinks_get — required_params: ['id']
  # SKIP: azure.dnsresolver.dnsresolvers.dnsresolvers_get — required_params: ['id']
  # SKIP: azure.dnsresolver.dnssecurityrules.dnssecurityrules_get — required_params: ['id']
  # SKIP: azure.dnsresolver.forwardingrules.forwardingrules_get — required_params: ['id']
  # SKIP: azure.dnsresolver.inboundendpoints.inboundendpoints_get — required_params: ['id']
  # SKIP: azure.dnsresolver.outboundendpoints.outboundendpoints_get — required_params: ['id']
  # SKIP: azure.dnsresolver.virtualnetworklinks.virtualnetworklinks_get — required_params: ['id']
