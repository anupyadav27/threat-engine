# Discovery YAML — iotfirmwaredefense (Azure)
# Generated: 2026-04-15T10:39:30Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: iotfirmwaredefense
services:
  client: iotfirmwaredefense
  module: azure.mgmt.iotfirmwaredefense
discovery:
  # ── workspaces.list_by_resource_group [root] ──
  - discovery_id: azure.iotfirmwaredefense.workspaces.list_by_resource_group
    calls:
      - action: workspaces.list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── workspaces.list_by_subscription [root] ──
  - discovery_id: azure.iotfirmwaredefense.workspaces.list_by_subscription
    calls:
      - action: workspaces.list_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ════════════════════════════════════════
  # Dependent operations (for_each + params)
  # ════════════════════════════════════════
  # ── binaryhardening.list_by_firmware [dependent] ──
  - discovery_id: azure.iotfirmwaredefense.binaryhardening.list_by_firmware
    for_each: azure.iotfirmwaredefense.workspaces.list_by_subscription
    calls:
      - action: binaryhardening.list_by_firmware
        save_as: response
        on_error: continue
        params:
          workspace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── cryptocertificates.list_by_firmware [dependent] ──
  - discovery_id: azure.iotfirmwaredefense.cryptocertificates.list_by_firmware
    for_each: azure.iotfirmwaredefense.workspaces.list_by_subscription
    calls:
      - action: cryptocertificates.list_by_firmware
        save_as: response
        on_error: continue
        params:
          workspace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── cryptokeys.list_by_firmware [dependent] ──
  - discovery_id: azure.iotfirmwaredefense.cryptokeys.list_by_firmware
    for_each: azure.iotfirmwaredefense.workspaces.list_by_subscription
    calls:
      - action: cryptokeys.list_by_firmware
        save_as: response
        on_error: continue
        params:
          workspace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── cves.list_by_firmware [dependent] ──
  - discovery_id: azure.iotfirmwaredefense.cves.list_by_firmware
    for_each: azure.iotfirmwaredefense.workspaces.list_by_subscription
    calls:
      - action: cves.list_by_firmware
        save_as: response
        on_error: continue
        params:
          workspace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── firmwares.list_by_workspace [dependent] ──
  - discovery_id: azure.iotfirmwaredefense.firmwares.list_by_workspace
    for_each: azure.iotfirmwaredefense.workspaces.list_by_subscription
    calls:
      - action: firmwares.list_by_workspace
        save_as: response
        on_error: continue
        params:
          workspace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── passwordhashes.list_by_firmware [dependent] ──
  - discovery_id: azure.iotfirmwaredefense.passwordhashes.list_by_firmware
    for_each: azure.iotfirmwaredefense.workspaces.list_by_subscription
    calls:
      - action: passwordhashes.list_by_firmware
        save_as: response
        on_error: continue
        params:
          workspace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── sbomcomponents.list_by_firmware [dependent] ──
  - discovery_id: azure.iotfirmwaredefense.sbomcomponents.list_by_firmware
    for_each: azure.iotfirmwaredefense.workspaces.list_by_subscription
    calls:
      - action: sbomcomponents.list_by_firmware
        save_as: response
        on_error: continue
        params:
          workspace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── summaries.list_by_firmware [dependent] ──
  - discovery_id: azure.iotfirmwaredefense.summaries.list_by_firmware
    for_each: azure.iotfirmwaredefense.workspaces.list_by_subscription
    calls:
      - action: summaries.list_by_firmware
        save_as: response
        on_error: continue
        params:
          workspace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── usagemetrics.list_by_workspace [dependent] ──
  - discovery_id: azure.iotfirmwaredefense.usagemetrics.list_by_workspace
    for_each: azure.iotfirmwaredefense.workspaces.list_by_subscription
    calls:
      - action: usagemetrics.list_by_workspace
        save_as: response
        on_error: continue
        params:
          workspace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
