# Discovery YAML — azsadmin (Azure)
# Generated: 2026-04-15T10:39:29Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: azsadmin
services:
  client: azsadmin
  module: azure.mgmt.azsadmin
discovery:
  # ── backuplocations.BackupLocations_List [root] ──
  - discovery_id: azure.azsadmin.backuplocations.backuplocations_list
    calls:
      - action: backuplocations.BackupLocations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── backups.Backups_List [root] ──
  - discovery_id: azure.azsadmin.backups.backups_list
    calls:
      - action: backups.Backups_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── containerservicequota.ContainerServiceQuota_List [root] ──
  - discovery_id: azure.azsadmin.containerservicequota.containerservicequota_list
    calls:
      - action: containerservicequota.ContainerServiceQuota_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── containerservices.ContainerServices_List [root] ──
  - discovery_id: azure.azsadmin.containerservices.containerservices_list
    calls:
      - action: containerservices.ContainerServices_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── diskmigrationjobs.DiskMigrationJobs_List [root] ──
  - discovery_id: azure.azsadmin.diskmigrationjobs.diskmigrationjobs_list
    calls:
      - action: diskmigrationjobs.DiskMigrationJobs_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── disks.Disks_List [root] ──
  - discovery_id: azure.azsadmin.disks.disks_list
    calls:
      - action: disks.Disks_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── fabriclocations.FabricLocations_List [root] ──
  - discovery_id: azure.azsadmin.fabriclocations.fabriclocations_list
    calls:
      - action: fabriclocations.FabricLocations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── features.Features_List [root] ──
  - discovery_id: azure.azsadmin.features.features_list
    calls:
      - action: features.Features_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── operations.Operations_List [root] ──
  - discovery_id: azure.azsadmin.operations.operations_list
    calls:
      - action: operations.Operations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── quotas.Quotas_List [root] ──
  - discovery_id: azure.azsadmin.quotas.quotas_list
    calls:
      - action: quotas.Quotas_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── updatelocations.UpdateLocations_List [root] ──
  - discovery_id: azure.azsadmin.updatelocations.updatelocations_list
    calls:
      - action: updatelocations.UpdateLocations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── virtualnetworkgatewayconnections.VirtualNetworkGatewayConnections_List [root] ──
  - discovery_id: azure.azsadmin.virtualnetworkgatewayconnections.virtualnetworkgatewayconnections_list
    calls:
      - action: virtualnetworkgatewayconnections.VirtualNetworkGatewayConnections_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.azsadmin.backuplocations.backuplocations_get — required_params: ['id']
  # SKIP: azure.azsadmin.backups.backups_get — required_params: ['id']
  # SKIP: azure.azsadmin.diskmigrationjobs.diskmigrationjobs_get — required_params: ['id']
  # SKIP: azure.azsadmin.disks.disks_get — required_params: ['id']
  # SKIP: azure.azsadmin.fabriclocations.fabriclocations_get — required_params: ['id']
  # SKIP: azure.azsadmin.features.features_get — required_params: ['id']
  # SKIP: azure.azsadmin.quotas.quotas_get — required_params: ['id']
  # SKIP: azure.azsadmin.scaleunits.scaleunits_get — required_params: ['id']
  # SKIP: azure.azsadmin.updatelocations.updatelocations_get — required_params: ['id']
  # SKIP: azure.azsadmin.updateruns.updateruns_get — required_params: ['id']
  # SKIP: azure.azsadmin.updates.updates_get — required_params: ['id']
