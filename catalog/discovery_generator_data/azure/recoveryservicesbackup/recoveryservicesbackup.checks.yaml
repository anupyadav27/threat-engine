version: '1.0'
provider: azure
service: recoveryservicesbackup
checks:
# ═══════════════════════════════════════════
# Note: recoveryservicesbackup has NO root discoveries in step6.
# All operations depend on backupengines.list which is not a root entry.
# Using the closest available dependent discoveries until step6 is fixed.
# For now, rules reference properties blob which is the common field across
# backup engine objects.
# TODO: Add a root discovery (e.g., vaults.list from recoveryservices service)
# ═══════════════════════════════════════════
# Placeholder — service needs step6 root discovery fix first
