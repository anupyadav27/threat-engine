# Discovery YAML — databasewatcher (Azure)
# Generated: 2026-04-15T10:39:30Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: databasewatcher
services:
  client: databasewatcher
  module: azure.mgmt.databasewatcher
discovery:
  # ── operations.Operations_List [root] ──
  - discovery_id: azure.databasewatcher.operations.operations_list
    calls:
      - action: operations.Operations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.databasewatcher.alertruleresources.alertruleresources_get — required_params: ['id']
  # SKIP: azure.databasewatcher.healthvalidations.healthvalidations_get — required_params: ['id']
  # SKIP: azure.databasewatcher.sharedprivatelinkresources.sharedprivatelinkresources_get — required_params: ['id']
  # SKIP: azure.databasewatcher.targets.targets_get — required_params: ['id']
  # SKIP: azure.databasewatcher.watchers.watchers_get — required_params: ['id']
