# Discovery YAML — fabric (Azure)
# Generated: 2026-04-15T10:39:30Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: fabric
services:
  client: fabric
  module: azure.mgmt.fabric
discovery:
  # ── fabriccapacities.list_by_resource_group [root] ──
  - discovery_id: azure.fabric.fabriccapacities.list_by_resource_group
    calls:
      - action: fabriccapacities.list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── fabriccapacities.list_by_subscription [root] ──
  - discovery_id: azure.fabric.fabriccapacities.list_by_subscription
    calls:
      - action: fabriccapacities.list_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── fabriccapacities.list_skus [root] ──
  - discovery_id: azure.fabric.fabriccapacities.list_skus
    calls:
      - action: fabriccapacities.list_skus
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        locations: '{{ item.locations }}'
        resource_type: '{{ item.resource_type }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.fabric.fabriccapacities.list_skus_for_capacity — required_params: ['capacity_name']
