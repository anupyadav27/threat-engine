# Discovery YAML — customer-insights (Azure)
# Generated: 2026-04-15T10:39:30Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: customer-insights
services:
  client: customer-insights
  module: azure.mgmt.customer-insights
discovery:
  # ── hubs.Hubs_List [root] ──
  - discovery_id: azure.customer-insights.hubs.hubs_list
    calls:
      - action: hubs.Hubs_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── operations.Operations_List [root] ──
  - discovery_id: azure.customer-insights.operations.operations_list
    calls:
      - action: operations.Operations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.customer-insights.authorizationpolicies.authorizationpolicies_get — required_params: ['id']
  # SKIP: azure.customer-insights.connectormappings.connectormappings_get — required_params: ['id']
  # SKIP: azure.customer-insights.connectors.connectors_get — required_params: ['id']
  # SKIP: azure.customer-insights.hubs.hubs_get — required_params: ['id']
  # SKIP: azure.customer-insights.interactions.interactions_get — required_params: ['id']
  # SKIP: azure.customer-insights.kpi.kpi_get — required_params: ['id']
  # SKIP: azure.customer-insights.links.links_get — required_params: ['id']
  # SKIP: azure.customer-insights.predictions.predictions_get — required_params: ['id']
  # SKIP: azure.customer-insights.profiles.profiles_get — required_params: ['id']
  # SKIP: azure.customer-insights.relationshiplinks.relationshiplinks_get — required_params: ['id']
  # SKIP: azure.customer-insights.relationships.relationships_get — required_params: ['id']
  # SKIP: azure.customer-insights.roleassignments.roleassignments_get — required_params: ['id']
  # SKIP: azure.customer-insights.views.views_get — required_params: ['id']
  # SKIP: azure.customer-insights.widgettypes.widgettypes_get — required_params: ['id']
