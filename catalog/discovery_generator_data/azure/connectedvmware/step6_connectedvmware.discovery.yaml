# Discovery YAML — connectedvmware (Azure)
# Generated: 2026-04-15T10:39:29Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: connectedvmware
services:
  client: connectedvmware
  module: azure.mgmt.connectedvmware
discovery:
  # ── clusters.Clusters_List [root] ──
  - discovery_id: azure.connectedvmware.clusters.clusters_list
    calls:
      - action: clusters.Clusters_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── datastores.Datastores_List [root] ──
  - discovery_id: azure.connectedvmware.datastores.datastores_list
    calls:
      - action: datastores.Datastores_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── hosts.Hosts_List [root] ──
  - discovery_id: azure.connectedvmware.hosts.hosts_list
    calls:
      - action: hosts.Hosts_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── operations.Operations_List [root] ──
  - discovery_id: azure.connectedvmware.operations.operations_list
    calls:
      - action: operations.Operations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── resourcepools.ResourcePools_List [root] ──
  - discovery_id: azure.connectedvmware.resourcepools.resourcepools_list
    calls:
      - action: resourcepools.ResourcePools_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── vcenters.VCenters_List [root] ──
  - discovery_id: azure.connectedvmware.vcenters.vcenters_list
    calls:
      - action: vcenters.VCenters_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── virtualmachinetemplates.VirtualMachineTemplates_List [root] ──
  - discovery_id: azure.connectedvmware.virtualmachinetemplates.virtualmachinetemplates_list
    calls:
      - action: virtualmachinetemplates.VirtualMachineTemplates_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── virtualnetworks.VirtualNetworks_List [root] ──
  - discovery_id: azure.connectedvmware.virtualnetworks.virtualnetworks_list
    calls:
      - action: virtualnetworks.VirtualNetworks_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.connectedvmware.clusters.clusters_get — required_params: ['id']
  # SKIP: azure.connectedvmware.datastores.datastores_get — required_params: ['id']
  # SKIP: azure.connectedvmware.hosts.hosts_get — required_params: ['id']
  # SKIP: azure.connectedvmware.inventoryitems.inventoryitems_get — required_params: ['id']
  # SKIP: azure.connectedvmware.resourcepools.resourcepools_get — required_params: ['id']
  # SKIP: azure.connectedvmware.vcenters.vcenters_get — required_params: ['id']
  # SKIP: azure.connectedvmware.virtualmachineinstances.virtualmachineinstances_get — required_params: ['id']
  # SKIP: azure.connectedvmware.virtualmachinetemplates.virtualmachinetemplates_get — required_params: ['id']
  # SKIP: azure.connectedvmware.virtualnetworks.virtualnetworks_get — required_params: ['id']
  # SKIP: azure.connectedvmware.vminstanceguestagents.vminstanceguestagents_get — required_params: ['id']
  # SKIP: azure.connectedvmware.vminstancehybrididentitymetadata.vminstancehybrididentitymetadata_get — required_params: ['id']
