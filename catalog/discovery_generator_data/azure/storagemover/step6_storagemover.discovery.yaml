# Discovery YAML — storagemover (Azure)
# Generated: 2026-04-15T10:39:31Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: storagemover
services:
  client: storagemover
  module: azure.mgmt.storagemover
discovery:
  # ── storagemovers.list [root] ──
  - discovery_id: azure.storagemover.storagemovers.list
    calls:
      - action: storagemovers.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── storagemovers.list_by_subscription [root] ──
  - discovery_id: azure.storagemover.storagemovers.list_by_subscription
    calls:
      - action: storagemovers.list_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ════════════════════════════════════════
  # Dependent operations (for_each + params)
  # ════════════════════════════════════════
  # ── agents.list [dependent] ──
  - discovery_id: azure.storagemover.agents.list
    for_each: azure.storagemover.storagemovers.list_by_subscription
    calls:
      - action: agents.list
        save_as: response
        on_error: continue
        params:
          storage_mover_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── endpoints.list [dependent] ──
  - discovery_id: azure.storagemover.endpoints.list
    for_each: azure.storagemover.storagemovers.list_by_subscription
    calls:
      - action: endpoints.list
        save_as: response
        on_error: continue
        params:
          storage_mover_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── jobdefinitions.list [dependent] ──
  - discovery_id: azure.storagemover.jobdefinitions.list
    for_each: azure.storagemover.storagemovers.list_by_subscription
    calls:
      - action: jobdefinitions.list
        save_as: response
        on_error: continue
        params:
          storage_mover_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── jobruns.list [dependent] ──
  - discovery_id: azure.storagemover.jobruns.list
    for_each: azure.storagemover.storagemovers.list_by_subscription
    calls:
      - action: jobruns.list
        save_as: response
        on_error: continue
        params:
          storage_mover_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── projects.list [dependent] ──
  - discovery_id: azure.storagemover.projects.list
    for_each: azure.storagemover.storagemovers.list_by_subscription
    calls:
      - action: projects.list
        save_as: response
        on_error: continue
        params:
          storage_mover_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
