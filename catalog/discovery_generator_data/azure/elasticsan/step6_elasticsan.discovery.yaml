# Discovery YAML — elasticsan (Azure)
# Generated: 2026-04-15T10:39:30Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: elasticsan
services:
  client: elasticsan
  module: azure.mgmt.elasticsan
discovery:
  # ── operations.Operations_List [root] ──
  - discovery_id: azure.elasticsan.operations.operations_list
    calls:
      - action: operations.Operations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── skus.Skus_List [root] ──
  - discovery_id: azure.elasticsan.skus.skus_list
    calls:
      - action: skus.Skus_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.elasticsan.elasticsans.elasticsans_get — required_params: ['id']
  # SKIP: azure.elasticsan.privateendpointconnections.privateendpointconnections_get — required_params: ['id']
  # SKIP: azure.elasticsan.volumegroups.volumegroups_get — required_params: ['id']
  # SKIP: azure.elasticsan.volumes.volumes_get — required_params: ['id']
  # SKIP: azure.elasticsan.volumesnapshots.volumesnapshots_get — required_params: ['id']
