# Discovery YAML — deviceregistry (Azure)
# Generated: 2026-04-15T10:39:30Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: deviceregistry
services:
  client: deviceregistry
  module: azure.mgmt.deviceregistry
discovery:
  # ── operations.Operations_List [root] ──
  - discovery_id: azure.deviceregistry.operations.operations_list
    calls:
      - action: operations.Operations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.deviceregistry.assetendpointprofiles.assetendpointprofiles_get — required_params: ['id']
  # SKIP: azure.deviceregistry.assets.assets_get — required_params: ['id']
  # SKIP: azure.deviceregistry.billingcontainers.billingcontainers_get — required_params: ['id']
  # SKIP: azure.deviceregistry.namespaceassets.namespaceassets_get — required_params: ['id']
  # SKIP: azure.deviceregistry.namespacedevices.namespacedevices_get — required_params: ['id']
  # SKIP: azure.deviceregistry.namespacediscoveredassets.namespacediscoveredassets_get — required_params: ['id']
  # SKIP: azure.deviceregistry.namespacediscovereddevices.namespacediscovereddevices_get — required_params: ['id']
  # SKIP: azure.deviceregistry.namespaces.namespaces_get — required_params: ['id']
  # SKIP: azure.deviceregistry.operationstatus.operationstatus_get — required_params: ['id']
  # SKIP: azure.deviceregistry.schemaregistries.schemaregistries_get — required_params: ['id']
  # SKIP: azure.deviceregistry.schemas.schemas_get — required_params: ['id']
  # SKIP: azure.deviceregistry.schemaversions.schemaversions_get — required_params: ['id']
