# Discovery YAML — datalake-analytics (Azure)
# Generated: 2026-04-15T10:39:30Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: datalake-analytics
services:
  client: datalake-analytics
  module: azure.mgmt.datalake-analytics
discovery:
  # ── accounts.Accounts_List [root] ──
  - discovery_id: azure.datalake-analytics.accounts.accounts_list
    calls:
      - action: accounts.Accounts_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── operations.Operations_List [root] ──
  - discovery_id: azure.datalake-analytics.operations.operations_list
    calls:
      - action: operations.Operations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.datalake-analytics.accounts.accounts_get — required_params: ['id']
  # SKIP: azure.datalake-analytics.computepolicies.computepolicies_get — required_params: ['id']
  # SKIP: azure.datalake-analytics.datalakestoreaccounts.datalakestoreaccounts_get — required_params: ['id']
  # SKIP: azure.datalake-analytics.firewallrules.firewallrules_get — required_params: ['id']
  # SKIP: azure.datalake-analytics.storageaccounts.storageaccounts_get — required_params: ['id']
