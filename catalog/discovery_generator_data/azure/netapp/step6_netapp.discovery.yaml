# Discovery YAML — netapp (Azure)
# Generated: 2026-04-15T10:39:30Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: netapp
services:
  client: netapp
  module: azure.mgmt.netapp
discovery:
  # ── accounts.Accounts_List [root] ──
  - discovery_id: azure.netapp.accounts.accounts_list
    calls:
      - action: accounts.Accounts_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── netappresourcequotalimits.NetAppResourceQuotaLimits_List [root] ──
  - discovery_id: azure.netapp.netappresourcequotalimits.netappresourcequotalimits_list
    calls:
      - action: netappresourcequotalimits.NetAppResourceQuotaLimits_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── netappresourceregioninfos.NetAppResourceRegionInfos_List [root] ──
  - discovery_id: azure.netapp.netappresourceregioninfos.netappresourceregioninfos_list
    calls:
      - action: netappresourceregioninfos.NetAppResourceRegionInfos_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── netappresourceusages.NetAppResourceUsages_List [root] ──
  - discovery_id: azure.netapp.netappresourceusages.netappresourceusages_list
    calls:
      - action: netappresourceusages.NetAppResourceUsages_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── operations.Operations_List [root] ──
  - discovery_id: azure.netapp.operations.operations_list
    calls:
      - action: operations.Operations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.netapp.accountbackups.accountbackups_get — required_params: ['id']
  # SKIP: azure.netapp.accounts.accounts_get — required_params: ['id']
  # SKIP: azure.netapp.backuppolicies.backuppolicies_get — required_params: ['id']
  # SKIP: azure.netapp.backups.backups_get — required_params: ['id']
  # SKIP: azure.netapp.backupvaults.backupvaults_get — required_params: ['id']
  # SKIP: azure.netapp.netappresourcequotalimits.netappresourcequotalimits_get — required_params: ['id']
  # SKIP: azure.netapp.netappresourcequotalimitsaccount.netappresourcequotalimitsaccount_get — required_params: ['id']
  # SKIP: azure.netapp.netappresourceregioninfos.netappresourceregioninfos_get — required_params: ['id']
  # SKIP: azure.netapp.netappresourceusages.netappresourceusages_get — required_params: ['id']
  # SKIP: azure.netapp.pools.pools_get — required_params: ['id']
  # SKIP: azure.netapp.ransomwarereports.ransomwarereports_get — required_params: ['id']
  # SKIP: azure.netapp.snapshotpolicies.snapshotpolicies_get — required_params: ['id']
  # SKIP: azure.netapp.snapshots.snapshots_get — required_params: ['id']
  # SKIP: azure.netapp.subvolumes.subvolumes_get — required_params: ['id']
  # SKIP: azure.netapp.volumebackupstatus.volumebackupstatus_get — required_params: ['id']
  # SKIP: azure.netapp.volumegroups.volumegroups_get — required_params: ['id']
  # SKIP: azure.netapp.volumequotarules.volumequotarules_get — required_params: ['id']
  # SKIP: azure.netapp.volumes.volumes_get — required_params: ['id']
