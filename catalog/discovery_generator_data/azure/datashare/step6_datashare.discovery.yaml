# Discovery YAML — datashare (Azure)
# Generated: 2026-04-15T10:39:30Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: datashare
services:
  client: datashare
  module: azure.mgmt.datashare
discovery:
  # ── accounts.list_by_resource_group [root] ──
  - discovery_id: azure.datashare.accounts.list_by_resource_group
    calls:
      - action: accounts.list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── accounts.list_by_subscription [root] ──
  - discovery_id: azure.datashare.accounts.list_by_subscription
    calls:
      - action: accounts.list_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── consumerinvitations.list_invitations [root] ──
  - discovery_id: azure.datashare.consumerinvitations.list_invitations
    calls:
      - action: consumerinvitations.list_invitations
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ════════════════════════════════════════
  # Dependent operations (for_each + params)
  # ════════════════════════════════════════
  # ── consumersourcedatasets.list_by_share_subscription [dependent] ──
  - discovery_id: azure.datashare.consumersourcedatasets.list_by_share_subscription
    for_each: azure.datashare.providersharesubscriptions.get_by_share
    calls:
      - action: consumersourcedatasets.list_by_share_subscription
        save_as: response
        on_error: continue
        params:
          account_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── datasetmappings.list_by_share_subscription [dependent] ──
  - discovery_id: azure.datashare.datasetmappings.list_by_share_subscription
    for_each: azure.datashare.providersharesubscriptions.get_by_share
    calls:
      - action: datasetmappings.list_by_share_subscription
        save_as: response
        on_error: continue
        params:
          account_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── datasets.list_by_share [dependent] ──
  - discovery_id: azure.datashare.datasets.list_by_share
    for_each: azure.datashare.providersharesubscriptions.get_by_share
    calls:
      - action: datasets.list_by_share
        save_as: response
        on_error: continue
        params:
          account_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── invitations.list_by_share [dependent] ──
  - discovery_id: azure.datashare.invitations.list_by_share
    for_each: azure.datashare.providersharesubscriptions.get_by_share
    calls:
      - action: invitations.list_by_share
        save_as: response
        on_error: continue
        params:
          account_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── providersharesubscriptions.list_by_share [dependent] ──
  - discovery_id: azure.datashare.providersharesubscriptions.list_by_share
    for_each: azure.datashare.providersharesubscriptions.get_by_share
    calls:
      - action: providersharesubscriptions.list_by_share
        save_as: response
        on_error: continue
        params:
          account_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── shares.list_by_account [dependent] ──
  - discovery_id: azure.datashare.shares.list_by_account
    for_each: azure.datashare.providersharesubscriptions.get_by_share
    calls:
      - action: shares.list_by_account
        save_as: response
        on_error: continue
        params:
          account_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── shares.list_synchronization_details [dependent] ──
  - discovery_id: azure.datashare.shares.list_synchronization_details
    for_each: azure.datashare.providersharesubscriptions.get_by_share
    calls:
      - action: shares.list_synchronization_details
        save_as: response
        on_error: continue
        params:
          account_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── shares.list_synchronizations [dependent] ──
  - discovery_id: azure.datashare.shares.list_synchronizations
    for_each: azure.datashare.providersharesubscriptions.get_by_share
    calls:
      - action: shares.list_synchronizations
        save_as: response
        on_error: continue
        params:
          account_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── sharesubscriptions.list_by_account [dependent] ──
  - discovery_id: azure.datashare.sharesubscriptions.list_by_account
    for_each: azure.datashare.providersharesubscriptions.get_by_share
    calls:
      - action: sharesubscriptions.list_by_account
        save_as: response
        on_error: continue
        params:
          account_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── sharesubscriptions.list_source_share_synchronization_settings [dependent] ──
  - discovery_id: azure.datashare.sharesubscriptions.list_source_share_synchronization_settings
    for_each: azure.datashare.providersharesubscriptions.get_by_share
    calls:
      - action: sharesubscriptions.list_source_share_synchronization_settings
        save_as: response
        on_error: continue
        params:
          account_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── sharesubscriptions.list_synchronization_details [dependent] ──
  - discovery_id: azure.datashare.sharesubscriptions.list_synchronization_details
    for_each: azure.datashare.providersharesubscriptions.get_by_share
    calls:
      - action: sharesubscriptions.list_synchronization_details
        save_as: response
        on_error: continue
        params:
          account_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── sharesubscriptions.list_synchronizations [dependent] ──
  - discovery_id: azure.datashare.sharesubscriptions.list_synchronizations
    for_each: azure.datashare.providersharesubscriptions.get_by_share
    calls:
      - action: sharesubscriptions.list_synchronizations
        save_as: response
        on_error: continue
        params:
          account_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── synchronizationsettings.list_by_share [dependent] ──
  - discovery_id: azure.datashare.synchronizationsettings.list_by_share
    for_each: azure.datashare.providersharesubscriptions.get_by_share
    calls:
      - action: synchronizationsettings.list_by_share
        save_as: response
        on_error: continue
        params:
          account_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── triggers.list_by_share_subscription [dependent] ──
  - discovery_id: azure.datashare.triggers.list_by_share_subscription
    for_each: azure.datashare.providersharesubscriptions.get_by_share
    calls:
      - action: triggers.list_by_share_subscription
        save_as: response
        on_error: continue
        params:
          account_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.datashare.providersharesubscriptions.get_by_share — required_params: ['account_name', 'share_name', 'provider_share_subscription_id']
