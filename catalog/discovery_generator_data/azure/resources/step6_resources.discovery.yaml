# Discovery YAML — resources (Azure)
# Generated: 2026-04-15T10:39:31Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: resources
services:
  client: resources
  module: azure.mgmt.resources
discovery:
  # ── authorizationoperations.AuthorizationOperations_List [root] ──
  - discovery_id: azure.resources.authorizationoperations.authorizationoperations_list
    calls:
      - action: authorizationoperations.AuthorizationOperations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── datapolicymanifests.DataPolicyManifests_List [root] ──
  - discovery_id: azure.resources.datapolicymanifests.datapolicymanifests_list
    calls:
      - action: datapolicymanifests.DataPolicyManifests_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── deployments.Deployments_List [root] ──
  - discovery_id: azure.resources.deployments.deployments_list
    calls:
      - action: deployments.Deployments_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── deploymentstackswhatifresultsatresourcegroup.DeploymentStacksWhatIfResultsAtResourceGroup_List [root] ──
  - discovery_id: azure.resources.deploymentstackswhatifresultsatresourcegroup.deploymentstackswhatifresultsatresourcegroup_list
    calls:
      - action: deploymentstackswhatifresultsatresourcegroup.DeploymentStacksWhatIfResultsAtResourceGroup_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── deploymentstackswhatifresultsatsubscription.DeploymentStacksWhatIfResultsAtSubscription_List [root] ──
  - discovery_id: azure.resources.deploymentstackswhatifresultsatsubscription.deploymentstackswhatifresultsatsubscription_list
    calls:
      - action: deploymentstackswhatifresultsatsubscription.DeploymentStacksWhatIfResultsAtSubscription_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── operations.Operations_List [root] ──
  - discovery_id: azure.resources.operations.operations_list
    calls:
      - action: operations.Operations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── policyassignments.PolicyAssignments_List [root] ──
  - discovery_id: azure.resources.policyassignments.policyassignments_list
    calls:
      - action: policyassignments.PolicyAssignments_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── policydefinitions.PolicyDefinitions_List [root] ──
  - discovery_id: azure.resources.policydefinitions.policydefinitions_list
    calls:
      - action: policydefinitions.PolicyDefinitions_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── policysetdefinitions.PolicySetDefinitions_List [root] ──
  - discovery_id: azure.resources.policysetdefinitions.policysetdefinitions_list
    calls:
      - action: policysetdefinitions.PolicySetDefinitions_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── providers.Providers_List [root] ──
  - discovery_id: azure.resources.providers.providers_list
    calls:
      - action: providers.Providers_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── resourcegroups.ResourceGroups_List [root] ──
  - discovery_id: azure.resources.resourcegroups.resourcegroups_list
    calls:
      - action: resourcegroups.ResourceGroups_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── resourcemanagementprivatelink.ResourceManagementPrivateLink_List [root] ──
  - discovery_id: azure.resources.resourcemanagementprivatelink.resourcemanagementprivatelink_list
    calls:
      - action: resourcemanagementprivatelink.ResourceManagementPrivateLink_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── resources.Resources_List [root] ──
  - discovery_id: azure.resources.resources.resources_list
    calls:
      - action: resources.Resources_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── subscriptions.Subscriptions_List [root] ──
  - discovery_id: azure.resources.subscriptions.subscriptions_list
    calls:
      - action: subscriptions.Subscriptions_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── tags.Tags_List [root] ──
  - discovery_id: azure.resources.tags.tags_list
    calls:
      - action: tags.Tags_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── tenants.Tenants_List [root] ──
  - discovery_id: azure.resources.tenants.tenants_list
    calls:
      - action: tenants.Tenants_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.resources.applicationdefinitions.applicationdefinitions_get — required_params: ['id']
  # SKIP: azure.resources.applications.applications_get — required_params: ['id']
  # SKIP: azure.resources.changes.changes_get — required_params: ['id']
  # SKIP: azure.resources.deploymentoperations.deploymentoperations_get — required_params: ['id']
  # SKIP: azure.resources.deployments.deployments_get — required_params: ['id']
  # SKIP: azure.resources.deploymentscripts.deploymentscripts_get — required_params: ['id']
  # SKIP: azure.resources.deploymentstackswhatifresultsatmanagementgroup.deploymentstackswhatifresultsatmanagementgroup_get — required_params: ['id']
  # SKIP: azure.resources.deploymentstackswhatifresultsatresourcegroup.deploymentstackswhatifresultsatresourcegroup_get — required_params: ['id']
  # SKIP: azure.resources.deploymentstackswhatifresultsatsubscription.deploymentstackswhatifresultsatsubscription_get — required_params: ['id']
  # SKIP: azure.resources.features.features_get — required_params: ['id']
  # SKIP: azure.resources.jitrequests.jitrequests_get — required_params: ['id']
  # SKIP: azure.resources.managementlocks.managementlocks_get — required_params: ['id']
  # SKIP: azure.resources.policyassignments.policyassignments_get — required_params: ['id']
  # SKIP: azure.resources.policydefinitions.policydefinitions_get — required_params: ['id']
  # SKIP: azure.resources.policydefinitionversions.policydefinitionversions_get — required_params: ['id']
  # SKIP: azure.resources.policysetdefinitions.policysetdefinitions_get — required_params: ['id']
  # SKIP: azure.resources.policysetdefinitionversions.policysetdefinitionversions_get — required_params: ['id']
  # SKIP: azure.resources.privatelinkassociation.privatelinkassociation_get — required_params: ['id']
  # SKIP: azure.resources.providers.providers_get — required_params: ['id']
  # SKIP: azure.resources.resourcegroups.resourcegroups_get — required_params: ['id']
  # SKIP: azure.resources.resourcelinks.resourcelinks_get — required_params: ['id']
  # SKIP: azure.resources.resourcemanagementprivatelink.resourcemanagementprivatelink_get — required_params: ['id']
  # SKIP: azure.resources.resources.resources_get — required_params: ['id']
  # SKIP: azure.resources.subscriptionfeatureregistrations.subscriptionfeatureregistrations_get — required_params: ['id']
  # SKIP: azure.resources.subscriptions.subscriptions_get — required_params: ['id']
  # SKIP: azure.resources.templatespecs.templatespecs_get — required_params: ['id']
  # SKIP: azure.resources.templatespecversions.templatespecversions_get — required_params: ['id']
