# Discovery YAML — guestconfiguration (Azure)
# Generated: 2026-04-15T10:39:30Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: guestconfiguration
services:
  client: guestconfiguration
  module: azure.mgmt.guestconfiguration
discovery:
  # ── operations.Operations_List [root] ──
  - discovery_id: azure.guestconfiguration.operations.operations_list
    calls:
      - action: operations.Operations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.guestconfiguration.guestconfigurationassignmentreports.guestconfigurationassignmentreports_get — required_params: ['id']
  # SKIP: azure.guestconfiguration.guestconfigurationassignmentreportsvmss.guestconfigurationassignmentreportsvmss_get — required_params: ['id']
  # SKIP: azure.guestconfiguration.guestconfigurationassignments.guestconfigurationassignments_get — required_params: ['id']
  # SKIP: azure.guestconfiguration.guestconfigurationassignmentsvmss.guestconfigurationassignmentsvmss_get — required_params: ['id']
  # SKIP: azure.guestconfiguration.guestconfigurationconnectedvmwarevsphereassignments.guestconfigurationconnectedvmwarevsphereassignments_get — required_params: ['id']
  # SKIP: azure.guestconfiguration.guestconfigurationconnectedvmwarevsphereassignmentsreports.guestconfigurationconnectedvmwarevsphereassignmentsreports_get — required_params: ['id']
  # SKIP: azure.guestconfiguration.guestconfigurationhcrpassignmentreports.guestconfigurationhcrpassignmentreports_get — required_params: ['id']
  # SKIP: azure.guestconfiguration.guestconfigurationhcrpassignments.guestconfigurationhcrpassignments_get — required_params: ['id']
