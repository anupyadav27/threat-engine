# Discovery YAML — maintenance (Azure)
# Generated: 2026-04-15T10:39:30Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: maintenance
services:
  client: maintenance
  module: azure.mgmt.maintenance
discovery:
  # ── applyupdateforresourcegroup.list [root] ──
  - discovery_id: azure.maintenance.applyupdateforresourcegroup.list
    calls:
      - action: applyupdateforresourcegroup.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        id: '{{ item.id }}'
        impact_duration_in_sec: '{{ item.impact_duration_in_sec }}'
        impact_type: '{{ item.impact_type }}'
        maintenance_scope: '{{ item.maintenance_scope }}'
        not_before: '{{ item.not_before }}'
        resource_id: '{{ item.resource_id }}'
  # ── applyupdates.list [root] ──
  - discovery_id: azure.maintenance.applyupdates.list
    calls:
      - action: applyupdates.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        id: '{{ item.id }}'
        impact_duration_in_sec: '{{ item.impact_duration_in_sec }}'
        impact_type: '{{ item.impact_type }}'
        maintenance_scope: '{{ item.maintenance_scope }}'
        not_before: '{{ item.not_before }}'
        resource_id: '{{ item.resource_id }}'
  # ── configurationassignmentswithinsubscription.list [root] ──
  - discovery_id: azure.maintenance.configurationassignmentswithinsubscription.list
    calls:
      - action: configurationassignmentswithinsubscription.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        id: '{{ item.id }}'
        impact_duration_in_sec: '{{ item.impact_duration_in_sec }}'
        impact_type: '{{ item.impact_type }}'
        maintenance_scope: '{{ item.maintenance_scope }}'
        not_before: '{{ item.not_before }}'
        resource_id: '{{ item.resource_id }}'
  # ── maintenanceconfigurations.list [root] ──
  - discovery_id: azure.maintenance.maintenanceconfigurations.list
    calls:
      - action: maintenanceconfigurations.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        id: '{{ item.id }}'
        impact_duration_in_sec: '{{ item.impact_duration_in_sec }}'
        impact_type: '{{ item.impact_type }}'
        maintenance_scope: '{{ item.maintenance_scope }}'
        not_before: '{{ item.not_before }}'
        resource_id: '{{ item.resource_id }}'
  # ── maintenanceconfigurationsforresourcegroup.list [root] ──
  - discovery_id: azure.maintenance.maintenanceconfigurationsforresourcegroup.list
    calls:
      - action: maintenanceconfigurationsforresourcegroup.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        id: '{{ item.id }}'
        impact_duration_in_sec: '{{ item.impact_duration_in_sec }}'
        impact_type: '{{ item.impact_type }}'
        maintenance_scope: '{{ item.maintenance_scope }}'
        not_before: '{{ item.not_before }}'
        resource_id: '{{ item.resource_id }}'
  # ── publicmaintenanceconfigurations.list [root] ──
  - discovery_id: azure.maintenance.publicmaintenanceconfigurations.list
    calls:
      - action: publicmaintenanceconfigurations.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        id: '{{ item.id }}'
        impact_duration_in_sec: '{{ item.impact_duration_in_sec }}'
        impact_type: '{{ item.impact_type }}'
        maintenance_scope: '{{ item.maintenance_scope }}'
        not_before: '{{ item.not_before }}'
        resource_id: '{{ item.resource_id }}'
  # ════════════════════════════════════════
  # Dependent operations (for_each + params)
  # ════════════════════════════════════════
  # ── applyupdates.get_parent [dependent] ──
  - discovery_id: azure.maintenance.applyupdates.get_parent
    for_each: azure.maintenance.applyupdates.list
    calls:
      - action: applyupdates.get_parent
        save_as: response
        on_error: continue
        params:
          provider_name: '{{ item.name }}'
          resource_name: '{{ item.name }}'
          apply_update_name: '{{ item.name }}'
    emit:
      as: item
      item:
        identity: '{{ response.identity }}'
        type: '{{ response.type }}'
        properties: '{{ response.properties }}'
        location: '{{ response.location }}'
        sku: '{{ response.sku }}'
        name: '{{ response.name }}'
        provisioning_state: '{{ response.provisioning_state }}'
        zones: '{{ response.zones }}'
        kind: '{{ response.kind }}'
        id: '{{ response.id }}'
        tags: '{{ response.tags }}'
  # ── configurationassignments.get_parent [dependent] ──
  - discovery_id: azure.maintenance.configurationassignments.get_parent
    for_each: azure.maintenance.applyupdates.list
    calls:
      - action: configurationassignments.get_parent
        save_as: response
        on_error: continue
        params:
          provider_name: '{{ item.name }}'
          resource_name: '{{ item.name }}'
          configuration_assignment_name: '{{ item.name }}'
    emit:
      as: item
      item:
        identity: '{{ response.identity }}'
        type: '{{ response.type }}'
        properties: '{{ response.properties }}'
        location: '{{ response.location }}'
        sku: '{{ response.sku }}'
        name: '{{ response.name }}'
        provisioning_state: '{{ response.provisioning_state }}'
        zones: '{{ response.zones }}'
        kind: '{{ response.kind }}'
        id: '{{ response.id }}'
        tags: '{{ response.tags }}'
  # ── configurationassignments.list [dependent] ──
  - discovery_id: azure.maintenance.configurationassignments.list
    for_each: azure.maintenance.applyupdates.list
    calls:
      - action: configurationassignments.list
        save_as: response
        on_error: continue
        params:
          provider_name: '{{ item.name }}'
          resource_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        id: '{{ item.id }}'
        impact_duration_in_sec: '{{ item.impact_duration_in_sec }}'
        impact_type: '{{ item.impact_type }}'
        maintenance_scope: '{{ item.maintenance_scope }}'
        not_before: '{{ item.not_before }}'
        resource_id: '{{ item.resource_id }}'
  # ── configurationassignments.list_parent [dependent] ──
  - discovery_id: azure.maintenance.configurationassignments.list_parent
    for_each: azure.maintenance.applyupdates.list
    calls:
      - action: configurationassignments.list_parent
        save_as: response
        on_error: continue
        params:
          provider_name: '{{ item.name }}'
          resource_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        id: '{{ item.id }}'
        impact_duration_in_sec: '{{ item.impact_duration_in_sec }}'
        impact_type: '{{ item.impact_type }}'
        maintenance_scope: '{{ item.maintenance_scope }}'
        not_before: '{{ item.not_before }}'
        resource_id: '{{ item.resource_id }}'
  # ── updates.list [dependent] ──
  - discovery_id: azure.maintenance.updates.list
    for_each: azure.maintenance.applyupdates.list
    calls:
      - action: updates.list
        save_as: response
        on_error: continue
        params:
          provider_name: '{{ item.name }}'
          resource_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        id: '{{ item.id }}'
        impact_duration_in_sec: '{{ item.impact_duration_in_sec }}'
        impact_type: '{{ item.impact_type }}'
        maintenance_scope: '{{ item.maintenance_scope }}'
        not_before: '{{ item.not_before }}'
        resource_id: '{{ item.resource_id }}'
  # ── updates.list_parent [dependent] ──
  - discovery_id: azure.maintenance.updates.list_parent
    for_each: azure.maintenance.applyupdates.list
    calls:
      - action: updates.list_parent
        save_as: response
        on_error: continue
        params:
          provider_name: '{{ item.name }}'
          resource_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        status: '{{ item.status }}'
        id: '{{ item.id }}'
        impact_duration_in_sec: '{{ item.impact_duration_in_sec }}'
        impact_type: '{{ item.impact_type }}'
        maintenance_scope: '{{ item.maintenance_scope }}'
        not_before: '{{ item.not_before }}'
        resource_id: '{{ item.resource_id }}'
