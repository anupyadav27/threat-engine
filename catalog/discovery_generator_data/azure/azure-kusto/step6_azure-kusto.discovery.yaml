# Discovery YAML — azure-kusto (Azure)
# Generated: 2026-04-15T10:39:29Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: azure-kusto
services:
  client: azure-kusto
  module: azure.mgmt.azure-kusto
discovery:
  # ── clusters.Clusters_List [root] ──
  - discovery_id: azure.azure-kusto.clusters.clusters_list
    calls:
      - action: clusters.Clusters_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── operations.Operations_List [root] ──
  - discovery_id: azure.azure-kusto.operations.operations_list
    calls:
      - action: operations.Operations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── skus.Skus_List [root] ──
  - discovery_id: azure.azure-kusto.skus.skus_list
    calls:
      - action: skus.Skus_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.azure-kusto.attacheddatabaseconfigurations.attacheddatabaseconfigurations_get — required_params: ['id']
  # SKIP: azure.azure-kusto.clusterprincipalassignments.clusterprincipalassignments_get — required_params: ['id']
  # SKIP: azure.azure-kusto.clusters.clusters_get — required_params: ['id']
  # SKIP: azure.azure-kusto.databaseprincipalassignments.databaseprincipalassignments_get — required_params: ['id']
  # SKIP: azure.azure-kusto.databases.databases_get — required_params: ['id']
  # SKIP: azure.azure-kusto.dataconnections.dataconnections_get — required_params: ['id']
  # SKIP: azure.azure-kusto.managedprivateendpoints.managedprivateendpoints_get — required_params: ['id']
  # SKIP: azure.azure-kusto.operationsresults.operationsresults_get — required_params: ['id']
  # SKIP: azure.azure-kusto.privateendpointconnections.privateendpointconnections_get — required_params: ['id']
  # SKIP: azure.azure-kusto.privatelinkresources.privatelinkresources_get — required_params: ['id']
  # SKIP: azure.azure-kusto.sandboxcustomimages.sandboxcustomimages_get — required_params: ['id']
  # SKIP: azure.azure-kusto.scripts.scripts_get — required_params: ['id']
