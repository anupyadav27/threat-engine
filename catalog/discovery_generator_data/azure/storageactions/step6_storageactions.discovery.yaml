# Discovery YAML — storageactions (Azure)
# Generated: 2026-04-15T10:39:31Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: storageactions
services:
  client: storageactions
  module: azure.mgmt.storageactions
discovery:
  # ── storagetasks.list_by_resource_group [root] ──
  - discovery_id: azure.storageactions.storagetasks.list_by_resource_group
    calls:
      - action: storagetasks.list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── storagetasks.list_by_subscription [root] ──
  - discovery_id: azure.storageactions.storagetasks.list_by_subscription
    calls:
      - action: storagetasks.list_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ════════════════════════════════════════
  # Dependent operations (for_each + params)
  # ════════════════════════════════════════
  # ── storagetaskassignment.list [dependent] ──
  - discovery_id: azure.storageactions.storagetaskassignment.list
    for_each: azure.storageactions.storagetasks.list_by_subscription
    calls:
      - action: storagetaskassignment.list
        save_as: response
        on_error: continue
        params:
          storage_task_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── storagetasksreport.list [dependent] ──
  - discovery_id: azure.storageactions.storagetasksreport.list
    for_each: azure.storageactions.storagetasks.list_by_subscription
    calls:
      - action: storagetasksreport.list
        save_as: response
        on_error: continue
        params:
          storage_task_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
