# Discovery YAML — batchai (Azure)
# Generated: 2026-04-15T10:39:29Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: batchai
services:
  client: batchai
  module: azure.mgmt.batchai
discovery:
  # ── workspaces.list [root] ──
  - discovery_id: azure.batchai.workspaces.list
    calls:
      - action: workspaces.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── workspaces.list_by_resource_group [root] ──
  - discovery_id: azure.batchai.workspaces.list_by_resource_group
    calls:
      - action: workspaces.list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ════════════════════════════════════════
  # Dependent operations (for_each + params)
  # ════════════════════════════════════════
  # ── clusters.list_by_workspace [dependent] ──
  - discovery_id: azure.batchai.clusters.list_by_workspace
    for_each: azure.batchai.workspaces.list
    calls:
      - action: clusters.list_by_workspace
        save_as: response
        on_error: continue
        params:
          workspace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── clusters.list_remote_login_information [dependent] ──
  - discovery_id: azure.batchai.clusters.list_remote_login_information
    for_each: azure.batchai.workspaces.list
    calls:
      - action: clusters.list_remote_login_information
        save_as: response
        on_error: continue
        params:
          workspace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── experiments.list_by_workspace [dependent] ──
  - discovery_id: azure.batchai.experiments.list_by_workspace
    for_each: azure.batchai.workspaces.list
    calls:
      - action: experiments.list_by_workspace
        save_as: response
        on_error: continue
        params:
          workspace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── fileservers.list_by_workspace [dependent] ──
  - discovery_id: azure.batchai.fileservers.list_by_workspace
    for_each: azure.batchai.workspaces.list
    calls:
      - action: fileservers.list_by_workspace
        save_as: response
        on_error: continue
        params:
          workspace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── jobs.list_by_experiment [dependent] ──
  - discovery_id: azure.batchai.jobs.list_by_experiment
    for_each: azure.batchai.workspaces.list
    calls:
      - action: jobs.list_by_experiment
        save_as: response
        on_error: continue
        params:
          workspace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── jobs.list_output_files [dependent] ──
  - discovery_id: azure.batchai.jobs.list_output_files
    for_each: azure.batchai.workspaces.list
    calls:
      - action: jobs.list_output_files
        save_as: response
        on_error: continue
        params:
          workspace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── jobs.list_remote_login_information [dependent] ──
  - discovery_id: azure.batchai.jobs.list_remote_login_information
    for_each: azure.batchai.workspaces.list
    calls:
      - action: jobs.list_remote_login_information
        save_as: response
        on_error: continue
        params:
          workspace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.batchai.usages.list — required_params: ['location']
