# Discovery YAML — oracle (Azure)
# Generated: 2026-04-15T10:39:31Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: oracle
services:
  client: oracle
  module: azure.mgmt.oracle
discovery:
  # ── operations.Operations_List [root] ──
  - discovery_id: azure.oracle.operations.operations_list
    calls:
      - action: operations.Operations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.oracle.autonomousdatabasebackups.autonomousdatabasebackups_get — required_params: ['id']
  # SKIP: azure.oracle.autonomousdatabasecharactersets.autonomousdatabasecharactersets_get — required_params: ['id']
  # SKIP: azure.oracle.autonomousdatabasenationalcharactersets.autonomousdatabasenationalcharactersets_get — required_params: ['id']
  # SKIP: azure.oracle.autonomousdatabases.autonomousdatabases_get — required_params: ['id']
  # SKIP: azure.oracle.autonomousdatabaseversions.autonomousdatabaseversions_get — required_params: ['id']
  # SKIP: azure.oracle.cloudexadatainfrastructures.cloudexadatainfrastructures_get — required_params: ['id']
  # SKIP: azure.oracle.cloudvmclusters.cloudvmclusters_get — required_params: ['id']
  # SKIP: azure.oracle.dbnodes.dbnodes_get — required_params: ['id']
  # SKIP: azure.oracle.dbservers.dbservers_get — required_params: ['id']
  # SKIP: azure.oracle.dbsystems.dbsystems_get — required_params: ['id']
  # SKIP: azure.oracle.dbsystemshapes.dbsystemshapes_get — required_params: ['id']
  # SKIP: azure.oracle.dbversions.dbversions_get — required_params: ['id']
  # SKIP: azure.oracle.dnsprivateviews.dnsprivateviews_get — required_params: ['id']
  # SKIP: azure.oracle.dnsprivatezones.dnsprivatezones_get — required_params: ['id']
  # SKIP: azure.oracle.exadbvmclusters.exadbvmclusters_get — required_params: ['id']
  # SKIP: azure.oracle.exascaledbnodes.exascaledbnodes_get — required_params: ['id']
  # SKIP: azure.oracle.exascaledbstoragevaults.exascaledbstoragevaults_get — required_params: ['id']
  # SKIP: azure.oracle.flexcomponents.flexcomponents_get — required_params: ['id']
  # SKIP: azure.oracle.giminorversions.giminorversions_get — required_params: ['id']
  # SKIP: azure.oracle.giversions.giversions_get — required_params: ['id']
  # SKIP: azure.oracle.networkanchors.networkanchors_get — required_params: ['id']
  # SKIP: azure.oracle.oraclesubscriptions.oraclesubscriptions_get — required_params: ['id']
  # SKIP: azure.oracle.resourceanchors.resourceanchors_get — required_params: ['id']
  # SKIP: azure.oracle.systemversions.systemversions_get — required_params: ['id']
  # SKIP: azure.oracle.virtualnetworkaddresses.virtualnetworkaddresses_get — required_params: ['id']
