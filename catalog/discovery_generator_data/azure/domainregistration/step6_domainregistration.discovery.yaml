# Discovery YAML — domainregistration (Azure)
# Generated: 2026-04-15T10:39:30Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: domainregistration
services:
  client: domainregistration
  module: azure.mgmt.domainregistration
discovery:
  # ── domains.Domains_List [root] ──
  - discovery_id: azure.domainregistration.domains.domains_list
    calls:
      - action: domains.Domains_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── topleveldomains.TopLevelDomains_List [root] ──
  - discovery_id: azure.domainregistration.topleveldomains.topleveldomains_list
    calls:
      - action: topleveldomains.TopLevelDomains_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.domainregistration.domains.domains_get — required_params: ['id']
  # SKIP: azure.domainregistration.topleveldomains.topleveldomains_get — required_params: ['id']
