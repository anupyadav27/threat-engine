# Discovery YAML — relay (Azure)
# Generated: 2026-04-15T10:39:31Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: relay
services:
  client: relay
  module: azure.mgmt.relay
discovery:
  # ── namespaces.list [root] ──
  - discovery_id: azure.relay.namespaces.list
    calls:
      - action: namespaces.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── namespaces.list_by_resource_group [root] ──
  - discovery_id: azure.relay.namespaces.list_by_resource_group
    calls:
      - action: namespaces.list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ════════════════════════════════════════
  # Dependent operations (for_each + params)
  # ════════════════════════════════════════
  # ── hybridconnections.list_authorization_rules [dependent] ──
  - discovery_id: azure.relay.hybridconnections.list_authorization_rules
    for_each: azure.relay.hybridconnections.get_authorization_rule
    calls:
      - action: hybridconnections.list_authorization_rules
        save_as: response
        on_error: continue
        params:
          namespace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── hybridconnections.list_by_namespace [dependent] ──
  - discovery_id: azure.relay.hybridconnections.list_by_namespace
    for_each: azure.relay.hybridconnections.get_authorization_rule
    calls:
      - action: hybridconnections.list_by_namespace
        save_as: response
        on_error: continue
        params:
          namespace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── hybridconnections.list_keys [dependent] ──
  - discovery_id: azure.relay.hybridconnections.list_keys
    for_each: azure.relay.hybridconnections.get_authorization_rule
    calls:
      - action: hybridconnections.list_keys
        save_as: response
        on_error: continue
        params:
          namespace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── namespaces.get_authorization_rule [dependent] ──
  - discovery_id: azure.relay.namespaces.get_authorization_rule
    for_each: azure.relay.hybridconnections.get_authorization_rule
    calls:
      - action: namespaces.get_authorization_rule
        save_as: response
        on_error: continue
        params:
          namespace_name: '{{ item.name }}'
    emit:
      as: item
      item:
        identity: '{{ response.identity }}'
        type: '{{ response.type }}'
        properties: '{{ response.properties }}'
        location: '{{ response.location }}'
        sku: '{{ response.sku }}'
        name: '{{ response.name }}'
        provisioning_state: '{{ response.provisioning_state }}'
        zones: '{{ response.zones }}'
        kind: '{{ response.kind }}'
        id: '{{ response.id }}'
        tags: '{{ response.tags }}'
  # ── namespaces.list_authorization_rules [dependent] ──
  - discovery_id: azure.relay.namespaces.list_authorization_rules
    for_each: azure.relay.hybridconnections.get_authorization_rule
    calls:
      - action: namespaces.list_authorization_rules
        save_as: response
        on_error: continue
        params:
          namespace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── namespaces.list_keys [dependent] ──
  - discovery_id: azure.relay.namespaces.list_keys
    for_each: azure.relay.hybridconnections.get_authorization_rule
    calls:
      - action: namespaces.list_keys
        save_as: response
        on_error: continue
        params:
          namespace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── wcfrelays.get_authorization_rule [dependent] ──
  - discovery_id: azure.relay.wcfrelays.get_authorization_rule
    for_each: azure.relay.hybridconnections.get_authorization_rule
    calls:
      - action: wcfrelays.get_authorization_rule
        save_as: response
        on_error: continue
        params:
          namespace_name: '{{ item.name }}'
    emit:
      as: item
      item:
        identity: '{{ response.identity }}'
        type: '{{ response.type }}'
        properties: '{{ response.properties }}'
        location: '{{ response.location }}'
        sku: '{{ response.sku }}'
        name: '{{ response.name }}'
        provisioning_state: '{{ response.provisioning_state }}'
        zones: '{{ response.zones }}'
        kind: '{{ response.kind }}'
        id: '{{ response.id }}'
        tags: '{{ response.tags }}'
  # ── wcfrelays.list_authorization_rules [dependent] ──
  - discovery_id: azure.relay.wcfrelays.list_authorization_rules
    for_each: azure.relay.hybridconnections.get_authorization_rule
    calls:
      - action: wcfrelays.list_authorization_rules
        save_as: response
        on_error: continue
        params:
          namespace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── wcfrelays.list_by_namespace [dependent] ──
  - discovery_id: azure.relay.wcfrelays.list_by_namespace
    for_each: azure.relay.hybridconnections.get_authorization_rule
    calls:
      - action: wcfrelays.list_by_namespace
        save_as: response
        on_error: continue
        params:
          namespace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── wcfrelays.list_keys [dependent] ──
  - discovery_id: azure.relay.wcfrelays.list_keys
    for_each: azure.relay.hybridconnections.get_authorization_rule
    calls:
      - action: wcfrelays.list_keys
        save_as: response
        on_error: continue
        params:
          namespace_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.relay.hybridconnections.get_authorization_rule — required_params: ['namespace_name', 'hybrid_connection_name', 'authorization_rule_name']
