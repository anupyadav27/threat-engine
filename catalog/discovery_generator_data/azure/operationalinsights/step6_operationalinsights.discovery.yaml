# Discovery YAML — operationalinsights (Azure)
# Generated: 2026-04-15T10:39:31Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: operationalinsights
services:
  client: operationalinsights
  module: azure.mgmt.operationalinsights
discovery:
  # ── clusters.Clusters_List [root] ──
  - discovery_id: azure.operationalinsights.clusters.clusters_list
    calls:
      - action: clusters.Clusters_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── deletedworkspaces.DeletedWorkspaces_List [root] ──
  - discovery_id: azure.operationalinsights.deletedworkspaces.deletedworkspaces_list
    calls:
      - action: deletedworkspaces.DeletedWorkspaces_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── operations.Operations_List [root] ──
  - discovery_id: azure.operationalinsights.operations.operations_list
    calls:
      - action: operations.Operations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── querypacks.QueryPacks_List [root] ──
  - discovery_id: azure.operationalinsights.querypacks.querypacks_list
    calls:
      - action: querypacks.QueryPacks_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── workspaces.Workspaces_List [root] ──
  - discovery_id: azure.operationalinsights.workspaces.workspaces_list
    calls:
      - action: workspaces.Workspaces_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.operationalinsights.clusters.clusters_get — required_params: ['id']
  # SKIP: azure.operationalinsights.dataexports.dataexports_get — required_params: ['id']
  # SKIP: azure.operationalinsights.datasources.datasources_get — required_params: ['id']
  # SKIP: azure.operationalinsights.linkedservices.linkedservices_get — required_params: ['id']
  # SKIP: azure.operationalinsights.linkedstorageaccounts.linkedstorageaccounts_get — required_params: ['id']
  # SKIP: azure.operationalinsights.operationstatuses.operationstatuses_get — required_params: ['id']
  # SKIP: azure.operationalinsights.queries.queries_get — required_params: ['id']
  # SKIP: azure.operationalinsights.querypacks.querypacks_get — required_params: ['id']
  # SKIP: azure.operationalinsights.savedsearches.savedsearches_get — required_params: ['id']
  # SKIP: azure.operationalinsights.schema.schema_get — required_params: ['id']
  # SKIP: azure.operationalinsights.storageinsightconfigs.storageinsightconfigs_get — required_params: ['id']
  # SKIP: azure.operationalinsights.storageinsights.storageinsights_get — required_params: ['id']
  # SKIP: azure.operationalinsights.summarylogs.summarylogs_get — required_params: ['id']
  # SKIP: azure.operationalinsights.tables.tables_get — required_params: ['id']
  # SKIP: azure.operationalinsights.workspaces.workspaces_get — required_params: ['id']
