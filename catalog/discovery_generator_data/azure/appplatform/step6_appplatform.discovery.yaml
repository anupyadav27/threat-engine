# Discovery YAML — appplatform (Azure)
# Generated: 2026-04-15T10:39:29Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: appplatform
services:
  client: appplatform
  module: azure.mgmt.appplatform
discovery:
  # ── runtimeversions.list_runtime_versions [root] ──
  - discovery_id: azure.appplatform.runtimeversions.list_runtime_versions
    calls:
      - action: runtimeversions.list_runtime_versions
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        platform: '{{ item.platform }}'
        version: '{{ item.version }}'
  # ── services.list [root] ──
  - discovery_id: azure.appplatform.services.list
    calls:
      - action: services.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── services.list_by_subscription [root] ──
  - discovery_id: azure.appplatform.services.list_by_subscription
    calls:
      - action: services.list_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
        system_data: '{{ item.system_data }}'
  # ── skus.list [root] ──
  - discovery_id: azure.appplatform.skus.list
    calls:
      - action: skus.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ════════════════════════════════════════
  # Dependent operations (for_each + params)
  # ════════════════════════════════════════
  # ── apiportalcustomdomains.list [dependent] ──
  - discovery_id: azure.appplatform.apiportalcustomdomains.list
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: apiportalcustomdomains.list
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── apiportals.list [dependent] ──
  - discovery_id: azure.appplatform.apiportals.list
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: apiportals.list
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── apms.list [dependent] ──
  - discovery_id: azure.appplatform.apms.list
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: apms.list
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── apms.list_secret_keys [dependent] ──
  - discovery_id: azure.appplatform.apms.list_secret_keys
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: apms.list_secret_keys
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── applicationaccelerators.list [dependent] ──
  - discovery_id: azure.appplatform.applicationaccelerators.list
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: applicationaccelerators.list
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── applicationliveviews.list [dependent] ──
  - discovery_id: azure.appplatform.applicationliveviews.list
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: applicationliveviews.list
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── apps.get_resource_upload_url [dependent] ──
  - discovery_id: azure.appplatform.apps.get_resource_upload_url
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: apps.get_resource_upload_url
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      item:
        identity: '{{ response.identity }}'
        type: '{{ response.type }}'
        properties: '{{ response.properties }}'
        location: '{{ response.location }}'
        sku: '{{ response.sku }}'
        name: '{{ response.name }}'
        provisioning_state: '{{ response.provisioning_state }}'
        zones: '{{ response.zones }}'
        kind: '{{ response.kind }}'
        id: '{{ response.id }}'
        tags: '{{ response.tags }}'
  # ── apps.list [dependent] ──
  - discovery_id: azure.appplatform.apps.list
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: apps.list
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── bindings.list [dependent] ──
  - discovery_id: azure.appplatform.bindings.list
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: bindings.list
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── buildpackbinding.list [dependent] ──
  - discovery_id: azure.appplatform.buildpackbinding.list
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: buildpackbinding.list
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── buildpackbinding.list_for_cluster [dependent] ──
  - discovery_id: azure.appplatform.buildpackbinding.list_for_cluster
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: buildpackbinding.list_for_cluster
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── buildservice.get_build [dependent] ──
  - discovery_id: azure.appplatform.buildservice.get_build
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: buildservice.get_build
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      item:
        identity: '{{ response.identity }}'
        type: '{{ response.type }}'
        properties: '{{ response.properties }}'
        location: '{{ response.location }}'
        sku: '{{ response.sku }}'
        name: '{{ response.name }}'
        provisioning_state: '{{ response.provisioning_state }}'
        zones: '{{ response.zones }}'
        kind: '{{ response.kind }}'
        id: '{{ response.id }}'
        tags: '{{ response.tags }}'
  # ── buildservice.get_build_result [dependent] ──
  - discovery_id: azure.appplatform.buildservice.get_build_result
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: buildservice.get_build_result
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      item:
        identity: '{{ response.identity }}'
        type: '{{ response.type }}'
        properties: '{{ response.properties }}'
        location: '{{ response.location }}'
        sku: '{{ response.sku }}'
        name: '{{ response.name }}'
        provisioning_state: '{{ response.provisioning_state }}'
        zones: '{{ response.zones }}'
        kind: '{{ response.kind }}'
        id: '{{ response.id }}'
        tags: '{{ response.tags }}'
  # ── buildservice.get_build_result_log [dependent] ──
  - discovery_id: azure.appplatform.buildservice.get_build_result_log
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: buildservice.get_build_result_log
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      item:
        identity: '{{ response.identity }}'
        type: '{{ response.type }}'
        properties: '{{ response.properties }}'
        location: '{{ response.location }}'
        sku: '{{ response.sku }}'
        name: '{{ response.name }}'
        provisioning_state: '{{ response.provisioning_state }}'
        zones: '{{ response.zones }}'
        kind: '{{ response.kind }}'
        id: '{{ response.id }}'
        tags: '{{ response.tags }}'
  # ── buildservice.get_build_service [dependent] ──
  - discovery_id: azure.appplatform.buildservice.get_build_service
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: buildservice.get_build_service
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      item:
        identity: '{{ response.identity }}'
        type: '{{ response.type }}'
        properties: '{{ response.properties }}'
        location: '{{ response.location }}'
        sku: '{{ response.sku }}'
        name: '{{ response.name }}'
        provisioning_state: '{{ response.provisioning_state }}'
        zones: '{{ response.zones }}'
        kind: '{{ response.kind }}'
        id: '{{ response.id }}'
        tags: '{{ response.tags }}'
  # ── buildservice.get_resource_upload_url [dependent] ──
  - discovery_id: azure.appplatform.buildservice.get_resource_upload_url
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: buildservice.get_resource_upload_url
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      item:
        identity: '{{ response.identity }}'
        type: '{{ response.type }}'
        properties: '{{ response.properties }}'
        location: '{{ response.location }}'
        sku: '{{ response.sku }}'
        name: '{{ response.name }}'
        provisioning_state: '{{ response.provisioning_state }}'
        zones: '{{ response.zones }}'
        kind: '{{ response.kind }}'
        id: '{{ response.id }}'
        tags: '{{ response.tags }}'
  # ── buildservice.get_supported_buildpack [dependent] ──
  - discovery_id: azure.appplatform.buildservice.get_supported_buildpack
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: buildservice.get_supported_buildpack
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      item:
        identity: '{{ response.identity }}'
        type: '{{ response.type }}'
        properties: '{{ response.properties }}'
        location: '{{ response.location }}'
        sku: '{{ response.sku }}'
        name: '{{ response.name }}'
        provisioning_state: '{{ response.provisioning_state }}'
        zones: '{{ response.zones }}'
        kind: '{{ response.kind }}'
        id: '{{ response.id }}'
        tags: '{{ response.tags }}'
  # ── buildservice.get_supported_stack [dependent] ──
  - discovery_id: azure.appplatform.buildservice.get_supported_stack
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: buildservice.get_supported_stack
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      item:
        identity: '{{ response.identity }}'
        type: '{{ response.type }}'
        properties: '{{ response.properties }}'
        location: '{{ response.location }}'
        sku: '{{ response.sku }}'
        name: '{{ response.name }}'
        provisioning_state: '{{ response.provisioning_state }}'
        zones: '{{ response.zones }}'
        kind: '{{ response.kind }}'
        id: '{{ response.id }}'
        tags: '{{ response.tags }}'
  # ── buildservice.list_build_results [dependent] ──
  - discovery_id: azure.appplatform.buildservice.list_build_results
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: buildservice.list_build_results
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── buildservice.list_build_services [dependent] ──
  - discovery_id: azure.appplatform.buildservice.list_build_services
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: buildservice.list_build_services
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── buildservice.list_builds [dependent] ──
  - discovery_id: azure.appplatform.buildservice.list_builds
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: buildservice.list_builds
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── buildservice.list_supported_buildpacks [dependent] ──
  - discovery_id: azure.appplatform.buildservice.list_supported_buildpacks
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: buildservice.list_supported_buildpacks
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── buildservice.list_supported_stacks [dependent] ──
  - discovery_id: azure.appplatform.buildservice.list_supported_stacks
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: buildservice.list_supported_stacks
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── buildserviceagentpool.list [dependent] ──
  - discovery_id: azure.appplatform.buildserviceagentpool.list
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: buildserviceagentpool.list
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── buildservicebuilder.list [dependent] ──
  - discovery_id: azure.appplatform.buildservicebuilder.list
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: buildservicebuilder.list
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── buildservicebuilder.list_deployments [dependent] ──
  - discovery_id: azure.appplatform.buildservicebuilder.list_deployments
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: buildservicebuilder.list_deployments
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── certificates.list [dependent] ──
  - discovery_id: azure.appplatform.certificates.list
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: certificates.list
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── configurationservices.list [dependent] ──
  - discovery_id: azure.appplatform.configurationservices.list
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: configurationservices.list
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── containerregistries.list [dependent] ──
  - discovery_id: azure.appplatform.containerregistries.list
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: containerregistries.list
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── customdomains.list [dependent] ──
  - discovery_id: azure.appplatform.customdomains.list
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: customdomains.list
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── customizedaccelerators.list [dependent] ──
  - discovery_id: azure.appplatform.customizedaccelerators.list
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: customizedaccelerators.list
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── deployments.get_log_file_url [dependent] ──
  - discovery_id: azure.appplatform.deployments.get_log_file_url
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: deployments.get_log_file_url
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      item:
        identity: '{{ response.identity }}'
        type: '{{ response.type }}'
        properties: '{{ response.properties }}'
        location: '{{ response.location }}'
        sku: '{{ response.sku }}'
        name: '{{ response.name }}'
        provisioning_state: '{{ response.provisioning_state }}'
        zones: '{{ response.zones }}'
        kind: '{{ response.kind }}'
        id: '{{ response.id }}'
        tags: '{{ response.tags }}'
  # ── deployments.get_remote_debugging_config [dependent] ──
  - discovery_id: azure.appplatform.deployments.get_remote_debugging_config
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: deployments.get_remote_debugging_config
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      item:
        identity: '{{ response.identity }}'
        type: '{{ response.type }}'
        properties: '{{ response.properties }}'
        location: '{{ response.location }}'
        sku: '{{ response.sku }}'
        name: '{{ response.name }}'
        provisioning_state: '{{ response.provisioning_state }}'
        zones: '{{ response.zones }}'
        kind: '{{ response.kind }}'
        id: '{{ response.id }}'
        tags: '{{ response.tags }}'
  # ── deployments.list [dependent] ──
  - discovery_id: azure.appplatform.deployments.list
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: deployments.list
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── deployments.list_for_cluster [dependent] ──
  - discovery_id: azure.appplatform.deployments.list_for_cluster
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: deployments.list_for_cluster
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── devtoolportals.list [dependent] ──
  - discovery_id: azure.appplatform.devtoolportals.list
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: devtoolportals.list
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── gatewaycustomdomains.list [dependent] ──
  - discovery_id: azure.appplatform.gatewaycustomdomains.list
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: gatewaycustomdomains.list
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── gatewayrouteconfigs.list [dependent] ──
  - discovery_id: azure.appplatform.gatewayrouteconfigs.list
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: gatewayrouteconfigs.list
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── gateways.list [dependent] ──
  - discovery_id: azure.appplatform.gateways.list
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: gateways.list
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── gateways.list_env_secrets [dependent] ──
  - discovery_id: azure.appplatform.gateways.list_env_secrets
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: gateways.list_env_secrets
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── predefinedaccelerators.list [dependent] ──
  - discovery_id: azure.appplatform.predefinedaccelerators.list
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: predefinedaccelerators.list
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── serviceregistries.list [dependent] ──
  - discovery_id: azure.appplatform.serviceregistries.list
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: serviceregistries.list
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── services.list_globally_enabled_apms [dependent] ──
  - discovery_id: azure.appplatform.services.list_globally_enabled_apms
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: services.list_globally_enabled_apms
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── services.list_supported_apm_types [dependent] ──
  - discovery_id: azure.appplatform.services.list_supported_apm_types
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: services.list_supported_apm_types
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── services.list_supported_server_versions [dependent] ──
  - discovery_id: azure.appplatform.services.list_supported_server_versions
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: services.list_supported_server_versions
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        server: '{{ item.server }}'
        version: '{{ item.version }}'
  # ── services.list_test_keys [dependent] ──
  - discovery_id: azure.appplatform.services.list_test_keys
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: services.list_test_keys
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── storages.list [dependent] ──
  - discovery_id: azure.appplatform.storages.list
    for_each: azure.appplatform.services.list_by_subscription
    calls:
      - action: storages.list
        save_as: response
        on_error: continue
        params:
          service_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
