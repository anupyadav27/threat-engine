# Discovery YAML — azurestackhci (Azure)
# Generated: 2026-04-15T10:39:29Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: azurestackhci
services:
  client: azurestackhci
  module: azure.mgmt.azurestackhci
discovery:
  # ── galleryimages.GalleryImages_List [root] ──
  - discovery_id: azure.azurestackhci.galleryimages.galleryimages_list
    calls:
      - action: galleryimages.GalleryImages_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── logicalnetworks.LogicalNetworks_List [root] ──
  - discovery_id: azure.azurestackhci.logicalnetworks.logicalnetworks_list
    calls:
      - action: logicalnetworks.LogicalNetworks_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── marketplacegalleryimages.MarketplaceGalleryImages_List [root] ──
  - discovery_id: azure.azurestackhci.marketplacegalleryimages.marketplacegalleryimages_list
    calls:
      - action: marketplacegalleryimages.MarketplaceGalleryImages_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── networkinterfaces.NetworkInterfaces_List [root] ──
  - discovery_id: azure.azurestackhci.networkinterfaces.networkinterfaces_list
    calls:
      - action: networkinterfaces.NetworkInterfaces_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── operations.Operations_List [root] ──
  - discovery_id: azure.azurestackhci.operations.operations_list
    calls:
      - action: operations.Operations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── storagecontainers.StorageContainers_List [root] ──
  - discovery_id: azure.azurestackhci.storagecontainers.storagecontainers_list
    calls:
      - action: storagecontainers.StorageContainers_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── virtualharddisks.VirtualHardDisks_List [root] ──
  - discovery_id: azure.azurestackhci.virtualharddisks.virtualharddisks_list
    calls:
      - action: virtualharddisks.VirtualHardDisks_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.azurestackhci.arcsettings.arcsettings_get — required_params: ['id']
  # SKIP: azure.azurestackhci.clusters.clusters_get — required_params: ['id']
  # SKIP: azure.azurestackhci.deploymentsettings.deploymentsettings_get — required_params: ['id']
  # SKIP: azure.azurestackhci.edgedevicejobs.edgedevicejobs_get — required_params: ['id']
  # SKIP: azure.azurestackhci.edgedevices.edgedevices_get — required_params: ['id']
  # SKIP: azure.azurestackhci.extensions.extensions_get — required_params: ['id']
  # SKIP: azure.azurestackhci.galleryimages.galleryimages_get — required_params: ['id']
  # SKIP: azure.azurestackhci.guestagent.guestagent_get — required_params: ['id']
  # SKIP: azure.azurestackhci.hybrididentitymetadata.hybrididentitymetadata_get — required_params: ['id']
  # SKIP: azure.azurestackhci.logicalnetworks.logicalnetworks_get — required_params: ['id']
  # SKIP: azure.azurestackhci.marketplacegalleryimages.marketplacegalleryimages_get — required_params: ['id']
  # SKIP: azure.azurestackhci.networkinterfaces.networkinterfaces_get — required_params: ['id']
  # SKIP: azure.azurestackhci.offers.offers_get — required_params: ['id']
  # SKIP: azure.azurestackhci.publishers.publishers_get — required_params: ['id']
  # SKIP: azure.azurestackhci.securitysettings.securitysettings_get — required_params: ['id']
  # SKIP: azure.azurestackhci.skus.skus_get — required_params: ['id']
  # SKIP: azure.azurestackhci.storagecontainers.storagecontainers_get — required_params: ['id']
  # SKIP: azure.azurestackhci.updateruns.updateruns_get — required_params: ['id']
  # SKIP: azure.azurestackhci.updates.updates_get — required_params: ['id']
  # SKIP: azure.azurestackhci.updatesummaries.updatesummaries_get — required_params: ['id']
  # SKIP: azure.azurestackhci.validatedsolutionrecipes.validatedsolutionrecipes_get — required_params: ['id']
  # SKIP: azure.azurestackhci.virtualharddisks.virtualharddisks_get — required_params: ['id']
  # SKIP: azure.azurestackhci.virtualmachineinstances.virtualmachineinstances_get — required_params: ['id']
