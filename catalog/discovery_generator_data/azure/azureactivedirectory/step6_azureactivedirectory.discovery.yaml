# Discovery YAML — azureactivedirectory (Azure)
# Generated: 2026-04-15T10:39:29Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: azureactivedirectory
services:
  client: azureactivedirectory
  module: azure.mgmt.azureactivedirectory
discovery:
  # ── diagnosticsettings.DiagnosticSettings_List [root] ──
  - discovery_id: azure.azureactivedirectory.diagnosticsettings.diagnosticsettings_list
    calls:
      - action: diagnosticsettings.DiagnosticSettings_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── diagnosticsettingscategory.DiagnosticSettingsCategory_List [root] ──
  - discovery_id: azure.azureactivedirectory.diagnosticsettingscategory.diagnosticsettingscategory_list
    calls:
      - action: diagnosticsettingscategory.DiagnosticSettingsCategory_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── operations.Operations_List [root] ──
  - discovery_id: azure.azureactivedirectory.operations.operations_list
    calls:
      - action: operations.Operations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── privatelinkforazuread.privateLinkForAzureAd_List [root] ──
  - discovery_id: azure.azureactivedirectory.privatelinkforazuread.privatelinkforazuread_list
    calls:
      - action: privatelinkforazuread.privateLinkForAzureAd_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.azureactivedirectory.diagnosticsettings.diagnosticsettings_get — required_params: ['id']
  # SKIP: azure.azureactivedirectory.privateendpointconnections.privateendpointconnections_get — required_params: ['id']
  # SKIP: azure.azureactivedirectory.privatelinkforazuread.privatelinkforazuread_get — required_params: ['id']
  # SKIP: azure.azureactivedirectory.privatelinkresources.privatelinkresources_get — required_params: ['id']
