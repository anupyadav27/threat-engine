# Discovery YAML — webpubsub (Azure)
# Generated: 2026-04-15T10:39:32Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: webpubsub
services:
  client: webpubsub
  module: azure.mgmt.webpubsub
discovery:
  # ── webpubsub.list_by_resource_group [root] ──
  - discovery_id: azure.webpubsub.webpubsub.list_by_resource_group
    calls:
      - action: webpubsub.list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        public_network_access: '{{ item.public_network_access }}'
        type: '{{ item.type }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
        disable_aad_auth: '{{ item.disable_aad_auth }}'
        disable_local_auth: '{{ item.disable_local_auth }}'
        external_ip: '{{ item.external_ip }}'
        host_name: '{{ item.host_name }}'
        host_name_prefix: '{{ item.host_name_prefix }}'
        live_trace_configuration: '{{ item.live_trace_configuration }}'
        network_ac_ls: '{{ item.network_ac_ls }}'
        private_endpoint_connections: '{{ item.private_endpoint_connections }}'
        public_port: '{{ item.public_port }}'
        region_endpoint_enabled: '{{ item.region_endpoint_enabled }}'
        resource_log_configuration: '{{ item.resource_log_configuration }}'
        resource_stopped: '{{ item.resource_stopped }}'
        server_port: '{{ item.server_port }}'
        shared_private_link_resources: '{{ item.shared_private_link_resources }}'
        socket_io: '{{ item.socket_io }}'
        system_data: '{{ item.system_data }}'
        tls: '{{ item.tls }}'
        version: '{{ item.version }}'
  # ── webpubsub.list_by_subscription [root] ──
  - discovery_id: azure.webpubsub.webpubsub.list_by_subscription
    calls:
      - action: webpubsub.list_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        public_network_access: '{{ item.public_network_access }}'
        type: '{{ item.type }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
        disable_aad_auth: '{{ item.disable_aad_auth }}'
        disable_local_auth: '{{ item.disable_local_auth }}'
        external_ip: '{{ item.external_ip }}'
        host_name: '{{ item.host_name }}'
        host_name_prefix: '{{ item.host_name_prefix }}'
        live_trace_configuration: '{{ item.live_trace_configuration }}'
        network_ac_ls: '{{ item.network_ac_ls }}'
        private_endpoint_connections: '{{ item.private_endpoint_connections }}'
        public_port: '{{ item.public_port }}'
        region_endpoint_enabled: '{{ item.region_endpoint_enabled }}'
        resource_log_configuration: '{{ item.resource_log_configuration }}'
        resource_stopped: '{{ item.resource_stopped }}'
        server_port: '{{ item.server_port }}'
        shared_private_link_resources: '{{ item.shared_private_link_resources }}'
        socket_io: '{{ item.socket_io }}'
        system_data: '{{ item.system_data }}'
        tls: '{{ item.tls }}'
        version: '{{ item.version }}'
  # ════════════════════════════════════════
  # Dependent operations (for_each + params)
  # ════════════════════════════════════════
  # ── webpubsub.list_keys [dependent] ──
  - discovery_id: azure.webpubsub.webpubsub.list_keys
    for_each: azure.webpubsub.webpubsub.list_by_subscription
    calls:
      - action: webpubsub.list_keys
        save_as: response
        on_error: continue
        params:
          resource_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── webpubsub.list_replica_skus [dependent] ──
  - discovery_id: azure.webpubsub.webpubsub.list_replica_skus
    for_each: azure.webpubsub.webpubsub.list_by_subscription
    calls:
      - action: webpubsub.list_replica_skus
        save_as: response
        on_error: continue
        params:
          resource_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        capacity: '{{ item.capacity }}'
        resource_type: '{{ item.resource_type }}'
  # ── webpubsub.list_skus [dependent] ──
  - discovery_id: azure.webpubsub.webpubsub.list_skus
    for_each: azure.webpubsub.webpubsub.list_by_subscription
    calls:
      - action: webpubsub.list_skus
        save_as: response
        on_error: continue
        params:
          resource_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        capacity: '{{ item.capacity }}'
        resource_type: '{{ item.resource_type }}'
  # ── webpubsubcustomcertificates.list [dependent] ──
  - discovery_id: azure.webpubsub.webpubsubcustomcertificates.list
    for_each: azure.webpubsub.webpubsub.list_by_subscription
    calls:
      - action: webpubsubcustomcertificates.list
        save_as: response
        on_error: continue
        params:
          resource_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        status: '{{ item.status }}'
        id: '{{ item.id }}'
        group_id: '{{ item.group_id }}'
        private_link_resource_id: '{{ item.private_link_resource_id }}'
        request_message: '{{ item.request_message }}'
        system_data: '{{ item.system_data }}'
  # ── webpubsubcustomdomains.list [dependent] ──
  - discovery_id: azure.webpubsub.webpubsubcustomdomains.list
    for_each: azure.webpubsub.webpubsub.list_by_subscription
    calls:
      - action: webpubsubcustomdomains.list
        save_as: response
        on_error: continue
        params:
          resource_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        status: '{{ item.status }}'
        id: '{{ item.id }}'
        group_id: '{{ item.group_id }}'
        private_link_resource_id: '{{ item.private_link_resource_id }}'
        request_message: '{{ item.request_message }}'
        system_data: '{{ item.system_data }}'
  # ── webpubsubhubs.list [dependent] ──
  - discovery_id: azure.webpubsub.webpubsubhubs.list
    for_each: azure.webpubsub.webpubsub.list_by_subscription
    calls:
      - action: webpubsubhubs.list
        save_as: response
        on_error: continue
        params:
          resource_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        status: '{{ item.status }}'
        id: '{{ item.id }}'
        group_id: '{{ item.group_id }}'
        private_link_resource_id: '{{ item.private_link_resource_id }}'
        request_message: '{{ item.request_message }}'
        system_data: '{{ item.system_data }}'
  # ── webpubsubprivateendpointconnections.list [dependent] ──
  - discovery_id: azure.webpubsub.webpubsubprivateendpointconnections.list
    for_each: azure.webpubsub.webpubsub.list_by_subscription
    calls:
      - action: webpubsubprivateendpointconnections.list
        save_as: response
        on_error: continue
        params:
          resource_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        status: '{{ item.status }}'
        id: '{{ item.id }}'
        group_id: '{{ item.group_id }}'
        private_link_resource_id: '{{ item.private_link_resource_id }}'
        request_message: '{{ item.request_message }}'
        system_data: '{{ item.system_data }}'
  # ── webpubsubprivatelinkresources.list [dependent] ──
  - discovery_id: azure.webpubsub.webpubsubprivatelinkresources.list
    for_each: azure.webpubsub.webpubsub.list_by_subscription
    calls:
      - action: webpubsubprivatelinkresources.list
        save_as: response
        on_error: continue
        params:
          resource_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        status: '{{ item.status }}'
        id: '{{ item.id }}'
        group_id: '{{ item.group_id }}'
        private_link_resource_id: '{{ item.private_link_resource_id }}'
        request_message: '{{ item.request_message }}'
        system_data: '{{ item.system_data }}'
  # ── webpubsubreplicas.list [dependent] ──
  - discovery_id: azure.webpubsub.webpubsubreplicas.list
    for_each: azure.webpubsub.webpubsub.list_by_subscription
    calls:
      - action: webpubsubreplicas.list
        save_as: response
        on_error: continue
        params:
          resource_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        status: '{{ item.status }}'
        id: '{{ item.id }}'
        group_id: '{{ item.group_id }}'
        private_link_resource_id: '{{ item.private_link_resource_id }}'
        request_message: '{{ item.request_message }}'
        system_data: '{{ item.system_data }}'
  # ── webpubsubreplicasharedprivatelinkresources.list [dependent] ──
  - discovery_id: azure.webpubsub.webpubsubreplicasharedprivatelinkresources.list
    for_each: azure.webpubsub.webpubsub.list_by_subscription
    calls:
      - action: webpubsubreplicasharedprivatelinkresources.list
        save_as: response
        on_error: continue
        params:
          resource_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        status: '{{ item.status }}'
        id: '{{ item.id }}'
        group_id: '{{ item.group_id }}'
        private_link_resource_id: '{{ item.private_link_resource_id }}'
        request_message: '{{ item.request_message }}'
        system_data: '{{ item.system_data }}'
  # ── webpubsubsharedprivatelinkresources.list [dependent] ──
  - discovery_id: azure.webpubsub.webpubsubsharedprivatelinkresources.list
    for_each: azure.webpubsub.webpubsub.list_by_subscription
    calls:
      - action: webpubsubsharedprivatelinkresources.list
        save_as: response
        on_error: continue
        params:
          resource_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        status: '{{ item.status }}'
        id: '{{ item.id }}'
        group_id: '{{ item.group_id }}'
        private_link_resource_id: '{{ item.private_link_resource_id }}'
        request_message: '{{ item.request_message }}'
        system_data: '{{ item.system_data }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.webpubsub.usages.list — required_params: ['location']
