# Discovery YAML — servicenetworking (Azure)
# Generated: 2026-04-15T10:39:31Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: servicenetworking
services:
  client: servicenetworking
  module: azure.mgmt.servicenetworking
discovery:
  # ── trafficcontrollerinterface.list_by_resource_group [root] ──
  - discovery_id: azure.servicenetworking.trafficcontrollerinterface.list_by_resource_group
    calls:
      - action: trafficcontrollerinterface.list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── trafficcontrollerinterface.list_by_subscription [root] ──
  - discovery_id: azure.servicenetworking.trafficcontrollerinterface.list_by_subscription
    calls:
      - action: trafficcontrollerinterface.list_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ════════════════════════════════════════
  # Dependent operations (for_each + params)
  # ════════════════════════════════════════
  # ── associationsinterface.list_by_traffic_controller [dependent] ──
  - discovery_id: azure.servicenetworking.associationsinterface.list_by_traffic_controller
    for_each: azure.servicenetworking.trafficcontrollerinterface.list_by_subscription
    calls:
      - action: associationsinterface.list_by_traffic_controller
        save_as: response
        on_error: continue
        params:
          traffic_controller_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── frontendsinterface.list_by_traffic_controller [dependent] ──
  - discovery_id: azure.servicenetworking.frontendsinterface.list_by_traffic_controller
    for_each: azure.servicenetworking.trafficcontrollerinterface.list_by_subscription
    calls:
      - action: frontendsinterface.list_by_traffic_controller
        save_as: response
        on_error: continue
        params:
          traffic_controller_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
  # ── securitypoliciesinterface.list_by_traffic_controller [dependent] ──
  - discovery_id: azure.servicenetworking.securitypoliciesinterface.list_by_traffic_controller
    for_each: azure.servicenetworking.trafficcontrollerinterface.list_by_subscription
    calls:
      - action: securitypoliciesinterface.list_by_traffic_controller
        save_as: response
        on_error: continue
        params:
          traffic_controller_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
