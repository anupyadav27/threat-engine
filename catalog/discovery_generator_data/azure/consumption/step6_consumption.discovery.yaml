# Discovery YAML — consumption (Azure)
# Generated: 2026-04-15T10:39:29Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: consumption
services:
  client: consumption
  module: azure.mgmt.consumption
discovery:
  # ════════════════════════════════════════
  # Dependent operations (for_each + params)
  # ════════════════════════════════════════
  # ── aggregatedcost.get_by_management_group [root] ──
  # NOTE: management_group_id undefined at root; fails gracefully on_error:continue
  - discovery_id: azure.consumption.aggregatedcost.get_by_management_group
    calls:
      - action: aggregatedcost.get_by_management_group
        save_as: response
        on_error: continue
        params:
          management_group_id: ''
    emit:
      as: item
      item:
        identity: '{{ response.identity }}'
        type: '{{ response.type }}'
        properties: '{{ response.properties }}'
        location: '{{ response.location }}'
        sku: '{{ response.sku }}'
        name: '{{ response.name }}'
        provisioning_state: '{{ response.provisioning_state }}'
        zones: '{{ response.zones }}'
        kind: '{{ response.kind }}'
        id: '{{ response.id }}'
        tags: '{{ response.tags }}'
  # ── aggregatedcost.get_for_billing_period_by_management_group [dependent] ──
  - discovery_id: azure.consumption.aggregatedcost.get_for_billing_period_by_management_group
    for_each: azure.consumption.aggregatedcost.get_by_management_group
    calls:
      - action: aggregatedcost.get_for_billing_period_by_management_group
        save_as: response
        on_error: continue
        params:
          billing_period_name: '{{ item.name }}'
    emit:
      as: item
      item:
        identity: '{{ response.identity }}'
        type: '{{ response.type }}'
        properties: '{{ response.properties }}'
        location: '{{ response.location }}'
        sku: '{{ response.sku }}'
        name: '{{ response.name }}'
        provisioning_state: '{{ response.provisioning_state }}'
        zones: '{{ response.zones }}'
        kind: '{{ response.kind }}'
        id: '{{ response.id }}'
        tags: '{{ response.tags }}'
  # ── balances.get_by_billing_account [dependent] ──
  - discovery_id: azure.consumption.balances.get_by_billing_account
    for_each: azure.consumption.balances.get_for_billing_period_by_billing_account
    calls:
      - action: balances.get_by_billing_account
        save_as: response
        on_error: continue
        params:
          billing_account_id: '{{ item.id }}'
    emit:
      as: item
      item:
        identity: '{{ response.identity }}'
        type: '{{ response.type }}'
        properties: '{{ response.properties }}'
        location: '{{ response.location }}'
        sku: '{{ response.sku }}'
        name: '{{ response.name }}'
        provisioning_state: '{{ response.provisioning_state }}'
        zones: '{{ response.zones }}'
        kind: '{{ response.kind }}'
        id: '{{ response.id }}'
        tags: '{{ response.tags }}'
  # ── balances.get_for_billing_period_by_billing_account [dependent] ──
  - discovery_id: azure.consumption.balances.get_for_billing_period_by_billing_account
    for_each: azure.consumption.aggregatedcost.get_by_management_group
    calls:
      - action: balances.get_for_billing_period_by_billing_account
        save_as: response
        on_error: continue
        params:
          billing_period_name: '{{ item.name }}'
    emit:
      as: item
      item:
        identity: '{{ response.identity }}'
        type: '{{ response.type }}'
        properties: '{{ response.properties }}'
        location: '{{ response.location }}'
        sku: '{{ response.sku }}'
        name: '{{ response.name }}'
        provisioning_state: '{{ response.provisioning_state }}'
        zones: '{{ response.zones }}'
        kind: '{{ response.kind }}'
        id: '{{ response.id }}'
        tags: '{{ response.tags }}'
  # ── events.list_by_billing_account [dependent] ──
  - discovery_id: azure.consumption.events.list_by_billing_account
    for_each: azure.consumption.balances.get_for_billing_period_by_billing_account
    calls:
      - action: events.list_by_billing_account
        save_as: response
        on_error: continue
        params:
          billing_account_id: '{{ item.id }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── events.list_by_billing_profile [dependent] ──
  - discovery_id: azure.consumption.events.list_by_billing_profile
    for_each: azure.consumption.balances.get_for_billing_period_by_billing_account
    calls:
      - action: events.list_by_billing_profile
        save_as: response
        on_error: continue
        params:
          billing_account_id: '{{ item.id }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── lots.list_by_billing_account [dependent] ──
  - discovery_id: azure.consumption.lots.list_by_billing_account
    for_each: azure.consumption.balances.get_for_billing_period_by_billing_account
    calls:
      - action: lots.list_by_billing_account
        save_as: response
        on_error: continue
        params:
          billing_account_id: '{{ item.id }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── lots.list_by_billing_profile [dependent] ──
  - discovery_id: azure.consumption.lots.list_by_billing_profile
    for_each: azure.consumption.balances.get_for_billing_period_by_billing_account
    calls:
      - action: lots.list_by_billing_profile
        save_as: response
        on_error: continue
        params:
          billing_account_id: '{{ item.id }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── lots.list_by_customer [dependent] ──
  - discovery_id: azure.consumption.lots.list_by_customer
    for_each: azure.consumption.balances.get_for_billing_period_by_billing_account
    calls:
      - action: lots.list_by_customer
        save_as: response
        on_error: continue
        params:
          billing_account_id: '{{ item.id }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── pricesheet.get_by_billing_period [dependent] ──
  - discovery_id: azure.consumption.pricesheet.get_by_billing_period
    for_each: azure.consumption.aggregatedcost.get_by_management_group
    calls:
      - action: pricesheet.get_by_billing_period
        save_as: response
        on_error: continue
        params:
          billing_period_name: '{{ item.name }}'
    emit:
      as: item
      item:
        identity: '{{ response.identity }}'
        type: '{{ response.type }}'
        properties: '{{ response.properties }}'
        location: '{{ response.location }}'
        sku: '{{ response.sku }}'
        name: '{{ response.name }}'
        provisioning_state: '{{ response.provisioning_state }}'
        zones: '{{ response.zones }}'
        kind: '{{ response.kind }}'
        id: '{{ response.id }}'
        tags: '{{ response.tags }}'
  # ── reservationtransactions.list [dependent] ──
  - discovery_id: azure.consumption.reservationtransactions.list
    for_each: azure.consumption.balances.get_for_billing_period_by_billing_account
    calls:
      - action: reservationtransactions.list
        save_as: response
        on_error: continue
        params:
          billing_account_id: '{{ item.id }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── reservationtransactions.list_by_billing_profile [dependent] ──
  - discovery_id: azure.consumption.reservationtransactions.list_by_billing_profile
    for_each: azure.consumption.balances.get_for_billing_period_by_billing_account
    calls:
      - action: reservationtransactions.list_by_billing_profile
        save_as: response
        on_error: continue
        params:
          billing_account_id: '{{ item.id }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.consumption.budgets.list — required_params: ['scope']
  # SKIP: azure.consumption.charges.list — required_params: ['scope']
  # SKIP: azure.consumption.marketplaces.list — required_params: ['scope']
  # SKIP: azure.consumption.reservationrecommendations.list — required_params: ['resource_scope']
  # SKIP: azure.consumption.reservationsdetails.list — required_params: ['resource_scope']
  # SKIP: azure.consumption.reservationsdetails.list_by_reservation_order — required_params: ['reservation_order_id', 'filter']
  # SKIP: azure.consumption.reservationsdetails.list_by_reservation_order_and_reservation — required_params: ['reservation_order_id', 'reservation_id', 'filter']
  # SKIP: azure.consumption.reservationssummaries.list — required_params: ['resource_scope', 'grain']
  # SKIP: azure.consumption.reservationssummaries.list_by_reservation_order — required_params: ['reservation_order_id', 'grain']
  # SKIP: azure.consumption.reservationssummaries.list_by_reservation_order_and_reservation — required_params: ['reservation_order_id', 'reservation_id', 'grain']
  # SKIP: azure.consumption.usagedetails.list — required_params: ['scope']
