# Discovery YAML — iotoperations (Azure)
# Generated: 2026-04-15T10:39:30Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: iotoperations
services:
  client: iotoperations
  module: azure.mgmt.iotoperations
discovery:
  # ── operations.Operations_List [root] ──
  - discovery_id: azure.iotoperations.operations.operations_list
    calls:
      - action: operations.Operations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.iotoperations.akriconnector.akriconnector_get — required_params: ['id']
  # SKIP: azure.iotoperations.akriconnectortemplate.akriconnectortemplate_get — required_params: ['id']
  # SKIP: azure.iotoperations.akriservice.akriservice_get — required_params: ['id']
  # SKIP: azure.iotoperations.broker.broker_get — required_params: ['id']
  # SKIP: azure.iotoperations.brokerauthentication.brokerauthentication_get — required_params: ['id']
  # SKIP: azure.iotoperations.brokerauthorization.brokerauthorization_get — required_params: ['id']
  # SKIP: azure.iotoperations.dataflow.dataflow_get — required_params: ['id']
  # SKIP: azure.iotoperations.dataflowendpoint.dataflowendpoint_get — required_params: ['id']
  # SKIP: azure.iotoperations.dataflowgraph.dataflowgraph_get — required_params: ['id']
  # SKIP: azure.iotoperations.dataflowprofile.dataflowprofile_get — required_params: ['id']
  # SKIP: azure.iotoperations.instance.instance_get — required_params: ['id']
  # SKIP: azure.iotoperations.registryendpoint.registryendpoint_get — required_params: ['id']
