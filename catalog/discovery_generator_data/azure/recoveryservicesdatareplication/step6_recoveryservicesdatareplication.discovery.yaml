# Discovery YAML — recoveryservicesdatareplication (Azure)
# Generated: 2026-04-15T10:39:31Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: recoveryservicesdatareplication
services:
  client: recoveryservicesdatareplication
  module: azure.mgmt.recoveryservicesdatareplication
discovery:
  # ── fabric.Fabric_List [root] ──
  - discovery_id: azure.recoveryservicesdatareplication.fabric.fabric_list
    calls:
      - action: fabric.Fabric_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── operations.Operations_List [root] ──
  - discovery_id: azure.recoveryservicesdatareplication.operations.operations_list
    calls:
      - action: operations.Operations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── vault.Vault_List [root] ──
  - discovery_id: azure.recoveryservicesdatareplication.vault.vault_list
    calls:
      - action: vault.Vault_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.recoveryservicesdatareplication.emailconfiguration.emailconfiguration_get — required_params: ['id']
  # SKIP: azure.recoveryservicesdatareplication.event.event_get — required_params: ['id']
  # SKIP: azure.recoveryservicesdatareplication.fabric.fabric_get — required_params: ['id']
  # SKIP: azure.recoveryservicesdatareplication.fabricagent.fabricagent_get — required_params: ['id']
  # SKIP: azure.recoveryservicesdatareplication.job.job_get — required_params: ['id']
  # SKIP: azure.recoveryservicesdatareplication.locationbasedoperationresults.locationbasedoperationresults_get — required_params: ['id']
  # SKIP: azure.recoveryservicesdatareplication.operationresults.operationresults_get — required_params: ['id']
  # SKIP: azure.recoveryservicesdatareplication.policy.policy_get — required_params: ['id']
  # SKIP: azure.recoveryservicesdatareplication.privateendpointconnectionproxies.privateendpointconnectionproxies_get — required_params: ['id']
  # SKIP: azure.recoveryservicesdatareplication.privateendpointconnections.privateendpointconnections_get — required_params: ['id']
  # SKIP: azure.recoveryservicesdatareplication.privatelinkresources.privatelinkresources_get — required_params: ['id']
  # SKIP: azure.recoveryservicesdatareplication.protecteditem.protecteditem_get — required_params: ['id']
  # SKIP: azure.recoveryservicesdatareplication.recoverypoint.recoverypoint_get — required_params: ['id']
  # SKIP: azure.recoveryservicesdatareplication.replicationextension.replicationextension_get — required_params: ['id']
  # SKIP: azure.recoveryservicesdatareplication.vault.vault_get — required_params: ['id']
