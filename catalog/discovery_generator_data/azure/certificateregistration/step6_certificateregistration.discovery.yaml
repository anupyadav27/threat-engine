# Discovery YAML — certificateregistration (Azure)
# Generated: 2026-04-15T10:39:29Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: certificateregistration
services:
  client: certificateregistration
  module: azure.mgmt.certificateregistration
discovery:
  # ── appservicecertificateorders.AppServiceCertificateOrders_List [root] ──
  - discovery_id: azure.certificateregistration.appservicecertificateorders.appservicecertificateorders_list
    calls:
      - action: appservicecertificateorders.AppServiceCertificateOrders_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.certificateregistration.appservicecertificateorders.appservicecertificateorders_get — required_params: ['id']
