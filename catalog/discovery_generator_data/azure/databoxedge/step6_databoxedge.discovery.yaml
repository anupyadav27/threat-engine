# Discovery YAML — databoxedge (Azure)
# Generated: 2026-04-15T10:39:30Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: databoxedge
services:
  client: databoxedge
  module: azure.mgmt.databoxedge
discovery:
  # ── availableskus.AvailableSkus_List [root] ──
  - discovery_id: azure.databoxedge.availableskus.availableskus_list
    calls:
      - action: availableskus.AvailableSkus_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── operations.Operations_List [root] ──
  - discovery_id: azure.databoxedge.operations.operations_list
    calls:
      - action: operations.Operations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── skus.Skus_List [root] ──
  - discovery_id: azure.databoxedge.skus.skus_list
    calls:
      - action: skus.Skus_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.databoxedge.addons.addons_get — required_params: ['id']
  # SKIP: azure.databoxedge.alerts.alerts_get — required_params: ['id']
  # SKIP: azure.databoxedge.bandwidthschedules.bandwidthschedules_get — required_params: ['id']
  # SKIP: azure.databoxedge.containers.containers_get — required_params: ['id']
  # SKIP: azure.databoxedge.devices.devices_get — required_params: ['id']
  # SKIP: azure.databoxedge.jobs.jobs_get — required_params: ['id']
  # SKIP: azure.databoxedge.monitoringconfig.monitoringconfig_get — required_params: ['id']
  # SKIP: azure.databoxedge.operationsstatus.operationsstatus_get — required_params: ['id']
  # SKIP: azure.databoxedge.orders.orders_get — required_params: ['id']
  # SKIP: azure.databoxedge.roles.roles_get — required_params: ['id']
  # SKIP: azure.databoxedge.shares.shares_get — required_params: ['id']
  # SKIP: azure.databoxedge.storageaccountcredentials.storageaccountcredentials_get — required_params: ['id']
  # SKIP: azure.databoxedge.storageaccounts.storageaccounts_get — required_params: ['id']
  # SKIP: azure.databoxedge.triggers.triggers_get — required_params: ['id']
  # SKIP: azure.databoxedge.users.users_get — required_params: ['id']
