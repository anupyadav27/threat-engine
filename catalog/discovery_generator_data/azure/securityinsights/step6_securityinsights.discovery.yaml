# Discovery YAML — securityinsights (Azure)
# Generated: 2026-04-15T10:39:31Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: securityinsights
services:
  client: securityinsights
  module: azure.mgmt.securityinsights
discovery:
  # ── operations.Operations_List [root] ──
  - discovery_id: azure.securityinsights.operations.operations_list
    calls:
      - action: operations.Operations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.securityinsights.actions.actions_get — required_params: ['id']
  # SKIP: azure.securityinsights.alertrules.alertrules_get — required_params: ['id']
  # SKIP: azure.securityinsights.alertruletemplates.alertruletemplates_get — required_params: ['id']
  # SKIP: azure.securityinsights.automationrules.automationrules_get — required_params: ['id']
  # SKIP: azure.securityinsights.bookmarks.bookmarks_get — required_params: ['id']
  # SKIP: azure.securityinsights.contentpackages.contentpackages_get — required_params: ['id']
  # SKIP: azure.securityinsights.contenttemplate.contenttemplate_get — required_params: ['id']
  # SKIP: azure.securityinsights.dataconnectordefinitions.dataconnectordefinitions_get — required_params: ['id']
  # SKIP: azure.securityinsights.dataconnectors.dataconnectors_get — required_params: ['id']
  # SKIP: azure.securityinsights.incidentcomments.incidentcomments_get — required_params: ['id']
  # SKIP: azure.securityinsights.incidentrelations.incidentrelations_get — required_params: ['id']
  # SKIP: azure.securityinsights.incidents.incidents_get — required_params: ['id']
  # SKIP: azure.securityinsights.incidenttasks.incidenttasks_get — required_params: ['id']
  # SKIP: azure.securityinsights.metadata.metadata_get — required_params: ['id']
  # SKIP: azure.securityinsights.productpackage.productpackage_get — required_params: ['id']
  # SKIP: azure.securityinsights.producttemplate.producttemplate_get — required_params: ['id']
  # SKIP: azure.securityinsights.securitymlanalyticssettings.securitymlanalyticssettings_get — required_params: ['id']
  # SKIP: azure.securityinsights.sentinelonboardingstates.sentinelonboardingstates_get — required_params: ['id']
  # SKIP: azure.securityinsights.sourcecontrols.sourcecontrols_get — required_params: ['id']
  # SKIP: azure.securityinsights.threatintelligenceindicator.threatintelligenceindicator_get — required_params: ['id']
