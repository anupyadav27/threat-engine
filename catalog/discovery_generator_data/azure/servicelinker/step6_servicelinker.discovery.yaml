# Discovery YAML — servicelinker (Azure)
# Generated: 2026-04-15T10:39:31Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: servicelinker
services:
  client: servicelinker
  module: azure.mgmt.servicelinker
discovery:

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.servicelinker.linker.list — required_params: ['resource_uri']
  # SKIP: azure.servicelinker.linker.list_configurations — required_params: ['resource_uri', 'linker_name']
