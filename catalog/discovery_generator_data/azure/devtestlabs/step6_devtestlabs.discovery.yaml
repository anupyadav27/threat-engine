# Discovery YAML — devtestlabs (Azure)
# Generated: 2026-04-15T10:39:30Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: devtestlabs
services:
  client: devtestlabs
  module: azure.mgmt.devtestlabs
discovery:
  # ── globalschedules.list_by_resource_group [root] ──
  - discovery_id: azure.devtestlabs.globalschedules.list_by_resource_group
    calls:
      - action: globalschedules.list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── globalschedules.list_by_subscription [root] ──
  - discovery_id: azure.devtestlabs.globalschedules.list_by_subscription
    calls:
      - action: globalschedules.list_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── labs.list_by_resource_group [root] ──
  - discovery_id: azure.devtestlabs.labs.list_by_resource_group
    calls:
      - action: labs.list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── labs.list_by_subscription [root] ──
  - discovery_id: azure.devtestlabs.labs.list_by_subscription
    calls:
      - action: labs.list_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── provider.list [root] ──
  - discovery_id: azure.devtestlabs.provider.list
    calls:
      - action: provider.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ════════════════════════════════════════
  # Dependent operations (for_each + params)
  # ════════════════════════════════════════
  # ── armtemplates.list [dependent] ──
  - discovery_id: azure.devtestlabs.armtemplates.list
    for_each: azure.devtestlabs.servicefabrics.list_applicable_schedules
    calls:
      - action: armtemplates.list
        save_as: response
        on_error: continue
        params:
          lab_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── artifacts.list [dependent] ──
  - discovery_id: azure.devtestlabs.artifacts.list
    for_each: azure.devtestlabs.servicefabrics.list_applicable_schedules
    calls:
      - action: artifacts.list
        save_as: response
        on_error: continue
        params:
          lab_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── artifactsources.list [dependent] ──
  - discovery_id: azure.devtestlabs.artifactsources.list
    for_each: azure.devtestlabs.servicefabrics.list_applicable_schedules
    calls:
      - action: artifactsources.list
        save_as: response
        on_error: continue
        params:
          lab_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── customimages.list [dependent] ──
  - discovery_id: azure.devtestlabs.customimages.list
    for_each: azure.devtestlabs.servicefabrics.list_applicable_schedules
    calls:
      - action: customimages.list
        save_as: response
        on_error: continue
        params:
          lab_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── disks.list [dependent] ──
  - discovery_id: azure.devtestlabs.disks.list
    for_each: azure.devtestlabs.servicefabrics.list_applicable_schedules
    calls:
      - action: disks.list
        save_as: response
        on_error: continue
        params:
          lab_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── environments.list [dependent] ──
  - discovery_id: azure.devtestlabs.environments.list
    for_each: azure.devtestlabs.servicefabrics.list_applicable_schedules
    calls:
      - action: environments.list
        save_as: response
        on_error: continue
        params:
          lab_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── formulas.list [dependent] ──
  - discovery_id: azure.devtestlabs.formulas.list
    for_each: azure.devtestlabs.servicefabrics.list_applicable_schedules
    calls:
      - action: formulas.list
        save_as: response
        on_error: continue
        params:
          lab_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── galleryimages.list [dependent] ──
  - discovery_id: azure.devtestlabs.galleryimages.list
    for_each: azure.devtestlabs.servicefabrics.list_applicable_schedules
    calls:
      - action: galleryimages.list
        save_as: response
        on_error: continue
        params:
          lab_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── labs.list_vhds [dependent] ──
  - discovery_id: azure.devtestlabs.labs.list_vhds
    for_each: azure.devtestlabs.servicefabrics.list_applicable_schedules
    calls:
      - action: labs.list_vhds
        save_as: response
        on_error: continue
        params:
          name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── notificationchannels.list [dependent] ──
  - discovery_id: azure.devtestlabs.notificationchannels.list
    for_each: azure.devtestlabs.servicefabrics.list_applicable_schedules
    calls:
      - action: notificationchannels.list
        save_as: response
        on_error: continue
        params:
          lab_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── policies.list [dependent] ──
  - discovery_id: azure.devtestlabs.policies.list
    for_each: azure.devtestlabs.servicefabrics.list_applicable_schedules
    calls:
      - action: policies.list
        save_as: response
        on_error: continue
        params:
          lab_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── schedules.list [dependent] ──
  - discovery_id: azure.devtestlabs.schedules.list
    for_each: azure.devtestlabs.servicefabrics.list_applicable_schedules
    calls:
      - action: schedules.list
        save_as: response
        on_error: continue
        params:
          lab_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── schedules.list_applicable [dependent] ──
  - discovery_id: azure.devtestlabs.schedules.list_applicable
    for_each: azure.devtestlabs.servicefabrics.list_applicable_schedules
    calls:
      - action: schedules.list_applicable
        save_as: response
        on_error: continue
        params:
          lab_name: '{{ item.name }}'
          name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── secrets.list [dependent] ──
  - discovery_id: azure.devtestlabs.secrets.list
    for_each: azure.devtestlabs.servicefabrics.list_applicable_schedules
    calls:
      - action: secrets.list
        save_as: response
        on_error: continue
        params:
          lab_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── servicefabrics.list [dependent] ──
  - discovery_id: azure.devtestlabs.servicefabrics.list
    for_each: azure.devtestlabs.servicefabrics.list_applicable_schedules
    calls:
      - action: servicefabrics.list
        save_as: response
        on_error: continue
        params:
          lab_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── servicefabricschedules.list [dependent] ──
  - discovery_id: azure.devtestlabs.servicefabricschedules.list
    for_each: azure.devtestlabs.servicefabrics.list_applicable_schedules
    calls:
      - action: servicefabricschedules.list
        save_as: response
        on_error: continue
        params:
          lab_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── users.list [dependent] ──
  - discovery_id: azure.devtestlabs.users.list
    for_each: azure.devtestlabs.servicefabrics.list_applicable_schedules
    calls:
      - action: users.list
        save_as: response
        on_error: continue
        params:
          lab_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── virtualmachines.get_rdp_file_contents [dependent] ──
  - discovery_id: azure.devtestlabs.virtualmachines.get_rdp_file_contents
    for_each: azure.devtestlabs.servicefabrics.list_applicable_schedules
    calls:
      - action: virtualmachines.get_rdp_file_contents
        save_as: response
        on_error: continue
        params:
          lab_name: '{{ item.name }}'
          name: '{{ item.name }}'
    emit:
      as: item
      item:
        identity: '{{ response.identity }}'
        type: '{{ response.type }}'
        properties: '{{ response.properties }}'
        location: '{{ response.location }}'
        sku: '{{ response.sku }}'
        name: '{{ response.name }}'
        provisioning_state: '{{ response.provisioning_state }}'
        zones: '{{ response.zones }}'
        kind: '{{ response.kind }}'
        id: '{{ response.id }}'
        tags: '{{ response.tags }}'
  # ── virtualmachines.list [dependent] ──
  - discovery_id: azure.devtestlabs.virtualmachines.list
    for_each: azure.devtestlabs.servicefabrics.list_applicable_schedules
    calls:
      - action: virtualmachines.list
        save_as: response
        on_error: continue
        params:
          lab_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── virtualmachines.list_applicable_schedules [dependent] ──
  - discovery_id: azure.devtestlabs.virtualmachines.list_applicable_schedules
    for_each: azure.devtestlabs.servicefabrics.list_applicable_schedules
    calls:
      - action: virtualmachines.list_applicable_schedules
        save_as: response
        on_error: continue
        params:
          lab_name: '{{ item.name }}'
          name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── virtualmachineschedules.list [dependent] ──
  - discovery_id: azure.devtestlabs.virtualmachineschedules.list
    for_each: azure.devtestlabs.servicefabrics.list_applicable_schedules
    calls:
      - action: virtualmachineschedules.list
        save_as: response
        on_error: continue
        params:
          lab_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── virtualnetworks.list [dependent] ──
  - discovery_id: azure.devtestlabs.virtualnetworks.list
    for_each: azure.devtestlabs.servicefabrics.list_applicable_schedules
    calls:
      - action: virtualnetworks.list
        save_as: response
        on_error: continue
        params:
          lab_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.devtestlabs.servicefabrics.list_applicable_schedules — required_params: ['lab_name', 'user_name', 'name']
