# Discovery YAML — loadtestservice (Azure)
# Generated: 2026-04-15T10:39:30Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: loadtestservice
services:
  client: loadtestservice
  module: azure.mgmt.loadtestservice
discovery:
  # ── operations.Operations_List [root] ──
  - discovery_id: azure.loadtestservice.operations.operations_list
    calls:
      - action: operations.Operations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── quotas.Quotas_List [root] ──
  - discovery_id: azure.loadtestservice.quotas.quotas_list
    calls:
      - action: quotas.Quotas_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.loadtestservice.loadtests.loadtests_get — required_params: ['id']
  # SKIP: azure.loadtestservice.playwrightquotas.playwrightquotas_get — required_params: ['id']
  # SKIP: azure.loadtestservice.playwrightworkspacequotas.playwrightworkspacequotas_get — required_params: ['id']
  # SKIP: azure.loadtestservice.playwrightworkspaces.playwrightworkspaces_get — required_params: ['id']
  # SKIP: azure.loadtestservice.quotas.quotas_get — required_params: ['id']
