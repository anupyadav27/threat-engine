# Discovery YAML — postgresqlhsc (Azure)
# Generated: 2026-04-15T10:39:31Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: postgresqlhsc
services:
  client: postgresqlhsc
  module: azure.mgmt.postgresqlhsc
discovery:
  # ── clusters.Clusters_List [root] ──
  - discovery_id: azure.postgresqlhsc.clusters.clusters_list
    calls:
      - action: clusters.Clusters_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── operations.Operations_List [root] ──
  - discovery_id: azure.postgresqlhsc.operations.operations_list
    calls:
      - action: operations.Operations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.postgresqlhsc.clusters.clusters_get — required_params: ['id']
  # SKIP: azure.postgresqlhsc.configurations.configurations_get — required_params: ['id']
  # SKIP: azure.postgresqlhsc.firewallrules.firewallrules_get — required_params: ['id']
  # SKIP: azure.postgresqlhsc.privateendpointconnections.privateendpointconnections_get — required_params: ['id']
  # SKIP: azure.postgresqlhsc.privatelinkresources.privatelinkresources_get — required_params: ['id']
  # SKIP: azure.postgresqlhsc.roles.roles_get — required_params: ['id']
  # SKIP: azure.postgresqlhsc.servers.servers_get — required_params: ['id']
