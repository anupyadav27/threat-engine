# Discovery YAML — hybridcompute (Azure)
# Generated: 2026-04-15T10:39:30Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: hybridcompute
services:
  client: hybridcompute
  module: azure.mgmt.hybridcompute
discovery:
  # ── licenses.list_by_resource_group [root] ──
  - discovery_id: azure.hybridcompute.licenses.list_by_resource_group
    calls:
      - action: licenses.list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
        system_data: '{{ item.system_data }}'
  # ── licenses.list_by_subscription [root] ──
  - discovery_id: azure.hybridcompute.licenses.list_by_subscription
    calls:
      - action: licenses.list_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        status: '{{ item.status }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
        ad_fqdn: '{{ item.ad_fqdn }}'
        agent_configuration: '{{ item.agent_configuration }}'
        agent_upgrade: '{{ item.agent_upgrade }}'
        agent_version: '{{ item.agent_version }}'
        client_public_key: '{{ item.client_public_key }}'
        cloud_metadata: '{{ item.cloud_metadata }}'
        detected_properties: '{{ item.detected_properties }}'
        display_name: '{{ item.display_name }}'
        dns_fqdn: '{{ item.dns_fqdn }}'
        domain_name: '{{ item.domain_name }}'
        error_details: '{{ item.error_details }}'
        extensions: '{{ item.extensions }}'
        last_status_change: '{{ item.last_status_change }}'
        license_profile: '{{ item.license_profile }}'
        location_data: '{{ item.location_data }}'
        machine_fqdn: '{{ item.machine_fqdn }}'
        mssql_discovered: '{{ item.mssql_discovered }}'
        network_profile: '{{ item.network_profile }}'
        os_edition: '{{ item.os_edition }}'
        os_name: '{{ item.os_name }}'
        os_profile: '{{ item.os_profile }}'
        os_sku: '{{ item.os_sku }}'
        os_type: '{{ item.os_type }}'
        os_version: '{{ item.os_version }}'
        parent_cluster_resource_id: '{{ item.parent_cluster_resource_id }}'
        private_link_scope_resource_id: '{{ item.private_link_scope_resource_id }}'
        resources: '{{ item.resources }}'
        service_statuses: '{{ item.service_statuses }}'
        system_data: '{{ item.system_data }}'
        vm_id: '{{ item.vm_id }}'
        vm_uuid: '{{ item.vm_uuid }}'
  # ── machines.list_by_resource_group [root] ──
  - discovery_id: azure.hybridcompute.machines.list_by_resource_group
    calls:
      - action: machines.list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
        system_data: '{{ item.system_data }}'
  # ── machines.list_by_subscription [root] ──
  - discovery_id: azure.hybridcompute.machines.list_by_subscription
    calls:
      - action: machines.list_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        status: '{{ item.status }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
        ad_fqdn: '{{ item.ad_fqdn }}'
        agent_configuration: '{{ item.agent_configuration }}'
        agent_upgrade: '{{ item.agent_upgrade }}'
        agent_version: '{{ item.agent_version }}'
        client_public_key: '{{ item.client_public_key }}'
        cloud_metadata: '{{ item.cloud_metadata }}'
        detected_properties: '{{ item.detected_properties }}'
        display_name: '{{ item.display_name }}'
        dns_fqdn: '{{ item.dns_fqdn }}'
        domain_name: '{{ item.domain_name }}'
        error_details: '{{ item.error_details }}'
        extensions: '{{ item.extensions }}'
        last_status_change: '{{ item.last_status_change }}'
        license_profile: '{{ item.license_profile }}'
        location_data: '{{ item.location_data }}'
        machine_fqdn: '{{ item.machine_fqdn }}'
        mssql_discovered: '{{ item.mssql_discovered }}'
        network_profile: '{{ item.network_profile }}'
        os_edition: '{{ item.os_edition }}'
        os_name: '{{ item.os_name }}'
        os_profile: '{{ item.os_profile }}'
        os_sku: '{{ item.os_sku }}'
        os_type: '{{ item.os_type }}'
        os_version: '{{ item.os_version }}'
        parent_cluster_resource_id: '{{ item.parent_cluster_resource_id }}'
        private_link_scope_resource_id: '{{ item.private_link_scope_resource_id }}'
        resources: '{{ item.resources }}'
        service_statuses: '{{ item.service_statuses }}'
        system_data: '{{ item.system_data }}'
        vm_id: '{{ item.vm_id }}'
        vm_uuid: '{{ item.vm_uuid }}'
  # ── privatelinkscopes.list [root] ──
  - discovery_id: azure.hybridcompute.privatelinkscopes.list
    calls:
      - action: privatelinkscopes.list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        extension_type: '{{ item.extension_type }}'
        publisher: '{{ item.publisher }}'
        system_data: '{{ item.system_data }}'
        version: '{{ item.version }}'
  # ── privatelinkscopes.list_by_resource_group [root] ──
  - discovery_id: azure.hybridcompute.privatelinkscopes.list_by_resource_group
    calls:
      - action: privatelinkscopes.list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
        system_data: '{{ item.system_data }}'
  # ════════════════════════════════════════
  # Dependent operations (for_each + params)
  # ════════════════════════════════════════
  # ── licenseprofiles.list [dependent] ──
  - discovery_id: azure.hybridcompute.licenseprofiles.list
    for_each: azure.hybridcompute.machineextensions.list
    calls:
      - action: licenseprofiles.list
        save_as: response
        on_error: continue
        params:
          machine_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        extension_type: '{{ item.extension_type }}'
        publisher: '{{ item.publisher }}'
        system_data: '{{ item.system_data }}'
        version: '{{ item.version }}'
  # ── networksecurityperimeterconfigurations.get_by_private_link_scope [dependent] ──
  - discovery_id: azure.hybridcompute.networksecurityperimeterconfigurations.get_by_private_link_scope
    for_each: azure.hybridcompute.privatelinkscopes.list
    calls:
      - action: networksecurityperimeterconfigurations.get_by_private_link_scope
        save_as: response
        on_error: continue
        params:
          scope_name: '{{ item.name }}'
    emit:
      as: item
      item:
        identity: '{{ response.identity }}'
        type: '{{ response.type }}'
        properties: '{{ response.properties }}'
        location: '{{ response.location }}'
        sku: '{{ response.sku }}'
        name: '{{ response.name }}'
        provisioning_state: '{{ response.provisioning_state }}'
        zones: '{{ response.zones }}'
        kind: '{{ response.kind }}'
        id: '{{ response.id }}'
        tags: '{{ response.tags }}'
  # ── networksecurityperimeterconfigurations.list_by_private_link_scope [dependent] ──
  - discovery_id: azure.hybridcompute.networksecurityperimeterconfigurations.list_by_private_link_scope
    for_each: azure.hybridcompute.privatelinkscopes.list
    calls:
      - action: networksecurityperimeterconfigurations.list_by_private_link_scope
        save_as: response
        on_error: continue
        params:
          scope_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── privateendpointconnections.list_by_private_link_scope [dependent] ──
  - discovery_id: azure.hybridcompute.privateendpointconnections.list_by_private_link_scope
    for_each: azure.hybridcompute.privatelinkscopes.list
    calls:
      - action: privateendpointconnections.list_by_private_link_scope
        save_as: response
        on_error: continue
        params:
          scope_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── privatelinkresources.list_by_private_link_scope [dependent] ──
  - discovery_id: azure.hybridcompute.privatelinkresources.list_by_private_link_scope
    for_each: azure.hybridcompute.privatelinkscopes.list
    calls:
      - action: privatelinkresources.list_by_private_link_scope
        save_as: response
        on_error: continue
        params:
          scope_name: '{{ item.name }}'
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        name: '{{ item.name }}'
        id: '{{ item.id }}'
        system_data: '{{ item.system_data }}'
  # ── privatelinkscopes.get_validation_details [dependent] ──
  - discovery_id: azure.hybridcompute.privatelinkscopes.get_validation_details
    for_each: azure.hybridcompute.privatelinkscopes.list
    calls:
      - action: privatelinkscopes.get_validation_details
        save_as: response
        on_error: continue
        params:
          location: '{{ item.location }}'
          private_link_scope_id: '{{ item.id }}'
    emit:
      as: item
      item:
        identity: '{{ response.identity }}'
        type: '{{ response.type }}'
        properties: '{{ response.properties }}'
        location: '{{ response.location }}'
        sku: '{{ response.sku }}'
        name: '{{ response.name }}'
        provisioning_state: '{{ response.provisioning_state }}'
        zones: '{{ response.zones }}'
        kind: '{{ response.kind }}'
        id: '{{ response.id }}'
        tags: '{{ response.tags }}'
  # ── privatelinkscopes.get_validation_details_for_machine [dependent] ──
  - discovery_id: azure.hybridcompute.privatelinkscopes.get_validation_details_for_machine
    for_each: azure.hybridcompute.machineextensions.list
    calls:
      - action: privatelinkscopes.get_validation_details_for_machine
        save_as: response
        on_error: continue
        params:
          machine_name: '{{ item.name }}'
    emit:
      as: item
      item:
        identity: '{{ response.identity }}'
        type: '{{ response.type }}'
        properties: '{{ response.properties }}'
        location: '{{ response.location }}'
        sku: '{{ response.sku }}'
        name: '{{ response.name }}'
        provisioning_state: '{{ response.provisioning_state }}'
        zones: '{{ response.zones }}'
        kind: '{{ response.kind }}'
        id: '{{ response.id }}'
        tags: '{{ response.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.hybridcompute.extensionmetadata.list — required_params: ['location', 'publisher', 'extension_type']
  # SKIP: azure.hybridcompute.machineextensions.list — required_params: ['machine_name']
