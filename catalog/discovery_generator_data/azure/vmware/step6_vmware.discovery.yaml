# Discovery YAML — vmware (Azure)
# Generated: 2026-04-15T10:39:32Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: vmware
services:
  client: vmware
  module: azure.mgmt.vmware
discovery:
  # ── operations.Operations_List [root] ──
  - discovery_id: azure.vmware.operations.operations_list
    calls:
      - action: operations.Operations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── privateclouds.PrivateClouds_List [root] ──
  - discovery_id: azure.vmware.privateclouds.privateclouds_list
    calls:
      - action: privateclouds.PrivateClouds_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── skus.Skus_List [root] ──
  - discovery_id: azure.vmware.skus.skus_list
    calls:
      - action: skus.Skus_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.vmware.addons.addons_get — required_params: ['id']
  # SKIP: azure.vmware.authorizations.authorizations_get — required_params: ['id']
  # SKIP: azure.vmware.cloudlinks.cloudlinks_get — required_params: ['id']
  # SKIP: azure.vmware.clusters.clusters_get — required_params: ['id']
  # SKIP: azure.vmware.datastores.datastores_get — required_params: ['id']
  # SKIP: azure.vmware.globalreachconnections.globalreachconnections_get — required_params: ['id']
  # SKIP: azure.vmware.hcxenterprisesites.hcxenterprisesites_get — required_params: ['id']
  # SKIP: azure.vmware.hosts.hosts_get — required_params: ['id']
  # SKIP: azure.vmware.iscsipaths.iscsipaths_get — required_params: ['id']
  # SKIP: azure.vmware.licenses.licenses_get — required_params: ['id']
  # SKIP: azure.vmware.maintenances.maintenances_get — required_params: ['id']
  # SKIP: azure.vmware.placementpolicies.placementpolicies_get — required_params: ['id']
  # SKIP: azure.vmware.privateclouds.privateclouds_get — required_params: ['id']
  # SKIP: azure.vmware.provisionednetworks.provisionednetworks_get — required_params: ['id']
  # SKIP: azure.vmware.purestoragepolicies.purestoragepolicies_get — required_params: ['id']
  # SKIP: azure.vmware.scriptcmdlets.scriptcmdlets_get — required_params: ['id']
  # SKIP: azure.vmware.scriptexecutions.scriptexecutions_get — required_params: ['id']
  # SKIP: azure.vmware.scriptpackages.scriptpackages_get — required_params: ['id']
  # SKIP: azure.vmware.virtualmachines.virtualmachines_get — required_params: ['id']
  # SKIP: azure.vmware.workloadnetworks.workloadnetworks_get — required_params: ['id']
