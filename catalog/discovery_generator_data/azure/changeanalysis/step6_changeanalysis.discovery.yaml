# Discovery YAML — changeanalysis (Azure)
# Generated: 2026-04-15T10:39:29Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: changeanalysis
services:
  client: changeanalysis
  module: azure.mgmt.changeanalysis
discovery:

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.changeanalysis.changes.list_changes_by_resource_group — required_params: ['start_time', 'end_time']
  # SKIP: azure.changeanalysis.changes.list_changes_by_subscription — required_params: ['start_time', 'end_time']
  # SKIP: azure.changeanalysis.resourcechanges.list — required_params: ['resource_id', 'start_time', 'end_time']
