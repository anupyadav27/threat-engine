# Discovery YAML — paloaltonetworks (Azure)
# Generated: 2026-04-15T10:39:31Z
# Source: step1b_operation_registry.json (qualified ops + dependency chains)
# Actions use {category}.{method} format for SDK method resolution
version: '1.0'
provider: azure
service: paloaltonetworks
services:
  client: paloaltonetworks
  module: azure.mgmt.paloaltonetworks
discovery:
  # ── globalrulestack.GlobalRulestack_List [root] ──
  - discovery_id: azure.paloaltonetworks.globalrulestack.globalrulestack_list
    calls:
      - action: globalrulestack.GlobalRulestack_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'
  # ── operations.Operations_List [root] ──
  - discovery_id: azure.paloaltonetworks.operations.operations_list
    calls:
      - action: operations.Operations_List
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        identity: '{{ item.identity }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        location: '{{ item.location }}'
        sku: '{{ item.sku }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        zones: '{{ item.zones }}'
        kind: '{{ item.kind }}'
        id: '{{ item.id }}'
        tags: '{{ item.tags }}'

  # ── Unwired dependent ops (no root producer found) ──
  # SKIP: azure.paloaltonetworks.certificateobjectglobalrulestack.certificateobjectglobalrulestack_get — required_params: ['id']
  # SKIP: azure.paloaltonetworks.certificateobjectlocalrulestack.certificateobjectlocalrulestack_get — required_params: ['id']
  # SKIP: azure.paloaltonetworks.firewalls.firewalls_get — required_params: ['id']
  # SKIP: azure.paloaltonetworks.firewallstatus.firewallstatus_get — required_params: ['id']
  # SKIP: azure.paloaltonetworks.globalrulestack.globalrulestack_get — required_params: ['id']
  # SKIP: azure.paloaltonetworks.localrules.localrules_get — required_params: ['id']
  # SKIP: azure.paloaltonetworks.localrulestacks.localrulestacks_get — required_params: ['id']
  # SKIP: azure.paloaltonetworks.metricsobjectfirewall.metricsobjectfirewall_get — required_params: ['id']
  # SKIP: azure.paloaltonetworks.postrules.postrules_get — required_params: ['id']
  # SKIP: azure.paloaltonetworks.prerules.prerules_get — required_params: ['id']
