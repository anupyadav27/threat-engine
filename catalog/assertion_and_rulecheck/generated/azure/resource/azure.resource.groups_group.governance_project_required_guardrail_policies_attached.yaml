version: '1.0'
rule_id: azure.resource.groups_group.governance_project_required_guardrail_policies_attached
for_each: azure.resource.list
severity: MEDIUM
conditions:
  all:
  - var: item.tags.governance_project
    op: exists
  - var: item.tags.guardrail_policies_attached
    op: equals
    value: 'true'
fixture_validated: false
fixture_note: 'PASS fixture returned FAIL — wrong field or operator (pass_fixture keys: [''type'', ''name'', ''id'', ''tenant_id''])'
