version: '1.0'
rule_id: azure.mysql.server.audit.log.connection.check
for_each: azure.mysql.list_servers
severity: MEDIUM
conditions:
  all:
  - var: item.properties.auditLogSettings
    op: exists
  - var: item.properties.auditLogSettings.connectionLogsEnabled
    op: is_true
