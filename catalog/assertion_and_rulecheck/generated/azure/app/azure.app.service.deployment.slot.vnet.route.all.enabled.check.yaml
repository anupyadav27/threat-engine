version: '1.0'
rule_id: azure.app.service.deployment.slot.vnet.route.all.enabled.check
for_each: azure.app.service.deployment.slot.list
severity: MEDIUM
conditions:
  all:
  - var: item.VirtualNetworkSubnetId
    op: exists
  - var: item.RouteAllTrafficThroughVnet
    op: is_true
