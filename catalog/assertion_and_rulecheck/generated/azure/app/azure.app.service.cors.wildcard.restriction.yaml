version: '1.0'
rule_id: azure.app.service.cors.wildcard.restriction
for_each: azure.app_service.list_web_apps
severity: MEDIUM
conditions:
  all:
  - var: item.siteConfig.cors.allowedOrigins
    op: exists
  - var: item.siteConfig.cors.allowedOrigins
    op: not_contains
    value: '*'
