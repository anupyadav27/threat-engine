version: '1.0'
rule_id: azure.storage.account.network.rule.trusted.services.check
for_each: azure.storage.storageaccounts.list
severity: MEDIUM
conditions:
  all:
  - var: item.properties.networkAcls.defaultAction
    op: equals
    value: Deny
  - var: item.properties.networkAcls.bypass
    op: not_contains
    value: AzureServices
fixture_validated: false
fixture_note: 'PASS fixture returned FAIL — wrong field or operator (pass_fixture keys: [''type'', ''properties'', ''name'',
  ''id''])'
