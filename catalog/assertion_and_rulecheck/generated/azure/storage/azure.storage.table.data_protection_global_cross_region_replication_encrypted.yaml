version: '1.0'
rule_id: azure.storage.table.data_protection_global_cross_region_replication_encrypted
for_each: azure.storage.storageaccounts.list
severity: MEDIUM
conditions:
  all:
  - var: item.properties.encryption.services.table.enabled
    op: is_true
  - var: item.properties.encryption.services.table.keyType
    op: equals
    value: Account
fixture_validated: false
fixture_note: 'PASS fixture returned FAIL — wrong field or operator (pass_fixture keys: [''type'', ''properties'', ''name'',
  ''id''])'
