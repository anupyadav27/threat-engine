version: '1.0'
rule_id: azure.aks.rbac.check.cluster.admin.usage
for_each: azure.aks.list_clusters
severity: MEDIUM
conditions:
  all:
  - var: item.properties.enableRBAC
    op: is_true
  - var: item.properties.disableLocalAccounts
    op: is_true
