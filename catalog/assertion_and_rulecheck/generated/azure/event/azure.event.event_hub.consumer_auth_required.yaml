version: '1.0'
rule_id: azure.event.event_hub.consumer_auth_required
for_each: azure.event_hub.list_namespaces
severity: MEDIUM
conditions:
  all:
  - var: item.properties.isAutoInflateEnabled
    op: is_true
  - var: item.properties.kafkaEnabled
    op: is_true
