version: '1.0'
rule_id: azure.event.event_hub_consumer.streaming_consumer_access_least_privilege
for_each: azure.event.list_event_hubs
severity: MEDIUM
conditions:
  all:
  - var: item.properties.authorizationRules[].rights[]
    op: not_contains
    value: Manage
  - var: item.properties.authorizationRules[].rights[]
    op: not_contains
    value: Listen,Manage,Send
