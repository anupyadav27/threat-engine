version: '1.0'
rule_id: azure.vm.managed.disks.compliance.check
for_each: azure.compute.list_virtual_machines
severity: MEDIUM
conditions:
  all:
  - var: item.storageProfile.osDisk.managedDisk
    op: exists
  - var: item.storageProfile.dataDisks[].managedDisk
    op: exists
