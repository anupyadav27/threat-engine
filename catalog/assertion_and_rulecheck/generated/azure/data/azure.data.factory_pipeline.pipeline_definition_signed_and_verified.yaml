version: '1.0'
rule_id: azure.data.factory_pipeline.pipeline_definition_signed_and_verified
for_each: azure.data.factory.list_pipelines
severity: MEDIUM
conditions:
  all:
  - var: item.Policy.SignatureVerificationEnabled
    op: is_true
  - var: item.Policy.SignatureRequired
    op: is_true
