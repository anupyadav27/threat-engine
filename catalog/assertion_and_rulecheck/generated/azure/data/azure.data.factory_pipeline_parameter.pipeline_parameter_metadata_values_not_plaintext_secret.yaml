version: '1.0'
rule_id: azure.data.factory_pipeline_parameter.pipeline_parameter_metadata_values_not_plaintext_secret
for_each: azure.data.factory.list_pipelines
severity: MEDIUM
conditions:
  all:
  - var: item.Parameters
    op: exists
  - var: item.Parameters[].DefaultValue
    op: not_exists
