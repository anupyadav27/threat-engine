version: '1.0'
rule_id: azure.kubernetes.service.containers_nodeport_usage_restricted
for_each: azure.kubernetes.managed_clusters.list
severity: MEDIUM
conditions:
  var: item.agentPoolProfiles[].ports[].port
  op: not_contains
  value: NodePort
