version: '1.0'
rule_id: azure.kubernetes.deployment.containers_workload_drop_net_raw_and_reduce_caps
for_each: azure.kubernetes.list_deployments
severity: MEDIUM
conditions:
  all:
  - var: item.spec.template.spec.containers[].securityContext.capabilities.drop[]
    op: contains
    value: NET_RAW
  - var: item.spec.template.spec.containers[].securityContext.capabilities.drop
    op: length_gte
    value: 2
