version: '1.0'
rule_id: azure.kubernetes.rbac.containers_no_cluster_admin_bindings_to_users
for_each: azure.kubernetes.rbac.list_cluster_role_bindings
severity: MEDIUM
conditions:
  all:
  - var: item.roleRef.name
    op: equals
    value: cluster-admin
  - var: item.subjects[].kind
    op: not_contains
    value: User
