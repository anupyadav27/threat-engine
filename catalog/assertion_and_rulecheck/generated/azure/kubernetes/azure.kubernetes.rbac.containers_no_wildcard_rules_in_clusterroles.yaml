version: '1.0'
rule_id: azure.kubernetes.rbac.containers_no_wildcard_rules_in_clusterroles
for_each: azure.kubernetes.rbac.clusterroles.list
severity: MEDIUM
conditions:
  all:
  - var: item.rules[].verbs
    op: not_contains
    value: '*'
  - var: item.rules[].apiGroups
    op: not_contains
    value: '*'
  - var: item.rules[].resources
    op: not_contains
    value: '*'
