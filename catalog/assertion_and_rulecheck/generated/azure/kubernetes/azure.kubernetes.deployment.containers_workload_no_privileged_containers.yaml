version: '1.0'
rule_id: azure.kubernetes.deployment.containers_workload_no_privileged_containers
for_each: azure.kubernetes.list_containers
severity: MEDIUM
conditions:
  all:
  - var: item.privileged
    op: is_false
