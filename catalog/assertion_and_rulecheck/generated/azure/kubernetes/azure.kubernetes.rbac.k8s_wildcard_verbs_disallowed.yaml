version: '1.0'
rule_id: azure.kubernetes.rbac.k8s_wildcard_verbs_disallowed
for_each: azure.kubernetes.list_role_bindings
severity: MEDIUM
conditions:
  all:
  - var: item.RoleRef.Name
    op: exists
  - var: item.Rules[].Verbs[]
    op: not_contains
    value: '*'
