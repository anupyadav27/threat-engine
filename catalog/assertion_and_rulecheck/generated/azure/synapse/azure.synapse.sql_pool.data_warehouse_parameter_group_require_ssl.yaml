version: '1.0'
rule_id: azure.synapse.sql_pool.data_warehouse_parameter_group_require_ssl
for_each: azure.synapse.workspaces.list
severity: MEDIUM
conditions:
  all:
  - var: item.properties.managedVirtualNetworkSettings.requireSsl
    op: exists
  - var: item.properties.managedVirtualNetworkSettings.requireSsl
    op: is_true
fixture_validated: false
fixture_note: 'PASS fixture returned FAIL — wrong field or operator (pass_fixture keys: [''identity'', ''type'', ''properties'',
  ''location'', ''sku'', ''name'', ''provisioning_state'', ''zones''])'
