version: '1.0'
rule_id: azure.compute.virtual_machine.vm_ssh_password_auth_disabled
for_each: azure.compute.virtual_machines.list
severity: HIGH
conditions:
  var: item.properties.osProfile.linuxConfiguration.disablePasswordAuthentication
  op: is_true
