version: '1.0'
rule_id: azure.compute.virtual_machine.vm_user_data_no_secrets
for_each: azure.compute.virtual_machines.list
severity: HIGH
conditions:
  var: item.properties.osProfile.customData
  op: not_exists
