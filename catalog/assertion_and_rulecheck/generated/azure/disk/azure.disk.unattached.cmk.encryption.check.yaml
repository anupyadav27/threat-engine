version: '1.0'
rule_id: azure.disk.unattached.cmk.encryption.check
for_each: azure.compute.list_disks
severity: MEDIUM
conditions:
  all:
  - var: item.Encryption.Type
    op: equals
    value: EncryptionAtRestWithCustomerKey
  - var: item.ManagedBy
    op: not_exists
