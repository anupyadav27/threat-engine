version: '1.0'
rule_id: azure.policy.assignment.governance_aggregation_authorization_not_expired_or_revoked
for_each: azure.policy.list_policy_assignments
severity: MEDIUM
conditions:
  all:
  - var: item.Properties.Metadata.expirationDate
    op: exists
  - var: item.Properties.Metadata.expirationDate
    op: gt
    value: '{{now}}'
