version: '1.0'
rule_id: azure.keyvault.private_ca.private_ca_ca_key_in_hsm_where_supported
for_each: azure.keyvault.vaults.list_by_subscription
severity: MEDIUM
conditions:
  all:
  - var: item.properties.sku.name
    op: equals
    value: premium
  - var: item.properties.hsmPoolResourceId
    op: exists
fixture_validated: false
fixture_note: 'PASS fixture returned FAIL — wrong field or operator (pass_fixture keys: [''type'', ''properties'', ''location'',
  ''name'', ''id'', ''tags''])'
