version: '1.0'
rule_id: azure.cdn.profile.edge_signed_urls_or_headers_required_for_private_content
for_each: azure.cdn.profiles.list
severity: MEDIUM
conditions:
  all:
  - var: item.parameters
    op: exists
  - var: item.parameters.signed_urls_enabled
    op: is_true
  - var: item.parameters.signed_headers_enabled
    op: is_true
fixture_validated: false
fixture_note: 'PASS fixture returned FAIL — wrong field or operator (pass_fixture keys: [''type'', ''name'', ''id'', ''current_value'',
  ''limit'', ''resource_type'', ''unit''])'
