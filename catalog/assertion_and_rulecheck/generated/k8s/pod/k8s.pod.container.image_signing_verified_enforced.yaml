version: '1.0'
rule_id: k8s.pod.container.image_signing_verified_enforced
for_each: k8s.pod.list_pods
severity: LOW
conditions:
  var: item.spec.containers[].imagePullPolicy
  op: equals
  value: Always
