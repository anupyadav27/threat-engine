version: '1.0'
rule_id: k8s.pod.labeling.standard
for_each: k8s.pod.list_pods
severity: LOW
conditions:
  all:
  - var: item.metadata.labels.app
    op: exists
  - var: item.metadata.labels.version
    op: exists
  - var: item.metadata.labels.environment
    op: exists
