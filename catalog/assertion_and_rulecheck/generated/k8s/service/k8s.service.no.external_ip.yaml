version: '1.0'
rule_id: k8s.service.no.external_ip
for_each: k8s.service.list_services
severity: MEDIUM
conditions:
  all:
  - var: item.spec.externalIPs
    op: exists
  - var: item.spec.externalIPs
    op: is_empty
