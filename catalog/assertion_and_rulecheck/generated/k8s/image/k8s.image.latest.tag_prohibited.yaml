version: '1.0'
rule_id: k8s.image.latest.tag_prohibited
for_each: k8s.image.list_pod_for_all_namespaces
severity: LOW
conditions:
  all:
  - var: item.spec[].containers[].image
    op: not_contains
    value: :latest
fixture_validated: false
fixture_note: 'FAIL fixture returned PASS — conditions too loose (fail_fixture keys: [''metadata'', ''spec''])'
