version: '1.0'
rule_id: k8s.image.pull.policy_always
for_each: k8s.image.list_pod_for_all_namespaces
severity: MEDIUM
conditions:
  all:
  - var: item.spec[].containers[].imagePullPolicy
    op: exists
  - var: item.spec[].containers[].imagePullPolicy
    op: equals
    value: Always
