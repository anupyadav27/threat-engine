version: '1.0'
rule_id: k8s.federation.server.basic_auth_disabled
for_each: k8s.federation.list_namespace
severity: HIGH
conditions:
  var: item.annotations.auth-basic
  op: not_exists
fixture_validated: false
fixture_note: 'FAIL fixture returned PASS — conditions too loose (fail_fixture keys: [''metadata''])'
