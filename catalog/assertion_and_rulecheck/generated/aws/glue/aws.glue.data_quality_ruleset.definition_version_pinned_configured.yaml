version: '1.0'
rule_id: aws.glue.data_quality_ruleset.definition_version_pinned_configured
for_each: aws.glue.get_data_quality_rulesets
severity: LOW
conditions:
  var: item.Ruleset
  op: exists
  value:
    var: item.Ruleset.Ruleset
    op: contains
    value: RulesetDefinitionVersion
