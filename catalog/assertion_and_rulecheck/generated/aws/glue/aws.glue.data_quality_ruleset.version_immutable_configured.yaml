version: '1.0'
rule_id: aws.glue.data_quality_ruleset.version_immutable_configured
for_each: aws.glue.get_data_quality_ruleset
severity: LOW
conditions:
  var: item.Ruleset.VersionLocked
  op: is_true
