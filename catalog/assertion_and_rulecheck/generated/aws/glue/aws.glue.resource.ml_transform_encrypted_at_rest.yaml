version: '1.0'
rule_id: aws.glue.resource.ml_transform_encrypted_at_rest
for_each: aws.glue.get_ml_transforms
severity: HIGH
conditions:
  var: item.TransformEncryption.MlUserDataEncryption.MlUserDataEncryptionMode
  op: exists
