version: '1.0'
rule_id: aws.ec2.ebs_public_snapshot.ebs_public_snapshot_configured
for_each: aws.ec2.describe_snapshots
severity: HIGH
conditions:
  all:
  - var: item.Snapshots[].CreateVolumePermissions[].Group
    op: not_contains
    value: all
fixture_validated: false
fixture_note: 'FAIL fixture returned PASS — conditions too loose (fail_fixture keys: [''Encrypted'', ''StateMessage'', ''Tags''])'
