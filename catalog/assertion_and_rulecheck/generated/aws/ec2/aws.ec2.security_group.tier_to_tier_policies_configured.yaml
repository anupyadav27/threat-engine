version: '1.0'
rule_id: aws.ec2.security_group.tier_to_tier_policies_configured
for_each: aws.ec2.describe_carrier_gateways
severity: MEDIUM
conditions:
  all:
  - var: item.SecurityGroupForVpcs
    op: exists
  - var: item.StaleSecurityGroupSet
    op: is_empty
