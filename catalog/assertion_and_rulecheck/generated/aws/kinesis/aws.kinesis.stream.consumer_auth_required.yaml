version: '1.0'
rule_id: aws.kinesis.stream.consumer_auth_required
for_each: aws.kinesis.list_stream_consumers
severity: MEDIUM
conditions:
  var: item.ConsumerStatus
  op: exists
fixture_validated: false
fixture_note: 'FAIL fixture returned PASS — conditions too loose (fail_fixture keys: [''ConsumerStatus''])'
