version: '1.0'
rule_id: aws.cloudwatch.metric.api_access_log_fields_identity_configured
for_each: aws.cloudwatch.describe_alarms
severity: MEDIUM
conditions:
  var: item.MetricAlarms[].Metrics[].MetricStat.Metric.Dimensions[?(@.Name == 'log-field' && @.Value == 'identity')]
  op: not_empty
