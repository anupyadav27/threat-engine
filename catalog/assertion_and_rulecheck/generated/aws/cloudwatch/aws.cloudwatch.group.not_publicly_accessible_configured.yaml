version: '1.0'
rule_id: aws.cloudwatch.group.not_publicly_accessible_configured
for_each: aws.cloudwatch.describe_alarms
severity: HIGH
conditions:
  var: item.MetricAlarms[].ActionsEnabled
  op: is_true
