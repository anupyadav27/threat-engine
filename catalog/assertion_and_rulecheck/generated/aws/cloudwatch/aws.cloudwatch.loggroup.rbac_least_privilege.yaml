version: '1.0'
rule_id: aws.cloudwatch.loggroup.rbac_least_privilege
for_each: aws.cloudwatch.describe_log_groups
severity: HIGH
conditions:
  all:
  - var: item.retentionInDays
    op: exists
  - var: item.retentionInDays
    op: gte
    value: 30
