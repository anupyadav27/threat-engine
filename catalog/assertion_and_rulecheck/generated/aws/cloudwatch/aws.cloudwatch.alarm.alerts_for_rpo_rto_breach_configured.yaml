version: '1.0'
rule_id: aws.cloudwatch.alarm.alerts_for_rpo_rto_breach_configured
for_each: aws.cloudwatch.describe_alarms
severity: MEDIUM
conditions:
  all:
  - var: item.AlarmActions
    op: not_empty
  - var: item.AlarmDescription
    op: contains
    value: RPO
