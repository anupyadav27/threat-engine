version: '1.0'
rule_id: aws.cloudwatch.alarm.alert_destinations_configured
for_each: aws.cloudwatch.describe_alarms
severity: MEDIUM
conditions:
  all:
  - var: item.AlarmActions
    op: not_empty
  - var: item.InsufficientDataActions
    op: not_empty
