version: '1.0'
rule_id: aws.cloudwatch.bucket.policy_changes_configured
for_each: aws.cloudwatch.describe_alarms
severity: HIGH
conditions:
  any:
  - var: item.AlarmName
    op: contains
    value: S3BucketPolicyChange
  - var: item.AlarmName
    op: contains
    value: BucketPolicyChange
