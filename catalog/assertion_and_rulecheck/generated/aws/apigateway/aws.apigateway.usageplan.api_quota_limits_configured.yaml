version: '1.0'
rule_id: aws.apigateway.usageplan.api_quota_limits_configured
for_each: aws.apigateway.get_usage_plans
severity: MEDIUM
conditions:
  all:
  - var: item.quota.limit
    op: exists
  - var: item.quota.period
    op: exists
