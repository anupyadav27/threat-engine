version: '1.0'
rule_id: aws.apigateway.authorizer.tls_min_1_2_enforced
for_each: aws.apigateway.get_domain_names
severity: HIGH
conditions:
  all:
  - var: item.Version
    op: exists
  - var: item.Version
    op: gte
    value: '1.2'
