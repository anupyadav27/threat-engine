version: '1.0'
rule_id: aws.fargate.service.fargate_env_no_plaintext_secrets_configured
for_each: aws.fargate.service.list
severity: MEDIUM
conditions:
  all:
  - var: item.taskDefinition.containerDefinitions[].environment[].value
    op: not_contains
    value: password
  - var: item.taskDefinition.containerDefinitions[].environment[].value
    op: not_contains
    value: secret
  - var: item.taskDefinition.containerDefinitions[].environment[].value
    op: not_contains
    value: key
