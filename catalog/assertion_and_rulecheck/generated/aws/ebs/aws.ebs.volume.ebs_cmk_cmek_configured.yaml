version: '1.0'
rule_id: aws.ebs.volume.ebs_cmk_cmek_configured
for_each: aws.ebs.volumes.list
severity: MEDIUM
conditions:
  var: item.Encrypted
  op: is_true
  all:
  - var: item.KmsKeyId
    op: exists
  - var: item.KmsKeyId
    op: not_contains
    value: alias/aws/ebs
