version: '1.0'
rule_id: aws.iam.user.console_password_present_only_if_required
for_each: aws.iam.get_account_authorization_details
severity: CRITICAL
conditions:
  all:
  - var: item.UserDetailList[].PasswordLastUsed
    op: not_exists
  - var: item.UserDetailList[].CreateDate
    op: exists
