version: '1.0'
rule_id: aws.iam.instanceprofile.role_enabled
for_each: aws.iam.get_account_authorization_details
severity: HIGH
conditions:
  var: item.RoleDetailList[].InstanceProfileList[].Roles[].Arn
  op: exists
