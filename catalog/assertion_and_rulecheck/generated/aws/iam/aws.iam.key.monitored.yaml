version: '1.0'
rule_id: aws.iam.key.monitored
for_each: aws.iam.get_account_authorization_details
severity: MEDIUM
conditions:
  all:
  - var: item.UserDetailList[].SSHPublicKey
    op: exists
  - var: item.UserDetailList[].SSHPublicKey[].Status
    op: equals
    value: Active
