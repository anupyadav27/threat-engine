version: '1.0'
rule_id: aws.iam.role.session_duration_reasonable_if_applicable_configured
for_each: aws.iam.get_account_authorization_details
severity: MEDIUM
conditions:
  any:
  - var: item.RoleDetailList[].MaxSessionDuration
    op: not_exists
  - var: item.RoleDetailList[].MaxSessionDuration
    op: lte
    value: 3600
fixture_validated: false
fixture_note: 'FAIL fixture returned PASS — conditions too loose (fail_fixture keys: [''Tags'', ''Version''])'
