version: '1.0'
rule_id: aws.rds.snapshot.not_public_configured
for_each: aws.rds.describe_db_cluster_snapshot_attributes
severity: HIGH
conditions:
  all:
  - var: item.DBClusterSnapshotAttributesResult.AttributeValues
    op: not_contains
    value: all
