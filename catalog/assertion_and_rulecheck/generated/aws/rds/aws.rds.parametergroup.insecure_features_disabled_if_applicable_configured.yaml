version: '1.0'
rule_id: aws.rds.parametergroup.insecure_features_disabled_if_applicable_configured
for_each: aws.rds.describe_db_cluster_parameters
severity: MEDIUM
conditions:
  var: item.ParameterValue
  op: not_contains
  value: '1'
fixture_validated: false
fixture_note: 'FAIL fixture returned PASS — conditions too loose (fail_fixture keys: [''AllowedValues'', ''MinimumEngineVersion''])'
