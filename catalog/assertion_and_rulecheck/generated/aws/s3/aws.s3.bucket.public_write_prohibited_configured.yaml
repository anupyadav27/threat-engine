version: '1.0'
rule_id: aws.s3.bucket.public_write_prohibited_configured
for_each: aws.s3.get_bucket_acl
severity: HIGH
conditions:
  all:
  - var: item.Grantee
    op: exists
  - var: item.Permission
    op: not_equals
    value: WRITE
