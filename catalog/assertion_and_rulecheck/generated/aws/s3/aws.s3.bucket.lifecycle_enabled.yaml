version: '1.0'
rule_id: aws.s3.bucket.lifecycle_enabled
for_each: aws.s3.get_bucket_lifecycle_configuration
severity: LOW
conditions:
  var: item.Status
  op: exists
fixture_validated: false
fixture_note: 'FAIL fixture returned PASS — conditions too loose (fail_fixture keys: [''Status''])'
