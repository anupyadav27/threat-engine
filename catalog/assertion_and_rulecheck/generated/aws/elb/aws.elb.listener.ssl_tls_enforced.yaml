version: '1.0'
rule_id: aws.elb.listener.ssl_tls_enforced
for_each: aws.elb.describe_load_balancers
severity: HIGH
conditions:
  all:
  - var: item.ListenerDescriptions[].Listener.Protocol
    op: not_in
    value:
    - HTTP
    - TCP
fixture_validated: false
fixture_note: 'FAIL fixture returned PASS — conditions too loose (fail_fixture keys: [''State''])'
