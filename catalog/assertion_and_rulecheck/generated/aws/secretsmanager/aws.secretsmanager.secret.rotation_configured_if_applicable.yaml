version: '1.0'
rule_id: aws.secretsmanager.secret.rotation_configured_if_applicable
for_each: aws.secretsmanager.list_secrets
severity: MEDIUM
conditions:
  any:
  - var: item.LastRotatedDate
    op: exists
  - var: item.NextRotationDate
    op: exists
  - var: item.ExternalSecretRotationRoleArn
    op: exists
