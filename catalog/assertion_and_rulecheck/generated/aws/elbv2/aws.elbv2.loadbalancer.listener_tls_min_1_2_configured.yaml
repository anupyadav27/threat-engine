version: '1.0'
rule_id: aws.elbv2.loadbalancer.listener_tls_min_1_2_configured
for_each: aws.elbv2.describe_listeners
severity: HIGH
conditions:
  all:
  - var: item.Protocol
    op: equals
    value: HTTPS
  - var: item.SslPolicy
    op: contains
    value: TLS-1-2
