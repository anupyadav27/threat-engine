version: '1.0'
rule_id: aws.config.resource.config_lambda_config_recording_enabled
for_each: aws.config.describe_configuration_recorder_status
severity: MEDIUM
conditions:
  all:
  - var: item.ConfigurationRecordersStatus
    op: exists
  - var: item.recording
    op: is_true
fixture_validated: false
fixture_note: 'PASS fixture returned FAIL — wrong field or operator (pass_fixture keys: [''ConfigurationRecordersStatus'',
  ''arn'', ''lastErrorCode'', ''lastErrorMessage'', ''lastStartTime'', ''lastStatus'', ''lastStatusChangeTime'', ''lastStopTime''])'
