version: '1.0'
rule_id: aws.acm.certificates.certificate_expiration_monitored
for_each: aws.acm.describe_certificate
severity: MEDIUM
conditions:
  all:
  - var: item.NotAfter
    op: exists
  - var: item.NotAfter
    op: gt
    value: now
