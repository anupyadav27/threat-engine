version: '1.0'
rule_id: aws.efs.filesystem.kms_key_policy_least_privilege
for_each: aws.efs.describe_file_systems
severity: HIGH
conditions:
  all:
  - var: item.Encrypted
    op: is_true
  - var: item.KmsKeyId
    op: exists
