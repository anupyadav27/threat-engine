version: '1.0'
rule_id: aws.efs.filesystem.private_network_only_configured
for_each: aws.efs.describe_mount_targets
severity: HIGH
conditions:
  all:
  - var: item.MountTargets
    op: not_empty
  - var: item.MountTargets[].SubnetId
    op: exists
fixture_validated: false
fixture_note: 'PASS fixture returned FAIL — wrong field or operator (pass_fixture keys: [''AvailabilityZoneId'', ''AvailabilityZoneName'',
  ''FileSystemId'', ''IpAddress'', ''Ipv6Address'', ''LifeCycleState'', ''MountTargetId'', ''MountTargets''])'
