version: '1.0'
rule_id: aws.batch.compute_environment.no_public_ip_assigned_configured
for_each: aws.batch.describe_compute_environments
severity: HIGH
conditions:
  all:
  - var: item.computeResources.publicIpAddress
    op: not_exists
fixture_validated: false
fixture_note: 'FAIL fixture returned PASS — conditions too loose (fail_fixture keys: [''Enabled'', ''Status'', ''Version'',
  ''eksConfiguration'', ''state'', ''status'', ''tags''])'
