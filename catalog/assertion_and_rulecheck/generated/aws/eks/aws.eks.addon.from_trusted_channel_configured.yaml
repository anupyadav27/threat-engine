version: '1.0'
rule_id: aws.eks.addon.from_trusted_channel_configured
for_each: aws.eks.describe_addon
severity: MEDIUM
conditions:
  var: item.addonVersion
  op: exists
fixture_validated: false
fixture_note: 'FAIL fixture returned PASS — conditions too loose (fail_fixture keys: [''addonVersion'', ''namespaceConfig'',
  ''status'', ''tags''])'
