version: '1.0'
rule_id: aws.eks.addon.version_pinned_and_supported_configured
for_each: aws.eks.describe_addon
severity: LOW
conditions:
  all:
  - var: item.addonVersion
    op: exists
  - var: item.status
    op: equals
    value: ACTIVE
