version: '1.0'
rule_id: gcp.bigquery.parameter.parameter_group_tls_required
for_each: gcp.bigquery.routines.list
severity: HIGH
conditions:
  var: item.arguments[].dataType
  op: not_contains
  value: STRING
fixture_validated: false
fixture_note: 'FAIL fixture returned PASS — conditions too loose (fail_fixture keys: [''language''])'
