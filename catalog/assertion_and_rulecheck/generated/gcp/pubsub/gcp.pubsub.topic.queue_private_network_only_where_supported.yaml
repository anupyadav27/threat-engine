version: '1.0'
rule_id: gcp.pubsub.topic.queue_private_network_only_where_supported
for_each: gcp.pubsub.projects.topics.list
severity: HIGH
conditions:
  var: item.messageStoragePolicy.allowedPersistenceRegions
  op: exists
