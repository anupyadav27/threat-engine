version: '1.0'
rule_id: gcp.pubsub.video_stream.video_retention_days_minimum_configured
for_each: gcp.pubsub.projects.subscriptions.list
severity: MEDIUM
conditions:
  all:
  - var: item.messageRetentionDuration
    op: exists
  - var: item.messageRetentionDuration
    op: gte
    value: 864000s
