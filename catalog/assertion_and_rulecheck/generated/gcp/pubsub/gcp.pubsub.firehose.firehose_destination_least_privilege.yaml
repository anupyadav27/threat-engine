version: '1.0'
rule_id: gcp.pubsub.firehose.firehose_destination_least_privilege
for_each: gcp.pubsub.projects.subscriptions.list
severity: HIGH
conditions:
  all:
  - var: item.bigqueryConfig
    op: exists
  - var: item.bigqueryConfig.keyStorageType
    op: equals
    value: example-type
