version: '1.0'
rule_id: gcp.sql.user.user_approved_list_of_superusers_only
for_each: gcp.sql.list_users
severity: HIGH
conditions:
  all:
  - var: item.type
    op: equals
    value: BUILT_IN
  - var: item.name
    op: in
    value:
    - cloudsqlsuperuser
    - cloudsqladmin
