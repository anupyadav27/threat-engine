version: '1.0'
rule_id: gcp.dataproc.job.job_script_location_private_and_encrypted
for_each: gcp.dataproc.projects.regions.jobs.list
severity: HIGH
conditions:
  all:
  - var: item.encryptionConfig
    op: exists
  - var: item.encryptionConfig.keyStorageType
    op: exists
