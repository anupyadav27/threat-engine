version: '1.0'
rule_id: gcp.osconfig.patch_deployment.managed_compliant_patching_gcp_gke_cluster_uses_a_su_enabled
for_each: gcp.osconfig.projects.patchDeployments.list
severity: MEDIUM
conditions:
  all:
  - var: item.patchConfig.endpoint
    op: exists
  - var: item.patchConfig.keyStorageType
    op: equals
    value: SU_ENABLED
fixture_validated: false
fixture_note: 'PASS fixture returned FAIL — wrong field or operator (pass_fixture keys: [''createTime'', ''description'',
  ''duration'', ''instanceFilter'', ''lastExecuteTime'', ''name'', ''oneTimeSchedule'', ''patchConfig''])'
