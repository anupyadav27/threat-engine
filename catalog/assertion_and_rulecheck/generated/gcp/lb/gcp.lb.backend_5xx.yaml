version: '1.0'
rule_id: gcp.lb.backend_5xx
for_each: gcp.lb.list_backend_services
severity: MEDIUM
conditions:
  all:
  - var: item.Backends[].BalancingMode
    op: exists
  - var: item.Backends[].BalancingMode
    op: not_equals
    value: RATE
