version: '1.0'
rule_id: gcp.gke.workload.workload_no_hostpath_mounts
for_each: gcp.gke.workload.list_workloads
severity: HIGH
conditions:
  all:
  - var: item.spec.volumes[].hostPath
    op: not_exists
