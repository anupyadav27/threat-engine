version: '1.0'
rule_id: gcp.gke.control_plane_apiserver.apiserver_insecure_port_disabled
for_each: gcp.container.projects.zones.clusters.list
severity: HIGH
conditions:
  var: item.legacyAbac.enabled
  op: is_false
