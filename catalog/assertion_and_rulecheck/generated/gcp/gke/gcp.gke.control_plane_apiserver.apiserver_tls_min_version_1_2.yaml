version: '1.0'
rule_id: gcp.gke.control_plane_apiserver.apiserver_tls_min_version_1_2
for_each: gcp.gke.clusters.list
severity: HIGH
conditions:
  var: item.masterAuth.config.issueClientCertificate
  op: is_false
