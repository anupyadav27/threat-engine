version: '1.0'
rule_id: gcp.gke.control_plane_apiserver.apiserver_tls_min_1_2_enforced
for_each: gcp.gke.clusters.list
severity: HIGH
conditions:
  var: item.masterAuth.clusterCaCertificate
  op: exists
