version: '1.0'
rule_id: gcp.gke.node_kubelet.k8s_kubelet_read_only_port_disabled
for_each: gcp.container.projects.zones.clusters.nodePools.list
severity: HIGH
conditions:
  var: item.config.kubeletConfig.readOnlyPort
  op: not_exists
