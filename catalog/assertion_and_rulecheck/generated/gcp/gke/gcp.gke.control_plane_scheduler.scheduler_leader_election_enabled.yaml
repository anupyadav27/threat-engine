version: '1.0'
rule_id: gcp.gke.control_plane_scheduler.scheduler_leader_election_enabled
for_each: gcp.gke.describe_clusters
severity: HIGH
conditions:
  var: item.controlPlaneSchedulerConfig.schedulerLeaderElectionEnabled
  op: is_true
