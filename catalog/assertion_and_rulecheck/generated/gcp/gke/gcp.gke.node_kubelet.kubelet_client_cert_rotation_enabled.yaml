version: '1.0'
rule_id: gcp.gke.node_kubelet.kubelet_client_cert_rotation_enabled
for_each: gcp.gke.describe_node_pools
severity: HIGH
conditions:
  var: item.Config.KubeletConfig.RotateKubeletClientCertificate
  op: is_true
