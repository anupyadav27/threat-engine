version: '1.0'
rule_id: gcp.gke.control_plane_apiserver.apiserver_authorization_mode_rbac
for_each: gcp.gke.list_clusters
severity: HIGH
conditions:
  var: item.MasterAuthorizedNetworksConfig.Enabled
  op: is_true
