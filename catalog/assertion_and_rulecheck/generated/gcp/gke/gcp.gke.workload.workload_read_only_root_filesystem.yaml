version: '1.0'
rule_id: gcp.gke.workload.workload_read_only_root_filesystem
for_each: gcp.gke.workload.list_workloads
severity: HIGH
conditions:
  var: item.spec.containers[].securityContext.readOnlyRootFilesystem
  op: is_true
