version: '1.0'
rule_id: gcp.gke.control_plane_apiserver.k8s_apiserver_audit_logging_enabled
for_each: gcp.gke.control_plane_apiserver.describe_clusters
severity: HIGH
conditions:
  var: item.auditConfig.enabled
  op: is_true
