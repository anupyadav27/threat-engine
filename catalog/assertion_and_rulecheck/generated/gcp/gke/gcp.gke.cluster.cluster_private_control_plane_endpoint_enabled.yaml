version: '1.0'
rule_id: gcp.gke.cluster.cluster_private_control_plane_endpoint_enabled
for_each: gcp.gke.list_clusters
severity: HIGH
conditions:
  var: item.PrivateClusterConfig.PrivateEndpointEnabled
  op: is_true
