version: '1.0'
rule_id: gcp.gke.network_policy.networkpolicy_required_allowlist_for_namespace_services
for_each: gcp.gke.list_clusters
severity: HIGH
conditions:
  all:
  - var: item.NetworkPolicy.Enabled
    op: is_true
  - var: item.NetworkPolicy.Provider
    op: equals
    value: CALICO
