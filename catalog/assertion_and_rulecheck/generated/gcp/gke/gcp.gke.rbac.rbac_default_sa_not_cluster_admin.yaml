version: '1.0'
rule_id: gcp.gke.rbac.rbac_default_sa_not_cluster_admin
for_each: gcp.gke.list_clusters
severity: HIGH
conditions:
  all:
  - var: item.NodePools[].Config.ServiceAccount
    op: exists
  - var: item.NodePools[].Config.ServiceAccount
    op: not_equals
    value: default
