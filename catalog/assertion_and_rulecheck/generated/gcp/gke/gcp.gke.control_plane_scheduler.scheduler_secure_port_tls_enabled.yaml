version: '1.0'
rule_id: gcp.gke.control_plane_scheduler.scheduler_secure_port_tls_enabled
for_each: gcp.gke.list_clusters
severity: HIGH
conditions:
  var: item.masterAuth.config.securePort
  op: is_true
