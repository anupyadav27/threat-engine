version: '1.0'
rule_id: gcp.gke.rbac.rbac_no_cluster_admin_bindings_to_users
for_each: gcp.gke.rbac.list_cluster_role_bindings
severity: HIGH
conditions:
  all:
  - var: item.roleRef.name
    op: equals
    value: cluster-admin
  - var: item.subjects[].kind
    op: not_contains
    value: User
