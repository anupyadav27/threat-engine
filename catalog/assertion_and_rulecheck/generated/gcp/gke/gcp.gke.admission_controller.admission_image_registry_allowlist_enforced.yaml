version: '1.0'
rule_id: gcp.gke.admission_controller.admission_image_registry_allowlist_enforced
for_each: gcp.gke.list_clusters
severity: HIGH
conditions:
  var: item.AdmissionControllerConfig.ImagePolicyConfig.AllowedImagePatterns
  op: not_empty
