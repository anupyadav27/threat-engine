version: '1.0'
rule_id: gcp.gke.control_plane_controller_manager.controller_manager_root_ca_file_configured
for_each: gcp.gke.control_plane_controller_manager.list
severity: HIGH
conditions:
  var: item.controllerManagerRootCAFile
  op: exists
