version: '1.0'
rule_id: gcp.gke.admission_controller.admission_host_namespace_usage_denied
for_each: gcp.gke.list_clusters
severity: HIGH
conditions:
  var: item.MasterAuthorizedNetworksConfig.Enabled
  op: is_true
