version: '1.0'
rule_id: gcp.gke.control_plane_scheduler.scheduler_authentication_kubeconfig_configured
for_each: gcp.gke.control_plane_scheduler.list
severity: HIGH
conditions:
  var: item.schedulerAuthentication.kubeconfig
  op: exists
