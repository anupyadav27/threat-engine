version: '1.0'
rule_id: gcp.gke.cluster.cluster_service_kube_system_services_not_public
for_each: gcp.gke.list_clusters
severity: HIGH
conditions:
  all:
  - var: item.privateClusterConfig.enablePrivateNodes
    op: is_true
  - var: item.privateClusterConfig.masterIpv4CidrBlock
    op: exists
