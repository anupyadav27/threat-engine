version: '1.0'
rule_id: gcp.gke.control_plane_scheduler.scheduler_authorization_kubeconfig_configured
for_each: gcp.gke.clusters.list
severity: HIGH
conditions:
  var: item.masterAuth.config
  op: exists
