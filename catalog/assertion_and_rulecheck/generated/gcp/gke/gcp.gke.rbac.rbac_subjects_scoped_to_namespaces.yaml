version: '1.0'
rule_id: gcp.gke.rbac.rbac_subjects_scoped_to_namespaces
for_each: gcp.gke.list_clusters
severity: HIGH
conditions:
  all:
  - var: item.RoleBindings[].Subjects[].Namespace
    op: exists
  - var: item.RoleBindings[].Subjects[].Kind
    op: not_equals
    value: ServiceAccount
