version: '1.0'
rule_id: gcp.gke.namespace.namespace_pod_security_level_restricted
for_each: gcp.container.projects.zones.clusters.list
severity: HIGH
conditions:
  all:
  - var: item.PodSecurityPolicyConfig
    op: exists
  - var: item.PodSecurityPolicyConfig.enabled
    op: is_true
