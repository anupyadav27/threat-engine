version: '1.0'
rule_id: gcp.gke.control_plane_apiserver.k8s_apiserver_anonymous_auth_disabled
for_each: gcp.container.projects.locations.clusters.list
severity: HIGH
conditions:
  var: item.masterAuth.anonymousAuthEnabled
  op: is_false
