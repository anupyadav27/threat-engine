version: '1.0'
rule_id: gcp.gke.workload.workload_env_no_plaintext_secrets
for_each: gcp.gke.workload.list_workloads
severity: HIGH
conditions:
  all:
  - var: item.spec.containers[].env[].valueFrom
    op: exists
  - var: item.spec.containers[].env[].value
    op: not_exists
