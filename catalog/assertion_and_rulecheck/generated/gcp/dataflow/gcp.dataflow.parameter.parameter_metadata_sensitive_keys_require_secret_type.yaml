version: '1.0'
rule_id: gcp.dataflow.parameter.parameter_metadata_sensitive_keys_require_secret_type
for_each: gcp.dataflow.projects.locations.jobs.list
severity: HIGH
conditions:
  all:
  - var: item.environment.sdkPipelineOptions.parameters
    op: exists
  - var: item.environment.sdkPipelineOptions.parameters[].key
    op: not_contains
    value: password
  - var: item.environment.sdkPipelineOptions.parameters[].key
    op: not_contains
    value: secret
  - var: item.environment.sdkPipelineOptions.parameters[].key
    op: not_contains
    value: key
  - var: item.environment.sdkPipelineOptions.parameters[].key
    op: not_contains
    value: token
  - var: item.environment.sdkPipelineOptions.parameters[].key
    op: not_contains
    value: credential
fixture_validated: false
fixture_note: 'PASS fixture returned FAIL — wrong field or operator (pass_fixture keys: [''clientRequestId'', ''createTime'',
  ''createdFromSnapshotId'', ''currentState'', ''currentStateTime'', ''environment'', ''executionInfo'', ''id''])'
