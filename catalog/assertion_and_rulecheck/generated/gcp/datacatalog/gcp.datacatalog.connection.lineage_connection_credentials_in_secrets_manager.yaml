version: '1.0'
rule_id: gcp.datacatalog.connection.lineage_connection_credentials_in_secrets_manager
for_each: gcp.datacatalog.projects.locations.entryGroups.entries.list
severity: HIGH
conditions:
  var: item.activatedPolicyTypes
  op: exists
