version: '1.0'
rule_id: gcp.datacatalog.tag.rule_parameters_no_plaintext_secrets
for_each: gcp.datacatalog.projects.locations.entryGroups.entries.tags.list
severity: HIGH
conditions:
  var: item.fields[].stringValue
  op: not_contains
  value: password
fixture_validated: false
fixture_note: 'FAIL fixture returned PASS — conditions too loose (fail_fixture keys: [''policyTagCount''])'
