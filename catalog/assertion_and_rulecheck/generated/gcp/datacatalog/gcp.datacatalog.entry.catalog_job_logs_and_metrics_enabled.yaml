version: '1.0'
rule_id: gcp.datacatalog.entry.catalog_job_logs_and_metrics_enabled
for_each: gcp.datacatalog.projects.locations.entryGroups.list
severity: MEDIUM
conditions:
  all:
  - var: item.activatedPolicyTypes
    op: exists
  - var: item.service
    op: exists
