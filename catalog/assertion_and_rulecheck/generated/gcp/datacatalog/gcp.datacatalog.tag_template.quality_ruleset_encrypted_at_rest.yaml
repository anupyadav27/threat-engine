version: '1.0'
rule_id: gcp.datacatalog.tag_template.quality_ruleset_encrypted_at_rest
for_each: gcp.datacatalog.projects.locations.taxonomies.list
severity: LOW
conditions:
  var: item.activatedPolicyTypes
  op: exists
