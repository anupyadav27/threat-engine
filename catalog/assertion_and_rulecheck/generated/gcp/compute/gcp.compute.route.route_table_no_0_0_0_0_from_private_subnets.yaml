version: '1.0'
rule_id: gcp.compute.route.route_table_no_0_0_0_0_from_private_subnets
for_each: gcp.compute.list_routes
severity: HIGH
conditions:
  all:
  - var: item.destRange
    op: not_equals
    value: 0.0.0.0/0
  - var: item.network
    op: contains
    value: /networks/
