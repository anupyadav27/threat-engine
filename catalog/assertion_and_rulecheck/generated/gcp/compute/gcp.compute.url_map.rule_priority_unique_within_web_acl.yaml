version: '1.0'
rule_id: gcp.compute.url_map.rule_priority_unique_within_web_acl
for_each: gcp.compute.list_url_maps
severity: MEDIUM
conditions:
  all:
  - var: item.rules[].priority
    op: not_contains
    value: null
