version: '1.0'
rule_id: gcp.compute.instance.vm_root_volume_encrypted_cmek
for_each: gcp.compute.list_instances
severity: HIGH
conditions:
  all:
  - var: item.disks[].diskEncryptionKey.kmsKeyName
    op: exists
