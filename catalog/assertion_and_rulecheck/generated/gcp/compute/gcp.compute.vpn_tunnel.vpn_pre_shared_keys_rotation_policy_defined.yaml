version: '1.0'
rule_id: gcp.compute.vpn_tunnel.vpn_pre_shared_keys_rotation_policy_defined
for_each: gcp.compute.vpn_tunnel.list
severity: HIGH
conditions:
  var: item.preSharedKeyRotationPolicy
  op: exists
