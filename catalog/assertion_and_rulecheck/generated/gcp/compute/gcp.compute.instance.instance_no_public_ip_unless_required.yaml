version: '1.0'
rule_id: gcp.compute.instance.instance_no_public_ip_unless_required
for_each: gcp.compute.list_instances
severity: HIGH
conditions:
  all:
  - var: item.networkInterfaces[].accessConfigs
    op: is_empty
  - var: item.networkInterfaces[].ipv6AccessConfigs
    op: is_empty
