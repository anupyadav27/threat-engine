version: '1.0'
rule_id: gcp.cloudidentity.group.group_attached_policies_not_admin_star
for_each: gcp.cloudidentity.groups.list
severity: HIGH
conditions:
  all:
  - var: item.roles[].name
    op: not_contains
    value: roles/owner
  - var: item.roles[].name
    op: not_contains
    value: roles/editor
fixture_validated: false
fixture_note: 'FAIL fixture returned PASS — conditions too loose (fail_fixture keys: [''labels''])'
