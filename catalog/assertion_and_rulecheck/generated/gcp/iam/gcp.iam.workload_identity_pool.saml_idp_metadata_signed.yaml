version: '1.0'
rule_id: gcp.iam.workload_identity_pool.saml_idp_metadata_signed
for_each: gcp.iam.projects.locations.workloadIdentityPools.list
severity: HIGH
conditions:
  var: item.inlineTrustConfig.keyStorageType
  op: exists
