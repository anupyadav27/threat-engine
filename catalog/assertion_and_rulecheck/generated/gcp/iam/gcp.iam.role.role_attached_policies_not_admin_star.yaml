version: '1.0'
rule_id: gcp.iam.role.role_attached_policies_not_admin_star
for_each: gcp.iam.organizations.roles.list
severity: HIGH
conditions:
  var: item.name
  op: not_contains
  value: admin
fixture_validated: false
fixture_note: 'FAIL fixture returned PASS — conditions too loose (fail_fixture keys: [''disabled'', ''inlineCertificateIssuanceConfig'',
  ''inlineTrustConfig'', ''state''])'
