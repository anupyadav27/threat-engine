version: '1.0'
rule_id: gcp.accessapproval.approval_request.enrollment_configured
for_each: gcp.accessapproval.projects.getAccessApprovalSettings
severity: MEDIUM
conditions:
  var: item.enrolledServices
  op: not_empty
