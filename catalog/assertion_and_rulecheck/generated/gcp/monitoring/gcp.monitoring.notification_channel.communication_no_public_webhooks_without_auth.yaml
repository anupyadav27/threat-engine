version: '1.0'
rule_id: gcp.monitoring.notification_channel.communication_no_public_webhooks_without_auth
for_each: gcp.monitoring.projects.notificationChannels.list
severity: HIGH
conditions:
  all:
  - var: item.type
    op: equals
    value: webhook_token
  - var: item.labels.auth_password
    op: exists
