version: '1.0'
rule_id: gcp.monitoring.notification_channel.notification_destinations_configured
for_each: gcp.monitoring.projects.notificationChannels.list
severity: HIGH
conditions:
  var: item
  op: not_empty
