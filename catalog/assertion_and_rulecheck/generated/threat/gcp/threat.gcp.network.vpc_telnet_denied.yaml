version: '1.0'
rule_id: threat.gcp.network.vpc_telnet_denied
for_each: gcp.compute.list_firewalls
severity: MEDIUM
conditions:
  all:
  - var: item.allowed[].IPProtocol
    op: not_contains
    value: tcp
  - var: item.allowed[].ports[]
    op: not_contains
    value: '23'
