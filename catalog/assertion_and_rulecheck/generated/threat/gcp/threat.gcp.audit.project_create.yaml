version: '1.0'
rule_id: threat.gcp.audit.project_create
for_each: gcp.cloudresourcemanager.list_projects
severity: MEDIUM
conditions:
  all:
  - var: item.lifecycleState
    op: equals
    value: ACTIVE
  - var: item.createTime
    op: exists
