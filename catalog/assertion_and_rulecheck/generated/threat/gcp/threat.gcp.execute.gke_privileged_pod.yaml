version: '1.0'
rule_id: threat.gcp.execute.gke_privileged_pod
for_each: gcp.container.list_clusters
severity: HIGH
conditions:
  all:
  - var: item.nodePools[].config.securityContext.privileged
    op: is_false
