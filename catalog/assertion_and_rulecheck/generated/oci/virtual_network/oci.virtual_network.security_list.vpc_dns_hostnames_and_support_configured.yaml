version: '1.0'
rule_id: oci.virtual_network.security_list.vpc_dns_hostnames_and_support_configured
for_each: oci.virtual_network.list_security_lists
severity: LOW
conditions:
  all:
  - var: item.egress_security_rules
    op: not_empty
  - var: item.ingress_security_rules
    op: not_empty
