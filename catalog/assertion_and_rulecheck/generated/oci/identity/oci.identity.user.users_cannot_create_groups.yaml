version: '1.0'
rule_id: oci.identity.user.users_cannot_create_groups
for_each: oci.identity.list_policies
severity: MEDIUM
conditions:
  all:
  - var: item.statements
    op: not_contains
    value: Allow any-user to manage groups in tenancy
  - var: item.statements
    op: not_contains
    value: Allow group
fixture_validated: false
fixture_note: 'FAIL fixture returned PASS — conditions too loose (fail_fixture keys: [''status'', ''freeform_tags'', ''defined_tags''])'
