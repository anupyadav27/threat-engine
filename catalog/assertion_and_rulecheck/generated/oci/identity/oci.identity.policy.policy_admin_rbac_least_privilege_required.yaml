version: '1.0'
rule_id: oci.identity.policy.policy_admin_rbac_least_privilege_required
for_each: oci.identity.list_policies
severity: CRITICAL
conditions:
  all:
  - var: item.statements
    op: not_contains
    value: Allow group Administrators to manage all-resources in tenancy
  - var: item.statements
    op: not_contains
    value: Allow group Administrators to manage all-resources in compartment
fixture_validated: false
fixture_note: 'FAIL fixture returned PASS — conditions too loose (fail_fixture keys: [''status'', ''freeform_tags'', ''defined_tags''])'
