version: '1.0'
rule_id: oci.compute.instance.elastic_ip_shodan
for_each: oci.compute.list_instances
severity: LOW
conditions:
  all:
  - var: item.metadata
    op: exists
  - var: item.metadata
    op: not_contains
    value: elastic_ip
  - var: item.metadata
    op: not_contains
    value: shodan
