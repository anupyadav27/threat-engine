version: '1.0'
rule_id: oci.compute.instance.ebs_volume_protected_by_backup_plan
for_each: oci.compute.list_instances
severity: MEDIUM
conditions:
  all:
  - var: item.ocid
    op: exists
  - var: item.status
    op: equals
    value: ACTIVE
