version: '1.0'
rule_id: oci.compute.instance.instance_group_profile_least_privilege_required
for_each: oci.compute.list_instances
severity: HIGH
conditions:
  all:
  - var: item.instance_group_profile_least_privilege_required
    op: exists
  - var: item.instance_group_profile_least_privilege_required
    op: is_true
fixture_validated: false
fixture_note: 'PASS fixture returned FAIL — wrong field or operator (pass_fixture keys: [''ocid'', ''compartment_id'', ''name'',
  ''status'', ''time_created'', ''freeform_tags'', ''defined_tags'', ''availability_domain''])'
