version: '1.0'
rule_id: alicloud.config.recorder_enabled_in_all_regions.configured
for_each: alicloud.config.delivery
severity: MEDIUM
conditions:
  all:
  - var: item.status
    op: equals
    value: ACTIVE
  - var: item.region
    op: exists
