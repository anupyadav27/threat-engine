version: '1.0'
rule_id: alicloud.general.cluster_secrets_kms_cmk_encryption_at_rest_enabled.configured
for_each: alicloud.general.accelerator_cluster
severity: MEDIUM
conditions:
  all:
  - var: item.encrypted
    op: is_true
  - var: item.kms_key_id
    op: exists
