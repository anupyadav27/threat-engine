version: '1.0'
rule_id: ibm.container_registry.image.not_stale
for_each: ibm.container_registry.list_images
severity: MEDIUM
conditions:
  all:
  - var: item.CreatedAt
    op: exists
  - var: item.CreatedAt
    op: gte
    value: now-30d
