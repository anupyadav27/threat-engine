# Discovery YAML — cloudap (AliCloud)
# Generated: 2026-02-21T06:19:57Z
version: '1.0'
provider: alicloud
service: cloudap
services:
  client: cloudap
  module: alibabacloud_python_sdk.cloudap
discovery:
  # ── DescribeCloudaps ──
  - discovery_id: alicloud.cloudap.DescribeCloudaps
    calls:
      - action: DescribeCloudaps
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.data }}'
      item:
        CreateTime: '{{ item.CreateTime }}'
        InstanceId: '{{ item.InstanceId }}'
        Name: '{{ item.Name }}'
        RegionId: '{{ item.RegionId }}'
        Status: '{{ item.Status }}'
        Tags: '{{ item.Tags }}'
        Description: '{{ item.Description }}'
        ExpireTime: '{{ item.ExpireTime }}'
        OwnerId: '{{ item.OwnerId }}'
        ResourceGroupId: '{{ item.ResourceGroupId }}'
        ZoneId: '{{ item.ZoneId }}'
  # ── Dependent ops ──
  # ── DescribeCloudap [dependent] ──
  - discovery_id: alicloud.cloudap.DescribeCloudap
    calls:
      - action: DescribeCloudap
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        CreateTime: '{{ response.CreateTime }}'
        InstanceId: '{{ response.InstanceId }}'
        Name: '{{ response.Name }}'
        RegionId: '{{ response.RegionId }}'
        Status: '{{ response.Status }}'
        Tags: '{{ response.Tags }}'
        Description: '{{ response.Description }}'
        ExpireTime: '{{ response.ExpireTime }}'
        OwnerId: '{{ response.OwnerId }}'
        ResourceGroupId: '{{ response.ResourceGroupId }}'
        ZoneId: '{{ response.ZoneId }}'
    # required_params: ['InstanceId']
