# Auto-generated catalog patch for aws/cloudsearchdomain
# Generated by scripts/catalog_gap_autogen.py (DCAT-01)

additions:
  aws.cloudsearchdomain.search:
    hits_cursor: '{{ response.hits.cursor }}'
    hits_found: '{{ response.hits.found }}'
    hits_hit[]_exprs: '{{ response.hits.hit[].exprs }}'
    hits_hit[]_fields: '{{ response.hits.hit[].fields }}'
    hits_hit[]_highlights: '{{ response.hits.hit[].highlights }}'
    hits_hit[]_id: '{{ response.hits.hit[].id }}'
    hits_start: '{{ response.hits.start }}'
    status_rid: '{{ response.status.rid }}'
    status_timems: '{{ response.status.timems }}'
renames:
  aws.cloudsearchdomain.search:
    status: stats
