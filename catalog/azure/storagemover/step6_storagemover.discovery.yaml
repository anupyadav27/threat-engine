# Discovery YAML — storagemover (Azure)
# Generated: 2026-02-21T05:02:44Z
# Actions use Azure Python SDK snake_case names (azure-mgmt-*)
version: '1.0'
provider: azure
service: storagemover
services:
  client: storagemover
  module: azure.mgmt.storagemover
discovery:
  # ── list_by_subscription ──
  - discovery_id: azure.storagemover.list_by_subscription
    calls:
      - action: list_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        properties: '{{ item.properties }}'
  # ── Dependent ops ──
  # ── list [dependent] ──
  - discovery_id: azure.storagemover.list
    calls:
      - action: list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        properties: '{{ item.properties }}'
    # required_params: ['storage_mover_name', 'project_name', 'job_definition_name']
