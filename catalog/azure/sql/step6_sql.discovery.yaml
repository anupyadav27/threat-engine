# Discovery YAML — sql (Azure)
# Generated: 2026-02-21T05:02:44Z
# Actions use Azure Python SDK snake_case names (azure-mgmt-*)
version: '1.0'
provider: azure
service: sql
services:
  client: sql
  module: azure.mgmt.sql
discovery:
  # ── list ──
  - discovery_id: azure.sql.list
    calls:
      - action: list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
  # ── list_by_instance_pool ──
  - discovery_id: azure.sql.list_by_instance_pool
    calls:
      - action: list_by_instance_pool
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['instance_pool_name']
  # ── list_by_location ──
  - discovery_id: azure.sql.list_by_location
    calls:
      - action: list_by_location
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['location_name']
  # ── list_by_managed_instance ──
  - discovery_id: azure.sql.list_by_managed_instance
    calls:
      - action: list_by_managed_instance
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['managed_instance_name']
  # ── list_by_resource_group ──
  - discovery_id: azure.sql.list_by_resource_group
    calls:
      - action: list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
  # ── list_by_resource_group_location ──
  - discovery_id: azure.sql.list_by_resource_group_location
    calls:
      - action: list_by_resource_group_location
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['location_name']
  # ── list_inaccessible_by_instance ──
  - discovery_id: azure.sql.list_inaccessible_by_instance
    calls:
      - action: list_inaccessible_by_instance
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['managed_instance_name']
  # ── list_inaccessible_by_server ──
  - discovery_id: azure.sql.list_inaccessible_by_server
    calls:
      - action: list_inaccessible_by_server
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['server_name']
  # ── list_sync_database_ids ──
  - discovery_id: azure.sql.list_sync_database_ids
    calls:
      - action: list_sync_database_ids
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['location_name']
  # ── Dependent ops ──
  # ── check_name_availability [dependent] ──
  - discovery_id: azure.sql.check_name_availability
    calls:
      - action: check_name_availability
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        available: '{{ response.available }}'
        message: '{{ response.message }}'
        reason: '{{ response.reason }}'
    # required_params: ['parameters']
  # ── get_by_resource_group [dependent] ──
  - discovery_id: azure.sql.get_by_resource_group
    calls:
      - action: get_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        type: '{{ response.type }}'
        backup_expiration_time: '{{ response.backup_expiration_time }}'
        backup_storage_redundancy: '{{ response.backup_storage_redundancy }}'
        backup_time: '{{ response.backup_time }}'
        database_deletion_time: '{{ response.database_deletion_time }}'
        database_name: '{{ response.database_name }}'
        managed_instance_create_time: '{{ response.managed_instance_create_time }}'
        managed_instance_name: '{{ response.managed_instance_name }}'
    # required_params: ['location_name', 'managed_instance_name', 'database_name', 'backup_name']
  # ── get_by_version [dependent] ──
  - discovery_id: azure.sql.get_by_version
    calls:
      - action: get_by_version
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        type: '{{ response.type }}'
        action: '{{ response.action }}'
        credential: '{{ response.credential }}'
        execution_options: '{{ response.execution_options }}'
        output: '{{ response.output }}'
        step_id: '{{ response.step_id }}'
        target_group: '{{ response.target_group }}'
    # required_params: ['server_name', 'job_agent_name', 'job_name', 'job_version', 'step_name']
  # ── list_by_agent [dependent] ──
  - discovery_id: azure.sql.list_by_agent
    calls:
      - action: list_by_agent
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['server_name', 'job_agent_name']
  # ── list_by_configuration [dependent] ──
  - discovery_id: azure.sql.list_by_configuration
    calls:
      - action: list_by_configuration
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['server_name', 'database_name', 'transparent_data_encryption_name']
  # ── list_by_database [dependent] ──
  - discovery_id: azure.sql.list_by_database
    calls:
      - action: list_by_database
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['server_name', 'database_name']
  # ── list_by_database_advisor [dependent] ──
  - discovery_id: azure.sql.list_by_database_advisor
    calls:
      - action: list_by_database_advisor
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
        details: '{{ item.details }}'
        error_details: '{{ item.error_details }}'
        estimated_impact: '{{ item.estimated_impact }}'
        execute_action_duration: '{{ item.execute_action_duration }}'
        execute_action_initiated_by: '{{ item.execute_action_initiated_by }}'
        execute_action_initiated_time: '{{ item.execute_action_initiated_time }}'
        execute_action_start_time: '{{ item.execute_action_start_time }}'
        implementation_details: '{{ item.implementation_details }}'
        is_archived_action: '{{ item.is_archived_action }}'
        is_executable_action: '{{ item.is_executable_action }}'
        is_revertable_action: '{{ item.is_revertable_action }}'
        kind: '{{ item.kind }}'
        last_refresh: '{{ item.last_refresh }}'
        linked_objects: '{{ item.linked_objects }}'
        observed_impact: '{{ item.observed_impact }}'
        recommendation_reason: '{{ item.recommendation_reason }}'
        revert_action_duration: '{{ item.revert_action_duration }}'
        revert_action_initiated_by: '{{ item.revert_action_initiated_by }}'
        revert_action_initiated_time: '{{ item.revert_action_initiated_time }}'
        revert_action_start_time: '{{ item.revert_action_start_time }}'
        score: '{{ item.score }}'
        state: '{{ item.state }}'
        time_series: '{{ item.time_series }}'
        valid_since: '{{ item.valid_since }}'
    # required_params: ['server_name', 'database_name', 'advisor_name']
  # ── list_by_elastic_pool [dependent] ──
  - discovery_id: azure.sql.list_by_elastic_pool
    calls:
      - action: list_by_elastic_pool
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['server_name', 'elastic_pool_name']
  # ── list_by_instance [dependent] ──
  - discovery_id: azure.sql.list_by_instance
    calls:
      - action: list_by_instance
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['location_name', 'managed_instance_name']
  # ── list_by_job [dependent] ──
  - discovery_id: azure.sql.list_by_job
    calls:
      - action: list_by_job
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['server_name', 'job_agent_name', 'job_name']
  # ── list_by_job_execution [dependent] ──
  - discovery_id: azure.sql.list_by_job_execution
    calls:
      - action: list_by_job_execution
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['server_name', 'job_agent_name', 'job_name', 'job_execution_id']
  # ── list_by_query [dependent] ──
  - discovery_id: azure.sql.list_by_query
    calls:
      - action: list_by_query
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['managed_instance_name', 'database_name', 'query_id']
  # ── list_by_resource_group_database [dependent] ──
  - discovery_id: azure.sql.list_by_resource_group_database
    calls:
      - action: list_by_resource_group_database
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['location_name', 'managed_instance_name', 'database_name']
  # ── list_by_resource_group_instance [dependent] ──
  - discovery_id: azure.sql.list_by_resource_group_instance
    calls:
      - action: list_by_resource_group_instance
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['location_name', 'managed_instance_name']
  # ── list_by_resource_group_server [dependent] ──
  - discovery_id: azure.sql.list_by_resource_group_server
    calls:
      - action: list_by_resource_group_server
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['location_name', 'long_term_retention_server_name']
  # ── list_by_restorable_dropped_database [dependent] ──
  - discovery_id: azure.sql.list_by_restorable_dropped_database
    calls:
      - action: list_by_restorable_dropped_database
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['managed_instance_name', 'restorable_dropped_database_id']
  # ── list_by_schema [dependent] ──
  - discovery_id: azure.sql.list_by_schema
    calls:
      - action: list_by_schema
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['managed_instance_name', 'database_name', 'schema_name']
  # ── list_by_server [dependent] ──
  - discovery_id: azure.sql.list_by_server
    calls:
      - action: list_by_server
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['location_name', 'long_term_retention_server_name']
  # ── list_by_step [dependent] ──
  - discovery_id: azure.sql.list_by_step
    calls:
      - action: list_by_step
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['server_name', 'job_agent_name', 'job_name', 'job_execution_id', 'step_name']
  # ── list_by_sync_group [dependent] ──
  - discovery_id: azure.sql.list_by_sync_group
    calls:
      - action: list_by_sync_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['server_name', 'database_name', 'sync_group_name']
  # ── list_by_table [dependent] ──
  - discovery_id: azure.sql.list_by_table
    calls:
      - action: list_by_table
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['managed_instance_name', 'database_name', 'schema_name', 'table_name']
  # ── list_by_version [dependent] ──
  - discovery_id: azure.sql.list_by_version
    calls:
      - action: list_by_version
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['server_name', 'job_agent_name', 'job_name', 'job_version']
  # ── list_by_workload_group [dependent] ──
  - discovery_id: azure.sql.list_by_workload_group
    calls:
      - action: list_by_workload_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['server_name', 'database_name', 'workload_group_name']
  # ── list_current_by_database [dependent] ──
  - discovery_id: azure.sql.list_current_by_database
    calls:
      - action: list_current_by_database
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['server_name', 'database_name']
  # ── list_hub_schemas [dependent] ──
  - discovery_id: azure.sql.list_hub_schemas
    calls:
      - action: list_hub_schemas
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['server_name', 'database_name', 'sync_group_name']
  # ── list_linked_databases [dependent] ──
  - discovery_id: azure.sql.list_linked_databases
    calls:
      - action: list_linked_databases
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['server_name', 'sync_agent_name']
  # ── list_logs [dependent] ──
  - discovery_id: azure.sql.list_logs
    calls:
      - action: list_logs
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['server_name', 'database_name', 'sync_group_name', 'start_time', 'end_time', 'type']
  # ── list_member_schemas [dependent] ──
  - discovery_id: azure.sql.list_member_schemas
    calls:
      - action: list_member_schemas
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['server_name', 'database_name', 'sync_group_name', 'sync_member_name']
  # ── list_metric_definitions [dependent] ──
  - discovery_id: azure.sql.list_metric_definitions
    calls:
      - action: list_metric_definitions
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['server_name', 'elastic_pool_name']
  # ── list_metrics [dependent] ──
  - discovery_id: azure.sql.list_metrics
    calls:
      - action: list_metrics
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['server_name', 'elastic_pool_name', 'filter']
  # ── list_recommended_by_database [dependent] ──
  - discovery_id: azure.sql.list_recommended_by_database
    calls:
      - action: list_recommended_by_database
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['server_name', 'database_name']
