# Discovery YAML — hdinsightcontainers (Azure)
# Generated: 2026-02-21T05:02:44Z
# Actions use Azure Python SDK snake_case names (azure-mgmt-*)
version: '1.0'
provider: azure
service: hdinsightcontainers
services:
  client: hdinsightcontainers
  module: azure.mgmt.hdinsightcontainers
discovery:
  # ── list_by_cluster_pool_name ──
  - discovery_id: azure.hdinsightcontainers.list_by_cluster_pool_name
    calls:
      - action: list_by_cluster_pool_name
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        tags: '{{ item.tags }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        system_data: '{{ item.system_data }}'
    # required_params: ['cluster_pool_name']
  # ── list_by_location ──
  - discovery_id: azure.hdinsightcontainers.list_by_location
    calls:
      - action: list_by_location
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        system_data: '{{ item.system_data }}'
    # required_params: ['location']
  # ── list_by_resource_group ──
  - discovery_id: azure.hdinsightcontainers.list_by_resource_group
    calls:
      - action: list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        tags: '{{ item.tags }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        system_data: '{{ item.system_data }}'
  # ── list_by_subscription ──
  - discovery_id: azure.hdinsightcontainers.list_by_subscription
    calls:
      - action: list_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        tags: '{{ item.tags }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        system_data: '{{ item.system_data }}'
  # ── Dependent ops ──
  # ── check_name_availability [dependent] ──
  - discovery_id: azure.hdinsightcontainers.check_name_availability
    calls:
      - action: check_name_availability
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        message: '{{ response.message }}'
        name_available: '{{ response.name_available }}'
        reason: '{{ response.reason }}'
    # required_params: ['location', 'name_availability_parameters']
  # ── get_instance_view [dependent] ──
  - discovery_id: azure.hdinsightcontainers.get_instance_view
    calls:
      - action: get_instance_view
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        properties: '{{ response.properties }}'
    # required_params: ['cluster_pool_name', 'cluster_name']
  # ── list [dependent] ──
  - discovery_id: azure.hdinsightcontainers.list
    calls:
      - action: list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        system_data: '{{ item.system_data }}'
    # required_params: ['cluster_pool_name', 'cluster_name']
  # ── list_instance_views [dependent] ──
  - discovery_id: azure.hdinsightcontainers.list_instance_views
    calls:
      - action: list_instance_views
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        name: '{{ item.name }}'
        properties: '{{ item.properties }}'
    # required_params: ['cluster_pool_name', 'cluster_name']
  # ── list_service_configs [dependent] ──
  - discovery_id: azure.hdinsightcontainers.list_service_configs
    calls:
      - action: list_service_configs
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        properties: '{{ item.properties }}'
    # required_params: ['cluster_pool_name', 'cluster_name']
