# Discovery YAML — eventgrid (Azure)
# Generated: 2026-02-21T05:02:44Z
# Actions use Azure Python SDK snake_case names (azure-mgmt-*)
version: '1.0'
provider: azure
service: eventgrid
services:
  client: eventgrid
  module: azure.mgmt.eventgrid
discovery:
  # ── list_by_domain ──
  - discovery_id: azure.eventgrid.list_by_domain
    calls:
      - action: list_by_domain
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        type: '{{ item.type }}'
        system_data: '{{ item.system_data }}'
    # required_params: ['domain_name']
  # ── list_by_namespace ──
  - discovery_id: azure.eventgrid.list_by_namespace
    calls:
      - action: list_by_namespace
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        type: '{{ item.type }}'
        description: '{{ item.description }}'
        system_data: '{{ item.system_data }}'
        topic_templates: '{{ item.topic_templates }}'
    # required_params: ['namespace_name']
  # ── list_by_partner_namespace ──
  - discovery_id: azure.eventgrid.list_by_partner_namespace
    calls:
      - action: list_by_partner_namespace
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        type: '{{ item.type }}'
        channel_type: '{{ item.channel_type }}'
        expiration_time_if_not_activated_utc: '{{ item.expiration_time_if_not_activated_utc }}'
        message_for_activation: '{{ item.message_for_activation }}'
        partner_topic_info: '{{ item.partner_topic_info }}'
        readiness_state: '{{ item.readiness_state }}'
        system_data: '{{ item.system_data }}'
    # required_params: ['partner_namespace_name']
  # ── list_by_partner_topic ──
  - discovery_id: azure.eventgrid.list_by_partner_topic
    calls:
      - action: list_by_partner_topic
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        type: '{{ item.type }}'
        dead_letter_destination: '{{ item.dead_letter_destination }}'
        dead_letter_with_resource_identity: '{{ item.dead_letter_with_resource_identity }}'
        delivery_with_resource_identity: '{{ item.delivery_with_resource_identity }}'
        destination: '{{ item.destination }}'
        event_delivery_schema: '{{ item.event_delivery_schema }}'
        expiration_time_utc: '{{ item.expiration_time_utc }}'
        filter: '{{ item.filter }}'
        labels: '{{ item.labels }}'
        retry_policy: '{{ item.retry_policy }}'
        system_data: '{{ item.system_data }}'
        topic: '{{ item.topic }}'
    # required_params: ['partner_topic_name']
  # ── list_by_resource_group ──
  - discovery_id: azure.eventgrid.list_by_resource_group
    calls:
      - action: list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        identity: '{{ item.identity }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        private_endpoint_connections: '{{ item.private_endpoint_connections }}'
        provisioning_state: '{{ item.provisioning_state }}'
        public_network_access: '{{ item.public_network_access }}'
        tags: '{{ item.tags }}'
        type: '{{ item.type }}'
        data_residency_boundary: '{{ item.data_residency_boundary }}'
        disable_local_auth: '{{ item.disable_local_auth }}'
        endpoint: '{{ item.endpoint }}'
        event_type_info: '{{ item.event_type_info }}'
        inbound_ip_rules: '{{ item.inbound_ip_rules }}'
        input_schema: '{{ item.input_schema }}'
        input_schema_mapping: '{{ item.input_schema_mapping }}'
        metric_resource_id: '{{ item.metric_resource_id }}'
        minimum_tls_version_allowed: '{{ item.minimum_tls_version_allowed }}'
        system_data: '{{ item.system_data }}'
  # ── list_by_subscription ──
  - discovery_id: azure.eventgrid.list_by_subscription
    calls:
      - action: list_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        identity: '{{ item.identity }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        private_endpoint_connections: '{{ item.private_endpoint_connections }}'
        provisioning_state: '{{ item.provisioning_state }}'
        public_network_access: '{{ item.public_network_access }}'
        tags: '{{ item.tags }}'
        type: '{{ item.type }}'
        data_residency_boundary: '{{ item.data_residency_boundary }}'
        disable_local_auth: '{{ item.disable_local_auth }}'
        endpoint: '{{ item.endpoint }}'
        event_type_info: '{{ item.event_type_info }}'
        inbound_ip_rules: '{{ item.inbound_ip_rules }}'
        input_schema: '{{ item.input_schema }}'
        input_schema_mapping: '{{ item.input_schema_mapping }}'
        metric_resource_id: '{{ item.metric_resource_id }}'
        minimum_tls_version_allowed: '{{ item.minimum_tls_version_allowed }}'
        system_data: '{{ item.system_data }}'
  # ── list_by_system_topic ──
  - discovery_id: azure.eventgrid.list_by_system_topic
    calls:
      - action: list_by_system_topic
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        type: '{{ item.type }}'
        dead_letter_destination: '{{ item.dead_letter_destination }}'
        dead_letter_with_resource_identity: '{{ item.dead_letter_with_resource_identity }}'
        delivery_with_resource_identity: '{{ item.delivery_with_resource_identity }}'
        destination: '{{ item.destination }}'
        event_delivery_schema: '{{ item.event_delivery_schema }}'
        expiration_time_utc: '{{ item.expiration_time_utc }}'
        filter: '{{ item.filter }}'
        labels: '{{ item.labels }}'
        retry_policy: '{{ item.retry_policy }}'
        system_data: '{{ item.system_data }}'
        topic: '{{ item.topic }}'
    # required_params: ['system_topic_name']
  # ── list_global_by_resource_group ──
  - discovery_id: azure.eventgrid.list_global_by_resource_group
    calls:
      - action: list_global_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        type: '{{ item.type }}'
        dead_letter_destination: '{{ item.dead_letter_destination }}'
        dead_letter_with_resource_identity: '{{ item.dead_letter_with_resource_identity }}'
        delivery_with_resource_identity: '{{ item.delivery_with_resource_identity }}'
        destination: '{{ item.destination }}'
        event_delivery_schema: '{{ item.event_delivery_schema }}'
        expiration_time_utc: '{{ item.expiration_time_utc }}'
        filter: '{{ item.filter }}'
        labels: '{{ item.labels }}'
        retry_policy: '{{ item.retry_policy }}'
        system_data: '{{ item.system_data }}'
        topic: '{{ item.topic }}'
  # ── list_global_by_resource_group_for_topic_type ──
  - discovery_id: azure.eventgrid.list_global_by_resource_group_for_topic_type
    calls:
      - action: list_global_by_resource_group_for_topic_type
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        type: '{{ item.type }}'
        dead_letter_destination: '{{ item.dead_letter_destination }}'
        dead_letter_with_resource_identity: '{{ item.dead_letter_with_resource_identity }}'
        delivery_with_resource_identity: '{{ item.delivery_with_resource_identity }}'
        destination: '{{ item.destination }}'
        event_delivery_schema: '{{ item.event_delivery_schema }}'
        expiration_time_utc: '{{ item.expiration_time_utc }}'
        filter: '{{ item.filter }}'
        labels: '{{ item.labels }}'
        retry_policy: '{{ item.retry_policy }}'
        system_data: '{{ item.system_data }}'
        topic: '{{ item.topic }}'
    # required_params: ['topic_type_name']
  # ── list_global_by_subscription ──
  - discovery_id: azure.eventgrid.list_global_by_subscription
    calls:
      - action: list_global_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        type: '{{ item.type }}'
        dead_letter_destination: '{{ item.dead_letter_destination }}'
        dead_letter_with_resource_identity: '{{ item.dead_letter_with_resource_identity }}'
        delivery_with_resource_identity: '{{ item.delivery_with_resource_identity }}'
        destination: '{{ item.destination }}'
        event_delivery_schema: '{{ item.event_delivery_schema }}'
        expiration_time_utc: '{{ item.expiration_time_utc }}'
        filter: '{{ item.filter }}'
        labels: '{{ item.labels }}'
        retry_policy: '{{ item.retry_policy }}'
        system_data: '{{ item.system_data }}'
        topic: '{{ item.topic }}'
  # ── list_global_by_subscription_for_topic_type ──
  - discovery_id: azure.eventgrid.list_global_by_subscription_for_topic_type
    calls:
      - action: list_global_by_subscription_for_topic_type
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        type: '{{ item.type }}'
        dead_letter_destination: '{{ item.dead_letter_destination }}'
        dead_letter_with_resource_identity: '{{ item.dead_letter_with_resource_identity }}'
        delivery_with_resource_identity: '{{ item.delivery_with_resource_identity }}'
        destination: '{{ item.destination }}'
        event_delivery_schema: '{{ item.event_delivery_schema }}'
        expiration_time_utc: '{{ item.expiration_time_utc }}'
        filter: '{{ item.filter }}'
        labels: '{{ item.labels }}'
        retry_policy: '{{ item.retry_policy }}'
        system_data: '{{ item.system_data }}'
        topic: '{{ item.topic }}'
    # required_params: ['topic_type_name']
  # ── list_regional_by_resource_group ──
  - discovery_id: azure.eventgrid.list_regional_by_resource_group
    calls:
      - action: list_regional_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        type: '{{ item.type }}'
        dead_letter_destination: '{{ item.dead_letter_destination }}'
        dead_letter_with_resource_identity: '{{ item.dead_letter_with_resource_identity }}'
        delivery_with_resource_identity: '{{ item.delivery_with_resource_identity }}'
        destination: '{{ item.destination }}'
        event_delivery_schema: '{{ item.event_delivery_schema }}'
        expiration_time_utc: '{{ item.expiration_time_utc }}'
        filter: '{{ item.filter }}'
        labels: '{{ item.labels }}'
        retry_policy: '{{ item.retry_policy }}'
        system_data: '{{ item.system_data }}'
        topic: '{{ item.topic }}'
    # required_params: ['location']
  # ── list_regional_by_subscription ──
  - discovery_id: azure.eventgrid.list_regional_by_subscription
    calls:
      - action: list_regional_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        type: '{{ item.type }}'
        dead_letter_destination: '{{ item.dead_letter_destination }}'
        dead_letter_with_resource_identity: '{{ item.dead_letter_with_resource_identity }}'
        delivery_with_resource_identity: '{{ item.delivery_with_resource_identity }}'
        destination: '{{ item.destination }}'
        event_delivery_schema: '{{ item.event_delivery_schema }}'
        expiration_time_utc: '{{ item.expiration_time_utc }}'
        filter: '{{ item.filter }}'
        labels: '{{ item.labels }}'
        retry_policy: '{{ item.retry_policy }}'
        system_data: '{{ item.system_data }}'
        topic: '{{ item.topic }}'
    # required_params: ['location']
  # ── Dependent ops ──
  # ── get_delivery_attributes [dependent] ──
  - discovery_id: azure.eventgrid.get_delivery_attributes
    calls:
      - action: get_delivery_attributes
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        name: '{{ response.name }}'
        type: '{{ response.type }}'
    # required_params: ['topic_name', 'event_subscription_name']
  # ── get_full_url [dependent] ──
  - discovery_id: azure.eventgrid.get_full_url
    calls:
      - action: get_full_url
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        endpoint_url: '{{ response.endpoint_url }}'
    # required_params: ['topic_name', 'event_subscription_name']
  # ── list [dependent] ──
  - discovery_id: azure.eventgrid.list
    calls:
      - action: list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        type: '{{ item.type }}'
        dead_letter_destination: '{{ item.dead_letter_destination }}'
        dead_letter_with_resource_identity: '{{ item.dead_letter_with_resource_identity }}'
        delivery_with_resource_identity: '{{ item.delivery_with_resource_identity }}'
        destination: '{{ item.destination }}'
        event_delivery_schema: '{{ item.event_delivery_schema }}'
        expiration_time_utc: '{{ item.expiration_time_utc }}'
        filter: '{{ item.filter }}'
        labels: '{{ item.labels }}'
        retry_policy: '{{ item.retry_policy }}'
        system_data: '{{ item.system_data }}'
        topic: '{{ item.topic }}'
    # required_params: ['domain_name', 'topic_name']
  # ── list_by_domain_topic [dependent] ──
  - discovery_id: azure.eventgrid.list_by_domain_topic
    calls:
      - action: list_by_domain_topic
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        type: '{{ item.type }}'
        dead_letter_destination: '{{ item.dead_letter_destination }}'
        dead_letter_with_resource_identity: '{{ item.dead_letter_with_resource_identity }}'
        delivery_with_resource_identity: '{{ item.delivery_with_resource_identity }}'
        destination: '{{ item.destination }}'
        event_delivery_schema: '{{ item.event_delivery_schema }}'
        expiration_time_utc: '{{ item.expiration_time_utc }}'
        filter: '{{ item.filter }}'
        labels: '{{ item.labels }}'
        retry_policy: '{{ item.retry_policy }}'
        system_data: '{{ item.system_data }}'
        topic: '{{ item.topic }}'
    # required_params: ['domain_name', 'topic_name']
  # ── list_by_namespace_topic [dependent] ──
  - discovery_id: azure.eventgrid.list_by_namespace_topic
    calls:
      - action: list_by_namespace_topic
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        type: '{{ item.type }}'
        delivery_configuration: '{{ item.delivery_configuration }}'
        event_delivery_schema: '{{ item.event_delivery_schema }}'
        expiration_time_utc: '{{ item.expiration_time_utc }}'
        filters_configuration: '{{ item.filters_configuration }}'
        system_data: '{{ item.system_data }}'
    # required_params: ['namespace_name', 'topic_name']
  # ── list_by_resource [dependent] ──
  - discovery_id: azure.eventgrid.list_by_resource
    calls:
      - action: list_by_resource
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
        display_name: '{{ item.display_name }}'
        group_id: '{{ item.group_id }}'
        required_members: '{{ item.required_members }}'
        required_zone_names: '{{ item.required_zone_names }}'
    # required_params: ['parent_type', 'parent_name']
  # ── list_event_types [dependent] ──
  - discovery_id: azure.eventgrid.list_event_types
    calls:
      - action: list_event_types
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
        description: '{{ item.description }}'
        display_name: '{{ item.display_name }}'
        is_in_default_set: '{{ item.is_in_default_set }}'
        schema_url: '{{ item.schema_url }}'
    # required_params: ['provider_namespace', 'resource_type_name', 'resource_name']
  # ── list_regional_by_resource_group_for_topic_type [dependent] ──
  - discovery_id: azure.eventgrid.list_regional_by_resource_group_for_topic_type
    calls:
      - action: list_regional_by_resource_group_for_topic_type
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        type: '{{ item.type }}'
        dead_letter_destination: '{{ item.dead_letter_destination }}'
        dead_letter_with_resource_identity: '{{ item.dead_letter_with_resource_identity }}'
        delivery_with_resource_identity: '{{ item.delivery_with_resource_identity }}'
        destination: '{{ item.destination }}'
        event_delivery_schema: '{{ item.event_delivery_schema }}'
        expiration_time_utc: '{{ item.expiration_time_utc }}'
        filter: '{{ item.filter }}'
        labels: '{{ item.labels }}'
        retry_policy: '{{ item.retry_policy }}'
        system_data: '{{ item.system_data }}'
        topic: '{{ item.topic }}'
    # required_params: ['location', 'topic_type_name']
  # ── list_regional_by_subscription_for_topic_type [dependent] ──
  - discovery_id: azure.eventgrid.list_regional_by_subscription_for_topic_type
    calls:
      - action: list_regional_by_subscription_for_topic_type
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        type: '{{ item.type }}'
        dead_letter_destination: '{{ item.dead_letter_destination }}'
        dead_letter_with_resource_identity: '{{ item.dead_letter_with_resource_identity }}'
        delivery_with_resource_identity: '{{ item.delivery_with_resource_identity }}'
        destination: '{{ item.destination }}'
        event_delivery_schema: '{{ item.event_delivery_schema }}'
        expiration_time_utc: '{{ item.expiration_time_utc }}'
        filter: '{{ item.filter }}'
        labels: '{{ item.labels }}'
        retry_policy: '{{ item.retry_policy }}'
        system_data: '{{ item.system_data }}'
        topic: '{{ item.topic }}'
    # required_params: ['location', 'topic_type_name']
  # ── list_shared_access_keys [dependent] ──
  - discovery_id: azure.eventgrid.list_shared_access_keys
    calls:
      - action: list_shared_access_keys
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.key1 }}'
      item:
        key1: '{{ item.key1 }}'
        key2: '{{ item.key2 }}'
    # required_params: ['namespace_name', 'topic_name']
