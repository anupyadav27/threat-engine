# Discovery YAML — changeanalysis (Azure)
# Generated: 2026-02-21T05:02:43Z
# Actions use Azure Python SDK snake_case names (azure-mgmt-*)
version: '1.0'
provider: azure
service: changeanalysis
services:
  client: changeanalysis
  module: azure.mgmt.changeanalysis
discovery:
  # ── list [dependent] ──
  - discovery_id: azure.changeanalysis.list
    calls:
      - action: list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['resource_id', 'start_time', 'end_time']
  # ── list_changes_by_resource_group [dependent] ──
  - discovery_id: azure.changeanalysis.list_changes_by_resource_group
    calls:
      - action: list_changes_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['start_time', 'end_time']
  # ── list_changes_by_subscription [dependent] ──
  - discovery_id: azure.changeanalysis.list_changes_by_subscription
    calls:
      - action: list_changes_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['start_time', 'end_time']
