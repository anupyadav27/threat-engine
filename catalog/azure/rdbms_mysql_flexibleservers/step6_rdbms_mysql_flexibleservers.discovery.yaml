# Discovery YAML — rdbms_mysql_flexibleservers (Azure)
# Generated: 2026-02-21T05:02:44Z
# Actions use Azure Python SDK snake_case names (azure-mgmt-*)
version: '1.0'
provider: azure
service: rdbms_mysql_flexibleservers
services:
  client: rdbms_mysql_flexibleservers
  module: azure.mgmt.rdbms_mysql_flexibleservers
discovery:
  # ── list ──
  - discovery_id: azure.rdbms_mysql_flexibleservers.list
    calls:
      - action: list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        identity: '{{ item.identity }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        tags: '{{ item.tags }}'
        type: '{{ item.type }}'
        administrator_login: '{{ item.administrator_login }}'
        administrator_login_password: '{{ item.administrator_login_password }}'
        availability_zone: '{{ item.availability_zone }}'
        backup: '{{ item.backup }}'
        create_mode: '{{ item.create_mode }}'
        data_encryption: '{{ item.data_encryption }}'
        fully_qualified_domain_name: '{{ item.fully_qualified_domain_name }}'
        high_availability: '{{ item.high_availability }}'
        maintenance_window: '{{ item.maintenance_window }}'
        network: '{{ item.network }}'
        replica_capacity: '{{ item.replica_capacity }}'
        replication_role: '{{ item.replication_role }}'
        restore_point_in_time: '{{ item.restore_point_in_time }}'
        sku: '{{ item.sku }}'
        source_server_resource_id: '{{ item.source_server_resource_id }}'
        state: '{{ item.state }}'
        storage: '{{ item.storage }}'
        system_data: '{{ item.system_data }}'
        version: '{{ item.version }}'
  # ── list_by_resource_group ──
  - discovery_id: azure.rdbms_mysql_flexibleservers.list_by_resource_group
    calls:
      - action: list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        identity: '{{ item.identity }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        tags: '{{ item.tags }}'
        type: '{{ item.type }}'
        administrator_login: '{{ item.administrator_login }}'
        administrator_login_password: '{{ item.administrator_login_password }}'
        availability_zone: '{{ item.availability_zone }}'
        backup: '{{ item.backup }}'
        create_mode: '{{ item.create_mode }}'
        data_encryption: '{{ item.data_encryption }}'
        fully_qualified_domain_name: '{{ item.fully_qualified_domain_name }}'
        high_availability: '{{ item.high_availability }}'
        maintenance_window: '{{ item.maintenance_window }}'
        network: '{{ item.network }}'
        replica_capacity: '{{ item.replica_capacity }}'
        replication_role: '{{ item.replication_role }}'
        restore_point_in_time: '{{ item.restore_point_in_time }}'
        sku: '{{ item.sku }}'
        source_server_resource_id: '{{ item.source_server_resource_id }}'
        state: '{{ item.state }}'
        storage: '{{ item.storage }}'
        system_data: '{{ item.system_data }}'
        version: '{{ item.version }}'
  # ── list_by_server ──
  - discovery_id: azure.rdbms_mysql_flexibleservers.list_by_server
    calls:
      - action: list_by_server
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        identity: '{{ item.identity }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        tags: '{{ item.tags }}'
        type: '{{ item.type }}'
        administrator_login: '{{ item.administrator_login }}'
        administrator_login_password: '{{ item.administrator_login_password }}'
        availability_zone: '{{ item.availability_zone }}'
        backup: '{{ item.backup }}'
        create_mode: '{{ item.create_mode }}'
        data_encryption: '{{ item.data_encryption }}'
        fully_qualified_domain_name: '{{ item.fully_qualified_domain_name }}'
        high_availability: '{{ item.high_availability }}'
        maintenance_window: '{{ item.maintenance_window }}'
        network: '{{ item.network }}'
        replica_capacity: '{{ item.replica_capacity }}'
        replication_role: '{{ item.replication_role }}'
        restore_point_in_time: '{{ item.restore_point_in_time }}'
        sku: '{{ item.sku }}'
        source_server_resource_id: '{{ item.source_server_resource_id }}'
        state: '{{ item.state }}'
        storage: '{{ item.storage }}'
        system_data: '{{ item.system_data }}'
        version: '{{ item.version }}'
    # required_params: ['server_name']
