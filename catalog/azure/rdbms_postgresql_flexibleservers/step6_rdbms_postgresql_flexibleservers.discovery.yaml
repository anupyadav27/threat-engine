# Discovery YAML — rdbms_postgresql_flexibleservers (Azure)
# Generated: 2026-02-21T05:02:44Z
# Actions use Azure Python SDK snake_case names (azure-mgmt-*)
version: '1.0'
provider: azure
service: rdbms_postgresql_flexibleservers
services:
  client: rdbms_postgresql_flexibleservers
  module: azure.mgmt.rdbms_postgresql_flexibleservers
discovery:
  # ── list ──
  - discovery_id: azure.rdbms_postgresql_flexibleservers.list
    calls:
      - action: list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        tags: '{{ item.tags }}'
        type: '{{ item.type }}'
        administrator_login: '{{ item.administrator_login }}'
        administrator_login_password: '{{ item.administrator_login_password }}'
        availability_zone: '{{ item.availability_zone }}'
        backup: '{{ item.backup }}'
        create_mode: '{{ item.create_mode }}'
        fully_qualified_domain_name: '{{ item.fully_qualified_domain_name }}'
        high_availability: '{{ item.high_availability }}'
        maintenance_window: '{{ item.maintenance_window }}'
        minor_version: '{{ item.minor_version }}'
        network: '{{ item.network }}'
        point_in_time_utc: '{{ item.point_in_time_utc }}'
        sku: '{{ item.sku }}'
        source_server_resource_id: '{{ item.source_server_resource_id }}'
        state: '{{ item.state }}'
        storage: '{{ item.storage }}'
        system_data: '{{ item.system_data }}'
        version: '{{ item.version }}'
  # ── list_by_resource_group ──
  - discovery_id: azure.rdbms_postgresql_flexibleservers.list_by_resource_group
    calls:
      - action: list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        tags: '{{ item.tags }}'
        type: '{{ item.type }}'
        administrator_login: '{{ item.administrator_login }}'
        administrator_login_password: '{{ item.administrator_login_password }}'
        availability_zone: '{{ item.availability_zone }}'
        backup: '{{ item.backup }}'
        create_mode: '{{ item.create_mode }}'
        fully_qualified_domain_name: '{{ item.fully_qualified_domain_name }}'
        high_availability: '{{ item.high_availability }}'
        maintenance_window: '{{ item.maintenance_window }}'
        minor_version: '{{ item.minor_version }}'
        network: '{{ item.network }}'
        point_in_time_utc: '{{ item.point_in_time_utc }}'
        sku: '{{ item.sku }}'
        source_server_resource_id: '{{ item.source_server_resource_id }}'
        state: '{{ item.state }}'
        storage: '{{ item.storage }}'
        system_data: '{{ item.system_data }}'
        version: '{{ item.version }}'
  # ── list_by_server ──
  - discovery_id: azure.rdbms_postgresql_flexibleservers.list_by_server
    calls:
      - action: list_by_server
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
        end_ip_address: '{{ item.end_ip_address }}'
        start_ip_address: '{{ item.start_ip_address }}'
        system_data: '{{ item.system_data }}'
    # required_params: ['server_name']
