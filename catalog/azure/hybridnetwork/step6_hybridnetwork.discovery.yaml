# Discovery YAML — hybridnetwork (Azure)
# Generated: 2026-02-21T05:02:44Z
# Actions use Azure Python SDK snake_case names (azure-mgmt-*)
version: '1.0'
provider: azure
service: hybridnetwork
services:
  client: hybridnetwork
  module: azure.mgmt.hybridnetwork
discovery:
  # ── list_by_network_function ──
  - discovery_id: azure.hybridnetwork.list_by_network_function
    calls:
      - action: list_by_network_function
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        system_data: '{{ item.system_data }}'
    # required_params: ['network_function_name']
  # ── list_by_publisher ──
  - discovery_id: azure.hybridnetwork.list_by_publisher
    calls:
      - action: list_by_publisher
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        tags: '{{ item.tags }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        system_data: '{{ item.system_data }}'
    # required_params: ['publisher_name']
  # ── list_by_resource_group ──
  - discovery_id: azure.hybridnetwork.list_by_resource_group
    calls:
      - action: list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        tags: '{{ item.tags }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        system_data: '{{ item.system_data }}'
  # ── list_by_subscription ──
  - discovery_id: azure.hybridnetwork.list_by_subscription
    calls:
      - action: list_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        tags: '{{ item.tags }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        system_data: '{{ item.system_data }}'
  # ── Dependent ops ──
  # ── list [dependent] ──
  - discovery_id: azure.hybridnetwork.list
    calls:
      - action: list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
        system_data: '{{ item.system_data }}'
    # required_params: ['publisher_name', 'artifact_store_name']
  # ── list_by_artifact_store [dependent] ──
  - discovery_id: azure.hybridnetwork.list_by_artifact_store
    calls:
      - action: list_by_artifact_store
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        tags: '{{ item.tags }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        system_data: '{{ item.system_data }}'
    # required_params: ['publisher_name', 'artifact_store_name']
  # ── list_by_network_function_definition_group [dependent] ──
  - discovery_id: azure.hybridnetwork.list_by_network_function_definition_group
    calls:
      - action: list_by_network_function_definition_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        tags: '{{ item.tags }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        system_data: '{{ item.system_data }}'
    # required_params: ['publisher_name', 'network_function_definition_group_name']
  # ── list_by_network_service_design_group [dependent] ──
  - discovery_id: azure.hybridnetwork.list_by_network_service_design_group
    calls:
      - action: list_by_network_service_design_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        tags: '{{ item.tags }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
        system_data: '{{ item.system_data }}'
    # required_params: ['publisher_name', 'network_service_design_group_name']
  # ── list_credential [dependent] ──
  - discovery_id: azure.hybridnetwork.list_credential
    calls:
      - action: list_credential
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.credential_type }}'
      item:
        credential_type: '{{ item.credential_type }}'
    # required_params: ['publisher_name', 'artifact_store_name', 'artifact_manifest_name']
