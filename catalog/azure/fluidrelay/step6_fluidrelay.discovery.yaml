# Discovery YAML — fluidrelay (Azure)
# Generated: 2026-02-21T05:02:44Z
# Actions use Azure Python SDK snake_case names (azure-mgmt-*)
version: '1.0'
provider: azure
service: fluidrelay
services:
  client: fluidrelay
  module: azure.mgmt.fluidrelay
discovery:
  # ── list ──
  - discovery_id: azure.fluidrelay.list
    calls:
      - action: list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
  # ── list_by_resource_group ──
  - discovery_id: azure.fluidrelay.list_by_resource_group
    calls:
      - action: list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['resource_group']
  # ── list_by_subscription ──
  - discovery_id: azure.fluidrelay.list_by_subscription
    calls:
      - action: list_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
  # ── Dependent ops ──
  # ── list_by_fluid_relay_servers [dependent] ──
  - discovery_id: azure.fluidrelay.list_by_fluid_relay_servers
    calls:
      - action: list_by_fluid_relay_servers
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['resource_group', 'fluid_relay_server_name']
  # ── list_keys [dependent] ──
  - discovery_id: azure.fluidrelay.list_keys
    calls:
      - action: list_keys
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.key1 }}'
      item:
        key1: '{{ item.key1 }}'
        key2: '{{ item.key2 }}'
    # required_params: ['resource_group', 'fluid_relay_server_name']
