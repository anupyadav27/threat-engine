# Discovery YAML — managedservices (Azure)
# Generated: 2026-02-21T05:02:44Z
# Actions use Azure Python SDK snake_case names (azure-mgmt-*)
version: '1.0'
provider: azure
service: managedservices
services:
  client: managedservices
  module: azure.mgmt.managedservices
discovery:
  # ── list ──
  - discovery_id: azure.managedservices.list
    calls:
      - action: list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['scope']
