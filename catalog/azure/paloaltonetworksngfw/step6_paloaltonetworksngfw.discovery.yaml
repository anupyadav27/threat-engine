# Discovery YAML — paloaltonetworksngfw (Azure)
# Generated: 2026-02-21T05:02:44Z
# Actions use Azure Python SDK snake_case names (azure-mgmt-*)
version: '1.0'
provider: azure
service: paloaltonetworksngfw
services:
  client: paloaltonetworksngfw
  module: azure.mgmt.paloaltonetworksngfw
discovery:
  # ── list ──
  - discovery_id: azure.paloaltonetworksngfw.list
    calls:
      - action: list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        type: '{{ item.type }}'
        audit_comment: '{{ item.audit_comment }}'
        description: '{{ item.description }}'
        prefix_list: '{{ item.prefix_list }}'
        system_data: '{{ item.system_data }}'
    # required_params: ['global_rulestack_name']
  # ── list_app_ids ──
  - discovery_id: azure.paloaltonetworksngfw.list_app_ids
    calls:
      - action: list_app_ids
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        identity: '{{ item.identity }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        status: '{{ item.status }}'
        tags: '{{ item.tags }}'
        type: '{{ item.type }}'
        DestinationArn: '{{ item.DestinationArn }}'
        Enabled: '{{ item.Enabled }}'
        Format: '{{ item.Format }}'
        Level: '{{ item.Level }}'
        PrincipalId: '{{ item.PrincipalId }}'
        Status: '{{ item.Status }}'
        Type: '{{ item.Type }}'
        account_id: '{{ item.account_id }}'
        account_registration_status: '{{ item.account_registration_status }}'
        action: '{{ item.action }}'
        action_type: '{{ item.action_type }}'
        application_insights_connection_string: '{{ item.application_insights_connection_string }}'
        application_insights_resource_id: '{{ item.application_insights_resource_id }}'
        applications: '{{ item.applications }}'
        associated_rulestack: '{{ item.associated_rulestack }}'
        associated_subscriptions: '{{ item.associated_subscriptions }}'
        audit_comment: '{{ item.audit_comment }}'
        category: '{{ item.category }}'
        certificate_self_signed: '{{ item.certificate_self_signed }}'
        certificate_signer_resource_id: '{{ item.certificate_signer_resource_id }}'
        code: '{{ item.code }}'
        credits: '{{ item.credits }}'
        decryption_rule_type: '{{ item.decryption_rule_type }}'
        default_mode: '{{ item.default_mode }}'
        description: '{{ item.description }}'
        destination: '{{ item.destination }}'
        display: '{{ item.display }}'
        dns_settings: '{{ item.dns_settings }}'
        enable_logging: '{{ item.enable_logging }}'
        end_date_for_credits: '{{ item.end_date_for_credits }}'
        fqdn_list: '{{ item.fqdn_list }}'
        free_trial: '{{ item.free_trial }}'
        free_trial_credit_left: '{{ item.free_trial_credit_left }}'
        free_trial_days_left: '{{ item.free_trial_days_left }}'
        front_end_settings: '{{ item.front_end_settings }}'
        health_reason: '{{ item.health_reason }}'
        health_status: '{{ item.health_status }}'
        help_url: '{{ item.help_url }}'
        hub_url: '{{ item.hub_url }}'
        inbound_inspection_certificate: '{{ item.inbound_inspection_certificate }}'
        is_data_action: '{{ item.is_data_action }}'
        is_panorama_managed: '{{ item.is_panorama_managed }}'
        is_strata_cloud_managed: '{{ item.is_strata_cloud_managed }}'
        marketplace_details: '{{ item.marketplace_details }}'
        min_app_id_version: '{{ item.min_app_id_version }}'
        monthly_credit_left: '{{ item.monthly_credit_left }}'
        negate_destination: '{{ item.negate_destination }}'
        negate_source: '{{ item.negate_source }}'
        network_profile: '{{ item.network_profile }}'
        origin: '{{ item.origin }}'
        pan_etag: '{{ item.pan_etag }}'
        pan_location: '{{ item.pan_location }}'
        panorama_config: '{{ item.panorama_config }}'
        panorama_status: '{{ item.panorama_status }}'
        plan_data: '{{ item.plan_data }}'
        prefix_list: '{{ item.prefix_list }}'
        priority: '{{ item.priority }}'
        product_serial: '{{ item.product_serial }}'
        product_sku: '{{ item.product_sku }}'
        protocol: '{{ item.protocol }}'
        protocol_port_list: '{{ item.protocol_port_list }}'
        register_url: '{{ item.register_url }}'
        rule_name: '{{ item.rule_name }}'
        rule_state: '{{ item.rule_state }}'
        scope: '{{ item.scope }}'
        security_services: '{{ item.security_services }}'
        serial_number: '{{ item.serial_number }}'
        source: '{{ item.source }}'
        start_date_for_credits: '{{ item.start_date_for_credits }}'
        strata_cloud_manager_config: '{{ item.strata_cloud_manager_config }}'
        strata_cloud_manager_info: '{{ item.strata_cloud_manager_info }}'
        support_url: '{{ item.support_url }}'
        system_data: '{{ item.system_data }}'
    # required_params: ['local_rulestack_name']
  # ── list_by_firewalls ──
  - discovery_id: azure.paloaltonetworksngfw.list_by_firewalls
    calls:
      - action: list_by_firewalls
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        type: '{{ item.type }}'
        application_insights_connection_string: '{{ item.application_insights_connection_string }}'
        application_insights_resource_id: '{{ item.application_insights_resource_id }}'
        pan_etag: '{{ item.pan_etag }}'
        system_data: '{{ item.system_data }}'
    # required_params: ['firewall_name']
  # ── list_by_local_rulestacks ──
  - discovery_id: azure.paloaltonetworksngfw.list_by_local_rulestacks
    calls:
      - action: list_by_local_rulestacks
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        type: '{{ item.type }}'
        audit_comment: '{{ item.audit_comment }}'
        description: '{{ item.description }}'
        prefix_list: '{{ item.prefix_list }}'
        system_data: '{{ item.system_data }}'
    # required_params: ['local_rulestack_name']
  # ── list_by_resource_group ──
  - discovery_id: azure.paloaltonetworksngfw.list_by_resource_group
    calls:
      - action: list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        identity: '{{ item.identity }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        tags: '{{ item.tags }}'
        type: '{{ item.type }}'
        associated_subscriptions: '{{ item.associated_subscriptions }}'
        default_mode: '{{ item.default_mode }}'
        description: '{{ item.description }}'
        min_app_id_version: '{{ item.min_app_id_version }}'
        pan_etag: '{{ item.pan_etag }}'
        pan_location: '{{ item.pan_location }}'
        scope: '{{ item.scope }}'
        security_services: '{{ item.security_services }}'
        system_data: '{{ item.system_data }}'
  # ── list_by_subscription ──
  - discovery_id: azure.paloaltonetworksngfw.list_by_subscription
    calls:
      - action: list_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        identity: '{{ item.identity }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        tags: '{{ item.tags }}'
        type: '{{ item.type }}'
        associated_subscriptions: '{{ item.associated_subscriptions }}'
        default_mode: '{{ item.default_mode }}'
        description: '{{ item.description }}'
        min_app_id_version: '{{ item.min_app_id_version }}'
        pan_etag: '{{ item.pan_etag }}'
        pan_location: '{{ item.pan_location }}'
        scope: '{{ item.scope }}'
        security_services: '{{ item.security_services }}'
        system_data: '{{ item.system_data }}'
  # ── list_cloud_manager_tenants ──
  - discovery_id: azure.paloaltonetworksngfw.list_cloud_manager_tenants
    calls:
      - action: list_cloud_manager_tenants
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
  # ── list_countries ──
  - discovery_id: azure.paloaltonetworksngfw.list_countries
    calls:
      - action: list_countries
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        code: '{{ item.code }}'
        description: '{{ item.description }}'
    # required_params: ['local_rulestack_name']
  # ── list_firewalls ──
  - discovery_id: azure.paloaltonetworksngfw.list_firewalls
    calls:
      - action: list_firewalls
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['local_rulestack_name']
  # ── list_predefined_url_categories ──
  - discovery_id: azure.paloaltonetworksngfw.list_predefined_url_categories
    calls:
      - action: list_predefined_url_categories
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        name: '{{ item.name }}'
        action: '{{ item.action }}'
    # required_params: ['local_rulestack_name']
  # ── list_product_serial_number_status ──
  - discovery_id: azure.paloaltonetworksngfw.list_product_serial_number_status
    calls:
      - action: list_product_serial_number_status
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.serial_number }}'
      item:
        status: '{{ item.status }}'
        serial_number: '{{ item.serial_number }}'
  # ── list_support_info ──
  - discovery_id: azure.paloaltonetworksngfw.list_support_info
    calls:
      - action: list_support_info
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.product_sku }}'
      item:
        account_id: '{{ item.account_id }}'
        account_registration_status: '{{ item.account_registration_status }}'
        credits: '{{ item.credits }}'
        end_date_for_credits: '{{ item.end_date_for_credits }}'
        free_trial: '{{ item.free_trial }}'
        free_trial_credit_left: '{{ item.free_trial_credit_left }}'
        free_trial_days_left: '{{ item.free_trial_days_left }}'
        help_url: '{{ item.help_url }}'
        hub_url: '{{ item.hub_url }}'
        monthly_credit_left: '{{ item.monthly_credit_left }}'
        product_serial: '{{ item.product_serial }}'
        product_sku: '{{ item.product_sku }}'
        register_url: '{{ item.register_url }}'
        start_date_for_credits: '{{ item.start_date_for_credits }}'
        support_url: '{{ item.support_url }}'
  # ── Dependent ops ──
  # ── get_change_log [dependent] ──
  - discovery_id: azure.paloaltonetworksngfw.get_change_log
    calls:
      - action: get_change_log
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        changes: '{{ response.changes }}'
        last_committed: '{{ response.last_committed }}'
        last_modified: '{{ response.last_modified }}'
    # required_params: ['local_rulestack_name']
  # ── get_counters [dependent] ──
  - discovery_id: azure.paloaltonetworksngfw.get_counters
    calls:
      - action: get_counters
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        app_seen: '{{ response.app_seen }}'
        firewall_name: '{{ response.firewall_name }}'
        hit_count: '{{ response.hit_count }}'
        last_updated_timestamp: '{{ response.last_updated_timestamp }}'
        priority: '{{ response.priority }}'
        request_timestamp: '{{ response.request_timestamp }}'
        rule_list_name: '{{ response.rule_list_name }}'
        rule_name: '{{ response.rule_name }}'
        rule_stack_name: '{{ response.rule_stack_name }}'
        timestamp: '{{ response.timestamp }}'
    # required_params: ['global_rulestack_name', 'priority']
  # ── get_global_rulestack [dependent] ──
  - discovery_id: azure.paloaltonetworksngfw.get_global_rulestack
    calls:
      - action: get_global_rulestack
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        azure_id: '{{ response.azure_id }}'
    # required_params: ['firewall_name']
  # ── get_log_profile [dependent] ──
  - discovery_id: azure.paloaltonetworksngfw.get_log_profile
    calls:
      - action: get_log_profile
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        application_insights: '{{ response.application_insights }}'
        common_destination: '{{ response.common_destination }}'
        decrypt_log_destination: '{{ response.decrypt_log_destination }}'
        log_option: '{{ response.log_option }}'
        log_type: '{{ response.log_type }}'
        threat_log_destination: '{{ response.threat_log_destination }}'
        traffic_log_destination: '{{ response.traffic_log_destination }}'
    # required_params: ['firewall_name']
  # ── get_support_info [dependent] ──
  - discovery_id: azure.paloaltonetworksngfw.get_support_info
    calls:
      - action: get_support_info
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        account_id: '{{ response.account_id }}'
        account_registered: '{{ response.account_registered }}'
        free_trial: '{{ response.free_trial }}'
        free_trial_credit_left: '{{ response.free_trial_credit_left }}'
        free_trial_days_left: '{{ response.free_trial_days_left }}'
        help_url: '{{ response.help_url }}'
        product_serial: '{{ response.product_serial }}'
        product_sku: '{{ response.product_sku }}'
        register_url: '{{ response.register_url }}'
        support_url: '{{ response.support_url }}'
        user_domain_supported: '{{ response.user_domain_supported }}'
        user_registered: '{{ response.user_registered }}'
    # required_params: ['local_rulestack_name']
  # ── list_advanced_security_objects [dependent] ──
  - discovery_id: azure.paloaltonetworksngfw.list_advanced_security_objects
    calls:
      - action: list_advanced_security_objects
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['local_rulestack_name', 'type']
  # ── list_security_services [dependent] ──
  - discovery_id: azure.paloaltonetworksngfw.list_security_services
    calls:
      - action: list_security_services
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['local_rulestack_name', 'type']
