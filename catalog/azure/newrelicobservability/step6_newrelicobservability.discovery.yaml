# Discovery YAML — newrelicobservability (Azure)
# Generated: 2026-02-21T05:02:44Z
# Actions use Azure Python SDK snake_case names (azure-mgmt-*)
version: '1.0'
provider: azure
service: newrelicobservability
services:
  client: newrelicobservability
  module: azure.mgmt.newrelicobservability
discovery:
  # ── list_by_new_relic_monitor_resource ──
  - discovery_id: azure.newrelicobservability.list_by_new_relic_monitor_resource
    calls:
      - action: list_by_new_relic_monitor_resource
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        type: '{{ item.type }}'
        log_rules: '{{ item.log_rules }}'
        metric_rules: '{{ item.metric_rules }}'
        system_data: '{{ item.system_data }}'
    # required_params: ['monitor_name']
  # ── list_by_resource_group ──
  - discovery_id: azure.newrelicobservability.list_by_resource_group
    calls:
      - action: list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        identity: '{{ item.identity }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        tags: '{{ item.tags }}'
        type: '{{ item.type }}'
        account_creation_source: '{{ item.account_creation_source }}'
        liftr_resource_category: '{{ item.liftr_resource_category }}'
        liftr_resource_preference: '{{ item.liftr_resource_preference }}'
        marketplace_subscription_id: '{{ item.marketplace_subscription_id }}'
        marketplace_subscription_status: '{{ item.marketplace_subscription_status }}'
        monitoring_status: '{{ item.monitoring_status }}'
        new_relic_account_properties: '{{ item.new_relic_account_properties }}'
        org_creation_source: '{{ item.org_creation_source }}'
        plan_data: '{{ item.plan_data }}'
        saa_s_azure_subscription_status: '{{ item.saa_s_azure_subscription_status }}'
        subscription_state: '{{ item.subscription_state }}'
        system_data: '{{ item.system_data }}'
        user_info: '{{ item.user_info }}'
  # ── list_by_subscription ──
  - discovery_id: azure.newrelicobservability.list_by_subscription
    calls:
      - action: list_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        identity: '{{ item.identity }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        provisioning_state: '{{ item.provisioning_state }}'
        tags: '{{ item.tags }}'
        type: '{{ item.type }}'
        account_creation_source: '{{ item.account_creation_source }}'
        liftr_resource_category: '{{ item.liftr_resource_category }}'
        liftr_resource_preference: '{{ item.liftr_resource_preference }}'
        marketplace_subscription_id: '{{ item.marketplace_subscription_id }}'
        marketplace_subscription_status: '{{ item.marketplace_subscription_status }}'
        monitoring_status: '{{ item.monitoring_status }}'
        new_relic_account_properties: '{{ item.new_relic_account_properties }}'
        org_creation_source: '{{ item.org_creation_source }}'
        plan_data: '{{ item.plan_data }}'
        saa_s_azure_subscription_status: '{{ item.saa_s_azure_subscription_status }}'
        subscription_state: '{{ item.subscription_state }}'
        system_data: '{{ item.system_data }}'
        user_info: '{{ item.user_info }}'
  # ── list_linked_resources ──
  - discovery_id: azure.newrelicobservability.list_linked_resources
    calls:
      - action: list_linked_resources
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
    # required_params: ['monitor_name']
  # ── list_monitored_resources ──
  - discovery_id: azure.newrelicobservability.list_monitored_resources
    calls:
      - action: list_monitored_resources
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        reason_for_logs_status: '{{ item.reason_for_logs_status }}'
        reason_for_metrics_status: '{{ item.reason_for_metrics_status }}'
        sending_logs: '{{ item.sending_logs }}'
        sending_metrics: '{{ item.sending_metrics }}'
    # required_params: ['monitor_name']
  # ── Dependent ops ──
  # ── get_metric_rules [dependent] ──
  - discovery_id: azure.newrelicobservability.get_metric_rules
    calls:
      - action: get_metric_rules
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        filtering_tags: '{{ response.filtering_tags }}'
        send_metrics: '{{ response.send_metrics }}'
        user_email: '{{ response.user_email }}'
    # required_params: ['monitor_name', 'request']
  # ── get_metric_status [dependent] ──
  - discovery_id: azure.newrelicobservability.get_metric_status
    calls:
      - action: get_metric_status
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        azure_resource_ids: '{{ response.azure_resource_ids }}'
    # required_params: ['monitor_name', 'request']
  # ── list [dependent] ──
  - discovery_id: azure.newrelicobservability.list
    calls:
      - action: list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
        billing_source: '{{ item.billing_source }}'
        organization_id: '{{ item.organization_id }}'
        organization_name: '{{ item.organization_name }}'
        system_data: '{{ item.system_data }}'
    # required_params: ['user_email', 'location']
  # ── list_app_services [dependent] ──
  - discovery_id: azure.newrelicobservability.list_app_services
    calls:
      - action: list_app_services
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        agent_status: '{{ item.agent_status }}'
        agent_version: '{{ item.agent_version }}'
        azure_resource_id: '{{ item.azure_resource_id }}'
    # required_params: ['monitor_name', 'request']
  # ── list_hosts [dependent] ──
  - discovery_id: azure.newrelicobservability.list_hosts
    calls:
      - action: list_hosts
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        agent_status: '{{ item.agent_status }}'
        agent_version: '{{ item.agent_version }}'
        vm_id: '{{ item.vm_id }}'
    # required_params: ['monitor_name', 'request']
