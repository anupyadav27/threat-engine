# Discovery YAML — datashare (Azure)
# Generated: 2026-02-21T05:02:44Z
# Actions use Azure Python SDK snake_case names (azure-mgmt-*)
version: '1.0'
provider: azure
service: datashare
services:
  client: datashare
  module: azure.mgmt.datashare
discovery:
  # ── list ──
  - discovery_id: azure.datashare.list
    calls:
      - action: list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
  # ── list_by_account ──
  - discovery_id: azure.datashare.list_by_account
    calls:
      - action: list_by_account
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['account_name']
  # ── list_by_resource_group ──
  - discovery_id: azure.datashare.list_by_resource_group
    calls:
      - action: list_by_resource_group
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
  # ── list_by_subscription ──
  - discovery_id: azure.datashare.list_by_subscription
    calls:
      - action: list_by_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
  # ── list_invitations ──
  - discovery_id: azure.datashare.list_invitations
    calls:
      - action: list_invitations
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
  # ── Dependent ops ──
  # ── get_by_share [dependent] ──
  - discovery_id: azure.datashare.get_by_share
    calls:
      - action: get_by_share
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        type: '{{ response.type }}'
        consumer_email: '{{ response.consumer_email }}'
        consumer_name: '{{ response.consumer_name }}'
        consumer_tenant_name: '{{ response.consumer_tenant_name }}'
        created_at: '{{ response.created_at }}'
        expiration_date: '{{ response.expiration_date }}'
        provider_email: '{{ response.provider_email }}'
        provider_name: '{{ response.provider_name }}'
        share_subscription_object_id: '{{ response.share_subscription_object_id }}'
        share_subscription_status: '{{ response.share_subscription_status }}'
        shared_at: '{{ response.shared_at }}'
        system_data: '{{ response.system_data }}'
    # required_params: ['account_name', 'share_name', 'provider_share_subscription_id']
  # ── list_by_share [dependent] ──
  - discovery_id: azure.datashare.list_by_share
    calls:
      - action: list_by_share
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['account_name', 'share_name']
  # ── list_by_share_subscription [dependent] ──
  - discovery_id: azure.datashare.list_by_share_subscription
    calls:
      - action: list_by_share_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['account_name', 'share_subscription_name']
  # ── list_source_share_synchronization_settings [dependent] ──
  - discovery_id: azure.datashare.list_source_share_synchronization_settings
    calls:
      - action: list_source_share_synchronization_settings
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['account_name', 'share_subscription_name']
  # ── list_synchronization_details [dependent] ──
  - discovery_id: azure.datashare.list_synchronization_details
    calls:
      - action: list_synchronization_details
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['account_name', 'share_name', 'share_synchronization']
  # ── list_synchronizations [dependent] ──
  - discovery_id: azure.datashare.list_synchronizations
    calls:
      - action: list_synchronizations
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        value: '{{ item.value }}'
    # required_params: ['account_name', 'share_name']
