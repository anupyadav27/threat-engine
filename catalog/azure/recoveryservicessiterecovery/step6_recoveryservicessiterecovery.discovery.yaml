# Discovery YAML — recoveryservicessiterecovery (Azure)
# Generated: 2026-02-21T05:02:44Z
# Actions use Azure Python SDK snake_case names (azure-mgmt-*)
version: '1.0'
provider: azure
service: recoveryservicessiterecovery
services:
  client: recoveryservicessiterecovery
  module: azure.mgmt.recoveryservicessiterecovery
discovery:
  # ── list ──
  - discovery_id: azure.recoveryservicessiterecovery.list
    calls:
      - action: list
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
  # ── list_by_replication_fabrics ──
  - discovery_id: azure.recoveryservicessiterecovery.list_by_replication_fabrics
    calls:
      - action: list_by_replication_fabrics
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
    # required_params: ['fabric_name']
  # ── Dependent ops ──
  # ── get_operation_results [dependent] ──
  - discovery_id: azure.recoveryservicessiterecovery.get_operation_results
    calls:
      - action: get_operation_results
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        id: '{{ response.id }}'
        name: '{{ response.name }}'
        type: '{{ response.type }}'
        properties: '{{ response.properties }}'
    # required_params: ['resource_name', 'fabric_name', 'protection_container_name', 'replication_protection_cluster_name', 'job_id']
  # ── list_by_replication_migration_items [dependent] ──
  - discovery_id: azure.recoveryservicessiterecovery.list_by_replication_migration_items
    calls:
      - action: list_by_replication_migration_items
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
    # required_params: ['fabric_name', 'protection_container_name', 'migration_item_name']
  # ── list_by_replication_networks [dependent] ──
  - discovery_id: azure.recoveryservicessiterecovery.list_by_replication_networks
    calls:
      - action: list_by_replication_networks
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
    # required_params: ['fabric_name', 'network_name']
  # ── list_by_replication_protected_items [dependent] ──
  - discovery_id: azure.recoveryservicessiterecovery.list_by_replication_protected_items
    calls:
      - action: list_by_replication_protected_items
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
    # required_params: ['fabric_name', 'protection_container_name', 'replicated_protected_item_name']
  # ── list_by_replication_protection_cluster [dependent] ──
  - discovery_id: azure.recoveryservicessiterecovery.list_by_replication_protection_cluster
    calls:
      - action: list_by_replication_protection_cluster
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
    # required_params: ['resource_name', 'fabric_name', 'protection_container_name', 'replication_protection_cluster_name']
  # ── list_by_replication_protection_containers [dependent] ──
  - discovery_id: azure.recoveryservicessiterecovery.list_by_replication_protection_containers
    calls:
      - action: list_by_replication_protection_containers
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
    # required_params: ['fabric_name', 'protection_container_name']
  # ── list_by_replication_storage_classifications [dependent] ──
  - discovery_id: azure.recoveryservicessiterecovery.list_by_replication_storage_classifications
    calls:
      - action: list_by_replication_storage_classifications
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.value }}'
      item:
        id: '{{ item.id }}'
        location: '{{ item.location }}'
        name: '{{ item.name }}'
        type: '{{ item.type }}'
        properties: '{{ item.properties }}'
    # required_params: ['fabric_name', 'storage_classification_name']
