# Discovery YAML — resource_search (OCI)
# Generated: 2026-02-21T04:45:39Z
version: '1.0'
provider: oci
service: resource_search
services:
  client: resource_search
  module: oci.resource_search
discovery:
  # ── get_resource_type [dependent] ──
  - discovery_id: oci.resource_search.get_resource_type
    calls:
      - action: get_resource_type
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        compartment_id: '{{ response.data.compartment_id }}'
        defined_tags: '{{ response.data.defined_tags }}'
        freeform_tags: '{{ response.data.freeform_tags }}'
        name: '{{ response.data.name }}'
        time_created: '{{ response.data.time_created }}'
        ocid: '{{ response.data.ocid }}'
        status: '{{ response.data.status }}'
    # required_params: ['ResourceTypeId']
  # ── list_resource_types [dependent] ──
  - discovery_id: oci.resource_search.list_resource_types
    calls:
      - action: list_resource_types
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.data }}'
      item:
        compartment_id: '{{ item.compartment_id }}'
        defined_tags: '{{ item.defined_tags }}'
        freeform_tags: '{{ item.freeform_tags }}'
        name: '{{ item.name }}'
        time_created: '{{ item.time_created }}'
        ocid: '{{ item.ocid }}'
        status: '{{ item.status }}'
    # required_params: ['compartmentId']
