# Discovery YAML — certificates (OCI)
# Generated: 2026-02-21T04:45:38Z
version: '1.0'
provider: oci
service: certificates
services:
  client: certificates
  module: oci.certificates
discovery:
  # ── get_ca_bundle [dependent] ──
  - discovery_id: oci.certificates.get_ca_bundle
    calls:
      - action: get_ca_bundle
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        compartment_id: '{{ response.data.compartment_id }}'
        defined_tags: '{{ response.data.defined_tags }}'
        freeform_tags: '{{ response.data.freeform_tags }}'
        name: '{{ response.data.name }}'
        time_created: '{{ response.data.time_created }}'
        ocid: '{{ response.data.ocid }}'
        status: '{{ response.data.status }}'
    # required_params: ['CaBundleId']
  # ── get_certificate_authority_bundle [dependent] ──
  - discovery_id: oci.certificates.get_certificate_authority_bundle
    calls:
      - action: get_certificate_authority_bundle
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        compartment_id: '{{ response.data.compartment_id }}'
        defined_tags: '{{ response.data.defined_tags }}'
        freeform_tags: '{{ response.data.freeform_tags }}'
        name: '{{ response.data.name }}'
        time_created: '{{ response.data.time_created }}'
        ocid: '{{ response.data.ocid }}'
        status: '{{ response.data.status }}'
    # required_params: ['CertificateAuthorityBundleId']
  # ── get_certificate_bundle [dependent] ──
  - discovery_id: oci.certificates.get_certificate_bundle
    calls:
      - action: get_certificate_bundle
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        compartment_id: '{{ response.data.compartment_id }}'
        defined_tags: '{{ response.data.defined_tags }}'
        freeform_tags: '{{ response.data.freeform_tags }}'
        name: '{{ response.data.name }}'
        time_created: '{{ response.data.time_created }}'
        ocid: '{{ response.data.ocid }}'
        status: '{{ response.data.status }}'
    # required_params: ['CertificateBundleId']
  # ── list_certificate_authority_bundle_versions [dependent] ──
  - discovery_id: oci.certificates.list_certificate_authority_bundle_versions
    calls:
      - action: list_certificate_authority_bundle_versions
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.data }}'
      item:
        compartment_id: '{{ item.compartment_id }}'
        defined_tags: '{{ item.defined_tags }}'
        freeform_tags: '{{ item.freeform_tags }}'
        name: '{{ item.name }}'
        time_created: '{{ item.time_created }}'
        ocid: '{{ item.ocid }}'
        status: '{{ item.status }}'
    # required_params: ['compartmentId']
  # ── list_certificate_bundle_versions [dependent] ──
  - discovery_id: oci.certificates.list_certificate_bundle_versions
    calls:
      - action: list_certificate_bundle_versions
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.data }}'
      item:
        compartment_id: '{{ item.compartment_id }}'
        defined_tags: '{{ item.defined_tags }}'
        freeform_tags: '{{ item.freeform_tags }}'
        name: '{{ item.name }}'
        time_created: '{{ item.time_created }}'
        ocid: '{{ item.ocid }}'
        status: '{{ item.status }}'
    # required_params: ['compartmentId']
