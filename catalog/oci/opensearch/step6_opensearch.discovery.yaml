# Discovery YAML — opensearch (OCI)
# Generated: 2026-02-21T04:45:39Z
version: '1.0'
provider: oci
service: opensearch
services:
  client: opensearch
  module: oci.opensearch
discovery:
  # ── get_opensearch_cluster_pipeline [dependent] ──
  - discovery_id: oci.opensearch.get_opensearch_cluster_pipeline
    calls:
      - action: get_opensearch_cluster_pipeline
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        compartment_id: '{{ response.data.compartment_id }}'
        defined_tags: '{{ response.data.defined_tags }}'
        freeform_tags: '{{ response.data.freeform_tags }}'
        name: '{{ response.data.name }}'
        time_created: '{{ response.data.time_created }}'
        ocid: '{{ response.data.ocid }}'
        status: '{{ response.data.status }}'
    # required_params: ['OpensearchClusterPipelineId']
  # ── list_opensearch_cluster_pipelines [dependent] ──
  - discovery_id: oci.opensearch.list_opensearch_cluster_pipelines
    calls:
      - action: list_opensearch_cluster_pipelines
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.data }}'
      item:
        compartment_id: '{{ item.compartment_id }}'
        defined_tags: '{{ item.defined_tags }}'
        freeform_tags: '{{ item.freeform_tags }}'
        name: '{{ item.name }}'
        time_created: '{{ item.time_created }}'
        ocid: '{{ item.ocid }}'
        status: '{{ item.status }}'
    # required_params: ['compartmentId']
