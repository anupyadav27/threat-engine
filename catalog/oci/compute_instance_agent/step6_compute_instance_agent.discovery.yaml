# Discovery YAML — compute_instance_agent (OCI)
# Generated: 2026-02-21T04:45:38Z
version: '1.0'
provider: oci
service: compute_instance_agent
services:
  client: compute_instance_agent
  module: oci.compute_instance_agent
discovery:
  # ── list_instanceagent_available_plugins [dependent] ──
  - discovery_id: oci.compute_instance_agent.list_instanceagent_available_plugins
    calls:
      - action: list_instanceagent_available_plugins
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.data }}'
      item:
        compartment_id: '{{ item.compartment_id }}'
        defined_tags: '{{ item.defined_tags }}'
        freeform_tags: '{{ item.freeform_tags }}'
        name: '{{ item.name }}'
        time_created: '{{ item.time_created }}'
        ocid: '{{ item.ocid }}'
        status: '{{ item.status }}'
    # required_params: ['compartmentId']
