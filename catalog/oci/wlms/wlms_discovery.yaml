version: '1.0'
provider: oci
service: wlms
services:
  client: wlms
  module: oci.wlms
discovery:
- discovery_id: oci.wlms.get_configuration
  calls:
  - action: get_configuration
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
checks: []
