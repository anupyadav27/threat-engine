# Discovery YAML — announcements_service (OCI)
# Generated: 2026-02-21T04:45:38Z
version: '1.0'
provider: oci
service: announcements_service
services:
  client: announcements_service
  module: oci.announcements_service
discovery:
  # ── list_services [dependent] ──
  - discovery_id: oci.announcements_service.list_services
    calls:
      - action: list_services
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.data }}'
      item:
        compartment_id: '{{ item.compartment_id }}'
        defined_tags: '{{ item.defined_tags }}'
        freeform_tags: '{{ item.freeform_tags }}'
        name: '{{ item.name }}'
        time_created: '{{ item.time_created }}'
        ocid: '{{ item.ocid }}'
        status: '{{ item.status }}'
    # required_params: ['compartmentId']
