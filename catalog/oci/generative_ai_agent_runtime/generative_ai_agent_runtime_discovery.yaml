version: '1.0'
provider: oci
service: generative_ai_agent_runtime
services:
  client: generative_ai_agent_runtime
  module: oci.generative_ai_agent_runtime
discovery:
- discovery_id: oci.generative_ai_agent_runtime.get_session
  calls:
  - action: get_session
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
checks: []
