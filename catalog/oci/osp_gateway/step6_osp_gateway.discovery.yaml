# Discovery YAML — osp_gateway (OCI)
# Generated: 2026-02-21T04:45:39Z
version: '1.0'
provider: oci
service: osp_gateway
services:
  client: osp_gateway
  module: oci.osp_gateway
discovery:
  # ── get_subscription [dependent] ──
  - discovery_id: oci.osp_gateway.get_subscription
    calls:
      - action: get_subscription
        save_as: response
        on_error: continue
    emit:
      as: item
      item:
        compartment_id: '{{ response.data.compartment_id }}'
        defined_tags: '{{ response.data.defined_tags }}'
        freeform_tags: '{{ response.data.freeform_tags }}'
        name: '{{ response.data.name }}'
        time_created: '{{ response.data.time_created }}'
        ocid: '{{ response.data.ocid }}'
        status: '{{ response.data.status }}'
    # required_params: ['SubscriptionId']
  # ── list_subscriptions [dependent] ──
  - discovery_id: oci.osp_gateway.list_subscriptions
    calls:
      - action: list_subscriptions
        save_as: response
        on_error: continue
    emit:
      as: item
      items_for: '{{ response.data }}'
      item:
        compartment_id: '{{ item.compartment_id }}'
        defined_tags: '{{ item.defined_tags }}'
        freeform_tags: '{{ item.freeform_tags }}'
        name: '{{ item.name }}'
        time_created: '{{ item.time_created }}'
        ocid: '{{ item.ocid }}'
        status: '{{ item.status }}'
    # required_params: ['compartmentId']
