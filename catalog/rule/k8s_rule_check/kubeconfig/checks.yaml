version: '1.0'
provider: k8s
service: kubeconfig
checks:
- rule_id: k8s.kubeconfig.file.permissions_configured
  for_each: k8s.kubeconfig.list_kubeconfigs
  severity: MEDIUM
  conditions:
    var: item.permissions
    op: not_contains
    value: '*'
