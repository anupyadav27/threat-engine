version: '1.0'
provider: k8s
service: inventory
checks:
- rule_id: k8s.inventory.review.scheduled
  for_each: k8s.inventory.list_pod_for_all_namespaces
  severity: LOW
  conditions:
    var: item.metadata.name
    op: exists
    value: null
