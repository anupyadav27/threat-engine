version: '1.0'
provider: k8s
service: admission
checks:
- rule_id: k8s.admission.control.windows_hostprocess_restriction_configured
  for_each: k8s.admission.list_validating_webhook_configuration
  severity: MEDIUM
  conditions:
    var: item.enabled
    op: is_true
