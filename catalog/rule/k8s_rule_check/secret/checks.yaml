version: '1.0'
provider: k8s
service: secret
checks:
- rule_id: k8s.secret.type_configured
  for_each: k8s.secret.list_secret_for_all_namespaces
  severity: MEDIUM
  conditions:
    var: item.metadata
    op: is_true
