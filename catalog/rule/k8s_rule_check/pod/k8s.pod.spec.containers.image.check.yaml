# Auto-generated check rule stub for k8s.pod.spec.containers.image
# Source: generate_check_rules_for_stubs.py
# Category: config
# TODO: Review and update conditions to match actual resource fields
version: '1.0'
provider: k8s
service: pod
checks:
- rule_id: k8s.pod.spec.containers.image
  for_each: k8s.apps.list_deployments
  severity: MEDIUM
  conditions:
    var: item.image
    op: exists
    value: null
