version: '1.0'
provider: k8s
service: authentication
checks:
- rule_id: k8s.authentication.user.client_certificate_disabled
  for_each: k8s.authentication.list_authentications
  severity: MEDIUM
  conditions:
    var: item.ssl_enabled
    op: is_true
