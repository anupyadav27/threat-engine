version: '1.0'
provider: k8s
service: threat
checks:
- rule_id: k8s.falco.daemonset.status_configured
  for_each: k8s.falco.list_falcos
  severity: MEDIUM
  conditions:
    var: item.logging_enabled
    op: is_true
