version: '1.0'
provider: azure
service: netappfiles
checks:
- rule_id: azure.netappfiles.encryption.key_source_cmk_check
  for_each: azure.netapp.netappresourcequotalimits.netappresourcequotalimits_list
  severity: MEDIUM
  conditions:
    var: item.identity
    op: is_true
