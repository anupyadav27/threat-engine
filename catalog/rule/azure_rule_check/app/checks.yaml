version: '1.0'
provider: azure
service: app
checks:
- rule_id: azure.app.function.not_publicly_accessible_enforced
  for_each: azure.app.operations.operations_list
  severity: MEDIUM
  conditions:
    var: item.identity
    op: is_true
