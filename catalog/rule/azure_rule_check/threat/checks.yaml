version: '1.0'
provider: azure
service: threat
checks:
- rule_id: azure.securitycenter.assessments.periodic_configured
  for_each: azure.security.locations.list
  severity: MEDIUM
  conditions:
    var: item.type
    op: is_true
