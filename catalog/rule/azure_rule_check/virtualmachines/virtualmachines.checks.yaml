version: '1.0'
provider: azure
service: virtualmachines
checks:
- rule_id: azure.virtualmachines.managed.by.automation
  for_each: azure.compute.virtualmachines.list
  severity: MEDIUM
  conditions:
    var: item.managed_by_automation
    op: is_true
    value: null
