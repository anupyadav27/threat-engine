version: '1.0'
provider: azure
service: devops
checks:
- rule_id: azure.devops.project.no_secrets_in_variables_enforced
  for_each: azure.devops.list_devopss
  severity: MEDIUM
  conditions:
    var: item.logging_enabled
    op: is_true
