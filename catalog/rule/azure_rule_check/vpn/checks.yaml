version: '1.0'
provider: azure
service: vpn
checks:
- rule_id: azure.vpn.virtual_network_gateway.split_tunnel_configured
  for_each: azure.vpn.list_vpns
  severity: MEDIUM
  conditions:
    var: item.status
    op: equals
    value: Active
