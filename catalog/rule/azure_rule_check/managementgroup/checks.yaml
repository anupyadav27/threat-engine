version: '1.0'
provider: azure
service: managementgroup
checks:
- rule_id: azure.managementgroup.subscription.entra_tenant_transfer_restriction_configured
  for_each: azure.managementgroup.list_managementgroups
  severity: MEDIUM
  conditions:
    var: item.status
    op: equals
    value: Active
