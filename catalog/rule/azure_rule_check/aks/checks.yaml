version: '1.0'
provider: azure
service: aks
checks:
- rule_id: azure.aks.cluster.rbac_enabled
  for_each: azure.aks.list_akss
  severity: MEDIUM
  conditions:
    var: item.logging_enabled
    op: is_true
