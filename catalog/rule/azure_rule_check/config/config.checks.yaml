version: '1.0'
provider: azure
service: config
checks:
- rule_id: azure.config.policy.cis_7_16_needs_development
  for_each: azure.appconfiguration.configurationstores.configurationstores_list
  severity: MEDIUM
  conditions:
    var: item.properties.mode
    op: equals
    value: Microsoft.Management/managementGroups
