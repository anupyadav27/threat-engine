version: '1.0'
provider: azure
service: mariadb
checks:
- rule_id: azure.mariadb.server.geo_redundant_backup
  for_each: azure.mariadb.servers.servers_list
  severity: MEDIUM
  conditions:
    var: item.StorageProfile.GeoRedundantBackup
    op: equals
    value: Enabled
