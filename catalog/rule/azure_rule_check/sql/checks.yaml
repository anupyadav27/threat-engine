version: '1.0'
provider: azure
service: sql
checks:
- rule_id: azure.sql.connection.credentials_in_keyvault_configured
  for_each: azure.sql.managedinstanceprivateendpointconnections.list_by_managed_instance
  severity: MEDIUM
  conditions:
    var: item.identity
    op: is_true
