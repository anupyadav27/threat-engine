version: '1.0'
provider: azure
service: notification
checks:
- rule_id: azure.notification.hub.topic_subscription_configured
  for_each: azure.notificationhubs.notificationhubs.list
  severity: MEDIUM
  conditions:
    var: item.properties.topicSubscriptions
    op: not_empty
    value: null
