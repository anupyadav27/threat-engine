version: '1.0'
provider: azure
service: cr
checks:
- rule_id: azure.cr.image.vulnerability_assessment_enabled
  for_each: azure.containerregistry.scopemaps.list
  severity: MEDIUM
  conditions:
    var: item.status
    op: equals
    value: Active
