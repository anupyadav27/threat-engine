version: '1.0'
provider: azure
service: sentinel
checks:
- rule_id: azure.sentinel.data_connector.is_configured
  for_each: azure.operationalinsights.workspaces.workspaces_list
  severity: MEDIUM
  conditions:
    var: item.identity
    op: is_true
