# =============================================================================
# IEDS Tier 1 — Kubernetes always-public resource types
# =============================================================================

rules:

  - rule_id: IEDS-T1-K8S-001
    tier: 1
    csp: k8s
    resource_type: networking.k8s.io.ingress
    origin_type: internet
    title: "Kubernetes Ingress is internet-accessible by default"
    severity: high
