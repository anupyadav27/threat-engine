# Auto-generated: check rules for IBM vsi from test-rulecheck_python_code
version: '1.0'
provider: ibm
service: vsi
checks:
- rule_id: ibm.vsi.activity.bare_metal_change
  for_each: ibm.vsi.list_instances
  severity: HIGH
  conditions:
    var: item.BareMetal
    op: is_false
- rule_id: ibm.vsi.activity.vsi_action
  for_each: ibm.vsi.list_instances
  severity: MEDIUM
  conditions:
    var: item.Status
    op: not_equals
    value: active
