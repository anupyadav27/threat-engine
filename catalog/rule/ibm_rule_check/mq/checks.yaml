version: '1.0'
provider: ibm
service: mq
checks:
- rule_id: ibm.mq.broker.logging_enabled
  for_each: ibm.mq.list-queue-managers
  severity: MEDIUM
  conditions:
    var: item.status
    op: equals
    value: running
