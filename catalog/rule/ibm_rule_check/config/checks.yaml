version: '1.0'
provider: ibm
service: config
checks:
- rule_id: ibm.config.recorder.all_regions_enabled
  for_each: ibm.resource_controller.list-reclamations
  severity: MEDIUM
  conditions:
    var: item.status
    op: equals
    value: Active
