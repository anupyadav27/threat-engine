version: '1.0'
provider: ibm
service: logdna
checks:
- rule_id: ibm.logdna.ingestion_key.retention_days_configured
  for_each: ibm.security_advisor.list-findings
  severity: MEDIUM
  conditions:
    var: item.enabled
    op: is_true
