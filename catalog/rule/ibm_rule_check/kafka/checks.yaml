version: '1.0'
provider: ibm
service: kafka
checks:
- rule_id: ibm.kafka.cluster.in_transit_encryption_enabled
  for_each: ibm.kafka.list-topics
  severity: MEDIUM
  conditions:
    var: item.is_ssl_enabled
    op: is_true
    value: ''
