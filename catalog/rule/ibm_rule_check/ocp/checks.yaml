version: '1.0'
provider: ibm
service: ocp
checks:
- rule_id: ibm.ocp.vm.check_shareable_disks_configured
  for_each: ibm.ocp.list-virtual-machines
  severity: MEDIUM
  conditions:
    var: item.running
    op: is_true
    value: ''
