# Auto-generated: check rules for IBM cos from test-rulecheck_python_code
version: '1.0'
provider: ibm
service: cos
checks:
- rule_id: ibm.cos.activity.bucket_change
  for_each: ibm.cos.list_buckets
  severity: HIGH
  conditions:
    var: item.ActivityTracking
    op: exists
