# Auto-generated: check rules for IBM event_streams from test-rulecheck_python_code
version: '1.0'
provider: ibm
service: event_streams
checks:
- rule_id: ibm.event_streams.instance.encryption_at_rest_enabled
  for_each: ibm.event_streams.list_instances
  severity: HIGH
  conditions:
    var: item.encryption_at_rest
    op: is_true
- rule_id: ibm.event_streams.instance.private_endpoint_only
  for_each: ibm.event_streams.list_instances
  severity: HIGH
  conditions:
    var: item.PrivateEndpointOnly
    op: is_true
- rule_id: ibm.event_streams.instance.schema_registry_enabled
  for_each: ibm.event_streams.list_instances
  severity: LOW
  conditions:
    var: item.schema_registry_enabled
    op: is_true
