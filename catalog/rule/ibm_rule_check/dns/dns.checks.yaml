# Auto-generated: check rules for IBM dns from test-rulecheck_python_code
version: '1.0'
provider: ibm
service: dns
checks:
- rule_id: ibm.dns.zone.no_wildcard_records
  for_each: ibm.dns.zones.list
  severity: MEDIUM
  conditions:
    var: item.records[].type
    op: not_contains
    value: wildcard
- rule_id: ibm.dns.zone.permitted_networks_configured
  for_each: ibm.dns.zones.list
  severity: HIGH
  conditions:
    var: item.permitted_networks
    op: not_empty
- rule_id: ibm.dns.zone.private_zone_only
  for_each: ibm.dns.list_zones
  severity: MEDIUM
  conditions:
    var: item.Visibility
    op: equals
    value: private
