version: '1.0'
provider: ibm
service: log_analysis
checks:
- rule_id: ibm.log_analysis.log_metric_filter.alarm_configured
  for_each: ibm.log_analysis.list-logs
  severity: MEDIUM
  conditions:
    var: item.enabled
    op: is_true
