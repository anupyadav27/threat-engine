# Auto-generated check rule stub for ibm.is.vpc.load_balancer.pool.session_persistence
# Source: generate_check_rules_for_stubs.py
# Category: config
# TODO: Review and update conditions to match actual resource fields
version: '1.0'
provider: ibm
service: is
checks:
- rule_id: ibm.is.vpc.load_balancer.pool.session_persistence
  for_each: ibm.is.list
  severity: MEDIUM
  conditions:
    var: item.session_persistence
    op: exists
    value: null
