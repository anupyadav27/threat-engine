version: '1.0'
provider: ibm
service: containerregistry
checks:
- rule_id: ibm.containerregistry.image.security_advisory_enabled
  for_each: ibm.container_registry.list-images
  severity: MEDIUM
  conditions:
    var: item.enabled
    op: is_true
