# Auto-generated: check rules for IBM secrets from test-rulecheck_python_code
version: '1.0'
provider: ibm
service: secrets
checks:
- rule_id: ibm.secrets.activity.secrets_manager_change
  for_each: ibm.secrets.list_secrets
  severity: HIGH
  conditions:
    all:
    - var: item.ChangeTrackingEnabled
      op: is_true
    - var: item.ActivityEventsEnabled
      op: is_true
