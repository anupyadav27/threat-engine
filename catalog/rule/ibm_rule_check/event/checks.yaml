version: '1.0'
provider: ibm
service: event
checks:
- rule_id: ibm.event.notifications.topic_subscription_configured
  for_each: ibm.event_notifications.list-topics
  severity: MEDIUM
  conditions:
    var: item.enabled
    op: is_true
