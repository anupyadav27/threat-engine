version: '1.0'
provider: ibm
service: code
checks:
- rule_id: ibm.code.engine.managed_updates_enabled
  for_each: ibm.code_engine.list-apps
  severity: MEDIUM
  conditions:
    var: item.enabled
    op: is_true
