version: '1.0'
provider: oci
service: events
checks:
- rule_id: oci.events.rule.enabled
  for_each: oci.events.list_rules
  severity: MEDIUM
  conditions:
    var: item.status
    op: equals
    value: Active
