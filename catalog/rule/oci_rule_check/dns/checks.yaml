version: '1.0'
provider: oci
service: dns
checks:
- rule_id: oci.dns.zone.protection_enabled
  for_each: oci.dns.list_zone_transfer_servers
  severity: MEDIUM
  conditions:
    var: item.status
    op: equals
    value: Active
