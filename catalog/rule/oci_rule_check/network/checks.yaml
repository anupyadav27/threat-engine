version: '1.0'
provider: oci
service: network
checks:
- rule_id: oci.network.security_list.restrict_default_configured
  for_each: oci.core.get_security_list
  severity: MEDIUM
  conditions:
    var: item.status
    op: equals
    value: Active
