version: '1.0'
provider: oci
service: containerregistry
checks:
- rule_id: oci.containerregistry.not.publicly_accessible_restricted
  for_each: oci.artifacts.list_container_image_signatures
  severity: MEDIUM
  conditions:
    var: item.status
    op: equals
    value: Active
