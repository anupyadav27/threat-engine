# Auto-generated check rule stub for oci.identity.user.password_policy.enforce_change
# Source: generate_check_rules_for_stubs.py
# Category: config
# TODO: Review and update conditions to match actual resource fields
version: '1.0'
provider: oci
service: identity
checks:
- rule_id: oci.identity.user.password_policy.enforce_change
  for_each: oci.identity.list_users
  severity: MEDIUM
  conditions:
    var: item.enforce_change
    op: exists
    value: null
