version: '1.0'
provider: aws
service: securityhub
checks:
- rule_id: aws.securityhub.findings.periodic_assessment_configured
  for_each: aws.securityhub.get_findings
  severity: MEDIUM
  conditions:
    var: item.Id
    op: is_true
