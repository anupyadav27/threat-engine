version: '1.0'
provider: aws
service: vpn
checks:
- rule_id: aws.vpn.client_endpoint.split_tunnel_configured
  for_each: aws.vpn.list_vpns
  severity: MEDIUM
  conditions:
    var: item.status
    op: equals
    value: Active
