version: '1.0'
provider: aws
service: guardduty
checks:
- rule_id: aws.guardduty.detector.status_configured
  for_each: aws.guardduty.get_member_detectors
  severity: MEDIUM
  conditions:
    var: item.AccountId
    op: is_true
