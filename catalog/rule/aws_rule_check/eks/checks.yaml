version: '1.0'
provider: aws
service: eks
checks:
- rule_id: aws.eks.cluster.rbac_managed_configured
  for_each: aws.eks.describe_cluster
  severity: MEDIUM
  conditions:
    var: item.status
    op: equals
    value: Active
