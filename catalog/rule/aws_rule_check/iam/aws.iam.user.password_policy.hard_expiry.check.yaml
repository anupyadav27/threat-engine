# Auto-generated check rule stub for aws.iam.user.password_policy.hard_expiry
# Source: generate_check_rules_for_stubs.py
# Category: config
# TODO: Review and update conditions to match actual resource fields
version: '1.0'
provider: aws
service: iam
checks:
- rule_id: aws.iam.user.password_policy.hard_expiry
  for_each: aws.chime.get_user
  severity: MEDIUM
  conditions:
    var: item.hard_expiry
    op: exists
    value: null
