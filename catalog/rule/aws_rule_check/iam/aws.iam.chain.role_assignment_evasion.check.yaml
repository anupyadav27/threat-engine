# Auto-generated check rule stub for aws.iam.chain.role_assignment_evasion
# Source: generate_check_rules_for_stubs.py
# Category: chain_ciem
# TODO: Review and update conditions to match actual resource fields
version: '1.0'
provider: aws
service: iam
checks:
- rule_id: aws.iam.chain.role_assignment_evasion
  check_type: log_correlation
  severity: HIGH
  description: Detect role assignment evasion via cloudtrail event correlation
  conditions:
    all:
    - op: equals
      field: source_type
      value: cloudtrail
    - op: equals
      field: outcome
      value: SUCCESS
  sequence:
  - event: initial
    conditions:
    - op: equals
      field: source_type
      value: cloudtrail
  - event: followup
    conditions:
    - op: equals
      field: actor.principal
      value: '{{initial.actor.principal}}'
