# Auto-generated check rule stub for aws.iam.account.allow_root_mfa
# Source: generate_check_rules_for_stubs.py
# Category: config
# TODO: Review and update conditions to match actual resource fields
version: '1.0'
provider: aws
service: iam
checks:
- rule_id: aws.iam.account.allow_root_mfa
  for_each: aws.iam.get_account_authorization_details
  severity: MEDIUM
  conditions:
    var: item.allow_root_mfa
    op: exists
    value: null
