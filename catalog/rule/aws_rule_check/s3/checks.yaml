version: '1.0'
provider: aws
service: s3
checks:
- rule_id: aws.s3.bucket.anonymous_auth_disabled
  for_each: aws.s3.list_buckets
  severity: MEDIUM
  conditions:
    var: item.BucketArn
    op: is_true
