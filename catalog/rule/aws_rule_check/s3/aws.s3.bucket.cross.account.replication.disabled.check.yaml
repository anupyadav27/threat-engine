# Auto-generated check rule stub for aws.s3.bucket.cross.account.replication.disabled
# Source: generate_check_rules_for_stubs.py
# Category: config
# TODO: Review and update conditions to match actual resource fields
version: '1.0'
provider: aws
service: s3
checks:
- rule_id: aws.s3.bucket.cross.account.replication.disabled
  for_each: aws.s3.list_buckets
  severity: MEDIUM
  conditions:
    var: item.disabled
    op: exists
    value: null
