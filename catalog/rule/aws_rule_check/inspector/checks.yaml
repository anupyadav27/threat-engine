version: '1.0'
provider: aws
service: inspector
checks:
- rule_id: aws.inspector.assessment.enabled
  for_each: aws.inspector.list_assessment_runs
  severity: MEDIUM
  conditions:
    var: item.assessmentRuns
    op: is_true
