# DOCKER — CIEM Detection Rules
# Real-time authentication & access event analysis
# Log source: dockerd journal, /var/log/docker.log

tech_type: docker
category: container
log_source: dockerd journal, /var/log/docker.log

rules:

  - rule_id: tciem.docker.auditing_is_configured_for_the_docker_daemon
    title: "Ensure auditing is configured for the Docker daemon"
    severity: medium
    mitre_technique: T1562
    mitre_tactic: defense_evasion
    cis_control: '1.1.3'
    cis_section: '1'
    log_source: "dockerd journal, /var/log/docker.log"
    detection:
      event_type: audit_event
      filters:
        change_type: security_relevant
      evidence_fields: [source_ip, username, timestamp, action]
    response: alert
    remediation: >
      

