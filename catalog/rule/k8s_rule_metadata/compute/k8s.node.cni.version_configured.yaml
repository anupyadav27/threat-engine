rule_id: k8s.node.cni.version_configured
service: compute
provider: k8s
check_type: resource
severity: medium
title: 'PARTIAL: k8s.kubernetes.node.cni.version ||| Can verify CNI version on self-managed
  nodes via node agent, but cannot enforce or verify for managed ser'
description: Validates that k8s compute cni has version configured configured according
  to security best practices.
rationale: Proper version configured configuration reduces security risk and ensures
  compliance with cis_controls_v8.
domain: container_and_kubernetes_security
subcategory: configuration
posture_category: container_posture
remediation: Review and configure version configured for the compute cni according
  to K8S security best practices and your organisational policy.
references:
- https://kubernetes.io/docs/tasks/debug/debug-cluster/audit/
source: csv_generated
generated_by: step1_build_metadata
compliance:
- cis_oci_4.3.1
