rule_id: k8s.job.audit.create_kube_system
rule_type: ciem
title: 'Job: Created in kube-system Namespace'
scope: job.audit
domain: container_and_kubernetes_security
subcategory: container_security
severity: high
service: job
resource: audit
requirement: Create Kube System
description: Kubernetes Job created in kube-system namespace — ad-hoc batch workload
  executed in sensitive system namespace; may be used for reconnaissance or cleanup.
rationale: 'Adversaries deploy containers into environments using Docker APIs or Kubernetes
  control planes to facilitate execution or evade defenses by running malicious images
  outside standard deployment processes. Deployed containers can host C2 infrastructure,
  cryptomining workloads, or data exfiltration tools. Detected via: container create
  + start by non-admin principals, images not on an allowlist or using ''latest''
  tag, privileged mode or host namespace mount at runtime, and unauthenticated Docker
  API access.'
remediation: '1. Audit the created pod or container for malicious images or startup
  commands.

  2. Enforce Binary Authorization or Cosign image signing via admission webhooks.

  3. Apply Pod Security Standards (Restricted profile) to prevent privileged execution.

  4. Restrict pod creation to specific service accounts in production namespaces.

  5. Enable runtime threat detection (Falco, Tetragon) for anomalous container behavior.

  '
references:
- https://attack.mitre.org/techniques/T1610/
- https://kubernetes.io/docs/concepts/security/pod-security-admission/
compliance:
- nist_800_171_r2_3_4_8
threat_category: execution
mitre_tactics:
- execution
mitre_techniques:
- T1610
risk_score: 78
action_category: execute
source: default
metadata_source: auto_generated
generated_by: k8s_ciem_generator
