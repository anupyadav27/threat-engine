# Auto-generated check rule stub for gcp.iam.project.allow_service_account_keys
# Source: generate_check_rules_for_stubs.py
# Category: config
# TODO: Review and update conditions to match actual resource fields
version: '1.0'
provider: gcp
service: iam
checks:
- rule_id: gcp.iam.project.allow_service_account_keys
  for_each: gcp.iam.projects.locations.workloadIdentityPools.list
  severity: MEDIUM
  conditions:
    var: item.allow_service_account_keys
    op: exists
    value: null
