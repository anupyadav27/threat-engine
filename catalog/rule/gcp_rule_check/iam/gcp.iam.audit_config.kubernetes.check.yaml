# Auto-generated check rule stub for gcp.iam.audit_config.kubernetes
# Source: generate_check_rules_for_stubs.py
# Category: config
# TODO: Review and update conditions to match actual resource fields
version: '1.0'
provider: gcp
service: iam
checks:
- rule_id: gcp.iam.audit_config.kubernetes
  for_each: gcp.iam.organizations.roles.list
  severity: MEDIUM
  conditions:
    var: item.kubernetes
    op: exists
    value: null
