# Auto-generated check rule stub for gcp.iam.project.password_policy.minimum_length
# Source: generate_check_rules_for_stubs.py
# Category: config
# TODO: Review and update conditions to match actual resource fields
version: '1.0'
provider: gcp
service: iam
checks:
- rule_id: gcp.iam.project.password_policy.minimum_length
  for_each: gcp.iam.projects.locations.workloadIdentityPools.list
  severity: MEDIUM
  conditions:
    var: item.minimum_length
    op: exists
    value: null
