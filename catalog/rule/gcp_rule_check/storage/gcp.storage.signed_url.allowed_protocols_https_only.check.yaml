# Auto-generated check rule stub for gcp.storage.signed_url.allowed_protocols_https_only
# Source: generate_check_rules_for_stubs.py
# Category: config
# TODO: Review and update conditions to match actual resource fields
version: '1.0'
provider: gcp
service: storage
checks:
- rule_id: gcp.storage.signed_url.allowed_protocols_https_only
  for_each: gcp.storage.buckets.list
  severity: MEDIUM
  conditions:
    var: item.allowed_protocols_https_only
    op: exists
    value: null
