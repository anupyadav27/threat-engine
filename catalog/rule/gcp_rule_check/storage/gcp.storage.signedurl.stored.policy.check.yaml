# Auto-generated check rule stub for gcp.storage.signedurl.stored.policy
# Source: generate_check_rules_for_stubs.py
# Category: config
# TODO: Review and update conditions to match actual resource fields
version: '1.0'
provider: gcp
service: storage
checks:
- rule_id: gcp.storage.signedurl.stored.policy
  for_each: gcp.storage.buckets.list
  severity: MEDIUM
  conditions:
    var: item.policy
    op: exists
    value: null
