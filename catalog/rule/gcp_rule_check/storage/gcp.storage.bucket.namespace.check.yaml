# Auto-generated check rule stub for gcp.storage.bucket.namespace
# Source: generate_check_rules_for_stubs.py
# Category: config
# TODO: Review and update conditions to match actual resource fields
version: '1.0'
provider: gcp
service: storage
checks:
- rule_id: gcp.storage.bucket.namespace
  for_each: gcp.storage.buckets.list
  severity: MEDIUM
  conditions:
    var: item.namespace
    op: exists
    value: null
