# Auto-generated check rule stub for gcp.logging.sink.kubernetes_audit
# Source: generate_check_rules_for_stubs.py
# Category: config
# TODO: Review and update conditions to match actual resource fields
version: '1.0'
provider: gcp
service: logging
checks:
- rule_id: gcp.logging.sink.kubernetes_audit
  for_each: gcp.logging.sinks.list
  severity: MEDIUM
  conditions:
    var: item.kubernetes_audit
    op: exists
    value: null
