# Auto-generated check rule stub for gcp.kms.cryptokey.key_management
# Source: generate_check_rules_for_stubs.py
# Category: config
# TODO: Review and update conditions to match actual resource fields
version: '1.0'
provider: gcp
service: kms
checks:
- rule_id: gcp.kms.cryptokey.key_management
  for_each: gcp.cloudkms.projects.locations.keyRings.cryptoKeys.list
  severity: MEDIUM
  conditions:
    var: item.key_management
    op: exists
    value: null
