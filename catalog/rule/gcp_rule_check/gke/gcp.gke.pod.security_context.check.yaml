# Auto-generated check rule stub for gcp.gke.pod.security_context
# Source: generate_check_rules_for_stubs.py
# Category: config
# TODO: Review and update conditions to match actual resource fields
version: '1.0'
provider: gcp
service: gke
checks:
- rule_id: gcp.gke.pod.security_context
  for_each: gcp.container.clusterroles.list
  severity: MEDIUM
  conditions:
    var: item.security_context
    op: exists
    value: null
