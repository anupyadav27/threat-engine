# Auto-generated check rule stub for gcp.gke.cluster.kubernetes_dashboard_disabled
# Source: generate_check_rules_for_stubs.py
# Category: config
# TODO: Review and update conditions to match actual resource fields
version: '1.0'
provider: gcp
service: gke
checks:
- rule_id: gcp.gke.cluster.kubernetes_dashboard_disabled
  for_each: gcp.container.clusterroles.list
  severity: MEDIUM
  conditions:
    var: item.kubernetes_dashboard_disabled
    op: exists
    value: null
