# Auto-generated check rule stub for gcp.gke.node_pool.shielded_nodes
# Source: generate_check_rules_for_stubs.py
# Category: config
# TODO: Review and update conditions to match actual resource fields
version: '1.0'
provider: gcp
service: gke
checks:
- rule_id: gcp.gke.node_pool.shielded_nodes
  for_each: gcp.gke.describe_node_pools
  severity: MEDIUM
  conditions:
    var: item.shielded_nodes
    op: exists
    value: null
