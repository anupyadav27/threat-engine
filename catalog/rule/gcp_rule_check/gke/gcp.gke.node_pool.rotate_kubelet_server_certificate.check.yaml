# Auto-generated check rule stub for gcp.gke.node_pool.rotate_kubelet_server_certificate
# Source: generate_check_rules_for_stubs.py
# Category: config
# TODO: Review and update conditions to match actual resource fields
version: '1.0'
provider: gcp
service: gke
checks:
- rule_id: gcp.gke.node_pool.rotate_kubelet_server_certificate
  for_each: gcp.gke.describe_node_pools
  severity: MEDIUM
  conditions:
    var: item.rotate_kubelet_server_certificate
    op: exists
    value: null
