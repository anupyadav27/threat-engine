version: '1.0'
provider: gcp
service: cloud_security_scanner
checks:
- rule_id: gcp.cloud_security_scanner.scan_config.exists
  for_each: gcp.websecurityscanner.projects.scanConfigs.list
  severity: MEDIUM
  conditions:
    var: item.ignoreHttpStatusErrors
    op: equals
    value: Active
