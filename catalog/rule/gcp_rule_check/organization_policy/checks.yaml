version: '1.0'
provider: gcp
service: organization_policy
checks:
- rule_id: gcp.organization_policy.constraint_configured
  for_each: gcp.orgpolicy.folders.constraints.list
  severity: MEDIUM
  conditions:
    var: item.booleanConstraint
    op: is_true
