version: '1.0'
provider: gcp
service: vpn
checks:
- rule_id: gcp.vpn.ha_vpn_gateway.split_tunnel_configured
  for_each: gcp.compute.list_computes
  severity: MEDIUM
  conditions:
    var: item.status
    op: equals
    value: Active
