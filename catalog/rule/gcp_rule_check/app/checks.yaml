version: '1.0'
provider: gcp
service: app
checks:
- rule_id: gcp.app.engine.managed_updates_enabled
  for_each: gcp.appengine.apps.authorizedCertificates.list
  severity: MEDIUM
  conditions:
    var: item.certificateRawData
    op: is_true
