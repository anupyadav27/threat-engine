version: '1.0'
provider: gcp
service: artifactregistry
checks:
- rule_id: gcp.artifactregistry.container.analysis_enabled
  for_each: gcp.artifactregistry.projects.locations.list
  severity: MEDIUM
  conditions:
    var: item.displayName
    op: is_true
