version: '1.0'
provider: gcp
service: gcr
checks:
- rule_id: gcp.gcr.container.scanning_enabled
  for_each: gcp.gcr.artifactregistry.list_repositories
  severity: MEDIUM
  conditions:
    var: item.vulnerabilityScanning.enablementConfig
    op: equals
    value: INHERITED
