# Auto-generated check rule stub for gcp.compute.instance.antivirus_osconfig
# Source: generate_check_rules_for_stubs.py
# Category: config
# TODO: Review and update conditions to match actual resource fields
version: '1.0'
provider: gcp
service: compute
checks:
- rule_id: gcp.compute.instance.antivirus_osconfig
  for_each: gcp.compute.instanceGroupManagers.list
  severity: MEDIUM
  conditions:
    var: item.antivirus_osconfig
    op: exists
    value: null
