# Auto-generated check rule stub for gcp.compute.instance.snapshot_schedule
# Source: generate_check_rules_for_stubs.py
# Category: config
# TODO: Review and update conditions to match actual resource fields
version: '1.0'
provider: gcp
service: compute
checks:
- rule_id: gcp.compute.instance.snapshot_schedule
  for_each: gcp.compute.instanceGroupManagers.list
  severity: MEDIUM
  conditions:
    var: item.snapshot_schedule
    op: exists
    value: null
