# Auto-generated check rule stub for gcp.compute.instance.os_config.time_sync
# Source: generate_check_rules_for_stubs.py
# Category: config
# TODO: Review and update conditions to match actual resource fields
version: '1.0'
provider: gcp
service: compute
checks:
- rule_id: gcp.compute.instance.os_config.time_sync
  for_each: gcp.compute.instanceGroupManagers.list
  severity: MEDIUM
  conditions:
    var: item.time_sync
    op: exists
    value: null
