# Auto-generated check rule stub for gcp.compute.disk.encryption_default
# Source: generate_check_rules_for_stubs.py
# Category: config
# TODO: Review and update conditions to match actual resource fields
version: '1.0'
provider: gcp
service: compute
checks:
- rule_id: gcp.compute.disk.encryption_default
  for_each: gcp.compute.disks.list
  severity: MEDIUM
  conditions:
    var: item.encryption_default
    op: exists
    value: null
