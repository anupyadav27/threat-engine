# DS Service - Check Metadata
# This file contains titles, descriptions, and severity for all checks
# The main YAML file only contains the logic

version: '1.0'
service: ds
checks:
  aws.directoryservice.resource.supported_mfa_radius_enabled:
    title: 'DIRECTORYSERVICE resource: Supported Mfa Radius Enabled'
    severity: critical
  aws.directoryservice.resource.directoryservice_directory_log_forwarding_enabled:
    title: 'DIRECTORYSERVICE resource: Directoryservice Directory Log Forwarding Enabled'
    severity: medium
