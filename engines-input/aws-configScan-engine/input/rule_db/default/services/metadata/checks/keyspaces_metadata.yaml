# KEYSPACES Service - Check Metadata
# This file contains titles, descriptions, and severity for all checks
# The main YAML file only contains the logic

version: '1.0'
service: keyspaces
checks:
  aws.keyspaces.resource.keyspace_security_configuration_configured:
    title: 'KEYSPACES resource: Keyspace Security Configuration Configured'
    severity: medium
  aws.keyspaces.resource.keyspace_encryption_at_rest_and_in_transit_configured:
    title: 'KEYSPACES resource: Keyspace Encryption At Rest And In Transit Configured'
    severity: high
