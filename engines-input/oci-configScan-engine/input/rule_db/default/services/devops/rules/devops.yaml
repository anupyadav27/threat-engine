devops:
  version: '1.0'
  provider: oci
  service: devops
  scope: regional
  discovery: []
  checks:
  - check_id: oci.devops.project.app_version_artifact_encrypted
    title: 'OCI DEVOPS Project: App Version Artifact Encrypted'
    severity: medium
    for_each: list_projects
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.devops.project.app_version_immutable_required
    title: 'OCI DEVOPS Project: App Version Immutable Required'
    severity: low
    for_each: list_projects
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.devops.project.project_no_public_admin_roles_blocked
    title: 'OCI DEVOPS Project: Project No Public Admin Roles Blocked'
    severity: critical
    for_each: list_projects
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.devops.project.project_required_guardrail_policies_attached
    title: 'OCI DEVOPS Project: Project Required Guardrail Policies Attached'
    severity: low
    for_each: list_projects
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.devops.project.project_security_services_mandatory_enabled
    title: 'OCI DEVOPS Project: Project Security Services Mandatory Enabled'
    severity: low
    for_each: list_projects
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.devops.project.project_logging_enabled
    title: 'OCI DEVOPS Project: Project Logging Enabled'
    severity: medium
    for_each: list_projects
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.devops.project.no_secrets_in_variables
    title: 'OCI DEVOPS Project: No Secrets In Variables'
    severity: high
    for_each: list_projects
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.devops.project.source_repo_url_no_sensitive_credentials
    title: 'OCI DEVOPS Project: Source Repo Url No Sensitive Credentials'
    severity: high
    for_each: list_projects
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.devops.project.project_artifact_encryption
    title: 'OCI DEVOPS Project: Project Artifact Encryption'
    severity: high
    for_each: list_projects
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.devops.project.project_envvar_awscred
    title: 'OCI DEVOPS Project: Project Envvar Awscred'
    severity: low
    for_each: list_projects
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.devops.project.project_object_storage_logs_encrypted
    title: 'OCI DEVOPS Project: Project Object Storage Logs Encrypted'
    severity: medium
    for_each: list_projects
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.devops.project.project_older_90_days
    title: 'OCI DEVOPS Project: Project Older 90 Days'
    severity: low
    for_each: list_projects
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.devops.project.project_source_repo_url
    title: 'OCI DEVOPS Project: Project Source Repo Url'
    severity: low
    for_each: list_projects
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.devops.project.project_user_controlled_buildspec
    title: 'OCI DEVOPS Project: Project User Controlled Buildspec'
    severity: medium
    for_each: list_projects
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.devops.repository.report_group_export_encrypted
    title: 'OCI DEVOPS Repository: Report Group Export Encrypted'
    severity: medium
    for_each: list_repositorys
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
