apiVersion: v1
kind: Service
metadata:
  name: cspm-backend
  namespace: cspm
  labels:
    app: cspm
    component: django-backend
spec:
  type: LoadBalancer
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: cspm
    component: django-backend

