{
  "filename": "types_should_be_named_in_pascalcase_metadata.json",
  "rule_id": "types_named_pascalcase",
  "title": "Types should be named in PascalCase",
  "description": "Types should be named in PascalCaseAnalyze your codeconsistency-identifiablemaintainabilityCode SmellconventionWhy is this an issue?More InfoShared naming conventions allow teams to collaborate efficiently.This rule raises an issue when a type name is not PascalCased.For example, the classesclass my_class {...} class SOMEName42 {...}should be renamed toclass MyClass {...} class SomeName42 {...}ExceptionsThe rule ignores types marked withComImportAttributeorInterfaceTypeAttribute.To reduce noise, two consecutive upper case characters are allowed unless they form the full type name. So,MyXClassis compliant, butXCis not.The rule allows having'_'characters in class names inside test projects: in that case, each word separated by'_'should be PascalCased.class Some_Name___42 {...} // Compliant i",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Security",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Security risk",
  "tags": [
    "security",
    "bug",
    ".net"
  ],
  "examples": {
    "noncompliant": [
      "// Noncompliant: snake_case naming\nclass my_class  // Noncompliant\n{\n    public void do_something() { }\n}\n\n// Noncompliant: ALL CAPS naming\nclass SOMEName42  // Noncompliant\n{\n}\n\n// Noncompliant: lowercase naming\nclass customer  // Noncompliant\n{\n}\n\n// Noncompliant: Mixed casing\ninterface iService  // Noncompliant\n{\n}"
    ],
    "compliant": [
      "// Compliant: PascalCase naming\nclass MyClass\n{\n    public void DoSomething() { }\n}\n\nclass SomeName42\n{\n}\n\nclass Customer\n{\n}\n\ninterface IService\n{\n}"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/csharp/RSPEC-101"
  ],
  "logic": {
    "node_types": ["ClassDeclaration", "InterfaceDeclaration"],
    "checks": [
      {
        "type": "custom_function",
        "function": "check_type_pascalcase_naming"
      }
    ],
    "target_ast_nodes": ["ClassDeclaration", "InterfaceDeclaration"],
    "detection_conditions": "Check if type names follow PascalCase convention: first letter uppercase, no underscores (except test classes), no consecutive uppercase beyond 2 chars",
    "triggering_conditions": {
      "operator": "OR",
      "conditions": [
        "Type name starts with lowercase letter",
        "Type name contains underscore (non-test context)",
        "Type name is all uppercase (more than 2 chars)",
        "Interface doesn't start with I followed by uppercase"
      ]
    },
    "exclusion_conditions": {
      "exclude_patterns": [
        "^\\s*//",
        "\\[Test\\]",
        "\\[Fact\\]",
        "\\[ComImport\\]",
        "\\[InterfaceType\\]"
      ]
    },
    "never_trigger_on": [
      "Comments describing naming patterns",
      "Test classes with [Test] or [Fact] attributes",
      "Types marked with [ComImport] or [InterfaceType]",
      "Properly PascalCased type names",
      "Short acronyms (2 chars like IO, UI) in names"
    ],
    "reasoning": "Consistent naming conventions improve code readability and maintainability. PascalCase is the standard C# convention for type names. Two consecutive uppercase letters are allowed unless they form the entire type name.",
    "validation_examples": {
      "should_trigger": [
        "class my_class { }",
        "class myClass { }",
        "class ALLCAPS { }",
        "interface service { }"
      ],
      "should_not_trigger": [
        "class MyClass { }",
        "class IOStream { }",
        "interface IService { }",
        "// class my_class comment"
      ]
    }
  }
}