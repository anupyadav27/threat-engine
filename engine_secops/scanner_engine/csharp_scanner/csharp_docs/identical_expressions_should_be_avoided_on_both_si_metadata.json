{
  "filename": "identical_expressions_should_be_avoided_on_both_si_metadata.json",
  "rule_id": "identical_expressions_avoided_both",
  "title": "Identical expressions should be avoided on both sides of operators",
  "description": "Using the same value on both sides of certain operators is a code defect. In the case of logical operators, it is either a copy/paste error and, therefore, a bug, or it is simply duplicated code and should be simplified. For bitwise operators and most binary mathematical operators, having the same value on both sides of an operator yields predictable results and should be simplified as well to avoid further code defects.",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Security",
  "recommendation": "Refactor to eliminate the flagged pattern.",
  "impact": "Security risk",
  "tags": [
    "security",
    "bug",
    ".net"
  ],
  "examples": {
    "noncompliant": [
      "if (a == a) // Noncompliant\n{\n    DoSomething();\n}\n\nint result = x / x; // Noncompliant\nbool same = b || b; // Noncompliant\nint value = num - num; // Noncompliant"
    ],
    "compliant": [
      "if (a == b) // Compliant\n{\n    DoSomething();\n}\n\nint result = x / y; // Compliant\nbool same = b || c; // Compliant\nint value = num - previousNum; // Compliant"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/csharp/RSPEC-1764"
  ],
  "logic": {
    "target_ast_nodes": ["Assignment", "Expression"],
    "node_types": ["Assignment", "Expression"],
    "detection_conditions": "Detect binary operators with identical operands on both sides (e.g., a == a, x / x, b || b) excluding valid patterns like squaring (x * x)",
    "triggering_conditions": "(Left operand equals right operand) AND (Operator is suspicious: ==, !=, /, -, ||, &&, |, &, ^) AND (NOT multiplication operator) AND (NOT in excluded contexts)",
    "exclusion_conditions": [
      "Multiplication operator (x * x is valid for squaring)",
      "Assignment operators (=, +=, -=, etc.)",
      "Comments and documentation",
      "Logging statements",
      "Test code"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": ["source"],
        "pattern": "\\b(\\w+)\\s*(==|!=|/|-|\\|\\||&&|\\||&|\\^)\\s*\\1\\b",
        "exclude_patterns": [
          "\\b(\\w+)\\s*\\*\\s*\\1\\b",
          "^\\s*//",
          "^\\s*/\\*",
          "Console\\.",
          "Logger\\.",
          "Debug\\.",
          "\\[Test"
        ]
      }
    ],
    "never_trigger_on": [
      "Multiplication operator (x * x for squaring)",
      "Assignment operators (=, +=, -=, etc.)",
      "Comments (// or /*)",
      "Logging statements (Console.*, Logger.*, Debug.*)",
      "Unit tests ([Test])",
      "String interpolation or formatting"
    ],
    "reasoning": "This rule detects identical expressions on both sides of binary operators, which is usually a bug or code smell. Pattern uses regex backreference (\\1) to match identical operands. Targets suspicious operators (==, !=, /, -, ||, &&, |, &, ^) but excludes multiplication (*) since x * x is valid for squaring. Excludes comments, logging, and test code. AST-first approach targeting Assignment and Expression nodes.",
    "validation_examples": {
      "should_trigger": [
        "if (a == a)",
        "int result = x / x;",
        "bool same = b || b;",
        "int value = num - num;"
      ],
      "should_not_trigger": [
        "if (a == b)",
        "int result = x / y;",
        "bool same = b || c;",
        "return x * x; // squaring"
      ]
    }
  }
}