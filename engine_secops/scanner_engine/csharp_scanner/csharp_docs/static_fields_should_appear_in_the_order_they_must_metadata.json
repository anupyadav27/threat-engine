{
  "filename": "static_fields_should_appear_in_the_order_they_must_metadata.json",
  "rule_id": "static_fields_appear_order",
  "title": "Static fields should appear in the order they must be initialized",
  "description": "Static fields should appear in the order they must be initializedAnalyze your codeintentionality-logicalreliabilityBugWhy is this an issue?Static field initializers are executed in the order in which they appear in the class from top to bottom. Thus, placing a static field in a class above the field or fields required for its initialization will yield unexpected results.Noncompliant code exampleclass MyClass { public static int X = Y; // Noncompliant; Y at this time is still assigned default(int), i.e. 0 public static int Y = 42; }Compliant solutionclass MyClass { public static int Y = 42; public static int X = Y; }orclass MyClass { public static int X; public static int Y = 42; static MyClass() { X = Y; } }Available In:Catch issues on the fly,in your IDEDetect issues in your GitHub, Azure",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Security",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Security risk",
  "tags": [
    "security",
    "bug",
    ".net"
  ],
  "examples": {
    "noncompliant": [
      "class MyClass\n{\n  public static int X = Y; // Noncompliant; Y at this time is still assigned default(int), i.e. 0\n  public static int Y = 42;\n}"
    ],
    "compliant": [
      "class MyClass\n{\n  public static int Y = 42;\n  public static int X = Y;\n}"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/csharp/RSPEC-3263"
  ],
  "logic": {
    "node_types": ["ClassDeclaration"],
    "checks": [
      {
        "type": "regex_match", 
        "property_path": ["source"],
        "pattern": "static\\s+\\w+\\s+\\w+\\s*=\\s*[A-Z][a-zA-Z]*\\s*;",
        "exclude_patterns": [],
        "message": "Static field may reference another field that is not yet initialized - check field declaration order"
      }
    ],
    "never_trigger_on": [
      "Comments (// or /* */)",
      "Unit tests ([Test], [Fact])",
      "Static fields initialized with numeric literals",
      "Static fields initialized with string literals",
      "Static fields initialized with new keyword",
      "Static fields initialized with true/false/null"
    ],
    "reasoning": "Static field initializers execute top-to-bottom. A field referencing another field declared below it will get the default value (0, null) instead of the expected value. This detects static fields initialized with identifiers starting with uppercase (likely other field/property references).",
    "validation_examples": {
      "should_trigger": [
        "public static int X = Y;",
        "public static string First = Second;"
      ],
      "should_not_trigger": [
        "public static int X = 42;",
        "public static string S = \"hello\";",
        "public static object O = new object();"
      ]
    }
  }
}