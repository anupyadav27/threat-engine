{
  "filename": "collection_sizes_and_array_length_comparisons_shou_metadata.json",
  "rule_id": "collection_sizes_array_length",
  "title": "Collection sizes and array length comparisons should make sense",
  "description": "Collection sizes and array length comparisons should make senseAnalyze your codeintentionality-clearreliabilityBugconfusingWhy is this an issue?The size of a collection and the length of an array are always greater than or equal to zero. Testing it doesn’t make sense, since the result is alwaystrue.if(collection.Count >= 0){...} // Noncompliant: always true if(array.Length >= 0){...} // Noncompliant: always trueSimilarly testing that it is less than zero will always returnfalse.if(enumerable.Count() < 0){...} // Noncompliant: always falseFix the code to properly check for emptiness if it was the intent, or remove the redundant code to keep the current behavior.Available In:Catch issues on the fly,in your IDEDetect issues in your GitHub, Azure DevOps Services, Bitbucket Cloud, GitLab reposi",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Security",
  "recommendation": "Remove the problematic code and refactor as needed.",
  "impact": "Security risk",
  "tags": [
    "security",
    "bug",
    ".net"
  ],
  "examples": {
    "noncompliant": [
      "if(collection.Count >= 0) { /* ... */ } // Noncompliant: always true\n\nif(array.Length >= 0) { /* ... */ } // Noncompliant: always true\n\nif(enumerable.Count() < 0) { /* ... */ } // Noncompliant: always false"
    ],
    "compliant": [
      "// Check for emptiness properly\nif(collection.Count > 0) { /* ... */ } // Compliant\n\nif(array.Length > 0) { /* ... */ } // Compliant\n\nif(enumerable.Any()) { /* ... */ } // Compliant"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/csharp/RSPEC-3981"
  ],
  "logic": {
    "node_types": [
      "ClassDeclaration"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "patterns": [
          "\\.Count\\s*>=\\s*0",
          "\\.Length\\s*>=\\s*0",
          "\\.Count\\(\\)\\s*<\\s*0"
        ],
        "message": "Remove the problematic code and refactor as needed."
      }
    ]
  }
}