{
  "filename": "multicharacter_literals_should_be_avoided_metadata.json",
  "rule_id": "multicharacter_literals_avoided",
  "title": "Multicharacter literals should be avoided",
  "description": "Multicharacter literals should not be usedAnalyze your codeintentionality-completemaintainabilityCode Smelllock-inconfusingWhy is this an issue?Multicharacter literals haveinttype and have an implementation-defined value. This means they might be interpreted differently by different compilers. For example, they might lead to different behavior when compiled with GCC than when compiled with MSVC.Even if they work as you expect with a specific compiler, they will make your code less portable.",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "General",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Code readability problem",
  "tags": [
    "confusing"
  ],
  "examples": {
    "noncompliant": [
      "void f(int i) {\n  if (i == 'abcd') { // Noncompliant: multicharacter literal\n    printf(\"Match\");\n  }\n}",
      "int magic = 'MAGIC'; // Noncompliant: implementation-defined behavior"
    ],
    "compliant": [
      "void f(int i) {\n  if (i == 0x61626364) { // Compliant: explicit hex value\n    printf(\"Match\");\n  }\n}",
      "const int MAGIC = 0x4D414749; // Compliant: explicit constant"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/c/RSPEC-5978"
  ],
  "logic": {
    "node_types": [
      "Statement",
      "ConditionalStatement",
      "Assignment",
      "FunctionDefinition"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "patterns": [
          "'[a-zA-Z0-9_]{2,}'",
          "'[^'\\\\]{2,}'",
          "'\\\\.[a-zA-Z0-9_]+'",
          "'[a-zA-Z][a-zA-Z0-9_]+'",
          "'[A-Z]{2,}'",
          "'[a-z]{2,}'"
        ],
        "message": "Multicharacter literals should be avoided. Review and fix according to the rule guidelines."
      }
    ]
  }
}