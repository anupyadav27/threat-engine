{
  "filename": "lines_starting_with_should_contain_valid_preproces_metadata.json",
  "rule_id": "lines_starting_contain_valid",
  "title": "Lines starting with # should contain valid preprocessing directives",
  "description": "Lines starting with \"#\" should contain valid preprocessing directivesAnalyze your codeintentionality-logicalreliabilityBugbased-on-misrapreprocessorWhy is this an issue?More InfoPreprocessing directives (lines that start with#) can be used to conditionally include or exclude code from compilation. Malformed preprocessing directives could lead to the exclusion or inclusion of more code than was intended. Therefore all preprocessing directives should be syntactically meaningful.Noncompliant code example#define AAA 2 ..",
  "status": "ready",
  "defaultSeverity": "Info",
  "category": "Reliability",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Potential bug",
  "tags": [
    "bug"
  ],
  "examples": {
    "noncompliant": [
      "#else1  // Noncompliant: invalid directive",
      "#ifdef  // Noncompliant: missing condition",
      "# define MAX 100  // Noncompliant: space after #",
      "#unknown_directive  // Noncompliant: invalid directive"
    ],
    "compliant": [
      "#define MAX_SIZE 100",
      "#ifdef DEBUG\n  printf(\"Debug mode\");\n#endif",
      "#ifndef CONFIG_H\n#define CONFIG_H\n#endif",
      "#include <stdio.h>"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/c/RSPEC-977"
  ],
  "logic": {
    "node_types": ["FunctionDefinition"],
    "checks": [
      {
        "type": "regex_match",
        "property_path": ["source"],
        "patterns": [
          "^\\s*#\\s+\\w+",
          "^\\s*#ifdef\\s*$",
          "^\\s*#ifndef\\s*$",
          "^\\s*#if\\s*$",
          "^\\s*#elif\\s*$",
          "^\\s*#else\\w+",
          "^\\s*#endif\\w+",
          "^\\s*#define\\s*$",
          "^\\s*#undef\\s*$",
          "^\\s*#include\\s*$",
          "^\\s*#[a-zA-Z_][a-zA-Z0-9_]*[0-9]+",
          "^\\s*#[^a-zA-Z_#\\s][^\\r\\n]*",
          "^\\s*#[a-zA-Z_][a-zA-Z0-9_]*(?!include|define|undef|ifdef|ifndef|if|elif|else|endif|pragma|error|warning|line)\\b",
          "^\\s*#\\s*$"
        ],
        "message": "Invalid preprocessing directive - check syntax and ensure directive name is valid and has required parameters"
      }
    ]
  }
}