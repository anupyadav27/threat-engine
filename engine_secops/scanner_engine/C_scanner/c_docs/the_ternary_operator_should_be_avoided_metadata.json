{
  "filename": "the_ternary_operator_should_be_avoided_metadata.json",
  "rule_id": "ternary_operator_avoided",
  "title": "The ternary operator should be avoided",
  "description": "The ternary operator should not be usedAnalyze your codeconsistency-conventionalmaintainabilityCode Smellbrain-overloadWhy is this an issue?Ternary expressions, while concise, can often lead to code that is difficult to read and understand, especially when they are nested or complex. Prioritizing readability fosters maintainability and reduces the likelihood of bugs. Therefore, they should be removed in favor of more explicit control structures, such asif/elsestatements, to improve the clarity and readability of the code.Code examplesNoncompliant code exampleprintf(\"%s\", (i > 10 ? \"yes\" : \"no\")); // NoncompliantCompliant solutionif (i > 10) { printf(\"yes\"); } else { printf(\"no\"); }ExceptionsFor C++11 mode only, the issue is not raised for ternary operators used insideconstexprfunctions.",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Maintainability",
  "recommendation": "Remove the problematic code and refactor as needed.",
  "impact": "Maintainability issue",
  "tags": [
    "bug",
    "convention"
  ],
  "examples": {
    "noncompliant": [
      "printf(\"%s\", (i > 10 ? \"yes\" : \"no\"));  // Noncompliant",
      "int max = (a > b ? a : b);  // Noncompliant\nint result = (x > 0 ? (y > 0 ? 1 : 0) : -1);  // Noncompliant: nested ternary",
      "return condition ? complex_function_call() : another_function();  // Noncompliant"
    ],
    "compliant": [
      "if (i > 10) {\n  printf(\"yes\");\n} else {\n  printf(\"no\");\n}",
      "int max;\nif (a > b) {\n  max = a;\n} else {\n  max = b;\n}\n\nint result;\nif (x > 0) {\n  if (y > 0) {\n    result = 1;\n  } else {\n    result = 0;\n  }\n} else {\n  result = -1;\n}",
      "if (condition) {\n  return complex_function_call();\n} else {\n  return another_function();\n}"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/c/RSPEC-1774"
  ],
  "logic": {
    "node_types": [
      "ConditionalExpression",
      "AssignmentExpression",
      "ReturnStatement", 
      "CallExpression",
      "ArithmeticExpression"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "patterns": [
          "[^\"]*\\w+\\s*\\?\\s*[^:\"]*:\\s*[^;,)\"]*[;,)]",
          "[^\"]*=\\s*[^;\"]*\\?[^:\"]*:[^;\"]*;",
          "return\\s+[^;\"]*\\?[^:\"]*:[^;\"]*;",
          "\\([^)\"]*\\?[^:\"]*:[^)\"]*\\)",
          "\\{[^}\"]*\\?[^:\"]*:[^}\"]*\\}",
          "\\[[^\\]\"]*\\?[^:\"]*:[^\\]\"]*\\]"
        ],
        "message": "The ternary operator should be avoided. Remove the problematic code and refactor as needed."
      }
    ]
  }
}