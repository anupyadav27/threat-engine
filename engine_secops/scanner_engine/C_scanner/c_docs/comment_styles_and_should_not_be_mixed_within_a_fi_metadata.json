{
  "filename": "comment_styles_and_should_not_be_mixed_within_a_fi_metadata.json",
  "rule_id": "comment_styles_mixed_within",
  "title": "Comment styles // and /* ... */ should not be mixed within a file",
  "description": "Comment styles \"//\" and \"/* ... */\" should not be mixed within a fileAnalyze your codeconsistency-conventionalmaintainabilityCode SmellconventioncertWhy is this an issue?Use either the// ...or/* ... */comment syntax, but be consistent and do not mix them within the same file.Noncompliant code example/* Noncompliant; both comment syntaxes are used in the same file */ // Foo /* Bar */Compliant solution// Compliant; uniform comment syntax // Foo // BarAvailable In:Catch issues on the fly,in your IDEDetect issues in your GitHub, Azure DevOps Services, Bitbucket Cloud, GitLab repositoriesAnalyze code in youron-premise CIDeveloper EditionAvailable Since9.1©2025SonarSource Sàrl.",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "General",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Code readability problem",
  "tags": [
    "cert",
    "convention"
  ],
  "examples": {
    "noncompliant": [
      "/* Noncompliant; both comment syntaxes are used in the same file */\n// Function to calculate sum\nint calculateSum(int a, int b) {\n  /* Return the sum */\n  return a + b;\n}\n// Another single-line comment"
    ],
    "compliant": [
      "// Compliant; uniform comment syntax\n// Function to calculate sum\nint calculateSum(int a, int b) {\n  // Return the sum\n  return a + b;\n}\n// Another single-line comment",
      "/* Compliant; uniform comment syntax */\n/* Function to calculate sum */\nint calculateSum(int a, int b) {\n  /* Return the sum */\n  return a + b;\n}\n/* Another comment */"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/c/RSPEC-1917"
  ],
  "logic": {
    "node_types": [
      "Comment", 
      "TranslationUnit",
      "ArithmeticExpression",
      "PointerDereference"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "patterns": [
          "(/\\*[\\s\\S]*?\\*/)[\\s\\S]*?(//.*?$)",
          "(//.*?$)[\\s\\S]*?(/\\*[\\s\\S]*?\\*/)"
        ],
        "message": "Comment styles // and /* ... */ should not be mixed within a file. Review and fix according to the rule guidelines."
      }
    ]
  }
}