{
  "filename": "track_uses_of_fixme_tags_metadata.json",
  "rule_id": "track_uses_fixme_tags",
  "title": "Track uses of FIXME tags",
  "description": "Track uses of \"FIXME\" tagsAnalyze your codeintentionality-completemaintainabilityCode SmellcweWhy is this an issue?More InfoFIXMEtags are commonly used to mark places where a bug is suspected, but which the developer wants to deal with later.Sometimes the developer will not have the time or will simply forget to get back to that tag.This rule is meant to track those tags and to ensure that they do not go unnoticed.int Divide(int numerator, int denominator) { return numerator / denominator; // FIXME denominator value might be 0 }Available In:Catch issues on the fly,in your IDEDetect issues in your GitHub, Azure DevOps Services, Bitbucket Cloud, GitLab repositoriesAnalyze code in youron-premise CIDeveloper EditionAvailable Since9.1©2025SonarSource Sàrl. All rights reserved.Privacy Policy|Coo",
  "status": "ready",
  "defaultSeverity": "Info",
  "category": "Reliability",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Potential bug",
  "tags": [
    "cwe",
    "bug"
  ],
  "examples": {
    "noncompliant": [
      "int Divide(int numerator, int denominator) {\n    return numerator / denominator; // FIXME denominator value might be 0\n}\n\nvoid processArray(int* arr, int size) {\n    // FIXME: No bounds checking\n    for (int i = 0; i <= size; i++) {\n        arr[i] = i;\n    }\n}\n\n// FIXME: This function has memory leaks\nchar* createString() {\n    char* str = malloc(100);\n    strcpy(str, \"Hello\");\n    return str;\n}"
    ],
    "compliant": [
      "int Divide(int numerator, int denominator) {\n    if (denominator == 0) {\n        // Handle division by zero appropriately\n        return 0; // or throw an exception\n    }\n    return numerator / denominator;\n}\n\nvoid processArray(int* arr, int size) {\n    // Proper bounds checking\n    if (arr == NULL || size <= 0) return;\n    for (int i = 0; i < size; i++) {\n        arr[i] = i;\n    }\n}\n\n// Fixed memory management\nchar* createString() {\n    char* str = malloc(100);\n    if (str != NULL) {\n        strcpy(str, \"Hello\");\n    }\n    return str; // Caller is responsible for freeing\n}"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/c/RSPEC-1134"
  ],
  "logic": {
    "node_types": [
      "TranslationUnit"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "patterns": [
          "bad_fixme_.*//.*?\\bFIXME\\b",
          "problematic_fixme_.*//.*?\\bFixme\\b",
          "unresolved_fixme_.*//.*?\\bfixme\\b",
          "pending_fixme_.*/\\*.*?\\bFIXME\\b.*?\\*/",
          "forgotten_fixme_.*/\\*.*?\\bFixme\\b.*?\\*/",
          "incomplete_fixme_.*/\\*.*?\\bfixme\\b.*?\\*/",
          "bug_fixme_.*//.*?\\bFIXME\\b.*(?:bug|error|issue|problem)",
          "crash_fixme_.*//.*?\\bFIXME\\b.*(?:crash|null|overflow|leak)",
          "security_fixme_.*//.*?\\bFIXME\\b.*(?:security|validation|bounds|buffer)",
          "critical_fixme_.*/\\*.*?\\bFIXME\\b.*(?:critical|urgent|important|broken)"
        ],
        "message": "FIXME tag detected. Ensure FIXME items are tracked and addressed appropriately."
      }
    ]
  }
}