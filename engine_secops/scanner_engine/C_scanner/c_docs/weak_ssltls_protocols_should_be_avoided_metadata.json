{
  "filename": "weak_ssltls_protocols_should_be_avoided_metadata.json",
  "rule_id": "weak_ssltls_protocols_avoided",
  "title": "Weak SSL/TLS protocols should be avoided",
  "description": "Encryption algorithms are essential for protecting sensitive information and ensuring secure communications in a variety of domains. They are used for several important reasons: Confidentiality, privacy, and intellectual property protection Security during transmission or on storage devices Data integrity, general trust, and authentication When selecting encryption algorithms, tools, or combinations, you should also consider two things: No encryption is unbreakable. The strength of an encryption algorithm is usually measured by the effort required to crack it within a reasonable time frame.",
  "status": "ready",
  "defaultSeverity": "Info",
  "category": "Security",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Security risk",
  "tags": [
    "security",
    "cwe",
    "deprecated"
  ],
  "examples": {
    "noncompliant": [
      "#include <openssl/ssl.h>\n\nvoid setupSSL() {\n  SSL_CTX* ctx = SSL_CTX_new(SSLv2_method()); // Noncompliant: SSLv2 is weak\n  \n  SSL_CTX* ctx2 = SSL_CTX_new(SSLv3_method()); // Noncompliant: SSLv3 is weak\n  \n  SSL_CTX* ctx3 = SSL_CTX_new(TLSv1_method()); // Noncompliant: TLSv1.0 is weak\n  \n  SSL_CTX* ctx4 = SSL_CTX_new(TLSv1_1_method()); // Noncompliant: TLSv1.1 is weak\n}"
    ],
    "compliant": [
      "#include <openssl/ssl.h>\n\nvoid setupSSL() {\n  SSL_CTX* ctx = SSL_CTX_new(TLS_method()); // Compliant: use latest TLS\n  \n  SSL_CTX* ctx2 = SSL_CTX_new(TLSv1_2_method()); // Compliant: TLSv1.2 is secure\n  \n  SSL_CTX* ctx3 = SSL_CTX_new(TLSv1_3_method()); // Compliant: TLSv1.3 is secure\n  \n  // Set minimum protocol version\n  SSL_CTX_set_min_proto_version(ctx, TLS1_2_VERSION);\n}"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/c/RSPEC-4423"
  ],
  "logic": {
    "node_types": [
      "CallExpression"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "patterns": [
          "SSLv2_method\\(\\)",
          "SSLv3_method\\(\\)", 
          "TLSv1_method\\(\\)",
          "TLSv1_1_method\\(\\)"
        ],
        "message": "Weak ssl/tls protocols should be avoided. Review and fix according to the rule guidelines."
      }
    ]
  }
}