{
  "filename": "appropriate_size_arguments_should_be_passed_to_str_metadata.json",
  "rule_id": "appropriate_size_arguments_passed",
  "title": "Appropriate size arguments should be passed to strncat and strlcpy",
  "description": "Appropriate size arguments should be passed to \"strncat\" and \"strlcpy\"Analyze your codeintentionality-completemaintainabilityCode Smellsymbolic-executionsuspiciousSize argument should be based on the size of the destination buffer to to prevent buffer overflows.Why is this an issue?What is the potential impact?How can I fix it?More InfoThe string manipulation functionsstrncat,strlcatandstrlcpyrequire a size argument that describes how many bytes from the source buffer are used at most. In many situations the size of the source buffer is unknown, which is why the size argument for these functions should be based on the size of the destination buffer. This helps to prevent buffer overflows.Note thatstrncatalways adds a terminating null character at the end of the appended characters; therefo",
  "status": "ready",
  "defaultSeverity": "Info",
  "category": "General",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Code readability problem",
  "tags": [
    "c"
  ],
  "examples": {
    "noncompliant": [
      "strncat(dest, src, strlen(src));          // Using source size instead of destination",
      "strncat(dest, src, sizeof(src));          // Using source buffer size",
      "strlcpy(dest, src, strlen(src));          // Using source length instead of destination size",
      "strncat(dest, src, 50);                   // Using arbitrary constant",
      "strlcat(dest, src, strlen(dest));         // Using current destination length"
    ],
    "compliant": [
      "strncat(dest, src, sizeof(dest) - strlen(dest) - 1);  // Proper remaining space calculation",
      "strlcpy(dest, src, sizeof(dest));                      // Using destination buffer size",
      "strlcat(dest, src, sizeof(dest));                      // Using destination buffer size",
      "strncat(dest, src, dest_size - strlen(dest) - 1);      // Using destination size parameter",
      "strlcpy(buffer, source, BUFFER_SIZE);                  // Using destination size constant"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/c/RSPEC-5273"
  ],
  "logic": {
    "node_types": [
      "FunctionDefinition"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "patterns": [
          "strncat\\s*\\([^,]+,\\s*[^,]+,\\s*strlen\\s*\\([^)]*\\)\\s*\\)",
          "strncat\\s*\\([^,]+,\\s*[^,]+,\\s*sizeof\\s*\\(\\s*\\w+\\s*\\)\\s*\\)(?!.*sizeof\\s*\\(\\s*dest)",
          "strlcpy\\s*\\([^,]+,\\s*[^,]+,\\s*strlen\\s*\\([^)]*\\)\\s*\\)",
          "strlcat\\s*\\([^,]+,\\s*[^,]+,\\s*strlen\\s*\\([^)]*\\)\\s*\\)",
          "strncat\\s*\\([^,]+,\\s*[^,]+,\\s*\\d+\\s*\\)(?!.*sizeof\\s*\\(.*dest)",
          "strlcpy\\s*\\([^,]+,\\s*[^,]+,\\s*\\d+\\s*\\)(?!.*sizeof\\s*\\(.*dest)"
        ],
        "message": "Size argument should be based on destination buffer size, not source size or arbitrary constants, to prevent buffer overflows."
      }
    ]
  }
}