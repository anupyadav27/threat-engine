{
  "filename": "cognitive_complexity_of_functions_should_not_be_to_metadata.json",
  "rule_id": "cognitive_complexity_functions_too",
  "title": "Cognitive Complexity of functions should not be too high",
  "description": "This rule raises an issue when the code cognitive complexity of a function is above a certain threshold. Cognitive Complexity is a measure of how hard it is to understand the control flow of a unit of code. Code with high cognitive complexity is hard to read, understand, test, and modify.",
  "status": "ready",
  "defaultSeverity": "Info",
  "category": "Maintainability",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Maintainability issue",
  "tags": [
    "cert"
  ],
  "examples": {
    "noncompliant": [
      "// High cognitive complexity function\nint processData(int *data, int size, int mode) {\n  if (data == NULL) {\n    if (size > 0) {\n      if (mode == 1) {\n        for (int i = 0; i < size; i++) {\n          if (i % 2 == 0) {\n            if (data[i] > 10) {\n              // Multiple nested conditions\n              return -1;\n            }\n          }\n        }\n      } else if (mode == 2) {\n        // More complex logic\n        return -2;\n      }\n    }\n  }\n  return 0;\n}"
    ],
    "compliant": [
      "// Split into smaller, focused functions\nint validateData(int *data, int size) {\n  return (data == NULL || size <= 0) ? -1 : 0;\n}\n\nint processEvenElements(int *data, int size) {\n  for (int i = 0; i < size; i += 2) {\n    if (data[i] > 10) return -1;\n  }\n  return 0;\n}\n\nint processData(int *data, int size, int mode) {\n  if (validateData(data, size) != 0) return -1;\n  if (mode == 1) return processEvenElements(data, size);\n  if (mode == 2) return processMode2(data, size);\n  return 0;\n}"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/c/RSPEC-3776"
  ],
  "logic": {
    "node_types": [
      "FunctionDefinition"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "patterns": [
          "if\\s*\\([^)]*\\)\\s*\\{[^}]*if\\s*\\([^)]*\\)\\s*\\{[^}]*if",
          "for\\s*\\([^)]*\\)\\s*\\{[^}]*for\\s*\\([^)]*\\)\\s*\\{[^}]*for",
          "while\\s*\\([^)]*\\)\\s*\\{[^}]*while\\s*\\([^)]*\\)\\s*\\{[^}]*while"
        ],
        "message": "Cognitive complexity of functions should not be too high. Review and fix according to the rule guidelines."
      }
    ]
  }
}