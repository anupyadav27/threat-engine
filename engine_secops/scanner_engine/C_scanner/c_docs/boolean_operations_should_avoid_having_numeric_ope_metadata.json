{
  "filename": "boolean_operations_should_avoid_having_numeric_ope_metadata.json",
  "rule_id": "boolean_operations_avoid_having",
  "title": "Boolean operations should avoid having numeric operands, and vice versa",
  "description": "There are several constructs in the language that work with boolean: If statements: if (b) ... Conditional operator: int i = b ? 0 : 42; Logical operators: (b1 || b2) && !b3 Those operations would also work with arithmetic or enum values operands, because there is a conversion from those types to bool. However, this conversion might not always be obvious, for instance, an integer return code might use the value 0 to indicate that everything worked as expected, but converted to boolean, this value would be false, which often denotes failure.",
  "status": "ready",
  "defaultSeverity": "Info",
  "category": "Reliability",
  "recommendation": "Refactor to eliminate the flagged pattern.",
  "impact": "Potential bug",
  "tags": [
    "cert",
    "bug",
    "design"
  ],
  "examples": {
    "noncompliant": [
      "if (result + 1) { /* mixing arithmetic with boolean context */ }",
      "if (value * 2) { /* mixing arithmetic with boolean context */ }",
      "if (count - 1) { /* mixing arithmetic with boolean context */ }",
      "if (total / 10) { /* mixing arithmetic with boolean context */ }",
      "bool flag = (value + 5); /* numeric expression in boolean context */",
      "if (b + 1) { /* boolean variable with arithmetic operation */ }"
    ],
    "compliant": [
      "if (result != 0) { /* explicit comparison instead of arithmetic */ }",
      "if (value > 0) { /* explicit boolean comparison */ }",
      "if (count >= 1) { /* explicit comparison */ }",
      "if (total != 0) { /* explicit comparison */ }",
      "bool flag = (value != -1); /* explicit boolean comparison */",
      "if (b && (counter == 1)) { /* separate boolean and numeric logic */ }"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/c/RSPEC-867"
  ],
  "logic": {
    "node_types": [
      "BinaryExpression", "AssignmentExpression", "ArithmeticExpression"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "patterns": [
          "if\\s*\\([^)=!<>]*\\+[^)=!<>]*\\)",
          "if\\s*\\([^)=!<>]*\\-[^)=!<>]*\\)",
          "if\\s*\\([^)=!<>]*\\*[^)=!<>]*\\)",
          "if\\s*\\([^)=!<>]*\\/[^)=!<>]*\\)",
          "bool\\s+\\w+\\s*=\\s*\\([^)=!<>]*[+\\-*/][^)=!<>]*\\)",
          "\\b[a-zA-Z_][a-zA-Z0-9_]*\\s*\\+\\s*[0-9]+"
        ],
        "message": "Boolean operations should avoid having numeric operands"
      }
    ]
  }
}