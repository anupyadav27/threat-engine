{
  "filename": "property_getters_and_setters_should_come_in_pairs_metadata.json",
  "rule_id": "property_getters_setters_come",
  "title": "Property getters and setters should come in pairs",
  "description": "Property getters and setters should come in pairsAnalyze your codeintentionality-clearmaintainabilityCode SmellpitfallWhy is this an issue?When an object is created with a setter for a property but without a getter for that property, the property is inaccessible and is thus useless.This rule also enforces the reverse situation (getter but no setter).Noncompliant code examplevar obj = { set foo(value) { this.fooval = value; } };Compliant solutionvar obj = { set foo(value) { this.fooval = value; }, get foo() { return this.fooval; } };orvar obj = { setFoo(value) { // a standard method, not a setter this.fooval = value; } };Available In:Catch issues on the fly,in your IDEDetect issues in your GitHub, Azure DevOps Services, Bitbucket Cloud, GitLab repositoriesAnalyze code in youron-premise CIAv",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Security",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Security risk",
  "tags": [
    "security",
    "bug",
    "deprecated",
    "async"
  ],
  "examples": {
    "noncompliant": [
      "var obj = {\n    set foo(value) {\n        this.fooval = value;\n    }\n};"
    ],
    "compliant": [
      "var obj = {\n    set foo(value) {\n        this.fooval = value;\n    },\n    get foo() {\n        return this.fooval;\n    }\n};"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/javascript/RSPEC-2376"
  ],
  "logic": {
    "node_types": [
      "ObjectExpression",
      "ClassDeclaration", 
      "ClassExpression"
    ],
    "checks": [
      {
        "type": "custom_function",
        "function": "check_property_getters_setters_pairs",
        "message": "Getters and setters should come in pairs."
      }
    ]
  }
}