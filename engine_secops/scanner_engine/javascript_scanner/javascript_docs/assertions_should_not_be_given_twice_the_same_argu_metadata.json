{
  "filename": "assertions_should_not_be_given_twice_the_same_argu_metadata.json",
  "rule_id": "assertions_given_twice_same",
  "title": "Assertions should not be given twice the same argument",
  "description": "Assertions are statements that check whether certain conditions are true. They are used to validate that the actual results of a code snippet match the expected outcomes. By using assertions, developers can ensure that their code behaves as intended and identify potential bugs or issues early in the development process.",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Best Practices",
  "recommendation": "Ensure assertions compare different values, not the same value twice.",
  "impact": "Useless tests that don't catch bugs",
  "tags": [
    "security",
    "bug",
    "deprecated",
    "async"
  ],
  "examples": {
    "noncompliant": [
      "const assert = require('chai').assert;\n\ndescribe(\"test the same object\", function() {\n    it(\"uses chai 'assert'\", function() {\n        const expected = '1';\n        const actual = (1).toString();\n        assert.equal(actual, actual); // Noncompliant: Asserting the same argument\n    });\n});"
    ],
    "compliant": [
      "const assert = require('chai').assert;\n\ndescribe(\"test the same object\", function() {\n    it(\"uses chai 'assert'\", function() {\n        const expected = '1';\n        const actual = (1).toString();\n        assert.equal(actual, expected);\n    });\n});"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/javascript/RSPEC-5863"
  ],
  "logic": {
    "node_types": [
      "CallExpression",
      "ExpressionStatement"
    ],
    "checks": [
      {
        "type": "regex_match",
        "message": "Ensure assertions compare different values, not the same value twice.",
        "property_path": [
          "source"
        ],
        "patterns": [
          "assert\\.(?:equal|strictEqual|deepEqual|notEqual)\\s*\\(\\s*([a-zA-Z_$][\\w.$\\[\\]]*?)\\s*,\\s*\\1\\s*\\)",
          "expect\\s*\\(([a-zA-Z_$][\\w.$\\[\\]]*?)\\)\\.(?:toBe|toEqual|toStrictEqual)\\s*\\(\\s*\\1\\s*\\)",
          "expect\\s*\\(([a-zA-Z_$][\\w.$\\[\\]()]*?)\\)\\.to\\.(?:equal|be)\\s*\\(\\s*\\1\\s*\\)"
        ]
      }
    ]
  }
}