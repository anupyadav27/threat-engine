{
  "filename": "function_constructors_should_be_avoided_metadata.json",
  "rule_id": "function_constructors_avoided",
  "title": "Function constructors should be avoided",
  "description": "Function constructors should not be usedAnalyze your codeconsistency-conventionalmaintainabilityCode SmellThis rule is deprecated; useS1523instead.Why is this an issue?More InfoIn addition to being obtuse from a syntax perspective, function constructors are also dangerous: their execution evaluates the constructor\u2019s string arguments similar to the wayevalworks, which could expose your program to random, unintended code which can be both slow and a security risk.In general it is better to avoid it altogether, particularly when used to parse JSON data. You should use ECMAScript 5\u2019s built-in JSON functions or a dedicated library.Noncompliant code examplevar obj = new Function(\"return \" + data)(); // NoncompliantCompliant solutionvar obj = JSON.parse(data);ExceptionsFunction calls where the ar",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Security",
  "recommendation": "Replace with the recommended alternative approach.",
  "impact": "Security risk",
  "tags": [
    "security",
    "bug",
    "deprecated",
    "async"
  ],
  "examples": {
    "noncompliant": [
      "eval"
    ],
    "compliant": [
      "var obj =  new Function(\"return \" + data)();  // Noncompliant"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/javascript/RSPEC-3523"
  ],
  "logic": {
    "node_types": [
      "NewExpression",
      "CallExpression"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "pattern": "new\\s+Function\\s*\\(",
        "message": "Replace with the recommended alternative approach."
      },
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "pattern": "Function\\s*\\([^)]*\\)\\s*\\(",
        "message": "Replace with the recommended alternative approach."
      }
    ]
  }
}