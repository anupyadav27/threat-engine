{
  "filename": "variables_should_be_declared_with_let_or_const_metadata.json",
  "rule_id": "variables_declared_let_const",
  "title": "Variables should be declared with let or const",
  "description": "Variables declared with var are function-scoped, meaning they are accessible within the entire function in which they are defined. If a variable is declared using var outside of any function, it becomes a global variable and is accessible throughout the entire JavaScript program. let and const were introduced in ECMAScript 6 (ES6) as a block-scoped alternative to var.",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Security",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Security risk",
  "tags": [
    "security",
    "bug",
    "deprecated",
    "async"
  ],
  "examples": {
    "noncompliant": [
      "var"
    ],
    "compliant": [
      "var"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/javascript/RSPEC-3504"
  ],
  "logic": {
    "node_types": [
      "VariableDeclaration"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "pattern": "^\\s*var\\s+\\w+",
        "message": "Variables should be declared with let or const instead of var"
      },
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "pattern": "^\\s*var\\s+\\{[^}]+\\}",
        "message": "Variables should be declared with let or const instead of var (destructuring)"
      },
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "pattern": "^\\s*var\\s+\\[[^\\]]+\\]",
        "message": "Variables should be declared with let or const instead of var (array destructuring)"
      },
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "pattern": "for\\s*\\(\\s*var\\s+\\w+",
        "message": "Variables should be declared with let or const instead of var (for loop)"
      }
    ]
  }
}