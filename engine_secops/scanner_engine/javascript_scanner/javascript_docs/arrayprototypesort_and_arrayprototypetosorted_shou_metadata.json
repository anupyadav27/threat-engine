{
  "filename": "arrayprototypesort_and_arrayprototypetosorted_shou_metadata.json",
  "rule_id": "arrayprototypesort_arrayprototypetosorted_use_compare",
  "title": "Array.prototype.sort() and Array.prototype.toSorted() should use a compare function",
  "description": "JavaScript provides built-in methods to sort arrays, making it convenient for developers to manipulate data. There are two primary ways to sort an array: Array.prototype.sort(): This method sorts the elements of an array in place and returns the sorted array. Array.prototype.toSorted(): This method is designed to return a new sorted array, leaving the original array unchanged.",
  "status": "ready",
  "defaultSeverity": "Minor",
  "category": "Best Practices",
  "recommendation": "Use a compare function with sort() to ensure consistent ordering.",
  "impact": "Potential inconsistent behavior",
  "tags": [
    "security",
    "bug",
    "deprecated",
    "async"
  ],
  "examples": {
    "noncompliant": [
      "const numbers = [3, 1, 4, 1, 5]; numbers.sort(); // No compare function",
      "const items = ['z', 'a', 'c']; items.sort(); // Default string conversion",
      "myArray.toSorted(); // Missing compare function"
    ],
    "compliant": [
      "const numbers = [3, 1, 4, 1, 5]; numbers.sort((a, b) => a - b);",
      "const items = ['z', 'a', 'c']; items.sort((a, b) => a.localeCompare(b));",
      "myArray.toSorted((a, b) => a - b);"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/javascript/RSPEC-2871"
  ],
  "logic": {
    "node_types": [
      "CallExpression"
    ],
    "checks": [
      {
        "type": "regex_match",
        "message": "Use a compare function with sort() to ensure consistent ordering",
        "property_path": [
          "source"
        ],
        "patterns": [
          "\\.(sort|toSorted)\\s*\\(\\s*\\)$"
        ]
      }
    ]
  }
}