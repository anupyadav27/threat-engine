{
  "filename": "c_style_and_functional_notation_casts_should_be_av_metadata.json",
  "rule_id": "cstyle_functional_notation_casts",
  "title": "C-style and functional notation casts should be avoided",
  "description": "C++ allows the traditional C-style casts [E.G. (int) f] and functional notation casts [E.G. int(f)], but adds its own forms: static_cast<type>(expression) const_cast<type>(expression) dynamic_cast<type>(expression) reinterpret_cast<type>(expression) std::bit_cast<type>(expression) (since C++20) C-style casts and functional notation casts are largely functionally equivalent.",
  "status": "ready",
  "defaultSeverity": "Info",
  "category": "Maintainability",
  "recommendation": "Remove the problematic code and refactor as needed.",
  "impact": "Maintainability issue",
  "tags": [
    "convention"
  ],
  "examples": {
    "noncompliant": [
      "double f = 3.14;\nint i = (int) f;           // Noncompliant: C-style cast\nint j = int(f);            // Noncompliant: functional notation cast\n\nclass Base {};\nclass Derived : public Base {};\nDerived* d = new Derived();\nBase* b = (Base*) d;       // Noncompliant: C-style cast"
    ],
    "compliant": [
      "double f = 3.14;\nint i = static_cast<int>(f);     // Compliant: explicit static_cast\n\nclass Base {};\nclass Derived : public Base {};\nDerived* d = new Derived();\nBase* b = static_cast<Base*>(d); // Compliant: explicit cast\n\n// Other appropriate C++ casts:\nconst int* ci = nullptr;\nint* i_ptr = const_cast<int*>(ci);           // const_cast for const removal\nBase* base_ptr = dynamic_cast<Base*>(d);     // dynamic_cast for safe downcasting\nint* int_ptr = reinterpret_cast<int*>(addr); // reinterpret_cast for low-level casts"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/cpp/RSPEC-871"
  ],
  "logic": {
    "node_types": [
      "declaration",
      "assignment_expression",
      "init_declarator"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "pattern": "=\\s*\\((?:int|char|float|double|bool|long|short|unsigned|signed|const|void|size_t|\\w+\\*?)\\s*\\)",
        "message": "Use C++ style casts (static_cast, dynamic_cast, etc.) instead of C-style casts."
      },
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "pattern": "=\\s*(?:int|char|float|double|bool|long|short|unsigned|signed|size_t)\\s*\\(",
        "message": "Use C++ style casts (static_cast, dynamic_cast, etc.) instead of functional notation casts."
      },
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "pattern": "=\\s*\\(\\s*\\w+\\s*\\*+\\s*\\)",
        "message": "Use C++ style casts (static_cast, reinterpret_cast, etc.) instead of C-style pointer casts."
      }
    ]
  }
}