{
  "filename": "continue_should_be_avoided_metadata.json",
  "rule_id": "continue_avoided",
  "title": "continue should be avoided",
  "description": "\"continue\" should not be usedAnalyze your codeintentionality-clearmaintainabilityCode Smellbased-on-misrabad-practiceWhy is this an issue?More Infocontinueis an unstructured control flow statement. It makes code less testable, less readable and less maintainable. Structured control flow statements such asifshould be used instead.Noncompliant code exampleint i; for (i = 0; i < 10; i++) { if (i == 5) { continue; /* Noncompliant */ } printf(\"i = %d\\n\", i); }Compliant solutionint i; for (i = 0; i < 10; i++) { if (i != 5) { printf(\"i = %d\\n\", i); } }Available In:Catch issues on the fly,in your IDEDetect issues in your GitHub, Azure DevOps Services, Bitbucket Cloud, GitLab repositoriesAnalyze code in youron-premise CIDeveloper EditionAvailable Since9.1©2025SonarSource Sàrl.",
  "status": "ready",
  "defaultSeverity": "Info",
  "category": "General",
  "recommendation": "Replace with the recommended alternative approach.",
  "impact": "Code readability problem",
  "tags": [
    "cpp"
  ],
  "examples": {
    "noncompliant": [
      "// Using continue statement",
      "for (int i = 0; i < 10; i++) {",
      "    if (i == 5) {",
      "        continue;  // Noncompliant: unstructured control flow",
      "    }",
      "    printf(\"i = %d\\n\", i);",
      "}",
      "",
      "// Another example with continue",
      "for (const auto& item : items) {",
      "    if (item.isEmpty()) {",
      "        continue;  // Noncompliant",
      "    }",
      "    processItem(item);",
      "}"
    ],
    "compliant": [
      "// Using structured control flow with if statement",
      "for (int i = 0; i < 10; i++) {",
      "    if (i != 5) {  // Compliant: structured condition",
      "        printf(\"i = %d\\n\", i);",
      "    }",
      "}",
      "",
      "// Better structured approach",
      "for (const auto& item : items) {",
      "    if (!item.isEmpty()) {  // Compliant: positive condition",
      "        processItem(item);",
      "    }",
      "}"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/cpp/RSPEC-909"
  ],
  "logic": {
    "node_types": [
      "FunctionDefinition",
      "ForStatement",
      "WhileStatement"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "patterns": [
          "\\bcontinue\\s*;"
        ],
        "message": "Avoid using continue statements for better structured control flow."
      }
    ]
  }
}