{
  "filename": "all_code_should_be_reachable_metadata.json",
  "rule_id": "all_code_reachable",
  "title": "All code should be reachable",
  "description": "All code should be reachableAnalyze your codeintentionality-logicalreliabilityBugcwebased-on-misracertunusedOnce control flow has been moved out of the current code block, any subsequent statements become effectively unreachable.Why is this an issue?How can I fix it?More InfoSome statements and expressions move the control flow out of the current code block. Additionally, some functions never return the control flow to the caller. Any unlabeled statements that come after such a jump or function call is unreachable.For instance, within a code block, code following a statement containing any of these keywords is effectively dead code:returnbreakcontinuegotoco_returnthrowExamples of functions that never return the control flow to the caller:exit()abort()std::terminate()Functions with the[[nor",
  "status": "ready",
  "defaultSeverity": "Info",
  "category": "Reliability",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Potential bug",
  "tags": [
    "cert",
    "cwe",
    "bug",
    "unused"
  ],
  "examples": {
    "noncompliant": [
      "void function() {\n    return;\n    int unreachable = 42; // Noncompliant: unreachable code\n}",
      "void anotherFunction() {\n    if (condition) {\n        throw std::exception();\n        doSomething(); // Noncompliant: unreachable code\n    }\n}"
    ],
    "compliant": [
      "void function() {\n    if (condition) {\n        return;\n    }\n    int reachable = 42; // Compliant: reachable code\n}",
      "void anotherFunction() {\n    if (condition) {\n        throw std::exception();\n    } else {\n        doSomething(); // Compliant: reachable code\n    }\n}"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/cpp/RSPEC-1763"
  ],
  "logic": {
    "node_types": [
      "FunctionDefinition",
      "CompoundStatement"
    ],
    "checks": [
      {
        "type": "custom_function",
        "function": "check_unreachable_code",
        "message": "Remove unreachable code that appears after return, break, continue, goto, throw or exit statements"
      }
    ]
  }
}