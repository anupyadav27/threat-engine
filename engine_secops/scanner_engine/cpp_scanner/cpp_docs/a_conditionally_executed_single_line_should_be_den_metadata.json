{
  "filename": "a_conditionally_executed_single_line_should_be_den_metadata.json",
  "rule_id": "conditionally_executed_single_line",
  "title": "A conditionally executed single line should be denoted by indentation",
  "description": "A conditionally executed single line should be denoted by indentationAnalyze your codeconsistency-formattedmaintainabilityCode SmellconfusingsuspiciousWhy is this an issue?When the line immediately after a conditional has neither curly braces nor indentation, the intent of the code is unclear and perhaps not what is executed. Additionally, such code is confusing to maintainers.if (condition) // Noncompliant doTheThing(); doTheOtherThing(); // Was the intent to call this function unconditionally?It becomes even more confusing and bug-prone if lines get commented out.if (condition) // Noncompliant // doTheThing(); doTheOtherThing(); // Was the intent to call this function conditionally?Indentation alone or together with curly braces makes the intent clear.if (condition) doTheThing(); doTheOt",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Reliability",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Potential bug",
  "tags": [
    "bug",
    "confusing"
  ],
  "examples": {
    "noncompliant": [
      "if (condition) // Noncompliant: no indentation or braces\ndoTheThing();\ndoTheOtherThing(); // Unclear if this should be conditional\n\nif (x > 0) // Noncompliant\nprintf(\"positive\");\nprintf(\"processing...\"); // Was this meant to be conditional?"
    ],
    "compliant": [
      "if (condition) {\n    doTheThing(); // Compliant: using braces\n}\ndoTheOtherThing(); // Clear this is unconditional\n\nif (condition)\n    doTheThing(); // Compliant: proper indentation\ndoTheOtherThing();\n\n// Even better with braces\nif (x > 0) {\n    printf(\"positive\");\n}\nprintf(\"processing...\");"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/cpp/RSPEC-3973"
  ],
  "logic": {
    "node_types": [
      "IfStatement"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "patterns": [
          "if\\s*\\([^)]+\\)\\s*(?://[^\n]*)?\n(?![\\s]*\\{)(?![\\s]{4,})[a-zA-Z_][a-zA-Z0-9_]*\\s*[\\(;]",
          "if\\s*\\([^)]+\\)\\s*(?://[^\n]*)?\n[a-zA-Z_][a-zA-Z0-9_:]*::[a-zA-Z_][a-zA-Z0-9_]*"
        ],
        "message": "A conditionally executed single line should be denoted by indentation"
      }
    ]
  }
}