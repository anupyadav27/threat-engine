{
  "filename": "control_structures_should_use_curly_braces_metadata.json",
  "rule_id": "control_structures_use_curly",
  "title": "Control structures should use curly braces",
  "description": "Control structures should use curly bracesAnalyze your codeintentionality-clearmaintainabilityCode Smellbased-on-misracertpitfallControl structures are code statements that impact the program’s control flow (e.g., if statements, for loops, etc.)Why is this an issue?More InfoWhile not technically incorrect, the omission of curly braces can be misleading and may lead to the introduction of errors during maintenance.In the following example, the two calls seem to be attached to theifstatement, but only the first one is, andcheckSomethingwill always be executed:if (condition) // Noncompliant executeSomething(); checkSomething();Adding curly braces improves the code readability and its robustness:if (condition) { executeSomething(); checkSomething(); }The rule raises an issue when a control str",
  "status": "ready",
  "defaultSeverity": "Info",
  "category": "Maintainability",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Maintainability issue",
  "tags": [
    "cert"
  ],
  "examples": {
    "noncompliant": [
      "// Noncompliant: Missing curly braces\nif (condition)\n    executeSomething();\n    checkSomething(); // This always executes!\n\nfor (int i = 0; i < 10; i++)\n    process(i);\n\nwhile (running)\n    update();"
    ],
    "compliant": [
      "// Compliant: Using curly braces\nif (condition) {\n    executeSomething();\n    checkSomething();\n}\n\nfor (int i = 0; i < 10; i++) {\n    process(i);\n}\n\nwhile (running) {\n    update();\n}"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/cpp/RSPEC-121"
  ],
  "logic": {
    "node_types": [
      "IfStatement",
      "ForStatement", 
      "WhileStatement"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": ["source"],
        "patterns": [
          "\\b(if|for|while)\\s*\\([^)]*\\)\\s*[^{]",
          "\\belse\\s+[^{]"
        ],
        "message": "Control structures should use curly braces"
      }
    ]
  }
}