{
  "filename": "contains_should_be_preferred_to_test_whether_a_sub_metadata.json",
  "rule_id": "contains_preferred_test_whether",
  "title": "contains should be preferred to test whether a substring is part of a string",
  "description": "There are multiple ways and useful functions to determine whether a substring or a character is part of another string. Historically, the standard C++ functions provide this feature through proxies that do not directly reflect the intent of the code: The find functions on std::string and std::string_view return an index into the searched string. The substring or character is not present when the index is equal to npos (often spelled -1).",
  "status": "ready",
  "defaultSeverity": "Info",
  "category": "Maintainability",
  "recommendation": "Replace with the recommended alternative approach.",
  "impact": "Maintainability issue",
  "tags": [
    "cpp"
  ],
  "examples": {
    "noncompliant": [
      "// Using find() and comparing with npos",
      "std::string text = \"Hello, World!\";",
      "if (text.find(\"World\") != std::string::npos) {",
      "    // substring found",
      "}",
      "",
      "// For std::string_view",
      "std::string_view text_view = \"Hello, World!\";",
      "if (text_view.find(\"World\") != std::string_view::npos) {",
      "    // substring found",
      "}"
    ],
    "compliant": [
      "// Using contains() (C++23)",
      "std::string text = \"Hello, World!\";",
      "if (text.contains(\"World\")) {",
      "    // substring found",
      "}",
      "",
      "// For std::string_view (C++23)",
      "std::string_view text_view = \"Hello, World!\";",
      "if (text_view.contains(\"World\")) {",
      "    // substring found",
      "}"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/cpp/RSPEC-7034"
  ],
  "logic": {
    "node_types": [
      "FunctionDefinition",
      "MethodDeclaration"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "patterns": [
          "\\bfind\\s*\\("
        ],
        "message": "Replace with the recommended alternative approach."
      }
    ]
  }
}