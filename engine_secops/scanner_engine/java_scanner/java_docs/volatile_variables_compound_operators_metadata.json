{
  "filename": "volatile_variables_compound_operators_metadata.json",
  "rule_id": "VULNERABILITY_AVOID_VOLATILE_COMPOUND_OPERATORS",
  "title": "Avoid using volatile variables with compound operators",
  "description": "Using compound operators as well as increments and decrements (and toggling, in the case of booleans) on primitive fields are not atomic operations. That is, they don't happen in a single step. For instance, when a volatile primitive field is incremented or decremented you run the risk of data loss if threads interleave in the steps of the update. Instead, use a guaranteed-atomic class such as AtomicInteger, or synchronize the access.",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "VULNERABILITY",
  "recommendation": "Use a guaranteed-atomic class such as AtomicInteger, or synchronize the access.",
  "impact": "Data loss due to non-atomic operations when using volatile variables with compound operators.",
  "examples": {
    "noncompliant": [],
    "compliant": []
  },
  "references": [
    "https://rules.sonarsource.com/java/RSPEC-3078"
  ],
  "logic": {
    "node_types": [
      "Class"
    ],
    "checks": [
      {
          "type": "regex_match",
          "property_path": ["source"],
          "pattern": "volatile\\s+\\w+|([+\\-*/%]=|\\+\\+|--)",
          "message": "Use a guaranteed-atomic class such as AtomicInteger, or synchronize the access."
      }
    ]
  }
}