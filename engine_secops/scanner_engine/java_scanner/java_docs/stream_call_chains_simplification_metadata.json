{
  "filename": "stream_call_chains_simplification_metadata.json",
  "rule_id": "quality_optimization_stream",
  "title": "Simplify Stream call chains for readability and performance",
  "description": "When using the Stream API, call chains should be simplified as much as possible. Not only does it make the code easier to read, it also avoid creating unnecessary temporary objects.\n\nThis rule raises an issue when one of the following substitution is possible:\n\nstream.filter(predicate).findFirst().isPresent()\n\nstream.filter(predicate).findAny().isPresent()\n\nstream.map(mapper).anyMatch(Boolean::booleanValue)",
  "status": "ready",
  "defaultSeverity": "Info",
  "category": "Optimization",
  "recommendation": "Simplify Stream call chains",
  "impact": "Improves code readability and performance",
  "examples": {
    "noncompliant": [],
    "compliant": []
  },
  "references": [],
  "logic": {
    "node_types": [
      "MethodInvocation"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": ["source"],
        "patterns": ["\\.collect\\s*\\(Collectors\\.toList\\(\\)\\)"],
        "message": "Simplify Stream call chains"
      }
    ]
  }
}