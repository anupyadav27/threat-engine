{
  "filename": "object_wait_notify_notifyall_synchronized_code_metadata.json",
  "rule_id": "VULNERABILITY_OBJECT_WAIT_NOTIFY_NOTIFYALL_SYNC",
  "title": "Use Object.wait(), Object.notify(), and Object.notifyAll() only in synchronized code",
  "description": "These methods are used in multithreaded environments to coordinate interdependent tasks. They require the invoking Thread to own the object's monitor. If a thread invokes one of these methods without owning the object's monitor, an IllegalMonitorStateException is thrown.",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "VULNERABILITY",
  "recommendation": "Ensure Object.wait(), Object.notify(), and Object.notifyAll() are called from synchronized code.",
  "impact": "Potential security vulnerabilities and unpredictable behavior in multithreaded environments.",
  "examples": {
    "noncompliant": [],
    "compliant": []
  },
  "references": [],
  "logic": {
    "node_types": [
      "Class"
    ],
    "checks": [
      {
        "type": "custom_function",
        "function": "check_vulnerability_object_wait_notify_notifyall_sync",
        "message": "Ensure Object.wait(), Object.notify(), and Object.notifyAll() are called from synchronized code."
      }
    ]
  }
}