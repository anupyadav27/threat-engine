{
  "filename": "inverted_boolean_checks_metadata.json",
  "rule_id": "logic_inverted_boolean_checks",
  "title": "Avoid inverted boolean checks",
  "description": "Inverting boolean comparison results with negation operators adds unnecessary complexity. It's clearer to use the opposite comparison operator directly instead of negating the result.",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Maintainability",
  "recommendation": "Use the opposite comparison operator instead of negating boolean results.",
  "impact": "Code readability problem",
  "examples": {
    "noncompliant": [
      "if (!(a == 2))",
      "b := !(i < 10)"
    ],
    "compliant": [
      "if (a != 2)",
      "b := (i >= 10)"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/go/RSPEC-1940"
  ],
  "logic": {
    "node_types": [
      "function_declaration"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source_text"
        ],
        "patterns": [
          "if\\s+!\\s*\\(",
          "!\\s*\\(.*==.*\\)",
          "!\\s*\\(.*!=.*\\)",
          "!\\s*\\(.*<.*\\)",
          "!\\s*\\(.*>.*\\)",
          "!\\s*\\(.*<=.*\\)",
          "!\\s*\\(.*>=.*\\)"
        ],
        "message": "Avoid inverted boolean checks - use the opposite comparison operator instead"
      }
    ]
  }
}