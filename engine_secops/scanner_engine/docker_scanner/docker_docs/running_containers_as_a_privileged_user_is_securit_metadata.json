{
  "filename": "running_containers_as_a_privileged_user_is_securit_metadata.json",
  "rule_id": "running_containers_as_privileged",
  "title": "Running containers as a privileged user is security-sensitive",
  "description": "Running containers as a privileged user is security-sensitiveAnalyze your codeintentionality-completesecuritySecurity HotspotdockerfilecweRunning containers as a privileged user weakens their runtime security, allowing any user whose code runs on the container to perform administrative actions.In Linux containers, the privileged user is usually namedroot. In Windows containers, the equivalent isContainerAdministrator.A malicious user can run code on a system either thanks to actions that could be deemed legitimate - depending on internal business logic or operational management shells - or thanks to malicious actions. For example, with arbitrary code execution after exploiting a service that the container hosts.Suppose the container is not hardened to prevent using a shell, interpreter, or",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Security",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Security risk",
  "tags": [
    "security",
    "cwe"
  ],
  "examples": {
    "noncompliant": [
      "root"
    ],
    "compliant": [
      "ContainerAdministrator"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/docker/RSPEC-6471"
  ],
  "logic": {
    "instruction_types": [
      "USER"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": ["value"],
        "patterns": [
          "^\\s*(root|0)\\s*$",
          "^\\s*root:",
          "^\\s*0:",
          "^\\s*ContainerAdministrator\\s*$"
        ],
        "message": "Running containers as a privileged user is security-sensitive. The USER instruction specifies a privileged user (root, uid 0, or ContainerAdministrator). Containers should run as non-privileged users to limit the impact of potential security breaches. Create and use a dedicated non-root user instead."
      }
    ]
  }
}