{
  "filename": "use_add_instruction_to_retrieve_remote_resources_metadata.json",
  "rule_id": "use_add_instruction_retrieve",
  "title": "Use ADD instruction to retrieve remote resources",
  "description": "Using wget or curl commands to retrieve remote content in Dockerfiles instead of the ADD instruction can lead to several issues, particularly related to the inefficient use of Docker’s build cache. Docker’s build cache is a powerful feature that can significantly speed up the build process by reusing intermediate layers from previous builds if no changes were detected. When you use wget, curl, or similar commands, these commands are run during the build process, and Docker has no way of knowing if the remote content has changed without executing the commands.",
  "status": "ready",
  "defaultSeverity": "Info",
  "category": "Maintainability",
  "recommendation": "Replace with the recommended alternative approach.",
  "impact": "Maintainability issue",
  "tags": [
    "convention",
    "design"
  ],
  "examples": {
    "noncompliant": [
      "wget"
    ],
    "compliant": [
      "curl"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/docker/RSPEC-7026"
  ],
  "logic": {
    "instruction_types": [
      "RUN"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": ["value"],
        "patterns": [
          "\\bwget\\s+(?:--?[a-z-]+\\s+)*https?://",
          "\\bcurl\\s+(?:--?[a-z-]+\\s+)*https?://",
          "\\bwget\\s+.*\\s+https?://",
          "\\bcurl\\s+.*\\s+https?://",
          "\\b(wget|curl)\\s+.*(http://|https://|ftp://)",
          "\\b(wget|curl)\\s+-[^\\s]*O\\s+[^\\s]+\\s+(http|https|ftp)://"
        ],
        "message": "Use ADD instruction to retrieve remote resources. Using wget or curl to download remote files prevents Docker from efficiently utilizing build cache. The ADD instruction can directly fetch remote resources and allows Docker to cache based on URL changes. Replace 'RUN wget/curl <URL>' with 'ADD <URL> <destination>' for better build performance and caching."
      }
    ]
  }
}