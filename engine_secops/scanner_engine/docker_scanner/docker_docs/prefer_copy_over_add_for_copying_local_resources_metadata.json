{
  "filename": "prefer_copy_over_add_for_copying_local_resources_metadata.json",
  "rule_id": "prefer_copy_over_add",
  "title": "Prefer COPY over ADD for copying local resources",
  "description": "Using the ADD instruction instead of COPY for local resources in Dockerfiles can lead to several issues, including unexpected behavior, increased complexity, and security risks. The ADD instruction has additional features that can introduce unintended side effects, such as automatically extracting compressed files and fetching remote resources. This can make the behavior of the instruction less predictable and harder to understand or even lead to security issues, if, for example, due to a typo in the source path, the ADD instruction could fetch a remote resource instead of copying a local file.",
  "status": "ready",
  "defaultSeverity": "Info",
  "category": "Security",
  "recommendation": "Replace with the recommended alternative approach.",
  "impact": "Security risk",
  "tags": [
    "security",
    "convention"
  ],
  "examples": {
    "noncompliant": [],
    "compliant": []
  },
  "references": [
    "https://rules.sonarsource.com/docker/RSPEC-7029"
  ],
  "logic": {
    "instruction_types": [
      "ADD"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": ["value"],
        "patterns": [
          "^(?!https?://|ftp://).*\\.(txt|json|xml|yml|yaml|conf|cfg|ini|properties|env|sh|py|js|ts|java|go|rb|php|html|css|md|jar|war)\\s",
          "^(?!https?://|ftp://)\\./",
          "^(?!https?://|ftp://)\\.$",
          "^(?!https?://|ftp://)\\. ",
          "^(?!https?://|ftp://)/[^\\s]+\\s",
          "^(?!https?://|ftp://)\\.\\.?/",
          "^(?!https?://|ftp://)[a-zA-Z0-9_-]+/"
        ],
        "message": "Prefer COPY over ADD for copying local resources. ADD has additional features (auto-extraction of archives, fetching remote URLs) that can introduce unexpected behavior and security risks. Use COPY for local files and directories, and only use ADD when you specifically need to extract tar archives or fetch remote resources."
      }
    ]
  }
}