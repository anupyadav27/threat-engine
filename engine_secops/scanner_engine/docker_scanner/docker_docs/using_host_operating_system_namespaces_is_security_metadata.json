{
  "filename": "using_host_operating_system_namespaces_is_security_metadata.json",
  "rule_id": "using_host_operating_system",
  "title": "Using host operating system namespaces is security-sensitive",
  "description": "Using host operating system namespaces is security-sensitiveAnalyze your codeintentionality-completesecuritySecurity HotspotdockercweUsing host operating system namespaces can lead to compromise of the host system.Opening network services of the local host system to the container creates a new attack surface for attackers.Host network sharing could provide a significant performance advantage for workloads that require critical network performance. However, the successful exploitation of this attack vector could have a catastrophic impact on confidentiality within the host.Ask Yourself WhetherThe host exposes sensitive network services.The container’s services performances donotrely on operating system namespaces.There is a risk if you answered yes to any of those questions.Recommended Secu",
  "status": "ready",
  "defaultSeverity": "Critical",
  "category": "Security",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Security risk",
  "tags": [
    "security",
    "cwe",
    "performance"
  ],
  "examples": {
    "noncompliant": [
      "# syntax=docker/dockerfile:1.3\nFROM example\n# Sensitive\nRUN --network=host wget -O /home/sessions http://127.0.0.1:9000/sessions"
    ],
    "compliant": [
      "# syntax=docker/dockerfile:1.3\nFROM example\nRUN --network=none wget -O /home/sessions http://127.0.0.1:9000/sessions"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/docker/RSPEC-6431"
  ],
  "logic": {
    "instruction_types": [
      "RUN"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": ["value"],
        "patterns": [
          "--network\\s*=\\s*host\\b"
        ],
        "message": "Using host operating system namespaces is security-sensitive. The --network=host flag shares the host's network namespace with the container, exposing all host network services and creating a significant attack surface. Successful exploitation could compromise the host system. Use --network=none or a custom bridge network instead, unless critical network performance is absolutely required."
      }
    ]
  }
}