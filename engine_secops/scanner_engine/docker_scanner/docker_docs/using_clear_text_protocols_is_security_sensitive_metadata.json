{
  "filename": "using_clear_text_protocols_is_security_sensitive_metadata.json",
  "rule_id": "using_cleartext_protocols_is",
  "title": "Using clear-text protocols is security-sensitive",
  "description": "Using clear-text protocols is security-sensitiveAnalyze your codeintentionality-completesecuritySecurity HotspotdockerfilecweClear-text protocols such asftp,telnet, orhttplack encryption of transported data, as well as the capability to build an authenticated connection. It means that an attacker able to sniff traffic from the network can read, modify, or corrupt the transported content. These protocols are not secure as they expose applications to an extensive range of risks:sensitive data exposuretraffic redirected to a malicious endpointmalware-infected software update or installerexecution of client-side codecorruption of critical informationEven in the context of isolated networks like offline environments or segmented cloud environments, the insider threat exists.",
  "status": "ready",
  "defaultSeverity": "Critical",
  "category": "Security",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Security risk",
  "tags": [
    "security",
    "cwe",
    "deprecated"
  ],
  "examples": {
    "noncompliant": [
      "ftp"
    ],
    "compliant": [
      "telnet"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/docker/RSPEC-5332"
  ],
  "logic": {
    "instruction_types": ["RUN", "ADD", "ENV", "ARG", "LABEL"],
    "checks": [
      {
        "type": "regex_match",
        "property_path": ["value"],
        "pattern": "\\bftp://",
        "message": "Using clear-text protocols is security-sensitive. The FTP protocol transmits data in clear text, making it vulnerable to eavesdropping and man-in-the-middle attacks. Use FTPS (FTP over SSL/TLS) or SFTP (SSH File Transfer Protocol) instead for secure file transfers."
      },
      {
        "type": "regex_match",
        "property_path": ["value"],
        "pattern": "\\btelnet\\s+",
        "message": "Using clear-text protocols is security-sensitive. Telnet transmits all data, including passwords, in clear text. Use SSH instead for secure remote access."
      },
      {
        "type": "regex_match",
        "property_path": ["value"],
        "pattern": "\\bhttp://(?!localhost|127\\.0\\.0\\.1|0\\.0\\.0\\.0)",
        "message": "Using clear-text protocols is security-sensitive. HTTP transmits data in clear text without encryption. Use HTTPS instead to protect data in transit from eavesdropping and tampering."
      },
      {
        "type": "regex_match",
        "property_path": ["value"],
        "pattern": "\\b(wget|curl)\\s+[^|&;]*http://(?!localhost|127\\.0\\.0\\.1|0\\.0\\.0\\.0)",
        "message": "Using clear-text protocols is security-sensitive. HTTP downloads are vulnerable to man-in-the-middle attacks. Use HTTPS URLs for secure downloads."
      },
      {
        "type": "regex_match",
        "property_path": ["value"],
        "pattern": "\\bapt(-get)?\\s+.*http://(?!localhost|127\\.0\\.0\\.1|0\\.0\\.0\\.0)",
        "message": "Using clear-text protocols is security-sensitive. Package repositories accessed over HTTP can be compromised. Use HTTPS repositories or ensure package signature verification is enabled."
      },
      {
        "type": "regex_match",
        "property_path": ["value"],
        "pattern": "\\brsh\\s+",
        "message": "Using clear-text protocols is security-sensitive. RSH (remote shell) transmits data in clear text. Use SSH instead for secure remote access."
      },
      {
        "type": "regex_match",
        "property_path": ["value"],
        "pattern": "\\brlogin\\s+",
        "message": "Using clear-text protocols is security-sensitive. Rlogin transmits data in clear text. Use SSH instead for secure remote access."
      }
    ]
  }
}