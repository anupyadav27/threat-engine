services:
  - name: gcs
    scope: global
    enabled: true
    apis:
      - storage.googleapis.com
      - storage-api.googleapis.com
    exceptions: []  # service-level reporting exceptions
  - name: compute
    scope: regional
    enabled: true
    apis:
      - compute.googleapis.com
    exceptions: []  # service-level reporting exceptions 