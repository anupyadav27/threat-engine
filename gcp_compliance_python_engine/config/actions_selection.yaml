profiles:
  default:
    selected_actions_by_check: {}
    # Example:
    # selected_actions_by_check:
    #   compute_instance_no_external_ip: [notify, stop]
    #   compute_firewall_no_ssh_from_anywhere: [notify, quarantine]
    #   gcs_bucket_logging_configured: [set-logging]
active_profile: default 