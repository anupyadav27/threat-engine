certificates:
  version: '1.0'
  provider: oci
  service: certificates
  scope: regional
  discovery: []
  checks:
  - check_id: oci.certificates.certificate.hsm_client_cert_key_length_minimum
    title: 'OCI CERTIFICATES Certificate: Hsm Client Cert Key Length Minimum'
    severity: high
    for_each: list_certificates
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.certificates.certificate.hsm_client_cert_not_expired
    title: 'OCI CERTIFICATES Certificate: Hsm Client Cert Not Expired'
    severity: medium
    for_each: list_certificates
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.certificates.certificate.hsm_client_cert_trusted_issuer
    title: 'OCI CERTIFICATES Certificate: Hsm Client Cert Trusted Issuer'
    severity: medium
    for_each: list_certificates
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.certificates.certificate.secrets_security_certificate_key_length_minimum
    title: 'OCI CERTIFICATES Certificate: Secrets Security Certificate Key Length Minimum'
    severity: high
    for_each: list_certificates
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.certificates.certificate.secrets_security_certificate_not_expired
    title: 'OCI CERTIFICATES Certificate: Secrets Security Certificate Not Expired'
    severity: high
    for_each: list_certificates
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.certificates.certificate.secrets_security_certificate_trusted_issuer
    title: 'OCI CERTIFICATES Certificate: Secrets Security Certificate Trusted Issuer'
    severity: high
    for_each: list_certificates
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.certificates.certificate_authority.secrets_security_private_ca_access_rbac_required
    title: 'OCI CERTIFICATES Certificate_authority: Secrets Security Private Ca Access RBAC Required'
    severity: high
    for_each: list_certificate_authoritys
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.certificates.certificate_authority.secrets_security_private_ca_key_in_enforced
    title: 'OCI CERTIFICATES Certificate_authority: Secrets Security Private Ca Key In Enforced'
    severity: high
    for_each: list_certificate_authoritys
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.certificates.certificate_authority.secrets_security_private_ca_crl_or_enforced
    title: 'OCI CERTIFICATES Certificate_authority: Secrets Security Private Ca Crl Or Enforced'
    severity: high
    for_each: list_certificate_authoritys
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.certificates.certificate_authority.secrets_security_private_ca_path_length_enforced
    title: 'OCI CERTIFICATES Certificate_authority: Secrets Security Private Ca Path Length Enforced'
    severity: high
    for_each: list_certificate_authoritys
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.certificates.certificate.certificates_expiration_monitored
    title: 'OCI CERTIFICATES Certificate: Certificates Expiration Monitored'
    severity: low
    for_each: list_certificates
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.certificates.certificates.expiration_check
    title: 'OCI CERTIFICATES Certificates: Expiration Check'
    severity: medium
    for_each: list_certificates
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.certificates.certificate.certificate_expiration
    title: 'OCI CERTIFICATES Certificate: Certificate Expiration'
    severity: medium
    for_each: list_certificates
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
