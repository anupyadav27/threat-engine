dns:
  version: '1.0'
  provider: oci
  service: dns
  scope: regional
  discovery: []
  checks:
  - check_id: oci.dns.record.dns_security_record_set_alias_targets_services
    title: 'OCI DNS Record: Dns Security Record Set Alias Targets Services'
    severity: low
    for_each: list_records
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.dns.record.dns_security_record_set_caa_records_wildcard
    title: 'OCI DNS Record: Dns Security Record Set Caa Records Wildcard'
    severity: low
    for_each: list_records
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.dns.record.dns_security_record_set_dmarc_present_present
    title: 'OCI DNS Record: Dns Security Record Set Dmarc Present Present'
    severity: low
    for_each: list_records
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.dns.zone.dns_security_record_set_no_overly_zones
    title: 'OCI DNS Zone: Dns Security Record Set No Overly Zones'
    severity: low
    for_each: list_zones
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.dns.zone.dns_security_record_set_no_rfc1918_blocked
    title: 'OCI DNS Zone: Dns Security Record Set No Rfc1918 Blocked'
    severity: low
    for_each: list_zones
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.dns.steering_policy.dns_security_traffic_policy_health_checks_failover
    title: 'OCI DNS Steering_policy: Dns Security Traffic Policy Health Checks Failover'
    severity: medium
    for_each: list_steering_policys
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.dns.steering_policy.dns_security_traffic_policy_documents_encrypted_and_private
    title: 'OCI DNS Steering_policy: Dns Security Traffic Policy Documents Encrypted And Private'
    severity: medium
    for_each: list_steering_policys
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.dns.steering_policy.dns_security_traffic_policy_targets_approved_domains_only
    title: 'OCI DNS Steering_policy: Dns Security Traffic Policy Targets Approved Domains Only'
    severity: medium
    for_each: list_steering_policys
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.dns.zone.dns_security_zone_cross_account_sharing_restricted
    title: 'OCI DNS Zone: Dns Security Zone Cross Account Sharing Restricted'
    severity: low
    for_each: list_zones
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.dns.zone.dns_security_zone_dnssec_enabled
    title: 'OCI DNS Zone: Dns Security Zone Dnssec Enabled'
    severity: low
    for_each: list_zones
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.dns.zone.dns_security_zone_query_logging_enabled
    title: 'OCI DNS Zone: Dns Security Zone Query Logging Enabled'
    severity: medium
    for_each: list_zones
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.dns.zone.dns_security_zone_transfer_restricted_or_tsig_required
    title: 'OCI DNS Zone: Dns Security Zone Transfer Restricted Or Tsig Required'
    severity: low
    for_each: list_zones
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.dns.record.dnssec_disabled
    title: 'OCI DNS Record: Dnssec Disabled'
    severity: low
    for_each: list_records
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.dns.zone.key_sign_in_dnssec
    title: 'OCI DNS Zone: Key Sign In Dnssec'
    severity: high
    for_each: list_zones
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.dns.zone.rsasha1_in_use_to_zone_sign_dnssec
    title: 'OCI DNS Zone: Rsasha1 In Use To Zone Sign Dnssec'
    severity: low
    for_each: list_zones
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.dns.zone.public_hosted_zones_monitoring_logging_enabled
    title: 'OCI DNS Zone: Public Hosted Zones Monitoring Logging Enabled'
    severity: critical
    for_each: list_zones
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
