checks: []
discovery:
- calls: []
  discovery_id: alicloud.function.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: function
version: '1.0'
