checks: []
discovery:
- calls: []
  discovery_id: alicloud.apikeys.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: apikeys
version: '1.0'
