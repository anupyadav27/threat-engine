checks: []
discovery:
- calls: []
  discovery_id: alicloud.ddos.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: ddos
version: '1.0'
