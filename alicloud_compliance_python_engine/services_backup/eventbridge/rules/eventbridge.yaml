checks: []
discovery:
- calls: []
  discovery_id: alicloud.eventbridge.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: eventbridge
version: '1.0'
