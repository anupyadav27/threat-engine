checks: []
discovery:
- calls: []
  discovery_id: alicloud.bss.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: bss
version: '1.0'
