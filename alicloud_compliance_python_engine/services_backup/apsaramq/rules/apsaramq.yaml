checks: []
discovery:
- calls: []
  discovery_id: alicloud.apsaramq.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: apsaramq
version: '1.0'
