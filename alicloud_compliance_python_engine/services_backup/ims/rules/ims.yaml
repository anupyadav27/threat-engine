checks: []
discovery:
- calls: []
  discovery_id: alicloud.ims.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: ims
version: '1.0'
