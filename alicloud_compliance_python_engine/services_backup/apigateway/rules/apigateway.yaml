checks: []
discovery:
- calls: []
  discovery_id: alicloud.apigateway.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: apigateway
version: '1.0'
