checks: []
discovery:
- calls: []
  discovery_id: alicloud.ack.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: ack
version: '1.0'
