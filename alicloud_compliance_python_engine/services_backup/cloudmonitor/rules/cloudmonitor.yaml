checks: []
discovery:
- calls: []
  discovery_id: alicloud.cloudmonitor.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: cloudmonitor
version: '1.0'
