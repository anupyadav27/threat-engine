checks: []
discovery:
- calls: []
  discovery_id: alicloud.arms.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: arms
version: '1.0'
