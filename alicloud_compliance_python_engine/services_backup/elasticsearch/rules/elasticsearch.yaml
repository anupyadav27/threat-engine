checks: []
discovery:
- calls: []
  discovery_id: alicloud.elasticsearch.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: elasticsearch
version: '1.0'
