checks: []
discovery:
- calls: []
  discovery_id: alicloud.cdn.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: cdn
version: '1.0'
