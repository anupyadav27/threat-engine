checks: []
discovery:
- calls: []
  discovery_id: alicloud.actiontrail.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: actiontrail
version: '1.0'
