checks: []
discovery:
- calls: []
  discovery_id: alicloud.emr.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: emr
version: '1.0'
