checks: []
discovery:
- calls: []
  discovery_id: alicloud.emas.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: emas
version: '1.0'
