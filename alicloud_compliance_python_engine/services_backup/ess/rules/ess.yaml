checks: []
discovery:
- calls: []
  discovery_id: alicloud.ess.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: ess
version: '1.0'
