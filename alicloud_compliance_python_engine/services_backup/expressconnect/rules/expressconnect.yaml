checks: []
discovery:
- calls: []
  discovery_id: alicloud.expressconnect.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: expressconnect
version: '1.0'
