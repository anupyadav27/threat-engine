checks: []
discovery:
- calls: []
  discovery_id: alicloud.auto.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: auto
version: '1.0'
