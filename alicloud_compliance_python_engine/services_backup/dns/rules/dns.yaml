checks: []
discovery:
- calls: []
  discovery_id: alicloud.dns.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: dns
version: '1.0'
