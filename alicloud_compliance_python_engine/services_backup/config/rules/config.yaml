checks: []
discovery:
- calls: []
  discovery_id: alicloud.config.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: config
version: '1.0'
