checks: []
discovery:
- calls: []
  discovery_id: alicloud.cfw.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: cfw
version: '1.0'
