checks: []
discovery:
- calls: []
  discovery_id: alicloud.cloudfw.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: cloudfw
version: '1.0'
