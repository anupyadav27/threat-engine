checks: []
discovery:
- calls: []
  discovery_id: alicloud.apsaravideo.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: apsaravideo
version: '1.0'
