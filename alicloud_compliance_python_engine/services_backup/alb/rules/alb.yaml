checks: []
discovery:
- calls: []
  discovery_id: alicloud.alb.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: alb
version: '1.0'
