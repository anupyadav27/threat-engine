checks: []
discovery:
- calls: []
  discovery_id: alicloud.asr.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: asr
version: '1.0'
