checks: []
discovery:
- calls: []
  discovery_id: alicloud.accessanalyzer.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: accessanalyzer
version: '1.0'
