checks: []
discovery:
- calls: []
  discovery_id: alicloud.dedicated.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: dedicated
version: '1.0'
