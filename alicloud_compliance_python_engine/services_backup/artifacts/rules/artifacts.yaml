checks: []
discovery:
- calls: []
  discovery_id: alicloud.artifacts.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: artifacts
version: '1.0'
