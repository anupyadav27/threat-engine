checks: []
discovery:
- calls: []
  discovery_id: alicloud.cr.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: cr
version: '1.0'
