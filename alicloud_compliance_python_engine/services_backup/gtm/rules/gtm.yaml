checks: []
discovery:
- calls: []
  discovery_id: alicloud.gtm.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: gtm
version: '1.0'
