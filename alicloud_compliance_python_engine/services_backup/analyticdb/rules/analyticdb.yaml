checks: []
discovery:
- calls: []
  discovery_id: alicloud.analyticdb.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: analyticdb
version: '1.0'
