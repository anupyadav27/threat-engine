checks: []
discovery:
- calls: []
  discovery_id: alicloud.general.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: general
version: '1.0'
