checks: []
discovery:
- calls: []
  discovery_id: alicloud.devops.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: devops
version: '1.0'
