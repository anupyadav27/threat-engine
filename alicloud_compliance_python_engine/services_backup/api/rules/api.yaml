checks: []
discovery:
- calls: []
  discovery_id: alicloud.api.resources
  emit:
    as: resource
    item:
      id: '{{ resource.id }}'
      name: '{{ resource.name }}'
    items_for: '{{ resources }}'
provider: alicloud
service: api
version: '1.0'
