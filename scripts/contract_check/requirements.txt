# Contract Check Agent — local dev dependencies
# Install with: pip install -r scripts/contract_check/requirements.txt

pydantic>=2.9.2
pydantic-ai[openai]==0.0.14   # DeepSeek uses OpenAI-compatible provider
