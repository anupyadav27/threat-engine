version: '1.0'
provider: ibm
service: botocore
services:
  client: botocore
  module: NullHandler
discovery:
- discovery_id: ibm.botocore.get-name
  calls:
  - action: get-name
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.item }}'
checks: []
