version: '1.0'
provider: ibm
service: context_based_restrictions
services:
  client: context_based_restrictions
  module: ContextBasedRestrictionsV1
discovery:
- discovery_id: ibm.context_based_restrictions.list-available-service-operations
  calls:
  - action: list-available-service-operations
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.items }}'
- discovery_id: ibm.context_based_restrictions.list-zones
  calls:
  - action: list-zones
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.items }}'
- discovery_id: ibm.context_based_restrictions.list-rules
  calls:
  - action: list-rules
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.items }}'
- discovery_id: ibm.context_based_restrictions.list-available-serviceref-targets
  calls:
  - action: list-available-serviceref-targets
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.items }}'
- discovery_id: ibm.context_based_restrictions.get-zone
  calls:
  - action: get-zone
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.item }}'
- discovery_id: ibm.context_based_restrictions.get-authenticator
  calls:
  - action: get-authenticator
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.item }}'
- discovery_id: ibm.context_based_restrictions.get-account-settings
  calls:
  - action: get-account-settings
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.item }}'
- discovery_id: ibm.context_based_restrictions.get-http-client
  calls:
  - action: get-http-client
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.item }}'
- discovery_id: ibm.context_based_restrictions.get-enable-gzip-compression
  calls:
  - action: get-enable-gzip-compression
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.item }}'
- discovery_id: ibm.context_based_restrictions.get-rule
  calls:
  - action: get-rule
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.item }}'
- discovery_id: ibm.context_based_restrictions.get-serviceref-target
  calls:
  - action: get-serviceref-target
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.item }}'
checks: []
