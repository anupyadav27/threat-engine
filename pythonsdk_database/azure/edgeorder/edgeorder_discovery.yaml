version: '1.0'
provider: azure
service: edgeorder
services:
  client: edgeorder
  module: azure.mgmt.edgeorder
discovery:
- discovery_id: azure.edgeorder.edgeordermanagementclientmixin.list_addresses_at_resource_group_level
  calls:
  - action: list_addresses_at_resource_group_level
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.value }}'
- discovery_id: azure.edgeorder.edgeordermanagementclientmixin.begin_create_order_item
  calls:
  - action: begin_create_order_item
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.value }}'
- discovery_id: azure.edgeorder.edgeordermanagementclientmixin.begin_create_order_item
  calls:
  - action: begin_create_order_item
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.value }}'
- discovery_id: azure.edgeorder.edgeordermanagementclientmixin.list_configurations
  calls:
  - action: list_configurations
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.value }}'
- discovery_id: azure.edgeorder.edgeordermanagementclientmixin.list_product_families_metadata
  calls:
  - action: list_product_families_metadata
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.value }}'
- discovery_id: azure.edgeorder.edgeordermanagementclientmixin.get_order_item_by_name
  calls:
  - action: get_order_item_by_name
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.id }}'
- discovery_id: azure.edgeorder.edgeordermanagementclientmixin.list_order_at_subscription_level
  calls:
  - action: list_order_at_subscription_level
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.value }}'
- discovery_id: azure.edgeorder.edgeordermanagementclientmixin.get_order_by_name
  calls:
  - action: get_order_by_name
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.id }}'
- discovery_id: azure.edgeorder.edgeordermanagementclientmixin.list_operations
  calls:
  - action: list_operations
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.value }}'
checks: []
