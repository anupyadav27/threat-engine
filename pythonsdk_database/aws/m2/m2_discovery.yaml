version: '1.0'
provider: aws
service: m2
services:
  client: m2
  module: boto3.client
discovery:
- discovery_id: aws.m2.list_applications
  calls:
  - action: list_applications
    save_as: response
    on_error: continue
    params:
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.applications }}'
- discovery_id: aws.m2.list_environments
  calls:
  - action: list_environments
    save_as: response
    on_error: continue
    params:
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.environments }}'
- discovery_id: aws.m2.get_signed_bluinsights_url
  calls:
  - action: get_signed_bluinsights_url
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.signedBiUrl }}'
- discovery_id: aws.m2.list_batch_job_executions
  for_each: aws.m2.list_applications
  calls:
  - action: list_batch_job_executions
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.applicationApplicationId }}'
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.batchJobExecutions }}'
- discovery_id: aws.m2.list_batch_job_restart_points
  for_each: aws.m2.list_applications
  calls:
  - action: list_batch_job_restart_points
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.batchJobExecutionExecutionId }}'
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.batchJobSteps }}'
- discovery_id: aws.m2.get_batch_job_execution
  for_each: aws.m2.list_applications
  calls:
  - action: get_batch_job_execution
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.batchJobExecutionExecutionId }}'
  emit:
    as: item
    items_for: '{{ response.batchJobIdentifier }}'
- discovery_id: aws.m2.list_data_sets
  for_each: aws.m2.list_applications
  calls:
  - action: list_data_sets
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.applicationApplicationId }}'
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.dataSets }}'
- discovery_id: aws.m2.get_data_set_details
  for_each: aws.m2.list_applications
  calls:
  - action: get_data_set_details
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.dataSetDataSetName }}'
  emit:
    as: item
    items_for: '{{ response.dataSetOrg }}'
- discovery_id: aws.m2.list_data_set_export_history
  for_each: aws.m2.list_applications
  calls:
  - action: list_data_set_export_history
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.applicationApplicationId }}'
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.dataSetExportTasks }}'
- discovery_id: aws.m2.get_data_set_export_task
  for_each: aws.m2.list_applications
  calls:
  - action: get_data_set_export_task
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.dataSetExportTaskTaskId }}'
  emit:
    as: item
    items_for: '{{ response.summary }}'
- discovery_id: aws.m2.list_batch_job_definitions
  for_each: aws.m2.list_applications
  calls:
  - action: list_batch_job_definitions
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.applicationApplicationId }}'
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.batchJobDefinitions }}'
- discovery_id: aws.m2.list_deployments
  for_each: aws.m2.list_applications
  calls:
  - action: list_deployments
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.applicationApplicationId }}'
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.deployments }}'
checks: []
