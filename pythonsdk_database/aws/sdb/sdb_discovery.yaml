version: '1.0'
provider: aws
service: sdb
services:
  client: sdb
  module: boto3.client
discovery:
- discovery_id: aws.sdb.get_attributes
  calls:
  - action: get_attributes
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.Attributes }}'
checks: []
