version: '1.0'
provider: gcp
service: kms
services:
  client: kms
  module: googleapiclient.discovery.build('kms', 'v1')
discovery:
- discovery_id: gcp.kms.organizations.getkajpolicyconfig
  calls:
  - action: getkajpolicyconfig
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
checks: []
