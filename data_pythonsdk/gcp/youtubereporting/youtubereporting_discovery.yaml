version: '1.0'
provider: gcp
service: youtubereporting
services:
  client: youtubereporting
  module: googleapiclient.discovery.build('youtubereporting', 'v1')
discovery:
- discovery_id: gcp.youtubereporting.reportTypes.list
  calls:
  - action: list
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
checks: []
