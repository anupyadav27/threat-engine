version: '1.0'
provider: gcp
service: accesscontextmanager
services:
  client: accesscontextmanager
  module: googleapiclient.discovery.build('accesscontextmanager', 'v1')
discovery:
- discovery_id: gcp.accesscontextmanager.services.get
  calls:
  - action: get
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
checks: []
