version: '1.0'
provider: azure
service: security
services:
  client: security
  module: azure.mgmt.security.v2015_06_01_preview
discovery:
- discovery_id: azure.security.tasks.list_by_home_region
  calls:
  - action: list_by_home_region
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.value }}'
- discovery_id: azure.security.tasks.list_by_home_region
  calls:
  - action: list_by_home_region
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.value }}'
- discovery_id: azure.security.locations.list
  calls:
  - action: list
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.value }}'
- discovery_id: azure.security.locations.list
  calls:
  - action: list
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.value }}'
checks: []
