version: '1.0'
provider: azure
service: msi
services:
  client: msi
  module: azure.mgmt.msi.v2021_09_30_preview
discovery:
- discovery_id: azure.msi.userassignedidentities.list_by_resource_group
  calls:
  - action: list_by_resource_group
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.value }}'
- discovery_id: azure.msi.userassignedidentities.list_by_subscription
  calls:
  - action: list_by_subscription
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.value }}'
- discovery_id: azure.msi.systemassignedidentities.get_by_scope
  calls:
  - action: get_by_scope
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.id }}'
- discovery_id: azure.msi.systemassignedidentities.get_by_scope
  calls:
  - action: get_by_scope
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.id }}'
- discovery_id: azure.msi.list
  calls:
  - action: list
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.value }}'
checks: []
