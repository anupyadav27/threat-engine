version: '1.0'
provider: oci
service: resource_scheduler
services:
  client: resource_scheduler
  module: oci.resource_scheduler
discovery:
- discovery_id: oci.resource_scheduler.get_schedule
  calls:
  - action: get_schedule
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: oci.resource_scheduler.get_work_request
  calls:
  - action: get_work_request
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: oci.resource_scheduler.list_resource_types
  for_each: oci.resource_scheduler.get_schedule
  calls:
  - action: list_resource_types
    save_as: response
    on_error: continue
    params:
      compartment_id: '{{ item.compartment_id }}'
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: oci.resource_scheduler.list_work_request_errors
  for_each: oci.resource_scheduler.get_schedule
  calls:
  - action: list_work_request_errors
    save_as: response
    on_error: continue
    params:
      compartment_id: '{{ item.compartment_id }}'
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: oci.resource_scheduler.list_schedules
  for_each: oci.resource_scheduler.get_schedule
  calls:
  - action: list_schedules
    save_as: response
    on_error: continue
    params:
      compartment_id: '{{ item.compartment_id }}'
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: oci.resource_scheduler.list_work_request_logs
  for_each: oci.resource_scheduler.get_schedule
  calls:
  - action: list_work_request_logs
    save_as: response
    on_error: continue
    params:
      compartment_id: '{{ item.compartment_id }}'
  emit:
    as: item
    items_for: '{{ response }}'
checks: []
