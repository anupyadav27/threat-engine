version: '1.0'
provider: oci
service: dashboard_service
services:
  client: dashboard_service
  module: oci.dashboard_service
discovery:
- discovery_id: oci.dashboard_service.get_dashboard_group
  calls:
  - action: get_dashboard_group
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
checks: []
