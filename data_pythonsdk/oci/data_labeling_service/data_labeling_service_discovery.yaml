version: '1.0'
provider: oci
service: data_labeling_service
services:
  client: data_labeling_service
  module: oci.data_labeling_service
discovery:
- discovery_id: oci.data_labeling_service.get_dataset
  calls:
  - action: get_dataset
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: oci.data_labeling_service.get_work_request
  calls:
  - action: get_work_request
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: oci.data_labeling_service.list_annotation_formats
  for_each: oci.data_labeling_service.get_dataset
  calls:
  - action: list_annotation_formats
    save_as: response
    on_error: continue
    params:
      compartment_id: '{{ item.compartment_id }}'
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: oci.data_labeling_service.list_work_request_errors
  for_each: oci.data_labeling_service.get_dataset
  calls:
  - action: list_work_request_errors
    save_as: response
    on_error: continue
    params:
      compartment_id: '{{ item.compartment_id }}'
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: oci.data_labeling_service.list_work_request_logs
  for_each: oci.data_labeling_service.get_dataset
  calls:
  - action: list_work_request_logs
    save_as: response
    on_error: continue
    params:
      compartment_id: '{{ item.compartment_id }}'
  emit:
    as: item
    items_for: '{{ response }}'
checks: []
