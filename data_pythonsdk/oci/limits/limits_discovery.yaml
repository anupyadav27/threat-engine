version: '1.0'
provider: oci
service: limits
services:
  client: limits
  module: oci.limits
discovery:
- discovery_id: oci.limits.get_quota
  calls:
  - action: get_quota
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
checks: []
