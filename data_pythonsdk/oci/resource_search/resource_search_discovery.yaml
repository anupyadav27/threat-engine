version: '1.0'
provider: oci
service: resource_search
services:
  client: resource_search
  module: oci.resource_search
discovery:
- discovery_id: oci.resource_search.get_resource_type
  calls:
  - action: get_resource_type
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
- discovery_id: oci.resource_search.list_resource_types
  for_each: oci.resource_search.get_resource_type
  calls:
  - action: list_resource_types
    save_as: response
    on_error: continue
    params:
      compartment_id: '{{ item.compartment_id }}'
  emit:
    as: item
    items_for: '{{ response }}'
checks: []
