version: '1.0'
provider: aws
service: geo-maps
services:
  client: geo-maps
  module: boto3.client
discovery:
- discovery_id: aws.geo-maps.get_tile
  calls:
  - action: get_tile
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response.Blob }}'
checks: []
