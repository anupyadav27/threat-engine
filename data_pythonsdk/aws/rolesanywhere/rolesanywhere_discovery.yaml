version: '1.0'
provider: aws
service: rolesanywhere
services:
  client: rolesanywhere
  module: boto3.client
discovery:
- discovery_id: aws.rolesanywhere.list_profiles
  calls:
  - action: list_profiles
    save_as: response
    on_error: continue
    params:
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.profiles }}'
- discovery_id: aws.rolesanywhere.list_trust_anchors
  calls:
  - action: list_trust_anchors
    save_as: response
    on_error: continue
    params:
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.trustAnchors }}'
- discovery_id: aws.rolesanywhere.list_crls
  calls:
  - action: list_crls
    save_as: response
    on_error: continue
    params:
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.crls }}'
- discovery_id: aws.rolesanywhere.list_subjects
  calls:
  - action: list_subjects
    save_as: response
    on_error: continue
    params:
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.subjects }}'
- discovery_id: aws.rolesanywhere.get_subject
  for_each: aws.rolesanywhere.list_subjects
  calls:
  - action: get_subject
    save_as: response
    on_error: continue
    params:
      subjectId: '{{ item.subjectSubjectId }}'
  emit:
    as: item
    items_for: '{{ response.subject }}'
- discovery_id: aws.rolesanywhere.list_tags_for_resource
  calls:
  - action: list_tags_for_resource
    save_as: response
    on_error: continue
    params:
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.tags }}'
checks: []
