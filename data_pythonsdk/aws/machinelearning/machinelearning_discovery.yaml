version: '1.0'
provider: aws
service: machinelearning
services:
  client: machinelearning
  module: boto3.client
discovery:
- discovery_id: aws.machinelearning.describe_ml_models
  calls:
  - action: describe_ml_models
    save_as: response
    on_error: continue
    params:
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.Results }}'
- discovery_id: aws.machinelearning.describe_data_sources
  calls:
  - action: describe_data_sources
    save_as: response
    on_error: continue
    params:
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.Results }}'
- discovery_id: aws.machinelearning.describe_batch_predictions
  calls:
  - action: describe_batch_predictions
    save_as: response
    on_error: continue
    params:
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.Results }}'
- discovery_id: aws.machinelearning.describe_evaluations
  calls:
  - action: describe_evaluations
    save_as: response
    on_error: continue
    params:
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.Results }}'
- discovery_id: aws.machinelearning.get_data_source
  for_each: aws.machinelearning.describe_data_sources
  calls:
  - action: get_data_source
    save_as: response
    on_error: continue
    params:
      DataSourceId: '{{ item.resultDataSourceId }}'
  emit:
    as: item
    items_for: '{{ response.RedshiftMetadata }}'
- discovery_id: aws.machinelearning.get_ml_model
  for_each: aws.machinelearning.describe_ml_models
  calls:
  - action: get_ml_model
    save_as: response
    on_error: continue
    params:
      MLModelId: '{{ item.resultMlModelId }}'
  emit:
    as: item
    items_for: '{{ response.EndpointInfo }}'
- discovery_id: aws.machinelearning.describe_tags
  calls:
  - action: describe_tags
    save_as: response
    on_error: continue
    params:
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.Tags }}'
- discovery_id: aws.machinelearning.get_evaluation
  for_each: aws.machinelearning.describe_evaluations
  calls:
  - action: get_evaluation
    save_as: response
    on_error: continue
    params:
      EvaluationId: '{{ item.resultEvaluationId }}'
  emit:
    as: item
    items_for: '{{ response.PerformanceMetrics }}'
checks: []
