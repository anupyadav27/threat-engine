version: '1.0'
provider: aws
service: signer
services:
  client: signer
  module: boto3.client
discovery:
- discovery_id: aws.signer.list_signing_jobs
  calls:
  - action: list_signing_jobs
    save_as: response
    on_error: continue
    params:
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.jobs }}'
- discovery_id: aws.signer.list_signing_profiles
  calls:
  - action: list_signing_profiles
    save_as: response
    on_error: continue
    params:
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.profiles }}'
- discovery_id: aws.signer.list_signing_platforms
  calls:
  - action: list_signing_platforms
    save_as: response
    on_error: continue
    params:
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.platforms }}'
- discovery_id: aws.signer.describe_signing_job
  for_each: aws.signer.list_signing_jobs
  calls:
  - action: describe_signing_job
    save_as: response
    on_error: continue
    params:
      jobId: '{{ item.jobJobId }}'
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.source }}'
- discovery_id: aws.signer.get_signing_profile
  for_each: aws.signer.list_signing_jobs
  calls:
  - action: get_signing_profile
    save_as: response
    on_error: continue
    params:
      profileName: '{{ item.jobProfileName }}'
  emit:
    as: item
    items_for: '{{ response.revocationRecord }}'
- discovery_id: aws.signer.list_profile_permissions
  for_each: aws.signer.list_signing_jobs
  calls:
  - action: list_profile_permissions
    save_as: response
    on_error: continue
    params:
      profileName: '{{ item.jobProfileName }}'
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.permissions }}'
- discovery_id: aws.signer.get_signing_platform
  for_each: aws.signer.list_signing_jobs
  calls:
  - action: get_signing_platform
    save_as: response
    on_error: continue
    params:
      platformId: '{{ item.jobPlatformId }}'
  emit:
    as: item
    items_for: '{{ response.signingConfiguration }}'
- discovery_id: aws.signer.get_revocation_status
  for_each: aws.signer.list_signing_jobs
  calls:
  - action: get_revocation_status
    save_as: response
    on_error: continue
    params:
      signatureTimestamp: '{{ item.profilProfileVersionArn }}'
  emit:
    as: item
    items_for: '{{ response.revokedEntities }}'
checks: []
