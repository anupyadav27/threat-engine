version: '1.0'
provider: aws
service: launch-wizard
services:
  client: launch-wizard
  module: boto3.client
discovery:
- discovery_id: aws.launch-wizard.list_deployments
  calls:
  - action: list_deployments
    save_as: response
    on_error: continue
    params:
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.deployments }}'
- discovery_id: aws.launch-wizard.list_workloads
  calls:
  - action: list_workloads
    save_as: response
    on_error: continue
    params:
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.workloads }}'
- discovery_id: aws.launch-wizard.get_deployment
  for_each: aws.launch-wizard.list_deployments
  calls:
  - action: get_deployment
    save_as: response
    on_error: continue
    params:
      deploymentId: '{{ item.deploymentId }}'
  emit:
    as: item
    items_for: '{{ response.deployment }}'
- discovery_id: aws.launch-wizard.get_workload_deployment_pattern
  for_each: aws.launch-wizard.list_deployments
  calls:
  - action: get_workload_deployment_pattern
    save_as: response
    on_error: continue
    params:
      deploymentPatternName: '{{ item.deploymentWorkloadName }}'
  emit:
    as: item
    items_for: '{{ response.workloadDeploymentPattern }}'
- discovery_id: aws.launch-wizard.get_workload
  for_each: aws.launch-wizard.list_deployments
  calls:
  - action: get_workload
    save_as: response
    on_error: continue
    params:
      workloadName: '{{ item.deploymentWorkloadName }}'
  emit:
    as: item
    items_for: '{{ response.workload }}'
- discovery_id: aws.launch-wizard.list_deployment_events
  for_each: aws.launch-wizard.list_deployments
  calls:
  - action: list_deployment_events
    save_as: response
    on_error: continue
    params:
      deploymentId: '{{ item.deploymentId }}'
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.deploymentEvents }}'
checks: []
