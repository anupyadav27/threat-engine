version: '1.0'
provider: aws
service: qbusiness
services:
  client: qbusiness
  module: boto3.client
discovery:
- discovery_id: aws.qbusiness.list_applications
  calls:
  - action: list_applications
    save_as: response
    on_error: continue
    params:
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.applications }}'
- discovery_id: aws.qbusiness.list_plugin_type_metadata
  calls:
  - action: list_plugin_type_metadata
    save_as: response
    on_error: continue
    params:
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.items }}'
- discovery_id: aws.qbusiness.get_application
  for_each: aws.qbusiness.list_applications
  calls:
  - action: get_application
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.applicationApplicationId }}'
  emit:
    as: item
    items_for: '{{ response.clientIdsForOIDC }}'
- discovery_id: aws.qbusiness.get_chat_controls_configuration
  for_each: aws.qbusiness.list_applications
  calls:
  - action: get_chat_controls_configuration
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.applicationApplicationId }}'
  emit:
    as: item
    items_for: '{{ response.topicConfigurations }}'
- discovery_id: aws.qbusiness.list_attachments
  for_each: aws.qbusiness.list_applications
  calls:
  - action: list_attachments
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.applicationApplicationId }}'
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.attachments }}'
- discovery_id: aws.qbusiness.list_messages
  for_each: aws.qbusiness.list_applications
  calls:
  - action: list_messages
    save_as: response
    on_error: continue
    params:
      conversationId: '{{ item.attachmentConversationId }}'
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.messages }}'
- discovery_id: aws.qbusiness.list_data_accessors
  for_each: aws.qbusiness.list_applications
  calls:
  - action: list_data_accessors
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.applicationApplicationId }}'
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.dataAccessors }}'
- discovery_id: aws.qbusiness.list_plugins
  for_each: aws.qbusiness.list_applications
  calls:
  - action: list_plugins
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.applicationApplicationId }}'
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.plugins }}'
- discovery_id: aws.qbusiness.get_plugin
  for_each: aws.qbusiness.list_applications
  calls:
  - action: get_plugin
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.pluginPluginId }}'
  emit:
    as: item
    items_for: '{{ response.authConfiguration }}'
- discovery_id: aws.qbusiness.list_subscriptions
  for_each: aws.qbusiness.list_applications
  calls:
  - action: list_subscriptions
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.applicationApplicationId }}'
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.subscriptions }}'
- discovery_id: aws.qbusiness.get_data_accessor
  for_each: aws.qbusiness.list_applications
  calls:
  - action: get_data_accessor
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.dataAccessorDataAccessorId }}'
  emit:
    as: item
    items_for: '{{ response.actionConfigurations }}'
- discovery_id: aws.qbusiness.list_chat_response_configurations
  for_each: aws.qbusiness.list_applications
  calls:
  - action: list_chat_response_configurations
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.applicationApplicationId }}'
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.chatResponseConfigurations }}'
- discovery_id: aws.qbusiness.get_chat_response_configuration
  for_each: aws.qbusiness.list_applications
  calls:
  - action: get_chat_response_configuration
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.chatResponseConfigurationChatResponseConfigurationId }}'
  emit:
    as: item
    items_for: '{{ response.inUseConfiguration }}'
- discovery_id: aws.qbusiness.list_web_experiences
  for_each: aws.qbusiness.list_applications
  calls:
  - action: list_web_experiences
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.applicationApplicationId }}'
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.webExperiences }}'
- discovery_id: aws.qbusiness.get_web_experience
  for_each: aws.qbusiness.list_applications
  calls:
  - action: get_web_experience
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.webExperiencWebExperienceId }}'
  emit:
    as: item
    items_for: '{{ response.origins }}'
- discovery_id: aws.qbusiness.list_plugin_actions
  for_each: aws.qbusiness.list_applications
  calls:
  - action: list_plugin_actions
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.pluginPluginId }}'
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.items }}'
- discovery_id: aws.qbusiness.list_tags_for_resource
  calls:
  - action: list_tags_for_resource
    save_as: response
    on_error: continue
    params:
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.tags }}'
- discovery_id: aws.qbusiness.list_indices
  for_each: aws.qbusiness.list_applications
  calls:
  - action: list_indices
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.applicationApplicationId }}'
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.indices }}'
- discovery_id: aws.qbusiness.get_index
  for_each: aws.qbusiness.list_applications
  calls:
  - action: get_index
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.userAliasIndexId }}'
  emit:
    as: item
    items_for: '{{ response.documentAttributeConfigurations }}'
- discovery_id: aws.qbusiness.list_data_sources
  for_each: aws.qbusiness.list_applications
  calls:
  - action: list_data_sources
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.userAliasIndexId }}'
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.dataSources }}'
- discovery_id: aws.qbusiness.list_data_source_sync_jobs
  for_each: aws.qbusiness.list_applications
  calls:
  - action: list_data_source_sync_jobs
    save_as: response
    on_error: continue
    params:
      dataSourceId: '{{ item.userAliasIndexId }}'
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.history }}'
- discovery_id: aws.qbusiness.get_data_source
  for_each: aws.qbusiness.list_applications
  calls:
  - action: get_data_source
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.userAliasIndexId }}'
  emit:
    as: item
    items_for: '{{ response.configuration }}'
- discovery_id: aws.qbusiness.list_retrievers
  for_each: aws.qbusiness.list_applications
  calls:
  - action: list_retrievers
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.applicationApplicationId }}'
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.retrievers }}'
- discovery_id: aws.qbusiness.get_retriever
  for_each: aws.qbusiness.list_applications
  calls:
  - action: get_retriever
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.retrieverRetrieverId }}'
  emit:
    as: item
    items_for: '{{ response.configuration }}'
- discovery_id: aws.qbusiness.list_groups
  for_each: aws.qbusiness.list_applications
  calls:
  - action: list_groups
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.userAliasIndexId }}'
      MaxResults: 1000
  emit:
    as: item
    items_for: '{{ response.items }}'
- discovery_id: aws.qbusiness.get_group
  for_each: aws.qbusiness.list_applications
  calls:
  - action: get_group
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.userAliasIndexId }}'
  emit:
    as: item
    items_for: '{{ response.statusHistory }}'
- discovery_id: aws.qbusiness.get_policy
  for_each: aws.qbusiness.list_applications
  calls:
  - action: get_policy
    save_as: response
    on_error: continue
    params:
      applicationId: '{{ item.applicationApplicationId }}'
  emit:
    as: item
    items_for: '{{ response.policy }}'
checks: []
