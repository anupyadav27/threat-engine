version: '1.0'
provider: k8s
service: configmap
services:
  client: configmap
  module: kubernetes.client
discovery:
- discovery_id: k8s.configmap.list
  calls:
  - action: list
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
checks: []
