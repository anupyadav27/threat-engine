version: '1.0'
provider: k8s
service: pod
services:
  client: pod
  module: kubernetes.client
discovery:
- discovery_id: k8s.pod.list
  calls:
  - action: list
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
checks: []
