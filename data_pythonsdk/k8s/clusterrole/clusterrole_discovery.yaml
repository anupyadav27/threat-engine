version: '1.0'
provider: k8s
service: clusterrole
services:
  client: clusterrole
  module: kubernetes.client
discovery:
- discovery_id: k8s.clusterrole.get
  calls:
  - action: get
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
checks: []
