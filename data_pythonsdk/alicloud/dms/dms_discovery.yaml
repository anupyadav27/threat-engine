version: '1.0'
provider: alicloud
service: dms
services:
  client: dms
  module: aliyunsdkdms
discovery:
- discovery_id: alicloud.dms.CreateAirflowLoginToken
  calls:
  - action: CreateAirflowLoginToken
    save_as: response
    on_error: continue
  emit:
    as: item
    items_for: '{{ response }}'
checks: []
