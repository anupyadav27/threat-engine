{
  "service": "accessanalyzer",
  "title": "Access Analyzer Enabled Without Findings",
  "description": "Check if access analyzer is enabled and has no status reason indicating findings",
  "remediation": "Enable the access analyzer and resolve any findings:\n1. Go to IAM > Access Analyzer\n2. Create or enable analyzer\n3. Review and resolve any findings\n4. Ensure statusReason is null",
  "rule_id": "aws.accessanalyzer.resource.analyzer_enabled_no_findings",
  "conditions": [
    {
      "field_name": "status",
      "operator": "equals",
      "value": "ACTIVE"
    },
    {
      "field_name": "statusReason",
      "operator": "not_equals",
      "value": null
    }
  ],
  "logical_operator": "all"
}

