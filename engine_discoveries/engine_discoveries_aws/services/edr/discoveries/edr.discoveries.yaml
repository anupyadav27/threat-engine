version: '1.0'
provider: aws
service: edr
services:
  client: edr
  module: boto3.client
discovery:
- discovery_id: aws.edr.list_memberships
  calls:
  - action: list_memberships
    params:
      maxResults: 1000
    save_as: response
  emit:
    items_for: '{{ response.items }}'
    as: item
