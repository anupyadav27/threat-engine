"""
Agent-facing routers for the onboarding engine.

These routers use Bearer token authentication (not the platform cookie /
X-Auth-Context flow) so that installed agents can reach them directly.
"""
