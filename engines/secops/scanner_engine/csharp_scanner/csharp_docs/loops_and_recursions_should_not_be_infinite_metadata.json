{
  "filename": "loops_and_recursions_should_not_be_infinite_metadata.json",
  "rule_id": "loops_recursions_infinite",
  "title": "Loops and recursions should not be infinite",
  "description": "Loops and recursions should not be infiniteAnalyze your codeintentionality-logicalreliabilityBugsuspiciousWhy is this an issue?How can I fix it?More InfoHaving an infinite loop or recursion will lead to a program failure or a program never finishing the execution.public int Sum() { var i = 0; var result = 0; while (true) // Noncompliant: the program will never stop { result += i; i++; } return result; }This can happen in multiple scenarios.Loop statementswhileandforloops with nobreakorreturnstatements that have exit conditions which are alwaysfalsewill be indefinitely executed.\"goto\" statementsgotostatement with nothing that stops it from being executed over and over again will prevent the program from the completion.RecursionWhen arecursivemethod call chain lacks an exit condition, thecal",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Security",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Security risk",
  "tags": [
    "security",
    "bug",
    ".net"
  ],
  "examples": {
    "noncompliant": [
      "public int Sum()\n{\n    var i = 0;\n    var result = 0;\n    while (true) // Noncompliant: the program will never stop\n    {\n        result += i;\n        i++;\n    }\n    return result;\n}"
    ],
    "compliant": [
      "public int Sum(int max)\n{\n    var result = 0;\n    for (int i = 0; i < max; i++) // Compliant: loop has exit condition\n    {\n        result += i;\n    }\n    return result;\n}"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/csharp/RSPEC-2190"
  ],
  "logic": {
    "node_types": ["MethodDeclaration"],
    "checks": [
      {
        "type": "regex_match",
        "property_path": ["source"],
        "pattern": "while\\s*\\(\\s*true\\s*\\)\\s*\\{(?:(?!break|return|throw)[^}])*\\}",
        "exclude_patterns": [
          "\\[Test\\]",
          "\\[Fact\\]",
          "Assert\\."
        ],
        "context_exclude_patterns": ["\\[Fact\\]", "\\[Test\\]"],
        "context_lines_to_check": 5
      }
    ],
    "triggering_conditions": "while(true) loop without break/return/throw statement",
    "exclusion_conditions": [
      "Loops containing break statements",
      "Loops containing return statements",
      "Loops containing throw statements",
      "Test methods"
    ],
    "never_trigger_on": [
      "while(true) with break inside",
      "while(true) with return inside",
      "Unit tests with [Test] or Assert"
    ],
    "reasoning": "Detects infinite while(true) loops. Excludes loops that contain break, return, or throw statements which provide exit conditions.",
    "validation_examples": {
      "should_trigger": [
        "while (true) { result += i; i++; }"
      ],
      "should_not_trigger": [
        "while (true) { if (i > 10) break; }",
        "for (int i = 0; i < max; i++) { }"
      ]
    }
  }
}