{
  "filename": "collections_should_not_be_passed_as_arguments_to_t_metadata.json",
  "rule_id": "collections_passed_as_arguments",
  "title": "Collections should not be passed as arguments to their own methods",
  "description": "Collections should not be passed as arguments to their own methodsAnalyze your codeintentionality-logicalreliabilityBugWhy is this an issue?More InfoPassing a collection as an argument to the collection’s own method is a code defect. Doing so might either have unexpected side effects or always have the same result.Another case is using set-like operations. For example, usingUnionbetween a list and itself will always return the same list.",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Security",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Security risk",
  "tags": [
    "security",
    "bug",
    ".net"
  ],
  "examples": {
    "noncompliant": [
      "var list = new List<int>();\n\nlist.AddRange(list);          // Noncompliant\nlist.Concat(list);            // Noncompliant\n\nlist.Union(list);             // Noncompliant: always returns list\nlist.Intersect(list);         // Noncompliant: always returns list\nlist.Except(list);            // Noncompliant: always returns empty\nlist.SequenceEqual(list);     // Noncompliant: always returns true\n\nvar set = new HashSet<int>();\nset.UnionWith(set);           // Noncompliant: no changes\nset.IntersectWith(set);       // Noncompliant: no changes\nset.ExceptWith(set);          // Noncompliant: always returns empty\nset.SymmetricExceptWith(set); // Noncompliant: always returns empty\nset.IsProperSubsetOf(set);    // Noncompliant: always returns false\nset.IsProperSupersetOf(set);  // Noncompliant: always returns false\nset.IsSubsetOf(set);          // Noncompliant: always returns true\nset.IsSupersetOf(set);        // Noncompliant: always returns true\nset.Overlaps(set);            // Noncompliant: always returns true\nset.SetEquals(set);           // Noncompliant: always returns true"
    ],
    "compliant": [
      "var list = new List<int>();\nvar copy = new List<int>(list);\n\nlist.AddRange(copy);          // Compliant: using different collection\nlist.Concat(copy);            // Compliant\n\nvar set = new HashSet<int>();\nvar otherSet = new HashSet<int>(set);\n\nset.UnionWith(otherSet);      // Compliant: using different set"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/csharp/RSPEC-2114"
  ],
  "logic": {
    "node_types": ["CallExpression"],
    "checks": [
      {
        "type": "custom_function",
        "function": "check_collection_self_reference"
      }
    ],
    "never_trigger_on": [
      "Logging statements (Console.*, Logger.*, Debug.*)",
      "String formatting ($\"\", string.Format, string.Join)",
      "Comments (// /* ///)",
      "Different variable names (list.AddRange(copy) is OK)"
    ],
    "reasoning": "Detects when a collection variable is passed as an argument to its own method (e.g., list.AddRange(list), set.UnionWith(set)). This is a code defect that either has unexpected side effects (like infinite loops in AddRange) or always returns the same predictable result (like Union returning the same list). Uses regex pattern matching to identify variable.Method(variable) calls for problematic collection methods. Excludes logging/formatting contexts where this might appear in string output.",
    "validation_examples": {
      "should_trigger": [
        "list.AddRange(list);",
        "list.Concat(list);",
        "set.UnionWith(set);",
        "set.IsSubsetOf(set);"
      ],
      "should_not_trigger": [
        "list.AddRange(copy);",
        "set.UnionWith(otherSet);",
        "Console.WriteLine($\"List: {string.Join(\\\", \\\", list.Concat(list))}\");"
      ]
    }
  }
}