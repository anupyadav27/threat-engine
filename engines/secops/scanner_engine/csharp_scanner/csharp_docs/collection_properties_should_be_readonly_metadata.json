{
  "filename": "collection_properties_should_be_readonly_metadata.json",
  "rule_id": "collection_properties_readonly",
  "title": "Collection properties should be readonly",
  "description": "Collection properties should be readonlyAnalyze your codeconsistency-conventionalmaintainabilityCode SmellWhy is this an issue?A writable collection property can be replaced by a completely different collection. Making itreadonlyprevents that while still allowing individual members to be set. If you want to allow the replacement of the whole collection the recommended pattern is to implement a method to remove all the elements (e.g.System.Collections.List<T>.Clear) and a method to populate the collection (e.g.System.Collections.List<T>.AddRange).This rule raises an issue when an externally visible writable property is of a type that implementsSystem.Collections.ICollectionorSystem.Collections.Generic.ICollection<T>.Noncompliant code exampleusing System; using System.Collections; namespace",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Security",
  "recommendation": "Remove the problematic code and refactor as needed.",
  "impact": "Security risk",
  "tags": [
    "security",
    "bug",
    ".net"
  ],
  "examples": {
    "noncompliant": [
      "public class MyClass\n{\n    public List<string> Items { get; set; } // Noncompliant - writable collection property\n}"
    ],
    "compliant": [
      "public class MyClass\n{\n    private readonly List<string> items = new List<string>();\n    \n    public IReadOnlyList<string> Items => items; // Compliant - readonly property\n    \n    public void Clear()\n    {\n        items.Clear();\n    }\n    \n    public void AddRange(IEnumerable<string> newItems)\n    {\n        items.AddRange(newItems);\n    }\n}"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/csharp/RSPEC-4004"
  ],
  "logic": {
    "custom_function": "check_collection_properties_readonly",
    "node_types": [
      "ClassDeclaration"
    ],
    "checks": [
      {
        "type": "custom_function",
        "function": "check_collection_properties_readonly",
        "message": "Collection properties should be readonly to prevent replacement of the entire collection."
      }
    ]
  }
}