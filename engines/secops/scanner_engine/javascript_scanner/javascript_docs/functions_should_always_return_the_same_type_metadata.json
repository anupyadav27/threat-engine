{
  "filename": "functions_should_always_return_the_same_type_metadata.json",
  "rule_id": "functions_always_return_same",
  "title": "Functions should always return the same type",
  "description": "Functions should always return the same typeAnalyze your codeintentionality-clearmaintainabilityCode Smellconfusingtype-dependentWhy is this an issue?More InfoUnlike strongly typed languages, JavaScript does not enforce a return type on a function. This means that different paths through a function can return different types of values.Returning different types from a function can make the code less readable and harder to understand. Maintainers may have to spend more time figuring out how the function works and what it returns.",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Security",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Security risk",
  "tags": [
    "security",
    "bug",
    "deprecated",
    "async"
  ],
  "examples": {
    "noncompliant": [
      "function foo(a) {  // Noncompliant: function returns 'boolean' or 'number'\n  if (a === 1) {\n    return true;\n  }\n  return 3;\n}"
    ],
    "compliant": [
      "function foo(a) {\n  if (a === 1) {\n    return true;\n  }\n  return false;\n}"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/javascript/RSPEC-3800"
  ],
  "logic": {
    "node_types": [
      "FunctionDeclaration",
      "FunctionExpression"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "pattern": "return\\s+(true|false);[\\s\\S]*return\\s+\\d+",
        "message": "Functions should always return the same type"
      },
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "pattern": "return\\s+\\d+;[\\s\\S]*return\\s+(true|false)",
        "message": "Functions should always return the same type"
      }
    ]
  }
}