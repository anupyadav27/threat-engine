{
  "filename": "ternary_operators_should_not_be_nested_metadata.json",
  "rule_id": "ternary_operators_nested",
  "title": "Ternary operators should not be nested",
  "description": "Ternary operators should not be nestedAnalyze your codeintentionality-clearmaintainabilityCode SmellconfusingWhy is this an issue?More InfoNested ternaries are hard to read and can make the order of operations complex to understand.function getReadableStatus(job) { return job.isRunning() ? \"Running\" : job.hasErrors() ? \"Failed\" : \"Succeeded \"; // Noncompliant }Instead, use another line to express the nested operation in a separate statement.function getReadableStatus(job) { if (job.isRunning()) { return \"Running\"; } return job.hasErrors() ? \"Failed\" : \"Succeeded\"; }ExceptionsThis rule does not apply in JSX expressions to support conditional rendering and conditional attributes as long as the nesting happens in separate JSX expression containers, i.e. JSX elements embedding JavaScript code,",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Security",
  "recommendation": "Replace with the recommended alternative approach.",
  "impact": "Security risk",
  "tags": [
    "security",
    "bug",
    "deprecated",
    "async"
  ],
  "examples": {
    "noncompliant": [
      "function getReadableStatus(job) {\n  return job.isRunning() ? \"Running\" : job.hasErrors() ? \"Failed\" : \"Succeeded \";  // Noncompliant\n}"
    ],
    "compliant": [
      "function getReadableStatus(job) {\n  if (job.isRunning()) {\n    return \"Running\";\n  }\n  return job.hasErrors() ? \"Failed\" : \"Succeeded\";\n}"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/javascript/RSPEC-3358"
  ],
  "logic": {
    "node_types": [
      "ReturnStatement",
      "ConditionalExpression",
      "ExpressionStatement",
      "VariableDeclarator", 
      "AssignmentExpression",
      "Property",
      "ArrayExpression"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "pattern": "\\?[^\\?\\r\\n]*:[^\\?\\r\\n]*\\?[^\\r\\n]*:",
        "message": "Replace with the recommended alternative approach."
      }
    ]
  }
}