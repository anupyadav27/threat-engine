{
  "filename": "unnecessary_constructors_should_be_removed_metadata.json",
  "rule_id": "unnecessary_constructors_removed",
  "title": "Unnecessary constructors should be removed",
  "description": "Unnecessary constructors should be removedAnalyze your codeintentionality-clearmaintainabilityCode SmellQuick FixIDE quick fixes available with SonarQube for IDEWhy is this an issue?More InfoIf the class declaration does not include a constructor, one is automatically created, so there is no need to provide an empty constructor, or one that just delegates to the parent class.class Foo { constructor() {} // Noncompliant, empty } class Bar extends Foo { constructor(params) { // Noncompliant: just delegates to the parent super(params); } }Instead, you can safely remove the empty constructor without affecting the functionality.class Foo {} class Bar extends Foo {}Available In:Catch issues on the fly,in your IDEDetect issues in your GitHub, Azure DevOps Services, Bitbucket Cloud, GitLab reposit",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Security",
  "recommendation": "Remove the problematic code and refactor as needed.",
  "impact": "Security risk",
  "tags": [
    "security",
    "bug",
    "deprecated",
    "async"
  ],
  "examples": {
    "noncompliant": [
      "class Foo {\n    constructor() {}  // Noncompliant, empty\n}\n\nclass Bar extends Foo {\n    constructor(params) { // Noncompliant: just delegates to the parent\n        super(params);\n    }\n}"
    ],
    "compliant": [
      "class Foo {}\n\nclass Bar extends Foo {}"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/javascript/RSPEC-6647"
  ],
  "logic": {
    "node_types": [
      "MethodDefinition"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "pattern": "constructor\\s*\\([^)]*\\)\\s*\\{\\s*(?://[^\\n]*\\n\\s*)?\\s*\\}",
        "message": "Empty constructors should be removed"
      },
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "pattern": "constructor\\s*\\(\\s*([a-zA-Z_$][a-zA-Z0-9_$]*)\\s*\\)\\s*\\{\\s*(?://[^\\n]*\\n)?\\s*super\\s*\\(\\s*\\1\\s*\\)\\s*;?\\s*(?://[^\\n]*)?\\s*\\}",
        "message": "Constructor only delegates to parent class and should be removed"
      },
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "pattern": "constructor\\s*\\(\\s*([a-zA-Z_$][a-zA-Z0-9_$]*)\\s*,\\s*([a-zA-Z_$][a-zA-Z0-9_$]*)\\s*\\)\\s*\\{\\s*(?://[^\\n]*\\n)?\\s*super\\s*\\(\\s*\\1\\s*,\\s*\\2\\s*\\)\\s*;?\\s*(?://[^\\n]*)?\\s*\\}",
        "message": "Constructor only delegates to parent class and should be removed"
      },
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "pattern": "constructor\\s*\\(\\s*\\.\\.\\.\\s*([a-zA-Z_$][a-zA-Z0-9_$]*)\\s*\\)\\s*\\{\\s*(?://[^\\n]*\\n)?\\s*super\\s*\\(\\s*\\.\\.\\.\\s*\\1\\s*\\)\\s*;?\\s*(?://[^\\n]*)?\\s*\\}",
        "message": "Constructor only delegates to parent class with rest parameters and should be removed"
      }
    ]
  }
}