{
  "filename": "regular_expressions_should_be_syntactically_valid_metadata.json",
  "rule_id": "regular_expressions_syntactically_valid",
  "title": "Regular expressions should be syntactically valid",
  "description": "Regular expressions should be syntactically validAnalyze your codeintentionality-logicalreliabilityBugtype-dependentWhy is this an issue?Regular expressions have their own syntax that is understood by regular expression engines. Those engines will throw an exception at runtime if they are given a regular expression that does not conform to that syntax.To avoid syntax errors, special characters should be escaped with backslashes when they are intended to be matched literally and references to capturing groups should use the correctly spelled name or number of the group.To match a literal string, rather than a regular expression, either all special characters should be escaped or methods that don\u2019t use regular expressions should be used.Noncompliant code examplenew RegExp(\"([\"); str.match(\"(",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Security",
  "recommendation": "Refactor to eliminate the flagged pattern.",
  "impact": "Security risk",
  "tags": [
    "security",
    "bug",
    "deprecated",
    "async"
  ],
  "examples": {
    "noncompliant": [
      "new RegExp(\"([\");",
      "str.match(\"([\");",
      "new RegExp(\"(\");",
      "new RegExp(\"[\");"
    ],
    "compliant": [
      "new RegExp(\"\\\\(\\\\[\");",
      "str.match(\"\\\\(\\\\[\");",
      "str.replace(\"([\", \"{\");"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/javascript/RSPEC-5856"
  ],
  "logic": {
    "node_types": [
      "Literal", 
      "NewExpression", 
      "CallExpression"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": ["source"],
        "patterns": [
          "new\\s+RegExp\\s*\\(\\s*['\"]\\s*\\([^)]*\\s*['\"]",
          "new\\s+RegExp\\s*\\(\\s*['\"]\\s*\\[[^\\]]*\\s*['\"]", 
          "new\\s+RegExp\\s*\\(\\s*['\"][^'\"]*[^\\\\]\\([^)]*['\"]\\s*[,)]",
          "new\\s+RegExp\\s*\\(\\s*['\"][^'\"]*[^\\\\]\\[[^\\]]*['\"]\\s*[,)]",
          "\\.match\\s*\\(\\s*['\"][^'\"]*[^\\\\]\\([^)]*['\"]\\s*\\)",
          "\\.match\\s*\\(\\s*['\"][^'\"]*[^\\\\]\\[[^\\]]*['\"]\\s*\\)",
          "\\.replace\\s*\\(\\s*['\"][^'\"]*[^\\\\]\\([^)]*['\"]",
          "\\.replace\\s*\\(\\s*['\"][^'\"]*[^\\\\]\\[[^\\]]*['\"]"
        ],
        "message": "Regular expression contains invalid syntax - check brackets and parentheses"
      }
    ]
  }
}