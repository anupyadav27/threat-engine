{
  "filename": "variables_should_not_be_accessed_outside_of_their__metadata.json",
  "rule_id": "variables_accessed_outside_their",
  "title": "Variables should not be accessed outside of their scope",
  "description": "Variables should not be accessed outside of their scope. Analyze your code intentionality-logical reliability. Bug symbolic-execution. Accessing local objects outside of their scope (for example, via a pointer taken inside the scope) has undefined behavior. This rule flags such access for local variables and lifetime-extended temporaries. Why is this an issue? How can I fix it? More Info: Local variables in C++ are attached to the scope and destroyed when the end of the scope is reached. Any access to a variable outside of their scope has undefined behavior. Such access occurs, for example, when the address of a variable is stored in a pointer that is later dereferenced.",
  "status": "ready",
  "defaultSeverity": "Info",
  "category": "Reliability",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Potential bug",
  "tags": [
    "bug"
  ],
  "examples": {
    "noncompliant": [
      "int func() {\n  int* ptr = nullptr;\n  {\n    int i = 10;\n    ptr = &i;\n  } // variable i goes out of scope here\n  *ptr = 10; // Noncompliant: writing to out-of-scope-variable\n}"
    ],
    "compliant": [
      "int func() {   int* ptr = nullptr;   int result;   {     int i = 10;     result = i; // Compliant: use value before scope ends   }   return result; }  // Or allocate on heap int* createInt() {   int* ptr = malloc(sizeof(int)); // Compliant: heap allocation   *ptr = 42;   return ptr; // valid outside scope }"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/c/RSPEC-6655"
  ],
  "logic": {
    "node_types": [
      "AssignmentExpression",
      "ReturnStatement", 
      "UnaryExpression"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "patterns": [
          "return\\s+&[a-zA-Z_][a-zA-Z0-9_]*(?:\\s*;|\\s*$)",
          "(?:^|\\n)\\s*}\\s*\\n[^{]*\\*[a-zA-Z_][a-zA-Z0-9_]*\\s*="
        ],
        "message": "Variables should not be accessed outside of their scope. Review and fix according to the rule guidelines."
      }
    ]
  }
}