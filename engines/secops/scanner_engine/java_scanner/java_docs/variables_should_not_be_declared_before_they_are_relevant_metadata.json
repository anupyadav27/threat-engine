{
  "filename": "variables_should_not_be_declared_before_they_are_relevant_metadata.json",
  "rule_id": "code_quality_variable_declaration",
  "title": "Avoid declaring variables before they are relevant",
  "description": "Declaring variables as close as possible to where they are used improves code clarity, particularly in methods with early returns and exception handling.",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "code_quality",
  "recommendation": "Declare variables after they are first used.",
  "impact": "Improves code readability and maintainability.",
  "examples": {
    "noncompliant": [
      "public boolean isConditionMet(int a, int b) {\n  int difference = a - b;\n  MyClass foo = new MyClass(a);  // Noncompliant; not used before early return\n\n  if (difference < 0) {\n    return false;\n  }\n\n  // ...\n\n  if (foo.doTheThing()) {\n    return true;\n  }\n  return false;"
    ],
    "compliant": [
      "public boolean isConditionMet(int a, int b) {\n  int difference = a - b;\n\n  if (difference < 0) {\n    return false;\n  }\n\n  // ...\n\n  MyClass foo = new MyClass(a);\n  if (foo.doTheThing()) {\n    return true;\n  }\n  return false;"
    ]
  },
  "references": [],
  "logic": {
      "node_types": [
        "MethodInvocation"
      ],
      "checks": [
        {
          "type": "regex_match",
          "property_path": [
            "source"
          ],
          "pattern": "\\bMyClass\\s+foo\\s*=\\s*new\\s+MyClass\\(.*\\);"
        }
      ]
  }
}