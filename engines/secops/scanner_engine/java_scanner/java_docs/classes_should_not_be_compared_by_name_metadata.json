{
  "filename": "classes_should_not_be_compared_by_name_metadata.json",
  "rule_id": "style_class_comparison",
  "title": "Avoid comparing classes by name",
  "description": "Comparing classes by name can lead to security vulnerabilities and should be avoided. Use the `instanceof` operator or `Class.isAssignableFrom()` method instead.",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Style",
  "recommendation": "Use `instanceof` operator or `Class.isAssignableFrom()` method",
  "impact": "Prevent potential security vulnerabilities",
  "examples": {
    "noncompliant": [
      "if (\"Pear\".equals(item.getClass().getSimpleName())) {  // Noncompliant\n      return true;  // Results in throwing away week-old computers\n    }"
    ],
    "compliant": [
      "if (item instanceof food.Pear) {\n      return true;\n    }"
    ]
  },
  "references": [],
  "logic": {
    "node_types": [
      "MethodInvocation"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "patterns": [
          "getClass\\(\\)\\.getSimpleName\\(\\)",
          "getClass\\(\\)\\.getName\\(\\)"
        ],
        "message": "Use instanceof operator or Class.isAssignableFrom() method instead of comparing classes by name"
      }
    ]
  }
}