{
  "filename": "keywords_shall_not_be_used_as_macros_identifiers_metadata.json",
  "rule_id": "keywords_shall_as_macros",
  "title": "Keywords shall not be used as macros identifiers",
  "description": "Keywords shall not be used as macros identifiersAnalyze your codeintentionality-completemaintainabilityCode Smellbased-on-misraunpredictablepitfallWhy is this an issue?More InfoIn programming languages, keywords have a special meaning and are reserved for the language. Hence, it is a bad idea to define macros with keywords as macro identifiers as it can easily lead to undefined behavior:The same object might be defined differently in different places, which violates the One Definition RuleIf you include any header from the standard library, it is undefined behavior to define such macrosAdditionally, it is awkward for anyone reading the code to have a keyword that means something different.Noncompliant code example#define int some_other_type // Noncompliant #include <stdlib.h>Available In:C",
  "status": "ready",
  "defaultSeverity": "Info",
  "category": "General",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Code readability problem",
  "tags": [
    "cpp"
  ],
  "examples": {
    "noncompliant": [
      "#define int some_other_type // Noncompliant\n#include <stdlib.h>",
      "#define class MyClass // Noncompliant - 'class' is a C++ keyword\n#define void VOID_TYPE // Noncompliant - 'void' is a keyword\n#define return my_return // Noncompliant - 'return' is a keyword"
    ],
    "compliant": [
      "#define INTEGER_TYPE some_other_type // Compliant\n#include <stdlib.h>",
      "#define MY_CLASS MyClass // Compliant\n#define VOID_TYPE void // Compliant\n#define RETURN_MACRO return // Compliant (but still not recommended)"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/cpp/RSPEC-5266"
  ],
  "logic": {
    "node_types": [
      "TranslationUnit"
    ],
    "checks": [
      {
        "type": "custom_function",
        "function": "check_macro_keywords",
        "message": "C++ keywords should not be used as macro identifiers"
      }
    ]
  }
}