{
  "filename": "empty_should_be_preferred_to_test_for_emptiness_metadata.json",
  "rule_id": "empty_preferred_test_emptiness",
  "title": "empty() should be preferred to test for emptiness",
  "description": "\"empty()\" should be used to test for emptinessAnalyze your codeintentionality-clearmaintainabilityCode SmellQuick FixIDE quick fixes available with SonarQube for IDEcppcoreguidelinesclumsyWhy is this an issue?More InfoWhen you callempty(), it clearly communicates the code’s intention, which is to check if the collection is empty. Usingsize() == 0for this purpose is less direct and makes the code slightly more complex.Moreover, in the standard library, depending on the implementation, thesize()method can have a time complexity ofO(n)wherenis the number of elements in the collection. On the other hand,empty()simply checks if there is at least one element in the collection, which is a constant time operation,O(1).",
  "status": "ready",
  "defaultSeverity": "Info",
  "category": "Maintainability",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Maintainability issue",
  "tags": [
    "cpp"
  ],
  "examples": {
    "noncompliant": [
      "void badEmptinessCheck() {\n    std::vector<int> vec;\n    std::string str;\n    std::list<double> lst;\n    \n    // Noncompliant: using size() == 0 instead of empty()\n    if (vec.size() == 0) {\n        std::cout << \"Vector is empty\" << std::endl;\n    }\n    \n    // Noncompliant: using size() comparison\n    if (str.size() == 0) {\n        std::cout << \"String is empty\" << std::endl;\n    }\n    \n    // Noncompliant: size() with comparison operator\n    bool isEmpty = (lst.size() == 0);\n}"
    ],
    "compliant": [
      "void goodEmptinessCheck() {\n    std::vector<int> vec;\n    std::string str;\n    std::list<double> lst;\n    \n    // Compliant: using empty() method\n    if (vec.empty()) {\n        std::cout << \"Vector is empty\" << std::endl;\n    }\n    \n    // Compliant: clear intention with empty()\n    if (str.empty()) {\n        std::cout << \"String is empty\" << std::endl;\n    }\n    \n    // Compliant: efficient and clear\n    bool isEmpty = lst.empty();\n    \n    // Compliant: works with all standard containers\n    std::map<int, std::string> map;\n    if (!map.empty()) {\n        process(map);\n    }\n}"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/cpp/RSPEC-1155"
  ],
  "logic": {
    "node_types": [
      "*"
    ],
    "checks": [
      {
        "type": "custom_function",
        "function": "empty_preferred_test_emptiness",
        "message": "Use empty() method instead of size() == 0 to test for emptiness."
      }
    ]
  }
}