{
  "filename": "macros_should_be_avoided_to_define_constants_metadata.json",
  "rule_id": "macros_avoided_define_constants",
  "title": "Macros should be avoided to define constants",
  "description": "Macros should not be used to define constantsAnalyze your codeintentionality-clearmaintainabilityCode Smellcppcoreguidelinesbad-practiceclumsyWhy is this an issue?More InfoA macro is a textual replacement, which means that it’s not respecting the type system, it’s not respecting scoping rules…​ There is no reason not to use a constant instead.Most of the time, a macro can be replaced by aconstexprdeclaration (a constant that is guaranteed to be computed during compilation). If your compiler is too old to properly handleconstexpr, you may useconstinstead.If you have a series of related integer macros, you might also consider replacing them by anenum.Noncompliant code example#define MAX_MEMORY 640 // Noncompliant #define LEFT 0 // Noncompliant #define RIGHT 1 // Noncompliant #define JUMP 2 /",
  "status": "ready",
  "defaultSeverity": "Info",
  "category": "General",
  "recommendation": "Replace with the recommended alternative approach.",
  "impact": "Code readability problem",
  "tags": [
    "cpp"
  ],
  "examples": {
    "noncompliant": [
      "#define MAX_MEMORY 640 // Noncompliant: using macro for constant",
      "#define PI 3.14159 // Noncompliant: should use constexpr",
      "#define LEFT 0 // Noncompliant\n#define RIGHT 1 // Noncompliant\n#define JUMP 2 // Noncompliant"
    ],
    "compliant": [
      "constexpr int MAX_MEMORY = 640; // Compliant: using constexpr",
      "constexpr double PI = 3.14159; // Compliant: type-safe constant",
      "enum Direction { LEFT = 0, RIGHT = 1, JUMP = 2 }; // Compliant: using enum for related constants"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/cpp/RSPEC-5028"
  ],
  "logic": {
    "node_types": [
      "*"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "patterns": [
          "#define\\s+\\w+\\s+\\d+",
          "#define\\s+\\w+\\s+\"[^\"]*\"",
          "#define\\s+\\w+\\s+'[^']*'"
        ],
        "message": "Avoid using macros to define constants. Use const variables or constexpr instead."
      }
    ]
  }
}