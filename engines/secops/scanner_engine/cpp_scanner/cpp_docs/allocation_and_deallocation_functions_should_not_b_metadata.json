{
  "filename": "allocation_and_deallocation_functions_should_not_b_metadata.json",
  "rule_id": "allocation_deallocation_functions_explicitly",
  "title": "Allocation and deallocation functions should not be explicitly declared static",
  "description": "Allocation and deallocation functions should not be explicitly declared \"static\"Analyze your codeintentionality-clearmaintainabilityCode SmellWhy is this an issue?More InfoAllocation functions are alwaysstatic. Explicitly declaring such a functionstaticneedlessly clutters the code.Noncompliant code examplestruct S { static void* operator new(std::size_t); // Noncompliant; static is redundant static void operator delete(void*); // Noncompliant; static is redundant };Compliant solutionstruct S { void* operator new(std::size_t); void operator delete(void*); };Available In:Catch issues on the fly,in your IDEDetect issues in your GitHub, Azure DevOps Services, Bitbucket Cloud, GitLab repositoriesAnalyze code in youron-premise CIDeveloper E",
  "status": "ready",
  "defaultSeverity": "Info",
  "category": "General",
  "recommendation": "Remove the problematic code and refactor as needed.",
  "impact": "Code readability problem",
  "tags": [
    "deprecated"
  ],
  "examples": {
    "noncompliant": [
      "struct S {\n    static void* operator new(std::size_t); // Noncompliant: static is redundant\n    static void operator delete(void*); // Noncompliant: static is redundant\n};"
    ],
    "compliant": [
      "struct S {\n    void* operator new(std::size_t); // Compliant: no explicit static\n    void operator delete(void*); // Compliant: no explicit static\n};"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/cpp/RSPEC-3541"
  ],
  "logic": {
    "node_types": [
      "StructDeclaration",
      "ClassDeclaration"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "patterns": [
          "\\bstatic\\s+[^;]*\\boperator\\s+(new|delete)\\b[^;]*;"
        ],
        "message": "Remove explicit 'static' keyword from allocation/deallocation functions - they are implicitly static"
      }
    ]
  }
}