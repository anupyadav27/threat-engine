{
  "filename": "atof_atoi_and_atol_from_stdlibh_should_be_avoided_metadata.json",
  "rule_id": "atof_atoi_atol_from",
  "title": "atof, atoi and atol from <stdlib.h> should be avoided",
  "description": "\"atof\", \"atoi\" and \"atol\" from <stdlib.h> should not be usedAnalyze your codeconsistency-conventionalreliabilityBugbased-on-misracertWhy is this an issue?More Info<stdlib.h>'satof,atoi, andatolfunctions, which convert strings to numbers, have undefined behavior when the strings cannot be converted, and should therefore be avoided.Noncompliant code exampleint converter (const char * numstr) { return atoi(numstr); // Noncompliant }Compliant solutionint converter (const char * numstr) { return strtol(numstr, NULL, 10); }Available In:Catch issues on the fly,in your IDEDetect issues in your GitHub, Azure DevOps Services, Bitbucket Cloud, GitLab repositoriesAnalyze code in youron-premise CIDeveloper EditionAvailable Since9.1©2025SonarSource Sàrl. All rights reserved.Privacy Policy|Cookie Policy|",
  "status": "ready",
  "defaultSeverity": "Info",
  "category": "Reliability",
  "recommendation": "Refactor to eliminate the flagged pattern.",
  "impact": "Potential bug",
  "tags": [
    "cert",
    "bug",
    "convention"
  ],
  "examples": {
    "noncompliant": [
      "int converter(const char* numstr) {\n    return atoi(numstr);  // Noncompliant: undefined behavior on conversion failure\n}",
      "double parseFloat(const char* str) {\n    return atof(str);     // Noncompliant: no error handling\n}",
      "long parseLong(const char* input) {\n    return atol(input);   // Noncompliant: unsafe conversion\n}",
      "int value = atoi(argv[1]);  // Noncompliant: command line argument conversion"
    ],
    "compliant": [
      "int converter(const char* numstr) {\n    char* endptr;\n    return strtol(numstr, &endptr, 10);  // Compliant: safer alternative\n}",
      "double parseFloat(const char* str) {\n    char* endptr;\n    return strtod(str, &endptr);         // Compliant: with error checking\n}",
      "long parseLong(const char* input) {\n    char* endptr;\n    long result = strtol(input, &endptr, 10);\n    if (*endptr != '\\0') { /* handle error */ }\n    return result;  // Compliant: proper error handling\n}"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/c/RSPEC-989"
  ],
  "logic": {
    "node_types": ["FunctionDefinition", "TranslationUnit"],
    "checks": [
      {
        "type": "custom_function",
        "function": "check_atof_atoi_atol_usage",
        "message": "Unsafe string conversion function detected. Replace with safer alternatives: atoi() -> strtol(), atof() -> strtod(), atol() -> strtol() for proper error handling."
      }
    ]
  }
}