{
  "filename": "using_pseudorandom_number_generators_prngs_is_secu_metadata.json",
  "rule_id": "using_pseudorandom_number_generators",
  "title": "Using pseudorandom number generators (PRNGs) is security-sensitive",
  "description": "Using pseudorandom number generators (PRNGs) is security-sensitiveAnalyze your coderesponsibility-trustworthysecuritySecurity HotspotcwecertPRNGs are algorithms that produce sequences of numbers that only approximate true randomness. While they are suitable for applications like simulations or modeling, they are not appropriate for security-sensitive contexts because their outputs can be predictable if the internal state is known.In contrast, cryptographically secure pseudorandom number generators (CSPRNGs) are designed to be secure against prediction attacks. CSPRNGs use cryptographic algorithms to ensure that the generated sequences are not only random but also unpredictable, even if part of the sequence or the internal state becomes known.",
  "status": "ready",
  "defaultSeverity": "Critical",
  "category": "Security",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Security risk",
  "tags": [
    "security",
    "cert",
    "cwe",
    "owasp",
    "design"
  ],
  "examples": {
    "noncompliant": [
      "#include <stdlib.h>\n\nint generate_session_token() {\n    return rand(); // Noncompliant: using predictable PRNG for security\n}\n\nint generate_password_salt() {\n    srand(time(NULL));\n    return rand() % 1000000; // Noncompliant: weak randomness for crypto\n}"
    ],
    "compliant": [
      "#include <sodium.h>\n\nint generate_session_token() {\n    uint32_t token;\n    randombytes_buf(&token, sizeof(token)); // Compliant: cryptographically secure\n    return token;\n}\n\n// Alternative with /dev/urandom\n#include <fcntl.h>\nint secure_random() {\n    int fd = open(\"/dev/urandom\", O_RDONLY);\n    uint32_t value;\n    read(fd, &value, sizeof(value)); // Compliant: OS entropy source\n    close(fd);\n    return value;\n}"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/c/RSPEC-2245"
  ],
  "logic": {
    "node_types": [
      "CallExpression"
    ],
    "checks": [
      {
        "type": "forbidden_value",
        "property_path": [
          "name"
        ],
        "forbidden_values": [
          "rand",
          "random",
          "srand",
          "srandom",
          "drand48",
          "lrand48", 
          "mrand48",
          "nrand48",
          "jrand48",
          "erand48",
          "srand48",
          "seed48",
          "lcong48",
          "rand_r",
          "random_r",
          "srandom_r",
          "initstate_r",
          "setstate_r",
          "GetTickCount",
          "QueryPerformanceCounter",
          "timeGetTime",
          "clock",
          "getpid",
          "getppid",
          "gettimeofday"
        ],
        "message": "Using pseudorandom number generators (PRNGs) is security-sensitive. Review and fix according to the rule guidelines."
      }
    ]
  }
}