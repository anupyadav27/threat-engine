{
  "filename": "operands_of_and_should_be_primary_c_or_postfix_c_e_metadata.json",
  "rule_id": "operands_primary_c_postfix",
  "title": "Operands of && and || should be primary (C) or postfix (C++) expressions",
  "description": "Operands of \"&&\" and \"||\" should be primary (C) or postfix (C++) expressionsAnalyze your codeintentionality-clearmaintainabilityCode Smellbased-on-misraWhy is this an issue?More InfoThe effect of this rule is to require that operands are appropriately parenthesized. Parentheses are important in this situation both for readability of code and for ensuring that the behavior is as the developer intended.Where an expression consists of either a sequence of only logical&&or a sequence of logical||, extra parentheses are not required.Noncompliant code exampleif (x == 0 && ishigh); // Noncompliant if (x || y || z); if (x || y && z); // Noncompliant if (x && !y); // Noncompliant if (is_odd(y) && x); if ((x > c1) && (y > c2) && (z > c3)); if ((x > c1) && (y > c2) || (z > c3)); // NoncompliantCompli",
  "status": "ready",
  "defaultSeverity": "Info",
  "category": "Maintainability",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Maintainability issue",
  "tags": [
    "c"
  ],
  "examples": {
    "noncompliant": [
      "if (x == 0 && ishigh); // Noncompliant: missing parentheses",
      "if (x || y && z); // Noncompliant: mixed operators without parentheses",
      "if (x && !y); // Noncompliant: unary operator without parentheses",
      "if ((x > c1) && (y > c2) || (z > c3)); // Noncompliant: mixed && and ||"
    ],
    "compliant": [
      "if ((x == 0) && ishigh); // Compliant: properly parenthesized",
      "if (x || (y && z)); // Compliant: precedence made clear",
      "if (x && (!y)); // Compliant: unary operator parenthesized",
      "if (((x > c1) && (y > c2)) || (z > c3)); // Compliant: clear precedence",
      "if (x || y || z); // Compliant: sequence of same operators"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/c/RSPEC-868"
  ],
  "logic": {
    "node_types": ["BinaryExpression", "ConditionalStatement"],
    "checks": [
      {
        "type": "regex_match",
        "property_path": ["source"],
        "patterns": ["[^(]\\s*&&\\s*[^(]", "[^(]\\s*\\|\\|\\s*[^(]", "&&.*\\|\\|", "\\|\\|.*&&"],
        "message": "Operands of && and || should be primary (C) or postfix (C++) expressions. Add parentheses to clarify precedence."
      }
    ]
  }
}