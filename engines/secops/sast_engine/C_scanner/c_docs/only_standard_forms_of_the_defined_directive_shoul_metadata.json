{
  "filename": "only_standard_forms_of_the_defined_directive_shoul_metadata.json",
  "rule_id": "only_standard_forms_defined",
  "title": "Only standard forms of the defined directive should be preferred",
  "description": "Only standard forms of the \"defined\" directive should be usedAnalyze your codeconsistency-conventionalmaintainabilityCode Smellbased-on-misrabad-practiceWhy is this an issue?More InfoThedefinedpreprocessing directive is used in the context of#ifand#elifexpressions to see whether a given identifier has been defined as a macro. It returns a value of 0 (false) or 1 (true), and has two valid forms,defined IDENTIFIERanddefined ( IDENTIFIER ). Since it is essentially a macro existence check, it cannot take expressions as arguments.Note that since#if defined AN_IDENTIFIERis equivalent to#ifdef AN_IDENTIFIERdefinedis most useful when there are multiple arguments to check, E.G.#if defined AAA || defined BBBNoncompliant code example#if defined ( X > Y ) // Noncompliant; expressions not allowedCompli",
  "status": "ready",
  "defaultSeverity": "Info",
  "category": "General",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Code readability problem",
  "tags": [
    "convention"
  ],
  "examples": {
    "noncompliant": [
      "#if defined(X > Y) // Noncompliant: expressions not allowed in defined",
      "#if defined X + Y // Noncompliant: arithmetic with defined",
      "#if defined(A && B) // Noncompliant: logical expression in defined"
    ],
    "compliant": [
      "#if defined(X) // Compliant: simple identifier",
      "#if defined X // Compliant: identifier without parentheses",
      "#if defined(AAA) || defined(BBB) // Compliant: multiple defined checks",
      "#ifdef X // Compliant: alternative form",
      "#ifndef Y // Compliant: alternative form"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/c/RSPEC-969"
  ],
  "logic": {
    "node_types": [
      "PreprocessorDirective"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "patterns": [
          "#\\s*(?:if|elif)\\s+defined\\s*\\([^)]*[<>=!+*/%&|^\\s-][^)]*\\)"
        ],
        "message": "Only standard forms of the defined directive should be used. Expressions (operators) are not allowed inside defined() parentheses. Use only: defined(IDENTIFIER) or defined IDENTIFIER"
      },
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "patterns": [
          "#\\s*(?:if|elif)\\s+defined\\s+[A-Za-z_][A-Za-z0-9_]*\\s*[+\\-*/%<>=!]"
        ],
        "message": "Only standard forms of the defined directive should be used. Arithmetic or comparison operations after defined are not allowed."
      },
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "patterns": [
          "[+\\-*/%<>=!]\\s*defined"
        ],
        "message": "Only standard forms of the defined directive should be used. Arithmetic or comparison operations before defined are not allowed."
      }
    ]
  }
}