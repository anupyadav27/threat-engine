{
  "filename": "wait_multiple_locks_held_metadata.json",
  "rule_id": "VULNERABILITY_AVOID_WAIT_MULTIPLE_LOCKS",
  "title": "Avoid waiting when multiple locks are held",
  "description": "When two locks are held simultaneously, a wait call only releases one of them. The other will be held until some other thread requests a lock on the awaited object. If no unrelated code tries to lock on that object, then all other threads will be locked out, resulting in a deadlock.",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "CONCURRENCY",
  "recommendation": "Refactor the code to avoid holding multiple locks simultaneously.",
  "impact": "Can lead to deadlocks and unresponsive applications.",
  "examples": {
    "noncompliant": [],
    "compliant": []
  },
  "references": [
    "https://rules.sonarsource.com/java/RSPEC-3046"
  ],
  "logic": {
    "node_types": [
      "Class"
    ],
    "checks": [
      {
          "type": "regex_match",
          "property_path": ["source"],
          "pattern": "synchronized\\s*\\(.*\\)\\s*\\{[^}]*synchronized\\s*\\(.*\\)\\s*\\{[^}]*wait\\s*\\(.*\\)",
          "message": "Refactor the code to avoid holding multiple locks simultaneously."
      }
    ]
  }
}