{
  "filename": "strings_should_not_be_concatenated_using_plus_in_a_loop_metadata.json",
  "rule_id": "code_quality_string_manipulation_loop",
  "title": "Avoid string concatenation in loops using '+'",
  "description": "Strings are immutable objects, so concatenation doesn't simply add the new String to the end of the existing string. Instead, in each loop iteration, the first String is converted to an intermediate object type, the second string is appended, and then the intermediate object is converted back to a String. Further, performance of these intermediate operations degrades as the String gets longer. Therefore, the use of StringBuilder is preferred.",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Code Quality",
  "recommendation": "Use StringBuilder",
  "impact": "Improves performance and reduces memory usage",
  "examples": {
    "noncompliant": [
      "String str = \"\";\nfor (int i = 0; i < arrayOfStrings.length ; ++i) {\n  str = str + arrayOfStrings[i];\n}"
    ],
    "compliant": [
      "StringBuilder bld = new StringBuilder();\n  for (int i = 0; i < arrayOfStrings.length; ++i) {\n    bld.append(arrayOfStrings[i]);\n  }\n  String str = bld.toString();"
    ]
  },
  "references": [],
  "logic": {
    "node_types": [
      "ForStatement"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "patterns": [
          "\\+.*\\w+"
        ],
        "message": "Use StringBuilder"
      }
    ]
  }
}