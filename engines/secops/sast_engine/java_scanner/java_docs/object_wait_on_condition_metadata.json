{
  "filename": "object-wait-on-condition-metadata.json",
  "rule_id": "java_method_call_misuse_on_condition",
  "title": "Avoid calling Object.wait on objects implementing java.util.concurrent.locks.Condition",
  "description": "Using Object.wait on objects implementing Condition interface is contradictory and should be avoided. Use Condition.await instead.",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Java Method Call Misuse",
  "recommendation": "Use Condition.await instead of Object.wait",
  "impact": "Improves code readability and adherence to best practices",
  "examples": {
    "noncompliant": [
      "void doSomething(Condition condition) {\n    condition.wait(); // Noncompliant, Object.wait is called\n}"
    ],
    "compliant": [
      "void doSomething(Condition condition) {\n    condition.await(); // Compliant, Condition.await is called\n}"
    ]
  },
  "references": [],
  "logic": {
    "node_types": [
      "Class"
    ],
    "checks": [
      {
        "type": "custom_function",
        "function": "check_java_method_call_misuse_on_condition",
        "message": "Use Condition.await instead of Object.wait"
      }
    ]
  }
}