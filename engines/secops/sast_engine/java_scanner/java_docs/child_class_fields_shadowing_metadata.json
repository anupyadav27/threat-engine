{
  "filename": "child_class_fields_shadowing_metadata.json",
  "rule_id": "VULNERABILITY_IGNORE_SHADOWING_FIELDS_IN_CLASS_HIERARCHY",
  "title": "Avoid shadowing parent class fields in child classes",
  "description": "Having a variable with the same name in two unrelated classes is fine, but do the same thing within a class hierarchy and you'll get confusion at best, chaos at worst.\n\nThis rule ignores same-name fields that are static in both the parent and child classes. This rule ignores private parent class fields, but in all other such cases, the child class field should be renamed.",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "NAMING_CONVENTIONS",
  "recommendation": "Rename the child class field",
  "impact": "Reduces confusion and improves code readability",
  "examples": {
    "noncompliant": [],
    "compliant": []
  },
  "references": [],
  "logic": {
    "node_types": [
      "Class"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "patterns": [
          "class\\s+\\w+\\s+extends\\s+\\w+.*\\n.*\\w+\\s+\\w+;"
        ],
        "message": "Rename the child class field"
      }
    ]
  }
}