{
  "filename": "url_hash_code_avoidance_metadata.json",
  "rule_id": "java_avoid_url_hash_code",
  "title": "Avoid using URL.hashCode and URL.equals",
  "description": "Avoid using URL.hashCode and URL.equals to prevent potential performance issues related to name service lookups. Use URI instead and convert to URL when needed.",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Performance",
  "recommendation": "Use URI instead and convert to URL when needed.",
  "impact": "Avoids potential performance issues related to name service lookups.",
  "examples": {
    "noncompliant": [],
    "compliant": []
  },
  "logic": {
    "node_types": ["Class"],
    "checks": [
      {
        "type": "regex_match",
        "property_path": ["source"],
        "pattern": "URL\\s*\\.\\s*(hashCode|equals)\\s*\\("
      },
      {
        "type": "regex_match",
        "property_path": ["source"],
        "patterns": ["\\.equals\\s*\\(|=="],
        "message": "Use URI instead and convert to URL when needed."
      }
    ]
  }
}