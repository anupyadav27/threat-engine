{
  "filename": "min_and_max_combination_metadata.json",
  "rule_id": "VULNERABILITY_CHECK_BOUNDS",
  "title": "Incorrect use of Math.min() and Math.max() for bounds checking",
  "description": "When using Math.min() and Math.max() together for bounds checking, it's important to feed the right operands to each method. Math.min() should be used with the upper end of the range being checked, and Math.max() should be used with the lower end of the range. Get it backwards, and the result will always be the same end of the range.\n\nSwapping method min() and max() invocations without changing parameters.\n\nor swapping bounds UPPER and LOWER used as parameters without changing the invoked methods.",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "VULNERABILITY",
  "recommendation": "Use Math.min() with the upper end of the range and Math.max() with the lower end of the range.",
  "impact": "Potential security vulnerability due to incorrect bounds checking.",
  "examples": {
    "noncompliant": [],
    "compliant": []
  },
  "references": [],
  "logic": {
    "node_types": [
      "Class"
    ],
    "checks": [
      {
        "type": "custom_function",
        "function": "check_vulnerability_check_bounds",
        "message": "Use Math.min() with the upper end of the range and Math.max() with the lower end of the range."
      }
    ]
  }
}