{
  "filename": "methods_should_not_call_same_class_methods_with_incompatible_transactional_values_metadata.json",
  "rule_id": "VULNERABILITY_CONSTANT_ISSUE_S2229",
  "title": "Methods should not call same-class methods with incompatible transactional values",
  "description": "Transactional methods have a propagation type parameter in the @Transaction annotation that specifies the requirements about the transactional context in which the method can be called and how it creates, appends, or suspends an ongoing transaction.\n\nWhen an instance that contains transactional methods is injected, Spring uses proxy objects to wrap these methods with the actual transaction code.\n\nHowever, if a transactional method is called from another method in the same class, the this argument is used as the receiver instance instead of the injected proxy object, which bypasses the wrapper code. This results in specific transitions from one transactional method to another, which are not allowed:",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "VULNERABILITY",
  "recommendation": "Avoid calling transactional methods from other transactional methods within the same class.",
  "impact": "Potential data inconsistency or transaction rollback issues.",
  "examples": {
    "noncompliant": [],
    "compliant": []
  },
  "references": [
    "https://rules.sonarsource.com/java/RSPEC-2229"
  ],
  "logic": {
    "node_types": [
      "Class"
    ],
    "checks": [
      {
        "type": "custom_function",
        "function": "check_vulnerability_constant_issue_s2229",
        "message": "Avoid calling transactional methods from other transactional methods within the same class."
      }
    ]
  }
}