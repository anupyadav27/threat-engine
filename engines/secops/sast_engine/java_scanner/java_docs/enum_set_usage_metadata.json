{
  "filename": "enum_set_usage_metadata.json",
  "rule_id": "java_replace_set_enumset_usage",
  "title": "Replace Sets with Enum Values using EnumSet",
  "description": "When all the elements in a Set are values from the same enum, the Set can be replaced with an EnumSet, which can be much more efficient than other sets because the underlying data structure is a simple bitmap.",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Performance",
  "recommendation": "Replace Set with EnumSet",
  "impact": "Improves performance by using a more efficient data structure",
  "examples": {
    "noncompliant": [
      "public void doSomething() {\n    Set<COLOR> warm = new HashSet<COLOR>();\n    warm.add(COLOR.RED);\n    warm.add(COLOR.ORANGE);\n  }"
    ],
    "compliant": [
      "public void doSomething() {\n    Set<COLOR> warm = EnumSet.of(COLOR.RED, COLOR.ORANGE);\n  }"
    ]
  },
  "references": [],
  "logic": {
    "node_types": ["Class"],
    "checks": [{
      "type": "regex_match",
      "property_path": ["source"],
      "patterns": ["Set<\\w+>\\s+\\w+\\s*=\\s*new\\s+HashSet<\\w+>\\(\\)"],
      "message": "Replace Set with EnumSet for enum values to improve performance"
    }]
  }
}