{
  "filename": "enum_members_with_implicit_values_should_not_colli_metadata.json",
  "rule_id": "enum_members_implicit_values",
  "title": "enum members with implicit values should not collide with other enumerators",
  "description": "In an enumeration, it is possible to have several enumerators with the same value. This can be useful in many circumstances. But if that happens without an explicit choice from the developer, it can also lead to errors that are hard to diagnose.",
  "status": "ready",
  "defaultSeverity": "Info",
  "category": "Reliability",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Code readability problem",
  "tags": [
    "cpp"
  ],
  "examples": {
    "noncompliant": [
      "enum color {\n  red = 3,\n  blue,      // blue = 4 (implicit)\n  green,     // green = 5 (implicit)\n  yellow = 5 // Noncompliant: yellow explicitly set to 5, collides with implicit green value\n};",
      "enum status {\n  IDLE = 0,\n  RUNNING,   // RUNNING = 1 (implicit)\n  PAUSED,    // PAUSED = 2 (implicit) \n  STOPPED = 1 // Noncompliant: STOPPED explicitly set to 1, collides with implicit RUNNING value\n};",
      "enum priority {\n  LOW = 10,\n  MEDIUM,    // MEDIUM = 11 (implicit)\n  HIGH,      // HIGH = 12 (implicit)\n  CRITICAL = 11 // Noncompliant: CRITICAL explicitly set to 11, collides with implicit MEDIUM value\n};"
    ],
    "compliant": [
      "enum color {\n  red = 3,\n  blue,      // blue = 4 (implicit)\n  green,     // green = 5 (implicit)\n  yellow = 6 // Compliant: no collision, explicit value doesn't match any implicit value\n};",
      "enum color {\n  red = 3,\n  blue = 5,  // Compliant: explicit value\n  green = 5, // Compliant: explicit value, intentional duplication\n  yellow = 5 // Compliant: explicit value, intentional duplication\n};",
      "enum status {\n  IDLE = 0,\n  RUNNING,   // RUNNING = 1 (implicit)\n  PAUSED,    // PAUSED = 2 (implicit)\n  STOPPED = RUNNING // Compliant: explicitly referencing another enum value\n};",
      "enum priority {\n  LOW = 10,\n  MEDIUM = 15, // Compliant: explicit value avoids collision\n  HIGH,        // HIGH = 16 (implicit)\n  CRITICAL = 20 // Compliant: explicit value, no collision\n};"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/cpp/RSPEC-841"
  ],
  "logic": {
    "node_types": [
      "enum_specifier"
    ],
    "checks": [
      {
        "type": "custom_function",
        "function": "check_enum_value_collisions",
        "message": "Enum members with implicit values should not collide with other enumerators."
      }
    ]
  }
}