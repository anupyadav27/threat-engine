{
  "filename": "constants_should_come_first_in_equality_tests_metadata.json",
  "rule_id": "constants_come_first_equality",
  "title": "Constants should come first in equality tests",
  "description": "Constants should come first in equality testsAnalyze your codeconsistency-conventionalmaintainabilityCode SmellWhy is this an issue?The result of the comparison is the same, regardless of whether the constant is on the left or right-hand side. But following this convention will help pinpoint the occasional error where=(assignment) is substituted for==(comparison).If the constant is on the right-hand side of the expression in such cases, the code will still compile and run - just not as expected. If the constant is on the left-hand side, the error will be caught at the first attempt to compile.Noncompliant code exampleif ( var == constant ) if ( pointer == NULL )Compliant solutionif ( constant == var ) if ( NULL == pointer )Available I",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Reliability",
  "recommendation": "Remove the problematic code and refactor as needed.",
  "impact": "Code readability problem",
  "tags": [
    "deprecated",
    "convention"
  ],
  "examples": {
    "noncompliant": [
      "void badComparisons() {\n    int var = 5;\n    char* pointer = nullptr;\n    \n    if (var == 42) {        // Noncompliant - constant on right\n        doSomething();\n    }\n    \n    if (pointer == NULL) {  // Noncompliant - constant on right\n        handleNull();\n    }\n    \n    if (var == 0) {         // Noncompliant - constant on right\n        handleZero();\n    }\n}"
    ],
    "compliant": [
      "void goodComparisons() {\n    int var = 5;\n    char* pointer = nullptr;\n    \n    if (42 == var) {        // Compliant - constant on left\n        doSomething();\n    }\n    \n    if (NULL == pointer) {  // Compliant - constant on left\n        handleNull();\n    }\n    \n    if (0 == var) {         // Compliant - constant on left\n        handleZero();\n    }\n}"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/cpp/RSPEC-1772"
  ],
  "logic": {
    "node_types": [
      "FunctionDefinition"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "pattern": "\\b(?:if|while|for)\\s*\\([^)]*\\b[a-zA-Z_][a-zA-Z0-9_]*\\s*==\\s*(?:[0-9]+|0[xX][0-9A-Fa-f]+|NULL)\\b",
        "message": "Move the constant to the left side of the comparison."
      }
    ]
  }
}