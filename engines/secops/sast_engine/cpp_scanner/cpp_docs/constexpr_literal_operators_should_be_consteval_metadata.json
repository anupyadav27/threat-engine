{
  "filename": "constexpr_literal_operators_should_be_consteval_metadata.json",
  "rule_id": "constexpr_literal_operators_consteval",
  "title": "constexpr literal operators should be consteval.",
  "description": "\"constexpr\" literal operators should be \"consteval\".Analyze your codeconsistency-conventionalmaintainabilityCode SmellQuick FixIDE quick fixes available with SonarQube for IDEsince-c++20Why is this an issue?C++20 introduces theconstevalkeyword, which enforces that a function is always evaluated at compile time.constexprfunctions can produce a compile-time constant in some contexts and when called with appropriate arguments, but they can also be invoked at run-time.constevalfunctions must always be evaluated at compile-time. If that cannot happen, a compilation error will occur.User-defined literal operators are designed to be called with constant operands known at compile time. Therefore, if these are intended to be evaluated at compile time withconstexpr,constevalshould be used instead to",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Reliability",
  "recommendation": "Replace with the recommended alternative approach.",
  "impact": "Potential bug",
  "tags": [
    "bug",
    "convention",
    "design"
  ],
  "examples": {
    "noncompliant": [
      "// C++20: constexpr literal operators should be consteval\nconstexpr long double operator\"\" _km(long double distance) { // Noncompliant\n    return distance * 1000.0;\n}\n\nconstexpr int operator\"\" _squared(unsigned long long n) { // Noncompliant\n    return n * n;\n}\n\nconstexpr auto operator\"\" _deg(long double angle) { // Noncompliant\n    return angle * 3.14159 / 180.0;\n}"
    ],
    "compliant": [
      "// C++20: Use consteval for literal operators\nconsteval long double operator\"\" _km(long double distance) { // Compliant\n    return distance * 1000.0;\n}\n\nconsteval int operator\"\" _squared(unsigned long long n) { // Compliant\n    return n * n;\n}\n\nconsteval auto operator\"\" _deg(long double angle) { // Compliant\n    return angle * 3.14159 / 180.0;\n}"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/cpp/RSPEC-6621"
  ],
  "logic": {
    "node_types": [
      "FunctionDefinition"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "pattern": "constexpr\\s+[^}]*operator\"\"[^}]*\\{",
        "message": "Replace 'constexpr' with 'consteval' for literal operators."
      }
    ]
  }
}