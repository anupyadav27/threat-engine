{
  "filename": "hard_coded_passwords_are_security_sensitive_metadata.json",
  "rule_id": "hardcoded_passwords_are_securitysensitive",
  "title": "Hard-coded passwords are security-sensitive",
  "description": "This rule looks for hard-coded passwords in variable names that match any of the patterns from the provided list. Credentials allow access to a sensitive component like a database, a file storage, an API or a service. Credentials are used in production environments.",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Security",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Security risk",
  "tags": [
    "security",
    "cert",
    "cwe",
    "owasp",
    "bug"
  ],
  "examples": {
    "noncompliant": [
      "dbi_conn conn = dbi_conn_new(\"mysql\");\nstring password = \"secret\"; // Sensitive\ndbi_conn_set_option(conn, \"password\", password.c_str());"
    ],
    "compliant": [
      "dbi_conn conn = dbi_conn_new(\"mysql\");\nstring password = getDatabasePassword(); // Compliant\ndbi_conn_set_option(conn, \"password\", password.c_str()); // Compliant"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/cpp/RSPEC-2068"
  ],
  "logic": {
    "node_types": [
      "*"
    ],
    "checks": [
      {
        "type": "custom_function",
        "function": "check_hardcoded_passwords",
        "message": "Hard-coded passwords are security-sensitive"
      }
    ]
  }
}