{
  "filename": "c_style_array_should_be_avoided_metadata.json",
  "rule_id": "cstyle_array_avoided",
  "title": "C-style array should be avoided",
  "description": "C-style arrays (such as int i[10]) are not very convenient to use: They are fixed size (even C Variable Length Arrays are not truly variable size, and they are not supported in C++) If the number of elements in the array can vary, it will lead to manual memory allocation (or people will use fixed-size arrays that \"should be large enough\", which is both a waste of memory and a limitation of the program) It is very easy to lose the size of the array since an array passed to a function decays into a pointer The C++ standard library proposes two types that are better than C-style arrays and together cover all the use cases of C-style arrays: For fixed-size arrays, where the memory is on the stack, use std::array. It is like a C-style array, except that it has normal argument passing semantics, and the size is always a part of the type. You can roll your version if std::array is unavailable to you (before C++11).",
  "status": "ready",
  "defaultSeverity": "Info",
  "category": "Maintainability",
  "recommendation": "Replace with the recommended alternative approach.",
  "impact": "Maintainability issue",
  "tags": [
    "cpp"
  ],
  "examples": {
    "noncompliant": [
      "void function() {\n    int numbers[10];        // Noncompliant: C-style array\n    char buffer[256];       // Noncompliant: fixed size buffer\n    double matrix[3][3];    // Noncompliant: multi-dimensional C-style array\n}\n\nvoid processArray(int arr[]) {  // Noncompliant: array parameter loses size info\n    // Size information is lost here\n}"
    ],
    "compliant": [
      "#include <array>\n#include <vector>\n\nvoid function() {\n    std::array<int, 10> numbers;           // Compliant: fixed-size array\n    std::vector<char> buffer(256);         // Compliant: dynamic array\n    std::array<std::array<double, 3>, 3> matrix;  // Compliant: multi-dimensional\n}\n\nvoid processArray(const std::array<int, 10>& arr) {  // Compliant: size preserved\n    // Size information is maintained\n}\n\nvoid processVector(const std::vector<int>& vec) {   // Compliant: for dynamic arrays\n    // Size available via vec.size()\n}"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/cpp/RSPEC-5945"
  ],
  "logic": {
    "node_types": [
      "declaration",
      "parameter_declaration", 
      "function_definition",
      "class_specifier",
      "struct_specifier",
      "field_declaration"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "pattern": "(?:int|char|float|double|long|short|unsigned|signed|bool|void|const|volatile|static|extern|typedef|struct|class|template|typename|auto)\\s+\\w+(?:\\s*\\[\\d*\\])(?:\\s*\\[\\d*\\])*",
        "message": "Use std::array or std::vector instead of C-style arrays."
      },
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "pattern": "\\w+\\s+\\w+\\s*\\[\\s*\\]",
        "message": "Function parameters should use std::array, std::vector, or std::span instead of C-style arrays."
      },
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "pattern": "\\w+\\s+\\w+\\s*\\[\\d*\\]\\s*\\[\\s*\\]",
        "message": "Multi-dimensional function parameters should use std::array or std::vector instead of C-style arrays."
      },
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "pattern": "(?:private|protected|public)\\s*:\\s*[^\\n]*\\w+\\s+\\w+\\s*\\[\\d*\\](?:\\s*\\[\\d*\\])*",
        "message": "Class/struct member arrays should use std::array or std::vector instead of C-style arrays."
      }
    ]
  }
}