{
  "filename": "default_parameters_should_not_be_defined_metadata.json",
  "rule_id": "default_parameters_defined",
  "title": "Default parameters should not be defined",
  "description": "Default parameters should not be definedAnalyze your codeintentionality-clearmaintainabilityCode SmellWhy is this an issue?More InfoSetting method parameter defaults seems like a tidy way to make a method more usable. However, function pointers to methods with defaulted parameters can be confusing, because the function signature may not seem to match the call signature. Therefore, the use of multiple, overloaded methods is preferred.Noncompliant code examplevoid HelloWorld (string name=\"World\") { cout << \"Hello \" << name << endl; }Compliant solutionvoid HelloWorld (string name) { cout << \"Hello \" << name << endl; } void HelloWorld () { HelloWorld(\"World\"); }Available In:Catch issues on the fly,in your IDEDetect issues in your GitHub, Azure DevOps Services, Bitbucket Cloud, GitLab repositor",
  "status": "ready",
  "defaultSeverity": "Info",
  "category": "General",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Code readability problem",
  "tags": [
    "confusing"
  ],
  "examples": {
    "noncompliant": [
      "void HelloWorld (string name=\"World\")\n{\n    cout << \"Hello \" << name << endl;\n}"
    ],
    "compliant": [
      "void HelloWorld (string name)\n{\n    cout << \"Hello \" << name << endl;\n}\n\nvoid HelloWorld ()\n{\n    HelloWorld(\"World\");\n}"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/cpp/RSPEC-1712"
  ],
  "logic": {
    "node_types": [
      "FunctionDefinition"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "patterns": [
          "\\w+\\s+\\w+\\s*\\([^\\)]*=\\s*[^\\),]+[^\\)]*\\)\\s*\\{"
        ],
        "message": "Default parameters should be declared in headers, not defined in implementation."
      }
    ]
  }
}