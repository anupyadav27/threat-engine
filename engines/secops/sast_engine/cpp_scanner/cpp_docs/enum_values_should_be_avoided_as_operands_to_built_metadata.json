{
  "filename": "enum_values_should_be_avoided_as_operands_to_built_metadata.json",
  "rule_id": "enum_values_avoided_as",
  "title": "enum values should be avoided as operands to built-in operators other than [ ], =, ==, !=, unary &, and the relational operators <, <=, >, >=",
  "description": "\"enum\" values should not be used as operands to built-in operators other than [ ], =, ==, !=, unary &, and the relational operators <, <=, >, >=Analyze your codeintentionality-logicalmaintainabilityCode Smellbased-on-misrasuspiciousWhy is this an issue?More InfoEnumerations are used to represent symbolic values, or sometimes bit fields. They are not supposed to be used in arithmetic contexts.Additionally, even though comparing them with integer numbers can make sense (for instance, to test if an enum lies with a certain range), comparing them with floating point numbers does not (and is deprecated since C++20).There are other restrictions related to the use of enums, see for instanceS2753.Noncompliant code exampleenum { COLOUR_0, COLOUR_1, COLOUR_2, COLOUR_COUNT } colour; if ( COLOUR_0 ==",
  "status": "ready",
  "defaultSeverity": "Info",
  "category": "General",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Code readability problem",
  "tags": [
    "cert",
    "deprecated"
  ],
  "examples": {
    "noncompliant": [
      "enum Color { RED, GREEN, BLUE, COLOR_COUNT };\nColor color = RED;\n\n// Noncompliant: arithmetic operations\nif ((RED + GREEN) == color) { }\n\n// Noncompliant: bitwise operations (except unary &)\nColor result = RED | GREEN;\n\n// Noncompliant: comparison with float\nif (color > 3.14) { }\n\n// Noncompliant: arithmetic increment\ncolor++;\n\n// Noncompliant: modulo operation\nint mod = color % 2;"
    ],
    "compliant": [
      "enum Color { RED, GREEN, BLUE, COLOR_COUNT };\nColor color = RED;\n\n// Compliant: assignment\ncolor = GREEN;\n\n// Compliant: equality comparison\nif (RED == color) { }\n\n// Compliant: inequality comparison\nif (color != BLUE) { }\n\n// Compliant: relational operators\nif (color < COLOR_COUNT) { }\nif (color >= RED) { }\n\n// Compliant: array subscript\nint values[COLOR_COUNT];\nvalues[color] = 10;\n\n// Compliant: unary address-of\nColor* ptr = &color;"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/cpp/RSPEC-873"
  ],
  "logic": {
    "node_types": [
      "*"
    ],
    "checks": [
      {
        "type": "custom_function",
        "function": "check_enum_operator_violations",
        "message": "Enum values should only be used with allowed operators: [], =, ==, !=, unary &, and relational operators (<, <=, >, >=). Avoid arithmetic, bitwise, and increment/decrement operations."
      }
    ]
  }
}