{
  "filename": "break_statements_should_be_avoided_except_for_swit_metadata.json",
  "rule_id": "break_statements_avoided_except",
  "title": "break statements should be avoided except for switch cases",
  "description": "break statements should not be used except for switch casesAnalyze your codeintentionality-clearmaintainabilityCode SmellWhy is this an issue?break;is an unstructured control flow statement which makes code harder to read.Ideally, every loop should have a single termination condition.Noncompliant code examplefor (element = list.first; element != null; element = element->next) { // First termination condition if (!matches(element->value)) { // Second termination condition break; // Noncompliant } /* ... */ }Compliant solution// Compliant for (element = list.first; element != null && matches(element->value); element = element->next) { /* ... */ }Available In:Catch issues on the fly,in your IDEDetect issues in your GitHub, Azure DevOps S",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "General",
  "recommendation": "Remove the problematic code and refactor as needed.",
  "impact": "Code readability problem",
  "tags": [
    "deprecated"
  ],
  "examples": {
    "noncompliant": [
      "for (element = list.first; element != null; element = element->next) {\n    if (!matches(element->value)) {\n        break;  // Noncompliant: break in loop\n    }\n    processElement(element);\n}\n\nwhile (true) {\n    if (condition) {\n        break;  // Noncompliant: break in while loop\n    }\n    doWork();\n}"
    ],
    "compliant": [
      "// Compliant: single termination condition in loop\nfor (element = list.first; element != null && matches(element->value); element = element->next) {\n    processElement(element);\n}\n\n// Compliant: break in switch statement\nswitch (value) {\n    case 1:\n        handleCase1();\n        break;  // Compliant: break in switch\n    case 2:\n        handleCase2();\n        break;  // Compliant: break in switch\n}\n\n// Compliant: using proper loop condition\nwhile (condition && !shouldStop()) {\n    doWork();\n}"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/cpp/RSPEC-1227"
  ],
  "logic": {
    "node_types": [
      "ForStatement",
      "WhileStatement", 
      "DoWhileStatement"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "pattern": "(?:if|while|for|do)\\s*[^{]*\\{[^{}]*\\bbreak\\s*;",
        "message": "Avoid break statements in loops. Restructure the loop condition instead."
      }
    ]
  }
}