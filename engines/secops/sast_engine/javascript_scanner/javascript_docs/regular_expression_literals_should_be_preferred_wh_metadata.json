{
  "filename": "regular_expression_literals_should_be_preferred_wh_metadata.json",
  "rule_id": "regular_expression_literals_preferred",
  "title": "Regular expression literals should be preferred when possible",
  "description": "Regular expression literals should be used when possibleAnalyze your codeintentionality-clearmaintainabilityCode SmellQuick FixIDE quick fixes available with SonarQube for IDEregexWhy is this an issue?More InfoUsing regular expression literals is recommended over using theRegExpconstructor calls if the pattern is a literal. Regular expression literals are shorter, more readable, and do not need to be escaped like string literals. They can also be more performant because regular expression literals are compiled only once when the script is loaded.new RegExp(/foo/); new RegExp('bar'); new RegExp('baz', 'i'); new RegExp(\"\\\\d+\"); new RegExp(`qux|quuz`);Using theRegExpconstructor is suitable when the pattern is computed dynamically, for example, when the user provides it.",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Security",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Security risk",
  "tags": [
    "security",
    "bug",
    "deprecated",
    "async"
  ],
  "examples": {
    "noncompliant": [
      "RegExp"
    ],
    "compliant": [
      "new RegExp(/foo/);\nnew RegExp('bar');\nnew RegExp('baz', 'i');\nnew RegExp(\"\\\\d+\");\nnew RegExp(`qux|quuz`);"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/javascript/RSPEC-6325"
  ],
  "logic": {
    "node_types": [
      "NewExpression",
      "CallExpression",
      "VariableDeclaration",
      "ReturnStatement"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "pattern": "new\\s+RegExp\\s*\\(\\s*['\"`][^'\"`]*['\"`]",
        "message": "Use regex literal instead of RegExp constructor when pattern is static"
      },
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "pattern": "=\\s*RegExp\\s*\\(\\s*['\"`][^'\"`]*['\"`]",
        "message": "Use regex literal instead of RegExp constructor when pattern is static"
      },
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "pattern": "return\\s+new\\s+RegExp\\s*\\(\\s*['\"`][^'\"`]*['\"`]",
        "message": "Use regex literal instead of RegExp constructor when pattern is static"
      }
    ]
  }
}