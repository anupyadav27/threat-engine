{
  "filename": "reacts_ismounted_should_be_avoided_metadata.json",
  "rule_id": "reacts_ismounted_avoided",
  "title": "React's isMounted should be avoided",
  "description": "React's \"isMounted\" should not be usedAnalyze your codeconsistency-conventionalmaintainabilityreliabilityCode SmellreactWhy is this an issue?More InfoReactisMounted()is primarily used to avoid callingsetState()after a component has unmounted, because callingsetState()after a component has unmounted will emit a warning. CheckingisMounted()before callingsetState()does eliminate the warning, but it also defeats the purpose of the warning, which is raising awareness that the app is still holding a reference to the component after the component has been unmounted.When using ES6 classes, usingisMounted()is already prohibited.class MyComponent extends React.Component { componentDidMount() { mydatastore.subscribe(this); } dataHandler() { if (this.isMounted()) { // Noncompliant: isMounted() hides t",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Security",
  "recommendation": "Refactor to eliminate the flagged pattern.",
  "impact": "Security risk",
  "tags": [
    "security",
    "bug",
    "deprecated",
    "async"
  ],
  "examples": {
    "noncompliant": [
      "isMounted()"
    ],
    "compliant": [
      "setState()"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/javascript/RSPEC-6789"
  ],
  "logic": {
    "node_types": [
      "CallExpression",
      "ExpressionStatement"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "pattern": "this\\.isMounted\\s*\\(",
        "message": "Refactor to eliminate the flagged pattern."
      }
    ]
  }
}