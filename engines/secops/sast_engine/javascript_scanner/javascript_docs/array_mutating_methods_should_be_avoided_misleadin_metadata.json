{
  "filename": "array_mutating_methods_should_be_avoided_misleadin_metadata.json",
  "rule_id": "arraymutating_methods_avoided_misleadingly",
  "title": "Array-mutating methods should be avoided misleadingly",
  "description": "Array-mutating methods should not be used misleadinglyAnalyze your codeintentionality-clearmaintainabilityCode SmellQuick FixIDE quick fixes available with SonarQube for IDEtype-dependentWhy is this an issue?More InfoIn JavaScript, someArraymethods do not mutate the existing array that the method was called on, but instead return a new array. Other methods mutate the array, and their return value differs depending on the method.reverseandsortare mutating methods and, in addition, return the altered version. This rule raises an issue when the return values of these methods are assigned, which could lead maintainers to overlook the fact that the original array has been modified.const reversed = a.reverse(); // Noncompliant: mutating method, no need to assign return value const sorted = b.sor",
  "status": "ready",
  "defaultSeverity": "Minor",
  "category": "Maintainability",
  "recommendation": "Avoid assigning return values of mutating methods like reverse() and sort().",
  "impact": "Code clarity and maintainability issues",
  "tags": [
    "security",
    "bug",
    "deprecated",
    "async"
  ],
  "examples": {
    "noncompliant": [
      "const reversed = arr.reverse(); // Misleading - arr is also modified",
      "const sorted = data.sort(); // Misleading - data is also modified",
      "let result = myArray.reverse(); // Assignment of mutating method"
    ],
    "compliant": [
      "arr.reverse(); // Clear that arr is modified",
      "data.sort(); // Clear that data is modified",
      "const reversed = [...arr].reverse(); // Copy first, then mutate"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/javascript/RSPEC-4043"
  ],
  "logic": {
    "node_types": [
      "VariableDeclaration"
    ],
    "checks": [
      {
        "type": "regex_match",
        "message": "Avoid assigning return values of mutating methods like reverse() and sort().",
        "property_path": [
          "source"
        ],
        "patterns": [
          "(const|let|var)\\s+\\w+\\s*=\\s*\\w+\\.(reverse|sort)\\s*\\("
        ]
      }
    ]
  }
}