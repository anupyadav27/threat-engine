{
  "filename": "parameters_should_be_passed_in_the_correct_order_metadata.json",
  "rule_id": "parameters_passed_correct_order",
  "title": "Parameters should be passed in the correct order",
  "description": "Parameters should be passed in the correct orderAnalyze your codeintentionality-clearmaintainabilityCode Smelltype-dependentWhy is this an issue?More InfoConsistent naming between arguments and parameters reduces the chances of making mistakes, such as passing the wrong value to a parameter or omitting an argument in a function call. When the names of arguments in a function call match the names of the function parameters, it contributes to clearer, more readable code.However, when the names match but are passed in a different order than their declaration in the function signature, it may indicate a mistake in the parameter order, likely leading to unexpected results.function divide(dividend, divisor) { return dividend / divisor; } function doTheThing() { const dividend = 15; const divisor",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Security",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Security risk",
  "tags": [
    "security",
    "bug",
    "deprecated",
    "async"
  ],
  "examples": {
    "noncompliant": [
      "function divide(dividend, divisor) {\n  return dividend / divisor;\n}\n\nfunction doTheThing() {\n  const dividend = 15;\n  const divisor = 5;\n\n  const result = divide(divisor, dividend); // Noncompliant: not the expected result\n  //...\n}"
    ],
    "compliant": [
      "function divide(dividend, divisor) {\n  return dividend / divisor;\n}\n\nfunction doTheThing() {\n  const dividend = 15;\n  const divisor = 5;\n\n  const result = divide(dividend, divisor);\n  //...\n}"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/javascript/RSPEC-2234"
  ],
  "logic": {
    "node_types": [
      "CallExpression",
      "ExpressionStatement"
    ],
    "checks": [
      {
        "type": "custom_function",
        "function": "check_parameter_order_consistency",
        "message": "Review and fix according to the rule guidelines."
      }
    ]
  }
}