{
  "filename": "ternary_operators_should_not_be_nested_metadata.json",
  "rule_id": "ternary_operators_nested",
  "title": "Ternary operators should not be nested",
  "description": "Ternary operators should not be nestedAnalyze your codeintentionality-clearmaintainabilityCode SmellconfusingWhy is this an issue?Nested ternaries are hard to read and can make the order of operations complex to understand.public string GetReadableStatus(Job j) { return j.IsRunning ? \"Running\" : j.HasErrors ? \"Failed\" : \"Succeeded\"; // Noncompliant }Instead, use another line to express the nested operation in a separate statement.public string GetReadableStatus(Job j) { if (j.IsRunning) { return \"Running\"; } return j.HasErrors ? \"Failed\" : \"Succeeded\"; }Available In:Catch issues on the fly,in your IDEDetect issues in your GitHub, Azure DevOps Services, Bitbucket Cloud, GitLab repositoriesAnalyze code in youron-premise CI©2025SonarSource Sàrl. All rights reserved.Privacy Policy|Cookie Polic",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Security",
  "recommendation": "Replace with the recommended alternative approach.",
  "impact": "Security risk",
  "tags": [
    "security",
    "bug",
    ".net"
  ],
  "examples": {
    "noncompliant": [
      "public string GetReadableStatus(Job j)\n{\n  return j.IsRunning ? \"Running\" : j.HasErrors ? \"Failed\" : \"Succeeded\";  // Noncompliant\n}"
    ],
    "compliant": [
      "public string GetReadableStatus(Job j)\n{\n  if (j.IsRunning)\n  {\n    return \"Running\";\n  }\n  return j.HasErrors ? \"Failed\" : \"Succeeded\";\n}"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/csharp/RSPEC-3358"
  ],
  "logic": {
    "node_types": ["MethodDeclaration"],
    "checks": [
      {
        "type": "regex_match",
        "property_path": ["source"],
        "pattern": "\\?[^;]*\\?[^;]*:",
        "exclude_patterns": [
          "Console\\.",
          "Logger\\.",
          "Debug\\.",
          "//.*\\?",
          "/\\*.*\\?"
        ],
        "message": "Ternary operators should not be nested - use if/else for better readability"
      }
    ],
    "never_trigger_on": [
      "Logging statements",
      "Comments",
      "Non-nested ternary operators"
    ],
    "reasoning": "Detects nested ternary operators (multiple ? : in same expression) which reduce code readability.",
    "validation_examples": {
      "should_trigger": [
        "return j.IsRunning ? \"Running\" : j.HasErrors ? \"Failed\" : \"Succeeded\";",
        "var result = a > b ? (c > d ? \"A\" : \"B\") : \"C\";"
      ],
      "should_not_trigger": [
        "return j.IsRunning ? \"Running\" : \"Stopped\";",
        "// Nested: a ? b : c ? d : e"
      ]
    }
  }
}