{
  "filename": "the_code_block_contains_too_many_logging_calls_metadata.json",
  "rule_id": "code_block_contains_too",
  "title": "The code block contains too many logging calls",
  "description": "Excessive logging within a code block can lead to several problems: Log file overload: generating an overwhelming number of log entries can fill up disk space quickly (thus increasing the storage space cost) and make it challenging to identify important log events promptly. Performance degradation: writing a large number of log statements can impact the performance of an application, especially when the logs are placed in frequently executed paths. Code readability and maintainability: excessive logging can clutter the code and increase the code’s complexity, making it difficult for developers to identify essential logic.",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Security",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Security risk",
  "tags": [
    "security",
    "bug",
    ".net"
  ],
  "examples": {
    "noncompliant": [
      "void MyMethod(List<MyObject> items)\n{\n    logger.Debug(\"The operation started\"); // Noncompliant - too many logging calls\n    foreach(var item in items)\n    {\n        logger.Debug($\"Evaluating {item.Name}\");\n        var result = Evaluate(item);\n        logger.Debug($\"Evaluating resulted in {result}\");\n    }\n    logger.Debug(\"The operation ended\");\n}"
    ],
    "compliant": [
      "void MyMethod(List<MyObject> items)\n{\n    logger.Debug(\"The operation started\"); // Compliant - moderate logging\n    var results = items.Select(item => Evaluate(item)).ToList();\n    logger.Debug($\"Evaluated {results.Count} items\");\n}"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/csharp/RSPEC-6664"
  ],
  "logic": {
    "node_types": ["MethodDeclaration"],
    "checks": [
      {
        "type": "regex_match",
        "property_path": ["source"],
        "pattern": "logger\\.Debug.*logger\\.Debug.*logger\\.Debug.*logger\\.Debug",
        "exclude_patterns": [],
        "message": "Too many logging calls in code block - consider consolidating log messages"
      }
    ],
    "never_trigger_on": [
      "Methods with moderate logging (1-3 calls)"
    ],
    "reasoning": "Detects methods with excessive logging calls (4+) which can impact performance and code readability.",
    "validation_examples": {
      "should_trigger": [
        "void Method() { logger.Debug(\"1\"); logger.Debug(\"2\"); logger.Debug(\"3\"); logger.Debug(\"4\"); }"
      ],
      "should_not_trigger": [
        "void Method() { logger.Debug(\"start\"); DoWork(); logger.Debug(\"end\"); }"
      ]
    }
  }
}