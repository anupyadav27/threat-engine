{
  "filename": "backslash_should_be_avoided_in_route_templates_metadata.json",
  "rule_id": "backslash_avoided_route_templates",
  "title": "Backslash should be avoided in route templates",
  "description": "Routing in ASP.NET MVC maps controllers and actions to paths in request URIs. In the former syntax specification of URIs, backslash characters (\\) were not allowed at all (see section \"2.4.3. Excluded US-ASCII Characters\" of RFC 2396).",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Security",
  "recommendation": "Refactor to eliminate the flagged pattern.",
  "impact": "Security risk",
  "tags": [
    "security",
    "bug",
    ".net"
  ],
  "examples": {
    "noncompliant": [
      "[Route(\"api\\products\")]\npublic class ProductsController : Controller // Noncompliant - using backslash",
      "[HttpGet(\"users\\{id}\")]\npublic IActionResult GetUser(int id) // Noncompliant",
      "[Route(\"api\\v1\\items\")]\npublic class ItemsController : Controller // Noncompliant"
    ],
    "compliant": [
      "[Route(\"api/products\")]\npublic class ProductsController : Controller // Compliant - using forward slash",
      "[HttpGet(\"users/{id}\")]\npublic IActionResult GetUser(int id) // Compliant",
      "[Route(\"api/v1/items\")]\npublic class ItemsController : Controller // Compliant"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/csharp/RSPEC-6930"
  ],
  "logic": {
    "node_types": [
      "CompilationUnit"
    ],
    "checks": [
      {
        "type": "custom_function",
        "function": "check_backslash_route_templates",
        "message": "Backslash should be avoided in route templates. Use forward slashes instead."
      }
    ]
  }
}