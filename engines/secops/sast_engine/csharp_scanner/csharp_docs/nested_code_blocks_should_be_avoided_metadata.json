{
  "filename": "nested_code_blocks_should_be_avoided_metadata.json",
  "rule_id": "nested_code_blocks_avoided",
  "title": "Nested code blocks should be avoided",
  "description": "Nested code blocks should not be usedAnalyze your codeintentionality-clearmaintainabilityCode Smellbad-practiceWhy is this an issue?How can I fix it?More InfoNested code blocks create new scopes where variables declared within are inaccessible from the outside, and their lifespan ends with the block.Although this may appear beneficial, their usage within a function often suggests that the function is overloaded. Thus, it may violate the Single Responsibility Principle, and the function needs to be broken down into smaller functions.The presence of nested blocks that don’t affect the control flow might suggest possible mistakes in the code.ExceptionsThe usage of a code block after acaseis allowed.Available In:Catch issues on the fly,in your IDEDetect issues in your GitHub, Azure DevOps Serv",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Security",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Security risk",
  "tags": [
    "security",
    "bug",
    ".net"
  ],
  "examples": {
    "noncompliant": [
      "public void ComplexMethod()\n{\n    if (condition1)\n    {\n        {\n            // Noncompliant - unnecessary nested block\n            var temp = ProcessData();\n            result = temp;\n        }\n    }\n}",
      "public void ProcessItems()\n{\n    for (int i = 0; i < items.Length; i++)\n    {\n        {\n            // Noncompliant - nested block without control structure\n            var item = items[i];\n            ProcessItem(item);\n        }\n    }\n}"
    ],
    "compliant": [
      "public void SimpleMethod()\n{\n    if (condition1)\n    {\n        var temp = ProcessData();\n        result = temp;\n    }\n}",
      "public void ProcessItems()\n{\n    for (int i = 0; i < items.Length; i++)\n    {\n        var item = items[i];\n        ProcessItem(item);\n    }\n}",
      "switch (value)\n{\n    case 1:\n        {\n            // Compliant - block after case is allowed\n            DoSpecialProcessing();\n            break;\n        }\n}"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/csharp/RSPEC-1199"
  ],
  "logic": {
    "node_types": ["BlockStatement"],
    "checks": [
      {
        "type": "custom_function",
        "function": "check_nested_code_blocks_should_be_avoided",
        "description": "Check for unnecessary nested code blocks that don't affect control flow"
      }
    ]
  }
}