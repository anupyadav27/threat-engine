{
  "filename": "if_else_if_constructs_should_have_else_clauses_metadata.json",
  "rule_id": "if_else_if_constructs",
  "title": "if ... else if constructs should have else clauses",
  "description": "\"if ... else if\" constructs should end with \"else\" clausesAnalyze your codeintentionality-clearmaintainabilityCode SmellWhy is this an issue?This rule applies whenever anifstatement is followed by one or moreelse ifstatements; the finalelse ifshould be followed by anelsestatement.The requirement for a finalelsestatement is defensive programming.Theelsestatement should either take appropriate action or contain a suitable comment as to why no action is taken. This is consistent with the requirement to have a finaldefaultclause in aswitchstatement.Noncompliant code exampleif (x == 0) { DoSomething(); } else if (x == 1) { DoSomethingElse(); }Compliant solutionif (x == 0) { DoSomething(); } else if (x == 1) { DoSomethingElse(); } else { throw new InvalidOperationException(); }ExceptionsNoneAvai",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Security",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Security risk",
  "tags": [
    "security",
    "bug",
    ".net"
  ],
  "examples": {
    "noncompliant": [
      "if (x == 0)\n{\n    DoSomething();\n}\nelse if (x == 1) // Noncompliant: missing final else clause\n{\n    DoSomethingElse();\n}"
    ],
    "compliant": [
      "if (x == 0)\n{\n    DoSomething();\n}\nelse if (x == 1)\n{\n    DoSomethingElse();\n}\nelse // Compliant: has final else clause\n{\n    throw new InvalidOperationException();\n}"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/csharp/RSPEC-126"
  ],
  "logic": {
    "custom_function": "check_if_else_if_without_else",
    "reasoning": "Uses custom function to detect if-else if constructs that do not end with a final else clause. Tracks brace matching to find end of else if blocks and checks what follows.",
    "validation_examples": {
      "should_trigger": [
        "if (x == 0) { } else if (x == 1) { }",
        "if (status == 1) { } else if (status == 2) { } else if (status == 3) { }"
      ],
      "should_not_trigger": [
        "if (x == 0) { } else if (x == 1) { } else { }",
        "if (condition) { } else { }",
        "if (condition) { }"
      ]
    }
  }
}