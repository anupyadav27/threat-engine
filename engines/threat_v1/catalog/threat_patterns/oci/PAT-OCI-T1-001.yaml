id: PAT-OCI-T1-001
version: 1
description: "OCI Autonomous Database with public snapshots accessible and no storage encryption — database backup data exposed without credentials."
tier: 1
severity_base: critical
confidence: confirmed
mitre_tactics: [Exfiltration, Collection]
mitre_techniques: [T1530, T1619]
tactic_chain_order: [Collection, Exfiltration]
csps: [oci]
entry:
  node_type: Resource
  resource_types: [oci_autonomous_database, oci_db_system]
  conditions:
    check_rules_failing:
      - oci.database.autonomous_database.snapshots_public_access
      - oci.database.autonomous_database.storage_encrypted
scoring:
  posture_severity: critical
  active_severity: critical
tests:
  positive:
    description: "Autonomous DB with public snapshot access and no encryption"
    expected_tier: 1
    expected_severity: critical
  negative:
    description: "Autonomous DB with private snapshots and encryption enabled"
    expected_tier: 1
    expected_severity: low
