id: PAT-OCI-T1-003
version: 1
description: "OCI DB System with unencrypted recovery volumes and no deletion protection — database compromise provides persistent access with no audit trail."
tier: 1
severity_base: high
confidence: confirmed
mitre_tactics: [Collection, Impact]
mitre_techniques: [T1005, T1485]
tactic_chain_order: [Collection, Impact]
csps: [oci]
entry:
  node_type: Resource
  resource_types: [oci_db_system, oci_autonomous_database]
  conditions:
    check_rules_failing:
      - oci.database.db_system.recovery_instance_volumes_encrypted
      - oci.database.autonomous_database.instance_deletion_protection_enabled
scoring:
  posture_severity: high
  active_severity: critical
tests:
  positive:
    description: "OCI DB with unencrypted recovery volumes and no deletion protection"
    expected_tier: 1
    expected_severity: high
  negative:
    description: "OCI DB with encrypted recovery volumes and deletion protection"
    expected_tier: 1
    expected_severity: low
