id: PAT-AWS-T1-008
version: 1
description: "EKS cluster with anonymous authentication enabled — unauthenticated API server access from any network."
tier: 1
severity_base: critical
confidence: confirmed
mitre_tactics: [Initial Access, Discovery]
mitre_techniques: [T1190, T1613]
tactic_chain_order: [Initial Access, Discovery]
csps: [aws]
entry:
  node_type: Resource
  resource_types: [aws_eks_cluster]
  conditions:
    check_rules_failing:
      - aws.eks.cluster.anonymous_auth_disabled
scoring:
  posture_severity: critical
  active_severity: critical
tests:
  positive:
    description: "EKS cluster with --anonymous-auth=true on API server"
    expected_tier: 1
    expected_severity: critical
  negative:
    description: "EKS cluster with anonymous auth disabled and RBAC enforced"
    expected_tier: 1
    expected_severity: low
