# SERVICECATALOG Service - Check Metadata
# This file contains titles, descriptions, and severity for all checks
# The main YAML file only contains the logic

version: '1.0'
service: servicecatalog
checks:
  aws.servicecatalog.portfolio.servicecatalog_shared_within_organization_only_configured:
    title: 'SERVICECATALOG portfolio: Servicecatalog Shared Within Organization Only
      Configured'
    severity: medium
