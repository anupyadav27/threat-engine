# DATASYNC Service - Check Metadata
# This file contains titles, descriptions, and severity for all checks
# The main YAML file only contains the logic

version: '1.0'
service: datasync
checks:
  aws.datasync.resource.task_logging_enabled:
    title: 'DATASYNC resource: Task Logging Enabled'
    severity: medium
