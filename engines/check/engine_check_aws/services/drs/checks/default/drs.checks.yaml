version: '1.0'
provider: aws
service: drs
checks:
- rule_id: aws.drs.resource.drs_failover_execution_configured
  for_each: aws.drs.describe_launch_configuration_templates
  conditions:
    all:
    - var: item.launchDisposition
      op: exists
      value: null
    - var: item.postLaunchEnabled
      op: equals
      value: 'true'
