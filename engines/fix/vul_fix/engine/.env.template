# VulFix Engine — environment variables
# Copy this to .env and fill in real values before running tests

# ── vulnerability_db (RDS PostgreSQL) ────────────────────────────────────────
VUL_DB_HOST=postgres-vulnerability-db.cbm92xowvx2t.ap-south-1.rds.amazonaws.com
VUL_DB_PORT=5432
VUL_DB_NAME=vulnerability_db
VUL_DB_USER=postgres
VUL_DB_PASSWORD=FILL_IN_FROM_K8S_SECRET

# ── Mistral AI ────────────────────────────────────────────────────────────────
MISTRAL_API_KEY=FILL_IN_YOUR_MISTRAL_KEY
MISTRAL_MODEL=mistral-medium
MISTRAL_TIMEOUT=90

# ── Git (for production use — not needed for local dummy test) ────────────────
# GIT_TOKEN=ghp_xxxxxxxxxxxx
# GIT_DEFAULT_BRANCH=main
