fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic>=2.0

# psycopg2-binary is used here for local dev / Windows compatibility.
# The Dockerfile overrides this with compiled psycopg2==2.9.9 (libpq-dev).
psycopg2-binary==2.9.9

# LLM client — Anthropic Claude (primary)
anthropic>=0.28.0

# Mistral fallback uses requests (stdlib HTTP, no heavy SDK)
requests==2.32.3

# ── Observability ─────────────────────────────────────────────────────────────
prometheus-fastapi-instrumentator==6.1.0
