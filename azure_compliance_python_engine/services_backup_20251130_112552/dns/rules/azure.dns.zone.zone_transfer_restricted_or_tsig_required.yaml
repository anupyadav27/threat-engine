provider: azure
service: dns

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.dns.zone.zone_transfer_restricted_or_tsig_required
    title: Dns Zone Zone Transfer Restricted Or Tsig Required
    severity: medium
    # TODO: add discovery and check logic based on service API
