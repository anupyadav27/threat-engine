provider: azure
service: storage

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.storage.nfs.root.squash.check
    title: Root Squash Check
    severity: medium
    # TODO: add discovery and check logic based on service API
