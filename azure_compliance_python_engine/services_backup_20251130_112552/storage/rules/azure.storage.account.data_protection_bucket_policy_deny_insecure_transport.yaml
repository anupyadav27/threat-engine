provider: azure
service: storage

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.storage.account.data_protection_bucket_policy_deny_insecure_transport
    title: Storage Account Data Protection Bucket Policy Deny Insecure Transport
    severity: medium
    # TODO: add discovery and check logic based on service API
