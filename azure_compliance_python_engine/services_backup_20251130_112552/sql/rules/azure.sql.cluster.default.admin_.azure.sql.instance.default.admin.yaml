provider: azure
service: sql
# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.sql.cluster.default.admin,.azure.sql.instance.default.admin
    title: Instance Default Admin
    severity: medium
    # TODO: add discovery and check logic based on service API
