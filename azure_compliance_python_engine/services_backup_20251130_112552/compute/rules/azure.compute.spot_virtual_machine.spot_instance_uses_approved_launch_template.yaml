provider: azure
service: compute

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.compute.spot_virtual_machine.spot_instance_uses_approved_launch_template
    title: Compute Spot Virtual Machine Spot Instance Uses Approved Launch Template
    severity: medium
    # TODO: add discovery and check logic based on service API
