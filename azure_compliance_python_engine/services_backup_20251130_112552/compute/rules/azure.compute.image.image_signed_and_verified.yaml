provider: azure
service: compute

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.compute.image.image_signed_and_verified
    title: Compute Image Image Signed And Verified
    severity: medium
    # TODO: add discovery and check logic based on service API
