provider: azure
service: compute

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.compute.virtual_machine_scale_set.instance_group_uses_approved_launch_template
    title: Compute Virtual Machine Scale Set Instance Group Uses Approved Launch Template
    severity: medium
    # TODO: add discovery and check logic based on service API
