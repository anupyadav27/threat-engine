provider: azure
service: function

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.function.app.deployment.slot.vnet.routing.config.check
    title: Routing Config Check
    severity: medium
    # TODO: add discovery and check logic based on service API
