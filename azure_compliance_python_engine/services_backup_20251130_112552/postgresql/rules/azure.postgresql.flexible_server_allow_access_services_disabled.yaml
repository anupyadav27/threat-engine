provider: azure
service: postgresql

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.postgresql.flexible_server_allow_access_services_disabled
    title: Azure Postgresql Flexible Server Allow Access Services Disabled
    severity: medium
    # TODO: add discovery and check logic based on service API
