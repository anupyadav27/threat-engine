provider: azure
service: kubernetes

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.kubernetes.scheduler.k8s_leader_election_enabled
    title: Kubernetes Scheduler K8S Leader Election Enabled
    severity: medium
    # TODO: add discovery and check logic based on service API
