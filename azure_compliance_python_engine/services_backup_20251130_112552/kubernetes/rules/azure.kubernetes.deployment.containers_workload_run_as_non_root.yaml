provider: azure
service: kubernetes

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.kubernetes.deployment.containers_workload_run_as_non_root
    title: Kubernetes Deployment Containers Workload Run As Non Root
    severity: medium
    # TODO: add discovery and check logic based on service API
