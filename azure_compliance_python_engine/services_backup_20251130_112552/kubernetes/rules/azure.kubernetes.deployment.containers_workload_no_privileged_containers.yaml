provider: azure
service: kubernetes

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.kubernetes.deployment.containers_workload_no_privileged_containers
    title: Kubernetes Deployment Containers Workload No Privileged Containers
    severity: medium
    # TODO: add discovery and check logic based on service API
