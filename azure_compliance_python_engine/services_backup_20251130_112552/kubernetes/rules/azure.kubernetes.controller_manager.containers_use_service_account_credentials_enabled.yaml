provider: azure
service: kubernetes

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.kubernetes.controller_manager.containers_use_service_account_credentials_enabled
    title: Kubernetes Controller Manager Containers Use Service Account Credentials Enabled
    severity: medium
    # TODO: add discovery and check logic based on service API
