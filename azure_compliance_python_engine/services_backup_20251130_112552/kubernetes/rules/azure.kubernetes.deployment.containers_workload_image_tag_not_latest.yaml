provider: azure
service: kubernetes

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.kubernetes.deployment.containers_workload_image_tag_not_latest
    title: Kubernetes Deployment Containers Workload Image Tag Not Latest
    severity: medium
    # TODO: add discovery and check logic based on service API
