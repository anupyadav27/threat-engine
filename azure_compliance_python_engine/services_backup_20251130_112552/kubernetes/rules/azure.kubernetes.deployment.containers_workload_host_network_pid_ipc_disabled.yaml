provider: azure
service: kubernetes

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.kubernetes.deployment.containers_workload_host_network_pid_ipc_disabled
    title: Kubernetes Deployment Containers Workload Host Network Pid Ipc Disabled
    severity: medium
    # TODO: add discovery and check logic based on service API
