provider: azure
service: kubernetes

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.kubernetes.agent_pool.containers_node_pool_workload_identity_enabled
    title: Kubernetes Agent Pool Containers Node Pool Workload Identity Enabled
    severity: medium
    # TODO: add discovery and check logic based on service API
