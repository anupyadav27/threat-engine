provider: azure
service: kubernetes

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.kubernetes.deployment.containers_workload_read_only_root_filesystem
    title: Kubernetes Deployment Containers Workload Read Only Root Filesystem
    severity: medium
    # TODO: add discovery and check logic based on service API
