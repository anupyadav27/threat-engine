provider: azure
service: kubernetes

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.kubernetes.deployment.containers_workload_seccomp_profile_runtime_default
    title: Kubernetes Deployment Containers Workload Seccomp Profile Runtime Default
    severity: medium
    # TODO: add discovery and check logic based on service API
