provider: azure
service: kubernetes

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.kubernetes.service.containers_type_loadbalancer_internal_only_where_required
    title: Kubernetes Service Containers Type Loadbalancer Internal Only Where Required
    severity: medium
    # TODO: add discovery and check logic based on service API
