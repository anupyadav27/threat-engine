provider: azure
service: ad
# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.active.directory_user.identity_access_user_console_password_present_only_if_required
    title: Active Directory User Identity Access User Console Password Present Only If Required
    severity: medium
    # TODO: add discovery and check logic based on service API
