provider: azure
service: ad
# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.active.directory_tenant.identity_access_tenant_password_policy_compliant
    title: Active Directory Tenant Identity Access Tenant Password Policy Compliant
    severity: medium
    # TODO: add discovery and check logic based on service API
