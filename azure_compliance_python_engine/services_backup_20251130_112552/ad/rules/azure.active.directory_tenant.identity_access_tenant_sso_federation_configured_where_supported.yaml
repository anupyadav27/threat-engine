provider: azure
service: ad
# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.active.directory_tenant.identity_access_tenant_sso_federation_configured_where_supported
    title: Active Directory Tenant Identity Access Tenant Sso Federation Configured Where Supported
    severity: medium
    # TODO: add discovery and check logic based on service API
