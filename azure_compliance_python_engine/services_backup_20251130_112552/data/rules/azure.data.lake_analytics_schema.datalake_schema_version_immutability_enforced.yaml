provider: azure
service: data

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.data.lake_analytics_schema.datalake_schema_version_immutability_enforced
    title: Data Lake Analytics Schema Datalake Schema Version Immutability Enforced
    severity: medium
    # TODO: add discovery and check logic based on service API
