provider: azure
service: policy

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.policy.set_definition.configuration_management_config_baseline_parameters_set
    title: Policy Set Definition Configuration Management Config Baseline Parameters Set
    severity: medium
    # TODO: add discovery and check logic based on service API
