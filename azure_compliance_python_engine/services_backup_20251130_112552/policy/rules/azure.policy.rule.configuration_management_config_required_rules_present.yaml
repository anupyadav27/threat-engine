provider: azure
service: policy

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.policy.rule.configuration_management_config_required_rules_present
    title: Policy Rule Configuration Management Config Required Rules Present
    severity: medium
    # TODO: add discovery and check logic based on service API
