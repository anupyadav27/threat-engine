provider: azure
service: managed

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.managed.identity_service_principal.identity_access_service_account_workload_identity_federation_used_where_supported
    title: Managed Identity Service Principal Identity Access Service Account Workload Identity Federation Used Where Supported
    severity: medium
    # TODO: add discovery and check logic based on service API
