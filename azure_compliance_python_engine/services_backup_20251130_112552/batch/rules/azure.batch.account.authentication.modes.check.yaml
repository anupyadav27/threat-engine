provider: azure
service: batch

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.batch.account.authentication.modes.check
    title: Authentication Modes Check
    severity: medium
    # TODO: add discovery and check logic based on service API
