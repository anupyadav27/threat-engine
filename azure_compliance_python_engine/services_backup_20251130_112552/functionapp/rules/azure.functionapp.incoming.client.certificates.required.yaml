provider: azure
service: functionapp

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.functionapp.incoming.client.certificates.required
    title: Client Certificates Required
    severity: medium
    # TODO: add discovery and check logic based on service API
