provider: azure
service: functionapp

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.functionapp.deployment.slot.identity.check
    title: Slot Identity Check
    severity: medium
    # TODO: add discovery and check logic based on service API
