provider: azure
service: app

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.app.service.deployment.slots.basic.auth.disabled
    title: Basic Auth Disabled
    severity: medium
    # TODO: add discovery and check logic based on service API
