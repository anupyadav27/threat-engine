provider: azure
service: rbac

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.rbac.role_definition.identity_access_role_trust_principals_allowlist_only
    title: Rbac Role Definition Identity Access Role Trust Principals Allowlist Only
    severity: medium
    # TODO: add discovery and check logic based on service API
