provider: azure
service: billing

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.billing.reservation.cost_approval_workflow_required
    title: Billing Reservation Cost Approval Workflow Required
    severity: medium
    # TODO: add discovery and check logic based on service API
