provider: azure
service: active

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.active.directory_user.identity_access_user_mfa_required
    title: Active Directory User Identity Access User Mfa Required
    severity: medium
    # TODO: add discovery and check logic based on service API
