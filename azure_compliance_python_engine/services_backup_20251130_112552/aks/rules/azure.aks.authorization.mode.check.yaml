provider: azure
service: aks

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.aks.authorization.mode.check
    title: Authorization Mode Check
    severity: medium
    # TODO: add discovery and check logic based on service API
