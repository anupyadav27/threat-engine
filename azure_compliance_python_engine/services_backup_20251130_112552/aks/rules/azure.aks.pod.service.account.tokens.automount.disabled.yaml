provider: azure
service: aks

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.aks.pod.service.account.tokens.automount.disabled
    title: Tokens Automount Disabled
    severity: medium
    # TODO: add discovery and check logic based on service API
