provider: azure
service: aks

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.aks.service.account.default.usage.check
    title: Default Usage Check
    severity: medium
    # TODO: add discovery and check logic based on service API
