provider: azure
service: api

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.api.management_rate_limit_policy.rate_limiting_api_stage_throttle_overrides_not_unbounded
    title: Api Management Rate Limit Policy Rate Limiting Api Stage Throttle Overrides Not Unbounded
    severity: medium
    # TODO: add discovery and check logic based on service API
