provider: azure
service: monitor

# discovery: add appropriate discovery calls later
checks:
  - check_id: azure.monitor.metric.filter.root.usage
    title: Filter Root Usage
    severity: medium
    # TODO: add discovery and check logic based on service API
