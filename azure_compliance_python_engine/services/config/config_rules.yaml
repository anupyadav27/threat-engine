# ============================================================================
# 🤖 CURSOR AI: SERVICE VALIDATION INSTRUCTIONS
# ============================================================================
# 
# SERVICE: config
# STATUS: ⏳ NOT_VALIDATED
#
# 🎯 YOUR TASK:
# 1. Run engine: export AZURE_ENGINE_FILTER_SERVICES="config" && python engine/azure_generic_engine.py > /tmp/test_config.json 2>&1
# 2. Analyze: tail -100 /tmp/test_config.json
# 3. Fix issues below in discovery/checks sections
# 4. Re-run until: inventory has resources (if exist), checks = PASS/FAIL (not ERROR)
# 5. Mark complete at bottom
#
# ❌ COMMON ISSUES & FIXES:
#
# "Failed to create client" → 
#   Fix sdk_package: azure.mgmt.config (use DOTS not dashes)
#   Install: pip install azure-mgmt-config
#
# "Action not found: X.list" →
#   Test in Python:
#     from azure.mgmt.config import *
#     client = <Client>(cred, sub_id)
#     print(dir(client.<resource>))  # Find correct method
#   Common: list_by_subscription, list_all, list_by_resource_group
#
# "ERROR" in checks →
#   Check resource structure in output
#   Fix field paths: properties.encryption.keySource
#   Fix params: add resource_group_name, account_name, etc.
#
# Empty inventory →
#   Create test resource: az <service> <resource> create ...
#   OR verify action name is correct
#
# ✅ SUCCESS WHEN:
# - No warnings
# - Inventory populated (if resources exist)  
# - Checks = PASS/FAIL (not ERROR)
# - JSON is clean objects (not strings)
#
# 📝 AFTER FIXING:
# Mark status below as VALIDATED and add notes at bottom
#
# ============================================================================

config:
  version: '1.0'
  provider: azure
  service: config
  sdk_package: azure.mgmt.appconfiguration
  client_class: AppConfigurationManagementClient
  api_type: management
  scope: subscription
  discovery:
  - discovery_id: azure.config.resources
    calls:
    - action: configuration_stores.list
      fields:
      - path: __self__
  checks:
  - check_id: azure.config.policy.cis_7_16_needs_development
    title: 'AZURE CONFIG Policy: CIS 7.16 Needs Development'
    severity: low
    for_each: azure.config.resources
    calls:
    - action: self
      fields:
      - path: encryption.key_vault_properties
        operator: exists


# ============================================================================
# VALIDATION TRACKING
# ============================================================================
# STATUS: ✅ VALIDATED
# VALIDATED_BY: Cursor AI
# VALIDATED_DATE: 2025-12-05
# 
# FIXES APPLIED:
# - Changed package to sdk_package: azure.mgmt.appconfiguration (dot format)
# - Added api_type: management
# - Removed client: and save_as fields from discovery calls
# - Added fields: __self__ to discovery calls
# - Fixed 1 check: verified for_each reference
#
# TEST RESULTS:
# - Engine Status: ✅ NO ERRORS
# - API Connection: ✅ SUCCESS
# - Resources Discovered: 0 (no config resources in subscription - expected)
#
# NOTES:
# - Engine successfully connects to Azure and makes API calls
# - All discovery actions correctly formatted
# ============================================================================
