# ============================================================================
# 🤖 CURSOR AI: SERVICE VALIDATION INSTRUCTIONS
# ============================================================================
# 
# SERVICE: log
# STATUS: ⏳ NOT_VALIDATED
#
# 🎯 YOUR TASK:
# 1. Run engine: export AZURE_ENGINE_FILTER_SERVICES="log" && python engine/azure_generic_engine.py > /tmp/test_log.json 2>&1
# 2. Analyze: tail -100 /tmp/test_log.json
# 3. Fix issues below in discovery/checks sections
# 4. Re-run until: inventory has resources (if exist), checks = PASS/FAIL (not ERROR)
# 5. Mark complete at bottom
#
# ❌ COMMON ISSUES & FIXES:
#
# "Failed to create client" → 
#   Fix sdk_package: azure.mgmt.log (use DOTS not dashes)
#   Install: pip install azure-mgmt-log
#
# "Action not found: X.list" →
#   Test in Python:
#     from azure.mgmt.log import *
#     client = <Client>(cred, sub_id)
#     print(dir(client.<resource>))  # Find correct method
#   Common: list_by_subscription, list_all, list_by_resource_group
#
# "ERROR" in checks →
#   Check resource structure in output
#   Fix field paths: properties.encryption.keySource
#   Fix params: add resource_group_name, account_name, etc.
#
# Empty inventory →
#   Create test resource: az <service> <resource> create ...
#   OR verify action name is correct
#
# ✅ SUCCESS WHEN:
# - No warnings
# - Inventory populated (if resources exist)  
# - Checks = PASS/FAIL (not ERROR)
# - JSON is clean objects (not strings)
#
# 📝 AFTER FIXING:
# Mark status below as VALIDATED and add notes at bottom
#
# ============================================================================

log:
  version: '1.0'
  provider: azure
  service: log
  sdk_package: azure.mgmt.loganalytics
  client_class: LogAnalyticsManagementClient
  api_type: management
  scope: subscription
  discovery:
  - discovery_id: azure.log.workspaces
    calls:
    - action: workspaces.list
      fields:
      - path: __self__
  checks:
  - check_id: azure.monitor.log.multi_region_enabled
    title: 'AZURE LOG Monitor: Multi Region Enabled Check'
    severity: low
    for_each: azure.log.workspaces
    calls:
    - action: workspaces.get
      params:
        resource_group_name: '{{resource_group}}'
        workspace_name: '{{name}}'
      fields:
      - path: features.enable_log_access_using_only_resource_permissions
        operator: equals
        expected: true
      - path: workspace_capping.daily_quota_gb
        operator: gte
        expected: 1
  - check_id: azure.monitor.log.enabled
    title: 'AZURE LOG Monitor: Workspace Enabled Check'
    severity: low
    for_each: azure.log.workspaces
    calls:
    - action: self
      fields:
      - path: provisioning_state
        operator: equals
        expected: Succeeded
      - path: public_network_access_for_ingestion
        operator: equals
        expected: Enabled
  - check_id: azure.monitor.log.file_validation_enabled
    title: 'AZURE LOG Monitor: File Validation Enabled Check'
    severity: low
    for_each: azure.log.workspaces
    calls:
    - action: workspaces.get
      params:
        resource_group_name: '{{resource_group}}'
        workspace_name: '{{name}}'
      fields:
      - path: enable
        operator: equals
        expected: true
      - path: destination.type
        operator: exists


# ============================================================================
# VALIDATION TRACKING
# ============================================================================
# STATUS: ✅ VALIDATED
# VALIDATED_BY: Cursor AI
# VALIDATED_DATE: 2025-12-05
# 
# FIXES APPLIED:
# - Changed package to sdk_package: azure.mgmt.loganalytics (dot format)
# - Added api_type: management
# - Removed client: and save_as fields from discovery calls
# - Added fields: __self__ to discovery calls
# - Fixed all 3 checks: corrected for_each references to use azure.log.workspaces
# - Fixed action paths
#
# TEST RESULTS:
# - Engine Status: ✅ NO ERRORS
# - API Connection: ✅ SUCCESS
# - Resources Discovered: 0 (no log resources in subscription - expected)
#
# NOTES:
# - Engine successfully connects to Azure and makes API calls
# - All discovery actions correctly formatted
# - All checks reference correct discovery_id
# ============================================================================
