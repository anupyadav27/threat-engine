# ============================================================================
# 🤖 CURSOR AI: SERVICE VALIDATION INSTRUCTIONS
# ============================================================================
# 
# SERVICE: container
# STATUS: ⏳ NOT_VALIDATED
#
# 🎯 YOUR TASK:
# 1. Run engine: export AZURE_ENGINE_FILTER_SERVICES="container" && python engine/azure_generic_engine.py > /tmp/test_container.json 2>&1
# 2. Analyze: tail -100 /tmp/test_container.json
# 3. Fix issues below in discovery/checks sections
# 4. Re-run until: inventory has resources (if exist), checks = PASS/FAIL (not ERROR)
# 5. Mark complete at bottom
#
# ❌ COMMON ISSUES & FIXES:
#
# "Failed to create client" → 
#   Fix sdk_package: azure.mgmt.container (use DOTS not dashes)
#   Install: pip install azure-mgmt-container
#
# "Action not found: X.list" →
#   Test in Python:
#     from azure.mgmt.container import *
#     client = <Client>(cred, sub_id)
#     print(dir(client.<resource>))  # Find correct method
#   Common: list_by_subscription, list_all, list_by_resource_group
#
# "ERROR" in checks →
#   Check resource structure in output
#   Fix field paths: properties.encryption.keySource
#   Fix params: add resource_group_name, account_name, etc.
#
# Empty inventory →
#   Create test resource: az <service> <resource> create ...
#   OR verify action name is correct
#
# ✅ SUCCESS WHEN:
# - No warnings
# - Inventory populated (if resources exist)  
# - Checks = PASS/FAIL (not ERROR)
# - JSON is clean objects (not strings)
#
# 📝 AFTER FIXING:
# Mark status below as VALIDATED and add notes at bottom
#
# ============================================================================

container:
  version: '1.0'
  provider: azure
  service: container
  sdk_package: azure.mgmt.containerinstance
  client_class: ContainerInstanceManagementClient
  api_type: management
  scope: subscription
  discovery:
  - discovery_id: azure.container.instances_groups
    calls:
    - action: container_groups.list
      fields:
      - path: __self__
  checks:
  - check_id: azure.container.registry.encryption_enabled
    title: Registry Repository Encryption Enabled
    severity: high
    for_each: azure.container.registries
    calls:
    - action: self
      fields:
      - path: encryption.status
        operator: equals
        expected: enabled
  - check_id: azure.container.instances_group.containers_kubernetes_instance_no_privileged_containers
    title: Containers Kubernetes Instance No Privileged Containers
    severity: high
    for_each: azure.container.instances_groups
    calls:
    - action: container_groups.get
      params:
        resource_group_name: '{{resource_group}}'
        container_group_name: '{{name}}'
      fields:
      - path: containers[*].security_context.privileged
        operator: equals
        expected: false
  - check_id: azure.container.instance.logging_enabled
    title: Instances Task Definition Logging Enabled
    severity: medium
    for_each: azure.container.instances
    calls:
    - action: containers.get
      params:
        resource_group_name: '{{resource_group}}'
        container_group_name: '{{container_group_name}}'
        container_name: '{{container_name}}'
      fields:
      - path: diagnostics_config.log_analytics
        operator: exists
  - check_id: azure.container.instances_group.containers_kubernetes_instance_read_only_root_filesystem
    title: Containers Kubernetes Instance Read Only Root Filesystem
    severity: critical
    for_each: azure.container.instances_groups
    calls:
    - action: container_groups.get
      params:
        resource_group_name: '{{resource_group}}'
        container_group_name: '{{name}}'
      fields:
      - path: containers[*].security_context.read_only_root_filesystem
        operator: equals
        expected: true
  - check_id: azure.container.instance.managed.identity.least.privilege.check
    title: Managed Identity Least Privilege Check
    severity: high
    for_each: azure.container.instances
    calls:
    - action: self
      fields:
      - path: identity.type
        operator: equals
        expected: UserAssigned
  - check_id: azure.container.registry.image_scan_enabled
    title: 'AZURE CONTAINER Resource: Registry Image Scan On Push Enabled'
    severity: low
    for_each: azure.container.resource
    calls:
    - action: self
      fields:
      - path: properties.policies.quarantinePolicy.status
        operator: equals
        expected: enabled
      - path: properties.policies.trustPolicy.status
        operator: equals
        expected: enabled
  - check_id: azure.container.instances_group.containers_kubernetes_instance_env_no_plaintext_secrets
    title: 'AZURE CONTAINER Instances_group: Containers Kubernetes Instance Env No Plaintext Secrets'
    severity: high
    for_each: azure.container.instances_group
    calls:
    - action: self
      fields:
      - path: properties.containers[*].properties.environmentVariables[*].secureValue
        operator: exists
        expected: true
      - path: properties.containers[*].properties.environmentVariables[*].value
        operator: not_contains_secrets
        expected: true


# ============================================================================
# VALIDATION TRACKING
# ============================================================================
# STATUS: ⏳ NOT_VALIDATED
# VALIDATED_BY: 
# VALIDATED_DATE: 
# 
# FIXES APPLIED:
# # - (Add fixes here after validation)
#
# TEST RESULTS:
# - Resources Discovered: 0
# - Checks Executed: 0
# - PASS: 0
# - FAIL: 0
# - ERROR: 0
#
# NOTES:
# # - (Add notes here)
# ============================================================================
