# ============================================================================
# 🤖 CURSOR AI: SERVICE VALIDATION INSTRUCTIONS
# ============================================================================
# 
# SERVICE: netappfiles
# STATUS: ⏳ NOT_VALIDATED
#
# 🎯 YOUR TASK:
# 1. Run engine: export AZURE_ENGINE_FILTER_SERVICES="netappfiles" && python engine/azure_generic_engine.py > /tmp/test_netappfiles.json 2>&1
# 2. Analyze: tail -100 /tmp/test_netappfiles.json
# 3. Fix issues below in discovery/checks sections
# 4. Re-run until: inventory has resources (if exist), checks = PASS/FAIL (not ERROR)
# 5. Mark complete at bottom
#
# ❌ COMMON ISSUES & FIXES:
#
# "Failed to create client" → 
#   Fix sdk_package: azure.mgmt.netappfiles (use DOTS not dashes)
#   Install: pip install azure-mgmt-netappfiles
#
# "Action not found: X.list" →
#   Test in Python:
#     from azure.mgmt.netappfiles import *
#     client = <Client>(cred, sub_id)
#     print(dir(client.<resource>))  # Find correct method
#   Common: list_by_subscription, list_all, list_by_resource_group
#
# "ERROR" in checks →
#   Check resource structure in output
#   Fix field paths: properties.encryption.keySource
#   Fix params: add resource_group_name, account_name, etc.
#
# Empty inventory →
#   Create test resource: az <service> <resource> create ...
#   OR verify action name is correct
#
# ✅ SUCCESS WHEN:
# - No warnings
# - Inventory populated (if resources exist)  
# - Checks = PASS/FAIL (not ERROR)
# - JSON is clean objects (not strings)
#
# 📝 AFTER FIXING:
# Mark status below as VALIDATED and add notes at bottom
#
# ============================================================================

netappfiles:
  version: '1.0'
  provider: azure
  service: netappfiles
  package: azure-mgmt-netapp
  client_class: NetAppManagementClient
  scope: subscription
  discovery:
  - discovery_id: azure.netappfiles.accounts
    calls:
    - client: netappfiles
      action: accounts.list
      save_as: accounts
  - discovery_id: azure.netappfiles.pools
    calls:
    - client: netappfiles
      action: pools.list
      save_as: pools
  - discovery_id: azure.netappfiles.volumes
    calls:
    - client: netappfiles
      action: volumes.list
      save_as: volumes
  - discovery_id: azure.netappfiles.snapshots
    calls:
    - client: netappfiles
      action: snapshots.list
      save_as: snapshots
  - discovery_id: azure.netappfiles.backups
    calls:
    - client: netappfiles
      action: backups.list
      save_as: backups
  checks:
  - check_id: azure.netappfiles.encryption.key.source.cmk.check
    title: 'AZURE NETAPPFILES Encryption: Key Source CMK Check'
    severity: high
    for_each: azure.netappfiles.volumes
    calls:
    - action: self
      fields:
      - path: encryption_key_source
        operator: equals
        expected: Microsoft.KeyVault


# ============================================================================
# VALIDATION TRACKING
# ============================================================================
# STATUS: ⏳ NOT_VALIDATED
# VALIDATED_BY: 
# VALIDATED_DATE: 
# 
# FIXES APPLIED:
# # - (Add fixes here after validation)
#
# TEST RESULTS:
# - Resources Discovered: 0
# - Checks Executed: 0
# - PASS: 0
# - FAIL: 0
# - ERROR: 0
#
# NOTES:
# # - (Add notes here)
# ============================================================================
