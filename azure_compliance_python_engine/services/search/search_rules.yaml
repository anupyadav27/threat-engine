# ============================================================================
# 🤖 CURSOR AI: SERVICE VALIDATION INSTRUCTIONS
# ============================================================================
# 
# SERVICE: search
# STATUS: ⏳ NOT_VALIDATED
#
# 🎯 YOUR TASK:
# 1. Run engine: export AZURE_ENGINE_FILTER_SERVICES="search" && python engine/azure_generic_engine.py > /tmp/test_search.json 2>&1
# 2. Analyze: tail -100 /tmp/test_search.json
# 3. Fix issues below in discovery/checks sections
# 4. Re-run until: inventory has resources (if exist), checks = PASS/FAIL (not ERROR)
# 5. Mark complete at bottom
#
# ❌ COMMON ISSUES & FIXES:
#
# "Failed to create client" → 
#   Fix sdk_package: azure.mgmt.search (use DOTS not dashes)
#   Install: pip install azure-mgmt-search
#
# "Action not found: X.list" →
#   Test in Python:
#     from azure.mgmt.search import *
#     client = <Client>(cred, sub_id)
#     print(dir(client.<resource>))  # Find correct method
#   Common: list_by_subscription, list_all, list_by_resource_group
#
# "ERROR" in checks →
#   Check resource structure in output
#   Fix field paths: properties.encryption.keySource
#   Fix params: add resource_group_name, account_name, etc.
#
# Empty inventory →
#   Create test resource: az <service> <resource> create ...
#   OR verify action name is correct
#
# ✅ SUCCESS WHEN:
# - No warnings
# - Inventory populated (if resources exist)  
# - Checks = PASS/FAIL (not ERROR)
# - JSON is clean objects (not strings)
#
# 📝 AFTER FIXING:
# Mark status below as VALIDATED and add notes at bottom
#
# ============================================================================

search:
  version: '1.0'
  provider: azure
  service: search
  sdk_package: azure.mgmt.search
  client_class: SearchManagementClient
  api_type: management
  scope: subscription
  discovery:
  - discovery_id: azure.search.services
    calls:
    - action: services.list_by_subscription
      fields:
      - path: __self__
  checks:
  - check_id: azure.search.service.domains_access_control_enabled
    title: 'AZURE SEARCH Service: Domains Access Control Enabled'
    severity: medium
    for_each: azure.search.services
    calls:
    - action: self
      fields:
      - path: network_rule_set.ip_rules
        operator: exists
      - path: network_rule_set.bypass
        operator: not_equals
        expected: None
  - check_id: azure.search.service.domains_encryption_at_rest_enabled
    title: 'AZURE SEARCH Service: Domains Encryption At Rest Enabled'
    severity: high
    for_each: azure.search.services
    calls:
    - action: self
      fields:
      - path: encryption_with_cmk.enforcement
        operator: equals
        expected: Enabled
  - check_id: azure.aisearch.service.not_publicly_accessible
    title: 'AZURE AISEARCH Service: Not Publicly Accessible'
    severity: critical
    for_each: azure.search.services
    calls:
    - action: self
      fields:
      - path: public_network_access
        operator: equals
        expected: Disabled
  - check_id: azure.search.service.domains_node_to_node_encryption_enabled
    title: 'AZURE SEARCH Service: Domains Node To Node Encryption Enabled'
    severity: high
    for_each: azure.search.services
    calls:
    - action: self
      fields:
      - path: encryption_with_cmk.enforcement
        operator: equals
        expected: Enabled
  - check_id: azure.search.service.domains_logging_enabled
    title: 'AZURE SEARCH Service: Domains Logging Enabled'
    severity: medium
    for_each: azure.search.services
    calls:
    - action: monitor_client.diagnostic_settings.list
      fields:
      - path: value
        operator: not_empty
      - path: value[*].logs[?(@.category=='OperationLogs')].enabled
        operator: contains
        expected: true


# ============================================================================
# VALIDATION TRACKING
# ============================================================================
# STATUS: ⏳ NOT_VALIDATED
# VALIDATED_BY: 
# VALIDATED_DATE: 
# 
# FIXES APPLIED:
# # - (Add fixes here after validation)
#
# TEST RESULTS:
# - Resources Discovered: 0
# - Checks Executed: 0
# - PASS: 0
# - FAIL: 0
# - ERROR: 0
#
# NOTES:
# # - (Add notes here)
# ============================================================================
