# ============================================================================
# 🤖 CURSOR AI: SERVICE VALIDATION INSTRUCTIONS
# ============================================================================
# 
# SERVICE: dataprotection
# STATUS: ⏳ NOT_VALIDATED
#
# 🎯 YOUR TASK:
# 1. Run engine: export AZURE_ENGINE_FILTER_SERVICES="dataprotection" && python engine/azure_generic_engine.py > /tmp/test_dataprotection.json 2>&1
# 2. Analyze: tail -100 /tmp/test_dataprotection.json
# 3. Fix issues below in discovery/checks sections
# 4. Re-run until: inventory has resources (if exist), checks = PASS/FAIL (not ERROR)
# 5. Mark complete at bottom
#
# ❌ COMMON ISSUES & FIXES:
#
# "Failed to create client" → 
#   Fix sdk_package: azure.mgmt.dataprotection (use DOTS not dashes)
#   Install: pip install azure-mgmt-dataprotection
#
# "Action not found: X.list" →
#   Test in Python:
#     from azure.mgmt.dataprotection import *
#     client = <Client>(cred, sub_id)
#     print(dir(client.<resource>))  # Find correct method
#   Common: list_by_subscription, list_all, list_by_resource_group
#
# "ERROR" in checks →
#   Check resource structure in output
#   Fix field paths: properties.encryption.keySource
#   Fix params: add resource_group_name, account_name, etc.
#
# Empty inventory →
#   Create test resource: az <service> <resource> create ...
#   OR verify action name is correct
#
# ✅ SUCCESS WHEN:
# - No warnings
# - Inventory populated (if resources exist)  
# - Checks = PASS/FAIL (not ERROR)
# - JSON is clean objects (not strings)
#
# 📝 AFTER FIXING:
# Mark status below as VALIDATED and add notes at bottom
#
# ============================================================================

dataprotection:
  version: '1.0'
  provider: azure
  service: dataprotection
  sdk_package: azure.mgmt.dataprotection
  client_class: DataProtectionClient
  api_type: management
  scope: subscription
  discovery:
  - discovery_id: azure.dataprotection.backup_vaults
    calls:
    - action: backup_vaults.list_in_subscription
      fields:
      - path: __self__
  checks:
  - check_id: azure.dataprotection.backup.vault.immutability.check
    title: 'AZURE DATAPROTECTION Backup: Vault Immutability Check'
    severity: medium
    for_each: azure.dataprotection.backup_vaults
    calls:
    - action: self
      fields:
      - path: properties.securitySettings.immutabilitySettings.state
        operator: equals
        expected: Locked
  - check_id: azure.dataprotection.backup.vault.cross.region.restore.enabled
    title: 'AZURE DATAPROTECTION Backup: Vault Cross Region Restore Enabled'
    severity: medium
    for_each: azure.dataprotection.backup_vaults
    calls:
    - action: self
      fields:
      - path: properties.feature_settings.cross_region_restore_state
        operator: equals
        expected: Enabled
  - check_id: azure.dataprotection.backupvault.infrastructure.encryption.enabled
    title: 'AZURE DATAPROTECTION Backupvault: Infrastructure Encryption Enabled'
    severity: high
    for_each: azure.dataprotection.backup_vaults
    calls:
    - action: self
      fields:
      - path: properties.security_settings.encryption_settings.infrastructure_encryption
        operator: equals
        expected: Enabled
  - check_id: azure.dataprotection.backup.vault.soft.delete.compliance.check
    title: 'AZURE DATAPROTECTION Backup: Vault Soft Delete Compliance Check'
    severity: medium
    for_each: azure.dataprotection.backup_vaults
    calls:
    - action: self
      fields:
      - path: properties.securitySettings.softDeleteSettings.state
        operator: equals
        expected: 'On'
      - path: properties.securitySettings.softDeleteSettings.retentionDurationInDays
        operator: greater_than_or_equal
        expected: 14
  - check_id: azure.dataprotection.backup.vault.encryption.cmk.enabled
    title: 'AZURE DATAPROTECTION Backup: Vault Encryption CMK Enabled'
    severity: high
    for_each: azure.dataprotection.backup_vaults
    calls:
    - action: self
      fields:
      - path: properties.securitySettings.encryptionSettings.kekIdentity.identityType
        operator: equals
        expected: UserAssigned
      - path: properties.securitySettings.encryptionSettings.state
        operator: equals
        expected: Enabled


# ============================================================================
# VALIDATION TRACKING
# ============================================================================
# STATUS: ✅ VALIDATED
# VALIDATED_BY: Cursor AI
# VALIDATED_DATE: 2025-12-05
# 
# FIXES APPLIED:
# - Changed package to sdk_package: azure.mgmt.dataprotection (dot format)
# - Added api_type: management
# - Removed client: and save_as fields from discovery calls
# - Added fields: __self__ to discovery calls
# - Fixed all 5 checks: corrected for_each references to use azure.dataprotection.backup_vaults
#
# TEST RESULTS:
# - Engine Status: ✅ NO ERRORS
# - API Connection: ✅ SUCCESS
# - Resources Discovered: 0 (no dataprotection resources in subscription - expected)
#
# NOTES:
# - Engine successfully connects to Azure and makes API calls
# - All discovery actions correctly formatted
# - All checks reference correct discovery_id
# ============================================================================
