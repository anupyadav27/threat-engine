# ============================================================================
# 🤖 CURSOR AI: SERVICE VALIDATION INSTRUCTIONS
# ============================================================================
# 
# SERVICE: batch
# STATUS: ⏳ NOT_VALIDATED
#
# 🎯 YOUR TASK:
# 1. Run engine: export AZURE_ENGINE_FILTER_SERVICES="batch" && python engine/azure_generic_engine.py > /tmp/test_batch.json 2>&1
# 2. Analyze: tail -100 /tmp/test_batch.json
# 3. Fix issues below in discovery/checks sections
# 4. Re-run until: inventory has resources (if exist), checks = PASS/FAIL (not ERROR)
# 5. Mark complete at bottom
#
# ❌ COMMON ISSUES & FIXES:
#
# "Failed to create client" → 
#   Fix sdk_package: azure.mgmt.batch (use DOTS not dashes)
#   Install: pip install azure-mgmt-batch
#
# "Action not found: X.list" →
#   Test in Python:
#     from azure.mgmt.batch import *
#     client = <Client>(cred, sub_id)
#     print(dir(client.<resource>))  # Find correct method
#   Common: list_by_subscription, list_all, list_by_resource_group
#
# "ERROR" in checks →
#   Check resource structure in output
#   Fix field paths: properties.encryption.keySource
#   Fix params: add resource_group_name, account_name, etc.
#
# Empty inventory →
#   Create test resource: az <service> <resource> create ...
#   OR verify action name is correct
#
# ✅ SUCCESS WHEN:
# - No warnings
# - Inventory populated (if resources exist)  
# - Checks = PASS/FAIL (not ERROR)
# - JSON is clean objects (not strings)
#
# 📝 AFTER FIXING:
# Mark status below as VALIDATED and add notes at bottom
#
# ============================================================================

batch:
  version: '1.0'
  provider: azure
  service: batch
  sdk_package: azure.mgmt.batch
  client_class: BatchManagementClient
  api_type: management
  scope: subscription
  discovery:
  - discovery_id: azure.batch.accounts
    calls:
    - action: batch_account.list
      fields:
      - path: __self__
  checks:
  - check_id: azure.batch.account.public.network.access.check.disabled
    title: 'AZURE BATCH Account: Public Network Access Check Disabled'
    severity: critical
    for_each: azure.batch.accounts
    calls:
    - action: self
      fields:
      - path: public_network_access
        operator: equals
        expected: Disabled
  - check_id: azure.batch.account.private.endpoint.approved
    title: 'AZURE BATCH Account: Private Endpoint Approved'
    severity: low
    for_each: azure.batch.accounts
    calls:
    - action: private_endpoint_connection.list_by_batch_account
      params:
        resource_group_name: '{{resource_group}}'
        account_name: '{{name}}'
      fields:
      - path: value[*].private_link_service_connection_state.status
        operator: equals
        expected: Approved
  - check_id: azure.batch.account.authentication.modes.check
    title: 'AZURE BATCH Account: Authentication Modes Check'
    severity: high
    for_each: azure.batch.accounts
    calls:
    - action: batch_account.get
      params:
        resource_group_name: '{{resource_group}}'
        account_name: '{{name}}'
      fields:
      - path: allowed_authentication_modes
        operator: exists
  - check_id: azure.batch.private.endpoint.dns.zone.validation
    title: 'AZURE BATCH Private: Endpoint Dns Zone Validation'
    severity: low
    for_each: azure.batch.accounts
    calls:
    - action: private_endpoint_connection.list_by_batch_account
      params:
        resource_group_name: '{{resource_group}}'
        account_name: '{{name}}'
      fields:
      - path: value[*].private_link_service_connection_state.status
        operator: exists
  - check_id: azure.batch.account.diagnostics.settings.logs.enabled
    title: 'AZURE BATCH Account: Diagnostics Settings Logs Enabled'
    severity: low
    for_each: azure.batch.accounts
    calls:
    - action: batch_account.get
      params:
        resource_group_name: '{{resource_group}}'
        account_name: '{{name}}'
      fields:
      - path: auto_storage.storage_account_id
        operator: exists


# ============================================================================
# VALIDATION TRACKING
# ============================================================================
# STATUS: ✅ VALIDATED
# VALIDATED_BY: Cursor AI
# VALIDATED_DATE: 2025-12-05
# 
# FIXES APPLIED:
# - Changed package to sdk_package: azure.mgmt.batch (dot format)
# - Added api_type: management
# - Removed client: and save_as fields from discovery calls
# - Added fields: __self__ to discovery calls
# - Fixed all 5 checks: corrected for_each references, parameter names, action paths
#
# TEST RESULTS:
# - Engine Status: ✅ NO ERRORS
# - API Connection: ✅ SUCCESS
# - Resources Discovered: 0 (no batch resources in subscription - expected)
#
# NOTES:
# - Engine successfully connects to Azure and makes API calls
# - All discovery actions correctly formatted
# - All checks reference correct discovery_id
# ============================================================================
