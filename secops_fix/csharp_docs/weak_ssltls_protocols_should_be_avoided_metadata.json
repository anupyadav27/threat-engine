{
  "filename": "weak_ssltls_protocols_should_be_avoided_metadata.json",
  "rule_id": "weak_ssltls_protocols_avoided",
  "title": "Weak SSL/TLS protocols should be avoided",
  "description": "Encryption algorithms are essential for protecting sensitive information and ensuring secure communications in a variety of domains. They are used for several important reasons: Confidentiality, privacy, and intellectual property protection Security during transmission or on storage devices Data integrity, general trust, and authentication When selecting encryption algorithms, tools, or combinations, you should also consider two things: No encryption is unbreakable. The strength of an encryption algorithm is usually measured by the effort required to crack it within a reasonable time frame.",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Security",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Security risk",
  "tags": [
    "security",
    "bug",
    ".net"
  ],
  "examples": {
    "noncompliant": [
      "// Noncompliant - Using deprecated SSL3 protocol\nServicePointManager.SecurityProtocol = SecurityProtocolType.Ssl3;\n\n// Noncompliant - Using deprecated TLS 1.0\nServicePointManager.SecurityProtocol = SecurityProtocolType.Tls;\n\n// Noncompliant - Using deprecated TLS 1.1\nServicePointManager.SecurityProtocol = SecurityProtocolType.Tls11;"
    ],
    "compliant": [
      "// Compliant - Using TLS 1.2 or higher\nServicePointManager.SecurityProtocol = SecurityProtocolType.Tls12;\n\n// Compliant - Using TLS 1.3\nServicePointManager.SecurityProtocol = SecurityProtocolType.Tls13;\n\n// Compliant - Using system default (recommended)\nServicePointManager.SecurityProtocol = SecurityProtocolType.SystemDefault;"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/csharp/RSPEC-4423"
  ],
  "logic": {
    "node_types": ["Assignment"],
    "checks": [
      {
        "type": "regex_match",
        "property_path": ["source"],
        "pattern": "SecurityProtocol\\s*=\\s*SecurityProtocolType\\.(Ssl3|Tls(?!1[23])(?![0-9])|Tls11)\\b",
        "exclude_patterns": [
          "^\\s*//",
          "^\\s*/\\*",
          "Console\\.",
          "Logger\\.",
          "\\$\"",
          "string\\.Format"
        ]
      }
    ],
    "never_trigger_on": [
      "Comments (// or /* */)",
      "Logging statements (Console.*, Logger.*)",
      "String formatting ($\"{}\", string.Format)",
      "Unit tests ([Test], [Fact], Assert.*)",
      "TLS 1.2 or higher (Tls12, Tls13)",
      "SystemDefault protocol"
    ],
    "reasoning": "Detects assignment of deprecated/weak SSL/TLS protocols (Ssl3, Tls, Tls11) to SecurityProtocol. These protocols have known vulnerabilities. TLS 1.2+ or SystemDefault should be used instead.",
    "validation_examples": {
      "should_trigger": [
        "ServicePointManager.SecurityProtocol = SecurityProtocolType.Ssl3;",
        "ServicePointManager.SecurityProtocol = SecurityProtocolType.Tls;",
        "ServicePointManager.SecurityProtocol = SecurityProtocolType.Tls11;"
      ],
      "should_not_trigger": [
        "ServicePointManager.SecurityProtocol = SecurityProtocolType.Tls12;",
        "ServicePointManager.SecurityProtocol = SecurityProtocolType.Tls13;",
        "ServicePointManager.SecurityProtocol = SecurityProtocolType.SystemDefault;",
        "// ServicePointManager.SecurityProtocol = SecurityProtocolType.Ssl3;",
        "Console.WriteLine(SecurityProtocolType.Ssl3.ToString());"
      ]
    }
  }
}