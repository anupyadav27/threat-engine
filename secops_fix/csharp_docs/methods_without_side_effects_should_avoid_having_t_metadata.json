{
  "filename": "methods_without_side_effects_should_avoid_having_t_metadata.json",
  "rule_id": "methods_without_side_effects",
  "title": "Methods without side effects should avoid having their return values ignored",
  "description": "When you do not use the return value of a method with no side effects, it indicates that something is wrong. Either this method is unnecessary, or the source code does not behave as expected and could lead to code defects. For example, there are methods, such as DateTime.AddYears, that don’t change the value of the input object, but instead, they return a new object whose value is the result of this operation, and as a result that you will have unexpected effects if you do not use the return value.",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Security",
  "recommendation": "Replace with the recommended alternative approach.",
  "impact": "Security risk",
  "tags": [
    "security",
    "bug",
    ".net"
  ],
  "examples": {
    "noncompliant": [
      "public void ProcessString(string text)\n{\n    text.ToUpper(); // Noncompliant: Return value is ignored\n    text.Trim(); // Noncompliant: Return value is ignored\n    \n    var list = new List<int> { 1, 2, 3 };\n    list.Where(x => x > 1); // Noncompliant: LINQ result ignored\n}"
    ],
    "compliant": [
      "public void ProcessString(string text)\n{\n    string upper = text.ToUpper(); // Compliant: Return value is used\n    string trimmed = text.Trim(); // Compliant: Return value is used\n    \n    var list = new List<int> { 1, 2, 3 };\n    var filtered = list.Where(x => x > 1); // Compliant: LINQ result is captured\n}"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/csharp/RSPEC-2201"
  ],
  "logic": {
    "node_types": ["CallExpression"],
    "checks": [
      {
        "type": "regex_match",
        "property_path": ["source"],
        "pattern": "\\b(\\w+)\\.(ToUpper|ToLower|Trim|Replace|Substring|PadLeft|PadRight|Insert|Remove|ToString|Where|Select|OrderBy|GroupBy|Take|Skip|First|Last|Single|AddDays|AddHours|AddMinutes|AddSeconds|AddYears|AddMonths)\\s*\\([^)]*\\)\\s*;",
        "exclude_patterns": [
          "Console\\.",
          "Debug\\.",
          "Logger\\.",
          "Log\\.",
          "\\$\"",
          "string\\.Format",
          "\\[Test\\]",
          "Assert\\.",
          "^\\s*return\\s+",
          "\\s+=\\s*\\w+\\."
        ]
      }
    ],
    "never_trigger_on": [
      "Logging statements (Console.*, Debug.*, Logger.*)",
      "String formatting ($\"\", string.Format)",
      "Unit tests ([Test], Assert.*)",
      "Assignment operations (= operator)",
      "Method parameters and return statements",
      "Void methods (no return value to ignore)"
    ],
    "reasoning": "Detects method calls where return values are ignored for methods that don't modify the original object (immutable operations). Focuses on string operations, LINQ methods, and DateTime operations that return new instances. Excludes logging, formatting, tests, and contexts where the return value is actually used.",
    "validation_examples": {
      "should_trigger": [
        "text.ToUpper();",
        "text.Trim();",
        "list.Where(x => x > 1);",
        "date.AddDays(1);"
      ],
      "should_not_trigger": [
        "var result = text.ToUpper();",
        "Console.WriteLine(text.ToUpper());",
        "return text.Trim();",
        "list.Add(item); // void method"
      ]
    }
  }
}