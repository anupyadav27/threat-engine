{
  "filename": "anonymous_inner_classes_to_lambdas_metadata.json",
  "rule_id": "java_refactor_anonymous_inner_classes",
  "title": "Refactor Anonymous Inner Classes to Lambdas",
  "description": "Use lambdas instead of anonymous inner classes for better readability and performance.",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Code Quality",
  "recommendation": "Replace anonymous inner classes with lambdas",
  "impact": "Improves readability and maintainability",
  "examples": {
    "noncompliant": [
      "myCollection.stream().map(new Function<String,String>() { // Noncompliant, use a lambda expression instead\n  @Override\n  public String apply(String input) {\n    return new StringBuilder(input).reverse().toString();\n  }\n})"
    ],
    "compliant": [
      "myCollection.stream().map(input -> new StringBuilder(input).reverse().toString()) // Compliant"
    ]
  },
  "references": [],
  "logic": {
    "node_types": [
      "Expression"
    ],
    "checks": [
      {
        "type": "custom_function",
        "function": "custom_java_refactor_anonymous_inner_classes"
      }
    ]
  }
}