{
  "filename": "write_object_synchronized_metadata.json",
  "rule_id": "vulnerability_avoid_single_synchronized_write_object",
  "title": "Avoid having 'writeObject' as the only 'synchronized' method in a Java class",
  "description": "Synchronization is a mechanism used when multithreading in Java to ensure that only one thread executes a given block of code at a time. This is done to avoid bugs that can occur when multiple threads share a given state and try to manipulate simultaneously.\n\nObject serialization is not thread-safe by default. In a multithreaded environment, one option is to mark writeObject with synchronized to improve thread safety. It is highly suspicious, however, if writeObject is the only synchronized method in a class. It may indicate that serialization is not required, as multithreading is not used. Alternatively, it could also suggest that other methods in the same class have been forgotten to be made thread-safe.",
  "status": "ready",
  "defaultSeverity": "Major",
  "category": "Vulnerability",
  "recommendation": "Ensure that other methods requiring thread safety are also synchronized or use alternative thread-safe methods.",
  "impact": "Potential data inconsistency or race conditions in multithreaded environments.",
  "examples": {
    "noncompliant": [],
    "compliant": []
  },
  "references": [],
  "logic": {
    "node_types": [
      "Class"
    ],
    "checks": [
      {
        "type": "regex_match",
        "property_path": [
          "source"
        ],
        "patterns": [
          "synchronized\\s+void\\s+writeObject\\s*\\(.*\\)",
          "^(?:(?!synchronized\\s+void\\s+writeObject).)*synchronized\\s+void\\s+\\w+\\s*\\(.*\\)"
        ],
        "message": "Ensure that other methods requiring thread safety are also synchronized or use alternative thread-safe methods."
      }
    ]
  }
}