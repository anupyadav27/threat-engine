{
  "filename": "gitlab_oauth_application_tokens_should_not_be_disc_metadata.json",
  "rule_id": "gitlab_oauth_application_tokens",
  "title": "GitLab OAuth application tokens should not be disclosed",
  "description": "In most cases, trust boundaries are violated when a secret is exposed in a source code repository or an uncontrolled deployment environment. Unintended people who don\u2019t need to know the secret might get access to it. They might then be able to use it to gain unwanted access to associated services or resources.",
  "status": "ready",
  "defaultSeverity": "Info",
  "category": "Security",
  "recommendation": "Review and fix according to the rule guidelines.",
  "impact": "Security risk",
  "tags": [
    "security",
    "cwe"
  ],
  "examples": {
    "noncompliant": [],
    "compliant": [
      "client_secret = os.environ.get('GITLAB_OAUTH_SECRET')",
      "client_secret = config['gitlab']['oauth_secret']"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/secrets/RSPEC-7268"
  ],
  "logic": {
    "node_types": [],
    "checks": [
      {
        "type": "regex",
        "pattern": "gloas-[0-9a-zA-Z_\\-]{64}",
        "message": "A GitLab OAuth application secret (client secret) is exposed in the source code. Tokens prefixed with 'gloas-' are GitLab OAuth application secrets that grant access to any application using GitLab as an OAuth 2.0 authentication provider. Exposing this secret allows attackers to impersonate the application, manipulate OAuth flows, and gain unauthorized access to GitLab resources on behalf of users. Store the secret in an environment variable or secrets manager and retrieve it at runtime.",
        "context_keywords": []
      }
    ]
  }
}