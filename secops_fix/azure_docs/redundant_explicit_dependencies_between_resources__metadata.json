{
  "filename": "redundant_explicit_dependencies_between_resources__metadata.json",
  "rule_id": "redundant_explicit_dependencies_between",
  "title": "Redundant explicit dependencies between resources should be removed",
  "description": "Redundant explicit dependencies between resources should be removedAnalyze your codeconsistency-conventionalmaintainabilityreliabilitysecurityCode SmellIn Azure Resource Manager (ARM) templates, dependencies between resources can be defined in two ways: implicitly or explicitly. We recommend not adding explicit dependencies when they are already defined implicitly to avoid redundancy and make the template more straightforward to read.Why is this an issue?How can I fix it?More InfoIn Azure Resource Manager (ARM) templates, dependencies between resources can be defined in two ways: implicitly or explicitly. An implicit dependency is set when you use thereferencefunction and pass in the resource name.",
  "status": "ready",
  "defaultSeverity": "Info",
  "category": "Security",
  "recommendation": "Remove the problematic code and refactor as needed.",
  "impact": "Security risk",
  "tags": [
    "security",
    "convention"
  ],
  "examples": {
    "noncompliant": [
      "reference"
    ],
    "compliant": [
      "dependsOn"
    ]
  },
  "references": [
    "https://rules.sonarsource.com/azureresourcemanager/RSPEC-6952"
  ],
  "logic": {
    "node_types": ["ResourceNode"],
    "checks": [
      {
        "type": "custom_function",
        "function": "check_redundant_explicit_dependencies",
        "description": "Check for redundant explicit dependencies when implicit dependencies already exist"
      }
    ]
  }
}