apiVersion: v1
kind: Service
metadata:
  name: admin-backend-service
  namespace: cspm
  labels:
    app: admin-backend
spec:
  type: LoadBalancer
  ports:
  - port: 8001
    targetPort: 8001
    protocol: TCP
    name: http
  selector:
    app: admin-backend
