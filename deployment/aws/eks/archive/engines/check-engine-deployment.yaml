apiVersion: apps/v1
kind: Deployment
metadata:
  name: engine-check-aws
  namespace: threat-engine-engines
  labels:
    app: engine-check-aws
    tier: engine
    engine-type: check
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  selector:
    matchLabels:
      app: engine-check-aws
  template:
    metadata:
      labels:
        app: engine-check-aws
        tier: engine
    spec:
      serviceAccountName: aws-compliance-engine-sa  # Has IRSA IAM role for AWS API access
      containers:
      # Main engine container
      - name: check-engine
        image: yadavanup84/engine-check-aws:latest
        ports:
        - containerPort: 8002
          name: http
        envFrom:
        - configMapRef:
            name: threat-engine-db-config
        - secretRef:
            name: threat-engine-db-passwords
        env:
        - name: PYTHONPATH
          value: /app
        - name: LOG_LEVEL
          value: "INFO"
        - name: S3_BUCKET
          valueFrom:
            configMapKeyRef:
              name: s3-mount-config
              key: s3-bucket
        - name: AWS_REGION
          valueFrom:
            configMapKeyRef:
              name: s3-mount-config
              key: s3-region
        - name: OUTPUT_DIR
          value: "/output"
        - name: INPUT_DIR
          value: "/input"
        - name: ENGINE_NAME
          value: "check-aws"
        volumeMounts:
        - name: output
          mountPath: /output
        - name: input
          mountPath: /input
        resources:
          requests:
            memory: "128Mi"
            cpu: "50m"
          limits:
            memory: "512Mi"
            cpu: "250m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 45
          periodSeconds: 15
          failureThreshold: 5
        readinessProbe:
          httpGet:
            path: /api/v1/health/ready
            port: 8002
          initialDelaySeconds: 15
          periodSeconds: 10
          failureThreshold: 3
      
      # S3 sync sidecar - syncs output to S3 and downloads input from S3
      - name: s3-sync
        image: amazon/aws-cli:latest
        command: ["/bin/sh", "-c"]
        args:
          - |
            echo "S3 Sync Sidecar starting..."
            echo "Bucket: s3://${S3_BUCKET}"
            echo "Input: s3://${S3_BUCKET}/engine_input/${ENGINE_NAME}/ -> /input"
            echo "Output: /output -> s3://${S3_BUCKET}/engine_output/${ENGINE_NAME}/"
            
            # Initial sync of input from S3
            if aws s3 ls "s3://${S3_BUCKET}/engine_input/${ENGINE_NAME}/" 2>/dev/null; then
              echo "Syncing input from S3..."
              aws s3 sync "s3://${S3_BUCKET}/engine_input/${ENGINE_NAME}/" /input \
                --region ${AWS_REGION}
            else
              echo "No input data in S3 yet"
            fi
            
            # Continuous sync loop
            while true; do
              # Sync output TO S3 (every 30s)
              if [ -d "/output" ] && [ "$(ls -A /output 2>/dev/null)" ]; then
                echo "$(date): Syncing output to S3..."
                aws s3 sync /output "s3://${S3_BUCKET}/engine_output/${ENGINE_NAME}/" \
                  --region ${AWS_REGION} \
                  --exclude "*.tmp" \
                  --exclude "*.lock" \
                  --delete || echo "Sync failed, retrying..."
              fi
              
              # Sync input FROM S3 (every 5 minutes for rule updates)
              if [ $((SECONDS % 300)) -eq 0 ]; then
                aws s3 sync "s3://${S3_BUCKET}/engine_input/${ENGINE_NAME}/" /input \
                  --region ${AWS_REGION} 2>/dev/null || true
              fi
              
              sleep 30
            done
        env:
        - name: S3_BUCKET
          valueFrom:
            configMapKeyRef:
              name: s3-mount-config
              key: s3-bucket
        - name: AWS_REGION
          valueFrom:
            configMapKeyRef:
              name: s3-mount-config
              key: s3-region
        - name: ENGINE_NAME
          value: "check-aws"
        volumeMounts:
        - name: output
          mountPath: /output
        - name: input
          mountPath: /input
        resources:
          requests:
            memory: "64Mi"
            cpu: "25m"
          limits:
            memory: "128Mi"
            cpu: "100m"
      
      volumes:
      - name: output
        emptyDir: {}
      - name: input
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: engine-check-aws
  namespace: threat-engine-engines
  labels:
    app: engine-check-aws
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8002
    protocol: TCP
    name: http
  selector:
    app: engine-check-aws
