apiVersion: apps/v1
kind: Deployment
metadata:
  name: engine-dbsec
  namespace: threat-engine-engines
  labels:
    app: engine-dbsec
    tier: engine
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  selector:
    matchLabels:
      app: engine-dbsec
  template:
    metadata:
      labels:
        app: engine-dbsec
        tier: engine
    spec:
      serviceAccountName: engine-sa
      containers:
      - name: engine-dbsec
        image: yadavanup84/engine-dbsec:v-std-cols
        imagePullPolicy: Always
        ports:
        - containerPort: 8007
          name: http
        envFrom:
        - configMapRef:
            name: threat-engine-db-config
        - secretRef:
            name: threat-engine-db-passwords
        env:
        - name: PYTHONPATH
          value: /app
        - name: LOG_LEVEL
          value: "INFO"
        # ── Database Security DB ────────────────────────────────────────────
        - name: DBSEC_DB_HOST
          value: "postgres-vulnerability-db.cbm92xowvx2t.ap-south-1.rds.amazonaws.com"
        - name: DBSEC_DB_PORT
          value: "5432"
        - name: DBSEC_DB_USER
          value: "postgres"
        - name: DBSEC_DB_NAME
          value: "threat_engine_dbsec"
        - name: DBSEC_DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: threat-engine-db-passwords
              key: DBSEC_DB_PASSWORD
              optional: true
        # ── Discoveries DB ──────────────────────────────────────────────────
        - name: DISCOVERIES_DB_HOST
          value: "postgres-vulnerability-db.cbm92xowvx2t.ap-south-1.rds.amazonaws.com"
        - name: DISCOVERIES_DB_PORT
          value: "5432"
        - name: DISCOVERIES_DB_USER
          value: "postgres"
        - name: DISCOVERIES_DB_NAME
          value: "threat_engine_discoveries"
        - name: DISCOVERIES_DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: threat-engine-db-passwords
              key: DISCOVERIES_DB_PASSWORD
              optional: true
        # ── Check DB ────────────────────────────────────────────────────────
        - name: CHECK_DB_HOST
          value: "postgres-vulnerability-db.cbm92xowvx2t.ap-south-1.rds.amazonaws.com"
        - name: CHECK_DB_PORT
          value: "5432"
        - name: CHECK_DB_USER
          value: "postgres"
        - name: CHECK_DB_NAME
          value: "threat_engine_check"
        - name: CHECK_DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: threat-engine-db-passwords
              key: CHECK_DB_PASSWORD
              optional: true
        # ── DataSec DB ──────────────────────────────────────────────────────
        - name: DATASEC_DB_HOST
          value: "postgres-vulnerability-db.cbm92xowvx2t.ap-south-1.rds.amazonaws.com"
        - name: DATASEC_DB_PORT
          value: "5432"
        - name: DATASEC_DB_USER
          value: "postgres"
        - name: DATASEC_DB_NAME
          value: "threat_engine_datasec"
        - name: DATASEC_DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: threat-engine-db-passwords
              key: DATASEC_DB_PASSWORD
              optional: true
        # ── Onboarding DB (for orchestration queries) ───────────────────────
        - name: ONBOARDING_DB_HOST
          value: "postgres-vulnerability-db.cbm92xowvx2t.ap-south-1.rds.amazonaws.com"
        - name: ONBOARDING_DB_PORT
          value: "5432"
        - name: ONBOARDING_DB_USER
          value: "postgres"
        - name: ONBOARDING_DB_NAME
          value: "threat_engine_onboarding"
        - name: ONBOARDING_DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: threat-engine-db-passwords
              key: ONBOARDING_DB_PASSWORD
              optional: true
        # ── Scanner Job config ──────────────────────────────────────────────
        - name: DBSEC_SCANNER_IMAGE
          value: "yadavanup84/engine-dbsec:v-std-cols"
        - name: SCANNER_CPU_REQUEST
          value: "100m"
        - name: SCANNER_MEM_REQUEST
          value: "512Mi"
        - name: SCANNER_CPU_LIMIT
          value: "250m"
        - name: SCANNER_MEM_LIMIT
          value: "1Gi"
        - name: SCANNER_NAMESPACE
          value: "threat-engine-engines"
        - name: SCANNER_SERVICE_ACCOUNT
          value: "engine-sa"
        # ── OpenTelemetry ───────────────────────────────────────────────────
        - name: OTEL_SERVICE_NAME
          value: "engine-dbsec"
        - name: OTEL_ENABLED
          valueFrom:
            configMapKeyRef:
              name: otel-config
              key: OTEL_ENABLED
              optional: true
        - name: OTEL_EXPORTER_OTLP_ENDPOINT
          valueFrom:
            configMapKeyRef:
              name: otel-config
              key: OTEL_EXPORTER_OTLP_ENDPOINT
              optional: true
        - name: OTEL_EXPORTER_OTLP_INSECURE
          valueFrom:
            configMapKeyRef:
              name: otel-config
              key: OTEL_EXPORTER_OTLP_INSECURE
              optional: true
        - name: OTEL_TRACES_SAMPLER
          valueFrom:
            configMapKeyRef:
              name: otel-config
              key: OTEL_TRACES_SAMPLER
              optional: true
        - name: OTEL_PROPAGATORS
          valueFrom:
            configMapKeyRef:
              name: otel-config
              key: OTEL_PROPAGATORS
              optional: true
        - name: S3_BUCKET
          valueFrom:
            configMapKeyRef:
              name: s3-mount-config
              key: s3-bucket
        - name: AWS_REGION
          valueFrom:
            configMapKeyRef:
              name: s3-mount-config
              key: s3-region
        - name: OUTPUT_DIR
          value: "/output"
        - name: ENGINE_NAME
          value: "database-security"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "250m"
        livenessProbe:
          httpGet:
            path: /api/v1/health/live
            port: 8007
          initialDelaySeconds: 30
          periodSeconds: 10
          failureThreshold: 6
        readinessProbe:
          httpGet:
            path: /api/v1/health/ready
            port: 8007
          initialDelaySeconds: 10
          periodSeconds: 5
          failureThreshold: 6

      volumes:
      - name: output
        emptyDir: {}
      - name: input
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: engine-dbsec
  namespace: threat-engine-engines
  labels:
    app: engine-dbsec
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8007
    protocol: TCP
    name: http
  selector:
    app: engine-dbsec
