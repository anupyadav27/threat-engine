apiVersion: v1
kind: Secret
metadata:
  name: s3-credentials
  namespace: threat-engine-engines
type: Opaque
stringData:
  # Note: In production, use IRSA (IAM Roles for Service Accounts) instead
  # For now, these can be empty if using IRSA
  aws-access-key-id: ""
  aws-secret-access-key: ""
  # If using IRSA, these will be automatically provided by AWS

