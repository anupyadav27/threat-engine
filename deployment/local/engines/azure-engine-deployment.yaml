apiVersion: apps/v1
kind: Deployment
metadata:
  name: azure-compliance-engine
  namespace: threat-engine-engines
  labels:
    app: azure-compliance-engine
    tier: engine
spec:
  replicas: 1
  selector:
    matchLabels:
      app: azure-compliance-engine
  template:
    metadata:
      labels:
        app: azure-compliance-engine
        tier: engine
    spec:
      containers:
      - name: azure-engine
        image: yadavanup84/threat-engine-azure-compliance:latest
        ports:
        - containerPort: 8000
          name: http
        env:
        - name: PYTHONPATH
          value: /app
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /api/v1/health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: azure-compliance-engine
  namespace: threat-engine-engines
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
  selector:
    app: azure-compliance-engine

