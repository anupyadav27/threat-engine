queue:
  version: '1.0'
  provider: oci
  service: queue
  scope: regional
  discovery: []
  checks:
  - check_id: oci.queue.queue.queue_auth_required
    title: 'OCI QUEUE Queue: Queue Auth Required'
    severity: low
    for_each: list_queues
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.queue.queue.queue_cross_account_send_receive_restricted
    title: 'OCI QUEUE Queue: Queue Cross Account Send Receive Restricted'
    severity: low
    for_each: list_queues
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.queue.queue.queue_encryption_enabled
    title: 'OCI QUEUE Queue: Queue Encryption Enabled'
    severity: high
    for_each: list_queues
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.queue.queue.queue_private_network_only_where_supported_enforced
    title: 'OCI QUEUE Queue: Queue Private Network Only Where Supported Enforced'
    severity: medium
    for_each: list_queues
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.queue.queue.queues_public_access_blocked
    title: 'OCI QUEUE Queue: Queues Public Access Blocked'
    severity: critical
    for_each: list_queues
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.queue.queue.queues_server_side_encryption_enabled
    title: 'OCI QUEUE Queue: Queues Server Side Encryption Enabled'
    severity: high
    for_each: list_queues
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
