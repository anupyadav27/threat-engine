resource_manager:
  version: '1.0'
  provider: oci
  service: resource_manager
  scope: regional
  discovery: []
  checks:
  - check_id: oci.resource_manager.stack.kubelet_config_ownership_restricted
    title: 'OCI RESOURCE_MANAGER Stack: Kubelet Config Ownership Restricted'
    severity: low
    for_each: list_stacks
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.resource_manager.stack.kubelet_config_permissions_restricted
    title: 'OCI RESOURCE_MANAGER Stack: Kubelet Config Permissions Restricted'
    severity: low
    for_each: list_stacks
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.resource_manager.stack.documentation_access_rbac_least_privilege_required
    title: 'OCI RESOURCE_MANAGER Stack: Documentation Access RBAC Least Privilege Required'
    severity: high
    for_each: list_stacks
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.resource_manager.stack.documentation_storage_encrypted_and_private
    title: 'OCI RESOURCE_MANAGER Stack: Documentation Storage Encrypted And Private'
    severity: medium
    for_each: list_stacks
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.resource_manager.stack.automation_change_audit_logging_enabled
    title: 'OCI RESOURCE_MANAGER Stack: Automation Change Audit Logging Enabled'
    severity: medium
    for_each: list_stacks
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.resource_manager.stack.automation_execution_roles_least_privilege_required
    title: 'OCI RESOURCE_MANAGER Stack: Automation Execution Roles Least Privilege Required'
    severity: high
    for_each: list_stacks
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.resource_manager.stack.runbook_access_rbac_least_privilege_required
    title: 'OCI RESOURCE_MANAGER Stack: Runbook Access RBAC Least Privilege Required'
    severity: high
    for_each: list_stacks
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.resource_manager.stack.runbook_change_audit_logging_enabled
    title: 'OCI RESOURCE_MANAGER Stack: Runbook Change Audit Logging Enabled'
    severity: medium
    for_each: list_stacks
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.resource_manager.stack.runbook_storage_encrypted_and_private
    title: 'OCI RESOURCE_MANAGER Stack: Runbook Storage Encrypted And Private'
    severity: medium
    for_each: list_stacks
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
  - check_id: oci.resource_manager.stack.portfolio_shared_within_organization_only
    title: 'OCI RESOURCE_MANAGER Stack: Portfolio Shared Within Organization Only'
    severity: low
    for_each: list_stacks
    logic: AND
    calls:
    - action: eval
      fields:
      - path: lifecycle_state
        operator: equals
        expected: ACTIVE
        description: Verify resource is in active state (placeholder check)
