glacier:
  scope: regional
  discovery: []
  checks:
  - check_id: glacier_client
    _todo: 'Manual review: add for_each, param, calls'
  - check_id: glacier_service
    _todo: 'Manual review: add for_each, param, calls'
  - check_id: glacier_vaults_policy_public_access
    _todo: 'Manual review: add for_each, param, calls'
