dlm:
  scope: regional
  discovery: []
  checks:
  - check_id: dlm_client
    _todo: 'Manual review: add for_each, param, calls'
  - check_id: dlm_service
    _todo: 'Manual review: add for_each, param, calls'
  - check_id: dlm_ebs_snapshot_lifecycle_policy_exists
    _todo: 'Manual review: add for_each, param, calls'
