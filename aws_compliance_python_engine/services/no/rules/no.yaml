# WARNING: 'no' is not a valid standalone AWS service
# This service should be removed or merged with the correct service
# Generated: 2025-11-25 12:07:29
# Status: INVALID

version: '1.0'
provider: aws
service: 'no'
discovery:
- discovery_id: aws.no.checkss
  calls:
  - client: 'no'
    action: list_nos
    save_as: checks_list
    on_error: continue
    fields:
    - Nos
  emit:
    items_for: checks_list[]
    as: checks
    item:
      id: '{{ checks.Id }}'
      name: '{{ checks.Name }}'
checks:
- title: 'NO checks: Security Checks Configured'
  severity: medium
  rule_id: aws.no.checks.security_checks_configured
  for_each:
    discovery: aws.no.checkss
    as: checks
    item: checks
  conditions:
    var: checks.compliant
    op: equals
    value: true
  remediation: 'Configure no checks for compliance:


    Requirement: Security Checks Configuration


    Description: Verifies security configuration for AWS NO checks to ensure alignment with AWS security best practices and
    compliance requirements. Proper configuration reduces security risks, maintains defense in depth, and supports overall
    security posture.


    Steps:

    1. Open NO console

    2. Select the checks

    3. Review security configuration

    4. Apply required settings per organizational policy

    5. Verify compliance

    6. Save changes


    General Security Recommendations:

    • Follow AWS Well-Architected Framework

    • Implement defense in depth

    • Enable logging and monitoring

    • Use encryption where applicable

    • Regular security audits'
  references:
  - https://docs.aws.amazon.com/wellarchitected/latest/security-pillar/welcome.html
