memorydb:
  scope: regional
  discovery: []
  checks:
  - check_id: memorydb_client
    _todo: 'Manual review: add for_each, param, calls'
  - check_id: memorydb_service
    _todo: 'Manual review: add for_each, param, calls'
  - check_id: memorydb_cluster_auto_minor_version_upgrades
    _todo: 'Manual review: add for_each, param, calls'
