# APPSTREAM Service - Check Metadata
# This file contains titles, descriptions, and severity for all checks
# The main YAML file only contains the logic

version: '1.0'
service: appstream
checks:
  aws.appstream.resource.fleet_default_internet_access_disabled:
    title: 'APPSTREAM resource: Fleet Default Internet Access Disabled'
    severity: medium
