# TRANSFER Service - Check Metadata
# This file contains titles, descriptions, and severity for all checks
# The main YAML file only contains the logic

version: '1.0'
service: transfer
checks:
  aws.transfer.resource.server_in_transit_encryption_enabled:
    title: 'TRANSFER resource: Server In Transit Encryption Enabled'
    severity: high
