# EDR Service - Check Metadata
# This file contains titles, descriptions, and severity for all checks
# The main YAML file only contains the logic

version: '1.0'
service: edr
checks:
  aws.edr.resource.edr_operational_readiness_configured:
    title: 'EDR resource: Edr Operational Readiness Configured'
    severity: medium
