# CODEARTIFACT Service - Check Metadata
# This file contains titles, descriptions, and severity for all checks
# The main YAML file only contains the logic

version: '1.0'
service: codeartifact
checks:
  aws.codeartifact.resource.packages_external_public_publishing_disabled:
    title: 'CODEARTIFACT resource: Packages External Public Publishing Disabled'
    severity: high
