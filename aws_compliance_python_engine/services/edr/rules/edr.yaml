# WARNING: 'edr' is not a valid standalone AWS service
# This service should be removed or merged with the correct service
# Generated: 2025-11-25 12:07:29
# Status: INVALID

version: '1.0'
provider: aws
service: edr
discovery:
- discovery_id: aws.edr.resources
  calls:
  - client: edr
    action: list_edrs
    save_as: resource_list
    on_error: continue
    fields:
    - Edrs
  emit:
    items_for: resource_list[]
    as: resource
    item:
      id: '{{ resource.Id }}'
      name: '{{ resource.Name }}'
checks:
- title: 'EDR resource: Edr Operational Readiness Configured'
  severity: medium
  rule_id: aws.edr.resource.edr_operational_readiness_configured
  for_each:
    discovery: aws.edr.resources
    as: resource
    item: resource
  conditions:
    var: resource.compliant
    op: equals
    value: true
  remediation: 'Configure edr resource for compliance:


    Requirement: Edr Operational Readiness Configuration


    Description: Verifies security configuration for AWS EDR resource to ensure alignment with AWS security best practices
    and compliance requirements. Proper configuration reduces security risks, maintains defense in depth, and supports overall
    security posture.


    Steps:

    1. Open EDR console

    2. Select the resource

    3. Review security configuration

    4. Apply required settings per organizational policy

    5. Verify compliance

    6. Save changes


    General Security Recommendations:

    • Follow AWS Well-Architected Framework

    • Implement defense in depth

    • Enable logging and monitoring

    • Use encryption where applicable

    • Regular security audits'
  references:
  - https://docs.aws.amazon.com/wellarchitected/latest/security-pillar/welcome.html
