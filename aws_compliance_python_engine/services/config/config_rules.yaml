config:
  scope: regional
  discovery: []
  checks:
  - check_id: config_service
    _todo: 'Manual review: add for_each, param, calls'
  - check_id: config_client
    _todo: 'Manual review: add for_each, param, calls'
  - check_id: config_recorder_all_regions_enabled
    _todo: 'Manual review: add for_each, param, calls'
  - check_id: config_recorder_using_aws_service_role
    _todo: 'Manual review: add for_each, param, calls'
