firehose:
  scope: regional
  discovery: []
  checks:
  - check_id: firehose_client
    _todo: 'Manual review: add for_each, param, calls'
  - check_id: firehose_service
    _todo: 'Manual review: add for_each, param, calls'
  - check_id: firehose_stream_encrypted_at_rest
    _todo: 'Manual review: add for_each, param, calls'
