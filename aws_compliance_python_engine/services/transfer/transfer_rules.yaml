transfer:
  scope: regional
  discovery: []
  checks:
  - check_id: transfer_service
    _todo: 'Manual review: add for_each, param, calls'
  - check_id: transfer_client
    _todo: 'Manual review: add for_each, param, calls'
  - check_id: transfer_server_in_transit_encryption_enabled
    _todo: 'Manual review: add for_each, param, calls'
