kinesis:
  scope: regional
  discovery: []
  checks:
  - check_id: kinesis_service
    _todo: 'Manual review: add for_each, param, calls'
  - check_id: kinesis_client
    _todo: 'Manual review: add for_each, param, calls'
  - check_id: kinesis_stream_data_retention_period
    _todo: 'Manual review: add for_each, param, calls'
  - check_id: kinesis_stream_encrypted_at_rest
    _todo: 'Manual review: add for_each, param, calls'
