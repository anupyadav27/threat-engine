rule_id: aws.kinesisvideostreams.stream.encryption_in_transit_required
service: kinesisvideostreams
resource: stream
requirement: Encryption in Transit
title: 'KINESISVIDEOSTREAMS stream: Encryption In Transit Required'
scope: kinesisvideostreams.stream.encryption
domain: data_protection_and_privacy
subcategory: encryption_at_rest
rationale: Ensures kinesisvideostreams stream has encryption in transit required properly
  configured for security compliance.
severity: high
assertion_id: data_protection.encryption_rest.kinesisvideostreams_stream_encryption_in_transit_required
source: expert_generated
description: Ensures AWS KINESISVIDEOSTREAMS stream enforces encryption in transit
  using TLS 1.2 or higher protocols. Unencrypted network traffic is vulnerable to
  man-in-the-middle attacks, packet sniffing, and session hijacking, exposing sensitive
  data during transmission.
references:
- https://docs.aws.amazon.com/wellarchitected/latest/security-pillar/welcome.html
remediation: "Enable encryption for kinesisvideostreams stream:\n\n1. Open KINESISVIDEOSTREAMS\
  \ console\n2. Select the stream\n3. Navigate to Properties/Configuration > Encryption\n\
  4. Enable encryption\n5. Choose encryption key:\n   - AWS managed key (default)\n\
  \   - Customer managed key (KMS) for enhanced control\n6. Save changes\n\nAWS CLI:\n\
  aws kinesisvideostreams modify-stream --stream-id <id> --encrypted --kms-key-id\
  \ <key-id>\n\nSecurity Best Practices:\n• Use customer-managed KMS keys for sensitive\
  \ data\n• Enable automatic key rotation\n• Implement proper key access policies\n\
  • Audit key usage regularly"
