emr:
  scope: regional
  discovery: []
  checks:
  - check_id: emr_service
    _todo: 'Manual review: add for_each, param, calls'
  - check_id: emr_client
    _todo: 'Manual review: add for_each, param, calls'
  - check_id: emr_cluster_account_public_block_enabled
    _todo: 'Manual review: add for_each, param, calls'
  - check_id: emr_cluster_master_nodes_no_public_ip
    _todo: 'Manual review: add for_each, param, calls'
  - check_id: emr_cluster_publicly_accesible
    _todo: 'Manual review: add for_each, param, calls'
