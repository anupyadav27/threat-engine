ssmincidents:
  scope: regional
  discovery: []
  checks:
  - check_id: ssmincidents_client
    _todo: 'Manual review: add for_each, param, calls'
  - check_id: ssmincidents_service
    _todo: 'Manual review: add for_each, param, calls'
  - check_id: ssmincidents_enabled_with_plans
    _todo: 'Manual review: add for_each, param, calls'
